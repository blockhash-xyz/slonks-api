# @blockhash/slonks-core

Shared Slonks utilities for blending embeddings, rendering local pixels, parsing
attributes, and computing diff counts.

This package is used by both `@blockhash/slonks-api` and `@blockhash/slonks`.
