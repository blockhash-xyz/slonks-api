global PIXELS: u32 = 576;
global DIMS: u32 = 10;
global PACKED_CHUNKS: u32 = 36;

fn signed_byte(value: u8) -> i32 {
    if value > 127 {
        (value as i32) - 256
    } else {
        value as i32
    }
}

fn predict_pixel_0(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_1(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_2(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_3(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_4(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_5(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_6(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_7(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_8(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -26 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_9(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_10(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_11(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_12(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_13(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_14(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -21 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_15(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_16(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_17(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_18(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_19(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -13 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_20(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -20 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_21(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_22(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_23(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_24(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_25(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_26(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_27(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_28(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_29(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_30(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -19 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_31(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_1 = embedding[0] * 6 + embedding[2] * -9 + embedding[3] * -16 + embedding[4] * 9 + embedding[5] * -3 + embedding[6] * -7 + embedding[7] * -5 + embedding[8] * -2 + embedding[9] * 1;
    let score_2 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_3 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_4 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_17 = embedding[0] * 7 + embedding[1] * -6 + embedding[2] * -11 + embedding[3] * -14 + embedding[4] * 18 + embedding[5] * 12 + embedding[6] * -13 + embedding[7] * -17 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_32(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_1 = embedding[0] * 6 + embedding[2] * -9 + embedding[3] * -16 + embedding[4] * 9 + embedding[5] * -3 + embedding[6] * -7 + embedding[7] * -4 + embedding[8] * -1 + embedding[9] * 1;
    let score_2 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_3 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_4 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_17 = embedding[0] * 7 + embedding[1] * -6 + embedding[2] * -12 + embedding[3] * -14 + embedding[4] * 18 + embedding[5] * 12 + embedding[6] * -13 + embedding[7] * -16 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_33(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_1 = embedding[0] * 3 + embedding[1] * -5 + embedding[2] * -6 + embedding[3] * -15 + embedding[4] * 7 + embedding[5] * -1 + embedding[6] * -3 + embedding[7] * -15 + embedding[8] * -4 + embedding[9] * -2;
    let score_2 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_3 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_4 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_5 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_6 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_7 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_8 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_9 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_10 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_11 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_12 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_13 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_14 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_15 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_16 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_17 = embedding[0] * 9 + embedding[1] * -2 + embedding[2] * -12 + embedding[3] * -14 + embedding[4] * 18 + embedding[5] * 13 + embedding[6] * -16 + embedding[7] * -14 + embedding[8] * 12 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_34(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_1 = embedding[0] * 6 + embedding[2] * -9 + embedding[3] * -15 + embedding[4] * 9 + embedding[5] * -4 + embedding[6] * -7 + embedding[7] * -5 + embedding[8] * -2 + embedding[9] * 1;
    let score_2 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_3 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_4 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_7 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_16 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_17 = embedding[0] * 7 + embedding[1] * -6 + embedding[2] * -11 + embedding[3] * -13 + embedding[4] * 18 + embedding[5] * 11 + embedding[6] * -13 + embedding[7] * -17 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_35(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * 6 + embedding[1] * -5 + embedding[2] * -8 + embedding[3] * -13 + embedding[4] * 9 + embedding[5] * 4 + embedding[6] * -5 + embedding[7] * -16 + embedding[9] * -4;
    let score_2 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 7 + embedding[1] * -1 + embedding[2] * -13 + embedding[3] * -14 + embedding[4] * 17 + embedding[5] * 11 + embedding[6] * -14 + embedding[7] * -13 + embedding[8] * 10 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_36(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_1 = embedding[0] * 6 + embedding[1] * -5 + embedding[2] * -10 + embedding[3] * -11 + embedding[4] * 9 + embedding[5] * 5 + embedding[6] * -4 + embedding[7] * -18 + embedding[8] * 2 + embedding[9] * -5;
    let score_2 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_3 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_4 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_5 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_6 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_7 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_8 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_9 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_10 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_11 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_12 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_13 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_14 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_15 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_16 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_17 = embedding[0] * 7 + embedding[1] * -2 + embedding[2] * -13 + embedding[3] * -15 + embedding[4] * 17 + embedding[5] * 11 + embedding[6] * -13 + embedding[7] * -14 + embedding[8] * 9 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_37(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_1 = embedding[0] * 5 + embedding[1] * -6 + embedding[2] * -8 + embedding[3] * -12 + embedding[4] * 7 + embedding[5] * 2 + embedding[6] * -2 + embedding[7] * -20 + embedding[8] * -2 + embedding[9] * -5;
    let score_2 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_3 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_4 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_5 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_6 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_7 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_8 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_9 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_10 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_11 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_12 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_13 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_14 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_15 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_16 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_17 = embedding[0] * 9 + embedding[1] * -1 + embedding[2] * -13 + embedding[3] * -14 + embedding[4] * 18 + embedding[5] * 13 + embedding[6] * -15 + embedding[7] * -14 + embedding[8] * 13 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_38(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_1 = embedding[0] * 7 + embedding[2] * -9 + embedding[3] * -15 + embedding[4] * 8 + embedding[5] * -3 + embedding[6] * -7 + embedding[7] * -4 + embedding[8] * -2 + embedding[9] * 1;
    let score_2 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_3 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_4 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_17 = embedding[0] * 7 + embedding[1] * -6 + embedding[2] * -11 + embedding[3] * -13 + embedding[4] * 18 + embedding[5] * 12 + embedding[6] * -13 + embedding[7] * -16 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_39(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_1 = embedding[0] * 6 + embedding[2] * -9 + embedding[3] * -15 + embedding[4] * 9 + embedding[5] * -3 + embedding[6] * -7 + embedding[7] * -5 + embedding[8] * -1 + embedding[9] * 1;
    let score_2 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_3 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_4 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_17 = embedding[0] * 7 + embedding[1] * -6 + embedding[2] * -12 + embedding[3] * -13 + embedding[4] * 18 + embedding[5] * 12 + embedding[6] * -13 + embedding[7] * -17 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_40(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_41(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_42(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_43(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_44(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_45(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_46(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_47(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_48(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_49(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_50(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 24 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_51(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -26 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_52(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_53(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_54(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_1 = embedding[0] * 6 + embedding[2] * -9 + embedding[3] * -15 + embedding[4] * 9 + embedding[5] * -3 + embedding[6] * -8 + embedding[7] * -5 + embedding[8] * -1 + embedding[9] * 1;
    let score_2 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_3 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_4 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_17 = embedding[0] * 7 + embedding[1] * -6 + embedding[2] * -11 + embedding[3] * -14 + embedding[4] * 18 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -17 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_55(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_1 = embedding[0] * 17 + embedding[1] * 12 + embedding[2] * -7 + embedding[3] * 12 + embedding[4] * 23 + embedding[5] * -35 + embedding[6] * -2 + embedding[7] * 19 + embedding[8] * -3;
    let score_2 = embedding[0] * 4 + embedding[1] * -3 + embedding[2] * -10 + embedding[3] * -17 + embedding[4] * 5 + embedding[5] * 1 + embedding[6] * -7 + embedding[7] * -8 + embedding[8] * -2;
    let score_3 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_4 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_5 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_6 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_7 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_8 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_9 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_10 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_11 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_12 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_13 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_14 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_15 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_16 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 5;
    let score_17 = embedding[0] * 3 + embedding[1] * -11 + embedding[2] * -11 + embedding[3] * -16 + embedding[4] * 13 + embedding[5] * 18 + embedding[6] * -12 + embedding[7] * -21 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 8;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_56(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_1 = embedding[0] * 3 + embedding[1] * -4 + embedding[2] * -7 + embedding[3] * -15 + embedding[4] * 7 + embedding[5] * -1 + embedding[6] * -4 + embedding[7] * -15 + embedding[8] * -4 + embedding[9] * -2;
    let score_2 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_3 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_4 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_5 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_6 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_7 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_8 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_9 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_10 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_11 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_12 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_13 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_14 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_15 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_16 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_17 = embedding[0] * 9 + embedding[1] * -2 + embedding[2] * -13 + embedding[3] * -14 + embedding[4] * 18 + embedding[5] * 14 + embedding[6] * -16 + embedding[7] * -14 + embedding[8] * 12 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_57(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_1 = embedding[0] * 3 + embedding[1] * -5 + embedding[2] * -6 + embedding[3] * -15 + embedding[4] * 7 + embedding[5] * -1 + embedding[6] * -3 + embedding[7] * -15 + embedding[8] * -5 + embedding[9] * -3;
    let score_2 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_3 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_4 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_5 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_6 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_7 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_8 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_9 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_10 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_11 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_12 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_13 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_14 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_15 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_16 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 11;
    let score_17 = embedding[0] * 9 + embedding[1] * -2 + embedding[2] * -12 + embedding[3] * -14 + embedding[4] * 18 + embedding[5] * 14 + embedding[6] * -16 + embedding[7] * -14 + embedding[8] * 12 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_58(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_1 = embedding[0] * 7 + embedding[1] * -5 + embedding[2] * -8 + embedding[3] * -13 + embedding[4] * 9 + embedding[5] * 4 + embedding[6] * -6 + embedding[7] * -16 + embedding[9] * -4;
    let score_2 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_3 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_4 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_5 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_6 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_7 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_8 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_9 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_10 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_11 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_12 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_13 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_14 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_15 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_16 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_17 = embedding[0] * 7 + embedding[1] * -1 + embedding[2] * -13 + embedding[3] * -14 + embedding[4] * 17 + embedding[5] * 11 + embedding[6] * -14 + embedding[7] * -14 + embedding[8] * 10 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_59(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * 5 + embedding[1] * -3 + embedding[2] * -7 + embedding[3] * -12 + embedding[4] * 4 + embedding[5] * 7 + embedding[6] * -4 + embedding[7] * -17 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * 8 + embedding[1] * -5 + embedding[2] * -4 + embedding[3] * -6 + embedding[4] * 3 + embedding[5] * 7 + embedding[6] * -2 + embedding[7] * -16 + embedding[8] * 1 + embedding[9] * -5;
    let score_3 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 24 + embedding[4] * -24 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 7 + embedding[2] * -11 + embedding[3] * -12 + embedding[4] * 15 + embedding[5] * 9 + embedding[6] * -13 + embedding[7] * -11 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_60(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_1 = embedding[0] * 4 + embedding[1] * -5 + embedding[2] * -6 + embedding[3] * -15 + embedding[4] * 1 + embedding[5] * 11 + embedding[6] * -6 + embedding[7] * -18 + embedding[8] * 4 + embedding[9] * -3;
    let score_2 = embedding[1] * -12 + embedding[4] * -4 + embedding[5] * 3 + embedding[6] * 9 + embedding[7] * -21 + embedding[8] * -7 + embedding[9] * -6;
    let score_3 = embedding[0] * 18 + embedding[1] * 20 + embedding[2] * -7 + embedding[3] * 15 + embedding[4] * 21 + embedding[5] * -40 + embedding[6] * -1 + embedding[7] * 24 + embedding[8] * -1;
    let score_4 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_5 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_6 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_7 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_8 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_9 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_10 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_11 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_12 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_13 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_14 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_15 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_16 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -15 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 6;
    let score_17 = embedding[0] * 5 + embedding[1] * -1 + embedding[2] * -10 + embedding[3] * -15 + embedding[4] * 12 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -13 + embedding[8] * 11 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 7;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_61(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_1 = embedding[0] * 5 + embedding[1] * -6 + embedding[2] * -8 + embedding[3] * -12 + embedding[4] * 7 + embedding[5] * 3 + embedding[6] * -2 + embedding[7] * -20 + embedding[8] * -2 + embedding[9] * -5;
    let score_2 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_3 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_4 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_5 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_6 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_7 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_8 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_9 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_10 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_11 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_12 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_13 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_14 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_15 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_16 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 10;
    let score_17 = embedding[0] * 9 + embedding[1] * -1 + embedding[2] * -13 + embedding[3] * -14 + embedding[4] * 18 + embedding[5] * 14 + embedding[6] * -16 + embedding[7] * -14 + embedding[8] * 13 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_62(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 1 + embedding[1] * -6 + embedding[2] * -9 + embedding[3] * -17 + embedding[4] * 9 + embedding[5] * 3 + embedding[6] * -7 + embedding[7] * -12 + embedding[8] * 1 + embedding[9] * -4;
    let score_2 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 9 + embedding[1] * -2 + embedding[2] * -13 + embedding[3] * -13 + embedding[4] * 17 + embedding[5] * 13 + embedding[6] * -15 + embedding[7] * -15 + embedding[8] * 11 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_63(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_1 = embedding[0] * 6 + embedding[1] * 1 + embedding[2] * -9 + embedding[3] * -15 + embedding[4] * 9 + embedding[5] * -3 + embedding[6] * -8 + embedding[7] * -5 + embedding[8] * -2 + embedding[9] * 1;
    let score_2 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_3 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_4 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_17 = embedding[0] * 7 + embedding[1] * -6 + embedding[2] * -11 + embedding[3] * -13 + embedding[4] * 18 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -17 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_64(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_1 = embedding[0] * 6 + embedding[2] * -9 + embedding[3] * -15 + embedding[4] * 9 + embedding[5] * -3 + embedding[6] * -7 + embedding[7] * -4 + embedding[8] * -2 + embedding[9] * 1;
    let score_2 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_3 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_4 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 10;
    let score_17 = embedding[0] * 7 + embedding[1] * -6 + embedding[2] * -12 + embedding[3] * -13 + embedding[4] * 18 + embedding[5] * 12 + embedding[6] * -13 + embedding[7] * -16 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_65(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_66(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_67(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_68(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_69(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_70(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_71(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_72(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_73(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_74(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_75(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_76(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_77(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -24 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_78(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_1 = embedding[0] * -5 + embedding[1] * -8 + embedding[2] * -4 + embedding[3] * -28 + embedding[4] * 13 + embedding[5] * -6 + embedding[6] * -14 + embedding[7] * 8 + embedding[8] * -8 + embedding[9] * 3;
    let score_2 = embedding[0] * 19 + embedding[1] * 16 + embedding[2] * -10 + embedding[3] * 13 + embedding[4] * 22 + embedding[5] * -31 + embedding[6] * -2 + embedding[7] * 17 + embedding[8] * 2 + embedding[9] * -3;
    let score_3 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_4 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_5 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_6 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_7 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_8 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_9 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_10 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_11 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_12 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_13 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_14 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_15 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_16 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_17 = embedding[0] * 5 + embedding[1] * -6 + embedding[2] * -13 + embedding[3] * -14 + embedding[4] * 11 + embedding[5] * 22 + embedding[6] * -12 + embedding[7] * -24 + embedding[8] * 13 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_79(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_1 = embedding[0] * -1 + embedding[1] * -5 + embedding[2] * -4 + embedding[3] * -24 + embedding[4] * 14 + embedding[5] * -8 + embedding[6] * -9 + embedding[7] * 2 + embedding[8] * -12 + embedding[9] * 2;
    let score_2 = embedding[0] * 18 + embedding[1] * 16 + embedding[2] * -9 + embedding[3] * 11 + embedding[4] * 23 + embedding[5] * -31 + embedding[6] * -3 + embedding[7] * 17 + embedding[8] * 2 + embedding[9] * -2;
    let score_3 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_4 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_5 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_6 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_7 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_8 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_9 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_10 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_11 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_12 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 5;
    let score_14 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_15 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_16 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_17 = embedding[0] * 4 + embedding[1] * -7 + embedding[2] * -13 + embedding[3] * -15 + embedding[4] * 11 + embedding[5] * 21 + embedding[6] * -14 + embedding[7] * -22 + embedding[8] * 14 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_80(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_1 = embedding[0] * -1 + embedding[1] * -5 + embedding[2] * 1 + embedding[3] * -23 + embedding[4] * -3 + embedding[5] * 9 + embedding[6] * -6 + embedding[7] * -17 + embedding[9] * 2;
    let score_2 = embedding[0] * 18 + embedding[1] * 15 + embedding[2] * -9 + embedding[3] * 15 + embedding[4] * 24 + embedding[5] * -37 + embedding[6] * -2 + embedding[7] * 20 + embedding[8] * -2 + embedding[9] * -2;
    let score_3 = embedding[0] * 3 + embedding[1] * -4 + embedding[2] * -14 + embedding[3] * 8 + embedding[4] * 16 + embedding[5] * -27 + embedding[6] * 3 + embedding[7] * -5 + embedding[8] * -15 + embedding[9] * -4;
    let score_4 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_5 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_6 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_7 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_8 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_9 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_10 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_11 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_12 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_13 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_14 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_15 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_16 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -14 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 8;
    let score_17 = embedding[0] * 6 + embedding[1] * -5 + embedding[2] * -10 + embedding[3] * -15 + embedding[4] * 13 + embedding[5] * 20 + embedding[6] * -17 + embedding[7] * -14 + embedding[8] * 14 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 18;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_81(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_1 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * -5 + embedding[3] * -23 + embedding[4] * 1 + embedding[5] * 6 + embedding[6] * -7 + embedding[7] * -11 + embedding[8] * 2;
    let score_2 = embedding[0] * 9 + embedding[1] * 29 + embedding[2] * 13 + embedding[3] * -6 + embedding[4] * -34 + embedding[5] * -2 + embedding[6] * -1 + embedding[7] * -11 + embedding[8] * 8 + embedding[9] * 16;
    let score_3 = embedding[0] * 24 + embedding[1] * -19 + embedding[2] * 12 + embedding[3] * 40 + embedding[4] * 12 + embedding[5] * -4 + embedding[6] * 9 + embedding[7] * 6 + embedding[8] * -12 + embedding[9] * -11;
    let score_4 = embedding[0] * -4 + embedding[1] * -11 + embedding[2] * -14 + embedding[3] * 11 + embedding[4] * 13 + embedding[5] * -29 + embedding[6] * 5 + embedding[7] * -3 + embedding[8] * -21 + embedding[9] * -5;
    let score_5 = embedding[0] * 9 + embedding[1] * 1 + embedding[2] * -13 + embedding[3] * -1 + embedding[4] * 7 + embedding[5] * 7 + embedding[6] * -13 + embedding[7] * 3 + embedding[8] * 9 + embedding[9] * -2;
    let score_6 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_17 = embedding[0] * 4 + embedding[1] * -1 + embedding[2] * -15 + embedding[3] * -13 + embedding[4] * 15 + embedding[5] * 14 + embedding[6] * -15 + embedding[7] * -10 + embedding[8] * 12 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 16;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 18;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 101;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_82(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_1 = embedding[0] * 1 + embedding[1] * -8 + embedding[2] * -1 + embedding[3] * -19 + embedding[4] * 1 + embedding[5] * 10 + embedding[6] * -2 + embedding[7] * -23 + embedding[8] * -3 + embedding[9] * -3;
    let score_2 = embedding[0] * -1 + embedding[1] * 17 + embedding[2] * -25 + embedding[3] * 5 + embedding[4] * 27 + embedding[5] * -39 + embedding[6] * -11 + embedding[7] * 41 + embedding[8] * 7 + embedding[9] * -2;
    let score_3 = embedding[0] * 8 + embedding[1] * 23 + embedding[2] * 18 + embedding[3] * -6 + embedding[4] * -37 + embedding[5] * -3 + embedding[6] * 5 + embedding[7] * -19 + embedding[8] * 3 + embedding[9] * 16;
    let score_4 = embedding[0] * 11 + embedding[1] * -9 + embedding[2] * 10 + embedding[3] * 4 + embedding[4] * 9 + embedding[5] * 2 + embedding[6] * -18 + embedding[7] * 52 + embedding[8] * 3 + embedding[9] * -3;
    let score_5 = embedding[0] * 9 + embedding[1] * 1 + embedding[2] * -14 + embedding[3] * 4 + embedding[4] * 14 + embedding[5] * -2 + embedding[6] * -10 + embedding[7] * 9 + embedding[8] * 7 + embedding[9] * -6;
    let score_6 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_7 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_8 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_9 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_10 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_11 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_12 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_13 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_14 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_15 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_16 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_17 = embedding[0] * 4 + embedding[1] * -7 + embedding[2] * -11 + embedding[3] * -12 + embedding[4] * 17 + embedding[5] * 13 + embedding[6] * -10 + embedding[7] * -14 + embedding[8] * 8 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 16;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 101;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_83(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -3 + embedding[1] * 1 + embedding[2] * 1 + embedding[3] * -21 + embedding[4] * -2 + embedding[5] * 3 + embedding[7] * -19 + embedding[8] * -5 + embedding[9] * 4;
    let score_2 = embedding[0] * 3 + embedding[1] * -7 + embedding[2] * -1 + embedding[3] * -5 + embedding[4] * 2 + embedding[5] * 6 + embedding[6] * 4 + embedding[7] * -18 + embedding[8] * -4 + embedding[9] * -2;
    let score_3 = embedding[0] * -5 + embedding[1] * -12 + embedding[2] * 4 + embedding[3] * 3 + embedding[4] * -4 + embedding[5] * -1 + embedding[6] * 17 + embedding[7] * -22 + embedding[8] * -15 + embedding[9] * -4;
    let score_4 = embedding[0] * 16 + embedding[1] * 26 + embedding[2] * -10 + embedding[3] * 20 + embedding[4] * 24 + embedding[5] * -46 + embedding[6] * 2 + embedding[7] * 31 + embedding[8] * -1;
    let score_5 = embedding[0] * 4 + embedding[1] * 30 + embedding[2] * 17 + embedding[3] * -8 + embedding[4] * -40 + embedding[6] * -1 + embedding[7] * -10 + embedding[8] * 8 + embedding[9] * 20;
    let score_6 = embedding[0] * 5 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * -7 + embedding[4] * -34 + embedding[5] * 17 + embedding[6] * -9 + embedding[7] * 9 + embedding[8] * 9 + embedding[9] * 11;
    let score_7 = embedding[0] * 21 + embedding[1] * -22 + embedding[2] * 19 + embedding[3] * 47 + embedding[4] * 10 + embedding[5] * -6 + embedding[6] * 19 + embedding[7] * 7 + embedding[8] * -19 + embedding[9] * -12;
    let score_8 = embedding[0] * -14 + embedding[1] * -24 + embedding[2] * 4 + embedding[3] * 14 + embedding[4] * 19 + embedding[5] * -11 + embedding[6] * -2 + embedding[7] * 59 + embedding[8] * -7 + embedding[9] * -13;
    let score_9 = embedding[0] * 3 + embedding[1] * -7 + embedding[2] * 12 + embedding[3] * 7 + embedding[4] * 9 + embedding[5] * -1 + embedding[6] * -19 + embedding[7] * 73 + embedding[8] * 6 + embedding[9] * -4;
    let score_10 = embedding[0] * 8 + embedding[1] * 8 + embedding[2] * -14 + embedding[3] * 3 + embedding[4] * 11 + embedding[5] * -1 + embedding[6] * -10 + embedding[7] * 10 + embedding[8] * 9 + embedding[9] * -1;
    let score_11 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[1] * 3 + embedding[2] * -12 + embedding[3] * -14 + embedding[4] * 15 + embedding[5] * 11 + embedding[6] * -12 + embedding[7] * -8 + embedding[8] * 12 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 8;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 16;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 44;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 13;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 6;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 12;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 101;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_84(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_1 = embedding[1] * -7 + embedding[2] * -3 + embedding[3] * -18 + embedding[4] * 9 + embedding[5] * 6 + embedding[6] * -3 + embedding[7] * -18 + embedding[8] * -3 + embedding[9] * -4;
    let score_2 = embedding[0] * -3 + embedding[1] * 18 + embedding[2] * -24 + embedding[3] * 5 + embedding[4] * 26 + embedding[5] * -41 + embedding[6] * -8 + embedding[7] * 39 + embedding[8] * 7 + embedding[9] * -2;
    let score_3 = embedding[0] * -4 + embedding[1] * -15 + embedding[2] * 2 + embedding[3] * -2 + embedding[5] * 1 + embedding[6] * 13 + embedding[7] * -22 + embedding[8] * -12 + embedding[9] * -7;
    let score_4 = embedding[0] * 7 + embedding[1] * 25 + embedding[2] * 18 + embedding[3] * -5 + embedding[4] * -38 + embedding[6] * 1 + embedding[7] * -11 + embedding[8] * 8 + embedding[9] * 16;
    let score_5 = embedding[0] * 6 + embedding[1] * -1 + embedding[2] * 20 + embedding[3] * -5 + embedding[4] * -33 + embedding[5] * 18 + embedding[6] * -7 + embedding[7] * 6 + embedding[8] * 7 + embedding[9] * 7;
    let score_6 = embedding[0] * 9 + embedding[1] * -5 + embedding[2] * 12 + embedding[3] * 1 + embedding[4] * 7 + embedding[5] * 1 + embedding[6] * -18 + embedding[7] * 56 + embedding[8] * 5;
    let score_7 = embedding[0] * 8 + embedding[1] * 3 + embedding[2] * -13 + embedding[3] * 5 + embedding[4] * 13 + embedding[5] * -4 + embedding[6] * -8 + embedding[7] * 9 + embedding[8] * 8 + embedding[9] * -4;
    let score_8 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_9 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_10 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_11 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_12 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[0] * 3 + embedding[1] * -4 + embedding[2] * -10 + embedding[3] * -12 + embedding[4] * 15 + embedding[5] * 11 + embedding[6] * -9 + embedding[7] * -13 + embedding[8] * 10 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 16;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 20;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 101;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_85(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_1 = embedding[0] * -2 + embedding[1] * -8 + embedding[2] * -7 + embedding[3] * -19 + embedding[4] * 4 + embedding[5] * 12 + embedding[6] * -9 + embedding[7] * -15 + embedding[8] * 4 + embedding[9] * -4;
    let score_2 = embedding[0] * 17 + embedding[1] * 15 + embedding[2] * -10 + embedding[3] * 18 + embedding[4] * 23 + embedding[5] * -41 + embedding[6] * 1 + embedding[7] * 24 + embedding[8] * -5 + embedding[9] * -2;
    let score_3 = embedding[0] * 8 + embedding[1] * 23 + embedding[2] * 17 + embedding[3] * -5 + embedding[4] * -36 + embedding[5] * -1 + embedding[6] * 1 + embedding[7] * -13 + embedding[8] * 7 + embedding[9] * 15;
    let score_4 = embedding[1] * -7 + embedding[2] * -14 + embedding[3] * 11 + embedding[4] * 14 + embedding[5] * -30 + embedding[6] * 7 + embedding[7] * -3 + embedding[8] * -18 + embedding[9] * -5;
    let score_5 = embedding[0] * 10 + embedding[1] * -1 + embedding[2] * -11 + embedding[3] * 3 + embedding[4] * 9 + embedding[5] * 2 + embedding[6] * -11 + embedding[7] * 6 + embedding[8] * 7 + embedding[9] * -3;
    let score_6 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_7 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_8 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_9 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_10 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_11 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_12 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[0] * 5 + embedding[1] * -6 + embedding[2] * -10 + embedding[3] * -13 + embedding[4] * 13 + embedding[5] * 15 + embedding[6] * -13 + embedding[7] * -11 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 16;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 18;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 101;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_86(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_1 = embedding[0] * 3 + embedding[1] * 1 + embedding[2] * -9 + embedding[3] * -18 + embedding[4] * 4 + embedding[5] * 9 + embedding[6] * -11 + embedding[7] * -14 + embedding[8] * 7 + embedding[9] * -1;
    let score_2 = embedding[0] * -9 + embedding[1] * -21 + embedding[2] * 2 + embedding[3] * 8 + embedding[4] * 17 + embedding[5] * -9 + embedding[6] * -3 + embedding[7] * 38 + embedding[8] * -6 + embedding[9] * -11;
    let score_3 = embedding[0] * -3 + embedding[1] * -10 + embedding[2] * -12 + embedding[3] * 10 + embedding[4] * 11 + embedding[5] * -26 + embedding[6] * 3 + embedding[7] * -3 + embedding[8] * -20 + embedding[9] * -5;
    let score_4 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_5 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_6 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_7 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_8 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_9 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_10 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_11 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_12 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_13 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_14 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_15 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_16 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * 2 + embedding[2] * -11 + embedding[3] * -14 + embedding[4] * 13 + embedding[5] * 16 + embedding[6] * -15 + embedding[7] * -14 + embedding[8] * 14 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 18;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_87(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_1 = embedding[0] * 4 + embedding[1] * 3 + embedding[2] * -5 + embedding[3] * -23 + embedding[4] * 13 + embedding[5] * -10 + embedding[6] * -8 + embedding[7] * 2 + embedding[8] * -10 + embedding[9] * 6;
    let score_2 = embedding[0] * -8 + embedding[1] * -21 + embedding[3] * 10 + embedding[4] * 17 + embedding[5] * -5 + embedding[6] * -2 + embedding[7] * 36 + embedding[8] * -1 + embedding[9] * -13;
    let score_3 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_4 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_5 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_6 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_7 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_8 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_9 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_10 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_11 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_12 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_13 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_14 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_15 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_16 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_17 = embedding[0] * 9 + embedding[1] * 2 + embedding[2] * -16 + embedding[3] * -13 + embedding[4] * 11 + embedding[5] * 18 + embedding[6] * -14 + embedding[7] * -22 + embedding[8] * 18 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_88(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_1 = embedding[0] * -6 + embedding[1] * -8 + embedding[2] * -4 + embedding[3] * -29 + embedding[4] * 13 + embedding[5] * -6 + embedding[6] * -15 + embedding[7] * 8 + embedding[8] * -8 + embedding[9] * 3;
    let score_2 = embedding[0] * 18 + embedding[1] * 16 + embedding[2] * -10 + embedding[3] * 12 + embedding[4] * 22 + embedding[5] * -31 + embedding[6] * -3 + embedding[7] * 17 + embedding[8] * 2 + embedding[9] * -3;
    let score_3 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_4 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_5 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_6 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_7 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_8 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_9 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_10 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_11 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_12 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_13 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_14 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_15 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_16 = embedding[0] * -6 + embedding[1] * 9 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_17 = embedding[0] * 4 + embedding[1] * -6 + embedding[2] * -13 + embedding[3] * -14 + embedding[4] * 11 + embedding[5] * 22 + embedding[6] * -13 + embedding[7] * -25 + embedding[8] * 13 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_89(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -24 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_90(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -20 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_91(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -22 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_92(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_93(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_94(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_95(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_96(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -21 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_97(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_98(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_99(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 11;
    let score_1 = embedding[0] * -1 + embedding[1] * -5 + embedding[2] * -1 + embedding[3] * -21 + embedding[4] * 19 + embedding[5] * -13 + embedding[6] * -10 + embedding[7] * 9 + embedding[8] * -12 + embedding[9] * 1;
    let score_2 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 11;
    let score_3 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 11;
    let score_4 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 11;
    let score_5 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 11;
    let score_6 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_7 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 11;
    let score_8 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_9 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_10 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_11 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 11;
    let score_12 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 11;
    let score_13 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 11;
    let score_14 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 11;
    let score_15 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 11;
    let score_16 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 11;
    let score_17 = embedding[0] * 9 + embedding[2] * -19 + embedding[3] * -15 + embedding[4] * 16 + embedding[5] * 17 + embedding[6] * -16 + embedding[7] * -19 + embedding[8] * 17 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_100(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_101(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_1 = embedding[0] * -11 + embedding[1] * -7 + embedding[3] * -20 + embedding[4] * 5 + embedding[5] * -1 + embedding[6] * -15 + embedding[7] * 15;
    let score_2 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_3 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_4 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_5 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_6 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_7 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_8 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_9 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_10 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_11 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_12 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_13 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_14 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_15 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_16 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_17 = embedding[0] * 10 + embedding[1] * -4 + embedding[2] * -19 + embedding[3] * -12 + embedding[4] * 19 + embedding[5] * 14 + embedding[6] * -12 + embedding[7] * -22 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_102(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_1 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * -12 + embedding[3] * -24 + embedding[4] * 12 + embedding[5] * -19 + embedding[6] * -9 + embedding[7] * -2 + embedding[8] * -9 + embedding[9] * 7;
    let score_2 = embedding[0] * -2 + embedding[1] * 15 + embedding[2] * -24 + embedding[3] * -5 + embedding[4] * 23 + embedding[5] * -31 + embedding[6] * -12 + embedding[7] * 31 + embedding[8] * 8;
    let score_3 = embedding[0] * 28 + embedding[1] * -23 + embedding[2] * 17 + embedding[3] * 36 + embedding[4] * 8 + embedding[5] * 9 + embedding[6] * 10 + embedding[7] * -7 + embedding[8] * -8 + embedding[9] * -13;
    let score_4 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_5 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_6 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_7 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_8 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_9 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_10 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_11 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_12 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_13 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_14 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_15 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_16 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_17 = embedding[0] * 7 + embedding[1] * -7 + embedding[2] * -11 + embedding[3] * -17 + embedding[4] * 10 + embedding[5] * 26 + embedding[6] * -14 + embedding[7] * -22 + embedding[8] * 15 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_103(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_1 = embedding[0] * -12 + embedding[1] * -5 + embedding[2] * -2 + embedding[3] * -30 + embedding[4] * 1 + embedding[5] * -6 + embedding[6] * -7 + embedding[7] * -13 + embedding[8] * -11 + embedding[9] * 7;
    let score_2 = embedding[0] * 20 + embedding[1] * 15 + embedding[2] * -11 + embedding[3] * 15 + embedding[4] * 24 + embedding[5] * -31 + embedding[6] * -2 + embedding[7] * 20 + embedding[9] * -3;
    let score_3 = embedding[0] * 20 + embedding[1] * -4 + embedding[3] * -1 + embedding[4] * 16 + embedding[5] * 3 + embedding[6] * -23 + embedding[7] * 46 + embedding[8] * 7 + embedding[9] * -4;
    let score_4 = embedding[0] * -2 + embedding[1] * -5 + embedding[2] * -18 + embedding[3] * 8 + embedding[4] * 13 + embedding[5] * -29 + embedding[6] * 3 + embedding[7] * -7 + embedding[8] * -16 + embedding[9] * -6;
    let score_5 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_6 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_7 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_8 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_9 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_10 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_11 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_12 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_13 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_14 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_15 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_16 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 8;
    let score_17 = embedding[0] * 7 + embedding[1] * -4 + embedding[2] * -14 + embedding[3] * -8 + embedding[4] * 9 + embedding[5] * 27 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 21 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 12;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 18;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_104(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -4 + embedding[1] * 4 + embedding[2] * 2 + embedding[3] * -33 + embedding[4] * -1 + embedding[5] * 14 + embedding[6] * -11 + embedding[7] * -17 + embedding[8] * 3 + embedding[9] * 7;
    let score_2 = embedding[0] * -6 + embedding[1] * 13 + embedding[2] * -26 + embedding[3] * 6 + embedding[4] * 33 + embedding[5] * -42 + embedding[6] * -8 + embedding[7] * 50 + embedding[8] * 2 + embedding[9] * -3;
    let score_3 = embedding[0] * 16 + embedding[1] * 5 + embedding[2] * -10 + embedding[3] * 46 + embedding[4] * -3 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * -22 + embedding[8] * 1 + embedding[9] * -11;
    let score_4 = embedding[0] * 4 + embedding[1] * 30 + embedding[2] * 21 + embedding[3] * -16 + embedding[4] * -41 + embedding[5] * 6 + embedding[7] * -20 + embedding[8] * 7 + embedding[9] * 21;
    let score_5 = embedding[0] * 24 + embedding[1] * -17 + embedding[2] * 16 + embedding[3] * 50 + embedding[4] * 13 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 5 + embedding[8] * -21 + embedding[9] * -15;
    let score_6 = embedding[0] * 10 + embedding[1] * -13 + embedding[2] * -1 + embedding[3] * 1 + embedding[4] * 26 + embedding[5] * -11 + embedding[6] * -16 + embedding[7] * 63 + embedding[8] * -4 + embedding[9] * -10;
    let score_7 = embedding[0] * -11 + embedding[1] * -28 + embedding[2] * -2 + embedding[3] * 16 + embedding[4] * 27 + embedding[5] * -20 + embedding[6] * -6 + embedding[7] * 71 + embedding[8] * -13 + embedding[9] * -19;
    let score_8 = embedding[0] * -2 + embedding[1] * -8 + embedding[2] * -16 + embedding[3] * 20 + embedding[4] * 18 + embedding[5] * -34 + embedding[6] * 9 + embedding[7] * 7 + embedding[8] * -25 + embedding[9] * -10;
    let score_9 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[2] * -11 + embedding[3] * -17 + embedding[4] * 6 + embedding[5] * 23 + embedding[6] * -11 + embedding[7] * -19 + embedding[8] * 15 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 10;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 16;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 13;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 18;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_105(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -6 + embedding[2] * -2 + embedding[3] * -29 + embedding[4] * 3 + embedding[5] * 11 + embedding[6] * -12 + embedding[7] * -13 + embedding[9] * 6;
    let score_2 = embedding[0] * -14 + embedding[1] * 10 + embedding[2] * 23 + embedding[3] * -42 + embedding[4] * -23 + embedding[5] * -1 + embedding[6] * -7 + embedding[7] * -10 + embedding[8] * -10 + embedding[9] * 24;
    let score_3 = embedding[0] * 19 + embedding[1] * 3 + embedding[2] * -15 + embedding[3] * 58 + embedding[4] * 5 + embedding[5] * -17 + embedding[6] * 16 + embedding[7] * -13 + embedding[8] * 1 + embedding[9] * -15;
    let score_4 = embedding[0] * 19 + embedding[1] * 15 + embedding[2] * -18 + embedding[3] * 42 + embedding[4] * 26 + embedding[5] * -43 + embedding[6] * 9 + embedding[7] * 20 + embedding[8] * -3 + embedding[9] * -8;
    let score_5 = embedding[0] * 2 + embedding[1] * 31 + embedding[2] * 22 + embedding[3] * -16 + embedding[4] * -43 + embedding[5] * -3 + embedding[6] * -2 + embedding[7] * -13 + embedding[8] * 1 + embedding[9] * 26;
    let score_6 = embedding[0] * 27 + embedding[1] * -19 + embedding[2] * 13 + embedding[3] * 65 + embedding[4] * 14 + embedding[5] * -11 + embedding[6] * 21 + embedding[7] * 7 + embedding[8] * -16 + embedding[9] * -19;
    let score_7 = embedding[0] * -15 + embedding[1] * -30 + embedding[2] * 1 + embedding[3] * 17 + embedding[4] * 20 + embedding[5] * -2 + embedding[6] * -4 + embedding[7] * 57 + embedding[8] * -6 + embedding[9] * -15;
    let score_8 = embedding[0] * -1 + embedding[1] * -5 + embedding[2] * -20 + embedding[3] * 30 + embedding[4] * 20 + embedding[5] * -40 + embedding[6] * 8 + embedding[7] * 3 + embedding[8] * -21 + embedding[9] * -7;
    let score_9 = embedding[0] * 3 + embedding[1] * -11 + embedding[2] * 7 + embedding[3] * 12 + embedding[4] * 9 + embedding[5] * 8 + embedding[6] * -20 + embedding[7] * 70 + embedding[8] * 10 + embedding[9] * -7;
    let score_10 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 3 + embedding[1] * -2 + embedding[2] * -16 + embedding[3] * -5 + embedding[4] * 15 + embedding[5] * 19 + embedding[6] * -13 + embedding[7] * -9 + embedding[8] * 16 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 89;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 10;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 8;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 16;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 13;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 18;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 12;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_106(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 8;
    let score_1 = embedding[0] * -4 + embedding[1] * -1 + embedding[2] * 5 + embedding[3] * -39 + embedding[4] * 1 + embedding[5] * 8 + embedding[6] * -11 + embedding[7] * -14 + embedding[8] * -5 + embedding[9] * 9;
    let score_2 = embedding[0] * -8 + embedding[1] * 23 + embedding[2] * -34 + embedding[3] * 8 + embedding[4] * 26 + embedding[5] * -43 + embedding[6] * -10 + embedding[7] * 43 + embedding[8] * 11 + embedding[9] * -2;
    let score_3 = embedding[0] * -4 + embedding[1] * -15 + embedding[2] * -11 + embedding[3] * 9 + embedding[4] * 8 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * -17 + embedding[8] * -12 + embedding[9] * -9;
    let score_4 = embedding[0] * 6 + embedding[1] * -16 + embedding[2] * 78 + embedding[3] * -20 + embedding[4] * -3 + embedding[5] * 11 + embedding[6] * 7 + embedding[7] * 32 + embedding[8] * -37 + embedding[9] * 9;
    let score_5 = embedding[0] * 20 + embedding[1] * 3 + embedding[2] * -14 + embedding[3] * 58 + embedding[4] * 1 + embedding[5] * -16 + embedding[6] * 19 + embedding[7] * -16 + embedding[8] * 4 + embedding[9] * -15;
    let score_6 = embedding[0] * -30 + embedding[1] * 17 + embedding[2] * -12 + embedding[3] * -18 + embedding[4] * -9 + embedding[5] * -38 + embedding[6] * 4 + embedding[7] * -15 + embedding[8] * -12 + embedding[9] * 16;
    let score_7 = embedding[0] * 5 + embedding[1] * 26 + embedding[2] * 20 + embedding[3] * -14 + embedding[4] * -42 + embedding[5] * 5 + embedding[6] * -5 + embedding[7] * -12 + embedding[8] * 9 + embedding[9] * 21;
    let score_8 = embedding[0] * 9 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * -8 + embedding[4] * -37 + embedding[5] * 27 + embedding[6] * -13 + embedding[7] * 9 + embedding[8] * 8 + embedding[9] * 10;
    let score_9 = embedding[0] * 28 + embedding[1] * -18 + embedding[2] * 16 + embedding[3] * 58 + embedding[4] * 7 + embedding[5] * -5 + embedding[6] * 23 + embedding[7] * -2 + embedding[8] * -14 + embedding[9] * -17;
    let score_10 = embedding[0] * -14 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * 18 + embedding[4] * 12 + embedding[5] * -62 + embedding[6] * 20 + embedding[7] * -4 + embedding[8] * -39;
    let score_11 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * 13 + embedding[3] * 10 + embedding[4] * 3 + embedding[5] * 11 + embedding[6] * -22 + embedding[7] * 69 + embedding[8] * 15 + embedding[9] * -2;
    let score_12 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 8;
    let score_13 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 8;
    let score_14 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 8;
    let score_15 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 8;
    let score_16 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 8;
    let score_17 = embedding[0] * 3 + embedding[2] * -16 + embedding[3] * -4 + embedding[4] * 13 + embedding[5] * 21 + embedding[6] * -12 + embedding[7] * -9 + embedding[8] * 21 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 90;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 9;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 10;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 25;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 16;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 44;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 13;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 18;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 12;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_107(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -5 + embedding[1] * 3 + embedding[2] * -5 + embedding[3] * -27 + embedding[4] * 6 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -7 + embedding[8] * 7 + embedding[9] * 1;
    let score_2 = embedding[0] * 7 + embedding[1] * -17 + embedding[2] * 13 + embedding[3] * -6 + embedding[4] * 5 + embedding[5] * 14 + embedding[6] * 2 + embedding[7] * -5 + embedding[8] * -10 + embedding[9] * -5;
    let score_3 = embedding[1] * -16 + embedding[2] * -1 + embedding[3] * 14 + embedding[4] * 8 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * -12 + embedding[8] * -18 + embedding[9] * -9;
    let score_4 = embedding[0] * 15 + embedding[1] * 2 + embedding[2] * -8 + embedding[3] * 52 + embedding[5] * -23 + embedding[6] * 20 + embedding[7] * -14 + embedding[8] * -4 + embedding[9] * -11;
    let score_5 = embedding[0] * 15 + embedding[1] * 17 + embedding[2] * -12 + embedding[3] * 31 + embedding[4] * 23 + embedding[5] * -48 + embedding[6] * 11 + embedding[7] * 21 + embedding[8] * -6 + embedding[9] * -5;
    let score_6 = embedding[0] * -26 + embedding[1] * 20 + embedding[2] * -11 + embedding[3] * -15 + embedding[4] * -8 + embedding[5] * -34 + embedding[7] * -9 + embedding[8] * -5 + embedding[9] * 14;
    let score_7 = embedding[0] * 1 + embedding[1] * 30 + embedding[2] * 20 + embedding[3] * -14 + embedding[4] * -41 + embedding[5] * 4 + embedding[6] * -6 + embedding[7] * -6 + embedding[8] * 11 + embedding[9] * 20;
    let score_8 = embedding[0] * 2 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * -13 + embedding[4] * -37 + embedding[5] * 24 + embedding[6] * -14 + embedding[7] * 16 + embedding[8] * 10 + embedding[9] * 11;
    let score_9 = embedding[0] * 26 + embedding[1] * -24 + embedding[2] * 25 + embedding[3] * 54 + embedding[4] * 5 + embedding[5] * -10 + embedding[6] * 27 + embedding[7] * 2 + embedding[8] * -28 + embedding[9] * -14;
    let score_10 = embedding[0] * -8 + embedding[1] * -4 + embedding[2] * -17 + embedding[3] * 28 + embedding[4] * 10 + embedding[5] * -56 + embedding[6] * 25 + embedding[7] * -2 + embedding[8] * -38 + embedding[9] * -2;
    let score_11 = embedding[0] * 5 + embedding[1] * -4 + embedding[2] * 16 + embedding[3] * -2 + embedding[4] * 2 + embedding[5] * 7 + embedding[6] * -23 + embedding[7] * 69 + embedding[8] * 10 + embedding[9] * 2;
    let score_12 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * -3 + embedding[1] * 1 + embedding[2] * -11 + embedding[3] * -17 + embedding[4] * 12 + embedding[5] * 16 + embedding[6] * -15 + embedding[7] * -5 + embedding[8] * 15 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 10;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 8;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 25;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 16;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 20;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 13;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 18;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 12;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_108(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_1 = embedding[0] * -4 + embedding[1] * 1 + embedding[2] * -2 + embedding[3] * -27 + embedding[4] * 2 + embedding[5] * 8 + embedding[6] * -8 + embedding[7] * -14 + embedding[8] * 1 + embedding[9] * 4;
    let score_2 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * -19 + embedding[3] * -5 + embedding[4] * 20 + embedding[5] * -11 + embedding[6] * -13 + embedding[7] * 24 + embedding[8] * 9 + embedding[9] * -3;
    let score_3 = embedding[0] * 2 + embedding[1] * -2 + embedding[2] * -1 + embedding[3] * -14 + embedding[4] * 5 + embedding[5] * 17 + embedding[6] * -8 + embedding[7] * 1 + embedding[8] * 4 + embedding[9] * -2;
    let score_4 = embedding[0] * -2 + embedding[1] * -14 + embedding[2] * -4 + embedding[3] * 11 + embedding[4] * 7 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * -15 + embedding[8] * -18 + embedding[9] * -7;
    let score_5 = embedding[0] * 20 + embedding[1] * 2 + embedding[2] * -4 + embedding[3] * 53 + embedding[4] * -1 + embedding[5] * -19 + embedding[6] * 21 + embedding[7] * -16 + embedding[8] * -4 + embedding[9] * -12;
    let score_6 = embedding[0] * -26 + embedding[1] * 20 + embedding[2] * -8 + embedding[3] * -14 + embedding[4] * -13 + embedding[5] * -39 + embedding[6] * 6 + embedding[7] * -15 + embedding[8] * -12 + embedding[9] * 16;
    let score_7 = embedding[0] * 5 + embedding[1] * 28 + embedding[2] * 24 + embedding[3] * -10 + embedding[4] * -44 + embedding[5] * 2 + embedding[6] * -1 + embedding[7] * -11 + embedding[8] * 7 + embedding[9] * 22;
    let score_8 = embedding[0] * 7 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * -6 + embedding[4] * -36 + embedding[5] * 22 + embedding[6] * -9 + embedding[7] * 8 + embedding[8] * 7 + embedding[9] * 9;
    let score_9 = embedding[0] * 28 + embedding[1] * -16 + embedding[2] * 20 + embedding[3] * 55 + embedding[4] * 8 + embedding[5] * -13 + embedding[6] * 23 + embedding[7] * 1 + embedding[8] * -21 + embedding[9] * -15;
    let score_10 = embedding[0] * -18 + embedding[1] * -22 + embedding[2] * -2 + embedding[3] * 16 + embedding[4] * 18 + embedding[5] * -16 + embedding[6] * -4 + embedding[7] * 65 + embedding[8] * -8 + embedding[9] * -13;
    let score_11 = embedding[0] * -12 + embedding[1] * -17 + embedding[2] * -11 + embedding[3] * 26 + embedding[4] * 12 + embedding[5] * -49 + embedding[6] * 22 + embedding[7] * -1 + embedding[8] * -41 + embedding[9] * -5;
    let score_12 = embedding[0] * 2 + embedding[1] * -9 + embedding[2] * 12 + embedding[3] * 7 + embedding[4] * 7 + embedding[5] * 3 + embedding[6] * -19 + embedding[7] * 71 + embedding[8] * 6 + embedding[9] * -4;
    let score_13 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[0] * 5 + embedding[2] * -6 + embedding[3] * -7 + embedding[4] * 11 + embedding[5] * 16 + embedding[6] * -10 + embedding[7] * -8 + embedding[8] * 12 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 9;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 10;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 25;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 16;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 44;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 13;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 6;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 18;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 12;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_109(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_1 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * -6 + embedding[3] * -27 + embedding[4] * 9 + embedding[5] * 5 + embedding[6] * -10 + embedding[7] * -10 + embedding[8] * 1 + embedding[9] * 1;
    let score_2 = embedding[0] * 17 + embedding[1] * 5 + embedding[2] * -8 + embedding[3] * 49 + embedding[4] * -1 + embedding[5] * -17 + embedding[6] * 19 + embedding[7] * -16 + embedding[8] * -1 + embedding[9] * -11;
    let score_3 = embedding[0] * 4 + embedding[1] * 28 + embedding[2] * 17 + embedding[3] * -8 + embedding[4] * -36 + embedding[5] * 1 + embedding[6] * -3 + embedding[7] * -8 + embedding[8] * 8 + embedding[9] * 19;
    let score_4 = embedding[0] * 4 + embedding[1] * 5 + embedding[2] * 16 + embedding[3] * -7 + embedding[4] * -32 + embedding[5] * 17 + embedding[6] * -10 + embedding[7] * 8 + embedding[8] * 8 + embedding[9] * 10;
    let score_5 = embedding[0] * 25 + embedding[1] * -16 + embedding[2] * 16 + embedding[3] * 53 + embedding[4] * 11 + embedding[5] * -12 + embedding[6] * 20 + embedding[7] * 4 + embedding[8] * -16 + embedding[9] * -15;
    let score_6 = embedding[0] * -12 + embedding[1] * 1 + embedding[2] * -10 + embedding[3] * -16 + embedding[4] * -3 + embedding[5] * 5 + embedding[6] * -3 + embedding[7] * -10 + embedding[8] * 5 + embedding[9] * 1;
    let score_7 = embedding[0] * -16 + embedding[1] * -26 + embedding[2] * 5 + embedding[3] * 10 + embedding[4] * 18 + embedding[5] * -9 + embedding[6] * -1 + embedding[7] * 51 + embedding[8] * -10 + embedding[9] * -12;
    let score_8 = embedding[0] * -12 + embedding[1] * -14 + embedding[2] * -11 + embedding[3] * 14 + embedding[4] * 16 + embedding[5] * -35 + embedding[6] * 8 + embedding[7] * 3 + embedding[8] * -28 + embedding[9] * -6;
    let score_9 = embedding[0] * 4 + embedding[1] * -7 + embedding[2] * 11 + embedding[3] * 7 + embedding[4] * 9 + embedding[5] * 1 + embedding[6] * -18 + embedding[7] * 68 + embedding[8] * 5 + embedding[9] * -4;
    let score_10 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_11 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_12 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[0] * 3 + embedding[1] * 1 + embedding[2] * -11 + embedding[3] * -9 + embedding[4] * 16 + embedding[5] * 12 + embedding[6] * -10 + embedding[7] * -9 + embedding[8] * 12 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 10;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 16;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 20;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 13;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 7;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 18;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 12;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_110(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_1 = embedding[0] * -3 + embedding[1] * -1 + embedding[2] * -5 + embedding[3] * -25 + embedding[4] * 1 + embedding[5] * 15 + embedding[6] * -12 + embedding[7] * -12 + embedding[8] * 6 + embedding[9] * 2;
    let score_2 = embedding[0] * 15 + embedding[1] * 9 + embedding[2] * -13 + embedding[3] * 44 + embedding[4] * -4 + embedding[5] * -17 + embedding[6] * 16 + embedding[7] * -19 + embedding[8] * 3 + embedding[9] * -9;
    let score_3 = embedding[0] * 17 + embedding[1] * 14 + embedding[2] * -10 + embedding[3] * 19 + embedding[4] * 25 + embedding[5] * -42 + embedding[6] * 3 + embedding[7] * 24 + embedding[8] * -9 + embedding[9] * -3;
    let score_4 = embedding[0] * 6 + embedding[1] * 28 + embedding[2] * 17 + embedding[3] * -10 + embedding[4] * -39 + embedding[5] * 3 + embedding[6] * -1 + embedding[7] * -14 + embedding[8] * 8 + embedding[9] * 18;
    let score_5 = embedding[0] * 12 + embedding[1] * -10 + embedding[2] * 2 + embedding[3] * 1 + embedding[4] * 21 + embedding[5] * -5 + embedding[6] * -18 + embedding[7] * 54 + embedding[8] * -4 + embedding[9] * -7;
    let score_6 = embedding[0] * -9 + embedding[1] * -28 + embedding[2] * 3 + embedding[3] * 9 + embedding[4] * 19 + embedding[5] * -7 + embedding[6] * -6 + embedding[7] * 52 + embedding[8] * -9 + embedding[9] * -14;
    let score_7 = embedding[0] * -3 + embedding[1] * -8 + embedding[2] * -16 + embedding[3] * 16 + embedding[4] * 16 + embedding[5] * -36 + embedding[6] * 7 + embedding[7] * 3 + embedding[8] * -23 + embedding[9] * -7;
    let score_8 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_9 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_10 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_11 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_12 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_14 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_15 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_16 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * 3 + embedding[1] * -2 + embedding[2] * -11 + embedding[3] * -13 + embedding[4] * 12 + embedding[5] * 18 + embedding[6] * -13 + embedding[7] * -13 + embedding[8] * 12 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 10;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 16;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 12;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 6;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 18;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_111(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_1 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * -1 + embedding[3] * -33 + embedding[4] * -4 + embedding[5] * -1 + embedding[6] * -6 + embedding[7] * -25 + embedding[8] * -4 + embedding[9] * 11;
    let score_2 = embedding[0] * 2 + embedding[1] * 13 + embedding[2] * -25 + embedding[3] * 9 + embedding[4] * 34 + embedding[5] * -39 + embedding[6] * -9 + embedding[7] * 48 + embedding[8] * 4 + embedding[9] * -4;
    let score_3 = embedding[0] * 18 + embedding[1] * -8 + embedding[2] * -3 + embedding[4] * 24 + embedding[5] * -7 + embedding[6] * -22 + embedding[7] * 63 + embedding[8] * 4 + embedding[9] * -9;
    let score_4 = embedding[0] * -6 + embedding[1] * -25 + embedding[2] * -5 + embedding[3] * 17 + embedding[4] * 19 + embedding[5] * -13 + embedding[6] * -9 + embedding[7] * 67 + embedding[8] * -1 + embedding[9] * -18;
    let score_5 = embedding[0] * -1 + embedding[1] * -1 + embedding[2] * -15 + embedding[3] * 6 + embedding[4] * 15 + embedding[5] * -31 + embedding[6] * 5 + embedding[7] * -1 + embedding[8] * -22 + embedding[9] * -4;
    let score_6 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_7 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_8 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_9 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_10 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_11 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_12 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_13 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_14 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_15 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_16 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_17 = embedding[0] * 12 + embedding[1] * -1 + embedding[2] * -7 + embedding[3] * -6 + embedding[4] * 6 + embedding[5] * 33 + embedding[6] * -8 + embedding[7] * -26 + embedding[8] * 20 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 12;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 18;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_112(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_1 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * -15 + embedding[3] * -20 + embedding[4] * 6 + embedding[5] * -2 + embedding[6] * -13 + embedding[7] * -8 + embedding[8] * 2;
    let score_2 = embedding[0] * 19 + embedding[1] * 12 + embedding[2] * -10 + embedding[3] * 13 + embedding[4] * 24 + embedding[5] * -32 + embedding[6] * -5 + embedding[7] * 20 + embedding[8] * -2 + embedding[9] * -1;
    let score_3 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_4 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_5 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_6 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_7 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_8 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_9 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_10 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_11 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_12 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_14 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_15 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_16 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 6;
    let score_17 = embedding[0] * 6 + embedding[1] * -10 + embedding[2] * -12 + embedding[3] * -14 + embedding[4] * 15 + embedding[5] * 20 + embedding[6] * -16 + embedding[7] * -20 + embedding[8] * 9 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_113(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_1 = embedding[0] * -9 + embedding[1] * -20 + embedding[2] * -4 + embedding[3] * 7 + embedding[4] * 17 + embedding[5] * -10 + embedding[6] * -1 + embedding[7] * 37 + embedding[8] * -3 + embedding[9] * -11;
    let score_2 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_3 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_4 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_5 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_6 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_7 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_8 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_9 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_10 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_11 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_12 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_13 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_14 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_15 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_16 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_17 = embedding[0] * 10 + embedding[1] * 4 + embedding[2] * -20 + embedding[3] * -19 + embedding[4] * 12 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -25 + embedding[8] * 14 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 6;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_114(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_1 = embedding[0] * -1 + embedding[1] * -5 + embedding[2] * -1 + embedding[3] * -21 + embedding[4] * 18 + embedding[5] * -13 + embedding[6] * -11 + embedding[7] * 8 + embedding[8] * -12 + embedding[9] * 1;
    let score_2 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_3 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_4 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_5 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_6 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_7 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_8 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_9 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_10 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_11 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_12 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_13 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_14 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_15 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_16 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_17 = embedding[0] * 9 + embedding[2] * -19 + embedding[3] * -15 + embedding[4] * 16 + embedding[5] * 17 + embedding[6] * -16 + embedding[7] * -20 + embedding[8] * 17 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_115(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -6 + embedding[3] * -26 + embedding[4] * 16 + embedding[5] * -9 + embedding[6] * -17 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 2;
    let score_2 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_3 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_4 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_5 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_6 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_7 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_8 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_9 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_10 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_11 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_12 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_13 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_14 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_15 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_16 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 9;
    let score_17 = embedding[0] * 9 + embedding[2] * -19 + embedding[3] * -12 + embedding[4] * 17 + embedding[5] * 16 + embedding[6] * -13 + embedding[7] * -21 + embedding[8] * 14 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_116(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_117(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_118(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_119(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_120(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_121(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -24 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_122(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_123(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_1 = embedding[0] * -6 + embedding[1] * -6 + embedding[3] * -26 + embedding[4] * 16 + embedding[5] * -9 + embedding[6] * -16 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 2;
    let score_2 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_3 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_4 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_5 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_6 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_7 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_8 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_9 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_10 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_11 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_12 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_13 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_14 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_15 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_16 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_17 = embedding[0] * 9 + embedding[2] * -20 + embedding[3] * -12 + embedding[4] * 17 + embedding[5] * 16 + embedding[6] * -13 + embedding[7] * -21 + embedding[8] * 14 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_124(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_1 = embedding[0] * -9 + embedding[1] * -7 + embedding[3] * -27 + embedding[4] * 14 + embedding[5] * -11 + embedding[6] * -9 + embedding[7] * 7 + embedding[8] * -14 + embedding[9] * 3;
    let score_2 = embedding[0] * 16 + embedding[1] * 22 + embedding[2] * -12 + embedding[3] * 9 + embedding[4] * 20 + embedding[5] * -35 + embedding[6] * -3 + embedding[7] * 19 + embedding[8] * 5 + embedding[9] * 1;
    let score_3 = embedding[0] * 23 + embedding[1] * -17 + embedding[2] * 11 + embedding[3] * 32 + embedding[4] * 7 + embedding[5] * 4 + embedding[6] * 7 + embedding[7] * -4 + embedding[8] * -5 + embedding[9] * -11;
    let score_4 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_5 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_6 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_7 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_8 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_9 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_10 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_11 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_12 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_13 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_14 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_15 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_16 = embedding[0] * -3 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_17 = embedding[2] * -17 + embedding[3] * -22 + embedding[4] * 9 + embedding[5] * 21 + embedding[6] * -16 + embedding[7] * -20 + embedding[8] * 17 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_125(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_1 = embedding[0] * -13 + embedding[1] * 1 + embedding[2] * -12 + embedding[3] * -27 + embedding[4] * 11 + embedding[5] * -8 + embedding[6] * -15 + embedding[7] * -3 + embedding[8] * -1 + embedding[9] * 2;
    let score_2 = embedding[0] * 15 + embedding[1] * 19 + embedding[2] * -11 + embedding[3] * 11 + embedding[4] * 21 + embedding[5] * -34 + embedding[6] * -4 + embedding[7] * 22 + embedding[8] * 2;
    let score_3 = embedding[0] * 23 + embedding[1] * -19 + embedding[2] * 12 + embedding[3] * 33 + embedding[4] * 8 + embedding[5] * 3 + embedding[6] * 7 + embedding[7] * -2 + embedding[8] * -8 + embedding[9] * -11;
    let score_4 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_5 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_6 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_7 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_8 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_9 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_10 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_11 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_12 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_13 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_14 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_15 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_16 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -14 + embedding[9] * 8;
    let score_17 = embedding[0] * 2 + embedding[1] * -4 + embedding[2] * -14 + embedding[3] * -20 + embedding[4] * 11 + embedding[5] * 19 + embedding[6] * -16 + embedding[7] * -18 + embedding[8] * 12 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_126(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 7 + embedding[3] * -39 + embedding[4] * -7 + embedding[5] * -7 + embedding[6] * -8 + embedding[7] * -7 + embedding[8] * -12 + embedding[9] * 19;
    let score_2 = embedding[0] * 4 + embedding[1] * 16 + embedding[2] * -24 + embedding[3] * 1 + embedding[4] * 32 + embedding[5] * -33 + embedding[6] * -14 + embedding[7] * 50 + embedding[8] * 8 + embedding[9] * -2;
    let score_3 = embedding[0] * -21 + embedding[1] * 23 + embedding[2] * -10 + embedding[3] * -16 + embedding[4] * -14 + embedding[5] * -25 + embedding[6] * 3 + embedding[7] * -21 + embedding[8] * -3 + embedding[9] * 17;
    let score_4 = embedding[0] * 29 + embedding[1] * -19 + embedding[2] * 15 + embedding[3] * 49 + embedding[4] * 11 + embedding[5] * 10 + embedding[6] * 15 + embedding[7] * -2 + embedding[8] * -6 + embedding[9] * -15;
    let score_5 = embedding[0] * 18 + embedding[1] * -11 + embedding[2] * -4 + embedding[3] * 1 + embedding[4] * 27 + embedding[5] * -7 + embedding[6] * -19 + embedding[7] * 62 + embedding[9] * -11;
    let score_6 = embedding[0] * -4 + embedding[1] * -28 + embedding[2] * -2 + embedding[3] * 21 + embedding[4] * 24 + embedding[5] * -6 + embedding[6] * -6 + embedding[7] * 66 + embedding[8] * -5 + embedding[9] * -21;
    let score_7 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 7 + embedding[1] * 2 + embedding[2] * -13 + embedding[3] * -10 + embedding[4] * 7 + embedding[5] * 30 + embedding[6] * -7 + embedding[7] * -29 + embedding[8] * 19 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 25;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 13;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 12;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 6;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_127(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 10 + embedding[3] * -46 + embedding[4] * -1 + embedding[5] * 8 + embedding[6] * -17 + embedding[7] * -6 + embedding[8] * -5 + embedding[9] * 14;
    let score_2 = embedding[0] * 20 + embedding[1] * 7 + embedding[2] * -13 + embedding[3] * 57 + embedding[4] * -4 + embedding[5] * -8 + embedding[6] * 17 + embedding[7] * -21 + embedding[8] * 8 + embedding[9] * -14;
    let score_3 = embedding[0] * 20 + embedding[1] * 14 + embedding[2] * -12 + embedding[3] * 33 + embedding[4] * 25 + embedding[5] * -38 + embedding[6] * 6 + embedding[7] * 20 + embedding[8] * -6 + embedding[9] * -6;
    let score_4 = embedding[0] * -33 + embedding[1] * 24 + embedding[2] * -9 + embedding[3] * -24 + embedding[4] * -14 + embedding[5] * -31 + embedding[6] * -2 + embedding[7] * -14 + embedding[8] * -7 + embedding[9] * 20;
    let score_5 = embedding[0] * 27 + embedding[1] * -19 + embedding[2] * 16 + embedding[3] * 56 + embedding[4] * 13 + embedding[5] * -3 + embedding[6] * 17 + embedding[7] * 9 + embedding[8] * -16 + embedding[9] * -18;
    let score_6 = embedding[0] * 15 + embedding[1] * -14 + embedding[2] * 3 + embedding[3] * 6 + embedding[4] * 20 + embedding[5] * 6 + embedding[6] * -17 + embedding[7] * 50 + embedding[8] * -1 + embedding[9] * -10;
    let score_7 = embedding[0] * -8 + embedding[1] * -30 + embedding[2] * 3 + embedding[3] * 14 + embedding[4] * 19 + embedding[5] * 2 + embedding[6] * -2 + embedding[7] * 50 + embedding[8] * -10 + embedding[9] * -18;
    let score_8 = embedding[0] * -6 + embedding[1] * -1 + embedding[2] * -18 + embedding[3] * 21 + embedding[4] * 12 + embedding[5] * -43 + embedding[6] * 10 + embedding[7] * -1 + embedding[8] * -23 + embedding[9] * -3;
    let score_9 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_10 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_11 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_12 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * 2 + embedding[1] * 3 + embedding[2] * -14 + embedding[3] * -8 + embedding[4] * 6 + embedding[5] * 28 + embedding[6] * -12 + embedding[7] * -18 + embedding[8] * 22 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 10;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 25;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 13;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 18;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_128(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 1 + embedding[1] * -3 + embedding[2] * -7 + embedding[3] * -31 + embedding[4] * 11 + embedding[5] * -1 + embedding[6] * -9 + embedding[7] * -25 + embedding[8] * -13 + embedding[9] * 7;
    let score_1 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * 47 + embedding[1] * 19 + embedding[2] * 37 + embedding[3] * 32 + embedding[4] * 10 + embedding[5] * 20 + embedding[6] * -11 + embedding[7] * 38 + embedding[8] * 17;
    let score_3 = embedding[0] * -7 + embedding[1] * 19 + embedding[2] * 20 + embedding[3] * -43 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * -9 + embedding[7] * -21 + embedding[8] * -16 + embedding[9] * 30;
    let score_4 = embedding[0] * 12 + embedding[1] * -17 + embedding[2] * 79 + embedding[3] * -10 + embedding[4] * 3 + embedding[5] * 25 + embedding[6] * 2 + embedding[7] * 30 + embedding[8] * -33 + embedding[9] * 9;
    let score_5 = embedding[0] * 24 + embedding[1] * 21 + embedding[2] * 36 + embedding[3] * -35 + embedding[4] * -1 + embedding[5] * 2 + embedding[6] * -16 + embedding[7] * 12 + embedding[8] * -13 + embedding[9] * 25;
    let score_6 = embedding[0] * 14 + embedding[1] * -5 + embedding[2] * -23 + embedding[3] * 69 + embedding[4] * 6 + embedding[5] * -16 + embedding[6] * 21 + embedding[7] * -15 + embedding[8] * 1 + embedding[9] * -20;
    let score_7 = embedding[0] * 17 + embedding[1] * 19 + embedding[2] * -22 + embedding[3] * 40 + embedding[4] * 27 + embedding[5] * -52 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -12 + embedding[9] * -4;
    let score_8 = embedding[0] * -38 + embedding[1] * 20 + embedding[2] * -23 + embedding[3] * -12 + embedding[4] * -6 + embedding[5] * -64 + embedding[6] * 11 + embedding[7] * -25 + embedding[8] * -27 + embedding[9] * 20;
    let score_9 = embedding[0] * 5 + embedding[1] * 39 + embedding[2] * 34 + embedding[3] * -30 + embedding[4] * -52 + embedding[5] * -14 + embedding[6] * -2 + embedding[7] * -19 + embedding[8] * -10 + embedding[9] * 42;
    let score_10 = embedding[0] * 24 + embedding[1] * -31 + embedding[2] * 6 + embedding[3] * 84 + embedding[4] * 11 + embedding[5] * 1 + embedding[6] * 25 + embedding[7] * 7 + embedding[8] * -8 + embedding[9] * -30;
    let score_11 = embedding[0] * -23 + embedding[1] * -44 + embedding[2] * -8 + embedding[3] * 47 + embedding[4] * 11 + embedding[5] * 13 + embedding[6] * 1 + embedding[7] * 73 + embedding[8] * 11 + embedding[9] * -32;
    let score_12 = embedding[0] * -23 + embedding[1] * -4 + embedding[2] * -28 + embedding[3] * 11 + embedding[4] * 15 + embedding[5] * -62 + embedding[6] * 13 + embedding[7] * -13 + embedding[8] * -34 + embedding[9] * 1;
    let score_13 = embedding[0] * -6 + embedding[1] * -24 + embedding[2] * -3 + embedding[3] * 47 + embedding[4] * 2 + embedding[5] * 26 + embedding[6] * -12 + embedding[7] * 81 + embedding[8] * 33 + embedding[9] * -25;
    let score_14 = embedding[0] * 12 + embedding[1] * 4 + embedding[2] * -22 + embedding[3] * 27 + embedding[4] * 13 + embedding[5] * 1 + embedding[6] * -4 + embedding[7] * 5 + embedding[8] * 12 + embedding[9] * -11;
    let score_15 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * -25 + embedding[1] * -12 + embedding[2] * -30 + embedding[3] * 30 + embedding[4] * -11 + embedding[5] * 41 + embedding[6] * -7 + embedding[7] * 15 + embedding[8] * 48 + embedding[9] * -22;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 17;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 89;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 9;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 50;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 10;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 8;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 25;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 16;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 13;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 6;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 18;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 12;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 101;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_129(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -2 + embedding[1] * -1 + embedding[2] * -3 + embedding[3] * -37 + embedding[4] * 4 + embedding[5] * 8 + embedding[6] * -12 + embedding[7] * -23 + embedding[8] * -10 + embedding[9] * 11;
    let score_1 = embedding[0] * -4 + embedding[1] * 6 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_2 = embedding[0] * -3 + embedding[1] * -20 + embedding[2] * -17 + embedding[3] * 54 + embedding[4] * 14 + embedding[5] * 12 + embedding[6] * 7 + embedding[7] * 23 + embedding[8] * 17 + embedding[9] * -25;
    let score_3 = embedding[0] * -12 + embedding[1] * 21 + embedding[2] * -40 + embedding[3] * 29 + embedding[4] * 29 + embedding[5] * -52 + embedding[6] * -2 + embedding[7] * 50 + embedding[8] * 10 + embedding[9] * -4;
    let score_4 = embedding[0] * 17 + embedding[1] * 2 + embedding[2] * -22 + embedding[3] * 90 + embedding[4] * -12 + embedding[5] * -6 + embedding[6] * 25 + embedding[7] * -15 + embedding[8] * 17 + embedding[9] * -22;
    let score_5 = embedding[0] * 43 + embedding[1] * 19 + embedding[2] * 35 + embedding[3] * 29 + embedding[4] * 4 + embedding[5] * 17 + embedding[6] * -8 + embedding[7] * 29 + embedding[8] * 14 + embedding[9] * 2;
    let score_6 = embedding[0] * -7 + embedding[1] * -21 + embedding[2] * -11 + embedding[3] * 10 + embedding[4] * 2 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * -27 + embedding[8] * -26 + embedding[9] * -6;
    let score_7 = embedding[0] * 14 + embedding[1] * -12 + embedding[2] * 81 + embedding[3] * -19 + embedding[4] * -8 + embedding[5] * 16 + embedding[6] * 4 + embedding[7] * 27 + embedding[8] * -39 + embedding[9] * 12;
    let score_8 = embedding[0] * 21 + embedding[1] * 20 + embedding[2] * 37 + embedding[3] * -36 + embedding[4] * -3 + embedding[6] * -18 + embedding[7] * 15 + embedding[8] * -12 + embedding[9] * 25;
    let score_9 = embedding[0] * -38 + embedding[1] * 19 + embedding[2] * -20 + embedding[3] * -16 + embedding[4] * -12 + embedding[5] * -55 + embedding[6] * 9 + embedding[7] * -24 + embedding[8] * -25 + embedding[9] * 21;
    let score_10 = embedding[0] * 6 + embedding[1] * 37 + embedding[2] * 29 + embedding[3] * -24 + embedding[4] * -55 + embedding[5] * -9 + embedding[7] * -19 + embedding[8] * -8 + embedding[9] * 38;
    let score_11 = embedding[0] * 24 + embedding[1] * -32 + embedding[2] * 14 + embedding[3] * 86 + embedding[4] * 6 + embedding[5] * 2 + embedding[6] * 29 + embedding[7] * 4 + embedding[8] * -11 + embedding[9] * -27;
    let score_12 = embedding[0] * -7 + embedding[2] * 17 + embedding[3] * -36 + embedding[4] * -4 + embedding[5] * 4 + embedding[6] * -16 + embedding[7] * -3 + embedding[8] * -2 + embedding[9] * 13;
    let score_13 = embedding[0] * -22 + embedding[1] * -13 + embedding[2] * -24 + embedding[3] * 21 + embedding[4] * 10 + embedding[5] * -64 + embedding[6] * 25 + embedding[7] * -11 + embedding[8] * -49 + embedding[9] * -1;
    let score_14 = embedding[0] * -4 + embedding[1] * -15 + embedding[2] * 4 + embedding[3] * 28 + embedding[4] * 3 + embedding[5] * 11 + embedding[6] * -16 + embedding[7] * 70 + embedding[8] * 22 + embedding[9] * -11;
    let score_15 = embedding[0] * -31 + embedding[1] * 17 + embedding[2] * 40 + embedding[3] * -15 + embedding[4] * -13 + embedding[5] * 7 + embedding[6] * 12 + embedding[7] * -9 + embedding[8] * 12 + embedding[9] * -2;
    let score_16 = embedding[0] * 9 + embedding[1] * 4 + embedding[2] * -21 + embedding[3] * 29 + embedding[4] * 7 + embedding[5] * 5 + embedding[6] * -3 + embedding[7] * 6 + embedding[8] * 14 + embedding[9] * -10;
    let score_17 = embedding[0] * -15 + embedding[1] * -5 + embedding[2] * -12 + embedding[3] * 13 + embedding[4] * -4 + embedding[5] * 24 + embedding[6] * -10 + embedding[7] * 9 + embedding[8] * 33 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 10;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 17;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 90;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 9;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 36;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 25;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 16;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 13;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 89;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 18;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 12;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 52;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 101;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_130(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[1] * -1 + embedding[2] * -2 + embedding[3] * -41 + embedding[4] * 9 + embedding[5] * 3 + embedding[6] * -13 + embedding[7] * -23 + embedding[8] * -13 + embedding[9] * 11;
    let score_1 = embedding[0] * -13 + embedding[1] * -1 + embedding[2] * -3 + embedding[3] * 3 + embedding[4] * -17 + embedding[5] * 34 + embedding[6] * -12 + embedding[7] * 14 + embedding[8] * 36 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * -25 + embedding[2] * -23 + embedding[3] * 72 + embedding[4] * 16 + embedding[5] * -5 + embedding[6] * 21 + embedding[7] * 3 + embedding[8] * 10 + embedding[9] * -32;
    let score_3 = embedding[0] * -11 + embedding[1] * 16 + embedding[2] * -41 + embedding[3] * 28 + embedding[4] * 33 + embedding[5] * -61 + embedding[6] * 4 + embedding[7] * 33 + embedding[9] * -6;
    let score_4 = embedding[0] * 13 + embedding[1] * -4 + embedding[2] * -26 + embedding[3] * 98 + embedding[4] * -8 + embedding[5] * -13 + embedding[6] * 31 + embedding[7] * -20 + embedding[8] * 13 + embedding[9] * -27;
    let score_5 = embedding[0] * 49 + embedding[1] * 21 + embedding[2] * 49 + embedding[3] * 15 + embedding[4] * 2 + embedding[5] * 23 + embedding[6] * -15 + embedding[7] * 39 + embedding[8] * 13 + embedding[9] * 9;
    let score_6 = embedding[0] * -6 + embedding[1] * -24 + embedding[2] * -12 + embedding[3] * 9 + embedding[4] * 9 + embedding[5] * -18 + embedding[6] * 18 + embedding[7] * -27 + embedding[8] * -29 + embedding[9] * -7;
    let score_7 = embedding[0] * 16 + embedding[1] * -14 + embedding[2] * 86 + embedding[3] * -22 + embedding[4] * -1 + embedding[5] * 24 + embedding[6] * -1 + embedding[7] * 30 + embedding[8] * -36 + embedding[9] * 14;
    let score_8 = embedding[0] * 26 + embedding[1] * 23 + embedding[2] * 45 + embedding[3] * -41 + embedding[4] * -11 + embedding[5] * 10 + embedding[6] * -23 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 30;
    let score_9 = embedding[0] * -38 + embedding[1] * 19 + embedding[2] * -22 + embedding[3] * -18 + embedding[4] * -5 + embedding[5] * -66 + embedding[6] * 10 + embedding[7] * -24 + embedding[8] * -31 + embedding[9] * 21;
    let score_10 = embedding[0] * 6 + embedding[1] * 36 + embedding[2] * 36 + embedding[3] * -33 + embedding[4] * -51 + embedding[5] * 5 + embedding[6] * -14 + embedding[7] * -7 + embedding[8] * 1 + embedding[9] * 38;
    let score_11 = embedding[0] * 8 + embedding[1] * 7 + embedding[2] * 35 + embedding[3] * -7 + embedding[4] * -58 + embedding[5] * 36 + embedding[6] * -17 + embedding[7] * 24 + embedding[8] * 15 + embedding[9] * 21;
    let score_12 = embedding[0] * 21 + embedding[1] * -37 + embedding[2] * 9 + embedding[3] * 88 + embedding[4] * 12 + embedding[5] * -1 + embedding[6] * 32 + embedding[8] * -13 + embedding[9] * -31;
    let score_13 = embedding[0] * -33 + embedding[1] * -17 + embedding[2] * -36 + embedding[3] * 25 + embedding[4] * 17 + embedding[5] * -76 + embedding[6] * 26 + embedding[7] * -11 + embedding[8] * -47 + embedding[9] * -7;
    let score_14 = embedding[0] * -37 + embedding[1] * -11 + embedding[2] * -29 + embedding[3] * 60 + embedding[4] * 10 + embedding[5] * -46 + embedding[6] * 23 + embedding[7] * -12 + embedding[8] * 1 + embedding[9] * -26;
    let score_15 = embedding[1] * -13 + embedding[2] * 12 + embedding[3] * 22 + embedding[4] * -1 + embedding[5] * 14 + embedding[6] * -19 + embedding[7] * 78 + embedding[8] * 24 + embedding[9] * -6;
    let score_16 = embedding[0] * -28 + embedding[1] * 24 + embedding[2] * 53 + embedding[3] * -18 + embedding[4] * -21 + embedding[5] * 15 + embedding[6] * 8 + embedding[7] * -1 + embedding[8] * 20 + embedding[9] * 1;
    let score_17 = embedding[0] * 11 + embedding[1] * 3 + embedding[2] * -18 + embedding[3] * 20 + embedding[4] * 12 + embedding[5] * -1 + embedding[6] * -4 + embedding[7] * 4 + embedding[8] * 7 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 10;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 17;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 7;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 9;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 36;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 25;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 16;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 20;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 13;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 18;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 11;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 12;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 52;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 94;
    }
    best_palette
}

fn predict_pixel_131(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[2] * -11 + embedding[3] * -36 + embedding[4] * 10 + embedding[5] * 2 + embedding[6] * -11 + embedding[7] * -18 + embedding[8] * -11 + embedding[9] * 7;
    let score_1 = embedding[0] * -4 + embedding[1] * -6 + embedding[2] * 12 + embedding[3] * 17 + embedding[4] * -5 + embedding[5] * 32 + embedding[6] * -12 + embedding[7] * 35 + embedding[8] * 37 + embedding[9] * -7;
    let score_2 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * -24 + embedding[3] * -12 + embedding[4] * 18 + embedding[5] * -16 + embedding[6] * -13 + embedding[7] * 17 + embedding[8] * 2 + embedding[9] * 2;
    let score_3 = embedding[0] * 4 + embedding[1] * -23 + embedding[2] * 13 + embedding[3] * -14 + embedding[4] * 4 + embedding[5] * 3 + embedding[6] * 10 + embedding[7] * -17 + embedding[8] * -32 + embedding[9] * 2;
    let score_4 = embedding[0] * 27 + embedding[1] * 7 + embedding[2] * -11 + embedding[3] * 112 + embedding[4] * -13 + embedding[5] * -19 + embedding[6] * 34 + embedding[7] * -15 + embedding[8] * 13 + embedding[9] * -23;
    let score_5 = embedding[0] * 45 + embedding[1] * 20 + embedding[2] * 42 + embedding[3] * 29 + embedding[4] * 2 + embedding[5] * 24 + embedding[6] * -13 + embedding[7] * 48 + embedding[8] * 21 + embedding[9] * 3;
    let score_6 = embedding[0] * -5 + embedding[1] * -24 + embedding[2] * -9 + embedding[3] * 11 + embedding[4] * 8 + embedding[5] * -17 + embedding[6] * 23 + embedding[7] * -24 + embedding[8] * -34 + embedding[9] * -7;
    let score_7 = embedding[0] * 7 + embedding[1] * -8 + embedding[2] * -4 + embedding[3] * 73 + embedding[4] * 1 + embedding[5] * 5 + embedding[6] * 16 + embedding[7] * 10 + embedding[8] * 22 + embedding[9] * -23;
    let score_8 = embedding[0] * 14 + embedding[1] * 22 + embedding[2] * 27 + embedding[3] * -35 + embedding[4] * -9 + embedding[5] * 5 + embedding[6] * -19 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 23;
    let score_9 = embedding[0] * -41 + embedding[1] * 19 + embedding[2] * -26 + embedding[3] * -16 + embedding[4] * -5 + embedding[5] * -63 + embedding[6] * 11 + embedding[7] * -19 + embedding[8] * -27 + embedding[9] * 18;
    let score_10 = embedding[0] * 3 + embedding[1] * 36 + embedding[2] * 28 + embedding[3] * -25 + embedding[4] * -47 + embedding[6] * -10 + embedding[7] * -7 + embedding[8] * -2 + embedding[9] * 33;
    let score_11 = embedding[0] * 14 + embedding[1] * 3 + embedding[2] * 38 + embedding[3] * 1 + embedding[4] * -45 + embedding[5] * 33 + embedding[6] * -16 + embedding[7] * 34 + embedding[8] * 15 + embedding[9] * 16;
    let score_12 = embedding[0] * 31 + embedding[1] * -27 + embedding[2] * 22 + embedding[3] * 79 + embedding[4] * 4 + embedding[5] * -5 + embedding[6] * 32 + embedding[7] * -1 + embedding[8] * -23 + embedding[9] * -20;
    let score_13 = embedding[0] * -30 + embedding[1] * -14 + embedding[2] * -28 + embedding[3] * 21 + embedding[4] * 8 + embedding[5] * -65 + embedding[6] * 29 + embedding[7] * -17 + embedding[8] * -47 + embedding[9] * -1;
    let score_14 = embedding[0] * -40 + embedding[1] * -3 + embedding[2] * -25 + embedding[3] * 15 + embedding[4] * 9 + embedding[5] * -50 + embedding[6] * 14 + embedding[7] * -14 + embedding[8] * -13 + embedding[9] * -8;
    let score_15 = embedding[0] * 17 + embedding[1] * -6 + embedding[2] * 25 + embedding[3] * 26 + embedding[4] * -2 + embedding[5] * 18 + embedding[6] * -23 + embedding[7] * 84 + embedding[8] * 27 + embedding[9] * -4;
    let score_16 = embedding[0] * -27 + embedding[1] * 21 + embedding[2] * 42 + embedding[3] * -26 + embedding[4] * -11 + embedding[5] * 3 + embedding[6] * 7 + embedding[7] * -4 + embedding[8] * 6 + embedding[9] * 3;
    let score_17 = embedding[0] * 10 + embedding[1] * 7 + embedding[2] * -12 + embedding[3] * 13 + embedding[4] * 8 + embedding[5] * 3 + embedding[6] * -8 + embedding[7] * 12 + embedding[8] * 8 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 9;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 10;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 17;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 7;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 36;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 132;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 16;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 44;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 13;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 18;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 11;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 12;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 52;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 94;
    }
    best_palette
}

fn predict_pixel_132(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * -6 + embedding[3] * -37 + embedding[4] * 9 + embedding[5] * -3 + embedding[6] * -9 + embedding[7] * -22 + embedding[8] * -11 + embedding[9] * 12;
    let score_1 = embedding[0] * -6 + embedding[1] * -16 + embedding[2] * 20 + embedding[3] * 7 + embedding[4] * -11 + embedding[5] * 41 + embedding[6] * -9 + embedding[7] * 23 + embedding[8] * 28 + embedding[9] * -5;
    let score_2 = embedding[0] * -9 + embedding[1] * 10 + embedding[2] * -24 + embedding[3] * -6 + embedding[4] * 21 + embedding[5] * -26 + embedding[6] * -9 + embedding[7] * 15 + embedding[9] * 2;
    let score_3 = embedding[0] * -15 + embedding[1] * -15 + embedding[2] * -20 + embedding[3] * 4 + embedding[4] * 6 + embedding[5] * -14 + embedding[6] * 12 + embedding[7] * -27 + embedding[8] * -15 + embedding[9] * -8;
    let score_4 = embedding[0] * -12 + embedding[1] * -34 + embedding[2] * -12 + embedding[3] * 69 + embedding[4] * 17 + embedding[5] * -4 + embedding[6] * 24 + embedding[7] * 6 + embedding[8] * 1 + embedding[9] * -32;
    let score_5 = embedding[0] * 4 + embedding[1] * -9 + embedding[2] * 8 + embedding[3] * -10 + embedding[5] * 27 + embedding[6] * -11 + embedding[7] * 14 + embedding[8] * 7 + embedding[9] * -1;
    let score_6 = embedding[0] * 15 + embedding[1] * -11 + embedding[2] * -16 + embedding[3] * 95 + embedding[4] * -1 + embedding[5] * -18 + embedding[6] * 35 + embedding[7] * -20 + embedding[9] * -27;
    let score_7 = embedding[0] * 47 + embedding[1] * 21 + embedding[2] * 45 + embedding[3] * 23 + embedding[4] * -4 + embedding[5] * 22 + embedding[6] * -12 + embedding[7] * 40 + embedding[8] * 17 + embedding[9] * 6;
    let score_8 = embedding[0] * 18 + embedding[1] * 28 + embedding[2] * 30 + embedding[3] * -35 + embedding[4] * -13 + embedding[5] * -1 + embedding[6] * -18 + embedding[7] * 13 + embedding[8] * -5 + embedding[9] * 27;
    let score_9 = embedding[0] * -35 + embedding[1] * 23 + embedding[2] * -21 + embedding[3] * -16 + embedding[4] * -5 + embedding[5] * -67 + embedding[6] * 10 + embedding[7] * -23 + embedding[8] * -28 + embedding[9] * 22;
    let score_10 = embedding[0] * 8 + embedding[1] * 42 + embedding[2] * 34 + embedding[3] * -25 + embedding[4] * -51 + embedding[5] * -4 + embedding[6] * -9 + embedding[7] * -11 + embedding[8] * -2 + embedding[9] * 38;
    let score_11 = embedding[0] * 10 + embedding[1] * -2 + embedding[2] * 39 + embedding[3] * 8 + embedding[4] * -53 + embedding[5] * 42 + embedding[6] * -12 + embedding[7] * 30 + embedding[8] * 19 + embedding[9] * 12;
    let score_12 = embedding[0] * 24 + embedding[1] * -34 + embedding[2] * 9 + embedding[3] * 103 + embedding[4] * 8 + embedding[5] * -4 + embedding[6] * 40 + embedding[7] * 1 + embedding[8] * -12 + embedding[9] * -35;
    let score_13 = embedding[0] * -35 + embedding[1] * -16 + embedding[2] * -31 + embedding[3] * 25 + embedding[4] * 13 + embedding[5] * -67 + embedding[6] * 23 + embedding[7] * -14 + embedding[8] * -38 + embedding[9] * -6;
    let score_14 = embedding[0] * -37 + embedding[1] * -28 + embedding[2] * -26 + embedding[3] * 50 + embedding[4] * 12 + embedding[5] * -45 + embedding[6] * 27 + embedding[7] * -14 + embedding[8] * -13 + embedding[9] * -24;
    let score_15 = embedding[0] * 1 + embedding[1] * -22 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * 1 + embedding[5] * 22 + embedding[6] * -15 + embedding[7] * 75 + embedding[8] * 17 + embedding[9] * -10;
    let score_16 = embedding[0] * -23 + embedding[1] * 23 + embedding[2] * 46 + embedding[3] * -26 + embedding[4] * -15 + embedding[5] * 3 + embedding[6] * 8 + embedding[7] * -9 + embedding[8] * 4 + embedding[9] * 7;
    let score_17 = embedding[0] * 14 + embedding[1] * 8 + embedding[2] * -9 + embedding[3] * 10 + embedding[4] * 8 + embedding[5] * -3 + embedding[6] * -7 + embedding[7] * 5 + embedding[8] * 6 + embedding[9] * -1;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 9;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 10;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 17;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 36;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 25;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 16;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 20;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 13;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 18;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 11;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 12;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 52;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 94;
    }
    best_palette
}

fn predict_pixel_133(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -4 + embedding[1] * 1 + embedding[2] * -9 + embedding[3] * -31 + embedding[4] * 8 + embedding[5] * 4 + embedding[6] * -10 + embedding[7] * -20 + embedding[8] * -5 + embedding[9] * 7;
    let score_1 = embedding[0] * -7 + embedding[1] * -11 + embedding[2] * 2 + embedding[3] * -2 + embedding[5] * 26 + embedding[6] * -12 + embedding[7] * 8 + embedding[8] * 21 + embedding[9] * -4;
    let score_2 = embedding[0] * -11 + embedding[1] * -24 + embedding[2] * -15 + embedding[3] * 48 + embedding[4] * 16 + embedding[5] * -4 + embedding[6] * 17 + embedding[7] * 2 + embedding[8] * 4 + embedding[9] * -23;
    let score_3 = embedding[0] * 3 + embedding[1] * 13 + embedding[2] * -26 + embedding[3] * 18 + embedding[4] * 19 + embedding[5] * -22 + embedding[6] * -4 + embedding[7] * 17 + embedding[8] * 7 + embedding[9] * -2;
    let score_4 = embedding[0] * 41 + embedding[1] * 20 + embedding[2] * 41 + embedding[3] * 7 + embedding[4] * 2 + embedding[5] * 19 + embedding[6] * -13 + embedding[7] * 33 + embedding[8] * 12 + embedding[9] * 10;
    let score_5 = embedding[0] * 13 + embedding[1] * -12 + embedding[2] * 80 + embedding[3] * -15 + embedding[4] * -4 + embedding[5] * 23 + embedding[7] * 32 + embedding[8] * -29 + embedding[9] * 11;
    let score_6 = embedding[0] * 23 + embedding[1] * 20 + embedding[2] * 37 + embedding[3] * -31 + embedding[4] * -8 + embedding[5] * 9 + embedding[6] * -19 + embedding[7] * 21 + embedding[8] * -4 + embedding[9] * 24;
    let score_7 = embedding[0] * 12 + embedding[1] * -4 + embedding[2] * -18 + embedding[3] * 69 + embedding[4] * 2 + embedding[5] * -20 + embedding[6] * 25 + embedding[7] * -18 + embedding[8] * -3 + embedding[9] * -16;
    let score_8 = embedding[0] * -36 + embedding[1] * 19 + embedding[2] * -24 + embedding[3] * -12 + embedding[4] * -6 + embedding[5] * -59 + embedding[6] * 11 + embedding[7] * -21 + embedding[8] * -23 + embedding[9] * 18;
    let score_9 = embedding[0] * 7 + embedding[1] * 34 + embedding[2] * 26 + embedding[3] * -21 + embedding[4] * -48 + embedding[5] * 1 + embedding[6] * -7 + embedding[7] * -11 + embedding[8] * 1 + embedding[9] * 33;
    let score_10 = embedding[0] * 13 + embedding[1] * 2 + embedding[2] * 39 + embedding[3] * -8 + embedding[4] * -43 + embedding[5] * 33 + embedding[6] * -15 + embedding[7] * 26 + embedding[8] * 6 + embedding[9] * 19;
    let score_11 = embedding[0] * 22 + embedding[1] * -28 + embedding[2] * 12 + embedding[3] * 88 + embedding[4] * 8 + embedding[5] * -2 + embedding[6] * 32 + embedding[7] * 5 + embedding[8] * -12 + embedding[9] * -27;
    let score_12 = embedding[0] * -23 + embedding[1] * -7 + embedding[2] * -15 + embedding[3] * -10 + embedding[4] * -1 + embedding[5] * -6 + embedding[6] * 5 + embedding[7] * -19 + embedding[8] * -2 + embedding[9] * -1;
    let score_13 = embedding[0] * -30 + embedding[1] * -5 + embedding[2] * -28 + embedding[3] * 15 + embedding[4] * 10 + embedding[5] * -63 + embedding[6] * 16 + embedding[7] * -9 + embedding[8] * -33 + embedding[9] * 1;
    let score_14 = embedding[0] * -42 + embedding[1] * -15 + embedding[2] * -21 + embedding[3] * 33 + embedding[4] * 5 + embedding[5] * -40 + embedding[6] * 22 + embedding[7] * -12 + embedding[8] * -3 + embedding[9] * -18;
    let score_15 = embedding[0] * 10 + embedding[1] * -7 + embedding[2] * -9 + embedding[3] * 72 + embedding[4] * -10 + embedding[5] * -2 + embedding[6] * 24 + embedding[7] * -9 + embedding[8] * 12 + embedding[9] * -19;
    let score_16 = embedding[0] * -1 + embedding[1] * -19 + embedding[2] * 19 + embedding[3] * 16 + embedding[4] * 3 + embedding[5] * 15 + embedding[6] * -16 + embedding[7] * 68 + embedding[8] * 12 + embedding[9] * -6;
    let score_17 = embedding[0] * -24 + embedding[1] * 16 + embedding[2] * 37 + embedding[3] * -21 + embedding[4] * -11 + embedding[5] * 6 + embedding[6] * 7 + embedding[7] * -10 + embedding[8] * 8 + embedding[9] * 1;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 17;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 9;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 36;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 98;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 25;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 16;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 44;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 13;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 7;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 18;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 11;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 10;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 12;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 52;
    }
    best_palette
}

fn predict_pixel_134(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -4 + embedding[2] * -11 + embedding[3] * -32 + embedding[4] * 4 + embedding[5] * 7 + embedding[6] * -10 + embedding[7] * -25 + embedding[8] * -5 + embedding[9] * 6;
    let score_1 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_2 = embedding[0] * -13 + embedding[1] * 23 + embedding[2] * -39 + embedding[3] * 14 + embedding[4] * 28 + embedding[5] * -54 + embedding[6] * -4 + embedding[7] * 48 + embedding[8] * 6 + embedding[9] * 3;
    let score_3 = embedding[0] * 18 + embedding[1] * -1 + embedding[2] * -17 + embedding[3] * 70 + embedding[4] * -7 + embedding[5] * -8 + embedding[6] * 20 + embedding[7] * -19 + embedding[8] * 7 + embedding[9] * -17;
    let score_4 = embedding[0] * 41 + embedding[1] * 18 + embedding[2] * 29 + embedding[3] * 25 + embedding[4] * 4 + embedding[5] * 14 + embedding[6] * -7 + embedding[7] * 23 + embedding[8] * 13 + embedding[9] * 1;
    let score_5 = embedding[0] * -16 + embedding[1] * -37 + embedding[2] * -6 + embedding[3] * 44 + embedding[4] * 12 + embedding[5] * 15 + embedding[6] * 11 + embedding[7] * 27 + embedding[8] * -1 + embedding[9] * -24;
    let score_6 = embedding[0] * 9 + embedding[1] * -15 + embedding[2] * 73 + embedding[3] * -14 + embedding[4] * -2 + embedding[5] * 20 + embedding[6] * 3 + embedding[7] * 28 + embedding[8] * -29 + embedding[9] * 6;
    let score_7 = embedding[0] * 20 + embedding[1] * 20 + embedding[2] * 28 + embedding[3] * -32 + embedding[4] * -3 + embedding[5] * -1 + embedding[6] * -16 + embedding[7] * 13 + embedding[8] * -8 + embedding[9] * 21;
    let score_8 = embedding[0] * -32 + embedding[1] * 21 + embedding[2] * -19 + embedding[3] * -19 + embedding[4] * -14 + embedding[5] * -50 + embedding[6] * 8 + embedding[7] * -27 + embedding[8] * -20 + embedding[9] * 20;
    let score_9 = embedding[0] * 5 + embedding[1] * 37 + embedding[2] * 22 + embedding[3] * -20 + embedding[4] * -54 + embedding[5] * -10 + embedding[6] * 1 + embedding[7] * -23 + embedding[8] * -4 + embedding[9] * 34;
    let score_10 = embedding[0] * 24 + embedding[1] * -27 + embedding[2] * 9 + embedding[3] * 82 + embedding[4] * 5 + embedding[6] * 28 + embedding[7] * 2 + embedding[8] * -12 + embedding[9] * -26;
    let score_11 = embedding[0] * -17 + embedding[1] * -1 + embedding[2] * -25 + embedding[3] * 5 + embedding[4] * 10 + embedding[5] * -53 + embedding[6] * 14 + embedding[7] * -9 + embedding[8] * -36 + embedding[9] * 4;
    let score_12 = embedding[0] * -6 + embedding[1] * -24 + embedding[2] * 13 + embedding[3] * 23 + embedding[4] * 4 + embedding[5] * 16 + embedding[6] * -16 + embedding[7] * 78 + embedding[8] * 14 + embedding[9] * -12;
    let score_13 = embedding[0] * -27 + embedding[1] * 16 + embedding[2] * 34 + embedding[3] * -22 + embedding[4] * -12 + embedding[5] * 2 + embedding[6] * 12 + embedding[7] * -16 + embedding[8] * 6 + embedding[9] * -1;
    let score_14 = embedding[0] * 13 + embedding[1] * 7 + embedding[2] * -18 + embedding[3] * 18 + embedding[4] * 7 + embedding[5] * 1 + embedding[6] * -3 + embedding[7] * -2 + embedding[8] * 9 + embedding[9] * -6;
    let score_15 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_16 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * -12 + embedding[1] * -16 + embedding[2] * -7 + embedding[3] * 4 + embedding[4] * 5 + embedding[5] * 25 + embedding[6] * -12 + embedding[7] * 11 + embedding[8] * 23 + embedding[9] * -12;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 10;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 17;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 9;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 36;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 25;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 16;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 13;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 18;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 12;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 52;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 112;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_135(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 16 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 5;
    let score_1 = embedding[0] * -6 + embedding[1] * -1 + embedding[2] * 16 + embedding[3] * -45 + embedding[4] * -1 + embedding[5] * 10 + embedding[6] * -14 + embedding[7] * -9 + embedding[8] * -10 + embedding[9] * 13;
    let score_2 = embedding[0] * -9 + embedding[1] * 12 + embedding[2] * -31 + embedding[3] * 12 + embedding[4] * 34 + embedding[5] * -48 + embedding[6] * -3 + embedding[7] * 45 + embedding[8] * -2 + embedding[9] * -3;
    let score_3 = embedding[0] * 22 + embedding[1] * 11 + embedding[2] * -6 + embedding[3] * 61 + embedding[4] * -20 + embedding[5] * 6 + embedding[6] * 19 + embedding[7] * -29 + embedding[8] * 19 + embedding[9] * -12;
    let score_4 = embedding[0] * 41 + embedding[1] * 16 + embedding[2] * 26 + embedding[3] * 21 + embedding[4] * 1 + embedding[5] * 27 + embedding[6] * -5 + embedding[7] * 10 + embedding[8] * 17;
    let score_5 = embedding[0] * -35 + embedding[1] * 19 + embedding[2] * -10 + embedding[3] * -17 + embedding[4] * -11 + embedding[5] * -39 + embedding[6] * 6 + embedding[7] * -17 + embedding[8] * -15 + embedding[9] * 18;
    let score_6 = embedding[0] * 15 + embedding[1] * -15 + embedding[2] * 4 + embedding[3] * -7 + embedding[4] * 23 + embedding[5] * -8 + embedding[6] * -24 + embedding[7] * 68 + embedding[8] * -5 + embedding[9] * -10;
    let score_7 = embedding[0] * -13 + embedding[1] * -31 + embedding[2] * -4 + embedding[3] * 19 + embedding[4] * 21 + embedding[5] * -17 + embedding[6] * -7 + embedding[7] * 70 + embedding[8] * -9 + embedding[9] * -19;
    let score_8 = embedding[0] * -24 + embedding[1] * -2 + embedding[2] * -30 + embedding[3] * 21 + embedding[4] * 15 + embedding[5] * -56 + embedding[6] * 14 + embedding[7] * 2 + embedding[8] * -24 + embedding[9] * -9;
    let score_9 = embedding[0] * -34 + embedding[1] * -1 + embedding[2] * -23 + embedding[3] * 19 + embedding[4] * 6 + embedding[5] * -34 + embedding[6] * 12 + embedding[7] * -14 + embedding[8] * 4 + embedding[9] * -12;
    let score_10 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 16 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 5;
    let score_11 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 16 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 5;
    let score_12 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 16 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 5;
    let score_13 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 16 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 5;
    let score_14 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 16 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 5;
    let score_15 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 16 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 5;
    let score_16 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 16 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -14 + embedding[9] * 5;
    let score_17 = embedding[0] * 6 + embedding[1] * -3 + embedding[2] * -8 + embedding[3] * -8 + embedding[4] * 6 + embedding[5] * 31 + embedding[6] * -9 + embedding[7] * -21 + embedding[8] * 15 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 10;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 17;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 25;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 18;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 11;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_136(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 11 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_1 = embedding[0] * -10 + embedding[1] * 1 + embedding[2] * 4 + embedding[3] * -35 + embedding[4] * -4 + embedding[5] * -9 + embedding[6] * -10 + embedding[7] * -3 + embedding[8] * -12 + embedding[9] * 16;
    let score_2 = embedding[0] * 24 + embedding[1] * 16 + embedding[2] * -9 + embedding[3] * 16 + embedding[4] * 25 + embedding[5] * -24 + embedding[6] * -3 + embedding[7] * 22 + embedding[8] * 2 + embedding[9] * -1;
    let score_3 = embedding[0] * -21 + embedding[1] * 19 + embedding[2] * -13 + embedding[3] * -13 + embedding[4] * -11 + embedding[5] * -27 + embedding[6] * 1 + embedding[7] * -18 + embedding[8] * -4 + embedding[9] * 14;
    let score_4 = embedding[0] * 21 + embedding[1] * -9 + embedding[2] * -2 + embedding[3] * 5 + embedding[4] * 21 + embedding[5] * 1 + embedding[6] * -18 + embedding[7] * 48 + embedding[8] * 2 + embedding[9] * -9;
    let score_5 = embedding[0] * -6 + embedding[1] * -30 + embedding[2] * -2 + embedding[3] * 17 + embedding[4] * 16 + embedding[5] * 1 + embedding[6] * -3 + embedding[7] * 44 + embedding[8] * -3 + embedding[9] * -17;
    let score_6 = embedding[0] * -7 + embedding[1] * 11 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_7 = embedding[0] * -7 + embedding[1] * 11 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_8 = embedding[0] * -7 + embedding[1] * 11 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_9 = embedding[0] * -7 + embedding[1] * 11 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_10 = embedding[0] * -7 + embedding[1] * 11 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_11 = embedding[0] * -7 + embedding[1] * 11 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_12 = embedding[0] * -7 + embedding[1] * 11 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 11 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_14 = embedding[0] * -7 + embedding[1] * 11 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_15 = embedding[0] * -7 + embedding[1] * 11 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_16 = embedding[0] * -7 + embedding[1] * 11 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_17 = embedding[0] * 8 + embedding[1] * -1 + embedding[2] * -15 + embedding[3] * -5 + embedding[4] * 10 + embedding[5] * 27 + embedding[6] * -9 + embedding[7] * -26 + embedding[8] * 18 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 25;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_137(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_1 = embedding[0] * -3 + embedding[2] * -9 + embedding[3] * -19 + embedding[4] * 13 + embedding[5] * -9 + embedding[6] * -11 + embedding[7] * -8 + embedding[8] * -4;
    let score_2 = embedding[0] * 3 + embedding[1] * 11 + embedding[2] * -22 + embedding[3] * -2 + embedding[4] * 23 + embedding[5] * -30 + embedding[6] * -11 + embedding[7] * 27 + embedding[8] * 6 + embedding[9] * -1;
    let score_3 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_4 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_5 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_6 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_7 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_8 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_9 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_10 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_11 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_12 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_13 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_14 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_15 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_16 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -11 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -10 + embedding[2] * -10 + embedding[3] * -16 + embedding[4] * 13 + embedding[5] * 22 + embedding[6] * -13 + embedding[7] * -21 + embedding[8] * 11 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_138(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_1 = embedding[0] * -3 + embedding[1] * -6 + embedding[2] * 3 + embedding[3] * -25 + embedding[4] * 13 + embedding[5] * -6 + embedding[6] * -15 + embedding[7] * 14 + embedding[8] * -5;
    let score_2 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * -20 + embedding[3] * -3 + embedding[4] * 10 + embedding[5] * -22 + embedding[6] * -12 + embedding[7] * 27 + embedding[8] * 9;
    let score_3 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_4 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_5 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_6 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_7 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_8 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_9 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_10 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_11 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_12 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_13 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_14 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_15 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_16 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 7;
    let score_17 = embedding[0] * 12 + embedding[1] * -2 + embedding[2] * -14 + embedding[3] * -12 + embedding[4] * 14 + embedding[5] * 19 + embedding[6] * -11 + embedding[7] * -23 + embedding[8] * 13 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_139(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_1 = embedding[0] * -5 + embedding[1] * 2 + embedding[2] * -21 + embedding[3] * -2 + embedding[4] * 11 + embedding[5] * -24 + embedding[6] * -14 + embedding[7] * 28 + embedding[8] * 7;
    let score_2 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_3 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_4 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_5 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_6 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_7 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_8 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_9 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_10 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_11 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_12 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -4 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * 13 + embedding[1] * -6 + embedding[2] * -13 + embedding[3] * -15 + embedding[4] * 18 + embedding[5] * 18 + embedding[6] * -15 + embedding[7] * -21 + embedding[8] * 10 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 8;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_140(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_141(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_142(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 22 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_143(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_144(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_145(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_146(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_1 = embedding[0] * 17 + embedding[1] * 11 + embedding[2] * -11 + embedding[3] * 12 + embedding[4] * 26 + embedding[5] * -35 + embedding[6] * -2 + embedding[7] * 18 + embedding[8] * -2 + embedding[9] * -1;
    let score_2 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_3 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_4 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_5 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_6 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_7 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_8 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_9 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_10 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_11 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 4;
    let score_12 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_13 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 4;
    let score_14 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 4;
    let score_15 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_16 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 5;
    let score_17 = embedding[0] * 2 + embedding[1] * -14 + embedding[2] * -16 + embedding[3] * -18 + embedding[4] * 16 + embedding[5] * 21 + embedding[6] * -14 + embedding[7] * -25 + embedding[8] * 8 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 8;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_147(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_1 = embedding[0] * -1 + embedding[1] * -5 + embedding[2] * -1 + embedding[3] * -21 + embedding[4] * 18 + embedding[5] * -13 + embedding[6] * -10 + embedding[7] * 8 + embedding[8] * -12 + embedding[9] * 1;
    let score_2 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_3 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_4 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_5 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_6 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_7 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_8 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_9 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_10 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_11 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_12 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_13 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_14 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_15 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_16 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 11;
    let score_17 = embedding[0] * 9 + embedding[2] * -19 + embedding[3] * -15 + embedding[4] * 15 + embedding[5] * 17 + embedding[6] * -16 + embedding[7] * -19 + embedding[8] * 17 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_148(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_1 = embedding[0] * -16 + embedding[1] * -10 + embedding[2] * -6 + embedding[3] * -27 + embedding[4] * 13 + embedding[5] * -6 + embedding[6] * -10 + embedding[7] * 12 + embedding[8] * -7 + embedding[9] * 1;
    let score_2 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_3 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_4 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_5 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_6 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_7 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_8 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_9 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_10 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_11 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_12 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_13 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_14 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_15 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 12 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_16 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 11;
    let score_17 = embedding[0] * 15 + embedding[1] * 2 + embedding[2] * -17 + embedding[3] * -12 + embedding[4] * 17 + embedding[5] * 15 + embedding[6] * -15 + embedding[7] * -22 + embedding[8] * 17 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_149(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_1 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * -5 + embedding[3] * -31 + embedding[4] * -2 + embedding[5] * 4 + embedding[6] * -7 + embedding[7] * -20 + embedding[8] * 2 + embedding[9] * 6;
    let score_2 = embedding[0] * 1 + embedding[1] * 18 + embedding[2] * -22 + embedding[3] * -1 + embedding[4] * 27 + embedding[5] * -35 + embedding[6] * -13 + embedding[7] * 45 + embedding[8] * 7;
    let score_3 = embedding[0] * 25 + embedding[1] * -16 + embedding[2] * 17 + embedding[3] * 41 + embedding[4] * 8 + embedding[5] * 2 + embedding[6] * 13 + embedding[7] * -2 + embedding[8] * -9 + embedding[9] * -11;
    let score_4 = embedding[0] * 14 + embedding[1] * -10 + embedding[2] * -2 + embedding[3] * -1 + embedding[4] * 24 + embedding[5] * -9 + embedding[6] * -15 + embedding[7] * 54 + embedding[8] * -3 + embedding[9] * -9;
    let score_5 = embedding[0] * -10 + embedding[1] * -24 + embedding[2] * -1 + embedding[3] * 10 + embedding[4] * 23 + embedding[5] * -14 + embedding[6] * -6 + embedding[7] * 65 + embedding[8] * -9 + embedding[9] * -15;
    let score_6 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_7 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_8 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_9 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_10 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_11 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_12 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_14 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_15 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_16 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_17 = embedding[0] * 4 + embedding[1] * 3 + embedding[2] * -11 + embedding[3] * -19 + embedding[4] * 5 + embedding[5] * 22 + embedding[6] * -9 + embedding[7] * -30 + embedding[8] * 15 + embedding[9] * -3;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_150(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_1 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 11 + embedding[3] * -42 + embedding[4] * 2 + embedding[5] * 5 + embedding[6] * -15 + embedding[7] * -6 + embedding[8] * -8 + embedding[9] * 12;
    let score_2 = embedding[0] * 21 + embedding[1] * 7 + embedding[2] * -10 + embedding[3] * 61 + embedding[4] * -7 + embedding[5] * -3 + embedding[6] * 19 + embedding[7] * -23 + embedding[8] * 11 + embedding[9] * -15;
    let score_3 = embedding[0] * 21 + embedding[1] * 17 + embedding[2] * -8 + embedding[3] * 26 + embedding[4] * 24 + embedding[5] * -28 + embedding[6] * 1 + embedding[7] * 27 + embedding[8] * 2 + embedding[9] * -3;
    let score_4 = embedding[0] * -35 + embedding[1] * 25 + embedding[2] * -11 + embedding[3] * -25 + embedding[4] * -14 + embedding[5] * -46 + embedding[6] * 1 + embedding[7] * -12 + embedding[8] * -13 + embedding[9] * 25;
    let score_5 = embedding[0] * 27 + embedding[1] * -21 + embedding[2] * 19 + embedding[3] * 56 + embedding[4] * 10 + embedding[5] * 7 + embedding[6] * 16 + embedding[7] * 6 + embedding[8] * -11 + embedding[9] * -18;
    let score_6 = embedding[0] * 17 + embedding[1] * -16 + embedding[2] * 2 + embedding[3] * 10 + embedding[4] * 22 + embedding[5] * 5 + embedding[6] * -15 + embedding[7] * 48 + embedding[8] * -1 + embedding[9] * -13;
    let score_7 = embedding[0] * -6 + embedding[1] * -32 + embedding[2] * 4 + embedding[3] * 20 + embedding[4] * 21 + embedding[5] * 3 + embedding[6] * -1 + embedding[7] * 51 + embedding[8] * -8 + embedding[9] * -20;
    let score_8 = embedding[0] * -13 + embedding[1] * 17 + embedding[2] * -1 + embedding[3] * -36 + embedding[4] * -2 + embedding[5] * -24 + embedding[6] * -9 + embedding[7] * 2 + embedding[8] * -13 + embedding[9] * 21;
    let score_9 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_10 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_11 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_12 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * 4 + embedding[2] * -16 + embedding[3] * -2 + embedding[4] * 10 + embedding[5] * 26 + embedding[6] * -10 + embedding[7] * -19 + embedding[8] * 20 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 10;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 25;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 13;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 81;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_151(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 2 + embedding[1] * -4 + embedding[2] * 3 + embedding[3] * -31 + embedding[4] * 4 + embedding[5] * 8 + embedding[6] * -8 + embedding[7] * -28 + embedding[8] * -9 + embedding[9] * 7;
    let score_1 = embedding[0] * -4 + embedding[1] * 6 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_2 = embedding[0] * -17 + embedding[1] * 15 + embedding[2] * -38 + embedding[3] * 16 + embedding[4] * 37 + embedding[5] * -59 + embedding[6] * -4 + embedding[7] * 51 + embedding[8] * -1 + embedding[9] * -6;
    let score_3 = embedding[0] * 46 + embedding[1] * 14 + embedding[2] * 45 + embedding[3] * 22 + embedding[4] * 3 + embedding[5] * 24 + embedding[6] * -10 + embedding[7] * 31 + embedding[8] * 13 + embedding[9] * 3;
    let score_4 = embedding[0] * -24 + embedding[1] * -24 + embedding[2] * -25 + embedding[3] * 48 + embedding[4] * 7 + embedding[5] * 5 + embedding[6] * 18 + embedding[7] * 5 + embedding[8] * 1 + embedding[9] * -23;
    let score_5 = embedding[0] * 18 + embedding[1] * -18 + embedding[2] * 77 + embedding[3] * -10 + embedding[4] * 6 + embedding[5] * 19 + embedding[7] * 31 + embedding[8] * -37 + embedding[9] * 5;
    let score_6 = embedding[0] * 25 + embedding[1] * 16 + embedding[2] * 42 + embedding[3] * -31 + embedding[4] * -7 + embedding[5] * 11 + embedding[6] * -14 + embedding[7] * 10 + embedding[8] * -7 + embedding[9] * 21;
    let score_7 = embedding[0] * 16 + embedding[1] * -7 + embedding[2] * -11 + embedding[3] * 54 + embedding[4] * 2 + embedding[5] * -8 + embedding[6] * 17 + embedding[7] * -18 + embedding[9] * -16;
    let score_8 = embedding[0] * -38 + embedding[1] * 19 + embedding[2] * -18 + embedding[3] * -13 + embedding[4] * -12 + embedding[5] * -59 + embedding[6] * 15 + embedding[7] * -25 + embedding[8] * -24 + embedding[9] * 20;
    let score_9 = embedding[0] * 9 + embedding[1] * 31 + embedding[2] * 36 + embedding[3] * -22 + embedding[4] * -50 + embedding[6] * -2 + embedding[7] * -20 + embedding[8] * -2 + embedding[9] * 32;
    let score_10 = embedding[0] * 27 + embedding[1] * -31 + embedding[2] * 19 + embedding[3] * 66 + embedding[4] * 14 + embedding[5] * 6 + embedding[6] * 19 + embedding[7] * 12 + embedding[8] * -16 + embedding[9] * -27;
    let score_11 = embedding[0] * 22 + embedding[1] * -22 + embedding[2] * 14 + embedding[3] * 7 + embedding[4] * 24 + embedding[5] * -6 + embedding[6] * -24 + embedding[7] * 74 + embedding[8] * -9 + embedding[9] * -11;
    let score_12 = embedding[0] * -26 + embedding[1] * -7 + embedding[2] * -32 + embedding[3] * 21 + embedding[4] * 14 + embedding[5] * -63 + embedding[6] * 19 + embedding[7] * -5 + embedding[8] * -29 + embedding[9] * -6;
    let score_13 = embedding[0] * -44 + embedding[2] * -30 + embedding[3] * 44 + embedding[4] * -1 + embedding[5] * -38 + embedding[6] * 17 + embedding[7] * -10 + embedding[8] * 9 + embedding[9] * -18;
    let score_14 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * -2 + embedding[3] * -30 + embedding[4] * -6 + embedding[5] * -22 + embedding[6] * 1 + embedding[7] * -18 + embedding[8] * -20 + embedding[9] * 19;
    let score_15 = embedding[0] * 12 + embedding[1] * -2 + embedding[2] * -13 + embedding[3] * 20 + embedding[4] * 12 + embedding[5] * 4 + embedding[6] * -8 + embedding[7] * 8 + embedding[8] * 9 + embedding[9] * -8;
    let score_16 = embedding[0] * -4 + embedding[1] * 6 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * -17 + embedding[1] * 5 + embedding[2] * -16 + embedding[3] * 10 + embedding[4] * -19 + embedding[5] * 34 + embedding[6] * -10 + embedding[7] * 14 + embedding[8] * 44 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 17;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 9;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 50;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 10;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 25;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 16;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 13;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 12;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 18;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 11;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 81;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 101;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_152(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -25 + embedding[1] * -3 + embedding[2] * -10 + embedding[3] * 4 + embedding[4] * -11 + embedding[5] * 24 + embedding[6] * -12 + embedding[7] * 13 + embedding[8] * 34 + embedding[9] * -4;
    let score_1 = embedding[0] * -21 + embedding[1] * 1 + embedding[2] * 2 + embedding[3] * -41 + embedding[4] * -8 + embedding[5] * 9 + embedding[6] * -13 + embedding[7] * -12 + embedding[8] * 1 + embedding[9] * 16;
    let score_2 = embedding[0] * -2 + embedding[1] * -22 + embedding[2] * -5 + embedding[3] * -5 + embedding[4] * 16 + embedding[5] * -16 + embedding[6] * 22 + embedding[7] * -14 + embedding[8] * -11 + embedding[9] * 7;
    let score_3 = embedding[0] * -1 + embedding[1] * -10 + embedding[2] * -10 + embedding[3] * -3 + embedding[4] * 5 + embedding[5] * -10 + embedding[6] * 7 + embedding[7] * -8 + embedding[8] * -33 + embedding[9] * -12;
    let score_4 = embedding[0] * -11 + embedding[1] * -19 + embedding[2] * -14 + embedding[3] * 22 + embedding[4] * 23 + embedding[5] * -27 + embedding[6] * 11 + embedding[7] * -14 + embedding[8] * -42 + embedding[9] * 4;
    let score_5 = embedding[0] * -19 + embedding[1] * 22 + embedding[2] * -34 + embedding[3] * -13 + embedding[4] * 17 + embedding[5] * -52 + embedding[6] * -5 + embedding[7] * 19 + embedding[8] * -12 + embedding[9] * 15;
    let score_6 = embedding[0] * 30 + embedding[1] * 14 + embedding[2] * -12 + embedding[3] * 110 + embedding[4] * -25 + embedding[5] * -12 + embedding[6] * 30 + embedding[7] * -16 + embedding[8] * 22 + embedding[9] * -21;
    let score_7 = embedding[0] * -8 + embedding[1] * 36 + embedding[2] * -15 + embedding[3] * -7 + embedding[4] * -8 + embedding[5] * -32 + embedding[6] * -2 + embedding[7] * -8 + embedding[9] * 20;
    let score_8 = embedding[0] * 53 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 64 + embedding[4] * 5 + embedding[5] * 17 + embedding[6] * 1 + embedding[7] * 25 + embedding[8] * 9 + embedding[9] * -15;
    let score_9 = embedding[0] * 27 + embedding[1] * -8 + embedding[2] * 14 + embedding[3] * 25 + embedding[4] * -12 + embedding[5] * 21 + embedding[6] * 3 + embedding[7] * -6 + embedding[9] * -9;
    let score_10 = embedding[1] * -2 + embedding[2] * -13 + embedding[3] * 54 + embedding[4] * -6 + embedding[5] * 13 + embedding[6] * 7 + embedding[7] * 5 + embedding[8] * 26 + embedding[9] * -16;
    let score_11 = embedding[0] * 35 + embedding[1] * -17 + embedding[2] * 40 + embedding[3] * 31 + embedding[4] * 8 + embedding[5] * 27 + embedding[6] * -10 + embedding[7] * 30 + embedding[8] * -4 + embedding[9] * -14;
    let score_12 = embedding[0] * 6 + embedding[1] * 19 + embedding[2] * 35 + embedding[3] * -36 + embedding[4] * -6 + embedding[5] * 3 + embedding[6] * -20 + embedding[7] * 24 + embedding[9] * 24;
    let score_13 = embedding[0] * -40 + embedding[1] * 26 + embedding[2] * -20 + embedding[3] * -13 + embedding[4] * -12 + embedding[5] * -62 + embedding[6] * 6 + embedding[7] * -15 + embedding[8] * -17 + embedding[9] * 24;
    let score_14 = embedding[0] * -10 + embedding[1] * -15 + embedding[2] * 10 + embedding[3] * 5 + embedding[4] * 9 + embedding[5] * -24 + embedding[6] * -5 + embedding[7] * -19 + embedding[8] * -12 + embedding[9] * -10;
    let score_15 = embedding[0] * -16 + embedding[1] * 6 + embedding[2] * 5 + embedding[3] * -36 + embedding[4] * -7 + embedding[5] * 9 + embedding[6] * -15 + embedding[7] * -10 + embedding[8] * 6 + embedding[9] * 17;
    let score_16 = embedding[0] * 48 + embedding[1] * -25 + embedding[2] * 32 + embedding[3] * 81 + embedding[4] * 6 + embedding[5] * 9 + embedding[6] * 23 + embedding[7] * 12 + embedding[8] * -19 + embedding[9] * -18;
    let score_17 = embedding[0] * 28 + embedding[1] * -3 + embedding[2] * 14 + embedding[3] * 70 + embedding[4] * -18 + embedding[5] * 28 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * 25 + embedding[9] * -18;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 3;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 4;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 8;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 10;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 21;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 14;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 19;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 6;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 9;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 36;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 25;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 5;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 16;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 13;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 17;
    }
    best_palette
}

fn predict_pixel_153(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -18 + embedding[2] * 10 + embedding[3] * -53 + embedding[4] * 6 + embedding[5] * 13 + embedding[6] * -21 + embedding[7] * 6 + embedding[9] * 12;
    let score_1 = embedding[0] * -17 + embedding[1] * -6 + embedding[2] * -4 + embedding[3] * -8 + embedding[4] * -6 + embedding[5] * 27 + embedding[6] * -12 + embedding[7] * 7 + embedding[8] * 25 + embedding[9] * -5;
    let score_2 = embedding[0] * -13 + embedding[1] * -29 + embedding[2] * -17 + embedding[3] * 5 + embedding[4] * 18 + embedding[5] * -24 + embedding[6] * 33 + embedding[7] * -18 + embedding[8] * -13 + embedding[9] * 4;
    let score_3 = embedding[0] * -15 + embedding[1] * -15 + embedding[2] * -20 + embedding[3] * 2 + embedding[4] * 3 + embedding[5] * -16 + embedding[6] * 15 + embedding[7] * -10 + embedding[8] * -34 + embedding[9] * -18;
    let score_4 = embedding[0] * -22 + embedding[1] * -30 + embedding[2] * -29 + embedding[3] * 39 + embedding[4] * 23 + embedding[5] * -37 + embedding[6] * 22 + embedding[7] * -22 + embedding[8] * -44 + embedding[9] * 1;
    let score_5 = embedding[0] * -22 + embedding[1] * -34 + embedding[2] * -15 + embedding[3] * 40 + embedding[4] * 14 + embedding[5] * -19 + embedding[6] * 12 + embedding[7] * -22 + embedding[8] * -3 + embedding[9] * -29;
    let score_6 = embedding[0] * -12 + embedding[1] * 42 + embedding[2] * -12 + embedding[3] * -23 + embedding[4] * -12 + embedding[5] * -24 + embedding[6] * -10 + embedding[7] * -5 + embedding[8] * 10 + embedding[9] * 25;
    let score_7 = embedding[0] * -23 + embedding[1] * 25 + embedding[2] * -30 + embedding[3] * -30 + embedding[4] * 20 + embedding[5] * -49 + embedding[6] * -11 + embedding[7] * 23 + embedding[8] * -9 + embedding[9] * 18;
    let score_8 = embedding[0] * 29 + embedding[1] * -1 + embedding[2] * -7 + embedding[3] * 103 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 37 + embedding[7] * -28 + embedding[8] * 7 + embedding[9] * -23;
    let score_9 = embedding[0] * 56 + embedding[1] * 14 + embedding[2] * 25 + embedding[3] * 54 + embedding[4] * -4 + embedding[5] * 24 + embedding[6] * -1 + embedding[7] * 20 + embedding[8] * 17 + embedding[9] * -5;
    let score_10 = embedding[0] * 34 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 28 + embedding[4] * -21 + embedding[5] * 26 + embedding[6] * 1 + embedding[7] * -6 + embedding[8] * 11 + embedding[9] * -2;
    let score_11 = embedding[0] * 30 + embedding[1] * -15 + embedding[2] * 74 + embedding[3] * 3 + embedding[4] * -1 + embedding[5] * 27 + embedding[6] * -5 + embedding[7] * 22 + embedding[8] * -17 + embedding[9] * -4;
    let score_12 = embedding[0] * 14 + embedding[1] * 19 + embedding[2] * 44 + embedding[3] * -42 + embedding[4] * -9 + embedding[5] * 14 + embedding[6] * -26 + embedding[7] * 32 + embedding[8] * 1 + embedding[9] * 24;
    let score_13 = embedding[0] * -48 + embedding[1] * 31 + embedding[2] * -28 + embedding[3] * -21 + embedding[4] * -5 + embedding[5] * -70 + embedding[6] * 5 + embedding[7] * -8 + embedding[8] * -14 + embedding[9] * 25;
    let score_14 = embedding[0] * 16 + embedding[1] * 30 + embedding[2] * 11 + embedding[3] * 3 + embedding[4] * -23 + embedding[5] * 12 + embedding[6] * -7 + embedding[7] * -11 + embedding[8] * 21 + embedding[9] * 17;
    let score_15 = embedding[0] * 31 + embedding[1] * 11 + embedding[2] * 33 + embedding[3] * 1 + embedding[4] * -26 + embedding[5] * 41 + embedding[6] * -25 + embedding[7] * 41 + embedding[8] * 19 + embedding[9] * 13;
    let score_16 = embedding[0] * 35 + embedding[1] * -36 + embedding[2] * 31 + embedding[3] * 100 + embedding[4] * 8 + embedding[5] * 2 + embedding[6] * 32 + embedding[8] * -14 + embedding[9] * -31;
    let score_17 = embedding[0] * 39 + embedding[1] * -2 + embedding[2] * 26 + embedding[3] * 57 + embedding[4] * -22 + embedding[5] * 38 + embedding[6] * -2 + embedding[7] * 23 + embedding[8] * 20 + embedding[9] * -12;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 3;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 4;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 21;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 8;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 10;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 14;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 9;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 36;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 25;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 16;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 20;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 13;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 17;
    }
    best_palette
}

fn predict_pixel_154(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -24 + embedding[1] * -17 + embedding[2] * 11 + embedding[3] * -49 + embedding[4] * -3 + embedding[5] * 15 + embedding[6] * -12 + embedding[7] * -3 + embedding[8] * -8 + embedding[9] * 10;
    let score_1 = embedding[0] * -12 + embedding[1] * -19 + embedding[2] * 9 + embedding[3] * -8 + embedding[4] * -7 + embedding[5] * 30 + embedding[6] * -4 + embedding[7] * 5 + embedding[8] * 13 + embedding[9] * -5;
    let score_2 = embedding[0] * -23 + embedding[1] * 16 + embedding[2] * -38 + embedding[3] * -17 + embedding[4] * 25 + embedding[5] * -46 + embedding[6] * -10 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -7 + embedding[1] * -9 + embedding[2] * -18 + embedding[3] * -4 + embedding[4] * 20 + embedding[5] * -19 + embedding[6] * 13 + embedding[7] * -9 + embedding[9] * 1;
    let score_4 = embedding[0] * -2 + embedding[2] * -24 + embedding[3] * 6 + embedding[4] * 13 + embedding[5] * -14 + embedding[6] * 3 + embedding[7] * -6 + embedding[8] * -11 + embedding[9] * -12;
    let score_5 = embedding[0] * -11 + embedding[1] * -14 + embedding[2] * -15 + embedding[3] * 30 + embedding[4] * 17 + embedding[5] * -15 + embedding[6] * 4 + embedding[7] * -12 + embedding[8] * 5 + embedding[9] * -22;
    let score_6 = embedding[0] * -16 + embedding[1] * 42 + embedding[2] * -28 + embedding[3] * -3 + embedding[4] * -12 + embedding[5] * -38 + embedding[7] * -12 + embedding[8] * 9 + embedding[9] * 17;
    let score_7 = embedding[0] * -15 + embedding[1] * -6 + embedding[2] * -27 + embedding[3] * 14 + embedding[4] * 25 + embedding[5] * -29 + embedding[6] * 4 + embedding[7] * -10 + embedding[8] * -22 + embedding[9] * -1;
    let score_8 = embedding[0] * 24 + embedding[1] * 6 + embedding[2] * -9 + embedding[3] * 86 + embedding[4] * -14 + embedding[5] * -20 + embedding[6] * 28 + embedding[7] * -23 + embedding[8] * 8 + embedding[9] * -18;
    let score_9 = embedding[0] * 63 + embedding[1] * 9 + embedding[2] * 64 + embedding[3] * 47 + embedding[4] * -19 + embedding[5] * 34 + embedding[6] * 1 + embedding[7] * 33 + embedding[8] * 11 + embedding[9] * 1;
    let score_10 = embedding[0] * 29 + embedding[1] * 4 + embedding[2] * 26 + embedding[3] * 22 + embedding[4] * -27 + embedding[5] * 24 + embedding[6] * 5 + embedding[7] * -10 + embedding[8] * 7;
    let score_11 = embedding[0] * 33 + embedding[1] * -23 + embedding[2] * 57 + embedding[3] * 17 + embedding[5] * 36 + embedding[6] * -7 + embedding[7] * 33 + embedding[8] * -7 + embedding[9] * -7;
    let score_12 = embedding[0] * 14 + embedding[1] * 5 + embedding[2] * 44 + embedding[3] * -33 + embedding[4] * -17 + embedding[5] * 20 + embedding[6] * -16 + embedding[7] * 18 + embedding[8] * -3 + embedding[9] * 22;
    let score_13 = embedding[0] * -41 + embedding[1] * 35 + embedding[2] * -30 + embedding[3] * -9 + embedding[4] * -10 + embedding[5] * -69 + embedding[6] * 7 + embedding[7] * -12 + embedding[8] * -10 + embedding[9] * 21;
    let score_14 = embedding[0] * 11 + embedding[1] * 26 + embedding[2] * 6 + embedding[3] * 5 + embedding[4] * -21 + embedding[6] * 1 + embedding[7] * -17 + embedding[8] * 13 + embedding[9] * 14;
    let score_15 = embedding[0] * 15 + embedding[2] * 45 + embedding[3] * -8 + embedding[4] * -49 + embedding[5] * 40 + embedding[6] * -14 + embedding[7] * 25 + embedding[8] * 9 + embedding[9] * 17;
    let score_16 = embedding[0] * 34 + embedding[1] * -27 + embedding[2] * 35 + embedding[3] * 79 + embedding[4] * 6 + embedding[5] * 1 + embedding[6] * 24 + embedding[7] * 8 + embedding[8] * -12 + embedding[9] * -25;
    let score_17 = embedding[0] * 37 + embedding[1] * -14 + embedding[2] * 40 + embedding[3] * 54 + embedding[4] * -24 + embedding[5] * 38 + embedding[6] * 6 + embedding[7] * 23 + embedding[8] * 10 + embedding[9] * -14;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 2;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 3;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 21;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 4;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 10;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 14;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 9;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 36;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 25;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 16;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 44;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 13;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 17;
    }
    best_palette
}

fn predict_pixel_155(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -20 + embedding[1] * -7 + embedding[2] * 7 + embedding[3] * -54 + embedding[4] * 5 + embedding[5] * 6 + embedding[6] * -16 + embedding[7] * -3 + embedding[8] * -12 + embedding[9] * 14;
    let score_1 = embedding[0] * -1 + embedding[1] * -14 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -8 + embedding[5] * 30 + embedding[6] * -1 + embedding[7] * 4 + embedding[8] * 24 + embedding[9] * -9;
    let score_2 = embedding[0] * -28 + embedding[1] * 19 + embedding[2] * -35 + embedding[3] * -32 + embedding[4] * 21 + embedding[5] * -49 + embedding[6] * -12 + embedding[7] * 19 + embedding[8] * -11 + embedding[9] * 16;
    let score_3 = embedding[0] * 8 + embedding[1] * -25 + embedding[2] * 33 + embedding[3] * -11 + embedding[5] * 21 + embedding[6] * 1 + embedding[7] * -5 + embedding[8] * -20 + embedding[9] * 1;
    let score_4 = embedding[0] * -15 + embedding[1] * -21 + embedding[2] * -19 + embedding[3] * 55 + embedding[4] * 13 + embedding[5] * -20 + embedding[6] * 15 + embedding[7] * -13 + embedding[8] * 10 + embedding[9] * -30;
    let score_5 = embedding[0] * -20 + embedding[1] * 46 + embedding[2] * -32 + embedding[3] * -5 + embedding[4] * -9 + embedding[5] * -47 + embedding[6] * -4 + embedding[7] * -3 + embedding[8] * 9 + embedding[9] * 19;
    let score_6 = embedding[0] * 26 + embedding[1] * -1 + embedding[2] * -11 + embedding[3] * 103 + embedding[4] * -14 + embedding[5] * -15 + embedding[6] * 33 + embedding[7] * -22 + embedding[8] * 10 + embedding[9] * -25;
    let score_7 = embedding[0] * 62 + embedding[1] * 10 + embedding[2] * 53 + embedding[3] * 57 + embedding[4] * -19 + embedding[5] * 36 + embedding[6] * 3 + embedding[7] * 29 + embedding[8] * 16 + embedding[9] * -2;
    let score_8 = embedding[0] * 30 + embedding[1] * -4 + embedding[2] * 21 + embedding[3] * 29 + embedding[4] * -22 + embedding[5] * 29 + embedding[6] * 6 + embedding[7] * -11 + embedding[8] * 5 + embedding[9] * -4;
    let score_9 = embedding[0] * -26 + embedding[1] * -21 + embedding[2] * -25 + embedding[3] * 9 + embedding[4] * 11 + embedding[5] * -21 + embedding[6] * 15 + embedding[7] * -17 + embedding[8] * -15 + embedding[9] * -11;
    let score_10 = embedding[0] * 6 + embedding[1] * 9 + embedding[2] * 39 + embedding[3] * -47 + embedding[4] * -3 + embedding[5] * 3 + embedding[6] * -20 + embedding[7] * 27 + embedding[8] * -14 + embedding[9] * 27;
    let score_11 = embedding[0] * -52 + embedding[1] * 34 + embedding[2] * -33 + embedding[3] * -22 + embedding[4] * -7 + embedding[5] * -78 + embedding[6] * 6 + embedding[7] * -6 + embedding[8] * -17 + embedding[9] * 26;
    let score_12 = embedding[0] * 1 + embedding[1] * 15 + embedding[2] * -7 + embedding[3] * -5 + embedding[5] * 2 + embedding[6] * -8 + embedding[7] * -12 + embedding[8] * 3 + embedding[9] * 12;
    let score_13 = embedding[0] * -4 + embedding[1] * -5 + embedding[2] * -18 + embedding[3] * 6 + embedding[4] * 15 + embedding[5] * -18 + embedding[6] * 14 + embedding[7] * -7 + embedding[9] * 1;
    let score_14 = embedding[0] * 36 + embedding[2] * 39 + embedding[3] * 19 + embedding[4] * -27 + embedding[5] * 40 + embedding[6] * -16 + embedding[7] * 39 + embedding[8] * 15 + embedding[9] * 10;
    let score_15 = embedding[0] * 11 + embedding[1] * 2 + embedding[2] * -21 + embedding[3] * 27 + embedding[4] * 5 + embedding[5] * -4 + embedding[6] * 5 + embedding[7] * -5 + embedding[8] * -1 + embedding[9] * -13;
    let score_16 = embedding[0] * 35 + embedding[1] * -28 + embedding[2] * 27 + embedding[3] * 99 + embedding[5] * 3 + embedding[6] * 34 + embedding[7] * 1 + embedding[8] * -8 + embedding[9] * -31;
    let score_17 = embedding[0] * 37 + embedding[1] * -12 + embedding[2] * 34 + embedding[3] * 68 + embedding[4] * -25 + embedding[5] * 41 + embedding[6] * 8 + embedding[7] * 19 + embedding[8] * 17 + embedding[9] * -16;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 9;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 21;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 10;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 14;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 19;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 7;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 36;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 25;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 16;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 2;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 20;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 3;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 13;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 17;
    }
    best_palette
}

fn predict_pixel_156(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -19 + embedding[1] * -18 + embedding[2] * 15 + embedding[3] * -49 + embedding[4] * 1 + embedding[5] * 13 + embedding[6] * -12 + embedding[7] * -6 + embedding[8] * -13 + embedding[9] * 10;
    let score_1 = embedding[0] * -7 + embedding[1] * -19 + embedding[2] * 23 + embedding[3] * 6 + embedding[4] * -12 + embedding[5] * 33 + embedding[6] * -1 + embedding[7] * 4 + embedding[8] * 18 + embedding[9] * -6;
    let score_2 = embedding[0] * -20 + embedding[1] * 15 + embedding[2] * -36 + embedding[3] * -15 + embedding[4] * 30 + embedding[5] * -48 + embedding[6] * -11 + embedding[7] * 22 + embedding[8] * -11 + embedding[9] * 7;
    let score_3 = embedding[0] * -21 + embedding[1] * -18 + embedding[2] * -13 + embedding[3] * -2 + embedding[4] * 10 + embedding[5] * -8 + embedding[6] * 6 + embedding[7] * -14 + embedding[8] * -11 + embedding[9] * -7;
    let score_4 = embedding[0] * -9 + embedding[1] * -14 + embedding[2] * -13 + embedding[3] * 38 + embedding[4] * 20 + embedding[5] * -16 + embedding[6] * 4 + embedding[7] * -7 + embedding[8] * 7 + embedding[9] * -25;
    let score_5 = embedding[0] * -13 + embedding[1] * 45 + embedding[2] * -26 + embedding[3] * 2 + embedding[4] * -13 + embedding[5] * -39 + embedding[6] * -1 + embedding[7] * -10 + embedding[8] * 10 + embedding[9] * 18;
    let score_6 = embedding[0] * -5 + embedding[1] * -25 + embedding[2] * 16 + embedding[3] * -21 + embedding[4] * 6 + embedding[5] * 22 + embedding[6] * -10 + embedding[7] * 10 + embedding[8] * -10 + embedding[9] * -2;
    let score_7 = embedding[0] * 29 + embedding[1] * 7 + embedding[2] * -7 + embedding[3] * 95 + embedding[4] * -16 + embedding[5] * -16 + embedding[6] * 29 + embedding[7] * -21 + embedding[8] * 12 + embedding[9] * -19;
    let score_8 = embedding[0] * 57 + embedding[1] * 4 + embedding[2] * 39 + embedding[3] * 75 + embedding[4] * -29 + embedding[5] * 33 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * 17 + embedding[9] * -9;
    let score_9 = embedding[0] * 33 + embedding[1] * 6 + embedding[2] * 25 + embedding[3] * 31 + embedding[4] * -36 + embedding[5] * 28 + embedding[6] * 10 + embedding[7] * -14 + embedding[8] * 12 + embedding[9] * 2;
    let score_10 = embedding[0] * 1 + embedding[1] * -2 + embedding[2] * -15 + embedding[4] * 23 + embedding[5] * -18 + embedding[6] * 7 + embedding[7] * 1 + embedding[8] * 2 + embedding[9] * 3;
    let score_11 = embedding[0] * 8 + embedding[1] * 6 + embedding[2] * -20 + embedding[3] * 12 + embedding[4] * 16 + embedding[5] * -11 + embedding[6] * -2 + embedding[7] * 5 + embedding[8] * -6 + embedding[9] * -12;
    let score_12 = embedding[0] * 13 + embedding[1] * 7 + embedding[2] * 38 + embedding[3] * -31 + embedding[4] * -14 + embedding[5] * 15 + embedding[6] * -14 + embedding[7] * 11 + embedding[8] * -6 + embedding[9] * 20;
    let score_13 = embedding[0] * -15 + embedding[1] * -3 + embedding[2] * -24 + embedding[3] * 6 + embedding[4] * 29 + embedding[5] * -31 + embedding[6] * -1 + embedding[7] * 1 + embedding[8] * -21 + embedding[9] * 2;
    let score_14 = embedding[0] * -42 + embedding[1] * 32 + embedding[2] * -27 + embedding[3] * -10 + embedding[4] * -8 + embedding[5] * -70 + embedding[6] * 7 + embedding[7] * -11 + embedding[8] * -14 + embedding[9] * 21;
    let score_15 = embedding[0] * 16 + embedding[1] * 25 + embedding[2] * 10 + embedding[3] * 9 + embedding[4] * -24 + embedding[5] * 6 + embedding[6] * 1 + embedding[7] * -15 + embedding[8] * 14 + embedding[9] * 14;
    let score_16 = embedding[0] * 18 + embedding[1] * 2 + embedding[2] * 40 + embedding[3] * 4 + embedding[4] * -49 + embedding[5] * 40 + embedding[6] * -10 + embedding[7] * 15 + embedding[8] * 11 + embedding[9] * 14;
    let score_17 = embedding[0] * 35 + embedding[1] * -23 + embedding[2] * 25 + embedding[3] * 90 + embedding[4] * 10 + embedding[5] * -10 + embedding[6] * 29 + embedding[7] * 8 + embedding[8] * -13 + embedding[9] * -26;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 21;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 9;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 10;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 14;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 19;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 2;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 3;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 36;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 4;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 25;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 16;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 44;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 13;
    }
    best_palette
}

fn predict_pixel_157(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 8 + embedding[3] * -47 + embedding[4] * 4 + embedding[5] * 16 + embedding[6] * -23 + embedding[7] * 9 + embedding[8] * 6 + embedding[9] * 12;
    let score_1 = embedding[0] * -10 + embedding[1] * -12 + embedding[2] * 9 + embedding[3] * -12 + embedding[4] * 6 + embedding[5] * 25 + embedding[6] * -14 + embedding[7] * 11 + embedding[8] * 17 + embedding[9] * -7;
    let score_2 = embedding[0] * -12 + embedding[1] * -28 + embedding[2] * -22 + embedding[3] * 9 + embedding[4] * 19 + embedding[5] * -27 + embedding[6] * 34 + embedding[7] * -19 + embedding[8] * -13 + embedding[9] * 3;
    let score_3 = embedding[0] * -14 + embedding[1] * -14 + embedding[2] * -26 + embedding[3] * 6 + embedding[4] * 5 + embedding[5] * -21 + embedding[6] * 17 + embedding[7] * -12 + embedding[8] * -34 + embedding[9] * -18;
    let score_4 = embedding[0] * -22 + embedding[1] * -27 + embedding[2] * -34 + embedding[3] * 41 + embedding[4] * 24 + embedding[5] * -42 + embedding[6] * 24 + embedding[7] * -24 + embedding[8] * -44;
    let score_5 = embedding[0] * -22 + embedding[1] * -32 + embedding[2] * -16 + embedding[3] * 39 + embedding[4] * 16 + embedding[5] * -25 + embedding[6] * 14 + embedding[7] * -21 + embedding[8] * -7 + embedding[9] * -28;
    let score_6 = embedding[0] * -22 + embedding[1] * 33 + embedding[2] * -31 + embedding[3] * -15 + embedding[4] * -12 + embedding[5] * -23 + embedding[6] * -4 + embedding[7] * -12 + embedding[8] * 16 + embedding[9] * 15;
    let score_7 = embedding[0] * -14 + embedding[1] * 23 + embedding[2] * -34 + embedding[3] * -20 + embedding[4] * 20 + embedding[5] * -40 + embedding[6] * -9 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 13;
    let score_8 = embedding[0] * 24 + embedding[1] * -2 + embedding[2] * -9 + embedding[3] * 102 + embedding[4] * -20 + embedding[5] * -20 + embedding[6] * 40 + embedding[7] * -32 + embedding[8] * 2 + embedding[9] * -21;
    let score_9 = embedding[0] * 63 + embedding[1] * 15 + embedding[2] * 71 + embedding[3] * 37 + embedding[4] * -21 + embedding[5] * 40 + embedding[6] * -5 + embedding[7] * 38 + embedding[8] * 17 + embedding[9] * 7;
    let score_10 = embedding[0] * 30 + embedding[1] * 7 + embedding[2] * 29 + embedding[3] * 18 + embedding[4] * -27 + embedding[5] * 28 + embedding[6] * 1 + embedding[7] * -9 + embedding[8] * 10 + embedding[9] * 1;
    let score_11 = embedding[0] * 31 + embedding[1] * -17 + embedding[2] * 76 + embedding[3] * 3 + embedding[4] * -3 + embedding[5] * 32 + embedding[6] * -8 + embedding[7] * 25 + embedding[8] * -14 + embedding[9] * -3;
    let score_12 = embedding[0] * 19 + embedding[1] * 20 + embedding[2] * 46 + embedding[3] * -40 + embedding[4] * -7 + embedding[5] * 15 + embedding[6] * -28 + embedding[7] * 40 + embedding[8] * 3 + embedding[9] * 24;
    let score_13 = embedding[0] * -44 + embedding[1] * 34 + embedding[2] * -32 + embedding[3] * -13 + embedding[4] * -13 + embedding[5] * -62 + embedding[6] * 6 + embedding[7] * -14 + embedding[8] * -7 + embedding[9] * 23;
    let score_14 = embedding[0] * 9 + embedding[1] * 26 + embedding[2] * 5 + embedding[3] * -1 + embedding[4] * -24 + embedding[5] * 7 + embedding[6] * -2 + embedding[7] * -16 + embedding[8] * 19 + embedding[9] * 16;
    let score_15 = embedding[0] * 34 + embedding[1] * 8 + embedding[2] * 48 + embedding[3] * -4 + embedding[4] * -27 + embedding[5] * 42 + embedding[6] * -25 + embedding[7] * 50 + embedding[8] * 16 + embedding[9] * 14;
    let score_16 = embedding[0] * 32 + embedding[1] * -37 + embedding[2] * 34 + embedding[3] * 95 + embedding[4] * 8 + embedding[5] * -2 + embedding[6] * 34 + embedding[7] * -2 + embedding[8] * -21 + embedding[9] * -30;
    let score_17 = embedding[0] * 38 + embedding[1] * -7 + embedding[2] * 46 + embedding[3] * 45 + embedding[4] * -24 + embedding[5] * 44 + embedding[6] * -3 + embedding[7] * 29 + embedding[8] * 16 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 3;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 4;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 21;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 8;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 10;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 14;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 9;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 36;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 25;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 16;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 20;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 13;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 17;
    }
    best_palette
}

fn predict_pixel_158(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -26 + embedding[1] * 4 + embedding[2] * 7 + embedding[3] * -54 + embedding[4] * -12 + embedding[5] * 14 + embedding[6] * -18 + embedding[7] * -4 + embedding[8] * 5 + embedding[9] * 18;
    let score_1 = embedding[0] * -15 + embedding[1] * -15 + embedding[3] * -2 + embedding[4] * -4 + embedding[5] * 30 + embedding[6] * -8 + embedding[7] * 4 + embedding[8] * 20 + embedding[9] * -10;
    let score_2 = embedding[0] * -5 + embedding[1] * -24 + embedding[2] * -15 + embedding[3] * 1 + embedding[4] * 25 + embedding[5] * -25 + embedding[6] * 26 + embedding[7] * -12 + embedding[8] * -12 + embedding[9] * 3;
    let score_3 = embedding[0] * -6 + embedding[1] * -10 + embedding[2] * -20 + embedding[3] * -1 + embedding[4] * 12 + embedding[5] * -19 + embedding[6] * 10 + embedding[7] * -4 + embedding[8] * -34 + embedding[9] * -18;
    let score_4 = embedding[0] * -13 + embedding[1] * -20 + embedding[2] * -26 + embedding[3] * 31 + embedding[4] * 33 + embedding[5] * -37 + embedding[6] * 14 + embedding[7] * -14 + embedding[8] * -44 + embedding[9] * 1;
    let score_5 = embedding[0] * -5 + embedding[1] * -32 + embedding[2] * -9 + embedding[3] * 46 + embedding[4] * 20 + embedding[5] * 6 + embedding[6] * 13 + embedding[7] * 7 + embedding[8] * 2 + embedding[9] * -29;
    let score_6 = embedding[0] * -7 + embedding[1] * 49 + embedding[2] * -14 + embedding[3] * -19 + embedding[4] * -12 + embedding[5] * -31 + embedding[6] * -11 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * 25;
    let score_7 = embedding[0] * -11 + embedding[1] * 30 + embedding[2] * -37 + embedding[3] * -15 + embedding[4] * 23 + embedding[5] * -52 + embedding[6] * -10 + embedding[7] * 21 + embedding[8] * -7 + embedding[9] * 14;
    let score_8 = embedding[0] * 25 + embedding[1] * -3 + embedding[2] * -9 + embedding[3] * 100 + embedding[4] * -11 + embedding[5] * -10 + embedding[6] * 37 + embedding[7] * -24 + embedding[8] * 8 + embedding[9] * -25;
    let score_9 = embedding[0] * 56 + embedding[1] * -4 + embedding[2] * 53 + embedding[3] * 61 + embedding[4] * -22 + embedding[5] * 43 + embedding[6] * 2 + embedding[7] * 27 + embedding[8] * 19 + embedding[9] * -9;
    let score_10 = embedding[0] * 32 + embedding[1] * 6 + embedding[2] * 27 + embedding[3] * 26 + embedding[4] * -30 + embedding[5] * 28 + embedding[6] * 6 + embedding[7] * -12 + embedding[8] * 12 + embedding[9] * 1;
    let score_11 = embedding[0] * 32 + embedding[1] * -24 + embedding[2] * 45 + embedding[3] * 27 + embedding[4] * 8 + embedding[5] * 30 + embedding[6] * -9 + embedding[7] * 33 + embedding[8] * -3 + embedding[9] * -16;
    let score_12 = embedding[0] * 11 + embedding[1] * 22 + embedding[2] * 40 + embedding[3] * -39 + embedding[4] * -23 + embedding[5] * 18 + embedding[6] * -22 + embedding[7] * 20 + embedding[8] * 7 + embedding[9] * 27;
    let score_13 = embedding[0] * -42 + embedding[1] * 35 + embedding[2] * -25 + embedding[3] * -13 + embedding[4] * -9 + embedding[5] * -70 + embedding[6] * 5 + embedding[7] * -16 + embedding[8] * -12 + embedding[9] * 23;
    let score_14 = embedding[0] * -23 + embedding[1] * -20 + embedding[2] * -10 + embedding[3] * 12 + embedding[4] * 17 + embedding[5] * -32 + embedding[6] * 1 + embedding[7] * -19 + embedding[8] * -8 + embedding[9] * -19;
    let score_15 = embedding[0] * 14 + embedding[1] * 36 + embedding[2] * 12 + embedding[3] * -3 + embedding[4] * -27 + embedding[5] * 6 + embedding[6] * -7 + embedding[7] * -14 + embedding[8] * 18 + embedding[9] * 23;
    let score_16 = embedding[0] * 6 + embedding[1] * 14 + embedding[2] * 38 + embedding[3] * -18 + embedding[4] * -57 + embedding[5] * 39 + embedding[6] * -21 + embedding[7] * 22 + embedding[8] * 15 + embedding[9] * 24;
    let score_17 = embedding[0] * 29 + embedding[1] * -40 + embedding[2] * 26 + embedding[3] * 90 + embedding[4] * 16 + embedding[5] * 8 + embedding[6] * 30 + embedding[7] * 6 + embedding[8] * -24 + embedding[9] * -30;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 3;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 4;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 21;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 8;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 98;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 14;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 9;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 36;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 25;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 5;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 16;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 44;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 13;
    }
    best_palette
}

fn predict_pixel_159(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * -3 + embedding[3] * -37 + embedding[4] * 3 + embedding[5] * 4 + embedding[6] * -8 + embedding[7] * -26 + embedding[8] * -8 + embedding[9] * 11;
    let score_1 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_2 = embedding[0] * -15 + embedding[1] * 15 + embedding[2] * -34 + embedding[3] * 10 + embedding[4] * 34 + embedding[5] * -54 + embedding[6] * -8 + embedding[7] * 49 + embedding[8] * 2 + embedding[9] * -1;
    let score_3 = embedding[0] * 12 + embedding[1] * -1 + embedding[2] * -17 + embedding[3] * 81 + embedding[4] * -14 + embedding[5] * -4 + embedding[6] * 23 + embedding[7] * -18 + embedding[8] * 17 + embedding[9] * -20;
    let score_4 = embedding[0] * 47 + embedding[1] * 13 + embedding[2] * 35 + embedding[3] * 43 + embedding[4] * 5 + embedding[5] * 20 + embedding[6] * -4 + embedding[7] * 32 + embedding[8] * 14 + embedding[9] * -5;
    let score_5 = embedding[0] * 9 + embedding[1] * -20 + embedding[2] * 71 + embedding[3] * -8 + embedding[4] * 3 + embedding[5] * 20 + embedding[6] * 3 + embedding[7] * 32 + embedding[8] * -39 + embedding[9] * 6;
    let score_6 = embedding[0] * 19 + embedding[1] * 19 + embedding[2] * 34 + embedding[3] * -36 + embedding[4] * -1 + embedding[5] * -3 + embedding[6] * -14 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 23;
    let score_7 = embedding[0] * -38 + embedding[1] * 25 + embedding[2] * -19 + embedding[3] * -22 + embedding[4] * -14 + embedding[5] * -60 + embedding[6] * 13 + embedding[7] * -25 + embedding[8] * -27 + embedding[9] * 26;
    let score_8 = embedding[0] * 3 + embedding[1] * 39 + embedding[2] * 30 + embedding[3] * -25 + embedding[4] * -54 + embedding[5] * -12 + embedding[6] * 3 + embedding[7] * -23 + embedding[8] * -10 + embedding[9] * 38;
    let score_9 = embedding[0] * 25 + embedding[1] * -28 + embedding[2] * 17 + embedding[3] * 80 + embedding[4] * 5 + embedding[5] * 13 + embedding[6] * 25 + embedding[7] * 8 + embedding[8] * -15 + embedding[9] * -25;
    let score_10 = embedding[0] * 15 + embedding[1] * -16 + embedding[2] * 8 + embedding[3] * 56 + embedding[4] * -7 + embedding[5] * 31 + embedding[7] * 25 + embedding[8] * 21 + embedding[9] * -20;
    let score_11 = embedding[0] * 21 + embedding[1] * -15 + embedding[2] * 9 + embedding[3] * 3 + embedding[4] * 21 + embedding[5] * -8 + embedding[6] * -27 + embedding[7] * 71 + embedding[8] * -4 + embedding[9] * -7;
    let score_12 = embedding[0] * -8 + embedding[1] * -22 + embedding[2] * -12 + embedding[3] * 51 + embedding[4] * -1 + embedding[5] * 21 + embedding[6] * 13 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -20;
    let score_13 = embedding[0] * -25 + embedding[2] * -28 + embedding[3] * 4 + embedding[4] * 12 + embedding[5] * -58 + embedding[6] * 13 + embedding[7] * -4 + embedding[8] * -33 + embedding[9] * 2;
    let score_14 = embedding[0] * -13 + embedding[1] * 16 + embedding[2] * -7 + embedding[3] * -37 + embedding[4] * -8 + embedding[5] * -25 + embedding[7] * -15 + embedding[8] * -20 + embedding[9] * 24;
    let score_15 = embedding[0] * 10 + embedding[1] * 3 + embedding[2] * -18 + embedding[3] * 26 + embedding[4] * 13 + embedding[5] * 4 + embedding[6] * -8 + embedding[7] * 13 + embedding[8] * 13 + embedding[9] * -8;
    let score_16 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_17 = embedding[0] * -16 + embedding[1] * -15 + embedding[2] * -12 + embedding[3] * 11 + embedding[4] * 2 + embedding[5] * 23 + embedding[6] * -9 + embedding[7] * 4 + embedding[8] * 27 + embedding[9] * -14;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 10;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 14;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 9;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 36;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 25;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 16;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 13;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 17;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 12;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 6;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 18;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 81;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 112;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_160(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_1 = embedding[0] * -7 + embedding[1] * -3 + embedding[2] * 12 + embedding[3] * -37 + embedding[4] * 2 + embedding[5] * 4 + embedding[6] * -9 + embedding[7] * -12 + embedding[8] * -11 + embedding[9] * 9;
    let score_2 = embedding[0] * 2 + embedding[1] * 13 + embedding[2] * -21 + embedding[3] * 10 + embedding[4] * 32 + embedding[5] * -32 + embedding[6] * -11 + embedding[7] * 54 + embedding[8] * 7 + embedding[9] * -3;
    let score_3 = embedding[0] * 40 + embedding[1] * 14 + embedding[2] * 19 + embedding[3] * 29 + embedding[4] * 6 + embedding[5] * 22 + embedding[7] * 7 + embedding[8] * 17 + embedding[9] * -5;
    let score_4 = embedding[0] * -34 + embedding[1] * 21 + embedding[2] * -9 + embedding[3] * -22 + embedding[4] * -15 + embedding[5] * -46 + embedding[6] * 7 + embedding[7] * -17 + embedding[8] * -16 + embedding[9] * 24;
    let score_5 = embedding[0] * 15 + embedding[1] * -15 + embedding[2] * -1 + embedding[3] * 2 + embedding[4] * 26 + embedding[5] * -12 + embedding[6] * -19 + embedding[7] * 68 + embedding[8] * -5 + embedding[9] * -13;
    let score_6 = embedding[0] * -7 + embedding[1] * -32 + embedding[2] * -3 + embedding[3] * 23 + embedding[4] * 21 + embedding[5] * -8 + embedding[6] * -5 + embedding[7] * 61 + embedding[8] * -6 + embedding[9] * -21;
    let score_7 = embedding[0] * -11 + embedding[1] * 12 + embedding[3] * -32 + embedding[4] * -2 + embedding[5] * -24 + embedding[6] * -3 + embedding[7] * -3 + embedding[8] * -17 + embedding[9] * 18;
    let score_8 = embedding[0] * 12 + embedding[1] * 3 + embedding[2] * -6 + embedding[3] * 43 + embedding[4] * -18 + embedding[5] * 18 + embedding[6] * 13 + embedding[7] * -24 + embedding[8] * 21 + embedding[9] * -12;
    let score_9 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_10 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_11 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_12 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_13 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_14 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_15 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_16 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_17 = embedding[0] * 5 + embedding[1] * -5 + embedding[2] * -14 + embedding[3] * 2 + embedding[4] * 9 + embedding[5] * 25 + embedding[6] * -4 + embedding[7] * -25 + embedding[8] * 16 + embedding[9] * -13;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 17;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 25;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 12;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 6;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 81;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 10;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_161(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 5;
    let score_1 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * -9 + embedding[3] * -23 + embedding[4] * 5 + embedding[5] * 5 + embedding[6] * -11 + embedding[7] * -18 + embedding[8] * 5 + embedding[9] * 1;
    let score_2 = embedding[0] * 19 + embedding[1] * 18 + embedding[2] * -7 + embedding[3] * 9 + embedding[4] * 22 + embedding[5] * -31 + embedding[6] * -3 + embedding[7] * 24 + embedding[8] * -2 + embedding[9] * 2;
    let score_3 = embedding[0] * 16 + embedding[1] * -7 + embedding[4] * 19 + embedding[5] * -5 + embedding[6] * -17 + embedding[7] * 48 + embedding[8] * -2 + embedding[9] * -6;
    let score_4 = embedding[0] * -9 + embedding[1] * -26 + embedding[3] * 10 + embedding[4] * 15 + embedding[5] * -6 + embedding[6] * -4 + embedding[7] * 44 + embedding[8] * -7 + embedding[9] * -13;
    let score_5 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 4;
    let score_6 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 5;
    let score_7 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 4;
    let score_8 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 5;
    let score_9 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 5;
    let score_10 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 4;
    let score_11 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 5;
    let score_12 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 4;
    let score_13 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 4;
    let score_14 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 4;
    let score_15 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 5;
    let score_16 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 5;
    let score_17 = embedding[0] * 5 + embedding[2] * -13 + embedding[3] * -13 + embedding[4] * 9 + embedding[5] * 18 + embedding[6] * -10 + embedding[7] * -22 + embedding[8] * 13 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 12;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_162(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 4;
    let score_1 = embedding[0] * -21 + embedding[1] * -12 + embedding[2] * -4 + embedding[3] * -29 + embedding[4] * 10 + embedding[5] * -5 + embedding[6] * -8 + embedding[7] * 13 + embedding[8] * -11 + embedding[9] * 2;
    let score_2 = embedding[0] * 26 + embedding[1] * 26 + embedding[2] * -11 + embedding[3] * 16 + embedding[4] * 20 + embedding[5] * -33 + embedding[6] * -2 + embedding[7] * 13 + embedding[8] * 7;
    let score_3 = embedding[0] * -16 + embedding[1] * -25 + embedding[2] * -1 + embedding[4] * 15 + embedding[5] * -5 + embedding[6] * -2 + embedding[7] * 35 + embedding[8] * -6 + embedding[9] * -11;
    let score_4 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 4;
    let score_5 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 4;
    let score_6 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 4;
    let score_7 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 4;
    let score_8 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 4;
    let score_9 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 4;
    let score_10 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 4;
    let score_11 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 4;
    let score_12 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 4;
    let score_13 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 4;
    let score_14 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 4;
    let score_15 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 4;
    let score_16 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 4;
    let score_17 = embedding[0] * 13 + embedding[1] * 3 + embedding[2] * -14 + embedding[3] * -13 + embedding[4] * 8 + embedding[5] * 21 + embedding[6] * -12 + embedding[7] * -30 + embedding[8] * 18 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_163(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_1 = embedding[0] * -8 + embedding[1] * -8 + embedding[2] * -9 + embedding[3] * -20 + embedding[4] * 11 + embedding[6] * -5 + embedding[7] * -1 + embedding[8] * -7 + embedding[9] * -2;
    let score_2 = embedding[0] * 21 + embedding[1] * 24 + embedding[2] * -9 + embedding[3] * 12 + embedding[4] * 19 + embedding[5] * -33 + embedding[6] * -3 + embedding[7] * 15 + embedding[8] * 5 + embedding[9] * 1;
    let score_3 = embedding[0] * -13 + embedding[1] * -23 + embedding[3] * 1 + embedding[4] * 14 + embedding[5] * -6 + embedding[6] * -3 + embedding[7] * 33 + embedding[8] * -6 + embedding[9] * -10;
    let score_4 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_5 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_6 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_7 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_8 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_9 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_10 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_11 = embedding[0] * -9 + embedding[1] * 12 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_12 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_13 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_14 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_15 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_16 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 5;
    let score_17 = embedding[0] * 8 + embedding[1] * 2 + embedding[2] * -12 + embedding[3] * -18 + embedding[4] * 8 + embedding[5] * 20 + embedding[6] * -15 + embedding[7] * -25 + embedding[8] * 18 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_164(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_1 = embedding[0] * -8 + embedding[1] * -21 + embedding[2] * -5 + embedding[3] * 7 + embedding[4] * 18 + embedding[5] * -10 + embedding[6] * -1 + embedding[7] * 37 + embedding[8] * -3 + embedding[9] * -11;
    let score_2 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_3 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_4 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_5 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_6 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_7 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_8 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_9 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_10 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_11 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_12 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_13 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_14 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_15 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_16 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 5;
    let score_17 = embedding[0] * 10 + embedding[1] * 4 + embedding[2] * -21 + embedding[3] * -19 + embedding[4] * 13 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -25 + embedding[8] * 13 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 6;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_165(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_166(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_167(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -24 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_168(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_169(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_170(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_1 = embedding[0] * 16 + embedding[1] * 11 + embedding[2] * -9 + embedding[3] * 12 + embedding[4] * 24 + embedding[5] * -36 + embedding[7] * 14 + embedding[8] * -4 + embedding[9] * -1;
    let score_2 = embedding[1] * -10 + embedding[2] * -14 + embedding[3] * 8 + embedding[4] * 17 + embedding[5] * -25 + embedding[6] * 6 + embedding[7] * -10 + embedding[8] * -16 + embedding[9] * -5;
    let score_3 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_4 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_5 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_6 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_7 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_8 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_9 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_10 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_11 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_12 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_13 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_14 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_15 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_16 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_17 = embedding[0] * 2 + embedding[1] * -10 + embedding[2] * -10 + embedding[3] * -20 + embedding[4] * 12 + embedding[5] * 24 + embedding[6] * -15 + embedding[7] * -20 + embedding[8] * 13 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 8;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 18;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_171(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_1 = embedding[0] * 17 + embedding[1] * 21 + embedding[2] * -10 + embedding[3] * 11 + embedding[4] * 21 + embedding[5] * -38 + embedding[6] * -1 + embedding[7] * 17;
    let score_2 = embedding[0] * -28 + embedding[1] * -9 + embedding[2] * -7 + embedding[3] * -27 + embedding[4] * -4 + embedding[5] * 8 + embedding[6] * -8 + embedding[7] * 11 + embedding[9] * 1;
    let score_3 = embedding[0] * 23 + embedding[1] * -18 + embedding[2] * 13 + embedding[3] * 33 + embedding[4] * 8 + embedding[6] * 10 + embedding[7] * -6 + embedding[8] * -9 + embedding[9] * -11;
    let score_4 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_5 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_6 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_7 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_8 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_9 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_10 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_11 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_12 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_13 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_14 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_15 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_16 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 18 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 8;
    let score_17 = embedding[0] * 2 + embedding[1] * -1 + embedding[2] * -14 + embedding[3] * -21 + embedding[4] * 11 + embedding[5] * 16 + embedding[6] * -14 + embedding[7] * -22 + embedding[8] * 12 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 8;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_172(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_1 = embedding[0] * -30 + embedding[1] * -7 + embedding[2] * -5 + embedding[3] * -33 + embedding[4] * 2 + embedding[5] * -5 + embedding[6] * -7 + embedding[7] * 9 + embedding[8] * -11 + embedding[9] * 7;
    let score_2 = embedding[0] * 21 + embedding[1] * 26 + embedding[2] * -9 + embedding[3] * 10 + embedding[4] * 19 + embedding[5] * -35 + embedding[6] * -2 + embedding[7] * 19 + embedding[8] * 2 + embedding[9] * 4;
    let score_3 = embedding[0] * 26 + embedding[1] * -13 + embedding[2] * 13 + embedding[3] * 44 + embedding[4] * 6 + embedding[6] * 11 + embedding[7] * -3 + embedding[8] * -6 + embedding[9] * -11;
    let score_4 = embedding[0] * 18 + embedding[1] * -7 + embedding[2] * -3 + embedding[3] * 5 + embedding[4] * 17 + embedding[5] * -5 + embedding[6] * -14 + embedding[7] * 40 + embedding[8] * -1 + embedding[9] * -7;
    let score_5 = embedding[0] * -16 + embedding[1] * -24 + embedding[2] * 1 + embedding[3] * 5 + embedding[4] * 14 + embedding[5] * -8 + embedding[6] * -4 + embedding[7] * 47 + embedding[8] * -8 + embedding[9] * -14;
    let score_6 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_7 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_8 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_9 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_10 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_11 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_12 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_14 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_15 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_16 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 6;
    let score_17 = embedding[0] * 4 + embedding[1] * 8 + embedding[2] * -16 + embedding[3] * -18 + embedding[4] * 1 + embedding[5] * 21 + embedding[6] * -11 + embedding[7] * -33 + embedding[8] * 18 + embedding[9] * -3;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_173(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 6;
    let score_1 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 1 + embedding[3] * -36 + embedding[4] * 3 + embedding[5] * 8 + embedding[6] * -10 + embedding[7] * -12 + embedding[8] * -1 + embedding[9] * 6;
    let score_2 = embedding[0] * 1 + embedding[1] * 19 + embedding[2] * -20 + embedding[3] * -2 + embedding[4] * 28 + embedding[5] * -35 + embedding[6] * -12 + embedding[7] * 44 + embedding[8] * 6;
    let score_3 = embedding[0] * 24 + embedding[1] * -14 + embedding[2] * 18 + embedding[3] * 39 + embedding[4] * 8 + embedding[5] * 4 + embedding[6] * 14 + embedding[7] * -4 + embedding[8] * -9 + embedding[9] * -12;
    let score_4 = embedding[0] * 13 + embedding[1] * -8 + embedding[3] * -5 + embedding[4] * 23 + embedding[5] * -11 + embedding[6] * -16 + embedding[7] * 57 + embedding[8] * -2 + embedding[9] * -8;
    let score_5 = embedding[0] * -11 + embedding[1] * -25 + embedding[3] * 9 + embedding[4] * 19 + embedding[5] * -12 + embedding[6] * -6 + embedding[7] * 65 + embedding[8] * -11 + embedding[9] * -15;
    let score_6 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 6;
    let score_7 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 6;
    let score_8 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 6;
    let score_9 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 6;
    let score_10 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 6;
    let score_11 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 6;
    let score_12 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 6;
    let score_13 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 6;
    let score_14 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 6;
    let score_15 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 6;
    let score_16 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 6;
    let score_17 = embedding[0] * 4 + embedding[1] * 5 + embedding[2] * -11 + embedding[3] * -13 + embedding[4] * 5 + embedding[5] * 21 + embedding[6] * -6 + embedding[7] * -31 + embedding[8] * 16 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_174(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * -2 + embedding[2] * -6 + embedding[3] * -43 + embedding[4] * 4 + embedding[5] * 1 + embedding[6] * -6 + embedding[7] * -36 + embedding[8] * -16 + embedding[9] * 9;
    let score_1 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_2 = embedding[0] * -15 + embedding[1] * 23 + embedding[2] * -36 + embedding[3] * -6 + embedding[4] * 27 + embedding[5] * -56 + embedding[6] * -14 + embedding[7] * 49 + embedding[8] * -2 + embedding[9] * 4;
    let score_3 = embedding[0] * 36 + embedding[1] * 10 + embedding[2] * 28 + embedding[3] * 26 + embedding[4] * 5 + embedding[5] * 21 + embedding[6] * -6 + embedding[7] * 24 + embedding[8] * 16 + embedding[9] * -4;
    let score_4 = embedding[0] * 11 + embedding[1] * -21 + embedding[2] * 72 + embedding[3] * -17 + embedding[4] * 6 + embedding[5] * 17 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -40 + embedding[9] * 4;
    let score_5 = embedding[0] * 12 + embedding[1] * 1 + embedding[2] * -17 + embedding[3] * 47 + embedding[5] * -15 + embedding[6] * 20 + embedding[7] * -25 + embedding[8] * -2 + embedding[9] * -13;
    let score_6 = embedding[0] * 31 + embedding[1] * -18 + embedding[2] * 19 + embedding[3] * 50 + embedding[4] * 9 + embedding[5] * -2 + embedding[6] * 21 + embedding[7] * -1 + embedding[8] * -21 + embedding[9] * -16;
    let score_7 = embedding[0] * 28 + embedding[1] * -10 + embedding[2] * 13 + embedding[3] * -3 + embedding[4] * 16 + embedding[5] * -2 + embedding[6] * -25 + embedding[7] * 63 + embedding[8] * -6 + embedding[9] * -3;
    let score_8 = embedding[0] * -14 + embedding[1] * -9 + embedding[2] * -21 + embedding[4] * 13 + embedding[5] * -41 + embedding[6] * 12 + embedding[7] * -18 + embedding[8] * -30 + embedding[9] * -5;
    let score_9 = embedding[0] * 12 + embedding[1] * 4 + embedding[2] * -15 + embedding[3] * 11 + embedding[4] * 11 + embedding[6] * -5 + embedding[7] * 1 + embedding[8] * 5 + embedding[9] * -4;
    let score_10 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_11 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_12 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * -19 + embedding[1] * 5 + embedding[2] * -18 + embedding[3] * 13 + embedding[4] * -18 + embedding[5] * 30 + embedding[6] * -11 + embedding[7] * 3 + embedding[8] * 49 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 17;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 9;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 10;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 13;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 12;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 68;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 101;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_175(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -23 + embedding[1] * 5 + embedding[2] * 11 + embedding[3] * -60 + embedding[4] * -16 + embedding[5] * 13 + embedding[6] * -21 + embedding[7] * -5 + embedding[8] * -2 + embedding[9] * 22;
    let score_1 = embedding[0] * -24 + embedding[2] * -5 + embedding[3] * -10 + embedding[4] * -17 + embedding[5] * 28 + embedding[6] * -21 + embedding[7] * 25 + embedding[8] * 34 + embedding[9] * -1;
    let score_2 = embedding[0] * -27 + embedding[1] * 18 + embedding[2] * -32 + embedding[3] * 5 + embedding[4] * 4 + embedding[5] * -48 + embedding[6] * 14 + embedding[7] * -32 + embedding[8] * -14 + embedding[9] * 7;
    let score_3 = embedding[0] * -21 + embedding[1] * -6 + embedding[2] * -18 + embedding[3] * -26 + embedding[4] * 6 + embedding[5] * -8 + embedding[6] * -1 + embedding[7] * -4 + embedding[8] * -24 + embedding[9] * -4;
    let score_4 = embedding[0] * -6 + embedding[1] * -26 + embedding[3] * -13 + embedding[4] * 16 + embedding[5] * -15 + embedding[6] * 25 + embedding[7] * -5 + embedding[8] * -14 + embedding[9] * 11;
    let score_5 = embedding[0] * -17 + embedding[1] * -22 + embedding[2] * 1 + embedding[3] * 16 + embedding[4] * 17 + embedding[5] * -11 + embedding[6] * 3 + embedding[7] * -10 + embedding[8] * -43 + embedding[9] * 14;
    let score_6 = embedding[0] * -5 + embedding[1] * -24 + embedding[2] * -10 + embedding[3] * 49 + embedding[4] * 15 + embedding[5] * 9 + embedding[6] * 11 + embedding[7] * 17 + embedding[8] * 8 + embedding[9] * -27;
    let score_7 = embedding[0] * 25 + embedding[1] * 7 + embedding[2] * -19 + embedding[3] * 108 + embedding[4] * -12 + embedding[5] * -23 + embedding[6] * 36 + embedding[7] * -22 + embedding[8] * 6 + embedding[9] * -22;
    let score_8 = embedding[0] * -7 + embedding[1] * 39 + embedding[2] * -18 + embedding[3] * 6 + embedding[4] * -11 + embedding[5] * -40 + embedding[6] * 8 + embedding[7] * -21 + embedding[8] * 1 + embedding[9] * 15;
    let score_9 = embedding[0] * 54 + embedding[1] * 13 + embedding[2] * 22 + embedding[3] * 56 + embedding[4] * 5 + embedding[5] * 17 + embedding[7] * 30 + embedding[8] * 20 + embedding[9] * -10;
    let score_10 = embedding[0] * 24 + embedding[1] * 9 + embedding[2] * 13 + embedding[3] * 11 + embedding[4] * -16 + embedding[5] * 17 + embedding[7] * -3 + embedding[8] * 14 + embedding[9] * -2;
    let score_11 = embedding[0] * 35 + embedding[1] * -13 + embedding[2] * 50 + embedding[3] * 19 + embedding[4] * 4 + embedding[5] * 34 + embedding[6] * -15 + embedding[7] * 43 + embedding[8] * 1 + embedding[9] * -7;
    let score_12 = embedding[0] * 3 + embedding[1] * 21 + embedding[2] * 42 + embedding[3] * -53 + embedding[4] * -14 + embedding[5] * 8 + embedding[6] * -27 + embedding[7] * 31 + embedding[8] * -4 + embedding[9] * 28;
    let score_13 = embedding[0] * 21 + embedding[1] * 22 + embedding[2] * -16 + embedding[3] * 61 + embedding[4] * 27 + embedding[5] * -62 + embedding[6] * 24 + embedding[7] * 18 + embedding[8] * -14 + embedding[9] * -4;
    let score_14 = embedding[0] * 34 + embedding[1] * -27 + embedding[2] * 20 + embedding[3] * 94 + embedding[4] * 13 + embedding[5] * 5 + embedding[6] * 31 + embedding[7] * 11 + embedding[8] * -15 + embedding[9] * -27;
    let score_15 = embedding[0] * 33 + embedding[1] * -1 + embedding[2] * 20 + embedding[3] * 60 + embedding[4] * -15 + embedding[5] * 30 + embedding[6] * -1 + embedding[7] * 31 + embedding[8] * 27 + embedding[9] * -15;
    let score_16 = embedding[0] * -19 + embedding[1] * -13 + embedding[2] * 8 + embedding[3] * -3 + embedding[4] * 8 + embedding[5] * -27 + embedding[6] * -6 + embedding[7] * -18 + embedding[8] * -12 + embedding[9] * -5;
    let score_17 = embedding[0] * -9 + embedding[1] * -2 + embedding[2] * -27 + embedding[3] * 39 + embedding[4] * 12 + embedding[5] * -35 + embedding[6] * 22 + embedding[7] * -31 + embedding[8] * -20 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 11;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 3;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 2;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 4;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 6;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 10;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 21;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 14;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 9;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 36;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 8;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 13;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 17;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 5;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 68;
    }
    best_palette
}

fn predict_pixel_176(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -31 + embedding[1] * -1 + embedding[2] * 16 + embedding[3] * -66 + embedding[4] * -4 + embedding[5] * 8 + embedding[6] * -24 + embedding[7] * -2 + embedding[8] * -4 + embedding[9] * 22;
    let score_1 = embedding[0] * -20 + embedding[1] * -1 + embedding[2] * -10 + embedding[3] * -11 + embedding[4] * -18 + embedding[5] * 22 + embedding[6] * -12 + embedding[7] * -3 + embedding[8] * 22;
    let score_2 = embedding[0] * -7 + embedding[1] * -13 + embedding[2] * 9 + embedding[3] * -27 + embedding[4] * 4 + embedding[5] * -2 + embedding[6] * 4 + embedding[7] * 6 + embedding[8] * -43 + embedding[9] * -8;
    let score_3 = embedding[0] * -7 + embedding[1] * -28 + embedding[2] * 17 + embedding[3] * -28 + embedding[4] * 18 + embedding[5] * -12 + embedding[6] * 21 + embedding[7] * 1 + embedding[8] * -18 + embedding[9] * 18;
    let score_4 = embedding[0] * 51 + embedding[1] * 6 + embedding[2] * 7 + embedding[3] * 78 + embedding[4] * 1 + embedding[5] * 13 + embedding[6] * 11 + embedding[7] * 13 + embedding[8] * 18 + embedding[9] * -18;
    let score_5 = embedding[0] * -17 + embedding[1] * -27 + embedding[2] * 18 + embedding[3] * 2 + embedding[4] * 19 + embedding[5] * -5 + embedding[7] * -4 + embedding[8] * -48 + embedding[9] * 17;
    let score_6 = embedding[0] * -11 + embedding[1] * 21 + embedding[2] * -38 + embedding[3] * 40 + embedding[4] * 4 + embedding[5] * -34 + embedding[6] * 17 + embedding[7] * -26 + embedding[8] * 7 + embedding[9] * -5;
    let score_7 = embedding[0] * -14 + embedding[1] * 38 + embedding[2] * -33 + embedding[3] * 22 + embedding[4] * -9 + embedding[5] * -28 + embedding[6] * 2 + embedding[7] * -15 + embedding[8] * 21 + embedding[9] * 5;
    let score_8 = embedding[0] * 7 + embedding[1] * -8 + embedding[2] * -30 + embedding[3] * 79 + embedding[4] * 8 + embedding[5] * -13 + embedding[6] * 24 + embedding[7] * -1 + embedding[8] * 6 + embedding[9] * -28;
    let score_9 = embedding[0] * -6 + embedding[1] * 24 + embedding[2] * -44 + embedding[3] * 48 + embedding[4] * 20 + embedding[5] * -57 + embedding[6] * 10 + embedding[7] * 24 + embedding[8] * 6 + embedding[9] * -3;
    let score_10 = embedding[0] * 27 + embedding[1] * 9 + embedding[2] * -24 + embedding[3] * 114 + embedding[4] * -10 + embedding[5] * -24 + embedding[6] * 37 + embedding[7] * -25 + embedding[8] * 9 + embedding[9] * -23;
    let score_11 = embedding[0] * 27 + embedding[1] * 4 + embedding[2] * 6 + embedding[3] * 31 + embedding[4] * -11 + embedding[5] * 14 + embedding[6] * 9 + embedding[7] * -10 + embedding[8] * 12 + embedding[9] * -8;
    let score_12 = embedding[0] * 22 + embedding[1] * -26 + embedding[2] * 78 + embedding[3] * -4 + embedding[4] * 1 + embedding[5] * 21 + embedding[6] * 1 + embedding[7] * 15 + embedding[8] * -30;
    let score_13 = embedding[0] * 2 + embedding[1] * 14 + embedding[2] * 53 + embedding[3] * -59 + embedding[4] * -6 + embedding[5] * 5 + embedding[6] * -28 + embedding[7] * 39 + embedding[8] * -9 + embedding[9] * 31;
    let score_14 = embedding[0] * 24 + embedding[1] * 8 + embedding[2] * 36 + embedding[3] * -2 + embedding[4] * -37 + embedding[5] * 37 + embedding[6] * -30 + embedding[7] * 49 + embedding[8] * 16 + embedding[9] * 20;
    let score_15 = embedding[0] * 41 + embedding[1] * -24 + embedding[2] * 21 + embedding[3] * 93 + embedding[4] * 11 + embedding[5] * 6 + embedding[6] * 32 + embedding[7] * 9 + embedding[8] * -11 + embedding[9] * -27;
    let score_16 = embedding[0] * -28 + embedding[1] * -1 + embedding[2] * 25 + embedding[3] * -33 + embedding[4] * -3 + embedding[5] * -9 + embedding[6] * -11 + embedding[7] * -16 + embedding[8] * 2;
    let score_17 = embedding[0] * -1 + embedding[1] * -1 + embedding[2] * -30 + embedding[3] * 58 + embedding[4] * 9 + embedding[5] * -23 + embedding[6] * 22 + embedding[7] * -28 + embedding[8] * -5 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 2;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 14;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 4;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 11;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 21;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 6;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 8;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 10;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 19;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 9;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 36;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 20;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 13;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 5;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 68;
    }
    best_palette
}

fn predict_pixel_177(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -21 + embedding[1] * -2 + embedding[2] * -3 + embedding[3] * -49 + embedding[4] * 1 + embedding[5] * 11 + embedding[6] * -18 + embedding[7] * -3 + embedding[8] * -1 + embedding[9] * 9;
    let score_1 = embedding[0] * -26 + embedding[1] * 9 + embedding[2] * -24 + embedding[3] * -7 + embedding[4] * 8 + embedding[5] * -40 + embedding[6] * 1 + embedding[7] * -27 + embedding[8] * -9 + embedding[9] * 2;
    let score_2 = embedding[0] * 14 + embedding[1] * -13 + embedding[2] * -22 + embedding[3] * 78 + embedding[4] * 10 + embedding[5] * 1 + embedding[6] * 19 + embedding[8] * 7 + embedding[9] * -27;
    let score_3 = embedding[0] * -17 + embedding[1] * -2 + embedding[2] * -2 + embedding[3] * -37 + embedding[4] * -4 + embedding[5] * 6 + embedding[6] * -6 + embedding[7] * -10 + embedding[8] * -11 + embedding[9] * -3;
    let score_4 = embedding[0] * 67 + embedding[1] * 3 + embedding[2] * 53 + embedding[3] * 90 + embedding[4] * -16 + embedding[5] * 28 + embedding[6] * 11 + embedding[7] * 27 + embedding[8] * 18 + embedding[9] * -12;
    let score_5 = embedding[0] * -14 + embedding[1] * -20 + embedding[3] * -25 + embedding[4] * 4 + embedding[5] * -3 + embedding[6] * 15 + embedding[7] * -12 + embedding[8] * -6 + embedding[9] * 11;
    let score_6 = embedding[0] * -21 + embedding[1] * -18 + embedding[3] * -4 + embedding[4] * 2 + embedding[5] * 7 + embedding[6] * -3 + embedding[7] * -17 + embedding[8] * -25 + embedding[9] * 15;
    let score_7 = embedding[0] * -21 + embedding[1] * 35 + embedding[2] * -28 + embedding[4] * -6 + embedding[5] * -36 + embedding[6] * -5 + embedding[7] * -12 + embedding[8] * 12 + embedding[9] * 9;
    let score_8 = embedding[0] * -15 + embedding[1] * 25 + embedding[2] * -43 + embedding[3] * 28 + embedding[4] * 24 + embedding[5] * -67 + embedding[6] * 6 + embedding[7] * 30 + embedding[8] * 1 + embedding[9] * 4;
    let score_9 = embedding[0] * 27 + embedding[1] * 3 + embedding[2] * -20 + embedding[3] * 120 + embedding[4] * -14 + embedding[5] * -18 + embedding[6] * 36 + embedding[7] * -22 + embedding[8] * 10 + embedding[9] * -24;
    let score_10 = embedding[0] * 40 + embedding[1] * -10 + embedding[2] * 36 + embedding[3] * 38 + embedding[4] * -5 + embedding[5] * 32 + embedding[6] * -1 + embedding[7] * 10 + embedding[8] * 6 + embedding[9] * -10;
    let score_11 = embedding[0] * 26 + embedding[1] * -6 + embedding[2] * 41 + embedding[3] * 1 + embedding[4] * -10 + embedding[5] * 29 + embedding[6] * -3 + embedding[7] * 8 + embedding[8] * -9 + embedding[9] * -4;
    let score_12 = embedding[0] * 8 + embedding[1] * 12 + embedding[2] * 19 + embedding[3] * -32 + embedding[5] * 10 + embedding[6] * -19 + embedding[7] * 21 + embedding[8] * 6 + embedding[9] * 14;
    let score_13 = embedding[0] * 7 + embedding[1] * 7 + embedding[2] * 36 + embedding[3] * -16 + embedding[4] * -48 + embedding[5] * 30 + embedding[6] * -19 + embedding[7] * 37 + embedding[8] * 5 + embedding[9] * 22;
    let score_14 = embedding[0] * 45 + embedding[1] * -25 + embedding[2] * 24 + embedding[3] * 110 + embedding[5] * 10 + embedding[6] * 35 + embedding[7] * 6 + embedding[8] * -7 + embedding[9] * -30;
    let score_15 = embedding[0] * -41 + embedding[1] * -7 + embedding[2] * 13 + embedding[3] * -16 + embedding[4] * 7 + embedding[5] * -8 + embedding[6] * -9 + embedding[7] * 30 + embedding[8] * 26 + embedding[9] * 6;
    let score_16 = embedding[0] * -24 + embedding[1] * -6 + embedding[2] * -32 + embedding[3] * 28 + embedding[4] * 18 + embedding[5] * -75 + embedding[6] * 26 + embedding[7] * -8 + embedding[8] * -42 + embedding[9] * -3;
    let score_17 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * -9 + embedding[3] * 37 + embedding[4] * -9 + embedding[5] * -5 + embedding[6] * 6 + embedding[7] * -25 + embedding[8] * -2 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 11;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 82;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 14;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 83;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 84;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 21;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 8;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 10;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 104;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 9;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 36;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 44;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 13;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 15;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 68;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 134;
    }
    best_palette
}

fn predict_pixel_178(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -23 + embedding[2] * 4 + embedding[3] * -59 + embedding[4] * -4 + embedding[5] * 9 + embedding[6] * -26 + embedding[7] * 6 + embedding[9] * 18;
    let score_1 = embedding[0] * 3 + embedding[1] * -5 + embedding[2] * -7 + embedding[3] * -15 + embedding[4] * 4 + embedding[5] * 8 + embedding[6] * -4 + embedding[7] * -2 + embedding[8] * -23 + embedding[9] * -7;
    let score_2 = embedding[0] * -1 + embedding[1] * -23 + embedding[2] * 11 + embedding[3] * -25 + embedding[4] * 14 + embedding[5] * -3 + embedding[6] * 14 + embedding[7] * -2 + embedding[8] * -9 + embedding[9] * 16;
    let score_3 = embedding[0] * 4 + embedding[1] * -15 + embedding[2] * -17 + embedding[3] * 88 + embedding[4] * 8 + embedding[5] * -4 + embedding[6] * 29 + embedding[7] * 8 + embedding[8] * 12 + embedding[9] * -29;
    let score_4 = embedding[0] * -5 + embedding[1] * -21 + embedding[2] * 9 + embedding[3] * 1 + embedding[4] * 12 + embedding[5] * 9 + embedding[6] * -2 + embedding[7] * -6 + embedding[8] * -35 + embedding[9] * 14;
    let score_5 = embedding[0] * 55 + embedding[1] * 4 + embedding[2] * 46 + embedding[3] * 63 + embedding[4] * -23 + embedding[5] * 32 + embedding[6] * 7 + embedding[7] * 22 + embedding[8] * 11 + embedding[9] * -11;
    let score_6 = embedding[0] * -10 + embedding[1] * 21 + embedding[2] * -36 + embedding[3] * 33 + embedding[4] * 2 + embedding[5] * -34 + embedding[6] * 16 + embedding[7] * -25 + embedding[8] * 3 + embedding[9] * -4;
    let score_7 = embedding[0] * -15 + embedding[1] * 39 + embedding[2] * -32 + embedding[3] * 18 + embedding[4] * -9 + embedding[5] * -34 + embedding[6] * 3 + embedding[7] * -15 + embedding[8] * 17 + embedding[9] * 6;
    let score_8 = embedding[0] * -12 + embedding[1] * 28 + embedding[2] * -41 + embedding[3] * 28 + embedding[4] * 24 + embedding[5] * -63 + embedding[6] * 8 + embedding[7] * 32 + embedding[8] * 1 + embedding[9] * 3;
    let score_9 = embedding[0] * 18 + embedding[1] * 8 + embedding[2] * -22 + embedding[3] * 114 + embedding[4] * -13 + embedding[5] * -26 + embedding[6] * 38 + embedding[7] * -28 + embedding[8] * 4 + embedding[9] * -23;
    let score_10 = embedding[0] * 29 + embedding[1] * -6 + embedding[2] * 23 + embedding[3] * 25 + embedding[4] * -20 + embedding[5] * 24 + embedding[6] * 9 + embedding[7] * -12 + embedding[8] * -7 + embedding[9] * -9;
    let score_11 = embedding[0] * 30 + embedding[1] * -28 + embedding[2] * 70 + embedding[3] * 10 + embedding[4] * -1 + embedding[5] * 22 + embedding[6] * 3 + embedding[7] * 12 + embedding[8] * -28 + embedding[9] * -5;
    let score_12 = embedding[0] * 18 + embedding[1] * 6 + embedding[2] * 37 + embedding[3] * -13 + embedding[4] * -38 + embedding[5] * 33 + embedding[6] * -29 + embedding[7] * 53 + embedding[8] * 10 + embedding[9] * 25;
    let score_13 = embedding[0] * -22 + embedding[1] * -1 + embedding[2] * 20 + embedding[3] * -26 + embedding[4] * 1 + embedding[5] * -8 + embedding[6] * -8 + embedding[7] * -14 + embedding[8] * 7 + embedding[9] * -2;
    let score_14 = embedding[0] * 37 + embedding[1] * -32 + embedding[2] * 31 + embedding[3] * 83 + embedding[4] * 5 + embedding[5] * 7 + embedding[6] * 34 + embedding[7] * 5 + embedding[8] * -23 + embedding[9] * -29;
    let score_15 = embedding[0] * -35 + embedding[1] * 3 + embedding[2] * -13 + embedding[3] * -25 + embedding[4] * -15 + embedding[5] * -11 + embedding[6] * -4 + embedding[7] * -7 + embedding[8] * 4 + embedding[9] * 9;
    let score_16 = embedding[0] * -18 + embedding[1] * 5 + embedding[2] * 8 + embedding[3] * -20 + embedding[4] * 10 + embedding[5] * -7 + embedding[6] * -12 + embedding[7] * 4 + embedding[8] * 26 + embedding[9] * 13;
    let score_17 = embedding[0] * -1 + embedding[1] * -1 + embedding[2] * -28 + embedding[3] * 47 + embedding[4] * 8 + embedding[5] * -22 + embedding[6] * 21 + embedding[7] * -28 + embedding[8] * -10 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 4;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 14;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 11;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 21;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 8;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 10;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 9;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 20;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 5;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 13;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 90;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 15;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 68;
    }
    best_palette
}

fn predict_pixel_179(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -21 + embedding[1] * -4 + embedding[2] * 5 + embedding[3] * -51 + embedding[4] * 6 + embedding[5] * 7 + embedding[6] * -17 + embedding[7] * -5 + embedding[8] * -7 + embedding[9] * 10;
    let score_1 = embedding[0] * -28 + embedding[1] * 26 + embedding[2] * -27 + embedding[3] * -26 + embedding[4] * 6 + embedding[5] * -29 + embedding[6] * -11 + embedding[7] * -4 + embedding[8] * 7 + embedding[9] * 12;
    let score_2 = embedding[0] * -23 + embedding[1] * 22 + embedding[2] * -32 + embedding[3] * -18 + embedding[4] * 16 + embedding[5] * -32 + embedding[6] * -14 + embedding[7] * 18 + embedding[8] * 2 + embedding[9] * 8;
    let score_3 = embedding[0] * 8 + embedding[1] * -20 + embedding[2] * -13 + embedding[3] * 77 + embedding[4] * 6 + embedding[5] * 2 + embedding[6] * 26 + embedding[7] * 13 + embedding[8] * 7 + embedding[9] * -26;
    let score_4 = embedding[0] * 61 + embedding[1] * 7 + embedding[2] * 54 + embedding[3] * 70 + embedding[4] * -22 + embedding[5] * 30 + embedding[6] * 8 + embedding[7] * 25 + embedding[8] * 18 + embedding[9] * -11;
    let score_5 = embedding[0] * -25 + embedding[1] * 50 + embedding[2] * -35 + embedding[3] * -4 + embedding[4] * -6 + embedding[5] * -47 + embedding[6] * -7 + embedding[7] * 1 + embedding[8] * 17 + embedding[9] * 17;
    let score_6 = embedding[0] * 23 + embedding[2] * -8 + embedding[3] * 104 + embedding[4] * -17 + embedding[5] * -12 + embedding[6] * 33 + embedding[7] * -19 + embedding[8] * 8 + embedding[9] * -22;
    let score_7 = embedding[0] * 5 + embedding[1] * -18 + embedding[2] * -1 + embedding[4] * 2 + embedding[5] * 10 + embedding[6] * 2 + embedding[7] * -7 + embedding[8] * -20 + embedding[9] * -11;
    let score_8 = embedding[0] * -2 + embedding[1] * -27 + embedding[2] * 10 + embedding[3] * -12 + embedding[4] * 8 + embedding[5] * 4 + embedding[6] * 17 + embedding[7] * -10 + embedding[8] * -10 + embedding[9] * 4;
    let score_9 = embedding[0] * -6 + embedding[1] * -30 + embedding[2] * 9 + embedding[3] * 19 + embedding[4] * 7 + embedding[5] * 9 + embedding[6] * 4 + embedding[7] * -11 + embedding[8] * -33 + embedding[9] * 2;
    let score_10 = embedding[0] * 30 + embedding[1] * -8 + embedding[2] * 28 + embedding[3] * 26 + embedding[4] * -21 + embedding[5] * 23 + embedding[6] * 10 + embedding[7] * -11 + embedding[8] * -3 + embedding[9] * -8;
    let score_11 = embedding[0] * 37 + embedding[1] * -19 + embedding[2] * 54 + embedding[3] * 21 + embedding[4] * 3 + embedding[5] * 27 + embedding[6] * -4 + embedding[7] * 24 + embedding[8] * -14 + embedding[9] * -9;
    let score_12 = embedding[0] * 5 + embedding[1] * 6 + embedding[2] * 34 + embedding[3] * -9 + embedding[4] * -50 + embedding[5] * 29 + embedding[6] * -14 + embedding[7] * 34 + embedding[8] * 8 + embedding[9] * 22;
    let score_13 = embedding[0] * 37 + embedding[1] * -37 + embedding[2] * 35 + embedding[3] * 87 + embedding[4] * 5 + embedding[5] * 15 + embedding[6] * 30 + embedding[7] * 8 + embedding[8] * -20 + embedding[9] * -26;
    let score_14 = embedding[0] * -34 + embedding[1] * 10 + embedding[2] * -19 + embedding[3] * -30 + embedding[4] * -10 + embedding[5] * -16 + embedding[6] * -4 + embedding[7] * -7 + embedding[9] * 13;
    let score_15 = embedding[0] * 7 + embedding[1] * 20 + embedding[2] * -1 + embedding[3] * 18 + embedding[4] * -5 + embedding[5] * -6 + embedding[6] * 4 + embedding[7] * 1 + embedding[8] * 30 + embedding[9] * 3;
    let score_16 = embedding[0] * -4 + embedding[1] * -14 + embedding[2] * -13 + embedding[3] * 26 + embedding[4] * 7 + embedding[5] * -8 + embedding[6] * 11 + embedding[7] * -21 + embedding[8] * -15 + embedding[9] * -6;
    let score_17 = embedding[0] * -5 + embedding[1] * -24 + embedding[2] * 18 + embedding[3] * 1 + embedding[4] * 1 + embedding[5] * -3 + embedding[6] * -3 + embedding[7] * -14 + embedding[8] * -10 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 11;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 14;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 21;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 10;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 3;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 2;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 4;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 9;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 44;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 13;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 7;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 15;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 68;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 5;
    }
    best_palette
}

fn predict_pixel_180(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -14 + embedding[1] * -4 + embedding[2] * 16 + embedding[3] * -52 + embedding[4] * -2 + embedding[5] * 14 + embedding[6] * -20 + embedding[7] * -3 + embedding[8] * -6 + embedding[9] * 17;
    let score_1 = embedding[0] * -11 + embedding[1] * 11 + embedding[2] * -29 + embedding[3] * 12 + embedding[4] * 15 + embedding[5] * -29 + embedding[6] * 1 + embedding[7] * 7 + embedding[9] * -3;
    let score_2 = embedding[0] * 6 + embedding[1] * -18 + embedding[2] * -22 + embedding[3] * 95 + embedding[4] * 6 + embedding[5] * -8 + embedding[6] * 34 + embedding[7] * 10 + embedding[8] * 9 + embedding[9] * -30;
    let score_3 = embedding[0] * 52 + embedding[1] * 12 + embedding[2] * 45 + embedding[3] * 62 + embedding[4] * -24 + embedding[5] * 28 + embedding[6] * 6 + embedding[7] * 28 + embedding[8] * 23 + embedding[9] * -5;
    let score_4 = embedding[0] * -10 + embedding[1] * 16 + embedding[2] * -33 + embedding[3] * 42 + embedding[5] * -35 + embedding[6] * 19 + embedding[7] * -26 + embedding[8] * 5 + embedding[9] * -8;
    let score_5 = embedding[0] * -18 + embedding[1] * -12 + embedding[2] * -18 + embedding[3] * 10 + embedding[4] * 2 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * -20 + embedding[8] * -12 + embedding[9] * -7;
    let score_6 = embedding[0] * -5 + embedding[1] * 39 + embedding[2] * -24 + embedding[3] * 23 + embedding[4] * -13 + embedding[5] * -30 + embedding[6] * 5 + embedding[7] * -12 + embedding[8] * 21 + embedding[9] * 6;
    let score_7 = embedding[0] * -6 + embedding[1] * -9 + embedding[2] * 17 + embedding[3] * -39 + embedding[4] * 1 + embedding[5] * 16 + embedding[6] * -18 + embedding[7] * 6 + embedding[8] * -10 + embedding[9] * 10;
    let score_8 = embedding[0] * -3 + embedding[1] * -8 + embedding[2] * 2 + embedding[3] * -26 + embedding[4] * 9 + embedding[5] * 1 + embedding[6] * -11 + embedding[7] * 7 + embedding[8] * -22;
    let score_9 = embedding[0] * 18 + embedding[1] * 6 + embedding[2] * -21 + embedding[3] * 118 + embedding[4] * -16 + embedding[5] * -29 + embedding[6] * 40 + embedding[7] * -26 + embedding[8] * 10 + embedding[9] * -23;
    let score_10 = embedding[0] * -4 + embedding[1] * -16 + embedding[2] * 10 + embedding[3] * -30 + embedding[4] * 14 + embedding[5] * -2 + embedding[6] * -2 + embedding[7] * 4 + embedding[8] * -13 + embedding[9] * 12;
    let score_11 = embedding[0] * -10 + embedding[1] * -15 + embedding[2] * 11 + embedding[3] * -19 + embedding[4] * 13 + embedding[5] * 2 + embedding[6] * -11 + embedding[7] * 2 + embedding[8] * -25 + embedding[9] * 11;
    let score_12 = embedding[0] * 21 + embedding[1] * -1 + embedding[2] * 20 + embedding[3] * 13 + embedding[4] * -24 + embedding[5] * 20 + embedding[6] * 5 + embedding[7] * -8 + embedding[8] * 3 + embedding[9] * -1;
    let score_13 = embedding[0] * 23 + embedding[1] * 6 + embedding[2] * 39 + embedding[3] * -2 + embedding[4] * -36 + embedding[5] * 36 + embedding[6] * -24 + embedding[7] * 46 + embedding[8] * 12 + embedding[9] * 18;
    let score_14 = embedding[0] * 31 + embedding[1] * -29 + embedding[2] * 19 + embedding[3] * 100 + embedding[4] * 6 + embedding[5] * 2 + embedding[6] * 40 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * -32;
    let score_15 = embedding[0] * 12 + embedding[1] * 24 + embedding[2] * 1 + embedding[3] * 26 + embedding[4] * -12 + embedding[5] * -6 + embedding[6] * 4 + embedding[8] * 31 + embedding[9] * 4;
    let score_16 = embedding[0] * -2 + embedding[1] * -1 + embedding[2] * -27 + embedding[3] * 54 + embedding[4] * 5 + embedding[5] * -25 + embedding[6] * 22 + embedding[7] * -26 + embedding[8] * -4 + embedding[9] * -13;
    let score_17 = embedding[0] * -13 + embedding[1] * -10 + embedding[2] * 15 + embedding[3] * -18 + embedding[4] * 9 + embedding[5] * -12 + embedding[6] * -14 + embedding[7] * -3 + embedding[8] * -12 + embedding[9] * 2;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 8;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 14;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 11;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 21;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 9;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 3;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 10;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 2;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 4;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 19;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 20;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 13;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 15;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 68;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 5;
    }
    best_palette
}

fn predict_pixel_181(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -13 + embedding[1] * -3 + embedding[2] * 12 + embedding[3] * -54 + embedding[4] * 5 + embedding[5] * 20 + embedding[6] * -27 + embedding[7] * 6 + embedding[8] * 1 + embedding[9] * 10;
    let score_1 = embedding[0] * -23 + embedding[1] * 29 + embedding[2] * -38 + embedding[3] * 9 + embedding[5] * -51 + embedding[6] * 12 + embedding[7] * -27 + embedding[8] * -3 + embedding[9] * 7;
    let score_2 = embedding[0] * 4 + embedding[1] * -21 + embedding[2] * -12 + embedding[3] * 76 + embedding[4] * 6 + embedding[5] * 4 + embedding[6] * 25 + embedding[7] * 15 + embedding[8] * 11 + embedding[9] * -28;
    let score_3 = embedding[0] * 56 + embedding[1] * 2 + embedding[2] * 58 + embedding[3] * 47 + embedding[4] * -17 + embedding[5] * 37 + embedding[6] * -2 + embedding[7] * 32 + embedding[8] * 19 + embedding[9] * -7;
    let score_4 = embedding[0] * -2 + embedding[1] * -12 + embedding[2] * -9 + embedding[3] * -10 + embedding[4] * 6 + embedding[5] * 1 + embedding[6] * -1 + embedding[7] * -4 + embedding[8] * -21 + embedding[9] * -9;
    let score_5 = embedding[0] * -5 + embedding[1] * -35 + embedding[2] * 10 + embedding[3] * -15 + embedding[4] * 12 + embedding[5] * -4 + embedding[6] * 21 + embedding[7] * -3 + embedding[8] * -13 + embedding[9] * 10;
    let score_6 = embedding[0] * -18 + embedding[1] * 47 + embedding[2] * -36 + embedding[3] * 14 + embedding[4] * -10 + embedding[5] * -47 + embedding[6] * 6 + embedding[7] * -15 + embedding[8] * 15 + embedding[9] * 12;
    let score_7 = embedding[0] * -12 + embedding[1] * 27 + embedding[2] * -42 + embedding[3] * 27 + embedding[4] * 20 + embedding[5] * -68 + embedding[6] * 9 + embedding[7] * 23 + embedding[8] * -2 + embedding[9] * 4;
    let score_8 = embedding[0] * -15 + embedding[1] * -34 + embedding[2] * 8 + embedding[3] * 10 + embedding[4] * 10 + embedding[5] * -1 + embedding[6] * 1 + embedding[7] * -8 + embedding[8] * -42 + embedding[9] * 12;
    let score_9 = embedding[0] * 18 + embedding[1] * 7 + embedding[2] * -19 + embedding[3] * 107 + embedding[4] * -17 + embedding[5] * -27 + embedding[6] * 38 + embedding[7] * -27 + embedding[8] * 6 + embedding[9] * -20;
    let score_10 = embedding[0] * 25 + embedding[1] * -7 + embedding[2] * 28 + embedding[3] * 10 + embedding[4] * -18 + embedding[5] * 27 + embedding[6] * 2 + embedding[7] * -6 + embedding[8] * 3 + embedding[9] * -4;
    let score_11 = embedding[0] * 33 + embedding[1] * -16 + embedding[2] * 55 + embedding[3] * 3 + embedding[4] * 4 + embedding[5] * 35 + embedding[6] * -14 + embedding[7] * 33 + embedding[8] * -6 + embedding[9] * -5;
    let score_12 = embedding[0] * 9 + embedding[1] * 3 + embedding[2] * 45 + embedding[3] * -19 + embedding[4] * -50 + embedding[5] * 40 + embedding[6] * -23 + embedding[7] * 40 + embedding[8] * 7 + embedding[9] * 23;
    let score_13 = embedding[0] * 32 + embedding[1] * -35 + embedding[2] * 32 + embedding[3] * 81 + embedding[4] * 9 + embedding[5] * 13 + embedding[6] * 29 + embedding[7] * 11 + embedding[8] * -17 + embedding[9] * -26;
    let score_14 = embedding[0] * -28 + embedding[1] * 10 + embedding[2] * -15 + embedding[3] * -21 + embedding[4] * -11 + embedding[5] * -12 + embedding[6] * -1 + embedding[7] * -15 + embedding[8] * 4 + embedding[9] * 6;
    let score_15 = embedding[0] * 8 + embedding[1] * 12 + embedding[2] * 6 + embedding[3] * 7 + embedding[4] * -5 + embedding[5] * 1 + embedding[6] * 1 + embedding[8] * 28 + embedding[9] * 3;
    let score_16 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * -30 + embedding[3] * 33 + embedding[4] * 7 + embedding[5] * -33 + embedding[6] * 18 + embedding[7] * -28 + embedding[8] * -13 + embedding[9] * -2;
    let score_17 = embedding[0] * -19 + embedding[1] * -4 + embedding[2] * 25 + embedding[3] * -30 + embedding[4] * -2 + embedding[5] * -3 + embedding[6] * -11 + embedding[7] * -12 + embedding[8] * 4 + embedding[9] * -2;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 11;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 14;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 3;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 2;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 21;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 8;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 4;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 10;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 9;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 44;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 13;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 7;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 15;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 68;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 5;
    }
    best_palette
}

fn predict_pixel_182(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -25 + embedding[2] * 2 + embedding[3] * -60 + embedding[4] * -6 + embedding[5] * 8 + embedding[6] * -25 + embedding[7] * 3 + embedding[8] * 2 + embedding[9] * 17;
    let score_1 = embedding[0] * -3 + embedding[1] * -4 + embedding[2] * -1 + embedding[3] * -25 + embedding[4] * -1 + embedding[5] * 6 + embedding[6] * -5 + embedding[7] * -1 + embedding[8] * -26 + embedding[9] * -7;
    let score_2 = embedding[0] * -5 + embedding[1] * -21 + embedding[2] * 8 + embedding[3] * -27 + embedding[4] * 12 + embedding[5] * -4 + embedding[6] * 14 + embedding[7] * -3 + embedding[8] * -6 + embedding[9] * 16;
    let score_3 = embedding[0] * 8 + embedding[1] * -14 + embedding[2] * -19 + embedding[3] * 91 + embedding[4] * 8 + embedding[5] * -4 + embedding[6] * 28 + embedding[7] * 10 + embedding[8] * 12 + embedding[9] * -29;
    let score_4 = embedding[0] * -8 + embedding[1] * -17 + embedding[2] * 6 + embedding[4] * 9 + embedding[5] * 8 + embedding[6] * -6 + embedding[7] * -8 + embedding[8] * -27 + embedding[9] * 16;
    let score_5 = embedding[0] * 54 + embedding[1] * 6 + embedding[2] * 38 + embedding[3] * 72 + embedding[4] * -22 + embedding[5] * 28 + embedding[6] * 8 + embedding[7] * 19 + embedding[8] * 14 + embedding[9] * -12;
    let score_6 = embedding[0] * -8 + embedding[1] * 13 + embedding[2] * -37 + embedding[3] * 32 + embedding[4] * 9 + embedding[5] * -38 + embedding[6] * 15 + embedding[7] * -21 + embedding[8] * -1 + embedding[9] * -6;
    let score_7 = embedding[0] * -16 + embedding[1] * 37 + embedding[2] * -29 + embedding[3] * 6 + embedding[4] * -8 + embedding[5] * -30 + embedding[6] * -4 + embedding[7] * -10 + embedding[8] * 17 + embedding[9] * 8;
    let score_8 = embedding[0] * 6 + embedding[1] * 20 + embedding[2] * -42 + embedding[3] * 43 + embedding[4] * 23 + embedding[5] * -40 + embedding[6] * 5 + embedding[7] * 21 + embedding[8] * 5 + embedding[9] * -6;
    let score_9 = embedding[0] * 23 + embedding[1] * 6 + embedding[2] * -25 + embedding[3] * 111 + embedding[4] * -10 + embedding[5] * -24 + embedding[6] * 34 + embedding[7] * -23 + embedding[8] * 5 + embedding[9] * -23;
    let score_10 = embedding[0] * 27 + embedding[1] * -5 + embedding[2] * 20 + embedding[3] * 27 + embedding[4] * -21 + embedding[5] * 23 + embedding[6] * 7 + embedding[7] * -14 + embedding[8] * -3 + embedding[9] * -7;
    let score_11 = embedding[0] * 27 + embedding[1] * -26 + embedding[2] * 66 + embedding[3] * 9 + embedding[4] * -3 + embedding[5] * 21 + embedding[6] * 2 + embedding[7] * 10 + embedding[8] * -26 + embedding[9] * -4;
    let score_12 = embedding[0] * 17 + embedding[1] * 7 + embedding[2] * 35 + embedding[3] * -12 + embedding[4] * -40 + embedding[5] * 33 + embedding[6] * -29 + embedding[7] * 51 + embedding[8] * 12 + embedding[9] * 25;
    let score_13 = embedding[0] * -23 + embedding[1] * -1 + embedding[2] * 19 + embedding[3] * -32 + embedding[4] * -1 + embedding[5] * -8 + embedding[6] * -10 + embedding[7] * -13 + embedding[8] * 7;
    let score_14 = embedding[0] * 37 + embedding[1] * -28 + embedding[2] * 24 + embedding[3] * 92 + embedding[4] * 4 + embedding[5] * 3 + embedding[6] * 34 + embedding[7] * 5 + embedding[8] * -16 + embedding[9] * -30;
    let score_15 = embedding[0] * -18 + embedding[1] * 6 + embedding[2] * 7 + embedding[3] * -23 + embedding[4] * 8 + embedding[5] * -6 + embedding[6] * -13 + embedding[7] * 3 + embedding[8] * 27 + embedding[9] * 13;
    let score_16 = embedding[0] * -11 + embedding[1] * -3 + embedding[2] * -38 + embedding[3] * 48 + embedding[4] * 19 + embedding[5] * -62 + embedding[6] * 26 + embedding[7] * -3 + embedding[8] * -35 + embedding[9] * -12;
    let score_17 = embedding[0] * 3 + embedding[1] * 7 + embedding[2] * -10 + embedding[3] * 42 + embedding[4] * -6 + embedding[5] * -1 + embedding[6] * 6 + embedding[7] * -22 + embedding[8] * -1 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 4;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 14;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 11;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 21;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 8;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 10;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 9;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 20;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 5;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 13;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 15;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 68;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 51;
    }
    best_palette
}

fn predict_pixel_183(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -17 + embedding[1] * 2 + embedding[2] * 18 + embedding[3] * -65 + embedding[4] * -11 + embedding[5] * 14 + embedding[6] * -21 + embedding[7] * 1 + embedding[8] * -10 + embedding[9] * 24;
    let score_1 = embedding[0] * -19 + embedding[1] * -13 + embedding[2] * -14 + embedding[3] * -2 + embedding[4] * -7 + embedding[5] * 20 + embedding[6] * -8 + embedding[7] * -5 + embedding[8] * 14 + embedding[9] * -8;
    let score_2 = embedding[0] * -34 + embedding[1] * 29 + embedding[2] * -37 + embedding[3] * 3 + embedding[4] * 5 + embedding[5] * -52 + embedding[6] * 7 + embedding[7] * -26 + embedding[8] * 6 + embedding[9] * 8;
    let score_3 = embedding[0] * -1 + embedding[1] * -12 + embedding[2] * 7 + embedding[3] * -27 + embedding[4] * 5 + embedding[5] * 1 + embedding[7] * 8 + embedding[8] * -41 + embedding[9] * -6;
    let score_4 = embedding[0] * -3 + embedding[1] * -27 + embedding[2] * 11 + embedding[3] * -20 + embedding[4] * 21 + embedding[5] * -9 + embedding[6] * 19 + embedding[7] * -1 + embedding[8] * -18 + embedding[9] * 12;
    let score_5 = embedding[0] * 61 + embedding[1] * 2 + embedding[2] * 48 + embedding[3] * 61 + embedding[4] * -18 + embedding[5] * 31 + embedding[6] * 5 + embedding[7] * 28 + embedding[8] * 16 + embedding[9] * -10;
    let score_6 = embedding[0] * -11 + embedding[1] * -25 + embedding[2] * 12 + embedding[4] * 20 + embedding[5] * -4 + embedding[6] * -1 + embedding[7] * -4 + embedding[8] * -41 + embedding[9] * 14;
    let score_7 = embedding[0] * -26 + embedding[1] * 49 + embedding[2] * -36 + embedding[3] * 9 + embedding[4] * -8 + embedding[5] * -51 + embedding[6] * 4 + embedding[7] * -17 + embedding[8] * 19 + embedding[9] * 15;
    let score_8 = embedding[0] * -4 + embedding[1] * -18 + embedding[2] * -25 + embedding[3] * 56 + embedding[4] * 11 + embedding[5] * -2 + embedding[6] * 14 + embedding[7] * 1 + embedding[8] * 5 + embedding[9] * -25;
    let score_9 = embedding[0] * 1 + embedding[1] * 22 + embedding[2] * -40 + embedding[3] * 33 + embedding[4] * 27 + embedding[5] * -38 + embedding[6] * -3 + embedding[7] * 27 + embedding[8] * 8 + embedding[9] * -1;
    let score_10 = embedding[0] * 29 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * 21 + embedding[6] * 6 + embedding[7] * -6 + embedding[8] * 11 + embedding[9] * -4;
    let score_11 = embedding[0] * 40 + embedding[1] * -20 + embedding[2] * 54 + embedding[3] * 16 + embedding[4] * 7 + embedding[5] * 29 + embedding[6] * -5 + embedding[7] * 35 + embedding[8] * -16 + embedding[9] * -6;
    let score_12 = embedding[0] * 21 + embedding[1] * 2 + embedding[2] * -16 + embedding[3] * 97 + embedding[4] * -1 + embedding[5] * -23 + embedding[6] * 30 + embedding[7] * -18 + embedding[8] * 6 + embedding[9] * -22;
    let score_13 = embedding[0] * 10 + embedding[1] * 9 + embedding[2] * 42 + embedding[3] * -14 + embedding[4] * -64 + embedding[5] * 40 + embedding[6] * -19 + embedding[7] * 35 + embedding[8] * 7 + embedding[9] * 27;
    let score_14 = embedding[0] * 37 + embedding[1] * -34 + embedding[2] * 28 + embedding[3] * 85 + embedding[4] * 12 + embedding[5] * 4 + embedding[6] * 30 + embedding[7] * 12 + embedding[8] * -19 + embedding[9] * -27;
    let score_15 = embedding[0] * -22 + embedding[1] * 1 + embedding[2] * 14 + embedding[3] * -18 + embedding[4] * 2 + embedding[5] * -13 + embedding[6] * -9 + embedding[7] * -18 + embedding[8] * 4 + embedding[9] * -6;
    let score_16 = embedding[0] * -26 + embedding[1] * -5 + embedding[2] * -23 + embedding[4] * 21 + embedding[5] * -17 + embedding[7] * -16 + embedding[8] * -1 + embedding[9] * -2;
    let score_17 = embedding[0] * 12 + embedding[1] * 7 + embedding[2] * -13 + embedding[3] * 84 + embedding[4] * -23 + embedding[5] * -4 + embedding[6] * 28 + embedding[7] * -14 + embedding[8] * 19 + embedding[9] * -20;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 11;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 3;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 2;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 14;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 4;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 21;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 6;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 8;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 9;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 10;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 44;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 13;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 5;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 68;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 98;
    }
    best_palette
}

fn predict_pixel_184(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[1] * 23 + embedding[2] * 6 + embedding[3] * -60 + embedding[4] * -10 + embedding[5] * -2 + embedding[6] * -10 + embedding[7] * -46 + embedding[8] * -15 + embedding[9] * 25;
    let score_1 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_2 = embedding[0] * 47 + embedding[1] * 9 + embedding[2] * 25 + embedding[3] * 56 + embedding[4] * -5 + embedding[5] * 24 + embedding[6] * -1 + embedding[7] * 20 + embedding[8] * 19 + embedding[9] * -8;
    let score_3 = embedding[0] * -9 + embedding[1] * 15 + embedding[2] * -39 + embedding[3] * 17 + embedding[4] * 29 + embedding[5] * -50 + embedding[6] * -7 + embedding[7] * 40 + embedding[8] * 2 + embedding[9] * -1;
    let score_4 = embedding[0] * 18 + embedding[1] * 11 + embedding[2] * -15 + embedding[3] * 67 + embedding[4] * -16 + embedding[5] * -9 + embedding[6] * 22 + embedding[7] * -30 + embedding[8] * 11 + embedding[9] * -13;
    let score_5 = embedding[0] * -21 + embedding[1] * -32 + embedding[2] * -12 + embedding[3] * 43 + embedding[4] * 12 + embedding[5] * 10 + embedding[6] * 16 + embedding[7] * 12 + embedding[8] * -8 + embedding[9] * -22;
    let score_6 = embedding[0] * 11 + embedding[1] * -18 + embedding[2] * 75 + embedding[3] * -16 + embedding[4] * 1 + embedding[5] * 20 + embedding[6] * 2 + embedding[7] * 27 + embedding[8] * -45 + embedding[9] * 7;
    let score_7 = embedding[0] * 28 + embedding[1] * -25 + embedding[2] * 14 + embedding[3] * 80 + embedding[4] * 6 + embedding[5] * 7 + embedding[6] * 22 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * -25;
    let score_8 = embedding[0] * 16 + embedding[1] * -20 + embedding[2] * 8 + embedding[3] * 1 + embedding[4] * 17 + embedding[5] * -11 + embedding[6] * -25 + embedding[7] * 76 + embedding[8] * -8 + embedding[9] * -10;
    let score_9 = embedding[0] * -21 + embedding[1] * 5 + embedding[2] * -27 + embedding[4] * 8 + embedding[5] * -53 + embedding[6] * 13 + embedding[7] * -9 + embedding[8] * -32;
    let score_10 = embedding[0] * -43 + embedding[1] * -10 + embedding[2] * -16 + embedding[3] * -16 + embedding[4] * 2 + embedding[5] * -4 + embedding[6] * 5 + embedding[7] * 12 + embedding[8] * -10 + embedding[9] * -1;
    let score_11 = embedding[0] * -47 + embedding[1] * -12 + embedding[2] * 12 + embedding[3] * -3 + embedding[4] * 13 + embedding[6] * -2 + embedding[7] * 30 + embedding[8] * 23 + embedding[9] * -6;
    let score_12 = embedding[0] * 19 + embedding[1] * 18 + embedding[2] * -15 + embedding[3] * 21 + embedding[4] * 3 + embedding[5] * 1 + embedding[6] * -8 + embedding[7] * 1 + embedding[8] * 11;
    let score_13 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_14 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_15 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_16 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * -26 + embedding[1] * -7 + embedding[2] * -10 + embedding[3] * -4 + embedding[4] * -2 + embedding[5] * 22 + embedding[6] * -6 + embedding[7] * 1 + embedding[8] * 24 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 17;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 10;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 9;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 13;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 12;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 68;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 54;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 76;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 112;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_185(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 5;
    let score_1 = embedding[2] * 9 + embedding[3] * -38 + embedding[4] * 5 + embedding[5] * 13 + embedding[6] * -13 + embedding[7] * -1 + embedding[8] * -6 + embedding[9] * 10;
    let score_2 = embedding[0] * 2 + embedding[1] * 15 + embedding[2] * -21 + embedding[3] * -1 + embedding[4] * 29 + embedding[5] * -31 + embedding[6] * -13 + embedding[7] * 45 + embedding[8] * 4 + embedding[9] * -2;
    let score_3 = embedding[0] * 18 + embedding[1] * 13 + embedding[2] * -9 + embedding[3] * 44 + embedding[4] * -10 + embedding[5] * -8 + embedding[6] * 21 + embedding[7] * -34 + embedding[8] * 6 + embedding[9] * -10;
    let score_4 = embedding[0] * 13 + embedding[1] * -10 + embedding[3] * -11 + embedding[4] * 24 + embedding[5] * -8 + embedding[6] * -20 + embedding[7] * 57 + embedding[8] * -6 + embedding[9] * -7;
    let score_5 = embedding[0] * -9 + embedding[1] * -26 + embedding[2] * -2 + embedding[3] * 11 + embedding[4] * 21 + embedding[5] * -14 + embedding[6] * -8 + embedding[7] * 59 + embedding[8] * -7 + embedding[9] * -16;
    let score_6 = embedding[0] * -30 + embedding[1] * -1 + embedding[2] * -13 + embedding[3] * 12 + embedding[4] * -2 + embedding[5] * -17 + embedding[6] * 9 + embedding[7] * -17 + embedding[8] * 11 + embedding[9] * -10;
    let score_7 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 5;
    let score_8 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 5;
    let score_9 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 5;
    let score_10 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 5;
    let score_11 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 5;
    let score_12 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 5;
    let score_13 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 5;
    let score_14 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 5;
    let score_15 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 5;
    let score_16 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 5;
    let score_17 = embedding[0] * 6 + embedding[1] * 1 + embedding[2] * -11 + embedding[3] * -13 + embedding[4] * 8 + embedding[5] * 24 + embedding[6] * -8 + embedding[7] * -28 + embedding[8] * 13 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 10;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 11;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_186(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 5;
    let score_1 = embedding[0] * -26 + embedding[1] * -12 + embedding[3] * -31 + embedding[4] * 9 + embedding[5] * -1 + embedding[6] * -6 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 2;
    let score_2 = embedding[0] * 4 + embedding[1] * 18 + embedding[2] * -20 + embedding[4] * 28 + embedding[5] * -34 + embedding[6] * -14 + embedding[7] * 48 + embedding[8] * 7 + embedding[9] * -2;
    let score_3 = embedding[0] * 21 + embedding[1] * 16 + embedding[2] * -5 + embedding[3] * 38 + embedding[4] * -14 + embedding[5] * -4 + embedding[6] * 18 + embedding[7] * -39 + embedding[8] * 9 + embedding[9] * -7;
    let score_4 = embedding[0] * 13 + embedding[1] * -5 + embedding[2] * -1 + embedding[3] * -6 + embedding[4] * 23 + embedding[5] * -9 + embedding[6] * -20 + embedding[7] * 55 + embedding[9] * -7;
    let score_5 = embedding[0] * -17 + embedding[1] * -24 + embedding[2] * -3 + embedding[3] * -1 + embedding[4] * 20 + embedding[5] * -15 + embedding[6] * -9 + embedding[7] * 66 + embedding[8] * -12 + embedding[9] * -12;
    let score_6 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 5;
    let score_7 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 5;
    let score_8 = embedding[0] * -4 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 5;
    let score_9 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 5;
    let score_10 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 5;
    let score_11 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 5;
    let score_12 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 5;
    let score_13 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 5;
    let score_14 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 5;
    let score_15 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 5;
    let score_16 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 5;
    let score_17 = embedding[0] * 8 + embedding[1] * 3 + embedding[2] * -8 + embedding[3] * -18 + embedding[4] * 4 + embedding[5] * 27 + embedding[6] * -9 + embedding[7] * -33 + embedding[8] * 16 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 10;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_187(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_1 = embedding[0] * -17 + embedding[1] * -9 + embedding[2] * -7 + embedding[3] * -20 + embedding[4] * 2 + embedding[5] * 4 + embedding[6] * -7 + embedding[7] * 9 + embedding[8] * -1 + embedding[9] * -1;
    let score_2 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -10 + embedding[9] * 8;
    let score_17 = embedding[0] * 12 + embedding[1] * -2 + embedding[2] * -15 + embedding[3] * -14 + embedding[4] * 19 + embedding[5] * 11 + embedding[6] * -14 + embedding[7] * -22 + embedding[8] * 11 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_188(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_1 = embedding[0] * -1 + embedding[1] * -10 + embedding[2] * -12 + embedding[3] * 7 + embedding[4] * 13 + embedding[5] * -27 + embedding[6] * 7 + embedding[7] * -13 + embedding[8] * -16 + embedding[9] * -5;
    let score_2 = embedding[0] * -15 + embedding[1] * -6 + embedding[2] * -11 + embedding[3] * -14 + embedding[5] * 7 + embedding[7] * 6 + embedding[8] * -1;
    let score_3 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_4 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_17 = embedding[0] * 10 + embedding[1] * -2 + embedding[2] * -9 + embedding[3] * -17 + embedding[4] * 16 + embedding[5] * 15 + embedding[6] * -15 + embedding[7] * -18 + embedding[8] * 15 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 18;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_189(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_190(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_191(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_192(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_193(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_194(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_1 = embedding[0] * 34 + embedding[1] * 13 + embedding[2] * 20 + embedding[3] * 14 + embedding[4] * 5 + embedding[5] * 10 + embedding[6] * -7 + embedding[7] * 7 + embedding[8] * 13;
    let score_2 = embedding[0] * -8 + embedding[1] * -13 + embedding[2] * -14 + embedding[3] * 6 + embedding[4] * 12 + embedding[5] * -29 + embedding[6] * 7 + embedding[7] * -13 + embedding[8] * -19 + embedding[9] * -5;
    let score_3 = embedding[0] * -25 + embedding[1] * -9 + embedding[2] * -13 + embedding[3] * -17 + embedding[4] * -1 + embedding[5] * 5 + embedding[6] * 1 + embedding[7] * 5 + embedding[8] * -3;
    let score_4 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 10;
    let score_17 = embedding[1] * -6 + embedding[2] * -12 + embedding[3] * -20 + embedding[4] * 15 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -18 + embedding[8] * 11 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 14;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 18;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_195(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_1 = embedding[0] * -14 + embedding[1] * -11 + embedding[2] * -10 + embedding[3] * -18 + embedding[4] * 12 + embedding[5] * -6 + embedding[6] * -1 + embedding[7] * -1 + embedding[8] * -12 + embedding[9] * -2;
    let score_2 = embedding[0] * 42 + embedding[1] * 22 + embedding[2] * 25 + embedding[3] * 15 + embedding[4] * -3 + embedding[5] * 17 + embedding[6] * -6 + embedding[8] * 18 + embedding[9] * 1;
    let score_3 = embedding[0] * -16 + embedding[2] * -18 + embedding[3] * -4 + embedding[4] * 7 + embedding[5] * -20 + embedding[6] * -13 + embedding[7] * 38 + embedding[8] * 9 + embedding[9] * 1;
    let score_4 = embedding[0] * -19 + embedding[1] * -21 + embedding[2] * -5 + embedding[3] * 3 + embedding[4] * 14 + embedding[5] * -17 + embedding[6] * -4 + embedding[7] * 46 + embedding[8] * -7 + embedding[9] * -9;
    let score_5 = embedding[0] * -11 + embedding[1] * -14 + embedding[2] * -12 + embedding[3] * 5 + embedding[4] * 10 + embedding[5] * -28 + embedding[6] * 8 + embedding[7] * -6 + embedding[8] * -25 + embedding[9] * -5;
    let score_6 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_7 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_8 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_9 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_10 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_11 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_12 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_13 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_14 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_15 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_16 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_17 = embedding[0] * 7 + embedding[1] * 3 + embedding[2] * -9 + embedding[3] * -19 + embedding[4] * 7 + embedding[5] * 21 + embedding[6] * -13 + embedding[7] * -25 + embedding[8] * 17 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 17;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 18;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_196(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_1 = embedding[0] * -17 + embedding[1] * -11 + embedding[2] * -4 + embedding[3] * -34 + embedding[4] * 11 + embedding[5] * -2 + embedding[6] * -9 + embedding[7] * 14 + embedding[8] * -16 + embedding[9] * 7;
    let score_2 = embedding[0] * 34 + embedding[1] * 16 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * 2 + embedding[5] * 16 + embedding[6] * -2 + embedding[7] * 6 + embedding[8] * 15;
    let score_3 = embedding[0] * 19 + embedding[1] * 18 + embedding[2] * -8 + embedding[3] * 20 + embedding[4] * 23 + embedding[5] * -39 + embedding[6] * 2 + embedding[7] * 25 + embedding[8] * -5 + embedding[9] * -2;
    let score_4 = embedding[0] * 3 + embedding[1] * 38 + embedding[2] * 13 + embedding[3] * -3 + embedding[4] * -52 + embedding[5] * 4 + embedding[6] * 2 + embedding[7] * -28 + embedding[8] * 18 + embedding[9] * 20;
    let score_5 = embedding[0] * 23 + embedding[1] * -16 + embedding[2] * 15 + embedding[3] * 44 + embedding[4] * 9 + embedding[5] * 1 + embedding[6] * 10 + embedding[7] * 7 + embedding[8] * -12 + embedding[9] * -12;
    let score_6 = embedding[0] * 17 + embedding[1] * -12 + embedding[2] * 1 + embedding[3] * 5 + embedding[4] * 19 + embedding[5] * -5 + embedding[6] * -16 + embedding[7] * 50 + embedding[8] * -5 + embedding[9] * -8;
    let score_7 = embedding[0] * -18 + embedding[1] * -31 + embedding[3] * 2 + embedding[4] * 17 + embedding[5] * -10 + embedding[6] * -1 + embedding[7] * 44 + embedding[8] * -17 + embedding[9] * -15;
    let score_8 = embedding[0] * -8 + embedding[1] * -8 + embedding[2] * -18 + embedding[3] * 19 + embedding[4] * 12 + embedding[5] * -35 + embedding[6] * 10 + embedding[7] * -5 + embedding[8] * -20 + embedding[9] * -9;
    let score_9 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_10 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_11 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_12 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_13 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_14 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_15 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_16 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_17 = embedding[0] * -6 + embedding[1] * 4 + embedding[2] * -18 + embedding[3] * -18 + embedding[4] * -2 + embedding[5] * 19 + embedding[6] * -11 + embedding[7] * -28 + embedding[8] * 20 + embedding[9] * -3;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 14;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 16;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 13;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 18;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_197(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * -10 + embedding[3] * -26 + embedding[4] * 1 + embedding[5] * 13 + embedding[6] * -10 + embedding[7] * -17 + embedding[8] * 9 + embedding[9] * 2;
    let score_2 = embedding[0] * -12 + embedding[1] * 15 + embedding[2] * -29 + embedding[3] * 7 + embedding[4] * 29 + embedding[5] * -46 + embedding[6] * -5 + embedding[7] * 42 + embedding[8] * 1 + embedding[9] * -1;
    let score_3 = embedding[0] * 33 + embedding[1] * 13 + embedding[2] * 24 + embedding[3] * 13 + embedding[4] * 5 + embedding[5] * 14 + embedding[6] * -2 + embedding[7] * 11 + embedding[8] * 6 + embedding[9] * 2;
    let score_4 = embedding[0] * 15 + embedding[1] * -17 + embedding[2] * 62 + embedding[3] * 4 + embedding[4] * 6 + embedding[5] * 16 + embedding[6] * 6 + embedding[7] * 32 + embedding[8] * -30 + embedding[9] * -3;
    let score_5 = embedding[0] * 2 + embedding[1] * 36 + embedding[2] * 17 + embedding[3] * -9 + embedding[4] * -49 + embedding[5] * 1 + embedding[6] * 3 + embedding[7] * -22 + embedding[8] * 10 + embedding[9] * 21;
    let score_6 = embedding[0] * 29 + embedding[1] * -20 + embedding[2] * 34 + embedding[3] * 36 + embedding[4] * 9 + embedding[5] * 11 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -22 + embedding[9] * -12;
    let score_7 = embedding[0] * 18 + embedding[1] * -15 + embedding[2] * 11 + embedding[3] * 2 + embedding[4] * 21 + embedding[5] * -3 + embedding[6] * -14 + embedding[7] * 59 + embedding[8] * -6 + embedding[9] * -10;
    let score_8 = embedding[0] * -14 + embedding[1] * -23 + embedding[2] * -2 + embedding[3] * 11 + embedding[4] * 20 + embedding[5] * -22 + embedding[6] * -5 + embedding[7] * 71 + embedding[8] * -12 + embedding[9] * -15;
    let score_9 = embedding[0] * -17 + embedding[1] * -4 + embedding[2] * -23 + embedding[3] * 15 + embedding[4] * 17 + embedding[5] * -44 + embedding[6] * 13 + embedding[7] * 3 + embedding[8] * -21 + embedding[9] * -9;
    let score_10 = embedding[0] * -38 + embedding[1] * 4 + embedding[2] * -19 + embedding[3] * 5 + embedding[4] * 1 + embedding[5] * -30 + embedding[6] * 8 + embedding[7] * -13 + embedding[8] * 8 + embedding[9] * -6;
    let score_11 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * -13 + embedding[3] * -21 + embedding[4] * 2 + embedding[5] * 17 + embedding[6] * -10 + embedding[7] * -23 + embedding[8] * 12 + embedding[9] * -2;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 17;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 9;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 16;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 13;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 12;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 6;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 18;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 11;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_198(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * -1 + embedding[2] * -4 + embedding[3] * -41 + embedding[4] * 5 + embedding[6] * -9 + embedding[7] * -27 + embedding[8] * -13 + embedding[9] * 9;
    let score_1 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 5;
    let score_2 = embedding[0] * -12 + embedding[1] * 17 + embedding[2] * -35 + embedding[3] * 12 + embedding[4] * 36 + embedding[5] * -67 + embedding[7] * 42 + embedding[8] * -6 + embedding[9] * -1;
    let score_3 = embedding[0] * 6 + embedding[1] * 37 + embedding[2] * -6 + embedding[3] * -13 + embedding[4] * -13 + embedding[5] * -23 + embedding[6] * -2 + embedding[7] * -21 + embedding[9] * 18;
    let score_4 = embedding[0] * 44 + embedding[1] * 14 + embedding[2] * 38 + embedding[3] * 36 + embedding[4] * -4 + embedding[5] * 25 + embedding[6] * -6 + embedding[7] * 36 + embedding[8] * 18 + embedding[9] * -5;
    let score_5 = embedding[0] * -24 + embedding[1] * -26 + embedding[2] * -19 + embedding[3] * 38 + embedding[4] * 9 + embedding[6] * 21 + embedding[8] * -9 + embedding[9] * -20;
    let score_6 = embedding[0] * 12 + embedding[1] * -23 + embedding[2] * 71 + embedding[3] * -11 + embedding[4] * 7 + embedding[5] * 18 + embedding[6] * 2 + embedding[7] * 33 + embedding[8] * -40 + embedding[9] * 4;
    let score_7 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -15 + embedding[3] * 44 + embedding[4] * 3 + embedding[5] * -15 + embedding[6] * 16 + embedding[7] * -19 + embedding[8] * -6 + embedding[9] * -11;
    let score_8 = embedding[0] * 2 + embedding[1] * 41 + embedding[2] * 18 + embedding[3] * -19 + embedding[4] * -55 + embedding[5] * -9 + embedding[6] * -2 + embedding[7] * -18 + embedding[8] * 6 + embedding[9] * 34;
    let score_9 = embedding[0] * 25 + embedding[1] * -30 + embedding[2] * 16 + embedding[3] * 62 + embedding[4] * 15 + embedding[5] * 3 + embedding[6] * 20 + embedding[7] * 13 + embedding[8] * -22 + embedding[9] * -24;
    let score_10 = embedding[0] * 17 + embedding[1] * -6 + embedding[2] * 19 + embedding[3] * 45 + embedding[4] * -23 + embedding[5] * 40 + embedding[6] * -2 + embedding[7] * 33 + embedding[8] * 30 + embedding[9] * -11;
    let score_11 = embedding[0] * 24 + embedding[1] * -19 + embedding[2] * 15 + embedding[3] * -2 + embedding[4] * 23 + embedding[5] * -9 + embedding[6] * -25 + embedding[7] * 78 + embedding[8] * -14 + embedding[9] * -8;
    let score_12 = embedding[0] * -20 + embedding[1] * -10 + embedding[2] * -28 + embedding[3] * 13 + embedding[4] * 20 + embedding[5] * -52 + embedding[6] * 14 + embedding[7] * -2 + embedding[8] * -30 + embedding[9] * -10;
    let score_13 = embedding[0] * -42 + embedding[1] * -5 + embedding[2] * -22 + embedding[3] * 28 + embedding[4] * 2 + embedding[5] * -39 + embedding[6] * 15 + embedding[7] * -14 + embedding[8] * 1 + embedding[9] * -15;
    let score_14 = embedding[0] * 10 + embedding[1] * 7 + embedding[2] * -16 + embedding[3] * 8 + embedding[4] * 12 + embedding[5] * -6 + embedding[6] * -7 + embedding[7] * 6 + embedding[8] * 4 + embedding[9] * -1;
    let score_15 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 5;
    let score_16 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 5;
    let score_17 = embedding[0] * -17 + embedding[1] * -1 + embedding[2] * -5 + embedding[3] * 2 + embedding[4] * -20 + embedding[5] * 32 + embedding[6] * -7 + embedding[7] * 6 + embedding[8] * 36 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 24;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 75;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 9;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 10;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 16;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 13;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 17;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 12;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 18;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 11;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 101;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_199(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -37 + embedding[1] * -3 + embedding[2] * 17 + embedding[3] * -75 + embedding[4] * -6 + embedding[5] * 13 + embedding[6] * -24 + embedding[7] * 3 + embedding[8] * -3 + embedding[9] * 25;
    let score_1 = embedding[0] * -26 + embedding[1] * -3 + embedding[2] * -9 + embedding[3] * -21 + embedding[4] * -18 + embedding[5] * 25 + embedding[6] * -15 + embedding[7] * 2 + embedding[8] * 23 + embedding[9] * 3;
    let score_2 = embedding[0] * -7 + embedding[1] * -22 + embedding[2] * 2 + embedding[3] * -18 + embedding[4] * 10 + embedding[5] * -9 + embedding[6] * 7 + embedding[7] * 11 + embedding[8] * -48 + embedding[9] * -17;
    let score_3 = embedding[0] * -9 + embedding[1] * -37 + embedding[2] * 9 + embedding[3] * -21 + embedding[4] * 25 + embedding[5] * -19 + embedding[6] * 25 + embedding[7] * 4 + embedding[8] * -22 + embedding[9] * 13;
    let score_4 = embedding[0] * 59 + embedding[1] * 6 + embedding[2] * 44 + embedding[3] * 74 + embedding[4] * -22 + embedding[5] * 32 + embedding[6] * 4 + embedding[7] * 19 + embedding[8] * 19 + embedding[9] * -7;
    let score_5 = embedding[0] * -20 + embedding[1] * -33 + embedding[2] * 14 + embedding[3] * 1 + embedding[4] * 24 + embedding[5] * -8 + embedding[6] * 2 + embedding[8] * -50 + embedding[9] * 13;
    let score_6 = embedding[0] * -1 + embedding[1] * 47 + embedding[2] * -21 + embedding[3] * 8 + embedding[4] * -15 + embedding[5] * -21 + embedding[6] * -6 + embedding[7] * -12 + embedding[8] * 23 + embedding[9] * 13;
    let score_7 = embedding[0] * 6 + embedding[1] * -18 + embedding[2] * -31 + embedding[3] * 79 + embedding[4] * 16 + embedding[5] * -14 + embedding[6] * 25 + embedding[7] * 6 + embedding[9] * -34;
    let score_8 = embedding[0] * -3 + embedding[1] * 39 + embedding[2] * -49 + embedding[3] * 50 + embedding[4] * 5 + embedding[5] * -49 + embedding[6] * 6 + embedding[7] * 4 + embedding[8] * 21 + embedding[9] * -4;
    let score_9 = embedding[0] * 29 + embedding[1] * 11 + embedding[2] * -24 + embedding[3] * 114 + embedding[4] * -13 + embedding[5] * -25 + embedding[6] * 35 + embedding[7] * -26 + embedding[8] * 14 + embedding[9] * -23;
    let score_10 = embedding[0] * 8 + embedding[1] * 21 + embedding[2] * -34 + embedding[3] * 44 + embedding[4] * 3 + embedding[5] * -24 + embedding[6] * 9 + embedding[7] * -22 + embedding[8] * 13 + embedding[9] * -9;
    let score_11 = embedding[0] * 35 + embedding[1] * 11 + embedding[2] * 20 + embedding[3] * 40 + embedding[4] * -29 + embedding[5] * 21 + embedding[6] * 7 + embedding[7] * -17 + embedding[8] * 13;
    let score_12 = embedding[0] * 30 + embedding[1] * -25 + embedding[2] * 54 + embedding[3] * 9 + embedding[4] * 8 + embedding[5] * 29 + embedding[6] * -9 + embedding[7] * 33 + embedding[8] * -17 + embedding[9] * -5;
    let score_13 = embedding[0] * -5 + embedding[1] * 19 + embedding[2] * 44 + embedding[3] * -61 + embedding[4] * -7 + embedding[5] * 7 + embedding[6] * -26 + embedding[7] * 30 + embedding[8] * -3 + embedding[9] * 31;
    let score_14 = embedding[0] * 17 + embedding[1] * 37 + embedding[2] * 3 + embedding[3] * 17 + embedding[4] * -26 + embedding[5] * 4 + embedding[6] * -3 + embedding[7] * -21 + embedding[8] * 23 + embedding[9] * 17;
    let score_15 = embedding[0] * 36 + embedding[1] * -31 + embedding[2] * 18 + embedding[3] * 91 + embedding[4] * 16 + embedding[5] * 3 + embedding[6] * 32 + embedding[7] * 11 + embedding[8] * -18 + embedding[9] * -31;
    let score_16 = embedding[0] * -26 + embedding[1] * -17 + embedding[2] * -13 + embedding[3] * 9 + embedding[4] * 13 + embedding[5] * -33 + embedding[6] * 1 + embedding[7] * -18 + embedding[8] * -8 + embedding[9] * -16;
    let score_17 = embedding[0] * -16 + embedding[1] * -22 + embedding[2] * -28 + embedding[3] * 40 + embedding[4] * 23 + embedding[5] * -56 + embedding[6] * 27 + embedding[7] * -6 + embedding[8] * -44 + embedding[9] * -16;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 2;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 14;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 4;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 24;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 8;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 10;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 40;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 19;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 9;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 36;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 16;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 13;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 5;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 18;
    }
    best_palette
}

fn predict_pixel_200(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -29 + embedding[1] * -9 + embedding[2] * -4 + embedding[3] * -54 + embedding[4] * 7 + embedding[5] * 4 + embedding[6] * -18 + embedding[7] * 1 + embedding[8] * -11 + embedding[9] * 8;
    let score_1 = embedding[0] * 16 + embedding[1] * -17 + embedding[2] * -22 + embedding[3] * 87 + embedding[4] * 13 + embedding[5] * 1 + embedding[6] * 21 + embedding[7] * 5 + embedding[8] * 5 + embedding[9] * -29;
    let score_2 = embedding[0] * -21 + embedding[1] * -8 + embedding[2] * -3 + embedding[3] * -39 + embedding[4] * 1 + embedding[6] * -4 + embedding[7] * -9 + embedding[8] * -18 + embedding[9] * -4;
    let score_3 = embedding[0] * -21 + embedding[1] * -25 + embedding[2] * -4 + embedding[3] * -27 + embedding[4] * 9 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * -13 + embedding[8] * -14 + embedding[9] * 9;
    let score_4 = embedding[0] * -26 + embedding[1] * -25 + embedding[2] * -13 + embedding[3] * -5 + embedding[4] * 14 + embedding[5] * -12 + embedding[6] * 3 + embedding[7] * -16 + embedding[8] * -31 + embedding[9] * 6;
    let score_5 = embedding[0] * -5 + embedding[1] * 39 + embedding[2] * -13 + embedding[3] * -9 + embedding[4] * -13 + embedding[5] * -19 + embedding[6] * -8 + embedding[7] * -6 + embedding[8] * 9 + embedding[9] * 14;
    let score_6 = embedding[0] * -12 + embedding[1] * 29 + embedding[2] * -39 + embedding[3] * 29 + embedding[4] * 11 + embedding[5] * -46 + embedding[6] * 3 + embedding[7] * 14 + embedding[8] * 8 + embedding[9] * 2;
    let score_7 = embedding[0] * 31 + embedding[1] * 1 + embedding[2] * -18 + embedding[3] * 127 + embedding[4] * -11 + embedding[5] * -15 + embedding[6] * 40 + embedding[7] * -15 + embedding[8] * 10 + embedding[9] * -27;
    let score_8 = embedding[0] * 69 + embedding[1] * 22 + embedding[2] * 54 + embedding[3] * 72 + embedding[4] * -14 + embedding[5] * 19 + embedding[6] * 6 + embedding[7] * 31 + embedding[8] * 18 + embedding[9] * 3;
    let score_9 = embedding[0] * -9 + embedding[1] * 13 + embedding[2] * -23 + embedding[3] * 10 + embedding[4] * 3 + embedding[5] * -28 + embedding[6] * 9 + embedding[7] * -19 + embedding[8] * -4 + embedding[9] * 1;
    let score_10 = embedding[0] * 37 + embedding[1] * 1 + embedding[2] * 27 + embedding[3] * 34 + embedding[4] * -8 + embedding[5] * 24 + embedding[6] * -3 + embedding[7] * 7 + embedding[8] * 10 + embedding[9] * -3;
    let score_11 = embedding[0] * -33 + embedding[1] * -19 + embedding[3] * -15 + embedding[4] * 8 + embedding[5] * -18 + embedding[6] * -2 + embedding[7] * -16 + embedding[8] * -9 + embedding[9] * -6;
    let score_12 = embedding[0] * 16 + embedding[1] * 18 + embedding[2] * 20 + embedding[3] * -18 + embedding[4] * -12 + embedding[5] * 14 + embedding[6] * -14 + embedding[7] * 4 + embedding[8] * 4 + embedding[9] * 12;
    let score_13 = embedding[0] * 7 + embedding[1] * 34 + embedding[2] * 12 + embedding[3] * -7 + embedding[4] * -31 + embedding[5] * 3 + embedding[6] * -7 + embedding[7] * -17 + embedding[8] * 2 + embedding[9] * 21;
    let score_14 = embedding[0] * 14 + embedding[1] * 16 + embedding[2] * 36 + embedding[3] * -10 + embedding[4] * -48 + embedding[5] * 26 + embedding[6] * -15 + embedding[7] * 27 + embedding[8] * 7 + embedding[9] * 24;
    let score_15 = embedding[0] * 42 + embedding[1] * -28 + embedding[2] * 16 + embedding[3] * 119 + embedding[4] * 4 + embedding[5] * 5 + embedding[6] * 40 + embedding[7] * 6 + embedding[8] * -9 + embedding[9] * -32;
    let score_16 = embedding[0] * 44 + embedding[1] * -6 + embedding[2] * 25 + embedding[3] * 95 + embedding[4] * -15 + embedding[5] * 20 + embedding[6] * 19 + embedding[7] * 17 + embedding[8] * 13 + embedding[9] * -19;
    let score_17 = embedding[0] * -48 + embedding[1] * -11 + embedding[2] * 16 + embedding[3] * -7 + embedding[4] * 11 + embedding[5] * -13 + embedding[6] * -7 + embedding[7] * 39 + embedding[8] * 27 + embedding[9] * 3;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 6;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 82;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 83;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 84;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 24;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 8;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 10;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 17;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 40;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 104;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 11;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 36;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 16;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 44;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 13;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 14;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 15;
    }
    best_palette
}

fn predict_pixel_201(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -23 + embedding[1] * -26 + embedding[2] * 35 + embedding[3] * -85 + embedding[5] * 20 + embedding[6] * -31 + embedding[7] * 32 + embedding[8] * -26 + embedding[9] * 24;
    let score_1 = embedding[0] * -1 + embedding[1] * -12 + embedding[2] * -19 + embedding[3] * -9 + embedding[4] * 8 + embedding[5] * 11 + embedding[6] * -6 + embedding[7] * -3 + embedding[8] * -18 + embedding[9] * -19;
    let score_2 = embedding[0] * -8 + embedding[1] * -33 + embedding[2] * -6 + embedding[3] * -19 + embedding[4] * 24 + embedding[5] * -4 + embedding[6] * 16 + embedding[9] * 3;
    let score_3 = embedding[0] * -18 + embedding[1] * -29 + embedding[2] * -8 + embedding[3] * 2 + embedding[4] * 23 + embedding[5] * 11 + embedding[6] * -10 + embedding[7] * -4 + embedding[8] * -24 + embedding[9] * 4;
    let score_4 = embedding[0] * -12 + embedding[1] * -33 + embedding[2] * -12 + embedding[3] * 62 + embedding[4] * 11 + embedding[5] * 10 + embedding[6] * 15 + embedding[7] * 34 + embedding[8] * 15 + embedding[9] * -30;
    let score_5 = embedding[0] * 1 + embedding[1] * 54 + embedding[2] * -25 + embedding[3] * 9 + embedding[4] * -17 + embedding[5] * -42 + embedding[6] * 9 + embedding[7] * -26 + embedding[8] * 9 + embedding[9] * 17;
    let score_6 = embedding[0] * 25 + embedding[1] * 24 + embedding[2] * -23 + embedding[3] * 93 + embedding[4] * -19 + embedding[5] * -27 + embedding[6] * 37 + embedding[7] * -31 + embedding[8] * 12 + embedding[9] * -16;
    let score_7 = embedding[0] * 63 + embedding[1] * 21 + embedding[2] * 56 + embedding[3] * 44 + embedding[4] * -22 + embedding[5] * 37 + embedding[6] * -3 + embedding[7] * 27 + embedding[8] * 19 + embedding[9] * -1;
    let score_8 = embedding[0] * -2 + embedding[1] * 27 + embedding[2] * -33 + embedding[3] * 23 + embedding[4] * -3 + embedding[5] * -42 + embedding[6] * 20 + embedding[7] * -36 + embedding[8] * -4 + embedding[9] * 2;
    let score_9 = embedding[0] * 32 + embedding[1] * 7 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * 25 + embedding[7] * -16 + embedding[8] * 3 + embedding[9] * -4;
    let score_10 = embedding[0] * -38 + embedding[1] * -27 + embedding[2] * 18 + embedding[3] * -9 + embedding[4] * 24 + embedding[5] * -19 + embedding[6] * -10 + embedding[7] * -10 + embedding[8] * 5 + embedding[9] * -15;
    let score_11 = embedding[0] * 8 + embedding[1] * 46 + embedding[2] * -39 + embedding[3] * 51 + embedding[4] * -1 + embedding[5] * -51 + embedding[6] * 18 + embedding[7] * -13 + embedding[8] * 13 + embedding[9] * -4;
    let score_12 = embedding[0] * -29 + embedding[1] * 36 + embedding[2] * -25 + embedding[3] * -1 + embedding[4] * -14 + embedding[5] * -64 + embedding[6] * 15 + embedding[7] * -29 + embedding[8] * -13 + embedding[9] * 20;
    let score_13 = embedding[0] * 19 + embedding[1] * 37 + embedding[2] * -4 + embedding[3] * 18 + embedding[4] * -25 + embedding[5] * -6 + embedding[6] * 8 + embedding[7] * -28 + embedding[8] * 14 + embedding[9] * 13;
    let score_14 = embedding[0] * 24 + embedding[1] * -6 + embedding[2] * 46 + embedding[3] * -9 + embedding[4] * -42 + embedding[5] * 46 + embedding[6] * -34 + embedding[7] * 77 + embedding[8] * 7 + embedding[9] * 22;
    let score_15 = embedding[0] * 43 + embedding[1] * -1 + embedding[2] * 38 + embedding[3] * 66 + embedding[4] * -31 + embedding[5] * 40 + embedding[6] * -1 + embedding[7] * 36 + embedding[8] * 25 + embedding[9] * -13;
    let score_16 = embedding[0] * 13 + embedding[1] * 18 + embedding[2] * 15 + embedding[3] * 39 + embedding[4] * -27 + embedding[5] * 13 + embedding[6] * -10 + embedding[7] * 26 + embedding[8] * 42 + embedding[9] * -2;
    let score_17 = embedding[0] * -22 + embedding[1] * -17 + embedding[2] * -28 + embedding[3] * 27 + embedding[4] * 20 + embedding[5] * -66 + embedding[6] * 29 + embedding[7] * -10 + embedding[8] * -47 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 24;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 10;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 17;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 40;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 19;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 5;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 8;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 25;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 16;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 20;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 14;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 15;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 18;
    }
    best_palette
}

fn predict_pixel_202(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * -11 + embedding[2] * 14 + embedding[3] * -46 + embedding[4] * 11 + embedding[5] * 15 + embedding[6] * -22 + embedding[7] * 13 + embedding[8] * -7 + embedding[9] * 6;
    let score_1 = embedding[0] * -4 + embedding[1] * -12 + embedding[2] * -15 + embedding[3] * -8 + embedding[4] * 8 + embedding[5] * 5 + embedding[6] * -3 + embedding[7] * -4 + embedding[8] * -12 + embedding[9] * -16;
    let score_2 = embedding[0] * -8 + embedding[1] * -25 + embedding[2] * -12 + embedding[3] * -11 + embedding[4] * 19 + embedding[5] * -6 + embedding[6] * 11 + embedding[7] * -4 + embedding[8] * -2 + embedding[9] * -1;
    let score_3 = embedding[0] * -15 + embedding[1] * -22 + embedding[2] * -14 + embedding[3] * 7 + embedding[4] * 17 + embedding[5] * 2 + embedding[6] * -4 + embedding[7] * -7 + embedding[8] * -20 + embedding[9] * 1;
    let score_4 = embedding[0] * -14 + embedding[1] * -28 + embedding[2] * -13 + embedding[3] * 52 + embedding[4] * 9 + embedding[5] * 3 + embedding[6] * 15 + embedding[7] * 24 + embedding[8] * 10 + embedding[9] * -25;
    let score_5 = embedding[0] * -2 + embedding[1] * 49 + embedding[2] * -25 + embedding[3] * 16 + embedding[4] * -29 + embedding[5] * -37 + embedding[6] * 10 + embedding[7] * -22 + embedding[8] * 14 + embedding[9] * 19;
    let score_6 = embedding[0] * 13 + embedding[1] * 13 + embedding[2] * -22 + embedding[3] * 79 + embedding[4] * -21 + embedding[5] * -23 + embedding[6] * 31 + embedding[7] * -29 + embedding[8] * 11 + embedding[9] * -13;
    let score_7 = embedding[0] * 60 + embedding[1] * 23 + embedding[2] * 55 + embedding[3] * 35 + embedding[4] * -13 + embedding[5] * 22 + embedding[6] * -5 + embedding[7] * 36 + embedding[8] * 14 + embedding[9] * 6;
    let score_8 = embedding[0] * -8 + embedding[1] * 18 + embedding[2] * -27 + embedding[3] * 16 + embedding[4] * -11 + embedding[5] * -34 + embedding[6] * 18 + embedding[7] * -33 + embedding[8] * -2 + embedding[9] * 4;
    let score_9 = embedding[0] * 28 + embedding[1] * -2 + embedding[2] * 25 + embedding[4] * -1 + embedding[5] * 25 + embedding[6] * -10 + embedding[7] * 11 + embedding[8] * 2 + embedding[9] * -2;
    let score_10 = embedding[0] * -30 + embedding[1] * -24 + embedding[2] * 7 + embedding[3] * -1 + embedding[4] * 19 + embedding[5] * -20 + embedding[6] * -5 + embedding[7] * -8 + embedding[8] * 2 + embedding[9] * -13;
    let score_11 = embedding[0] * 23 + embedding[1] * 14 + embedding[2] * 40 + embedding[3] * -31 + embedding[4] * -8 + embedding[5] * 15 + embedding[6] * -23 + embedding[7] * 29 + embedding[9] * 20;
    let score_12 = embedding[0] * -27 + embedding[1] * 30 + embedding[2] * -22 + embedding[3] * 1 + embedding[4] * -21 + embedding[5] * -53 + embedding[6] * 14 + embedding[7] * -27 + embedding[8] * -7 + embedding[9] * 18;
    let score_13 = embedding[0] * 11 + embedding[1] * 35 + embedding[2] * -5 + embedding[3] * 9 + embedding[4] * -26 + embedding[5] * -10 + embedding[6] * 7 + embedding[7] * -24 + embedding[8] * 13 + embedding[9] * 16;
    let score_14 = embedding[0] * 26 + embedding[1] * 10 + embedding[2] * 44 + embedding[3] * -6 + embedding[4] * -42 + embedding[5] * 32 + embedding[6] * -19 + embedding[7] * 32 + embedding[8] * 9 + embedding[9] * 17;
    let score_15 = embedding[0] * 28 + embedding[1] * -4 + embedding[2] * 31 + embedding[3] * 48 + embedding[4] * -19 + embedding[5] * 32 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * 13 + embedding[9] * -8;
    let score_16 = embedding[0] * 9 + embedding[1] * 7 + embedding[2] * 6 + embedding[3] * 25 + embedding[4] * -4 + embedding[5] * 13 + embedding[6] * -4 + embedding[7] * 5 + embedding[8] * 19 + embedding[9] * -3;
    let score_17 = embedding[0] * -27 + embedding[1] * -28 + embedding[2] * -22 + embedding[3] * 24 + embedding[4] * 16 + embedding[5] * -56 + embedding[6] * 26 + embedding[7] * -4 + embedding[8] * -45 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 24;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 10;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 17;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 40;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 19;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 5;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 50;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 25;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 16;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 44;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 14;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 15;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 18;
    }
    best_palette
}

fn predict_pixel_203(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -18 + embedding[1] * -6 + embedding[2] * 11 + embedding[3] * -61 + embedding[4] * 2 + embedding[5] * 13 + embedding[6] * -25 + embedding[7] * 22 + embedding[8] * -8 + embedding[9] * 17;
    let score_1 = embedding[0] * -2 + embedding[1] * -13 + embedding[2] * -16 + embedding[3] * -7 + embedding[4] * 12 + embedding[5] * 6 + embedding[6] * -5 + embedding[7] * -2 + embedding[8] * -16 + embedding[9] * -18;
    let score_2 = embedding[0] * -6 + embedding[1] * -28 + embedding[2] * -8 + embedding[3] * -13 + embedding[4] * 24 + embedding[5] * -5 + embedding[6] * 13 + embedding[7] * -2 + embedding[8] * -1 + embedding[9] * -1;
    let score_3 = embedding[0] * -15 + embedding[1] * -23 + embedding[2] * -11 + embedding[3] * 6 + embedding[4] * 25 + embedding[5] * 5 + embedding[6] * -6 + embedding[7] * -6 + embedding[8] * -19 + embedding[9] * 1;
    let score_4 = embedding[0] * -16 + embedding[1] * -32 + embedding[2] * -9 + embedding[3] * 55 + embedding[4] * 15 + embedding[5] * 8 + embedding[6] * 18 + embedding[7] * 27 + embedding[8] * 11 + embedding[9] * -29;
    let score_5 = embedding[0] * -2 + embedding[1] * 50 + embedding[2] * -26 + embedding[3] * 18 + embedding[4] * -28 + embedding[5] * -37 + embedding[6] * 10 + embedding[7] * -28 + embedding[8] * 13 + embedding[9] * 16;
    let score_6 = embedding[0] * 20 + embedding[1] * 15 + embedding[2] * -18 + embedding[3] * 81 + embedding[4] * -21 + embedding[5] * -21 + embedding[6] * 31 + embedding[7] * -35 + embedding[8] * 9 + embedding[9] * -14;
    let score_7 = embedding[0] * 64 + embedding[1] * 24 + embedding[2] * 60 + embedding[3] * 44 + embedding[4] * -23 + embedding[5] * 29 + embedding[6] * -1 + embedding[7] * 26 + embedding[8] * 12 + embedding[9] * 5;
    let score_8 = embedding[0] * -22 + embedding[1] * -21 + embedding[3] * 5 + embedding[4] * 19 + embedding[5] * -18 + embedding[6] * -5 + embedding[7] * -13 + embedding[9] * -18;
    let score_9 = embedding[0] * -4 + embedding[1] * 20 + embedding[2] * -26 + embedding[3] * 20 + embedding[4] * -9 + embedding[5] * -32 + embedding[6] * 18 + embedding[7] * -37 + embedding[8] * -2 + embedding[9] * 2;
    let score_10 = embedding[0] * 32 + embedding[1] * 7 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -20 + embedding[5] * 19 + embedding[6] * 4 + embedding[7] * -18 + embedding[8] * 2 + embedding[9] * -5;
    let score_11 = embedding[0] * 16 + embedding[1] * 19 + embedding[2] * 32 + embedding[3] * -39 + embedding[4] * -11 + embedding[5] * 12 + embedding[6] * -26 + embedding[7] * 36 + embedding[8] * -1 + embedding[9] * 26;
    let score_12 = embedding[0] * -28 + embedding[1] * 29 + embedding[2] * -23 + embedding[3] * -2 + embedding[4] * -18 + embedding[5] * -50 + embedding[6] * 13 + embedding[7] * -26 + embedding[8] * -10 + embedding[9] * 17;
    let score_13 = embedding[0] * 18 + embedding[1] * 35 + embedding[2] * -3 + embedding[3] * 20 + embedding[4] * -29 + embedding[5] * -5 + embedding[6] * 9 + embedding[7] * -31 + embedding[8] * 13 + embedding[9] * 13;
    let score_14 = embedding[0] * 23 + embedding[1] * 11 + embedding[2] * 36 + embedding[3] * -22 + embedding[4] * -28 + embedding[5] * 30 + embedding[6] * -30 + embedding[7] * 55 + embedding[8] * 7 + embedding[9] * 22;
    let score_15 = embedding[0] * 40 + embedding[1] * -1 + embedding[2] * 38 + embedding[3] * 63 + embedding[4] * -26 + embedding[5] * 37 + embedding[6] * 7 + embedding[7] * 26 + embedding[8] * 13 + embedding[9] * -9;
    let score_16 = embedding[0] * -52 + embedding[1] * -19 + embedding[2] * 21 + embedding[3] * -2 + embedding[4] * 6 + embedding[5] * -5 + embedding[6] * -8 + embedding[7] * 51 + embedding[8] * 30 + embedding[9] * -1;
    let score_17 = embedding[0] * -24 + embedding[1] * -29 + embedding[2] * -21 + embedding[3] * 24 + embedding[4] * 21 + embedding[5] * -53 + embedding[6] * 24 + embedding[7] * -6 + embedding[8] * -45 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 24;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 10;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 17;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 5;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 40;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 50;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 25;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 16;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 20;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 14;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 15;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 18;
    }
    best_palette
}

fn predict_pixel_204(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * -15 + embedding[2] * 15 + embedding[3] * -38 + embedding[4] * 2 + embedding[5] * 21 + embedding[6] * -13 + embedding[7] * -3 + embedding[8] * -5 + embedding[9] * 4;
    let score_1 = embedding[0] * -2 + embedding[1] * -33 + embedding[2] * 2 + embedding[3] * -15 + embedding[4] * 22 + embedding[5] * 5 + embedding[6] * 7 + embedding[7] * 1 + embedding[8] * -12 + embedding[9] * -3;
    let score_2 = embedding[1] * -25 + embedding[2] * -3 + embedding[3] * -12 + embedding[4] * 16 + embedding[5] * 9 + embedding[6] * -6 + embedding[7] * 4 + embedding[8] * -25 + embedding[9] * -16;
    let score_3 = embedding[0] * -9 + embedding[1] * -34 + embedding[2] * -6 + embedding[3] * 5 + embedding[4] * 26 + embedding[5] * 2 + embedding[6] * -3 + embedding[7] * -1 + embedding[8] * -32 + embedding[9] * -6;
    let score_4 = embedding[0] * -7 + embedding[1] * -36 + embedding[2] * -9 + embedding[3] * 70 + embedding[4] * 13 + embedding[5] * 8 + embedding[6] * 18 + embedding[7] * 38 + embedding[8] * 3 + embedding[9] * -28;
    let score_5 = embedding[0] * 47 + embedding[1] * 6 + embedding[2] * 35 + embedding[3] * 54 + embedding[4] * -13 + embedding[5] * 20 + embedding[6] * 2 + embedding[7] * 27 + embedding[8] * 15 + embedding[9] * -6;
    let score_6 = embedding[0] * 1 + embedding[1] * 60 + embedding[2] * -20 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -43 + embedding[6] * 3 + embedding[7] * -12 + embedding[8] * 19 + embedding[9] * 19;
    let score_7 = embedding[0] * 26 + embedding[1] * 22 + embedding[2] * -20 + embedding[3] * 107 + embedding[4] * -19 + embedding[5] * -33 + embedding[6] * 35 + embedding[7] * -19 + embedding[8] * 16 + embedding[9] * -13;
    let score_8 = embedding[0] * 1 + embedding[1] * 26 + embedding[2] * -20 + embedding[3] * 21 + embedding[4] * -5 + embedding[5] * -42 + embedding[6] * 16 + embedding[7] * -25 + embedding[8] * -5 + embedding[9] * 6;
    let score_9 = embedding[0] * 8 + embedding[1] * -18 + embedding[2] * 8 + embedding[3] * -12 + embedding[4] * 6 + embedding[5] * 22 + embedding[6] * -9 + embedding[8] * -4 + embedding[9] * -8;
    let score_10 = embedding[0] * -9 + embedding[1] * -29 + embedding[2] * 14 + embedding[3] * -9 + embedding[4] * 16 + embedding[5] * -5 + embedding[6] * -15 + embedding[7] * -3 + embedding[8] * -16 + embedding[9] * -13;
    let score_11 = embedding[0] * 15 + embedding[1] * 8 + embedding[2] * 36 + embedding[3] * -30 + embedding[4] * -13 + embedding[5] * 19 + embedding[6] * -17 + embedding[7] * 18 + embedding[8] * 2 + embedding[9] * 17;
    let score_12 = embedding[0] * 12 + embedding[1] * 56 + embedding[2] * -34 + embedding[3] * 62 + embedding[4] * -9 + embedding[5] * -55 + embedding[6] * 17 + embedding[7] * -4 + embedding[8] * 22 + embedding[9] * 2;
    let score_13 = embedding[0] * -37 + embedding[1] * 17 + embedding[2] * -12 + embedding[3] * -9 + embedding[4] * -20 + embedding[5] * -17 + embedding[6] * -1 + embedding[7] * 8 + embedding[8] * 18 + embedding[9] * 10;
    let score_14 = embedding[0] * -22 + embedding[1] * 42 + embedding[2] * -16 + embedding[3] * 4 + embedding[4] * -21 + embedding[5] * -59 + embedding[6] * 10 + embedding[7] * -22 + embedding[8] * -2 + embedding[9] * 20;
    let score_15 = embedding[0] * 10 + embedding[1] * 43 + embedding[2] * -4 + embedding[3] * 15 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * -2 + embedding[7] * -12 + embedding[8] * 26 + embedding[9] * 15;
    let score_16 = embedding[0] * 13 + embedding[1] * 5 + embedding[2] * 42 + embedding[3] * -13 + embedding[4] * -45 + embedding[5] * 36 + embedding[6] * -17 + embedding[7] * 26 + embedding[8] * 8 + embedding[9] * 18;
    let score_17 = embedding[0] * -24 + embedding[1] * 5 + embedding[2] * -9 + embedding[3] * -17 + embedding[4] * -20 + embedding[5] * -5 + embedding[6] * 2 + embedding[7] * -10 + embedding[8] * 3 + embedding[9] * 8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 14;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 24;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 10;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 40;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 19;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 5;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 50;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 8;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 11;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 25;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 16;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 44;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 7;
    }
    best_palette
}

fn predict_pixel_205(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * -9 + embedding[2] * 3 + embedding[3] * -36 + embedding[4] * -2 + embedding[5] * 17 + embedding[6] * -13 + embedding[7] * 9 + embedding[8] * -5 + embedding[9] * 7;
    let score_1 = embedding[0] * -4 + embedding[1] * -10 + embedding[2] * -9 + embedding[3] * -18 + embedding[4] * 13 + embedding[5] * 3 + embedding[6] * -10 + embedding[7] * 5 + embedding[8] * -17 + embedding[9] * -13;
    let score_2 = embedding[0] * -6 + embedding[1] * -21 + embedding[2] * -3 + embedding[3] * -19 + embedding[4] * 22 + embedding[5] * -3 + embedding[6] * 4 + embedding[8] * -2 + embedding[9] * 1;
    let score_3 = embedding[0] * -12 + embedding[1] * -17 + embedding[2] * -6 + embedding[3] * -4 + embedding[4] * 21 + embedding[5] * 4 + embedding[6] * -11 + embedding[7] * -4 + embedding[8] * -17 + embedding[9] * 2;
    let score_4 = embedding[0] * -17 + embedding[1] * -29 + embedding[2] * -3 + embedding[3] * 50 + embedding[4] * 9 + embedding[5] * 10 + embedding[6] * 15 + embedding[7] * 31 + embedding[8] * 14 + embedding[9] * -25;
    let score_5 = embedding[0] * 56 + embedding[1] * 15 + embedding[2] * 51 + embedding[3] * 46 + embedding[4] * -26 + embedding[5] * 31 + embedding[6] * -1 + embedding[7] * 23 + embedding[8] * 17 + embedding[9] * -1;
    let score_6 = embedding[1] * 40 + embedding[2] * -15 + embedding[3] * 5 + embedding[4] * -19 + embedding[5] * -27 + embedding[6] * 3 + embedding[7] * -20 + embedding[8] * 9 + embedding[9] * 15;
    let score_7 = embedding[0] * -21 + embedding[1] * -18 + embedding[2] * 2 + embedding[3] * -5 + embedding[4] * 19 + embedding[5] * -18 + embedding[6] * -9 + embedding[7] * -8 + embedding[8] * -4 + embedding[9] * -13;
    let score_8 = embedding[0] * 18 + embedding[1] * 15 + embedding[2] * -12 + embedding[3] * 71 + embedding[4] * -19 + embedding[5] * -18 + embedding[6] * 25 + embedding[7] * -27 + embedding[8] * 10 + embedding[9] * -10;
    let score_9 = embedding[0] * -4 + embedding[1] * 16 + embedding[2] * -19 + embedding[3] * 12 + embedding[4] * -5 + embedding[5] * -27 + embedding[6] * 11 + embedding[7] * -30 + embedding[8] * -3 + embedding[9] * 3;
    let score_10 = embedding[0] * 31 + embedding[1] * 8 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * 19 + embedding[6] * 1 + embedding[7] * -14 + embedding[8] * 3;
    let score_11 = embedding[0] * 21 + embedding[1] * 15 + embedding[2] * 24 + embedding[3] * -19 + embedding[4] * -11 + embedding[5] * 13 + embedding[6] * -16 + embedding[7] * 18 + embedding[8] * 3 + embedding[9] * 16;
    let score_12 = embedding[0] * -24 + embedding[1] * 27 + embedding[2] * -19 + embedding[3] * 2 + embedding[4] * -14 + embedding[5] * -51 + embedding[6] * 11 + embedding[7] * -25 + embedding[8] * -10 + embedding[9] * 15;
    let score_13 = embedding[0] * 19 + embedding[1] * 29 + embedding[2] * 4 + embedding[3] * 13 + embedding[4] * -24 + embedding[5] * -1 + embedding[6] * 4 + embedding[7] * -24 + embedding[8] * 11 + embedding[9] * 12;
    let score_14 = embedding[0] * 28 + embedding[1] * 7 + embedding[2] * 30 + embedding[3] * -1 + embedding[4] * -28 + embedding[5] * 32 + embedding[6] * -20 + embedding[7] * 42 + embedding[8] * 9 + embedding[9] * 13;
    let score_15 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * -22 + embedding[3] * 6 + embedding[4] * -14 + embedding[6] * 4 + embedding[7] * -12 + embedding[8] * 7 + embedding[9] * -2;
    let score_16 = embedding[0] * -40 + embedding[1] * -18 + embedding[2] * 18 + embedding[3] * 8 + embedding[4] * 8 + embedding[5] * -5 + embedding[6] * -4 + embedding[7] * 43 + embedding[8] * 29 + embedding[9] * -5;
    let score_17 = embedding[0] * -24 + embedding[1] * -23 + embedding[2] * -15 + embedding[3] * 17 + embedding[4] * 18 + embedding[5] * -54 + embedding[6] * 18 + embedding[7] * -3 + embedding[8] * -43 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 14;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 24;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 5;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 10;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 40;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 50;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 25;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 16;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 20;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 7;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 15;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 18;
    }
    best_palette
}

fn predict_pixel_206(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * -9 + embedding[2] * 11 + embedding[3] * -42 + embedding[4] * 7 + embedding[5] * 16 + embedding[6] * -19 + embedding[7] * 9 + embedding[8] * -6 + embedding[9] * 6;
    let score_1 = embedding[0] * -6 + embedding[1] * -19 + embedding[2] * -11 + embedding[3] * -7 + embedding[4] * 4 + embedding[5] * 14 + embedding[7] * -5 + embedding[8] * -15 + embedding[9] * -16;
    let score_2 = embedding[0] * -8 + embedding[1] * -33 + embedding[2] * -4 + embedding[3] * -9 + embedding[4] * 12 + embedding[5] * 6 + embedding[6] * 19 + embedding[7] * -6 + embedding[8] * -3 + embedding[9] * -1;
    let score_3 = embedding[0] * -16 + embedding[1] * -28 + embedding[2] * -6 + embedding[3] * 6 + embedding[4] * 10 + embedding[5] * 15 + embedding[6] * -1 + embedding[7] * -9 + embedding[8] * -23 + embedding[9] * 3;
    let score_4 = embedding[0] * 55 + embedding[1] * 16 + embedding[2] * 44 + embedding[3] * 49 + embedding[4] * -19 + embedding[5] * 27 + embedding[7] * 27 + embedding[8] * 19 + embedding[9] * -2;
    let score_5 = embedding[0] * -1 + embedding[1] * 48 + embedding[2] * -19 + embedding[3] * 2 + embedding[4] * -17 + embedding[5] * -35 + embedding[6] * 2 + embedding[7] * -12 + embedding[8] * 11 + embedding[9] * 19;
    let score_6 = embedding[0] * -9 + embedding[1] * 35 + embedding[2] * -41 + embedding[3] * 30 + embedding[4] * 4 + embedding[5] * -55 + embedding[6] * 9 + embedding[7] * 5 + embedding[8] * 10 + embedding[9] * 4;
    let score_7 = embedding[0] * -15 + embedding[1] * -24 + embedding[2] * 2 + embedding[3] * 20 + embedding[4] * 13 + embedding[6] * -1 + embedding[7] * 26 + embedding[8] * 13 + embedding[9] * -17;
    let score_8 = embedding[0] * 18 + embedding[1] * 16 + embedding[2] * -15 + embedding[3] * 76 + embedding[4] * -18 + embedding[5] * -22 + embedding[6] * 31 + embedding[7] * -26 + embedding[8] * 7 + embedding[9] * -12;
    let score_9 = embedding[0] * -8 + embedding[1] * 19 + embedding[2] * -28 + embedding[3] * 17 + embedding[5] * -38 + embedding[6] * 16 + embedding[7] * -25 + embedding[8] * -6 + embedding[9] * 3;
    let score_10 = embedding[0] * 28 + embedding[1] * -3 + embedding[2] * 24 + embedding[3] * 4 + embedding[4] * -3 + embedding[5] * 27 + embedding[6] * -8 + embedding[7] * 7 + embedding[8] * 2 + embedding[9] * -4;
    let score_11 = embedding[0] * -31 + embedding[1] * -24 + embedding[2] * 14 + embedding[3] * -4 + embedding[4] * 13 + embedding[5] * -10 + embedding[6] * -1 + embedding[7] * -11 + embedding[8] * 5 + embedding[9] * -16;
    let score_12 = embedding[0] * 22 + embedding[1] * 15 + embedding[2] * 36 + embedding[3] * -29 + embedding[4] * -11 + embedding[5] * 15 + embedding[6] * -21 + embedding[7] * 25 + embedding[8] * 1 + embedding[9] * 19;
    let score_13 = embedding[0] * -29 + embedding[1] * 34 + embedding[2] * -25 + embedding[3] * 3 + embedding[4] * -12 + embedding[5] * -62 + embedding[6] * 13 + embedding[7] * -20 + embedding[8] * -9 + embedding[9] * 16;
    let score_14 = embedding[0] * 12 + embedding[1] * 33 + embedding[2] * 1 + embedding[3] * 2 + embedding[4] * -20 + embedding[5] * -3 + embedding[6] * 1 + embedding[7] * -16 + embedding[8] * 13 + embedding[9] * 15;
    let score_15 = embedding[0] * 25 + embedding[1] * 9 + embedding[2] * 40 + embedding[3] * -4 + embedding[4] * -44 + embedding[5] * 33 + embedding[6] * -17 + embedding[7] * 29 + embedding[8] * 9 + embedding[9] * 17;
    let score_16 = embedding[0] * 11 + embedding[1] * 10 + embedding[2] * 10 + embedding[3] * 16 + embedding[4] * -5 + embedding[5] * 13 + embedding[6] * -8 + embedding[7] * 7 + embedding[8] * 20 + embedding[9] * 1;
    let score_17 = embedding[0] * -21 + embedding[1] * -13 + embedding[2] * -27 + embedding[3] * 26 + embedding[4] * 16 + embedding[5] * -58 + embedding[6] * 26 + embedding[7] * -6 + embedding[8] * -42 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 14;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 24;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 8;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 10;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 40;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 19;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 5;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 50;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 25;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 16;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 44;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 15;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 18;
    }
    best_palette
}

fn predict_pixel_207(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -34 + embedding[1] * -8 + embedding[2] * -7 + embedding[3] * -54 + embedding[4] * -1 + embedding[5] * 1 + embedding[6] * -23 + embedding[7] * 4 + embedding[8] * -4 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * -13 + embedding[3] * -14 + embedding[4] * 13 + embedding[5] * -4 + embedding[7] * 2 + embedding[8] * -32 + embedding[9] * -16;
    let score_2 = embedding[0] * -4 + embedding[1] * -28 + embedding[2] * -5 + embedding[3] * -14 + embedding[4] * 27 + embedding[5] * -10 + embedding[6] * 20 + embedding[7] * -5 + embedding[8] * -9 + embedding[9] * 7;
    let score_3 = embedding[0] * -13 + embedding[1] * -25 + embedding[2] * -19 + embedding[3] * 11 + embedding[4] * 28 + embedding[5] * -13 + embedding[6] * 5 + embedding[7] * -9 + embedding[8] * -39 + embedding[9] * 1;
    let score_4 = embedding[0] * 4 + embedding[1] * -27 + embedding[2] * -15 + embedding[3] * 87 + embedding[4] * 12 + embedding[5] * 9 + embedding[6] * 29 + embedding[7] * 20 + embedding[8] * 9 + embedding[9] * -31;
    let score_5 = embedding[0] * -16 + embedding[1] * 33 + embedding[2] * -35 + embedding[3] * -6 + embedding[4] * -7 + embedding[5] * -23 + embedding[6] * -8 + embedding[7] * -8 + embedding[8] * 16 + embedding[9] * 12;
    let score_6 = embedding[0] * 23 + embedding[1] * 12 + embedding[2] * -14 + embedding[3] * 103 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 34 + embedding[7] * -26 + embedding[8] * 16 + embedding[9] * -19;
    let score_7 = embedding[0] * 65 + embedding[1] * 20 + embedding[2] * 56 + embedding[3] * 57 + embedding[4] * -26 + embedding[5] * 31 + embedding[6] * 4 + embedding[7] * 27 + embedding[8] * 19 + embedding[9] * 2;
    let score_8 = embedding[0] * -12 + embedding[1] * 14 + embedding[2] * -29 + embedding[3] * 6 + embedding[4] * 1 + embedding[5] * -32 + embedding[6] * 12 + embedding[7] * -29 + embedding[8] * -9 + embedding[9] * 5;
    let score_9 = embedding[0] * 33 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 35 + embedding[4] * -28 + embedding[5] * 21 + embedding[6] * 10 + embedding[7] * -17 + embedding[8] * 7 + embedding[9] * -2;
    let score_10 = embedding[0] * 27 + embedding[1] * -22 + embedding[2] * 62 + embedding[3] * 8 + embedding[4] * 4 + embedding[5] * 15 + embedding[6] * -1 + embedding[7] * 15 + embedding[8] * -27 + embedding[9] * -7;
    let score_11 = embedding[0] * 17 + embedding[1] * 18 + embedding[2] * 28 + embedding[3] * -30 + embedding[4] * -12 + embedding[5] * 8 + embedding[6] * -26 + embedding[7] * 29 + embedding[8] * 3 + embedding[9] * 24;
    let score_12 = embedding[0] * -38 + embedding[1] * 28 + embedding[2] * -22 + embedding[3] * -20 + embedding[4] * -15 + embedding[5] * -46 + embedding[6] * -1 + embedding[7] * -19 + embedding[8] * -4 + embedding[9] * 18;
    let score_13 = embedding[0] * 12 + embedding[1] * 34 + embedding[2] * -3 + embedding[3] * 22 + embedding[4] * -25 + embedding[5] * 8 + embedding[6] * -2 + embedding[7] * -16 + embedding[8] * 28 + embedding[9] * 10;
    let score_14 = embedding[0] * -39 + embedding[1] * -11 + embedding[2] * 9 + embedding[3] * -26 + embedding[4] * 14 + embedding[5] * -21 + embedding[6] * -8 + embedding[7] * -12 + embedding[8] * 4 + embedding[9] * -4;
    let score_15 = embedding[0] * 31 + embedding[1] * 14 + embedding[2] * 33 + embedding[3] * -2 + embedding[4] * -35 + embedding[5] * 28 + embedding[6] * -28 + embedding[7] * 52 + embedding[8] * 14 + embedding[9] * 19;
    let score_16 = embedding[0] * 31 + embedding[1] * -40 + embedding[2] * 28 + embedding[3] * 84 + embedding[4] * 15 + embedding[5] * 9 + embedding[6] * 35 + embedding[7] * 9 + embedding[8] * -24 + embedding[9] * -31;
    let score_17 = embedding[0] * 44 + embedding[1] * -4 + embedding[2] * 28 + embedding[3] * 88 + embedding[4] * -31 + embedding[5] * 32 + embedding[6] * 18 + embedding[7] * 21 + embedding[8] * 17 + embedding[9] * -21;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 24;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 10;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 17;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 40;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 19;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 9;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 50;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 25;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 16;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 5;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 20;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 13;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 14;
    }
    best_palette
}

fn predict_pixel_208(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 6 + embedding[1] * 3 + embedding[2] * 10 + embedding[3] * -39 + embedding[4] * 6 + embedding[5] * 17 + embedding[6] * -14 + embedding[7] * -13 + embedding[8] * -5 + embedding[9] * 11;
    let score_1 = embedding[0] * -9 + embedding[1] * -12 + embedding[2] * -7 + embedding[3] * 5 + embedding[4] * -4 + embedding[5] * 24 + embedding[6] * -3 + embedding[7] * -9 + embedding[8] * 15 + embedding[9] * -10;
    let score_2 = embedding[0] * 1 + embedding[1] * 44 + embedding[2] * -4 + embedding[3] * -15 + embedding[4] * -18 + embedding[5] * -19 + embedding[6] * -6 + embedding[7] * -12 + embedding[8] * 10 + embedding[9] * 23;
    let score_3 = embedding[0] * -7 + embedding[1] * -22 + embedding[2] * -27 + embedding[3] * 70 + embedding[4] * 10 + embedding[5] * -7 + embedding[6] * 19 + embedding[7] * 7 + embedding[8] * 5 + embedding[9] * -30;
    let score_4 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * -37 + embedding[3] * 31 + embedding[4] * 30 + embedding[5] * -53 + embedding[6] * 1 + embedding[7] * 38 + embedding[8] * -8 + embedding[9] * -4;
    let score_5 = embedding[0] * 8 + embedding[1] * -3 + embedding[2] * -23 + embedding[3] * 87 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 30 + embedding[7] * -21 + embedding[8] * 13 + embedding[9] * -21;
    let score_6 = embedding[0] * 17 + embedding[1] * -20 + embedding[2] * 80 + embedding[3] * -19 + embedding[4] * 3 + embedding[5] * 17 + embedding[6] * 2 + embedding[7] * 36 + embedding[8] * -43 + embedding[9] * 9;
    let score_7 = embedding[0] * 18 + embedding[1] * 22 + embedding[2] * 38 + embedding[3] * -43 + embedding[4] * -5 + embedding[5] * 6 + embedding[6] * -19 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 28;
    let score_8 = embedding[0] * -35 + embedding[1] * 28 + embedding[2] * -12 + embedding[3] * -16 + embedding[4] * -14 + embedding[5] * -51 + embedding[6] * 8 + embedding[7] * -17 + embedding[8] * -13 + embedding[9] * 23;
    let score_9 = embedding[0] * 13 + embedding[1] * 47 + embedding[2] * 28 + embedding[3] * -24 + embedding[4] * -47 + embedding[5] * -8 + embedding[6] * -5 + embedding[7] * -14 + embedding[8] * 2 + embedding[9] * 37;
    let score_10 = embedding[0] * 20 + embedding[1] * -38 + embedding[2] * 16 + embedding[3] * 75 + embedding[4] * 8 + embedding[5] * -2 + embedding[6] * 28 + embedding[7] * 17 + embedding[8] * -23 + embedding[9] * -28;
    let score_11 = embedding[0] * 8 + embedding[1] * -19 + embedding[2] * 7 + embedding[3] * 36 + embedding[4] * -15 + embedding[5] * 32 + embedding[6] * 2 + embedding[7] * 12 + embedding[8] * 12 + embedding[9] * -13;
    let score_12 = embedding[0] * 15 + embedding[1] * -25 + embedding[2] * 8 + embedding[3] * -6 + embedding[4] * 20 + embedding[5] * -3 + embedding[6] * -15 + embedding[7] * 57 + embedding[8] * -17 + embedding[9] * -7;
    let score_13 = embedding[0] * -28 + embedding[1] * -6 + embedding[2] * -34 + embedding[3] * 27 + embedding[4] * 16 + embedding[5] * -70 + embedding[6] * 17 + embedding[7] * 8 + embedding[8] * -30 + embedding[9] * -6;
    let score_14 = embedding[0] * -32 + embedding[1] * -12 + embedding[2] * -27 + embedding[3] * 58 + embedding[4] * 1 + embedding[5] * -34 + embedding[6] * 24 + embedding[7] * -10 + embedding[8] * 14 + embedding[9] * -20;
    let score_15 = embedding[0] * -40 + embedding[1] * -16 + embedding[2] * 21 + embedding[3] * 11 + embedding[4] * 20 + embedding[5] * -8 + embedding[6] * 6 + embedding[7] * 13 + embedding[8] * 18 + embedding[9] * -10;
    let score_16 = embedding[0] * -16 + embedding[1] * 10 + embedding[2] * -25 + embedding[3] * 47 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 19 + embedding[7] * -11 + embedding[8] * 15 + embedding[9] * -9;
    let score_17 = embedding[0] * 7 + embedding[1] * -1 + embedding[2] * -20 + embedding[3] * 8 + embedding[4] * 13 + embedding[5] * -9 + embedding[6] * -9 + embedding[7] * 15 + embedding[8] * -3 + embedding[9] * -1;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 24;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 8;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 10;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 9;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 50;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 25;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 16;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 13;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 17;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 12;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 18;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 11;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 77;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 35;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 112;
    }
    best_palette
}

fn predict_pixel_209(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * -13 + embedding[3] * -23 + embedding[4] * 6 + embedding[5] * 2 + embedding[6] * -13 + embedding[7] * -10 + embedding[8] * 7 + embedding[9] * 4;
    let score_2 = embedding[0] * 13 + embedding[1] * 9 + embedding[2] * -14 + embedding[3] * 52 + embedding[4] * -12 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * -25 + embedding[8] * 12 + embedding[9] * -10;
    let score_3 = embedding[0] * -14 + embedding[1] * -22 + embedding[2] * -13 + embedding[3] * 19 + embedding[4] * 12 + embedding[5] * 3 + embedding[6] * 6 + embedding[7] * 11 + embedding[8] * -6 + embedding[9] * -14;
    let score_4 = embedding[0] * 8 + embedding[1] * -11 + embedding[2] * 62 + embedding[3] * -10 + embedding[4] * 1 + embedding[5] * 11 + embedding[6] * 3 + embedding[7] * 39 + embedding[8] * -24 + embedding[9] * 2;
    let score_5 = embedding[0] * 11 + embedding[1] * 20 + embedding[2] * 14 + embedding[3] * -24 + embedding[6] * -17 + embedding[7] * 12 + embedding[8] * 4 + embedding[9] * 14;
    let score_6 = embedding[0] * 7 + embedding[1] * 34 + embedding[2] * 15 + embedding[3] * -8 + embedding[4] * -38 + embedding[5] * -5 + embedding[6] * -3 + embedding[7] * -14 + embedding[8] * 7 + embedding[9] * 21;
    let score_7 = embedding[0] * 10 + embedding[1] * -14 + embedding[2] * 5 + embedding[3] * -3 + embedding[4] * 17 + embedding[5] * 2 + embedding[6] * -17 + embedding[7] * 48 + embedding[8] * -6 + embedding[9] * -7;
    let score_8 = embedding[0] * -14 + embedding[1] * -16 + embedding[2] * -16 + embedding[3] * 14 + embedding[4] * 13 + embedding[5] * -31 + embedding[6] * 9 + embedding[7] * -2 + embedding[8] * -26 + embedding[9] * -8;
    let score_9 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 2 + embedding[2] * -15 + embedding[3] * -13 + embedding[4] * 13 + embedding[5] * 14 + embedding[6] * -12 + embedding[7] * -17 + embedding[8] * 11 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 10;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 9;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 50;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 16;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 12;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 18;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_210(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 5 + embedding[1] * -5 + embedding[2] * -2 + embedding[3] * -28 + embedding[4] * 17 + embedding[5] * 11 + embedding[6] * -16 + embedding[7] * 9 + embedding[8] * -3 + embedding[9] * 1;
    let score_2 = embedding[0] * 16 + embedding[1] * 8 + embedding[2] * -11 + embedding[3] * 47 + embedding[4] * -12 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * -22 + embedding[8] * 10 + embedding[9] * -9;
    let score_3 = embedding[0] * 10 + embedding[1] * 28 + embedding[2] * 19 + embedding[3] * -6 + embedding[4] * -38 + embedding[5] * 1 + embedding[6] * -1 + embedding[7] * -13 + embedding[8] * 7 + embedding[9] * 17;
    let score_4 = embedding[0] * 13 + embedding[1] * -11 + embedding[2] * 7 + embedding[3] * -13 + embedding[4] * 19 + embedding[5] * 1 + embedding[6] * -24 + embedding[7] * 54 + embedding[8] * -8;
    let score_5 = embedding[0] * -9 + embedding[1] * -12 + embedding[2] * -16 + embedding[3] * 13 + embedding[4] * 10 + embedding[5] * -28 + embedding[6] * 7 + embedding[7] * -9 + embedding[8] * -18 + embedding[9] * -8;
    let score_6 = embedding[0] * -31 + embedding[1] * -5 + embedding[2] * -12 + embedding[3] * 9 + embedding[4] * 2 + embedding[5] * -24 + embedding[6] * 7 + embedding[7] * -14 + embedding[8] * 6 + embedding[9] * -10;
    let score_7 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 4 + embedding[1] * -5 + embedding[2] * -12 + embedding[3] * -17 + embedding[4] * 14 + embedding[5] * 16 + embedding[6] * -15 + embedding[7] * -12 + embedding[8] * 8 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 10;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 16;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 18;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 11;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_211(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_1 = embedding[0] * 17 + embedding[1] * 8 + embedding[2] * -7 + embedding[3] * 39 + embedding[4] * -8 + embedding[5] * -7 + embedding[6] * 9 + embedding[7] * -21 + embedding[8] * 12 + embedding[9] * -9;
    let score_2 = embedding[0] * -5 + embedding[1] * -12 + embedding[2] * -10 + embedding[3] * 3 + embedding[4] * 14 + embedding[5] * -24 + embedding[6] * 4 + embedding[7] * -8 + embedding[8] * -20 + embedding[9] * -5;
    let score_3 = embedding[0] * -21 + embedding[1] * -8 + embedding[2] * -9 + embedding[3] * -22 + embedding[4] * 2 + embedding[5] * 10 + embedding[6] * -2 + embedding[7] * 12 + embedding[8] * -4 + embedding[9] * 1;
    let score_4 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_5 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_6 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_7 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_8 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_9 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_10 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_11 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_12 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_13 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_14 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_15 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_16 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * 16 + embedding[8] * -11 + embedding[9] * 10;
    let score_17 = embedding[0] * 4 + embedding[1] * -5 + embedding[2] * -8 + embedding[3] * -25 + embedding[4] * 18 + embedding[5] * 18 + embedding[6] * -17 + embedding[7] * -12 + embedding[8] * 11 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 10;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 18;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_212(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_1 = embedding[0] * -1 + embedding[1] * -10 + embedding[2] * -12 + embedding[3] * 7 + embedding[4] * 14 + embedding[5] * -27 + embedding[6] * 7 + embedding[7] * -13 + embedding[8] * -16 + embedding[9] * -5;
    let score_2 = embedding[0] * -15 + embedding[1] * -6 + embedding[2] * -10 + embedding[3] * -14 + embedding[4] * 1 + embedding[5] * 7 + embedding[6] * 1 + embedding[7] * 6;
    let score_3 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_4 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -13 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_17 = embedding[0] * 10 + embedding[1] * -3 + embedding[2] * -9 + embedding[3] * -18 + embedding[4] * 16 + embedding[5] * 15 + embedding[6] * -15 + embedding[7] * -18 + embedding[8] * 16 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 18;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_213(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_1 = embedding[0] * -13 + embedding[1] * -6 + embedding[2] * -14 + embedding[3] * -14 + embedding[4] * 3 + embedding[5] * 5 + embedding[6] * 2 + embedding[7] * 4 + embedding[8] * -2 + embedding[9] * -1;
    let score_2 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_3 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_4 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_5 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_6 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_7 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_8 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_9 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_10 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_11 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_12 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_13 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_14 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_15 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_16 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_17 = embedding[0] * 10 + embedding[1] * -3 + embedding[2] * -12 + embedding[3] * -16 + embedding[4] * 20 + embedding[5] * 11 + embedding[6] * -14 + embedding[7] * -21 + embedding[8] * 12 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_214(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_215(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_216(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -6 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_217(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -21 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_218(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 12;
    let score_1 = embedding[0] * -16 + embedding[1] * -4 + embedding[2] * -10 + embedding[3] * -23 + embedding[4] * 11 + embedding[5] * -7 + embedding[6] * -4 + embedding[7] * 1 + embedding[8] * -9;
    let score_2 = embedding[0] * 22 + embedding[1] * -12 + embedding[2] * 12 + embedding[3] * 36 + embedding[4] * 9 + embedding[5] * -4 + embedding[6] * 9 + embedding[7] * 2 + embedding[8] * -6 + embedding[9] * -10;
    let score_3 = embedding[0] * 13 + embedding[1] * 1 + embedding[2] * 4 + embedding[3] * 18 + embedding[4] * -10 + embedding[5] * 20 + embedding[6] * -5 + embedding[7] * 2 + embedding[8] * 16 + embedding[9] * -7;
    let score_4 = embedding[0] * 5 + embedding[1] * -6 + embedding[2] * -1 + embedding[3] * -8 + embedding[4] * 14 + embedding[5] * -13 + embedding[6] * -11 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * -2;
    let score_5 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 11;
    let score_6 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 12;
    let score_7 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 11;
    let score_8 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 11;
    let score_9 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 12;
    let score_10 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 12;
    let score_11 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 11;
    let score_12 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 11;
    let score_13 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 11;
    let score_14 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 12;
    let score_15 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 11;
    let score_16 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 12;
    let score_17 = embedding[1] * 5 + embedding[2] * -14 + embedding[3] * -20 + embedding[4] * 9 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -19 + embedding[8] * 15 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 13;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 14;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_219(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 11;
    let score_1 = embedding[0] * -39 + embedding[1] * -15 + embedding[2] * -9 + embedding[3] * -36 + embedding[4] * 7 + embedding[5] * -11 + embedding[6] * -6 + embedding[7] * 13 + embedding[8] * -16 + embedding[9] * 7;
    let score_2 = embedding[0] * 41 + embedding[1] * 12 + embedding[2] * 22 + embedding[3] * 33 + embedding[4] * -1 + embedding[5] * 15 + embedding[6] * -3 + embedding[7] * 16 + embedding[8] * 15 + embedding[9] * -3;
    let score_3 = embedding[0] * 2 + embedding[1] * 31 + embedding[2] * 10 + embedding[3] * -5 + embedding[4] * -43 + embedding[5] * -4 + embedding[6] * 2 + embedding[7] * -24 + embedding[8] * 10 + embedding[9] * 19;
    let score_4 = embedding[0] * 24 + embedding[1] * -18 + embedding[2] * 12 + embedding[3] * 46 + embedding[4] * 9 + embedding[6] * 9 + embedding[7] * 6 + embedding[8] * -9 + embedding[9] * -12;
    let score_5 = embedding[0] * 27 + embedding[1] * -3 + embedding[2] * 11 + embedding[3] * 36 + embedding[4] * -8 + embedding[5] * 22 + embedding[6] * -1 + embedding[7] * 13 + embedding[8] * 13 + embedding[9] * -10;
    let score_6 = embedding[0] * 8 + embedding[1] * -9 + embedding[3] * -7 + embedding[4] * 14 + embedding[5] * -7 + embedding[6] * -22 + embedding[7] * 50 + embedding[8] * -1 + embedding[9] * -1;
    let score_7 = embedding[0] * -16 + embedding[1] * -14 + embedding[2] * -21 + embedding[3] * 6 + embedding[4] * 10 + embedding[5] * -31 + embedding[6] * 8 + embedding[7] * -13 + embedding[8] * -23 + embedding[9] * -4;
    let score_8 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 11;
    let score_9 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 11;
    let score_10 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 11;
    let score_11 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 11;
    let score_12 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 11;
    let score_13 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 11;
    let score_14 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 11;
    let score_15 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 11;
    let score_16 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 11;
    let score_17 = embedding[0] * -7 + embedding[1] * -2 + embedding[2] * -21 + embedding[3] * -20 + embedding[4] * 9 + embedding[5] * 11 + embedding[6] * -11 + embedding[7] * -25 + embedding[8] * 11 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 14;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 16;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 13;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 17;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 18;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_220(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_1 = embedding[0] * -27 + embedding[1] * -18 + embedding[2] * 5 + embedding[3] * -37 + embedding[4] * 8 + embedding[6] * -4 + embedding[7] * 13 + embedding[8] * -20 + embedding[9] * 5;
    let score_2 = embedding[0] * -12 + embedding[1] * 20 + embedding[2] * -32 + embedding[3] * 3 + embedding[4] * 25 + embedding[5] * -46 + embedding[6] * -9 + embedding[7] * 34 + embedding[8] * 8 + embedding[9] * 2;
    let score_3 = embedding[0] * 41 + embedding[1] * 8 + embedding[2] * 29 + embedding[3] * 31 + embedding[5] * 19 + embedding[6] * -2 + embedding[7] * 16 + embedding[8] * 13 + embedding[9] * -4;
    let score_4 = embedding[0] * 2 + embedding[1] * 31 + embedding[2] * 13 + embedding[3] * -5 + embedding[4] * -48 + embedding[5] * -2 + embedding[6] * 3 + embedding[7] * -28 + embedding[8] * 11 + embedding[9] * 19;
    let score_5 = embedding[0] * 24 + embedding[1] * -19 + embedding[2] * 17 + embedding[3] * 42 + embedding[4] * 9 + embedding[5] * 4 + embedding[6] * 9 + embedding[7] * 4 + embedding[8] * -14 + embedding[9] * -11;
    let score_6 = embedding[0] * 26 + embedding[1] * -5 + embedding[2] * 15 + embedding[3] * 31 + embedding[4] * -8 + embedding[5] * 25 + embedding[6] * -1 + embedding[7] * 10 + embedding[8] * 12 + embedding[9] * -10;
    let score_7 = embedding[0] * 20 + embedding[1] * -7 + embedding[2] * 5 + embedding[3] * -1 + embedding[4] * 17 + embedding[5] * -6 + embedding[6] * -20 + embedding[7] * 48 + embedding[8] * -2 + embedding[9] * -3;
    let score_8 = embedding[0] * -9 + embedding[1] * -7 + embedding[2] * -21 + embedding[3] * 10 + embedding[4] * 14 + embedding[5] * -36 + embedding[6] * 9 + embedding[7] * -12 + embedding[8] * -20 + embedding[9] * -6;
    let score_9 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_10 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_11 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_12 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_13 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_14 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_15 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_16 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 19 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_17 = embedding[0] * -9 + embedding[1] * -3 + embedding[2] * -19 + embedding[3] * -20 + embedding[4] * 5 + embedding[5] * 14 + embedding[6] * -10 + embedding[7] * -30 + embedding[8] * 12 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 17;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 16;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 13;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 14;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 12;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 18;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_221(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -14 + embedding[1] * 4 + embedding[2] * -14 + embedding[3] * -27 + embedding[4] * 2 + embedding[5] * 7 + embedding[6] * -10 + embedding[7] * -21 + embedding[8] * 8 + embedding[9] * 2;
    let score_2 = embedding[0] * -14 + embedding[1] * 16 + embedding[2] * -30 + embedding[3] * 5 + embedding[4] * 29 + embedding[5] * -49 + embedding[6] * -6 + embedding[7] * 42 + embedding[8] * 1;
    let score_3 = embedding[0] * 41 + embedding[1] * 9 + embedding[2] * 34 + embedding[3] * 30 + embedding[5] * 20 + embedding[6] * -1 + embedding[7] * 23 + embedding[8] * 8 + embedding[9] * -2;
    let score_4 = embedding[0] * 11 + embedding[1] * -18 + embedding[2] * 60 + embedding[3] * 4 + embedding[4] * 7 + embedding[5] * 11 + embedding[6] * 8 + embedding[7] * 33 + embedding[8] * -31 + embedding[9] * -3;
    let score_5 = embedding[0] * -2 + embedding[1] * 36 + embedding[2] * 13 + embedding[3] * -8 + embedding[4] * -48 + embedding[5] * -3 + embedding[6] * 4 + embedding[7] * -25 + embedding[8] * 9 + embedding[9] * 22;
    let score_6 = embedding[0] * 30 + embedding[1] * -21 + embedding[2] * 34 + embedding[3] * 40 + embedding[4] * 9 + embedding[5] * 9 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -22 + embedding[9] * -10;
    let score_7 = embedding[0] * 26 + embedding[1] * -2 + embedding[2] * 19 + embedding[3] * 30 + embedding[4] * -8 + embedding[5] * 25 + embedding[6] * -1 + embedding[7] * 18 + embedding[8] * 9 + embedding[9] * -8;
    let score_8 = embedding[0] * 17 + embedding[1] * -16 + embedding[2] * 10 + embedding[3] * 4 + embedding[4] * 21 + embedding[5] * -8 + embedding[6] * -15 + embedding[7] * 61 + embedding[8] * -7 + embedding[9] * -10;
    let score_9 = embedding[0] * -15 + embedding[1] * -23 + embedding[2] * -2 + embedding[3] * 9 + embedding[4] * 20 + embedding[5] * -23 + embedding[6] * -6 + embedding[7] * 69 + embedding[8] * -11 + embedding[9] * -15;
    let score_10 = embedding[0] * -13 + embedding[1] * -7 + embedding[2] * -21 + embedding[3] * 12 + embedding[4] * 16 + embedding[5] * -39 + embedding[6] * 10 + embedding[7] * 2 + embedding[8] * -27 + embedding[9] * -6;
    let score_11 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * -16 + embedding[3] * -21 + embedding[4] * 3 + embedding[5] * 11 + embedding[6] * -8 + embedding[7] * -26 + embedding[8] * 11 + embedding[9] * -1;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 75;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 9;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 16;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 13;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 17;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 12;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 6;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 18;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_222(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 6 + embedding[1] * 4 + embedding[2] * -4 + embedding[3] * -27 + embedding[4] * 10 + embedding[5] * 9 + embedding[6] * -12 + embedding[7] * -20 + embedding[8] * -1 + embedding[9] * 4;
    let score_1 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_2 = embedding[0] * -17 + embedding[1] * -15 + embedding[2] * -22 + embedding[3] * 33 + embedding[4] * 10 + embedding[5] * -3 + embedding[6] * 10 + embedding[7] * 8 + embedding[8] * 9 + embedding[9] * -21;
    let score_3 = embedding[0] * 12 + embedding[1] * 38 + embedding[2] * -2 + embedding[3] * -8 + embedding[4] * -14 + embedding[5] * -12 + embedding[6] * -4 + embedding[7] * -16 + embedding[8] * 11 + embedding[9] * 16;
    let score_4 = embedding[0] * 18 + embedding[1] * -22 + embedding[2] * 73 + embedding[3] * -4 + embedding[4] * 6 + embedding[5] * 9 + embedding[6] * 9 + embedding[7] * 34 + embedding[8] * -45 + embedding[9] * 2;
    let score_5 = embedding[0] * 14 + embedding[1] * 49 + embedding[2] * 20 + embedding[3] * -9 + embedding[4] * -45 + embedding[5] * -13 + embedding[6] * 2 + embedding[7] * -18 + embedding[8] * 8 + embedding[9] * 29;
    let score_6 = embedding[0] * -25 + embedding[1] * 3 + embedding[2] * -25 + embedding[3] * 17 + embedding[4] * 4 + embedding[5] * -28 + embedding[6] * -6 + embedding[7] * 44 + embedding[8] * 13 + embedding[9] * -2;
    let score_7 = embedding[0] * 19 + embedding[1] * -34 + embedding[2] * 24 + embedding[3] * 40 + embedding[4] * 15 + embedding[5] * 6 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -25 + embedding[9] * -17;
    let score_8 = embedding[0] * 7 + embedding[1] * -7 + embedding[2] * -1 + embedding[3] * 29 + embedding[4] * -11 + embedding[5] * 25 + embedding[6] * 1 + embedding[7] * 12 + embedding[8] * 15 + embedding[9] * -9;
    let score_9 = embedding[0] * 16 + embedding[1] * -22 + embedding[2] * 14 + embedding[4] * 21 + embedding[5] * -2 + embedding[6] * -11 + embedding[7] * 51 + embedding[8] * -19 + embedding[9] * -7;
    let score_10 = embedding[0] * -21 + embedding[1] * -18 + embedding[2] * -18 + embedding[3] * 15 + embedding[4] * 11 + embedding[5] * -41 + embedding[6] * 12 + embedding[7] * -5 + embedding[8] * -26 + embedding[9] * -8;
    let score_11 = embedding[0] * -42 + embedding[1] * -6 + embedding[2] * -25 + embedding[3] * 34 + embedding[4] * 1 + embedding[5] * -39 + embedding[6] * 17 + embedding[7] * -6 + embedding[8] * 6 + embedding[9] * -20;
    let score_12 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[0] * -12 + embedding[1] * 1 + embedding[2] * -14 + embedding[4] * -6 + embedding[5] * 17 + embedding[6] * -3 + embedding[7] * -6 + embedding[8] * 21 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 24;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 9;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 16;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 8;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 13;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 75;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 12;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 18;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 11;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_223(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -2 + embedding[1] * -5 + embedding[2] * 6 + embedding[3] * -32 + embedding[4] * 4 + embedding[5] * 11 + embedding[6] * -16 + embedding[7] * -6 + embedding[8] * -1 + embedding[9] * 4;
    let score_1 = embedding[0] * 7 + embedding[1] * 1 + embedding[2] * -7 + embedding[3] * -12 + embedding[4] * 7 + embedding[5] * -3 + embedding[7] * -3 + embedding[8] * -20 + embedding[9] * -10;
    let score_2 = embedding[0] * 7 + embedding[1] * -8 + embedding[2] * -3 + embedding[3] * -12 + embedding[4] * 17 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * -7 + embedding[8] * -4 + embedding[9] * 7;
    let score_3 = embedding[0] * -1 + embedding[1] * -3 + embedding[2] * -4 + embedding[3] * -1 + embedding[4] * 15 + embedding[5] * -2 + embedding[6] * -4 + embedding[7] * -8 + embedding[8] * -18 + embedding[9] * 6;
    let score_4 = embedding[0] * -2 + embedding[1] * -22 + embedding[2] * -9 + embedding[3] * 63 + embedding[4] * -2 + embedding[5] * 6 + embedding[6] * 23 + embedding[7] * 18 + embedding[8] * 6 + embedding[9] * -21;
    let score_5 = embedding[0] * 5 + embedding[1] * 40 + embedding[2] * -13 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -20 + embedding[6] * 5 + embedding[7] * -11 + embedding[8] * 15 + embedding[9] * 11;
    let score_6 = embedding[0] * -1 + embedding[1] * -3 + embedding[2] * 12 + embedding[3] * -9 + embedding[4] * 7 + embedding[5] * -14 + embedding[6] * -12 + embedding[7] * -13 + embedding[8] * -4 + embedding[9] * -5;
    let score_7 = embedding[0] * 8 + embedding[1] * -6 + embedding[2] * 7 + embedding[3] * -11 + embedding[4] * -4 + embedding[5] * 15 + embedding[6] * -8 + embedding[7] * -8 + embedding[8] * -2 + embedding[9] * -2;
    let score_8 = embedding[0] * 6 + embedding[1] * 21 + embedding[3] * -2 + embedding[4] * -13 + embedding[7] * -17 + embedding[8] * 9 + embedding[9] * 12;
    let score_9 = embedding[0] * 9 + embedding[1] * 2 + embedding[2] * 28 + embedding[3] * -7 + embedding[4] * -44 + embedding[5] * 24 + embedding[6] * -13 + embedding[7] * 13 + embedding[8] * 5 + embedding[9] * 14;
    let score_10 = embedding[0] * 11 + embedding[1] * -13 + embedding[2] * 10 + embedding[3] * 44 + embedding[4] * -20 + embedding[5] * 20 + embedding[6] * 8 + embedding[7] * 10 + embedding[8] * 7 + embedding[9] * -11;
    let score_11 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * -21 + embedding[3] * 14 + embedding[4] * 4 + embedding[5] * -28 + embedding[6] * -6 + embedding[7] * 34 + embedding[8] * 4 + embedding[9] * 4;
    let score_12 = embedding[0] * 14 + embedding[1] * -31 + embedding[2] * 12 + embedding[3] * 54 + embedding[4] * 12 + embedding[5] * -4 + embedding[6] * 26 + embedding[7] * 12 + embedding[8] * -26 + embedding[9] * -17;
    let score_13 = embedding[0] * -1 + embedding[1] * 17 + embedding[2] * 7 + embedding[3] * 29 + embedding[4] * -26 + embedding[5] * 1 + embedding[6] * -6 + embedding[7] * 20 + embedding[8] * 30 + embedding[9] * 1;
    let score_14 = embedding[0] * -13 + embedding[1] * -16 + embedding[2] * -16 + embedding[3] * 24 + embedding[4] * 14 + embedding[5] * -39 + embedding[6] * 22 + embedding[7] * -3 + embedding[8] * -35 + embedding[9] * -6;
    let score_15 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * -14 + embedding[3] * 26 + embedding[4] * -7 + embedding[5] * -18 + embedding[6] * 10 + embedding[7] * -8 + embedding[8] * 10 + embedding[9] * -10;
    let score_16 = embedding[0] * -9 + embedding[1] * 27 + embedding[2] * -14 + embedding[4] * -9 + embedding[5] * -19 + embedding[6] * -6 + embedding[8] * 12 + embedding[9] * 9;
    let score_17 = embedding[0] * 3 + embedding[1] * 2 + embedding[2] * -5 + embedding[3] * 51 + embedding[4] * -25 + embedding[5] * 4 + embedding[6] * 14 + embedding[7] * -1 + embedding[8] * 16 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 21;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 5;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 19;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 16;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 44;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 14;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 8;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 13;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 15;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 18;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 11;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 24;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 10;
    }
    best_palette
}

fn predict_pixel_224(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 5 + embedding[1] * -7 + embedding[2] * -2 + embedding[3] * -22 + embedding[4] * 9 + embedding[5] * 5 + embedding[6] * -13 + embedding[7] * -4 + embedding[8] * -7 + embedding[9] * 2;
    let score_1 = embedding[0] * 4 + embedding[1] * -10 + embedding[2] * -6 + embedding[3] * -14 + embedding[4] * 10 + embedding[5] * 1 + embedding[6] * 11 + embedding[7] * -5 + embedding[8] * 2 + embedding[9] * 7;
    let score_2 = embedding[0] * 4 + embedding[1] * -2 + embedding[2] * -10 + embedding[3] * -14 + embedding[4] * -1 + embedding[5] * 6 + embedding[6] * -3 + embedding[7] * -3 + embedding[8] * -14 + embedding[9] * -11;
    let score_3 = embedding[0] * -2 + embedding[1] * -5 + embedding[2] * -7 + embedding[3] * -2 + embedding[4] * 9 + embedding[5] * 9 + embedding[6] * -8 + embedding[7] * -8 + embedding[8] * -12 + embedding[9] * 7;
    let score_4 = embedding[0] * -4 + embedding[1] * -8 + embedding[2] * 12 + embedding[3] * -6 + embedding[4] * 7 + embedding[5] * -7 + embedding[6] * -14 + embedding[7] * -9 + embedding[8] * -2 + embedding[9] * -9;
    let score_5 = embedding[0] * -11 + embedding[1] * -13 + embedding[2] * 7 + embedding[3] * 12 + embedding[4] * 4 + embedding[5] * 1 + embedding[6] * -2 + embedding[7] * 30 + embedding[8] * 16 + embedding[9] * -10;
    let score_6 = embedding[0] * 6 + embedding[1] * 36 + embedding[2] * -12 + embedding[3] * 11 + embedding[4] * -11 + embedding[5] * -15 + embedding[6] * 4 + embedding[7] * -5 + embedding[8] * 6 + embedding[9] * 7;
    let score_7 = embedding[0] * 14 + embedding[1] * 1 + embedding[2] * 6 + embedding[3] * 7 + embedding[4] * -15 + embedding[5] * 13 + embedding[6] * 6 + embedding[7] * -15 + embedding[8] * -3;
    let score_8 = embedding[0] * -17 + embedding[1] * -14 + embedding[2] * 11 + embedding[3] * -4 + embedding[4] * 3 + embedding[6] * 3 + embedding[7] * 1 + embedding[8] * 6 + embedding[9] * -7;
    let score_9 = embedding[0] * 8 + embedding[1] * 15 + embedding[2] * -11 + embedding[3] * 22 + embedding[4] * 23 + embedding[5] * -41 + embedding[6] * 19 + embedding[7] * 20 + embedding[8] * -20 + embedding[9] * -2;
    let score_10 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -1 + embedding[3] * -4 + embedding[4] * -10 + embedding[5] * 14 + embedding[6] * -14 + embedding[7] * 14 + embedding[9] * 4;
    let score_11 = embedding[0] * 11 + embedding[1] * 30 + embedding[2] * 2 + embedding[3] * 9 + embedding[4] * -28 + embedding[5] * 1 + embedding[6] * 3 + embedding[7] * -6 + embedding[8] * 7 + embedding[9] * 11;
    let score_12 = embedding[0] * 15 + embedding[1] * -6 + embedding[2] * 14 + embedding[3] * 31 + embedding[4] * -20 + embedding[5] * 19 + embedding[7] * 15 + embedding[8] * 7 + embedding[9] * -4;
    let score_13 = embedding[0] * 2 + embedding[1] * 12 + embedding[2] * 13 + embedding[3] * 36 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 1 + embedding[7] * 28 + embedding[8] * 33 + embedding[9] * -1;
    let score_14 = embedding[0] * -10 + embedding[1] * -8 + embedding[2] * -14 + embedding[3] * 17 + embedding[4] * 16 + embedding[5] * -34 + embedding[6] * 23 + embedding[8] * -35 + embedding[9] * -4;
    let score_15 = embedding[0] * -9 + embedding[1] * 28 + embedding[2] * -19 + embedding[3] * 11 + embedding[4] * -18 + embedding[5] * -16 + embedding[6] * 2 + embedding[7] * -4 + embedding[8] * 9 + embedding[9] * 5;
    let score_16 = embedding[0] * 6 + embedding[1] * 12 + embedding[2] * -3 + embedding[3] * 29 + embedding[4] * -25 + embedding[5] * 5 + embedding[6] * 9 + embedding[7] * -4 + embedding[8] * 12;
    let score_17 = embedding[0] * -31 + embedding[1] * -14 + embedding[2] * 27 + embedding[3] * -2 + embedding[4] * 11 + embedding[5] * -11 + embedding[6] * 1 + embedding[7] * 25 + embedding[8] * 11 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 24;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 19;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 11;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 8;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 20;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 16;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 17;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 15;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 18;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 21;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 10;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 77;
    }
    best_palette
}

fn predict_pixel_225(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 2 + embedding[1] * -2 + embedding[3] * -25 + embedding[4] * 3 + embedding[5] * 2 + embedding[6] * -15 + embedding[7] * -8 + embedding[8] * -2 + embedding[9] * 3;
    let score_1 = embedding[0] * 9 + embedding[1] * 1 + embedding[2] * -1 + embedding[3] * -12 + embedding[4] * 6 + embedding[5] * 1 + embedding[6] * 2 + embedding[7] * 1 + embedding[8] * -25 + embedding[9] * -11;
    let score_2 = embedding[0] * 9 + embedding[1] * -10 + embedding[2] * 4 + embedding[3] * -13 + embedding[4] * 20 + embedding[5] * -7 + embedding[6] * 15 + embedding[7] * -3 + embedding[8] * -2 + embedding[9] * 11;
    let score_3 = embedding[0] * 2 + embedding[1] * -4 + embedding[3] * 2 + embedding[4] * 16 + embedding[5] * 4 + embedding[6] * -2 + embedding[7] * -6 + embedding[8] * -23 + embedding[9] * 9;
    let score_4 = embedding[0] * -3 + embedding[1] * -21 + embedding[2] * -7 + embedding[3] * 60 + embedding[4] * -4 + embedding[5] * 9 + embedding[6] * 14 + embedding[7] * 25 + embedding[8] * 12 + embedding[9] * -20;
    let score_5 = embedding[0] * -8 + embedding[1] * -10 + embedding[2] * 22 + embedding[3] * -8 + embedding[4] * 16 + embedding[5] * -13 + embedding[6] * -11 + embedding[7] * -8 + embedding[8] * 2 + embedding[9] * -8;
    let score_6 = embedding[0] * -12 + embedding[1] * -11 + embedding[2] * 10 + embedding[3] * 2 + embedding[4] * 1 + embedding[5] * -4 + embedding[6] * -7 + embedding[7] * -3 + embedding[8] * 10 + embedding[9] * -11;
    let score_7 = embedding[0] * 6 + embedding[1] * 41 + embedding[2] * -11 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -16 + embedding[6] * 3 + embedding[7] * -8 + embedding[8] * 15 + embedding[9] * 9;
    let score_8 = embedding[0] * 14 + embedding[1] * 4 + embedding[2] * 11 + embedding[3] * 4 + embedding[4] * -14 + embedding[5] * 11 + embedding[6] * 5 + embedding[7] * -15 + embedding[8] * -1 + embedding[9] * 3;
    let score_9 = embedding[0] * -3 + embedding[1] * 2 + embedding[2] * -13 + embedding[3] * -4 + embedding[4] * -12 + embedding[5] * 4 + embedding[6] * -8 + embedding[7] * 6 + embedding[8] * -5 + embedding[9] * -9;
    let score_10 = embedding[0] * -7 + embedding[1] * -4 + embedding[2] * -13 + embedding[3] * 8 + embedding[4] * -6 + embedding[5] * 7 + embedding[6] * -10 + embedding[7] * 2 + embedding[8] * -5 + embedding[9] * 7;
    let score_11 = embedding[0] * -2 + embedding[1] * -7 + embedding[2] * -10 + embedding[3] * -6 + embedding[4] * -2 + embedding[5] * -2 + embedding[6] * 9 + embedding[7] * 4 + embedding[8] * 11 + embedding[9] * 8;
    let score_12 = embedding[0] * 12 + embedding[1] * 2 + embedding[2] * -1 + embedding[3] * -4 + embedding[4] * -15 + embedding[5] * 13 + embedding[6] * -16 + embedding[7] * 13 + embedding[8] * 5 + embedding[9] * 6;
    let score_13 = embedding[0] * 9 + embedding[1] * 38 + embedding[2] * 11 + embedding[3] * 3 + embedding[4] * -36 + embedding[5] * -3 + embedding[6] * 6 + embedding[7] * -7 + embedding[8] * 9 + embedding[9] * 18;
    let score_14 = embedding[0] * 11 + embedding[1] * -7 + embedding[2] * 2 + embedding[3] * 53 + embedding[4] * -23 + embedding[5] * 17 + embedding[6] * 7 + embedding[7] * 12 + embedding[8] * 16 + embedding[9] * -12;
    let score_15 = embedding[1] * 9 + embedding[2] * 7 + embedding[3] * 39 + embedding[4] * -25 + embedding[5] * 1 + embedding[6] * -3 + embedding[7] * 28 + embedding[8] * 33 + embedding[9] * -4;
    let score_16 = embedding[0] * -14 + embedding[1] * -15 + embedding[2] * -10 + embedding[3] * 24 + embedding[4] * 18 + embedding[5] * -37 + embedding[6] * 29 + embedding[7] * -3 + embedding[8] * -40 + embedding[9] * -6;
    let score_17 = embedding[0] * -8 + embedding[1] * 23 + embedding[2] * -14 + embedding[3] * -1 + embedding[4] * -10 + embedding[5] * -17 + embedding[6] * -5 + embedding[7] * -4 + embedding[8] * 8 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 11;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 21;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 19;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 82;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 84;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 83;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 20;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 16;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 17;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 15;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 18;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 24;
    }
    best_palette
}

fn predict_pixel_226(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 2 + embedding[1] * -4 + embedding[2] * -9 + embedding[3] * -16 + embedding[4] * 4 + embedding[5] * 4 + embedding[6] * -5 + embedding[8] * -17 + embedding[9] * -12;
    let score_1 = embedding[0] * 3 + embedding[1] * -12 + embedding[2] * -5 + embedding[3] * -17 + embedding[4] * 16 + embedding[5] * -2 + embedding[6] * 10 + embedding[7] * -3 + embedding[8] * 1 + embedding[9] * 6;
    let score_2 = embedding[0] * 4 + embedding[1] * -4 + embedding[2] * -1 + embedding[3] * -20 + embedding[4] * 6 + embedding[5] * 2 + embedding[6] * -16 + embedding[7] * -4 + embedding[8] * -4 + embedding[9] * 2;
    let score_3 = embedding[0] * -6 + embedding[1] * -6 + embedding[2] * -5 + embedding[3] * -2 + embedding[4] * 15 + embedding[5] * 8 + embedding[6] * -9 + embedding[7] * -5 + embedding[8] * -15 + embedding[9] * 7;
    let score_4 = embedding[0] * -4 + embedding[1] * -8 + embedding[2] * 17 + embedding[3] * -8 + embedding[4] * 10 + embedding[5] * -12 + embedding[6] * -20 + embedding[7] * -7 + embedding[8] * -4 + embedding[9] * -8;
    let score_5 = embedding[1] * -7 + embedding[2] * -4 + embedding[3] * 47 + embedding[4] * -6 + embedding[5] * 12 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * 10 + embedding[9] * -13;
    let score_6 = embedding[0] * 6 + embedding[1] * 36 + embedding[2] * -9 + embedding[3] * 8 + embedding[4] * -11 + embedding[5] * -15 + embedding[6] * 1 + embedding[7] * -6 + embedding[8] * 7 + embedding[9] * 9;
    let score_7 = embedding[0] * 13 + embedding[1] * 2 + embedding[2] * 7 + embedding[3] * 6 + embedding[4] * -15 + embedding[5] * 11 + embedding[6] * 3 + embedding[7] * -14 + embedding[8] * -2 + embedding[9] * 1;
    let score_8 = embedding[0] * 10 + embedding[1] * 3 + embedding[2] * -2 + embedding[3] * -6 + embedding[4] * -13 + embedding[5] * 10 + embedding[6] * -13 + embedding[7] * 3 + embedding[8] * 2 + embedding[9] * 6;
    let score_9 = embedding[0] * 9 + embedding[1] * 38 + embedding[2] * 10 + embedding[3] * 5 + embedding[4] * -36 + embedding[5] * -4 + embedding[6] * 6 + embedding[7] * -5 + embedding[8] * 6 + embedding[9] * 18;
    let score_10 = embedding[0] * 12 + embedding[1] * -6 + embedding[2] * 10 + embedding[3] * 43 + embedding[4] * -21 + embedding[5] * 23 + embedding[6] * 5 + embedding[7] * 14 + embedding[8] * 11 + embedding[9] * -7;
    let score_11 = embedding[1] * 13 + embedding[2] * 6 + embedding[3] * 40 + embedding[4] * -22 + embedding[6] * 2 + embedding[7] * 22 + embedding[8] * 30 + embedding[9] * -1;
    let score_12 = embedding[0] * -13 + embedding[1] * -14 + embedding[2] * -11 + embedding[3] * 16 + embedding[4] * 13 + embedding[5] * -35 + embedding[6] * 24 + embedding[7] * -5 + embedding[8] * -40 + embedding[9] * -2;
    let score_13 = embedding[0] * -7 + embedding[1] * 28 + embedding[2] * -18 + embedding[3] * 10 + embedding[4] * -16 + embedding[5] * -17 + embedding[6] * -1 + embedding[7] * -4 + embedding[8] * 9 + embedding[9] * 6;
    let score_14 = embedding[0] * 4 + embedding[1] * 7 + embedding[2] * -2 + embedding[3] * 48 + embedding[4] * -25 + embedding[5] * 8 + embedding[6] * 11 + embedding[7] * 1 + embedding[8] * 16 + embedding[9] * -5;
    let score_15 = embedding[0] * -8 + embedding[1] * -14 + embedding[2] * 23 + embedding[3] * -2 + embedding[4] * -2 + embedding[5] * 6 + embedding[6] * -2 + embedding[7] * 11 + embedding[8] * 4 + embedding[9] * -7;
    let score_16 = embedding[0] * -26 + embedding[1] * -11 + embedding[2] * 25 + embedding[3] * -5 + embedding[4] * 15 + embedding[5] * -11 + embedding[6] * -3 + embedding[7] * 20 + embedding[8] * 9 + embedding[9] * -1;
    let score_17 = embedding[0] * 4 + embedding[1] * -12 + embedding[2] * 2 + embedding[3] * -3 + embedding[4] * 12 + embedding[5] * -9 + embedding[6] * -3 + embedding[7] * 13 + embedding[8] * -4 + embedding[9] * 24;
    let mut best_score = score_0;
    let mut best_palette = 3;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 24;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 19;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 20;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 16;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 17;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 15;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 18;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 21;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 10;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 11;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 30;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 23;
    }
    best_palette
}

fn predict_pixel_227(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 6 + embedding[1] * -4 + embedding[2] * -1 + embedding[3] * -24 + embedding[4] * 11 + embedding[5] * 2 + embedding[6] * -16 + embedding[7] * -3 + embedding[8] * -8 + embedding[9] * 4;
    let score_1 = embedding[0] * 4 + embedding[1] * -5 + embedding[2] * -9 + embedding[3] * -16 + embedding[4] * 3 + embedding[5] * 3 + embedding[6] * -4 + embedding[7] * 1 + embedding[8] * -19 + embedding[9] * -12;
    let score_2 = embedding[0] * 5 + embedding[1] * -13 + embedding[2] * -6 + embedding[3] * -16 + embedding[4] * 15 + embedding[5] * -1 + embedding[6] * 9 + embedding[7] * -2 + embedding[9] * 8;
    let score_3 = embedding[0] * -2 + embedding[1] * -7 + embedding[2] * -6 + embedding[3] * -2 + embedding[4] * 14 + embedding[5] * 7 + embedding[6] * -10 + embedding[7] * -6 + embedding[8] * -17 + embedding[9] * 7;
    let score_4 = embedding[0] * -3 + embedding[1] * -10 + embedding[2] * 16 + embedding[3] * -7 + embedding[4] * 10 + embedding[5] * -11 + embedding[6] * -17 + embedding[7] * -6 + embedding[8] * -5 + embedding[9] * -8;
    let score_5 = embedding[0] * -1 + embedding[1] * -8 + embedding[2] * -3 + embedding[3] * 55 + embedding[4] * -10 + embedding[5] * 11 + embedding[6] * 16 + embedding[7] * 15 + embedding[8] * 14 + embedding[9] * -15;
    let score_6 = embedding[0] * 8 + embedding[1] * 35 + embedding[2] * -8 + embedding[3] * 7 + embedding[4] * -13 + embedding[5] * -15 + embedding[6] * 1 + embedding[7] * -8 + embedding[8] * 6 + embedding[9] * 11;
    let score_7 = embedding[0] * 9 + embedding[1] * 7 + embedding[2] * 4 + embedding[4] * -11 + embedding[5] * 8 + embedding[6] * 2 + embedding[7] * -15 + embedding[8] * 2 + embedding[9] * 3;
    let score_8 = embedding[0] * -17 + embedding[1] * -16 + embedding[2] * 14 + embedding[3] * 2 + embedding[4] * 5 + embedding[6] * 5 + embedding[7] * -1 + embedding[8] * 6 + embedding[9] * -10;
    let score_9 = embedding[0] * 11 + embedding[1] * 3 + embedding[3] * -9 + embedding[4] * -11 + embedding[5] * 9 + embedding[6] * -14 + embedding[7] * 5 + embedding[9] * 7;
    let score_10 = embedding[0] * 10 + embedding[1] * 39 + embedding[2] * 13 + embedding[3] * 4 + embedding[4] * -38 + embedding[5] * -4 + embedding[6] * 8 + embedding[7] * -6 + embedding[8] * 4 + embedding[9] * 20;
    let score_11 = embedding[0] * 12 + embedding[1] * -8 + embedding[2] * 10 + embedding[3] * 51 + embedding[4] * -22 + embedding[5] * 22 + embedding[6] * 8 + embedding[7] * 15 + embedding[8] * 13 + embedding[9] * -9;
    let score_12 = embedding[0] * -1 + embedding[1] * 14 + embedding[2] * 6 + embedding[3] * 47 + embedding[4] * -27 + embedding[6] * 5 + embedding[7] * 24 + embedding[8] * 34 + embedding[9] * -2;
    let score_13 = embedding[0] * -12 + embedding[1] * -14 + embedding[2] * -12 + embedding[3] * 17 + embedding[4] * 13 + embedding[5] * -36 + embedding[6] * 24 + embedding[7] * -5 + embedding[8] * -38 + embedding[9] * -3;
    let score_14 = embedding[0] * -8 + embedding[1] * 25 + embedding[2] * -17 + embedding[3] * 6 + embedding[4] * -13 + embedding[5] * -17 + embedding[6] * -2 + embedding[7] * -5 + embedding[8] * 7 + embedding[9] * 6;
    let score_15 = embedding[0] * 2 + embedding[1] * 8 + embedding[2] * -4 + embedding[3] * 56 + embedding[4] * -30 + embedding[5] * 6 + embedding[6] * 15 + embedding[7] * 3 + embedding[8] * 21 + embedding[9] * -6;
    let score_16 = embedding[0] * -30 + embedding[1] * -12 + embedding[2] * 26 + embedding[3] * -4 + embedding[4] * 14 + embedding[5] * -12 + embedding[7] * 21 + embedding[8] * 10 + embedding[9] * -2;
    let score_17 = embedding[0] * -5 + embedding[1] * 29 + embedding[2] * -14 + embedding[3] * 34 + embedding[4] * -37 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 3 + embedding[8] * 18 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 21;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 19;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 11;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 20;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 16;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 17;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 15;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 18;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 24;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 10;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 77;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 35;
    }
    best_palette
}

fn predict_pixel_228(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[1] * -4 + embedding[2] * -4 + embedding[3] * -27 + embedding[4] * 5 + embedding[5] * 1 + embedding[6] * -14 + embedding[7] * -9 + embedding[8] * -11 + embedding[9] * 8;
    let score_1 = embedding[1] * -10 + embedding[2] * -14 + embedding[3] * -17 + embedding[4] * 5 + embedding[6] * -4 + embedding[7] * 2 + embedding[8] * -22 + embedding[9] * -14;
    let score_2 = embedding[0] * 1 + embedding[1] * -17 + embedding[2] * -10 + embedding[3] * -16 + embedding[4] * 17 + embedding[5] * -5 + embedding[6] * 10 + embedding[7] * -3 + embedding[8] * -2 + embedding[9] * 6;
    let score_3 = embedding[0] * -7 + embedding[1] * -12 + embedding[2] * -10 + embedding[3] * -2 + embedding[4] * 16 + embedding[5] * 4 + embedding[6] * -9 + embedding[7] * -7 + embedding[8] * -18 + embedding[9] * 9;
    let score_4 = embedding[0] * -9 + embedding[1] * -15 + embedding[2] * 11 + embedding[3] * -4 + embedding[4] * 11 + embedding[5] * -13 + embedding[6] * -15 + embedding[7] * -8 + embedding[8] * -6 + embedding[9] * -10;
    let score_5 = embedding[0] * -2 + embedding[1] * -10 + embedding[2] * -5 + embedding[3] * 59 + embedding[4] * -8 + embedding[5] * 12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * 18 + embedding[9] * -15;
    let score_6 = embedding[0] * 6 + embedding[1] * 33 + embedding[2] * -8 + embedding[3] * 2 + embedding[4] * -14 + embedding[5] * -14 + embedding[6] * -2 + embedding[7] * -9 + embedding[8] * 2 + embedding[9] * 14;
    let score_7 = embedding[0] * 37 + embedding[1] * 23 + embedding[2] * 25 + embedding[3] * 37 + embedding[4] * -11 + embedding[5] * 12 + embedding[6] * 2 + embedding[7] * 19 + embedding[8] * 13 + embedding[9] * 4;
    let score_8 = embedding[0] * 8 + embedding[1] * 8 + embedding[2] * 3 + embedding[3] * 2 + embedding[4] * -14 + embedding[5] * 7 + embedding[6] * -1 + embedding[7] * -14 + embedding[8] * 3 + embedding[9] * 4;
    let score_9 = embedding[0] * -24 + embedding[1] * -20 + embedding[2] * 10 + embedding[3] * 3 + embedding[4] * 6 + embedding[5] * -3 + embedding[6] * 5 + embedding[7] * -2 + embedding[8] * 5 + embedding[9] * -11;
    let score_10 = embedding[0] * 13 + embedding[1] * 41 + embedding[2] * 17 + embedding[3] * 5 + embedding[4] * -42 + embedding[5] * -7 + embedding[6] * 5 + embedding[7] * -7 + embedding[8] * 3 + embedding[9] * 22;
    let score_11 = embedding[0] * 15 + embedding[1] * 9 + embedding[2] * 1 + embedding[4] * -15 + embedding[5] * 11 + embedding[6] * -14 + embedding[7] * 7 + embedding[8] * 3 + embedding[9] * 8;
    let score_12 = embedding[0] * 19 + embedding[1] * -1 + embedding[2] * 14 + embedding[3] * 66 + embedding[4] * -27 + embedding[5] * 23 + embedding[6] * 7 + embedding[7] * 21 + embedding[8] * 22 + embedding[9] * -7;
    let score_13 = embedding[0] * 4 + embedding[1] * 14 + embedding[2] * 9 + embedding[3] * 53 + embedding[4] * -27 + embedding[5] * 4 + embedding[6] * 3 + embedding[7] * 25 + embedding[8] * 38;
    let score_14 = embedding[0] * -16 + embedding[1] * -20 + embedding[2] * -18 + embedding[3] * 11 + embedding[4] * 16 + embedding[5] * -40 + embedding[6] * 22 + embedding[7] * -5 + embedding[8] * -41 + embedding[9] * -3;
    let score_15 = embedding[0] * -4 + embedding[1] * 14 + embedding[2] * -20 + embedding[3] * 13 + embedding[4] * -13 + embedding[5] * -3 + embedding[6] * -1 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * 2;
    let score_16 = embedding[0] * 5 + embedding[1] * 6 + embedding[2] * -2 + embedding[3] * 60 + embedding[4] * -28 + embedding[5] * 11 + embedding[6] * 12 + embedding[7] * 4 + embedding[8] * 24 + embedding[9] * -7;
    let score_17 = embedding[0] * -37 + embedding[1] * -17 + embedding[2] * 22 + embedding[3] * -3 + embedding[4] * 15 + embedding[5] * -15 + embedding[6] * 1 + embedding[7] * 20 + embedding[8] * 8 + embedding[9] * -3;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 24;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 17;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 19;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 11;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 16;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 20;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 14;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 15;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 18;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 21;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 10;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 76;
    }
    best_palette
}

fn predict_pixel_229(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 2 + embedding[1] * -2 + embedding[2] * -4 + embedding[3] * -20 + embedding[4] * 2 + embedding[5] * 2 + embedding[6] * -14 + embedding[7] * -8 + embedding[8] * -2 + embedding[9] * 3;
    let score_1 = embedding[0] * -1 + embedding[1] * -8 + embedding[2] * -11 + embedding[3] * -15 + embedding[4] * 6 + embedding[5] * 2 + embedding[6] * -3 + embedding[7] * 3 + embedding[8] * -23 + embedding[9] * -14;
    let score_2 = embedding[0] * 1 + embedding[1] * -17 + embedding[2] * -7 + embedding[3] * -15 + embedding[4] * 20 + embedding[5] * -3 + embedding[6] * 14 + embedding[8] * 1 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * -10 + embedding[2] * -8 + embedding[3] * 1 + embedding[4] * 17 + embedding[5] * 7 + embedding[6] * -7 + embedding[7] * -4 + embedding[8] * -19 + embedding[9] * 6;
    let score_4 = embedding[0] * -11 + embedding[1] * -14 + embedding[2] * 16 + embedding[3] * -4 + embedding[4] * 13 + embedding[5] * -15 + embedding[6] * -17 + embedding[7] * -6 + embedding[8] * -6 + embedding[9] * -12;
    let score_5 = embedding[0] * 33 + embedding[1] * 14 + embedding[2] * 22 + embedding[3] * 41 + embedding[4] * -11 + embedding[5] * 16 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * 8 + embedding[9] * -1;
    let score_6 = embedding[0] * 5 + embedding[1] * 39 + embedding[2] * -13 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -17 + embedding[6] * 4 + embedding[7] * -9 + embedding[8] * 13 + embedding[9] * 11;
    let score_7 = embedding[0] * 17 + embedding[1] * 3 + embedding[2] * 10 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * 14 + embedding[6] * 5 + embedding[7] * -11 + embedding[8] * 3 + embedding[9] * 2;
    let score_8 = embedding[0] * -9 + embedding[1] * -8 + embedding[2] * 9 + embedding[3] * 23 + embedding[4] * 1 + embedding[5] * 1 + embedding[6] * 6 + embedding[7] * 13 + embedding[8] * 19 + embedding[9] * -8;
    let score_9 = embedding[0] * -21 + embedding[1] * -15 + embedding[2] * 10 + embedding[3] * -4 + embedding[4] * 5 + embedding[5] * -3 + embedding[6] * 2 + embedding[7] * -1 + embedding[8] * 5 + embedding[9] * -8;
    let score_10 = embedding[0] * 12 + embedding[1] * 5 + embedding[2] * -1 + embedding[3] * -3 + embedding[4] * -15 + embedding[5] * 12 + embedding[6] * -12 + embedding[7] * 4 + embedding[8] * 4 + embedding[9] * 8;
    let score_11 = embedding[0] * 8 + embedding[1] * 38 + embedding[2] * 8 + embedding[3] * 7 + embedding[4] * -38 + embedding[5] * -3 + embedding[6] * 6 + embedding[7] * -6 + embedding[8] * 11 + embedding[9] * 19;
    let score_12 = embedding[0] * 23 + embedding[2] * 23 + embedding[3] * 47 + embedding[4] * -22 + embedding[5] * 24 + embedding[6] * 7 + embedding[7] * 16 + embedding[8] * 11 + embedding[9] * -5;
    let score_13 = embedding[0] * -15 + embedding[2] * -13 + embedding[3] * -8 + embedding[4] * -11 + embedding[5] * -4 + embedding[6] * 2 + embedding[7] * -8 + embedding[8] * -1 + embedding[9] * 4;
    let score_14 = embedding[0] * -26 + embedding[1] * -8 + embedding[2] * 17 + embedding[3] * 8 + embedding[4] * 8 + embedding[5] * -11 + embedding[6] * -4 + embedding[7] * 26 + embedding[8] * 25 + embedding[9] * -3;
    let score_15 = embedding[0] * -18 + embedding[1] * -15 + embedding[2] * -14 + embedding[3] * 19 + embedding[4] * 14 + embedding[5] * -41 + embedding[6] * 29 + embedding[7] * -6 + embedding[8] * -40 + embedding[9] * -4;
    let score_16 = embedding[0] * -12 + embedding[1] * 21 + embedding[2] * -16 + embedding[3] * -1 + embedding[4] * -9 + embedding[5] * -17 + embedding[6] * -5 + embedding[7] * -4 + embedding[8] * 7 + embedding[9] * 7;
    let score_17 = embedding[0] * 10 + embedding[1] * 10 + embedding[2] * 3 + embedding[3] * 39 + embedding[4] * -25 + embedding[5] * 8 + embedding[6] * 12 + embedding[7] * -4 + embedding[8] * 13 + embedding[9] * -1;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 17;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 21;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 19;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 6;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 11;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 20;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 16;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 14;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 7;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 15;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 18;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 24;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 10;
    }
    best_palette
}

fn predict_pixel_230(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[1] * -3 + embedding[2] * -4 + embedding[3] * -22 + embedding[4] * 6 + embedding[5] * 1 + embedding[6] * -13 + embedding[7] * -7 + embedding[8] * -6 + embedding[9] * 4;
    let score_1 = embedding[0] * -2 + embedding[1] * -6 + embedding[2] * -13 + embedding[3] * -15 + embedding[4] * 3 + embedding[5] * 2 + embedding[6] * -2 + embedding[7] * -3 + embedding[8] * -19 + embedding[9] * -12;
    let score_2 = embedding[0] * -1 + embedding[1] * -14 + embedding[2] * -11 + embedding[3] * -15 + embedding[4] * 14 + embedding[5] * -3 + embedding[6] * 13 + embedding[7] * -5 + embedding[8] * -1 + embedding[9] * 7;
    let score_3 = embedding[0] * -9 + embedding[1] * -9 + embedding[2] * -11 + embedding[3] * -2 + embedding[4] * 13 + embedding[5] * 6 + embedding[6] * -6 + embedding[7] * -9 + embedding[8] * -18 + embedding[9] * 7;
    let score_4 = embedding[0] * -12 + embedding[1] * -13 + embedding[2] * 13 + embedding[3] * -5 + embedding[4] * 10 + embedding[5] * -14 + embedding[6] * -15 + embedding[7] * -9 + embedding[8] * -6 + embedding[9] * -11;
    let score_5 = embedding[0] * -17 + embedding[1] * -16 + embedding[2] * 5 + embedding[3] * 14 + embedding[4] * 8 + embedding[5] * -3 + embedding[6] * -1 + embedding[7] * 33 + embedding[8] * 15 + embedding[9] * -11;
    let score_6 = embedding[0] * 34 + embedding[1] * 14 + embedding[2] * 20 + embedding[3] * 39 + embedding[4] * -11 + embedding[5] * 17 + embedding[6] * 7 + embedding[7] * 17 + embedding[8] * 9;
    let score_7 = embedding[0] * 5 + embedding[1] * 34 + embedding[2] * -12 + embedding[3] * 6 + embedding[4] * -11 + embedding[5] * -15 + embedding[6] * 1 + embedding[7] * -8 + embedding[8] * 6 + embedding[9] * 11;
    let score_8 = embedding[0] * 18 + embedding[1] * 4 + embedding[2] * 8 + embedding[3] * 12 + embedding[4] * -15 + embedding[5] * 13 + embedding[6] * 5 + embedding[7] * -12 + embedding[9] * 2;
    let score_9 = embedding[0] * -24 + embedding[1] * -17 + embedding[2] * 9 + embedding[3] * -3 + embedding[4] * 7 + embedding[5] * -4 + embedding[6] * 4 + embedding[8] * 4 + embedding[9] * -8;
    let score_10 = embedding[0] * 19 + embedding[1] * 5 + embedding[3] * 4 + embedding[4] * -11 + embedding[5] * 14 + embedding[6] * -14 + embedding[7] * 17 + embedding[8] * 5 + embedding[9] * 5;
    let score_11 = embedding[0] * 11 + embedding[1] * 38 + embedding[2] * 10 + embedding[3] * 6 + embedding[4] * -35 + embedding[5] * -3 + embedding[6] * 6 + embedding[7] * -6 + embedding[8] * 7 + embedding[9] * 18;
    let score_12 = embedding[0] * 27 + embedding[1] * 3 + embedding[2] * 20 + embedding[3] * 49 + embedding[4] * -24 + embedding[5] * 23 + embedding[6] * 3 + embedding[7] * 23 + embedding[8] * 13 + embedding[9] * -5;
    let score_13 = embedding[0] * 12 + embedding[1] * 16 + embedding[2] * 15 + embedding[3] * 41 + embedding[4] * -21 + embedding[5] * -2 + embedding[6] * -1 + embedding[7] * 28 + embedding[8] * 34;
    let score_14 = embedding[0] * -20 + embedding[1] * -18 + embedding[2] * -16 + embedding[3] * 18 + embedding[4] * 14 + embedding[5] * -38 + embedding[6] * 27 + embedding[7] * -5 + embedding[8] * -41 + embedding[9] * -5;
    let score_15 = embedding[0] * -11 + embedding[1] * 24 + embedding[2] * -22 + embedding[3] * 8 + embedding[4] * -14 + embedding[5] * -18 + embedding[7] * -7 + embedding[8] * 8 + embedding[9] * 5;
    let score_16 = embedding[0] * 13 + embedding[1] * 14 + embedding[2] * 1 + embedding[3] * 36 + embedding[4] * -26 + embedding[5] * 8 + embedding[6] * 7 + embedding[8] * 16 + embedding[9] * -1;
    let score_17 = embedding[0] * -39 + embedding[1] * -16 + embedding[2] * 24 + embedding[3] * -1 + embedding[4] * 15 + embedding[5] * -15 + embedding[6] * 2 + embedding[7] * 25 + embedding[8] * 10 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 17;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 24;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 19;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 11;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 20;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 16;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 14;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 15;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 18;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 21;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 10;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 30;
    }
    best_palette
}

fn predict_pixel_231(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 5 + embedding[1] * -2 + embedding[2] * 2 + embedding[3] * -27 + embedding[4] * 4 + embedding[5] * 4 + embedding[6] * -15 + embedding[7] * -4 + embedding[8] * -5 + embedding[9] * 5;
    let score_1 = embedding[0] * 6 + embedding[1] * -3 + embedding[2] * -13 + embedding[3] * -8 + embedding[4] * 3 + embedding[5] * 5 + embedding[7] * -1 + embedding[8] * -18 + embedding[9] * -14;
    let score_2 = embedding[0] * 6 + embedding[1] * -14 + embedding[2] * -10 + embedding[3] * -8 + embedding[4] * 15 + embedding[5] * -1 + embedding[6] * 15 + embedding[7] * -4 + embedding[9] * 6;
    let score_3 = embedding[0] * -1 + embedding[1] * -8 + embedding[2] * -11 + embedding[3] * 6 + embedding[4] * 13 + embedding[5] * 7 + embedding[6] * -4 + embedding[7] * -5 + embedding[8] * -18 + embedding[9] * 4;
    let score_4 = embedding[0] * -6 + embedding[1] * -9 + embedding[2] * 14 + embedding[4] * 10 + embedding[5] * -11 + embedding[6] * -13 + embedding[7] * -8 + embedding[8] * -2 + embedding[9] * -12;
    let score_5 = embedding[0] * -15 + embedding[1] * -14 + embedding[2] * 6 + embedding[3] * 18 + embedding[4] * 8 + embedding[5] * -1 + embedding[6] * 2 + embedding[7] * 30 + embedding[8] * 15 + embedding[9] * -12;
    let score_6 = embedding[0] * 8 + embedding[1] * 40 + embedding[2] * -13 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -16 + embedding[6] * 8 + embedding[7] * -8 + embedding[8] * 12 + embedding[9] * 10;
    let score_7 = embedding[0] * 12 + embedding[1] * -3 + embedding[3] * -3 + embedding[4] * -3 + embedding[5] * 12 + embedding[6] * -2 + embedding[7] * -8 + embedding[8] * -7 + embedding[9] * -3;
    let score_8 = embedding[0] * -18 + embedding[1] * -15 + embedding[2] * 15 + embedding[4] * 4 + embedding[5] * -2 + embedding[6] * 3 + embedding[7] * 3 + embedding[8] * 9 + embedding[9] * -10;
    let score_9 = embedding[0] * 13 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * -2 + embedding[4] * -43 + embedding[5] * 19 + embedding[6] * -12 + embedding[7] * 16 + embedding[8] * 3 + embedding[9] * 14;
    let score_10 = embedding[0] * 11 + embedding[1] * 23 + embedding[2] * 2 + embedding[3] * -3 + embedding[4] * -15 + embedding[6] * 4 + embedding[7] * -9 + embedding[8] * 10 + embedding[9] * 13;
    let score_11 = embedding[0] * 12 + embedding[1] * -6 + embedding[2] * 11 + embedding[3] * 30 + embedding[4] * -16 + embedding[5] * 17 + embedding[6] * 2 + embedding[7] * 10 + embedding[8] * 6 + embedding[9] * -6;
    let score_12 = embedding[0] * -3 + embedding[1] * 13 + embedding[2] * 12 + embedding[3] * 30 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * -1 + embedding[7] * 27 + embedding[8] * 33;
    let score_13 = embedding[0] * -12 + embedding[1] * -15 + embedding[2] * -16 + embedding[3] * 23 + embedding[4] * 14 + embedding[5] * -36 + embedding[6] * 29 + embedding[7] * -4 + embedding[8] * -39 + embedding[9] * -5;
    let score_14 = embedding[0] * -6 + embedding[1] * 24 + embedding[2] * -15 + embedding[3] * 1 + embedding[4] * -9 + embedding[5] * -15 + embedding[6] * -2 + embedding[7] * -2 + embedding[8] * 8 + embedding[9] * 8;
    let score_15 = embedding[0] * 7 + embedding[1] * 13 + embedding[2] * -4 + embedding[3] * 38 + embedding[4] * -23 + embedding[5] * 4 + embedding[6] * 14 + embedding[7] * -4 + embedding[8] * 13 + embedding[9] * -1;
    let score_16 = embedding[0] * 4 + embedding[1] * 13 + embedding[2] * -7 + embedding[3] * 13 + embedding[4] * -8 + embedding[5] * 2 + embedding[6] * 3 + embedding[7] * -15 + embedding[8] * -5 + embedding[9] * 11;
    let score_17 = embedding[0] * -35 + embedding[1] * -14 + embedding[2] * 29 + embedding[3] * -1 + embedding[4] * 13 + embedding[5] * -13 + embedding[7] * 29 + embedding[8] * 14 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 21;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 19;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 11;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 44;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 16;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 17;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 15;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 18;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 24;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 10;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 51;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 30;
    }
    best_palette
}

fn predict_pixel_232(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -4 + embedding[3] * -28 + embedding[4] * 8 + embedding[5] * 14 + embedding[6] * -15 + embedding[7] * -17 + embedding[8] * -5 + embedding[9] * 4;
    let score_1 = embedding[0] * -13 + embedding[1] * -25 + embedding[2] * -16 + embedding[3] * 52 + embedding[4] * 9 + embedding[5] * -3 + embedding[6] * 21 + embedding[7] * 15 + embedding[8] * 4 + embedding[9] * -19;
    let score_2 = embedding[0] * 12 + embedding[1] * 36 + embedding[2] * -7 + embedding[3] * -3 + embedding[4] * -16 + embedding[5] * -9 + embedding[6] * -4 + embedding[7] * -14 + embedding[8] * 8 + embedding[9] * 15;
    let score_3 = embedding[0] * 4 + embedding[1] * -24 + embedding[2] * 43 + embedding[3] * 2 + embedding[4] * 17 + embedding[5] * 20 + embedding[6] * 22 + embedding[7] * 28 + embedding[8] * -36 + embedding[9] * -6;
    let score_4 = embedding[0] * 14 + embedding[2] * 28 + embedding[3] * -17 + embedding[4] * -45 + embedding[5] * 22 + embedding[6] * -14 + embedding[7] * 7 + embedding[8] * -6 + embedding[9] * 20;
    let score_5 = embedding[0] * 14 + embedding[1] * 38 + embedding[2] * 14 + embedding[3] * -9 + embedding[4] * -40 + embedding[6] * 1 + embedding[7] * -13 + embedding[8] * 3 + embedding[9] * 25;
    let score_6 = embedding[0] * -18 + embedding[1] * -3 + embedding[2] * -18 + embedding[3] * 8 + embedding[4] * 3 + embedding[5] * -28 + embedding[6] * -10 + embedding[7] * 44 + embedding[8] * 4 + embedding[9] * 5;
    let score_7 = embedding[0] * 9 + embedding[1] * -29 + embedding[2] * 14 + embedding[3] * 31 + embedding[4] * 16 + embedding[5] * 10 + embedding[6] * 15 + embedding[7] * 10 + embedding[8] * -25 + embedding[9] * -16;
    let score_8 = embedding[0] * -8 + embedding[1] * 17 + embedding[2] * -4 + embedding[3] * 31 + embedding[4] * -17 + embedding[5] * -16 + embedding[6] * -1 + embedding[7] * 20 + embedding[8] * 33 + embedding[9] * 4;
    let score_9 = embedding[0] * 14 + embedding[1] * -22 + embedding[2] * 8 + embedding[3] * -8 + embedding[4] * 17 + embedding[5] * 4 + embedding[6] * -13 + embedding[7] * 40 + embedding[8] * -19 + embedding[9] * -4;
    let score_10 = embedding[0] * -34 + embedding[1] * -10 + embedding[2] * -18 + embedding[3] * 47 + embedding[4] * -2 + embedding[5] * -36 + embedding[6] * 22 + embedding[7] * -1 + embedding[8] * 6 + embedding[9] * -12;
    let score_11 = embedding[0] * -15 + embedding[1] * -15 + embedding[2] * -16 + embedding[3] * 12 + embedding[4] * 6 + embedding[5] * -25 + embedding[6] * 10 + embedding[7] * -10 + embedding[8] * -23 + embedding[9] * -6;
    let score_12 = embedding[0] * -8 + embedding[1] * 28 + embedding[2] * -22 + embedding[3] * 4 + embedding[4] * -15 + embedding[5] * -19 + embedding[6] * -5 + embedding[7] * -9 + embedding[8] * 11 + embedding[9] * 8;
    let score_13 = embedding[0] * -3 + embedding[1] * 11 + embedding[2] * -17 + embedding[3] * 33 + embedding[4] * -24 + embedding[5] * -7 + embedding[6] * 9 + embedding[7] * -5 + embedding[8] * 16 + embedding[9] * -1;
    let score_14 = embedding[0] * -35 + embedding[1] * -16 + embedding[2] * 26 + embedding[3] * 5 + embedding[4] * 18 + embedding[5] * -14 + embedding[6] * 3 + embedding[7] * 19 + embedding[8] * 14 + embedding[9] * -5;
    let score_15 = embedding[0] * -16 + embedding[1] * 30 + embedding[2] * -23 + embedding[3] * 32 + embedding[4] * -36 + embedding[5] * -23 + embedding[6] * 11 + embedding[8] * 19 + embedding[9] * 7;
    let score_16 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 8;
    let score_17 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 6;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 24;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 9;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 20;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 16;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 8;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 13;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 15;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 12;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 11;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 18;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 21;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 10;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 77;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 35;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_233(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * 7 + embedding[1] * 6 + embedding[2] * -2 + embedding[3] * -22 + embedding[4] * 6 + embedding[5] * 9 + embedding[6] * -15 + embedding[7] * -12 + embedding[8] * 7 + embedding[9] * 3;
    let score_2 = embedding[0] * -17 + embedding[1] * -21 + embedding[2] * -14 + embedding[3] * 25 + embedding[4] * 11 + embedding[5] * -7 + embedding[6] * 11 + embedding[7] * 7 + embedding[8] * -10 + embedding[9] * -16;
    let score_3 = embedding[1] * -17 + embedding[2] * 30 + embedding[3] * -5 + embedding[4] * 6 + embedding[5] * 14 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -19 + embedding[9] * -4;
    let score_4 = embedding[0] * 11 + embedding[1] * 32 + embedding[2] * 18 + embedding[3] * -9 + embedding[4] * -34 + embedding[5] * -1 + embedding[6] * -1 + embedding[7] * -14 + embedding[8] * 8 + embedding[9] * 20;
    let score_5 = embedding[0] * -17 + embedding[1] * 3 + embedding[2] * -19 + embedding[3] * 9 + embedding[4] * 7 + embedding[5] * -27 + embedding[6] * -9 + embedding[7] * 36 + embedding[8] * 6;
    let score_6 = embedding[0] * 12 + embedding[1] * -10 + embedding[2] * 4 + embedding[3] * -4 + embedding[4] * 15 + embedding[5] * 1 + embedding[6] * -15 + embedding[7] * 40 + embedding[8] * -5 + embedding[9] * -6;
    let score_7 = embedding[0] * -31 + embedding[1] * -7 + embedding[2] * -17 + embedding[3] * 22 + embedding[4] * 2 + embedding[5] * -32 + embedding[6] * 14 + embedding[7] * -9 + embedding[8] * 1 + embedding[9] * -16;
    let score_8 = embedding[0] * -15 + embedding[1] * -12 + embedding[2] * -18 + embedding[3] * 24 + embedding[4] * 8 + embedding[5] * -28 + embedding[6] * 12 + embedding[7] * -7 + embedding[8] * -16 + embedding[9] * -11;
    let score_9 = embedding[0] * 10 + embedding[1] * 6 + embedding[2] * -4 + embedding[3] * 29 + embedding[4] * -16 + embedding[5] * 4 + embedding[6] * 9 + embedding[7] * -16 + embedding[8] * 12 + embedding[9] * -5;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 8 + embedding[1] * 1 + embedding[2] * -9 + embedding[3] * -13 + embedding[4] * 11 + embedding[5] * 13 + embedding[6] * -9 + embedding[7] * -15 + embedding[8] * 8 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 9;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 16;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 8;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 11;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 18;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 10;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_234(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_1 = embedding[0] * 12 + embedding[1] * 2 + embedding[2] * 4 + embedding[3] * -20 + embedding[4] * 9 + embedding[5] * 18 + embedding[6] * -8 + embedding[7] * -6 + embedding[8] * 3 + embedding[9] * 1;
    let score_2 = embedding[0] * 14 + embedding[1] * 31 + embedding[2] * 21 + embedding[3] * -5 + embedding[4] * -39 + embedding[5] * 2 + embedding[6] * 4 + embedding[7] * -18 + embedding[8] * 6 + embedding[9] * 19;
    let score_3 = embedding[0] * -17 + embedding[1] * -2 + embedding[2] * -18 + embedding[3] * 8 + embedding[4] * 9 + embedding[5] * -25 + embedding[6] * -9 + embedding[7] * 42 + embedding[8] * 5 + embedding[9] * -2;
    let score_4 = embedding[0] * 11 + embedding[1] * -15 + embedding[2] * 4 + embedding[3] * -1 + embedding[4] * 16 + embedding[5] * 2 + embedding[6] * -17 + embedding[7] * 51 + embedding[8] * -5 + embedding[9] * -6;
    let score_5 = embedding[0] * -11 + embedding[1] * -29 + embedding[2] * 1 + embedding[3] * 18 + embedding[4] * 21 + embedding[5] * -21 + embedding[6] * -5 + embedding[7] * 75 + embedding[8] * -11 + embedding[9] * -14;
    let score_6 = embedding[0] * -12 + embedding[1] * -13 + embedding[2] * -14 + embedding[3] * 14 + embedding[4] * 10 + embedding[5] * -34 + embedding[6] * 12 + embedding[7] * -6 + embedding[8] * -25 + embedding[9] * -8;
    let score_7 = embedding[0] * -28 + embedding[1] * -2 + embedding[2] * -15 + embedding[3] * 13 + embedding[4] * 5 + embedding[5] * -32 + embedding[6] * 10 + embedding[7] * -9 + embedding[8] * 5 + embedding[9] * -8;
    let score_8 = embedding[0] * 11 + embedding[1] * 6 + embedding[2] * -2 + embedding[3] * 23 + embedding[4] * -17 + embedding[5] * 9 + embedding[6] * 7 + embedding[7] * -19 + embedding[8] * 12 + embedding[9] * -4;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_17 = embedding[0] * 8 + embedding[1] * -2 + embedding[2] * -8 + embedding[3] * -16 + embedding[4] * 11 + embedding[5] * 17 + embedding[6] * -10 + embedding[7] * -18 + embedding[8] * 7 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 16;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 18;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 11;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 10;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_235(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_1 = embedding[0] * 14 + embedding[1] * 34 + embedding[2] * 21 + embedding[3] * -4 + embedding[4] * -43 + embedding[5] * 2 + embedding[6] * 5 + embedding[7] * -22 + embedding[8] * 9 + embedding[9] * 18;
    let score_2 = embedding[0] * -15 + embedding[1] * -3 + embedding[2] * -16 + embedding[3] * 4 + embedding[4] * 8 + embedding[5] * -20 + embedding[6] * -10 + embedding[7] * 50 + embedding[8] * 4 + embedding[9] * -1;
    let score_3 = embedding[0] * 10 + embedding[1] * -14 + embedding[2] * 2 + embedding[4] * 14 + embedding[5] * -1 + embedding[6] * -16 + embedding[7] * 50 + embedding[8] * -3 + embedding[9] * -7;
    let score_4 = embedding[0] * -15 + embedding[1] * -28 + embedding[2] * 1 + embedding[3] * 16 + embedding[4] * 19 + embedding[5] * -22 + embedding[6] * -6 + embedding[7] * 78 + embedding[8] * -12 + embedding[9] * -13;
    let score_5 = embedding[0] * -6 + embedding[1] * -12 + embedding[2] * -11 + embedding[3] * 10 + embedding[4] * 8 + embedding[5] * -26 + embedding[6] * 10 + embedding[7] * -8 + embedding[8] * -24 + embedding[9] * -6;
    let score_6 = embedding[0] * 12 + embedding[1] * 8 + embedding[2] * -1 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * 10 + embedding[6] * 7 + embedding[7] * -24 + embedding[8] * 15 + embedding[9] * -4;
    let score_7 = embedding[0] * -19 + embedding[1] * -5 + embedding[2] * -11 + embedding[3] * -11 + embedding[4] * -2 + embedding[5] * 6 + embedding[6] * 5 + embedding[7] * 11 + embedding[8] * -5 + embedding[9] * 1;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_17 = embedding[0] * 8 + embedding[1] * 1 + embedding[2] * -8 + embedding[3] * -18 + embedding[4] * 8 + embedding[5] * 17 + embedding[6] * -9 + embedding[7] * -23 + embedding[8] * 11 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 16;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 12;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 18;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 10;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 1;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_236(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_1 = embedding[0] * 9 + embedding[1] * 4 + embedding[2] * -5 + embedding[3] * 23 + embedding[4] * -15 + embedding[5] * 4 + embedding[6] * 4 + embedding[7] * -17 + embedding[8] * 13 + embedding[9] * -6;
    let score_2 = embedding[0] * -19 + embedding[1] * -6 + embedding[2] * -12 + embedding[3] * -14 + embedding[4] * -2 + embedding[5] * 5 + embedding[6] * 2 + embedding[7] * 8 + embedding[8] * -6;
    let score_3 = embedding[0] * 12 + embedding[1] * -10 + embedding[2] * 1 + embedding[3] * -5 + embedding[4] * 15 + embedding[5] * -7 + embedding[6] * -12 + embedding[7] * 22 + embedding[8] * -11 + embedding[9] * -4;
    let score_4 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_5 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_6 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_7 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_8 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_9 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_10 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_11 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_12 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_13 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_14 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_15 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_16 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 10;
    let score_17 = embedding[0] * 5 + embedding[1] * -3 + embedding[2] * -11 + embedding[3] * -16 + embedding[4] * 13 + embedding[5] * 11 + embedding[6] * -14 + embedding[7] * -17 + embedding[8] * 9 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 10;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 12;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_237(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_1 = embedding[0] * -14 + embedding[1] * -6 + embedding[2] * -14 + embedding[3] * -14 + embedding[4] * 3 + embedding[5] * 4 + embedding[6] * 2 + embedding[7] * 4 + embedding[8] * -3 + embedding[9] * -1;
    let score_2 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_3 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_4 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_5 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_6 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_7 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_8 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_9 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_10 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_11 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_12 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_13 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_14 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_15 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_16 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_17 = embedding[0] * 10 + embedding[1] * -3 + embedding[2] * -12 + embedding[3] * -16 + embedding[4] * 20 + embedding[5] * 10 + embedding[6] * -14 + embedding[7] * -21 + embedding[8] * 12 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_238(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_239(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -21 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_240(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_241(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_242(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -20 + embedding[1] * -8 + embedding[2] * -8 + embedding[3] * -19 + embedding[4] * -3 + embedding[5] * 4 + embedding[6] * -6 + embedding[7] * 10 + embedding[8] * -1;
    let score_2 = embedding[0] * 15 + embedding[1] * -8 + embedding[2] * -3 + embedding[3] * -2 + embedding[4] * 16 + embedding[5] * -8 + embedding[6] * -11 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * -5;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 9 + embedding[2] * -16 + embedding[3] * -13 + embedding[4] * 13 + embedding[5] * 11 + embedding[6] * -13 + embedding[7] * -23 + embedding[8] * 12 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 12;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_243(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_1 = embedding[0] * 29 + embedding[1] * 2 + embedding[2] * 13 + embedding[3] * 21 + embedding[4] * -6 + embedding[5] * 18 + embedding[6] * -5 + embedding[7] * 2 + embedding[8] * 14 + embedding[9] * -4;
    let score_2 = embedding[0] * 20 + embedding[1] * 14 + embedding[2] * -7 + embedding[3] * 14 + embedding[4] * 23 + embedding[5] * -36 + embedding[6] * -3 + embedding[7] * 24 + embedding[8] * -5 + embedding[9] * -2;
    let score_3 = embedding[0] * -35 + embedding[1] * -14 + embedding[2] * -10 + embedding[3] * -26 + embedding[4] * -6 + embedding[5] * 5 + embedding[6] * -4 + embedding[7] * 4 + embedding[8] * -4 + embedding[9] * 2;
    let score_4 = embedding[0] * 19 + embedding[1] * -6 + embedding[2] * -1 + embedding[3] * -3 + embedding[4] * 18 + embedding[5] * -7 + embedding[6] * -17 + embedding[7] * 35 + embedding[8] * -2 + embedding[9] * -5;
    let score_5 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_6 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_7 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_8 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_9 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_10 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_11 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_12 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * -6 + embedding[1] * -6 + embedding[2] * -18 + embedding[3] * -21 + embedding[4] * 9 + embedding[5] * 13 + embedding[6] * -10 + embedding[7] * -30 + embedding[8] * 8 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 14;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_244(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_1 = embedding[0] * -30 + embedding[1] * -11 + embedding[3] * -40 + embedding[4] * 8 + embedding[5] * -4 + embedding[6] * -6 + embedding[7] * 9 + embedding[8] * -18 + embedding[9] * 7;
    let score_2 = embedding[0] * 39 + embedding[1] * 14 + embedding[2] * 24 + embedding[3] * 31 + embedding[4] * -2 + embedding[5] * 17 + embedding[6] * -1 + embedding[7] * 10 + embedding[8] * 16 + embedding[9] * -3;
    let score_3 = embedding[0] * 20 + embedding[1] * 25 + embedding[2] * -6 + embedding[3] * 17 + embedding[4] * 22 + embedding[5] * -36 + embedding[6] * -1 + embedding[7] * 22 + embedding[8] * 3;
    let score_4 = embedding[0] * 25 + embedding[1] * -13 + embedding[2] * 15 + embedding[3] * 46 + embedding[4] * 7 + embedding[5] * 2 + embedding[6] * 7 + embedding[7] * 3 + embedding[8] * -10 + embedding[9] * -11;
    let score_5 = embedding[0] * 25 + embedding[1] * 2 + embedding[2] * 11 + embedding[3] * 31 + embedding[4] * -10 + embedding[5] * 23 + embedding[6] * -2 + embedding[7] * 5 + embedding[8] * 15 + embedding[9] * -9;
    let score_6 = embedding[0] * 14 + embedding[1] * -8 + embedding[2] * -2 + embedding[3] * 4 + embedding[4] * 18 + embedding[5] * -6 + embedding[6] * -16 + embedding[7] * 43 + embedding[8] * -1 + embedding[9] * -8;
    let score_7 = embedding[0] * -22 + embedding[1] * -28 + embedding[4] * 13 + embedding[5] * -9 + embedding[6] * -5 + embedding[7] * 43 + embedding[8] * -11 + embedding[9] * -12;
    let score_8 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_9 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_10 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_11 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_12 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_14 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_15 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_16 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_17 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * -23 + embedding[3] * -23 + embedding[4] * 1 + embedding[5] * 12 + embedding[6] * -11 + embedding[7] * -38 + embedding[8] * 15 + embedding[9] * -2;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 75;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 13;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 17;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_245(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_1 = embedding[0] * -12 + embedding[1] * -1 + embedding[2] * 12 + embedding[3] * -36 + embedding[4] * -7 + embedding[5] * 11 + embedding[6] * -6 + embedding[7] * -15 + embedding[8] * -2 + embedding[9] * 9;
    let score_2 = embedding[0] * -2 + embedding[1] * 23 + embedding[2] * -29 + embedding[3] * 2 + embedding[4] * 28 + embedding[5] * -38 + embedding[6] * -13 + embedding[7] * 40 + embedding[8] * 9 + embedding[9] * -1;
    let score_3 = embedding[0] * 9 + embedding[1] * -21 + embedding[2] * 68 + embedding[3] * -3 + embedding[4] * 4 + embedding[5] * 13 + embedding[6] * 6 + embedding[7] * 29 + embedding[8] * -36 + embedding[9] * 1;
    let score_4 = embedding[0] * 26 + embedding[1] * -19 + embedding[2] * 25 + embedding[3] * 32 + embedding[4] * 10 + embedding[5] * 6 + embedding[6] * 12 + embedding[7] * 11 + embedding[8] * -21 + embedding[9] * -12;
    let score_5 = embedding[0] * 16 + embedding[1] * 4 + embedding[3] * 16 + embedding[4] * -11 + embedding[5] * 24 + embedding[6] * -3 + embedding[7] * -2 + embedding[8] * 15 + embedding[9] * -5;
    let score_6 = embedding[0] * 18 + embedding[1] * -12 + embedding[2] * 1 + embedding[3] * 3 + embedding[4] * 22 + embedding[5] * -8 + embedding[6] * -15 + embedding[7] * 54 + embedding[8] * -6 + embedding[9] * -11;
    let score_7 = embedding[0] * -8 + embedding[1] * -22 + embedding[2] * -5 + embedding[3] * 13 + embedding[4] * 23 + embedding[5] * -22 + embedding[6] * -7 + embedding[7] * 67 + embedding[8] * -9 + embedding[9] * -17;
    let score_8 = embedding[0] * -34 + embedding[1] * 11 + embedding[2] * -22 + embedding[3] * 9 + embedding[4] * -5 + embedding[5] * -26 + embedding[6] * 9 + embedding[7] * -19 + embedding[8] * 15 + embedding[9] * -6;
    let score_9 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_10 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_11 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_12 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_14 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_15 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_16 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_17 = embedding[0] * 2 + embedding[1] * 13 + embedding[2] * -21 + embedding[3] * -14 + embedding[4] * 5 + embedding[5] * 13 + embedding[6] * -8 + embedding[7] * -32 + embedding[8] * 15 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 9;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 13;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 75;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 11;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_246(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 20 + embedding[1] * 1 + embedding[2] * 14 + embedding[3] * -35 + embedding[4] * 17 + embedding[5] * 15 + embedding[6] * -11 + embedding[7] * -31 + embedding[8] * -16 + embedding[9] * 5;
    let score_1 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_2 = embedding[0] * -37 + embedding[1] * -12 + embedding[2] * -27 + embedding[3] * 55 + embedding[4] * 2 + embedding[5] * -1 + embedding[6] * 19 + embedding[7] * 23 + embedding[8] * 19 + embedding[9] * -24;
    let score_3 = embedding[0] * 37 + embedding[1] * -14 + embedding[2] * 1 + embedding[3] * -20 + embedding[4] * -27 + embedding[5] * 8 + embedding[6] * 2 + embedding[7] * -7 + embedding[8] * -31 + embedding[9] * 24;
    let score_4 = embedding[0] * -35 + embedding[1] * 14 + embedding[2] * -33 + embedding[3] * 24 + embedding[4] * -1 + embedding[5] * -30 + embedding[6] * -9 + embedding[7] * 54 + embedding[8] * 20 + embedding[9] * -2;
    let score_5 = embedding[0] * 29 + embedding[1] * -26 + embedding[2] * 33 + embedding[3] * 29 + embedding[4] * 22 + embedding[5] * 7 + embedding[6] * 10 + embedding[7] * -3 + embedding[8] * -29 + embedding[9] * -13;
    let score_6 = embedding[0] * -28 + embedding[1] * 17 + embedding[2] * -11 + embedding[3] * 39 + embedding[4] * -14 + embedding[5] * -14 + embedding[6] * -2 + embedding[7] * 39 + embedding[8] * 46 + embedding[9] * -6;
    let score_7 = embedding[0] * 19 + embedding[1] * -22 + embedding[2] * 18 + embedding[3] * -6 + embedding[4] * 26 + embedding[5] * 1 + embedding[6] * -12 + embedding[7] * 38 + embedding[8] * -28 + embedding[9] * -6;
    let score_8 = embedding[0] * -55 + embedding[1] * -1 + embedding[2] * -27 + embedding[3] * 46 + embedding[4] * -11 + embedding[5] * -24 + embedding[6] * 15 + embedding[7] * 4 + embedding[8] * 19 + embedding[9] * -22;
    let score_9 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_10 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_11 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_12 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_13 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_14 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_15 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_16 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_17 = embedding[0] * -23 + embedding[1] * 14 + embedding[2] * -24 + embedding[3] * -2 + embedding[4] * -9 + embedding[5] * 10 + embedding[6] * -9 + embedding[7] * -5 + embedding[8] * 22 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 22;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 8;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 13;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 15;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 12;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 11;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_247(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 5 + embedding[1] * -5 + embedding[2] * -4 + embedding[3] * -15 + embedding[4] * 11 + embedding[5] * 6 + embedding[6] * -11 + embedding[7] * -17 + embedding[8] * -2 + embedding[9] * -5;
    let score_1 = embedding[0] * 10 + embedding[1] * -3 + embedding[2] * -3 + embedding[3] * -10 + embedding[4] * 11 + embedding[5] * 3 + embedding[6] * 16 + embedding[7] * -14 + embedding[8] * -3 + embedding[9] * 7;
    let score_2 = embedding[0] * 11 + embedding[1] * 6 + embedding[2] * -8 + embedding[3] * -9 + embedding[4] * 1 + embedding[5] * 9 + embedding[7] * -11 + embedding[8] * -19 + embedding[9] * -7;
    let score_3 = embedding[0] * 8 + embedding[1] * 3 + embedding[2] * -7 + embedding[3] * 1 + embedding[4] * 7 + embedding[5] * 12 + embedding[6] * -2 + embedding[7] * -14 + embedding[8] * -18 + embedding[9] * 7;
    let score_4 = embedding[0] * -16 + embedding[1] * -19 + embedding[2] * -9 + embedding[3] * 40 + embedding[4] * 13 + embedding[5] * -5 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * 5 + embedding[9] * -19;
    let score_5 = embedding[0] * 2 + embedding[1] * -1 + embedding[2] * 22 + embedding[3] * -3 + embedding[4] * 7 + embedding[5] * -10 + embedding[6] * -15 + embedding[7] * -19 + embedding[8] * -3 + embedding[9] * -9;
    let score_6 = embedding[0] * 23 + embedding[1] * -13 + embedding[2] * -17 + embedding[3] * -1 + embedding[4] * -26 + embedding[5] * 5 + embedding[6] * 8 + embedding[7] * -2 + embedding[8] * -21 + embedding[9] * 16;
    let score_7 = embedding[0] * 1 + embedding[1] * 11 + embedding[2] * 24 + embedding[3] * -10 + embedding[4] * -46 + embedding[5] * 17 + embedding[6] * -12 + embedding[7] * 21 + embedding[8] * 4 + embedding[9] * 18;
    let score_8 = embedding[0] * -16 + embedding[1] * 2 + embedding[2] * -16 + embedding[3] * 12 + embedding[4] * 9 + embedding[5] * -31 + embedding[6] * -8 + embedding[7] * 41 + embedding[8] * 5 + embedding[9] * 1;
    let score_9 = embedding[0] * -11 + embedding[1] * 21 + embedding[2] * 16 + embedding[3] * 14 + embedding[4] * -16 + embedding[5] * -7 + embedding[6] * -9 + embedding[7] * 35 + embedding[8] * 35 + embedding[9] * 7;
    let score_10 = embedding[0] * -37 + embedding[1] * -7 + embedding[2] * -7 + embedding[3] * 34 + embedding[4] * 6 + embedding[5] * -32 + embedding[6] * 17 + embedding[7] * 1 + embedding[8] * 12 + embedding[9] * -13;
    let score_11 = embedding[0] * -17 + embedding[1] * 27 + embedding[2] * 12 + embedding[3] * -14 + embedding[4] * -35 + embedding[6] * -12 + embedding[7] * 41 + embedding[8] * 18 + embedding[9] * 17;
    let score_12 = embedding[0] * -36 + embedding[1] * -8 + embedding[2] * 26 + embedding[3] * 11 + embedding[4] * 21 + embedding[5] * -19 + embedding[6] * 4 + embedding[7] * 19 + embedding[8] * 15 + embedding[9] * -10;
    let score_13 = embedding[0] * -24 + embedding[1] * 12 + embedding[2] * -8 + embedding[3] * 17 + embedding[4] * -25 + embedding[5] * -11 + embedding[6] * 10 + embedding[7] * 7 + embedding[8] * 12 + embedding[9] * -1;
    let score_14 = embedding[0] * 11 + embedding[1] * -6 + embedding[2] * -4 + embedding[3] * 4 + embedding[4] * 3 + embedding[5] * -4 + embedding[6] * 2 + embedding[7] * 1 + embedding[8] * -3 + embedding[9] * 25;
    let score_15 = embedding[0] * 3 + embedding[1] * -5 + embedding[2] * 6 + embedding[3] * 7 + embedding[4] * 3 + embedding[5] * -7 + embedding[6] * -16 + embedding[7] * 76 + embedding[8] * 4 + embedding[9] * -4;
    let score_16 = embedding[0] * -28 + embedding[1] * 23 + embedding[2] * 50 + embedding[3] * -10 + embedding[4] * -11 + embedding[5] * -4 + embedding[6] * 11 + embedding[7] * -1 + embedding[8] * 12 + embedding[9] * -5;
    let score_17 = embedding[0] * 8 + embedding[1] * 9 + embedding[2] * -7 + embedding[3] * 4 + embedding[4] * -14 + embedding[5] * 2 + embedding[6] * -14 + embedding[8] * -13 + embedding[9] * 2;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 20;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 8;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 15;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 11;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 29;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 76;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 35;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 23;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 12;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 52;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 100;
    }
    best_palette
}

fn predict_pixel_248(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 1 + embedding[1] * -3 + embedding[3] * -14 + embedding[4] * 17 + embedding[5] * 4 + embedding[6] * -8 + embedding[7] * -13 + embedding[8] * -3 + embedding[9] * -7;
    let score_1 = embedding[0] * 3 + embedding[1] * -4 + embedding[2] * -9 + embedding[3] * -7 + embedding[4] * 15 + embedding[5] * 4 + embedding[6] * 16 + embedding[7] * -5 + embedding[8] * 3 + embedding[9] * 7;
    let score_2 = embedding[0] * 1 + embedding[1] * 6 + embedding[2] * -15 + embedding[3] * -6 + embedding[4] * 2 + embedding[5] * 11 + embedding[6] * -1 + embedding[7] * 1 + embedding[8] * -17 + embedding[9] * -13;
    let score_3 = embedding[0] * -3 + embedding[1] * 2 + embedding[2] * -13 + embedding[3] * 8 + embedding[4] * 11 + embedding[5] * 15 + embedding[6] * -4 + embedding[7] * -4 + embedding[8] * -15 + embedding[9] * 5;
    let score_4 = embedding[0] * -6 + embedding[1] * -1 + embedding[2] * 19 + embedding[4] * 12 + embedding[5] * -11 + embedding[6] * -16 + embedding[7] * -13 + embedding[8] * 1 + embedding[9] * -12;
    let score_5 = embedding[0] * 13 + embedding[1] * -8 + embedding[2] * 5 + embedding[3] * -9 + embedding[4] * 5 + embedding[5] * -10 + embedding[6] * -12 + embedding[7] * -10 + embedding[8] * -10 + embedding[9] * 1;
    let score_6 = embedding[0] * -14 + embedding[1] * 5 + embedding[2] * -3 + embedding[3] * 24 + embedding[4] * 4 + embedding[5] * -4 + embedding[6] * 7 + embedding[7] * 19 + embedding[8] * 18 + embedding[9] * -9;
    let score_7 = embedding[0] * 25 + embedding[1] * -21 + embedding[2] * -3 + embedding[3] * -2 + embedding[4] * -26 + embedding[5] * -19 + embedding[6] * -13 + embedding[7] * -2 + embedding[8] * -19 + embedding[9] * 15;
    let score_8 = embedding[0] * 27 + embedding[1] * -23 + embedding[2] * -16 + embedding[3] * 2 + embedding[4] * -33 + embedding[5] * -7 + embedding[6] * 1 + embedding[7] * 3 + embedding[8] * -25 + embedding[9] * 21;
    let score_9 = embedding[0] * -1 + embedding[1] * 10 + embedding[2] * 26 + embedding[3] * -7 + embedding[4] * -42 + embedding[5] * 19 + embedding[6] * -9 + embedding[7] * 24 + embedding[8] * 4 + embedding[9] * 16;
    let score_10 = embedding[0] * -13 + embedding[1] * 27 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -14 + embedding[5] * -5 + embedding[6] * -4 + embedding[7] * 40 + embedding[8] * 36 + embedding[9] * 7;
    let score_11 = embedding[0] * -34 + embedding[1] * 4 + embedding[2] * -6 + embedding[3] * 18 + embedding[4] * 3 + embedding[5] * -30 + embedding[6] * 16 + embedding[7] * -8 + embedding[8] * 6 + embedding[9] * -9;
    let score_12 = embedding[0] * -15 + embedding[1] * -8 + embedding[2] * 31 + embedding[3] * -3 + embedding[4] * -3 + embedding[5] * 10 + embedding[6] * 3 + embedding[7] * 11 + embedding[8] * 7 + embedding[9] * -7;
    let score_13 = embedding[0] * -24 + embedding[1] * 29 + embedding[2] * 11 + embedding[3] * -10 + embedding[4] * -28 + embedding[6] * -7 + embedding[7] * 49 + embedding[8] * 20 + embedding[9] * 16;
    let score_14 = embedding[0] * -34 + embedding[2] * 29 + embedding[3] * 1 + embedding[4] * 15 + embedding[5] * -10 + embedding[6] * 4 + embedding[7] * 11 + embedding[8] * 16 + embedding[9] * -6;
    let score_15 = embedding[0] * -30 + embedding[1] * 19 + embedding[2] * -12 + embedding[3] * 24 + embedding[4] * -27 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 12 + embedding[8] * 12 + embedding[9] * 1;
    let score_16 = embedding[0] * 15 + embedding[1] * -13 + embedding[2] * -5 + embedding[3] * 7 + embedding[5] * -13 + embedding[6] * 2 + embedding[7] * 6 + embedding[8] * -5 + embedding[9] * 29;
    let score_17 = embedding[0] * -33 + embedding[1] * 22 + embedding[2] * 51 + embedding[3] * -7 + embedding[4] * -4 + embedding[5] * -3 + embedding[6] * 13 + embedding[7] * 3 + embedding[8] * 14 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 41;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 6;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 61;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 22;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 20;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 15;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 11;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 40;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 29;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 76;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 35;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 23;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 52;
    }
    best_palette
}

fn predict_pixel_249(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[1] * -2 + embedding[2] * 3 + embedding[3] * -10 + embedding[4] * 14 + embedding[6] * -8 + embedding[7] * -14 + embedding[8] * -1 + embedding[9] * -7;
    let score_1 = embedding[0] * 1 + embedding[1] * -6 + embedding[2] * -12 + embedding[3] * -9 + embedding[4] * 12 + embedding[5] * 4 + embedding[6] * 22 + embedding[7] * -4 + embedding[8] * 9 + embedding[9] * 10;
    let score_2 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * -20 + embedding[3] * -7 + embedding[4] * -3 + embedding[5] * 12 + embedding[6] * -3 + embedding[7] * 2 + embedding[8] * -17 + embedding[9] * -15;
    let score_3 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * -17 + embedding[3] * 8 + embedding[4] * 8 + embedding[5] * 16 + embedding[6] * -5 + embedding[7] * -4 + embedding[8] * -15 + embedding[9] * 7;
    let score_4 = embedding[0] * -5 + embedding[2] * 23 + embedding[3] * 2 + embedding[4] * 7 + embedding[5] * -14 + embedding[6] * -19 + embedding[7] * -14 + embedding[8] * 2 + embedding[9] * -14;
    let score_5 = embedding[0] * 16 + embedding[1] * -7 + embedding[2] * 7 + embedding[3] * -7 + embedding[4] * 2 + embedding[5] * -13 + embedding[6] * -13 + embedding[7] * -11 + embedding[8] * -9;
    let score_6 = embedding[0] * 28 + embedding[1] * -21 + embedding[2] * -4 + embedding[4] * -28 + embedding[5] * -22 + embedding[6] * -13 + embedding[7] * -2 + embedding[8] * -18 + embedding[9] * 15;
    let score_7 = embedding[0] * 28 + embedding[1] * -23 + embedding[2] * -19 + embedding[3] * 3 + embedding[4] * -37 + embedding[5] * -9 + embedding[6] * 3 + embedding[7] * 3 + embedding[8] * -25 + embedding[9] * 22;
    let score_8 = embedding[0] * -18 + embedding[1] * 17 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -13 + embedding[5] * -6 + embedding[6] * 3 + embedding[7] * 44 + embedding[8] * 38 + embedding[9] * 8;
    let score_9 = embedding[0] * -27 + embedding[1] * -5 + embedding[2] * 11 + embedding[3] * 22 + embedding[4] * -16 + embedding[5] * 12 + embedding[6] * 15 + embedding[7] * 35 + embedding[8] * 22 + embedding[9] * -2;
    let score_10 = embedding[0] * 3 + embedding[1] * 1 + embedding[2] * -10 + embedding[3] * -7 + embedding[4] * 11 + embedding[5] * 6 + embedding[6] * 9 + embedding[7] * -6 + embedding[8] * -5 + embedding[9] * -1;
    let score_11 = embedding[0] * 2 + embedding[1] * 2 + embedding[2] * -12 + embedding[3] * -6 + embedding[4] * 10 + embedding[5] * 8 + embedding[6] * 9 + embedding[7] * -6 + embedding[8] * -6 + embedding[9] * -1;
    let score_12 = embedding[0] * -15 + embedding[1] * -10 + embedding[2] * 32 + embedding[3] * 1 + embedding[4] * -5 + embedding[5] * 8 + embedding[6] * 7 + embedding[7] * 9 + embedding[8] * 8 + embedding[9] * -10;
    let score_13 = embedding[0] * -24 + embedding[1] * 29 + embedding[2] * 4 + embedding[3] * -12 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * -6 + embedding[7] * 40 + embedding[8] * 16 + embedding[9] * 15;
    let score_14 = embedding[0] * -35 + embedding[2] * 33 + embedding[3] * 3 + embedding[4] * 10 + embedding[5] * -9 + embedding[6] * 3 + embedding[7] * 23 + embedding[8] * 20 + embedding[9] * -4;
    let score_15 = embedding[0] * 14 + embedding[1] * -9 + embedding[2] * -2 + embedding[3] * 5 + embedding[4] * -3 + embedding[5] * -16 + embedding[7] * 7 + embedding[8] * -3 + embedding[9] * 30;
    let score_16 = embedding[0] * -32 + embedding[1] * 24 + embedding[2] * 50 + embedding[3] * -7 + embedding[4] * -5 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 1 + embedding[8] * 12 + embedding[9] * -7;
    let score_17 = embedding[0] * 9 + embedding[1] * 5 + embedding[2] * -12 + embedding[3] * 8 + embedding[4] * -17 + embedding[5] * -2 + embedding[6] * -13 + embedding[7] * 5 + embedding[8] * -22 + embedding[9] * 3;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 41;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 28;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 22;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 15;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 6;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 12;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 69;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 40;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 29;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 77;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 23;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 52;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 80;
    }
    best_palette
}

fn predict_pixel_250(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 3 + embedding[1] * 2 + embedding[2] * 1 + embedding[3] * -9 + embedding[4] * 16 + embedding[5] * 3 + embedding[6] * -8 + embedding[7] * -14 + embedding[8] * 4 + embedding[9] * -7;
    let score_1 = embedding[0] * 2 + embedding[1] * -4 + embedding[2] * -9 + embedding[3] * -11 + embedding[4] * 12 + embedding[5] * 8 + embedding[6] * 20 + embedding[7] * -3 + embedding[8] * 14 + embedding[9] * 10;
    let score_2 = embedding[0] * -1 + embedding[1] * 10 + embedding[2] * -17 + embedding[3] * -9 + embedding[4] * -3 + embedding[5] * 16 + embedding[6] * -3 + embedding[7] * 1 + embedding[8] * -11 + embedding[9] * -13;
    let score_3 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * -13 + embedding[3] * 6 + embedding[4] * 8 + embedding[5] * 21 + embedding[6] * -5 + embedding[7] * -4 + embedding[8] * -7 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 22 + embedding[4] * 9 + embedding[5] * -10 + embedding[6] * -18 + embedding[7] * -14 + embedding[8] * 8 + embedding[9] * -13;
    let score_5 = embedding[0] * 15 + embedding[1] * -4 + embedding[2] * 6 + embedding[3] * -6 + embedding[4] * 3 + embedding[5] * -10 + embedding[6] * -13 + embedding[7] * -11 + embedding[8] * -3 + embedding[9] * 1;
    let score_6 = embedding[0] * 27 + embedding[1] * -19 + embedding[2] * -5 + embedding[4] * -27 + embedding[5] * -18 + embedding[6] * -13 + embedding[7] * -2 + embedding[8] * -13 + embedding[9] * 16;
    let score_7 = embedding[0] * 28 + embedding[1] * -21 + embedding[2] * -19 + embedding[3] * 3 + embedding[4] * -38 + embedding[5] * -5 + embedding[6] * 3 + embedding[7] * 6 + embedding[8] * -20 + embedding[9] * 25;
    let score_8 = embedding[0] * -19 + embedding[1] * -21 + embedding[2] * 4 + embedding[3] * 13 + embedding[4] * 20 + embedding[5] * -10 + embedding[6] * 6 + embedding[7] * 43 + embedding[8] * -11 + embedding[9] * -12;
    let score_9 = embedding[0] * -39 + embedding[1] * 1 + embedding[2] * -1 + embedding[3] * 11 + embedding[4] * 6 + embedding[5] * -25 + embedding[6] * 17 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -8;
    let score_10 = embedding[0] * -13 + embedding[1] * -5 + embedding[2] * 30 + embedding[3] * -5 + embedding[4] * -3 + embedding[5] * 11 + embedding[6] * 7 + embedding[7] * 6 + embedding[8] * 9 + embedding[9] * -6;
    let score_11 = embedding[0] * -22 + embedding[1] * 28 + embedding[2] * 2 + embedding[3] * -17 + embedding[4] * -19 + embedding[5] * -3 + embedding[6] * -4 + embedding[7] * 23 + embedding[8] * 8 + embedding[9] * 17;
    let score_12 = embedding[0] * -36 + embedding[1] * -2 + embedding[2] * 23 + embedding[3] * 1 + embedding[4] * 19 + embedding[5] * -12 + embedding[6] * 8 + embedding[7] * 9 + embedding[8] * 12 + embedding[9] * -8;
    let score_13 = embedding[0] * 13 + embedding[1] * -8 + embedding[2] * -4 + embedding[3] * 6 + embedding[4] * -2 + embedding[5] * -11 + embedding[7] * 8 + embedding[8] * 3 + embedding[9] * 33;
    let score_14 = embedding[0] * -28 + embedding[1] * 20 + embedding[2] * 42 + embedding[3] * -8 + embedding[4] * -6 + embedding[5] * -6 + embedding[6] * 10 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -9;
    let score_15 = embedding[0] * 10 + embedding[1] * 7 + embedding[2] * -11 + embedding[3] * 6 + embedding[4] * -17 + embedding[5] * 1 + embedding[6] * -14 + embedding[7] * 5 + embedding[8] * -15 + embedding[9] * 5;
    let score_16 = embedding[0] * 17 + embedding[1] * -6 + embedding[2] * -1 + embedding[3] * 1 + embedding[4] * -24 + embedding[5] * -13 + embedding[6] * -1 + embedding[7] * -12 + embedding[8] * -5 + embedding[9] * 6;
    let score_17 = embedding[0] * -6 + embedding[1] * 6 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 41;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 28;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 22;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 6;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 11;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 40;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 29;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 77;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 23;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 52;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 80;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 93;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_251(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[1] * -2 + embedding[2] * 9 + embedding[3] * -14 + embedding[4] * 15 + embedding[5] * -4 + embedding[6] * -11 + embedding[7] * -13 + embedding[8] * 4 + embedding[9] * -5;
    let score_1 = embedding[0] * 3 + embedding[1] * -6 + embedding[2] * -7 + embedding[3] * -8 + embedding[4] * 12 + embedding[5] * 3 + embedding[6] * 21 + embedding[7] * -3 + embedding[8] * 15 + embedding[9] * 9;
    let score_2 = embedding[0] * -1 + embedding[1] * 9 + embedding[2] * -18 + embedding[3] * -5 + embedding[4] * -4 + embedding[5] * 14 + embedding[6] * -2 + embedding[7] * 3 + embedding[8] * -10 + embedding[9] * -16;
    let score_3 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * -14 + embedding[3] * 13 + embedding[4] * 8 + embedding[5] * 19 + embedding[6] * -5 + embedding[7] * -3 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 29 + embedding[3] * -1 + embedding[4] * 9 + embedding[5] * -16 + embedding[6] * -21 + embedding[7] * -14 + embedding[8] * 8 + embedding[9] * -11;
    let score_5 = embedding[0] * 22 + embedding[1] * -18 + embedding[2] * -1 + embedding[3] * -3 + embedding[4] * -23 + embedding[5] * -19 + embedding[6] * -12 + embedding[7] * -3 + embedding[8] * -10 + embedding[9] * 13;
    let score_6 = embedding[0] * 26 + embedding[1] * -21 + embedding[2] * -18 + embedding[3] * 5 + embedding[4] * -36 + embedding[5] * -5 + embedding[6] * 5 + embedding[7] * 7 + embedding[8] * -19 + embedding[9] * 24;
    let score_7 = embedding[0] * -10 + embedding[1] * -7 + embedding[2] * -13 + embedding[3] * 43 + embedding[4] * -3 + embedding[5] * 10 + embedding[6] * 22 + embedding[7] * 2 + embedding[8] * -9 + embedding[9] * -12;
    let score_8 = embedding[0] * -12 + embedding[1] * -8 + embedding[2] * 34 + embedding[3] * -3 + embedding[4] * -3 + embedding[5] * 6 + embedding[6] * 5 + embedding[7] * 8 + embedding[8] * 10 + embedding[9] * -6;
    let score_9 = embedding[0] * -21 + embedding[1] * 26 + embedding[2] * 4 + embedding[3] * -16 + embedding[4] * -19 + embedding[5] * -6 + embedding[6] * -5 + embedding[7] * 23 + embedding[8] * 9 + embedding[9] * 16;
    let score_10 = embedding[0] * -26 + embedding[1] * -3 + embedding[2] * 31 + embedding[3] * -2 + embedding[4] * 17 + embedding[5] * -12 + embedding[7] * 10 + embedding[8] * 14 + embedding[9] * -5;
    let score_11 = embedding[0] * 11 + embedding[1] * -9 + embedding[2] * -1 + embedding[3] * 7 + embedding[4] * 1 + embedding[5] * -14 + embedding[6] * 3 + embedding[7] * 7 + embedding[8] * 4 + embedding[9] * 31;
    let score_12 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * -13 + embedding[3] * 1 + embedding[4] * -2 + embedding[5] * 16 + embedding[6] * 4 + embedding[8] * -4 + embedding[9] * -3;
    let score_13 = embedding[0] * -26 + embedding[1] * 19 + embedding[2] * 48 + embedding[3] * -9 + embedding[4] * -4 + embedding[5] * -10 + embedding[6] * 8 + embedding[7] * -6 + embedding[8] * 11 + embedding[9] * -7;
    let score_14 = embedding[0] * 7 + embedding[1] * 6 + embedding[2] * -11 + embedding[3] * 10 + embedding[4] * -16 + embedding[5] * 1 + embedding[6] * -13 + embedding[7] * 6 + embedding[8] * -15 + embedding[9] * 3;
    let score_15 = embedding[0] * -19 + embedding[1] * 15 + embedding[2] * -8 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * 10 + embedding[6] * -10 + embedding[7] * 17 + embedding[8] * -16 + embedding[9] * -14;
    let score_16 = embedding[0] * -4 + embedding[2] * -8 + embedding[3] * -1 + embedding[4] * 4 + embedding[5] * -2 + embedding[6] * 16 + embedding[7] * 12 + embedding[8] * 5 + embedding[9] * -4;
    let score_17 = embedding[0] * 14 + embedding[1] * -8 + embedding[2] * 1 + embedding[3] * 1 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * -2 + embedding[7] * -11 + embedding[8] * -2 + embedding[9] * 5;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 28;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 40;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 29;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 76;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 108;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 52;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 80;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 162;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 164;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 93;
    }
    best_palette
}

fn predict_pixel_252(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[1] * 1 + embedding[2] * 1 + embedding[3] * -12 + embedding[4] * 15 + embedding[5] * 1 + embedding[6] * -11 + embedding[7] * -10 + embedding[8] * 3 + embedding[9] * -6;
    let score_1 = embedding[0] * 1 + embedding[1] * 6 + embedding[2] * -12 + embedding[3] * -6 + embedding[4] * 3 + embedding[5] * 11 + embedding[6] * -4 + embedding[7] * -1 + embedding[8] * -6 + embedding[9] * -12;
    let score_2 = embedding[0] * 1 + embedding[1] * -4 + embedding[2] * -8 + embedding[3] * -8 + embedding[4] * 13 + embedding[5] * 4 + embedding[6] * 12 + embedding[7] * -4 + embedding[8] * 10 + embedding[9] * 4;
    let score_3 = embedding[0] * -4 + embedding[1] * 3 + embedding[2] * -11 + embedding[3] * 5 + embedding[4] * 11 + embedding[5] * 14 + embedding[6] * -6 + embedding[7] * -4 + embedding[8] * -4 + embedding[9] * 4;
    let score_4 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 17 + embedding[3] * -2 + embedding[4] * 10 + embedding[5] * -10 + embedding[6] * -17 + embedding[7] * -12 + embedding[8] * 6 + embedding[9] * -12;
    let score_5 = embedding[0] * 18 + embedding[1] * -15 + embedding[2] * -6 + embedding[3] * -2 + embedding[4] * -22 + embedding[5] * -14 + embedding[6] * -11 + embedding[8] * -10 + embedding[9] * 12;
    let score_6 = embedding[0] * 22 + embedding[1] * -18 + embedding[2] * -17 + embedding[3] * 4 + embedding[4] * -32 + embedding[5] * -4 + embedding[6] * 3 + embedding[7] * 8 + embedding[8] * -17 + embedding[9] * 20;
    let score_7 = embedding[0] * 12 + embedding[2] * 9 + embedding[3] * 26 + embedding[4] * -9 + embedding[5] * 19 + embedding[6] * 2 + embedding[7] * 13 + embedding[8] * 4 + embedding[9] * -6;
    let score_8 = embedding[0] * -30 + embedding[1] * 2 + embedding[2] * -3 + embedding[3] * 8 + embedding[4] * 3 + embedding[5] * -23 + embedding[6] * 10 + embedding[7] * -8 + embedding[8] * 6 + embedding[9] * -8;
    let score_9 = embedding[0] * -13 + embedding[1] * -5 + embedding[2] * 27 + embedding[3] * -4 + embedding[4] * -2 + embedding[5] * 9 + embedding[6] * 2 + embedding[7] * 9 + embedding[8] * 8 + embedding[9] * -5;
    let score_10 = embedding[0] * -19 + embedding[1] * 23 + embedding[2] * 5 + embedding[3] * -16 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * -9 + embedding[7] * 21 + embedding[8] * 9 + embedding[9] * 13;
    let score_11 = embedding[0] * 9 + embedding[1] * -8 + embedding[2] * -4 + embedding[3] * 6 + embedding[4] * 3 + embedding[5] * -9 + embedding[7] * 7 + embedding[8] * 3 + embedding[9] * 24;
    let score_12 = embedding[0] * -6 + embedding[1] * 4 + embedding[2] * -8 + embedding[3] * -2 + embedding[4] * 2 + embedding[5] * 10 + embedding[6] * 1 + embedding[7] * -3 + embedding[8] * -1 + embedding[9] * -4;
    let score_13 = embedding[0] * -25 + embedding[1] * 18 + embedding[2] * 37 + embedding[3] * -8 + embedding[4] * -5 + embedding[5] * -7 + embedding[6] * 7 + embedding[7] * -6 + embedding[8] * 8 + embedding[9] * -8;
    let score_14 = embedding[0] * 6 + embedding[1] * 9 + embedding[2] * -7 + embedding[3] * 9 + embedding[4] * -13 + embedding[5] * -1 + embedding[6] * -14 + embedding[7] * 6 + embedding[8] * -9 + embedding[9] * 2;
    let score_15 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -2 + embedding[3] * 1 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * -1 + embedding[7] * -8 + embedding[8] * -3 + embedding[9] * 3;
    let score_16 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_17 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 28;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 17;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 11;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 40;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 29;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 172;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 52;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 80;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_253(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 5 + embedding[1] * -2 + embedding[2] * 9 + embedding[3] * -14 + embedding[4] * 15 + embedding[5] * -4 + embedding[6] * -11 + embedding[7] * -15 + embedding[8] * 2 + embedding[9] * -5;
    let score_1 = embedding[0] * 1 + embedding[1] * -8 + embedding[2] * -7 + embedding[3] * -10 + embedding[4] * 13 + embedding[5] * 3 + embedding[6] * 24 + embedding[7] * -2 + embedding[8] * 14 + embedding[9] * 11;
    let score_2 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * -19 + embedding[3] * -7 + embedding[4] * -4 + embedding[5] * 14 + embedding[6] * -2 + embedding[7] * 4 + embedding[8] * -12 + embedding[9] * -17;
    let score_3 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * -14 + embedding[3] * 11 + embedding[4] * 9 + embedding[5] * 18 + embedding[6] * -6 + embedding[7] * -3 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -6 + embedding[1] * -1 + embedding[2] * 29 + embedding[3] * -1 + embedding[4] * 10 + embedding[5] * -16 + embedding[6] * -21 + embedding[7] * -15 + embedding[8] * 7 + embedding[9] * -12;
    let score_5 = embedding[0] * 23 + embedding[1] * -19 + embedding[2] * -2 + embedding[3] * -4 + embedding[4] * -23 + embedding[5] * -18 + embedding[6] * -12 + embedding[7] * -4 + embedding[8] * -11 + embedding[9] * 14;
    let score_6 = embedding[0] * 26 + embedding[1] * -23 + embedding[2] * -18 + embedding[3] * 3 + embedding[4] * -37 + embedding[5] * -6 + embedding[6] * 5 + embedding[7] * 7 + embedding[8] * -20 + embedding[9] * 25;
    let score_7 = embedding[0] * 13 + embedding[1] * 1 + embedding[2] * 10 + embedding[3] * 31 + embedding[4] * -12 + embedding[5] * 22 + embedding[6] * 4 + embedding[7] * 15 + embedding[8] * 3 + embedding[9] * -7;
    let score_8 = embedding[0] * 1 + embedding[1] * 8 + embedding[2] * -12 + embedding[3] * 35 + embedding[4] * 9 + embedding[5] * -9 + embedding[6] * 10 + embedding[7] * 12 + embedding[8] * 8 + embedding[9] * -13;
    let score_9 = embedding[0] * -14 + embedding[1] * -9 + embedding[2] * 36 + embedding[3] * -3 + embedding[4] * -4 + embedding[5] * 8 + embedding[6] * 5 + embedding[7] * 8 + embedding[8] * 9 + embedding[9] * -6;
    let score_10 = embedding[0] * -24 + embedding[1] * 26 + embedding[2] * 1 + embedding[3] * -16 + embedding[4] * -21 + embedding[5] * -6 + embedding[6] * -5 + embedding[7] * 24 + embedding[8] * 8 + embedding[9] * 16;
    let score_11 = embedding[0] * -28 + embedding[1] * -4 + embedding[2] * 31 + embedding[3] * -2 + embedding[4] * 18 + embedding[5] * -12 + embedding[7] * 10 + embedding[8] * 14 + embedding[9] * -5;
    let score_12 = embedding[0] * -19 + embedding[1] * 24 + embedding[2] * -19 + embedding[3] * 18 + embedding[4] * -29 + embedding[5] * 1 + embedding[6] * 17 + embedding[7] * 5 + embedding[8] * 3 + embedding[9] * 5;
    let score_13 = embedding[0] * 10 + embedding[1] * -10 + embedding[2] * -2 + embedding[3] * 6 + embedding[4] * 1 + embedding[5] * -13 + embedding[6] * 3 + embedding[7] * 7 + embedding[8] * 3 + embedding[9] * 32;
    let score_14 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * -15 + embedding[3] * -1 + embedding[4] * -4 + embedding[5] * 15 + embedding[6] * 4 + embedding[8] * -3 + embedding[9] * -3;
    let score_15 = embedding[0] * -28 + embedding[1] * 19 + embedding[2] * 49 + embedding[3] * -9 + embedding[4] * -5 + embedding[5] * -11 + embedding[6] * 8 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -8;
    let score_16 = embedding[0] * 7 + embedding[1] * 6 + embedding[2] * -11 + embedding[3] * 10 + embedding[4] * -17 + embedding[6] * -14 + embedding[7] * 7 + embedding[8] * -15 + embedding[9] * 4;
    let score_17 = embedding[0] * 14 + embedding[1] * -8 + embedding[2] * 2 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * -3 + embedding[7] * -11 + embedding[8] * -4 + embedding[9] * 4;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 28;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 17;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 6;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 40;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 29;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 76;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 35;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 23;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 108;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 52;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 80;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 93;
    }
    best_palette
}

fn predict_pixel_254(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 1 + embedding[1] * 2 + embedding[2] * 2 + embedding[3] * -9 + embedding[4] * 13 + embedding[6] * -7 + embedding[7] * -12 + embedding[8] * 4 + embedding[9] * -6;
    let score_1 = embedding[1] * -4 + embedding[2] * -9 + embedding[3] * -9 + embedding[4] * 11 + embedding[5] * 4 + embedding[6] * 20 + embedding[7] * -4 + embedding[8] * 13 + embedding[9] * 10;
    let score_2 = embedding[0] * -4 + embedding[1] * 9 + embedding[2] * -17 + embedding[3] * -7 + embedding[4] * -5 + embedding[5] * 12 + embedding[6] * -2 + embedding[7] * 1 + embedding[8] * -11 + embedding[9] * -13;
    let score_3 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * -13 + embedding[3] * 8 + embedding[4] * 6 + embedding[5] * 16 + embedding[6] * -4 + embedding[7] * -4 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 2 + embedding[4] * 7 + embedding[5] * -14 + embedding[6] * -17 + embedding[7] * -13 + embedding[8] * 7 + embedding[9] * -12;
    let score_5 = embedding[0] * 13 + embedding[1] * -4 + embedding[2] * 7 + embedding[3] * -6 + embedding[4] * 1 + embedding[5] * -13 + embedding[6] * -12 + embedding[7] * -9 + embedding[8] * -4 + embedding[9] * 2;
    let score_6 = embedding[0] * 26 + embedding[1] * -19 + embedding[2] * -3 + embedding[3] * 1 + embedding[4] * -29 + embedding[5] * -22 + embedding[6] * -13 + embedding[7] * -1 + embedding[8] * -13 + embedding[9] * 16;
    let score_7 = embedding[0] * 26 + embedding[1] * -20 + embedding[2] * -18 + embedding[3] * 4 + embedding[4] * -38 + embedding[5] * -8 + embedding[6] * 3 + embedding[7] * 4 + embedding[8] * -19 + embedding[9] * 23;
    let score_8 = embedding[0] * 12 + embedding[1] * 2 + embedding[2] * 12 + embedding[3] * 29 + embedding[4] * -12 + embedding[5] * 20 + embedding[6] * 4 + embedding[7] * 14 + embedding[8] * 4 + embedding[9] * -5;
    let score_9 = embedding[0] * -20 + embedding[1] * -21 + embedding[2] * 5 + embedding[3] * 15 + embedding[4] * 17 + embedding[5] * -11 + embedding[6] * 7 + embedding[7] * 43 + embedding[8] * -10 + embedding[9] * -11;
    let score_10 = embedding[0] * 1 + embedding[1] * 3 + embedding[2] * -9 + embedding[3] * -6 + embedding[4] * 10 + embedding[5] * 6 + embedding[6] * 9 + embedding[7] * -5 + embedding[8] * 1 + embedding[9] * 1;
    let score_11 = embedding[1] * 4 + embedding[2] * -10 + embedding[3] * -5 + embedding[4] * 9 + embedding[5] * 8 + embedding[6] * 9 + embedding[7] * -5 + embedding[8] * -1;
    let score_12 = embedding[0] * -14 + embedding[1] * -5 + embedding[2] * 30 + embedding[3] * -3 + embedding[4] * -5 + embedding[5] * 7 + embedding[6] * 7 + embedding[7] * 8 + embedding[8] * 8 + embedding[9] * -5;
    let score_13 = embedding[0] * -22 + embedding[1] * 27 + embedding[2] * 3 + embedding[3] * -16 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * -4 + embedding[7] * 23 + embedding[8] * 9 + embedding[9] * 17;
    let score_14 = embedding[0] * -28 + embedding[1] * -5 + embedding[2] * 28 + embedding[3] * 1 + embedding[4] * 18 + embedding[5] * -12 + embedding[6] * 2 + embedding[7] * 16 + embedding[8] * 12 + embedding[9] * -6;
    let score_15 = embedding[0] * 12 + embedding[1] * -7 + embedding[2] * -1 + embedding[3] * 5 + embedding[4] * -4 + embedding[5] * -16 + embedding[7] * 8 + embedding[8] * 2 + embedding[9] * 31;
    let score_16 = embedding[0] * -27 + embedding[1] * 20 + embedding[2] * 43 + embedding[3] * -8 + embedding[4] * -7 + embedding[5] * -8 + embedding[6] * 10 + embedding[7] * -5 + embedding[8] * 10 + embedding[9] * -7;
    let score_17 = embedding[0] * 9 + embedding[1] * 7 + embedding[2] * -10 + embedding[3] * 9 + embedding[4] * -19 + embedding[5] * -3 + embedding[6] * -13 + embedding[7] * 6 + embedding[8] * -15 + embedding[9] * 4;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 41;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 28;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 22;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 17;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 6;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 12;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 69;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 40;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 29;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 77;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 23;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 52;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 80;
    }
    best_palette
}

fn predict_pixel_255(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 5 + embedding[1] * -4 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * 19 + embedding[6] * -9 + embedding[7] * -14 + embedding[8] * -3 + embedding[9] * -8;
    let score_1 = embedding[0] * 4 + embedding[1] * -6 + embedding[2] * -14 + embedding[3] * -10 + embedding[4] * 16 + embedding[5] * 4 + embedding[6] * 19 + embedding[7] * -6 + embedding[8] * 4 + embedding[9] * 8;
    let score_2 = embedding[1] * 7 + embedding[2] * -21 + embedding[3] * -8 + embedding[4] * 1 + embedding[5] * 14 + embedding[6] * -1 + embedding[7] * 1 + embedding[8] * -20 + embedding[9] * -16;
    let score_3 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * -19 + embedding[3] * 8 + embedding[4] * 13 + embedding[5] * 19 + embedding[6] * -3 + embedding[7] * -4 + embedding[8] * -18 + embedding[9] * 8;
    let score_4 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 23 + embedding[4] * 14 + embedding[5] * -15 + embedding[6] * -20 + embedding[7] * -15 + embedding[8] * 1 + embedding[9] * -15;
    let score_5 = embedding[0] * 16 + embedding[1] * -9 + embedding[2] * 7 + embedding[3] * -9 + embedding[4] * 7 + embedding[5] * -14 + embedding[6] * -14 + embedding[7] * -12 + embedding[8] * -11;
    let score_6 = embedding[0] * 29 + embedding[1] * -22 + embedding[2] * -4 + embedding[3] * -3 + embedding[4] * -25 + embedding[5] * -22 + embedding[6] * -14 + embedding[7] * -3 + embedding[8] * -20 + embedding[9] * 15;
    let score_7 = embedding[0] * 30 + embedding[1] * -24 + embedding[2] * -20 + embedding[3] * 1 + embedding[4] * -34 + embedding[5] * -7 + embedding[6] * 3 + embedding[7] * 2 + embedding[8] * -26 + embedding[9] * 23;
    let score_8 = embedding[1] * 10 + embedding[2] * 24 + embedding[3] * -7 + embedding[4] * -46 + embedding[5] * 21 + embedding[6] * -9 + embedding[7] * 25 + embedding[8] * 4 + embedding[9] * 18;
    let score_9 = embedding[0] * -20 + embedding[1] * 25 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -1 + embedding[6] * 1 + embedding[7] * 43 + embedding[8] * 40 + embedding[9] * 8;
    let score_10 = embedding[0] * -31 + embedding[1] * -3 + embedding[2] * 11 + embedding[3] * 21 + embedding[4] * -16 + embedding[5] * 12 + embedding[6] * 13 + embedding[7] * 34 + embedding[8] * 23 + embedding[9] * -1;
    let score_11 = embedding[0] * -39 + embedding[1] * 3 + embedding[2] * -7 + embedding[3] * 17 + embedding[4] * 4 + embedding[5] * -29 + embedding[6] * 19 + embedding[7] * -9 + embedding[8] * 5 + embedding[9] * -9;
    let score_12 = embedding[0] * -13 + embedding[1] * -11 + embedding[2] * 32 + embedding[3] * -1 + embedding[4] * -1 + embedding[5] * 8 + embedding[6] * 4 + embedding[7] * 8 + embedding[8] * 6 + embedding[9] * -10;
    let score_13 = embedding[0] * -27 + embedding[1] * 34 + embedding[2] * 9 + embedding[3] * -9 + embedding[4] * -32 + embedding[6] * -4 + embedding[7] * 51 + embedding[8] * 21 + embedding[9] * 18;
    let score_14 = embedding[0] * -43 + embedding[1] * 1 + embedding[2] * 31 + embedding[3] * 4 + embedding[4] * 13 + embedding[5] * -12 + embedding[6] * 7 + embedding[7] * 13 + embedding[8] * 19 + embedding[9] * -6;
    let score_15 = embedding[0] * 15 + embedding[1] * -12 + embedding[2] * -5 + embedding[3] * 5 + embedding[4] * 2 + embedding[5] * -15 + embedding[7] * 6 + embedding[8] * -5 + embedding[9] * 32;
    let score_16 = embedding[0] * -32 + embedding[1] * 20 + embedding[2] * -17 + embedding[3] * 22 + embedding[4] * -28 + embedding[5] * -2 + embedding[6] * 17 + embedding[7] * 14 + embedding[8] * 10 + embedding[9] * 1;
    let score_17 = embedding[0] * -38 + embedding[1] * 26 + embedding[2] * 54 + embedding[3] * -6 + embedding[4] * -5 + embedding[5] * -5 + embedding[6] * 15 + embedding[7] * 4 + embedding[8] * 16 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 41;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 28;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 22;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 20;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 15;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 6;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 11;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 40;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 29;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 77;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 23;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 35;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 52;
    }
    best_palette
}

fn predict_pixel_256(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[1] * 1 + embedding[2] * -7 + embedding[3] * -16 + embedding[4] * 16 + embedding[5] * 12 + embedding[6] * -6 + embedding[7] * -21 + embedding[8] * -5 + embedding[9] * -3;
    let score_1 = embedding[0] * -19 + embedding[1] * -13 + embedding[2] * -11 + embedding[3] * 41 + embedding[4] * 17 + embedding[6] * 20 + embedding[7] * 16 + embedding[8] * 5 + embedding[9] * -19;
    let score_2 = embedding[0] * 16 + embedding[1] * -4 + embedding[3] * -12 + embedding[4] * 3 + embedding[5] * -3 + embedding[6] * -10 + embedding[7] * -18 + embedding[8] * -12 + embedding[9] * 3;
    let score_3 = embedding[0] * 26 + embedding[1] * -16 + embedding[2] * -3 + embedding[3] * -5 + embedding[4] * -26 + embedding[5] * -13 + embedding[6] * -13 + embedding[7] * -9 + embedding[8] * -19 + embedding[9] * 13;
    let score_4 = embedding[0] * 28 + embedding[1] * -19 + embedding[2] * -14 + embedding[3] * -2 + embedding[4] * -35 + embedding[5] * -3 + embedding[6] * -1 + embedding[7] * -3 + embedding[8] * -24 + embedding[9] * 18;
    let score_5 = embedding[1] * 14 + embedding[2] * 24 + embedding[3] * -10 + embedding[4] * -45 + embedding[5] * 21 + embedding[6] * -8 + embedding[7] * 17 + embedding[8] * 2 + embedding[9] * 19;
    let score_6 = embedding[0] * -22 + embedding[1] * 6 + embedding[2] * -17 + embedding[3] * 13 + embedding[4] * 11 + embedding[5] * -24 + embedding[6] * -4 + embedding[7] * 41 + embedding[8] * 5;
    let score_7 = embedding[0] * -15 + embedding[1] * 27 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -13 + embedding[5] * -2 + embedding[6] * -3 + embedding[7] * 33 + embedding[8] * 34 + embedding[9] * 7;
    let score_8 = embedding[0] * -41 + embedding[1] * -2 + embedding[2] * -9 + embedding[3] * 35 + embedding[4] * 9 + embedding[5] * -27 + embedding[6] * 21 + embedding[7] * -1 + embedding[8] * 12 + embedding[9] * -13;
    let score_9 = embedding[0] * 6 + embedding[1] * -9 + embedding[2] * -2 + embedding[3] * 1 + embedding[4] * 18 + embedding[5] * 2 + embedding[6] * -9 + embedding[7] * 36 + embedding[8] * -15 + embedding[9] * -6;
    let score_10 = embedding[0] * -22 + embedding[1] * 31 + embedding[2] * 10 + embedding[3] * -13 + embedding[4] * -31 + embedding[5] * 3 + embedding[6] * -6 + embedding[7] * 38 + embedding[8] * 19 + embedding[9] * 18;
    let score_11 = embedding[0] * -41 + embedding[1] * -3 + embedding[2] * 24 + embedding[3] * 12 + embedding[4] * 25 + embedding[5] * -14 + embedding[6] * 10 + embedding[7] * 18 + embedding[8] * 14 + embedding[9] * -9;
    let score_12 = embedding[0] * -27 + embedding[1] * 18 + embedding[2] * -9 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -6 + embedding[6] * 15 + embedding[7] * 4 + embedding[8] * 11;
    let score_13 = embedding[0] * -30 + embedding[1] * 28 + embedding[2] * 45 + embedding[3] * -10 + embedding[4] * -8 + embedding[5] * 2 + embedding[6] * 16 + embedding[7] * -1 + embedding[8] * 11 + embedding[9] * -5;
    let score_14 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 16 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 9;
    let score_15 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 9;
    let score_16 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 9;
    let score_17 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 9;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 6;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 41;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 61;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 22;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 20;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 8;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 15;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 11;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 12;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 29;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 76;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 35;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 52;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_257(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_1 = embedding[0] * 11 + embedding[1] * 1 + embedding[3] * -18 + embedding[4] * 7 + embedding[5] * 8 + embedding[6] * -15 + embedding[7] * -13 + embedding[8] * 6;
    let score_2 = embedding[0] * -10 + embedding[1] * -16 + embedding[2] * -9 + embedding[3] * 17 + embedding[4] * 10 + embedding[6] * 6 + embedding[7] * 9 + embedding[8] * -7 + embedding[9] * -12;
    let score_3 = embedding[0] * 1 + embedding[1] * -13 + embedding[2] * 32 + embedding[3] * -5 + embedding[4] * 8 + embedding[5] * 16 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -17 + embedding[9] * -2;
    let score_4 = embedding[0] * -17 + embedding[1] * 6 + embedding[2] * -20 + embedding[3] * 7 + embedding[4] * 6 + embedding[5] * -28 + embedding[6] * -8 + embedding[7] * 31 + embedding[8] * 6;
    let score_5 = embedding[0] * -31 + embedding[1] * 6 + embedding[2] * -18 + embedding[3] * 18 + embedding[4] * -5 + embedding[5] * -28 + embedding[6] * 12 + embedding[7] * -9 + embedding[8] * 9 + embedding[9] * -9;
    let score_6 = embedding[0] * 12 + embedding[1] * -6 + embedding[2] * 6 + embedding[3] * -5 + embedding[4] * 14 + embedding[5] * 3 + embedding[6] * -15 + embedding[7] * 35 + embedding[8] * -5 + embedding[9] * -3;
    let score_7 = embedding[0] * -17 + embedding[1] * 18 + embedding[2] * -18 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 10 + embedding[7] * -10 + embedding[8] * 10;
    let score_8 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_17 = embedding[0] * 8 + embedding[1] * 5 + embedding[2] * -11 + embedding[3] * -15 + embedding[4] * 10 + embedding[5] * 13 + embedding[6] * -9 + embedding[7] * -20 + embedding[8] * 7 + embedding[9] * -3;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 9;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 8;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 11;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 35;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_258(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_1 = embedding[0] * 3 + embedding[1] * -7 + embedding[2] * 22 + embedding[3] * -25 + embedding[4] * 9 + embedding[5] * 11 + embedding[6] * -4 + embedding[7] * -1 + embedding[8] * -8 + embedding[9] * 2;
    let score_2 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * -22 + embedding[3] * 10 + embedding[4] * 10 + embedding[5] * -22 + embedding[6] * -12 + embedding[7] * 37 + embedding[8] * 11 + embedding[9] * -2;
    let score_3 = embedding[0] * 13 + embedding[1] * -8 + embedding[2] * -2 + embedding[3] * -3 + embedding[4] * 16 + embedding[5] * -3 + embedding[6] * -17 + embedding[7] * 44 + embedding[9] * -5;
    let score_4 = embedding[0] * -8 + embedding[1] * -19 + embedding[2] * -6 + embedding[3] * 17 + embedding[4] * 17 + embedding[5] * -19 + embedding[6] * -8 + embedding[7] * 63 + embedding[8] * -3 + embedding[9] * -14;
    let score_5 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_6 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_7 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_8 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_9 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_10 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_11 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_12 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_13 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_14 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_15 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_16 = embedding[0] * -10 + embedding[1] * 9 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 6;
    let score_17 = embedding[0] * 9 + embedding[1] * 9 + embedding[2] * -21 + embedding[3] * -12 + embedding[4] * 8 + embedding[5] * 16 + embedding[6] * -9 + embedding[7] * -34 + embedding[8] * 16 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 12;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_259(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 9;
    let score_1 = embedding[1] * 5 + embedding[2] * 3 + embedding[3] * -8 + embedding[4] * 18 + embedding[5] * 9 + embedding[6] * -1 + embedding[7] * -10 + embedding[8] * 8 + embedding[9] * -9;
    let score_2 = embedding[0] * 18 + embedding[1] * -10 + embedding[4] * 9 + embedding[5] * -6 + embedding[6] * -18 + embedding[7] * 42 + embedding[8] * -2 + embedding[9] * -1;
    let score_3 = embedding[0] * -15 + embedding[1] * -8 + embedding[2] * -17 + embedding[3] * -8 + embedding[4] * -6 + embedding[5] * 2 + embedding[6] * 3 + embedding[7] * 8 + embedding[8] * -5 + embedding[9] * 2;
    let score_4 = embedding[0] * 8 + embedding[1] * 3 + embedding[2] * 10 + embedding[3] * 3 + embedding[4] * 17 + embedding[5] * -4 + embedding[6] * -9 + embedding[7] * 40 + embedding[8] * -2 + embedding[9] * -4;
    let score_5 = embedding[0] * -25 + embedding[1] * 3 + embedding[2] * 2 + embedding[3] * -2 + embedding[4] * 4 + embedding[5] * 6 + embedding[6] * 22 + embedding[7] * 21 + embedding[8] * -8 + embedding[9] * 1;
    let score_6 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 9;
    let score_7 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 9;
    let score_8 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 9;
    let score_9 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 9;
    let score_10 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 9;
    let score_11 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 9;
    let score_12 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 9;
    let score_13 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 9;
    let score_14 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 9;
    let score_15 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 9;
    let score_16 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -12 + embedding[9] * 9;
    let score_17 = embedding[0] * 9 + embedding[1] * -5 + embedding[2] * -17 + embedding[3] * -11 + embedding[4] * 8 + embedding[5] * 9 + embedding[6] * -11 + embedding[7] * -17 + embedding[8] * 9 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 31;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 12;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 187;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 152;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_260(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -8 + embedding[1] * -21 + embedding[2] * 1 + embedding[3] * 9 + embedding[4] * 14 + embedding[5] * -10 + embedding[6] * -3 + embedding[7] * 40 + embedding[8] * -5 + embedding[9] * -10;
    let score_2 = embedding[0] * -15 + embedding[2] * -14 + embedding[3] * -12 + embedding[4] * -4 + embedding[5] * 6 + embedding[6] * 3 + embedding[7] * 2 + embedding[8] * -2 + embedding[9] * 2;
    let score_3 = embedding[0] * 11 + embedding[1] * -7 + embedding[2] * -3 + embedding[4] * 14 + embedding[5] * -9 + embedding[6] * -9 + embedding[7] * 20 + embedding[8] * -6 + embedding[9] * -5;
    let score_4 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * 6 + embedding[2] * -13 + embedding[3] * -15 + embedding[4] * 8 + embedding[5] * 13 + embedding[6] * -12 + embedding[7] * -28 + embedding[8] * 16 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 6;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 12;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_261(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_1 = embedding[0] * -14 + embedding[1] * -7 + embedding[2] * -14 + embedding[3] * -14 + embedding[4] * 3 + embedding[5] * 4 + embedding[6] * 2 + embedding[7] * 4 + embedding[8] * -3 + embedding[9] * -1;
    let score_2 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_3 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_4 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_5 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_6 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_7 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_8 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_9 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_10 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_11 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_12 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_13 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_14 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_15 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_16 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_17 = embedding[0] * 10 + embedding[1] * -4 + embedding[2] * -13 + embedding[3] * -16 + embedding[4] * 20 + embedding[5] * 10 + embedding[6] * -14 + embedding[7] * -21 + embedding[8] * 12 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_262(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -13 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_263(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_264(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -24 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_265(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_266(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_1 = embedding[0] * 25 + embedding[1] * -16 + embedding[2] * 13 + embedding[3] * 34 + embedding[4] * 9 + embedding[5] * -3 + embedding[6] * 9 + embedding[7] * -3 + embedding[8] * -7 + embedding[9] * -10;
    let score_2 = embedding[0] * -21 + embedding[1] * -2 + embedding[2] * -17 + embedding[3] * -18 + embedding[4] * -1 + embedding[5] * 4 + embedding[7] * 6 + embedding[8] * -1 + embedding[9] * 1;
    let score_3 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_4 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_5 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_6 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_7 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_8 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_9 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_10 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_11 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_12 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_13 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_14 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_15 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_16 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_17 = embedding[0] * 2 + embedding[1] * 2 + embedding[2] * -16 + embedding[3] * -21 + embedding[4] * 15 + embedding[5] * 12 + embedding[6] * -16 + embedding[7] * -18 + embedding[8] * 14 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 13;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_267(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_1 = embedding[0] * -14 + embedding[1] * -7 + embedding[2] * -6 + embedding[3] * -27 + embedding[4] * 9 + embedding[5] * 4 + embedding[6] * -4 + embedding[7] * -7 + embedding[8] * -10 + embedding[9] * 2;
    let score_2 = embedding[0] * 28 + embedding[1] * 6 + embedding[2] * 15 + embedding[3] * 24 + embedding[4] * -3 + embedding[5] * 21 + embedding[6] * -4 + embedding[7] * 6 + embedding[8] * 17 + embedding[9] * -6;
    let score_3 = embedding[0] * 20 + embedding[1] * 23 + embedding[2] * -4 + embedding[3] * 15 + embedding[4] * 23 + embedding[5] * -36 + embedding[6] * -2 + embedding[7] * 27;
    let score_4 = embedding[0] * 24 + embedding[1] * -13 + embedding[2] * 15 + embedding[3] * 43 + embedding[4] * 9 + embedding[6] * 7 + embedding[7] * 8 + embedding[8] * -12 + embedding[9] * -10;
    let score_5 = embedding[0] * 17 + embedding[1] * -8 + embedding[2] * 1 + embedding[3] * 4 + embedding[4] * 19 + embedding[5] * -6 + embedding[6] * -15 + embedding[7] * 48 + embedding[8] * -2 + embedding[9] * -7;
    let score_6 = embedding[0] * -16 + embedding[1] * -27 + embedding[2] * 2 + embedding[3] * 2 + embedding[4] * 15 + embedding[5] * -8 + embedding[6] * -4 + embedding[7] * 43 + embedding[8] * -11 + embedding[9] * -12;
    let score_7 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_8 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_9 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_10 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_11 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_12 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_14 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_15 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_16 = embedding[0] * -7 + embedding[1] * 9 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_17 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * -19 + embedding[3] * -25 + embedding[4] * 2 + embedding[5] * 13 + embedding[6] * -12 + embedding[7] * -31 + embedding[8] * 14 + embedding[9] * -1;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 14;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 13;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 12;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 6;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_268(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_1 = embedding[0] * -28 + embedding[1] * -13 + embedding[2] * -6 + embedding[3] * -26 + embedding[4] * 7 + embedding[5] * 1 + embedding[6] * -4 + embedding[7] * 7 + embedding[8] * -14 + embedding[9] * 1;
    let score_2 = embedding[0] * 14 + embedding[1] * -14 + embedding[2] * 61 + embedding[4] * 6 + embedding[5] * 5 + embedding[6] * 7 + embedding[7] * 37 + embedding[8] * -35 + embedding[9] * -1;
    let score_3 = embedding[0] * 20 + embedding[1] * 33 + embedding[2] * -11 + embedding[3] * 11 + embedding[4] * 19 + embedding[5] * -40 + embedding[7] * 19 + embedding[8] * 3 + embedding[9] * 4;
    let score_4 = embedding[0] * 27 + embedding[1] * -16 + embedding[2] * 27 + embedding[3] * 34 + embedding[4] * 8 + embedding[5] * 4 + embedding[6] * 12 + embedding[7] * 11 + embedding[8] * -19 + embedding[9] * -11;
    let score_5 = embedding[0] * 15 + embedding[1] * 8 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * 23 + embedding[6] * -5 + embedding[7] * -5 + embedding[8] * 20 + embedding[9] * -4;
    let score_6 = embedding[0] * 17 + embedding[1] * -10 + embedding[2] * 6 + embedding[3] * -1 + embedding[4] * 17 + embedding[5] * -7 + embedding[6] * -14 + embedding[7] * 50 + embedding[8] * -9 + embedding[9] * -8;
    let score_7 = embedding[0] * -17 + embedding[1] * -32 + embedding[2] * 8 + embedding[4] * 17 + embedding[5] * -10 + embedding[6] * -1 + embedding[7] * 57 + embedding[8] * -20 + embedding[9] * -13;
    let score_8 = embedding[0] * -39 + embedding[1] * 13 + embedding[2] * -19 + embedding[3] * 4 + embedding[4] * -12 + embedding[5] * -25 + embedding[6] * 9 + embedding[7] * -26 + embedding[8] * 18 + embedding[9] * -4;
    let score_9 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_10 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_11 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_12 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_13 = embedding[0] * -8 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_14 = embedding[0] * -8 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_15 = embedding[0] * -8 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_16 = embedding[0] * -8 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_17 = embedding[0] * 1 + embedding[1] * 17 + embedding[2] * -19 + embedding[3] * -17 + embedding[5] * 13 + embedding[6] * -10 + embedding[7] * -33 + embedding[8] * 21 + embedding[9] * -2;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 13;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 75;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 11;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_269(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_1 = embedding[0] * 3 + embedding[1] * 2 + embedding[2] * -10 + embedding[3] * -19 + embedding[4] * 11 + embedding[5] * 6 + embedding[6] * -10 + embedding[7] * -15 + embedding[8] * 7 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * -18 + embedding[2] * -9 + embedding[3] * 19 + embedding[4] * 13 + embedding[5] * 2 + embedding[6] * 4 + embedding[7] * 9 + embedding[8] * -6 + embedding[9] * -13;
    let score_3 = embedding[0] * 3 + embedding[1] * 1 + embedding[2] * 35 + embedding[3] * -13 + embedding[4] * 3 + embedding[5] * 8 + embedding[6] * 2 + embedding[7] * 10 + embedding[8] * -7 + embedding[9] * 1;
    let score_4 = embedding[0] * -15 + embedding[1] * 3 + embedding[2] * -18 + embedding[3] * 2 + embedding[4] * 8 + embedding[5] * -26 + embedding[6] * -11 + embedding[7] * 35 + embedding[8] * 7;
    let score_5 = embedding[0] * 13 + embedding[1] * -16 + embedding[2] * 13 + embedding[3] * 26 + embedding[4] * 12 + embedding[5] * 4 + embedding[6] * 10 + embedding[7] * -2 + embedding[8] * -6 + embedding[9] * -13;
    let score_6 = embedding[0] * 9 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -2 + embedding[4] * 15 + embedding[5] * -4 + embedding[6] * -15 + embedding[7] * 36 + embedding[8] * -2 + embedding[9] * -6;
    let score_7 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_8 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_9 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 7;
    let score_10 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_11 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_12 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_13 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 7;
    let score_14 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 7;
    let score_15 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 7;
    let score_16 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 6;
    let score_17 = embedding[0] * 2 + embedding[1] * 6 + embedding[2] * -14 + embedding[3] * -16 + embedding[4] * 8 + embedding[5] * 9 + embedding[6] * -9 + embedding[7] * -23 + embedding[8] * 12 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 9;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 8;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 13;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_270(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 18 + embedding[1] * -7 + embedding[3] * -19 + embedding[4] * 12 + embedding[5] * 11 + embedding[6] * -9 + embedding[7] * -22 + embedding[8] * -8 + embedding[9] * 1;
    let score_1 = embedding[0] * -11 + embedding[1] * 3 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 9;
    let score_2 = embedding[0] * -16 + embedding[1] * -9 + embedding[2] * -13 + embedding[3] * 22 + embedding[4] * -2 + embedding[5] * 5 + embedding[6] * 6 + embedding[7] * 10 + embedding[8] * 10 + embedding[9] * -12;
    let score_3 = embedding[0] * -20 + embedding[1] * 7 + embedding[2] * -21 + embedding[3] * 11 + embedding[4] * -2 + embedding[5] * -22 + embedding[6] * -4 + embedding[7] * 29 + embedding[8] * 10 + embedding[9] * 3;
    let score_4 = embedding[0] * -14 + embedding[1] * 13 + embedding[2] * 2 + embedding[3] * 17 + embedding[4] * -18 + embedding[5] * -2 + embedding[6] * -2 + embedding[7] * 26 + embedding[8] * 30 + embedding[9] * 2;
    let score_5 = embedding[0] * 23 + embedding[1] * -22 + embedding[2] * 18 + embedding[3] * 30 + embedding[4] * 14 + embedding[5] * 6 + embedding[6] * 16 + embedding[7] * -2 + embedding[8] * -19 + embedding[9] * -11;
    let score_6 = embedding[0] * 20 + embedding[1] * -16 + embedding[2] * 10 + embedding[3] * -4 + embedding[4] * 16 + embedding[5] * -1 + embedding[6] * -13 + embedding[7] * 32 + embedding[8] * -19 + embedding[9] * -3;
    let score_7 = embedding[0] * -35 + embedding[2] * -20 + embedding[3] * 25 + embedding[4] * -8 + embedding[5] * -18 + embedding[6] * 10 + embedding[7] * 2 + embedding[8] * 7 + embedding[9] * -10;
    let score_8 = embedding[0] * -22 + embedding[1] * 18 + embedding[2] * -4 + embedding[3] * -10 + embedding[4] * -25 + embedding[5] * -2 + embedding[6] * -6 + embedding[7] * 28 + embedding[8] * 18 + embedding[9] * 11;
    let score_9 = embedding[0] * -29 + embedding[1] * 14 + embedding[2] * -16 + embedding[3] * 21 + embedding[4] * -30 + embedding[5] * -7 + embedding[6] * 10 + embedding[7] * 8 + embedding[8] * 17;
    let score_10 = embedding[0] * -25 + embedding[1] * 20 + embedding[2] * 36 + embedding[3] * -8 + embedding[4] * -15 + embedding[5] * 1 + embedding[6] * 15 + embedding[7] * -4 + embedding[8] * 10 + embedding[9] * -4;
    let score_11 = embedding[0] * -11 + embedding[1] * 3 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_12 = embedding[0] * -11 + embedding[1] * 3 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_13 = embedding[0] * -11 + embedding[1] * 3 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 9;
    let score_14 = embedding[0] * -11 + embedding[1] * 3 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_15 = embedding[0] * -11 + embedding[1] * 3 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 9;
    let score_16 = embedding[0] * -11 + embedding[1] * 3 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 9;
    let score_17 = embedding[0] * -2 + embedding[1] * -1 + embedding[2] * -12 + embedding[3] * -8 + embedding[5] * 10 + embedding[6] * -7 + embedding[7] * -11 + embedding[8] * 5 + embedding[9] * -3;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 15;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 13;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 11;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 29;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 35;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 52;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_271(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 2 + embedding[1] * -6 + embedding[2] * -8 + embedding[3] * -10 + embedding[4] * 7 + embedding[5] * 5 + embedding[6] * -12 + embedding[7] * -9 + embedding[8] * 4 + embedding[9] * -5;
    let score_1 = embedding[0] * 4 + embedding[1] * 11 + embedding[2] * -4 + embedding[3] * -4 + embedding[4] * 3 + embedding[5] * 5 + embedding[6] * 3 + embedding[7] * -2 + embedding[8] * -14 + embedding[9] * -10;
    let score_2 = embedding[0] * 7 + embedding[1] * 1 + embedding[3] * -6 + embedding[4] * 13 + embedding[6] * 19 + embedding[7] * -5 + embedding[8] * 4 + embedding[9] * 6;
    let score_3 = embedding[0] * 1 + embedding[1] * 7 + embedding[2] * -2 + embedding[3] * 6 + embedding[4] * 11 + embedding[5] * 9 + embedding[6] * 1 + embedding[7] * -7 + embedding[8] * -12 + embedding[9] * 4;
    let score_4 = embedding[0] * -8 + embedding[1] * -7 + embedding[2] * -5 + embedding[3] * 16 + embedding[4] * 11 + embedding[5] * 2 + embedding[6] * 5 + embedding[7] * 11 + embedding[8] * 5 + embedding[9] * -14;
    let score_5 = embedding[0] * -2 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 3 + embedding[4] * 7 + embedding[5] * -10 + embedding[6] * -10 + embedding[7] * -10 + embedding[8] * 2 + embedding[9] * -8;
    let score_6 = embedding[0] * 17 + embedding[1] * -23 + embedding[2] * -25 + embedding[4] * -37 + embedding[5] * -1 + embedding[6] * -1 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 16;
    let score_7 = embedding[0] * 18 + embedding[1] * -20 + embedding[2] * -9 + embedding[3] * -1 + embedding[4] * -26 + embedding[5] * -12 + embedding[6] * -14 + embedding[7] * 2 + embedding[8] * -8 + embedding[9] * 11;
    let score_8 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * -8 + embedding[4] * -17 + embedding[5] * 14 + embedding[6] * -4 + embedding[7] * 11 + embedding[8] * 5 + embedding[9] * 7;
    let score_9 = embedding[0] * -8 + embedding[1] * 17 + embedding[2] * 12 + embedding[3] * 6 + embedding[4] * -2 + embedding[5] * 1 + embedding[7] * 16 + embedding[8] * 18;
    let score_10 = embedding[0] * -24 + embedding[1] * 3 + embedding[2] * 1 + embedding[3] * 11 + embedding[4] * 7 + embedding[5] * -14 + embedding[6] * 11 + embedding[7] * -5 + embedding[8] * 4 + embedding[9] * -10;
    let score_11 = embedding[0] * -28 + embedding[1] * -3 + embedding[2] * 20 + embedding[3] * 1 + embedding[4] * 12 + embedding[5] * -11 + embedding[7] * 11 + embedding[8] * 13 + embedding[9] * -6;
    let score_12 = embedding[0] * -15 + embedding[1] * 20 + embedding[2] * 7 + embedding[3] * -11 + embedding[4] * -14 + embedding[5] * 1 + embedding[6] * -2 + embedding[7] * 25 + embedding[8] * 8 + embedding[9] * 9;
    let score_13 = embedding[0] * 11 + embedding[1] * -8 + embedding[2] * -3 + embedding[3] * 9 + embedding[4] * -2 + embedding[5] * -9 + embedding[6] * -1 + embedding[7] * 10 + embedding[8] * 3 + embedding[9] * 25;
    let score_14 = embedding[0] * -15 + embedding[1] * 15 + embedding[2] * -8 + embedding[3] * 15 + embedding[4] * -12 + embedding[5] * -5 + embedding[6] * 11 + embedding[7] * 6 + embedding[8] * 7;
    let score_15 = embedding[0] * -25 + embedding[1] * 19 + embedding[2] * 40 + embedding[3] * -6 + embedding[4] * -9 + embedding[5] * -5 + embedding[6] * 11 + embedding[7] * 1 + embedding[8] * 12 + embedding[9] * -6;
    let score_16 = embedding[0] * 7 + embedding[1] * 1 + embedding[2] * -7 + embedding[3] * 7 + embedding[4] * -17 + embedding[5] * -3 + embedding[6] * -16 + embedding[7] * 3 + embedding[8] * -6 + embedding[9] * 1;
    let score_17 = embedding[0] * 9 + embedding[1] * -7 + embedding[2] * -10 + embedding[3] * 5 + embedding[4] * -25 + embedding[5] * -10 + embedding[6] * -2 + embedding[7] * -7 + embedding[8] * -3 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 61;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 20;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 15;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 11;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 76;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 29;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 23;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 35;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 52;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 100;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 93;
    }
    best_palette
}

fn predict_pixel_272(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 7 + embedding[1] * -10 + embedding[2] * -7 + embedding[3] * -9 + embedding[4] * 8 + embedding[5] * 1 + embedding[6] * -11 + embedding[7] * -12 + embedding[8] * 3 + embedding[9] * -4;
    let score_1 = embedding[0] * -4 + embedding[1] * -1 + embedding[2] * -5 + embedding[3] * -10 + embedding[4] * 15 + embedding[5] * 6 + embedding[6] * 39 + embedding[7] * 5 + embedding[8] * 17 + embedding[9] * 14;
    let score_2 = embedding[0] * -9 + embedding[1] * 19 + embedding[2] * -12 + embedding[3] * -6 + embedding[4] * -3 + embedding[5] * 15 + embedding[6] * 6 + embedding[7] * 11 + embedding[8] * -20 + embedding[9] * -19;
    let score_3 = embedding[0] * -18 + embedding[1] * 14 + embedding[2] * -8 + embedding[3] * 14 + embedding[4] * 11 + embedding[5] * 23 + embedding[6] * 4 + embedding[7] * 3 + embedding[8] * -15 + embedding[9] * 14;
    let score_4 = embedding[0] * -7 + embedding[2] * 24 + embedding[3] * 6 + embedding[4] * 5 + embedding[5] * -15 + embedding[6] * -23 + embedding[7] * -13 + embedding[8] * 2 + embedding[9] * -13;
    let score_5 = embedding[0] * 17 + embedding[1] * -12 + embedding[2] * 5 + embedding[3] * -6 + embedding[4] * -3 + embedding[5] * -15 + embedding[6] * -16 + embedding[7] * -10 + embedding[8] * -5 + embedding[9] * 2;
    let score_6 = embedding[0] * 20 + embedding[1] * -27 + embedding[2] * -26 + embedding[3] * -1 + embedding[4] * -37 + embedding[5] * -4 + embedding[6] * 2 + embedding[7] * 11 + embedding[8] * -13 + embedding[9] * 17;
    let score_7 = embedding[0] * 26 + embedding[1] * -25 + embedding[2] * -3 + embedding[4] * -26 + embedding[5] * -22 + embedding[6] * -18 + embedding[7] * -3 + embedding[8] * -10 + embedding[9] * 13;
    let score_8 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * 18 + embedding[3] * -4 + embedding[4] * -6 + embedding[5] * -15 + embedding[6] * -12 + embedding[7] * -5 + embedding[8] * -7 + embedding[9] * -21;
    let score_9 = embedding[0] * -3 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * 13 + embedding[4] * 8 + embedding[5] * -11 + embedding[6] * -19 + embedding[7] * -13 + embedding[8] * -5 + embedding[9] * 1;
    let score_10 = embedding[0] * 7 + embedding[1] * -6 + embedding[2] * 18 + embedding[3] * -7 + embedding[4] * 6 + embedding[5] * -18 + embedding[6] * -2 + embedding[7] * -9 + embedding[8] * 8 + embedding[9] * -1;
    let score_11 = embedding[0] * -20 + embedding[1] * 29 + embedding[2] * 26 + embedding[3] * 19 + embedding[4] * 4 + embedding[5] * 7 + embedding[6] * 23 + embedding[7] * 36 + embedding[8] * 25 + embedding[9] * 4;
    let score_12 = embedding[0] * -35 + embedding[1] * 11 + embedding[2] * 10 + embedding[3] * 17 + embedding[4] * -3 + embedding[5] * 21 + embedding[6] * 33 + embedding[7] * 32 + embedding[8] * 15 + embedding[9] * -3;
    let score_13 = embedding[0] * -39 + embedding[1] * 16 + embedding[2] * 5 + embedding[3] * 13 + embedding[4] * 12 + embedding[5] * -11 + embedding[6] * 28 + embedding[7] * -2 + embedding[8] * 1 + embedding[9] * -9;
    let score_14 = embedding[0] * -28 + embedding[1] * 35 + embedding[2] * 6 + embedding[3] * -9 + embedding[4] * -5 + embedding[5] * 9 + embedding[6] * 17 + embedding[7] * 37 + embedding[8] * 6 + embedding[9] * 12;
    let score_15 = embedding[0] * 15 + embedding[1] * -13 + embedding[2] * -1 + embedding[3] * 5 + embedding[4] * -4 + embedding[5] * -16 + embedding[6] * -3 + embedding[7] * 7 + embedding[8] * 1 + embedding[9] * 32;
    let score_16 = embedding[0] * 10 + embedding[1] * -8 + embedding[2] * 14 + embedding[3] * -2 + embedding[4] * -11 + embedding[5] * -27 + embedding[6] * -17 + embedding[7] * -8 + embedding[9] * -11;
    let score_17 = embedding[0] * 9 + embedding[2] * -7 + embedding[3] * 7 + embedding[4] * -18 + embedding[5] * -4 + embedding[6] * -18 + embedding[7] * 3 + embedding[8] * -12 + embedding[9] * 2;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 42;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 28;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 63;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 66;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 72;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 15;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 6;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 11;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 29;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 23;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 115;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 80;
    }
    best_palette
}

fn predict_pixel_273(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 7 + embedding[1] * -9 + embedding[2] * -9 + embedding[3] * -4 + embedding[4] * -1 + embedding[5] * -2 + embedding[6] * -11 + embedding[7] * -7 + embedding[8] * 4 + embedding[9] * -2;
    let score_1 = embedding[0] * 2 + embedding[1] * 1 + embedding[2] * 2 + embedding[3] * -5 + embedding[4] * 10 + embedding[6] * 25 + embedding[7] * 1 + embedding[8] * 9 + embedding[9] * 12;
    let score_2 = embedding[0] * -1 + embedding[1] * 13 + embedding[2] * -5 + embedding[3] * -2 + embedding[4] * -4 + embedding[5] * 4 + embedding[7] * 3 + embedding[8] * -17 + embedding[9] * -10;
    let score_3 = embedding[0] * -7 + embedding[1] * 10 + embedding[3] * 10 + embedding[4] * 5 + embedding[5] * 11 + embedding[8] * -14 + embedding[9] * 11;
    let score_4 = embedding[0] * -6 + embedding[1] * -2 + embedding[3] * -10 + embedding[4] * 4 + embedding[5] * 4 + embedding[6] * 23 + embedding[7] * 13 + embedding[8] * 16 + embedding[9] * 4;
    let score_5 = embedding[0] * -14 + embedding[1] * 12 + embedding[4] * -11 + embedding[5] * 9 + embedding[6] * -3 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * -19;
    let score_6 = embedding[0] * -20 + embedding[1] * 8 + embedding[3] * 14 + embedding[4] * 2 + embedding[5] * 15 + embedding[6] * -8 + embedding[7] * 7 + embedding[8] * -3 + embedding[9] * 6;
    let score_7 = embedding[0] * 8 + embedding[1] * -6 + embedding[2] * -1 + embedding[3] * -4 + embedding[4] * 1 + embedding[5] * -8 + embedding[6] * -6 + embedding[7] * -9 + embedding[8] * 1 + embedding[9] * -1;
    let score_8 = embedding[0] * 14 + embedding[1] * -5 + embedding[2] * 8 + embedding[3] * -2 + embedding[4] * -1 + embedding[5] * -15 + embedding[6] * -11 + embedding[7] * -7 + embedding[8] * -4 + embedding[9] * 2;
    let score_9 = embedding[0] * 6 + embedding[1] * -2 + embedding[2] * 5 + embedding[3] * -3 + embedding[4] * 6 + embedding[5] * -7 + embedding[6] * -4 + embedding[7] * -8 + embedding[9] * -2;
    let score_10 = embedding[0] * 5 + embedding[2] * 26 + embedding[4] * 5 + embedding[5] * -21 + embedding[6] * -8 + embedding[7] * -9 + embedding[8] * 4 + embedding[9] * -2;
    let score_11 = embedding[0] * -6 + embedding[1] * 5 + embedding[2] * -7 + embedding[3] * -4 + embedding[4] * 7 + embedding[5] * 11 + embedding[6] * 7 + embedding[7] * 2 + embedding[8] * 4 + embedding[9] * -1;
    let score_12 = embedding[0] * -2 + embedding[1] * -5 + embedding[2] * -2 + embedding[3] * -1 + embedding[4] * -3 + embedding[5] * -2 + embedding[6] * -10 + embedding[7] * -1 + embedding[8] * 6 + embedding[9] * -4;
    let score_13 = embedding[0] * -12 + embedding[1] * 2 + embedding[2] * -6 + embedding[3] * -4 + embedding[4] * 2 + embedding[5] * 11 + embedding[6] * 5 + embedding[7] * 8 + embedding[8] * 6 + embedding[9] * -1;
    let score_14 = embedding[0] * 19 + embedding[1] * -16 + embedding[2] * -15 + embedding[3] * 4 + embedding[4] * -30 + embedding[5] * -5 + embedding[6] * 3 + embedding[7] * 8 + embedding[8] * -12 + embedding[9] * 16;
    let score_15 = embedding[0] * 2 + embedding[1] * 2 + embedding[2] * -5 + embedding[3] * -3 + embedding[4] * 6 + embedding[5] * 3 + embedding[6] * 8 + embedding[7] * -1 + embedding[8] * 1 + embedding[9] * 1;
    let score_16 = embedding[0] * 5 + embedding[1] * 2 + embedding[2] * 18 + embedding[3] * -2 + embedding[4] * -7 + embedding[5] * -21 + embedding[6] * -9 + embedding[7] * -2 + embedding[8] * -3 + embedding[9] * -17;
    let score_17 = embedding[0] * 3 + embedding[1] * -3 + embedding[2] * 12 + embedding[3] * 8 + embedding[4] * 5 + embedding[5] * -11 + embedding[6] * -13 + embedding[7] * -6 + embedding[8] * 3 + embedding[9] * 8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 27;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 7;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 34;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 2;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 3;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 4;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 30;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 42;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 58;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 57;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 69;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 5;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 12;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 64;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 71;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 99;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 102;
    }
    best_palette
}

fn predict_pixel_274(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 7 + embedding[1] * -9 + embedding[2] * -9 + embedding[3] * -5 + embedding[5] * -1 + embedding[6] * -11 + embedding[7] * -7 + embedding[8] * 4 + embedding[9] * -2;
    let score_1 = embedding[0] * -9 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 8 + embedding[6] * 22 + embedding[7] * 13 + embedding[8] * 16 + embedding[9] * 4;
    let score_2 = embedding[0] * 2 + embedding[1] * 1 + embedding[2] * 1 + embedding[3] * -6 + embedding[4] * 11 + embedding[5] * 1 + embedding[6] * 24 + embedding[8] * 8 + embedding[9] * 11;
    let score_3 = embedding[0] * -1 + embedding[1] * 13 + embedding[2] * -6 + embedding[3] * -3 + embedding[4] * -3 + embedding[5] * 5 + embedding[7] * 3 + embedding[8] * -17 + embedding[9] * -10;
    let score_4 = embedding[0] * -16 + embedding[1] * 12 + embedding[2] * -4 + embedding[3] * -3 + embedding[4] * -6 + embedding[5] * 15 + embedding[6] * 1 + embedding[7] * 16 + embedding[8] * -4 + embedding[9] * -15;
    let score_5 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * -1 + embedding[3] * 8 + embedding[4] * 6 + embedding[5] * 11 + embedding[6] * 1 + embedding[7] * -1 + embedding[8] * -14 + embedding[9] * 10;
    let score_6 = embedding[0] * -19 + embedding[1] * 8 + embedding[2] * -3 + embedding[3] * 8 + embedding[4] * 4 + embedding[5] * 18 + embedding[6] * -4 + embedding[7] * 9 + embedding[8] * -1 + embedding[9] * 4;
    let score_7 = embedding[0] * 8 + embedding[1] * -6 + embedding[2] * -1 + embedding[3] * -5 + embedding[4] * 2 + embedding[5] * -7 + embedding[6] * -6 + embedding[7] * -9 + embedding[9] * -2;
    let score_8 = embedding[0] * 14 + embedding[1] * -5 + embedding[2] * 8 + embedding[3] * -3 + embedding[5] * -14 + embedding[6] * -11 + embedding[7] * -7 + embedding[8] * -4 + embedding[9] * 2;
    let score_9 = embedding[0] * 7 + embedding[1] * -2 + embedding[2] * 4 + embedding[3] * -4 + embedding[4] * 7 + embedding[5] * -6 + embedding[6] * -4 + embedding[7] * -8 + embedding[8] * -1 + embedding[9] * -2;
    let score_10 = embedding[0] * -3 + embedding[1] * -4 + embedding[2] * -3 + embedding[3] * -2 + embedding[4] * -1 + embedding[6] * -11 + embedding[7] * -2 + embedding[8] * 5 + embedding[9] * -6;
    let score_11 = embedding[0] * 1 + embedding[1] * 2 + embedding[2] * 32 + embedding[3] * 5 + embedding[4] * 4 + embedding[5] * -22 + embedding[6] * -15 + embedding[7] * -9 + embedding[8] * -1 + embedding[9] * -4;
    let score_12 = embedding[0] * 19 + embedding[1] * -16 + embedding[2] * -15 + embedding[3] * 3 + embedding[4] * -29 + embedding[5] * -4 + embedding[6] * 3 + embedding[7] * 8 + embedding[8] * -12 + embedding[9] * 15;
    let score_13 = embedding[0] * 2 + embedding[1] * 2 + embedding[2] * -6 + embedding[3] * -4 + embedding[4] * 7 + embedding[5] * 4 + embedding[6] * 8 + embedding[7] * -2;
    let score_14 = embedding[0] * 5 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * -2 + embedding[4] * -7 + embedding[5] * -20 + embedding[6] * -11 + embedding[7] * -3 + embedding[8] * -5 + embedding[9] * -18;
    let score_15 = embedding[0] * 3 + embedding[1] * -3 + embedding[2] * 12 + embedding[3] * 7 + embedding[4] * 6 + embedding[5] * -10 + embedding[6] * -14 + embedding[7] * -7 + embedding[8] * 2 + embedding[9] * 7;
    let score_16 = embedding[0] * 6 + embedding[1] * -1 + embedding[2] * 24 + embedding[3] * -4 + embedding[4] * 5 + embedding[5] * -21 + embedding[6] * 3 + embedding[7] * -5 + embedding[8] * 8 + embedding[9] * 1;
    let score_17 = embedding[1] * 3 + embedding[2] * -6 + embedding[3] * -4 + embedding[4] * 7 + embedding[5] * 6 + embedding[6] * 9 + embedding[7] * -1 + embedding[8] * -1;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 27;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 3;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 34;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 4;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 30;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 42;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 58;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 5;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 57;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 64;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 71;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 99;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 102;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 103;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 70;
    }
    best_palette
}

fn predict_pixel_275(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 12 + embedding[1] * -12 + embedding[2] * -4 + embedding[3] * -12 + embedding[4] * 10 + embedding[5] * -5 + embedding[6] * -20 + embedding[7] * -18 + embedding[8] * 8 + embedding[9] * -9;
    let score_1 = embedding[0] * -1 + embedding[1] * -3 + embedding[2] * -3 + embedding[3] * -12 + embedding[4] * 18 + embedding[5] * 7 + embedding[6] * 35 + embedding[8] * 20 + embedding[9] * 9;
    let score_2 = embedding[0] * -8 + embedding[1] * 17 + embedding[2] * -11 + embedding[3] * -7 + embedding[4] * 2 + embedding[5] * 16 + embedding[6] * 1 + embedding[7] * 4 + embedding[8] * -15 + embedding[9] * -23;
    let score_3 = embedding[0] * -17 + embedding[1] * 14 + embedding[2] * -6 + embedding[3] * 11 + embedding[4] * 14 + embedding[5] * 25 + embedding[6] * 1 + embedding[7] * -2 + embedding[8] * -10 + embedding[9] * 9;
    let score_4 = embedding[0] * -3 + embedding[1] * -3 + embedding[2] * 27 + embedding[3] * 2 + embedding[4] * 7 + embedding[5] * -16 + embedding[6] * -28 + embedding[7] * -17 + embedding[8] * 9 + embedding[9] * -18;
    let score_5 = embedding[0] * 24 + embedding[1] * -23 + embedding[2] * -18 + embedding[3] * -1 + embedding[4] * -26 + embedding[5] * -4 + embedding[6] * -1 + embedding[7] * 3 + embedding[8] * -8 + embedding[9] * 13;
    let score_6 = embedding[0] * 10 + embedding[1] * -19 + embedding[2] * -22 + embedding[3] * -5 + embedding[4] * -24 + embedding[5] * 2 + embedding[6] * -1 + embedding[7] * 11 + embedding[8] * -1 + embedding[9] * 7;
    let score_7 = embedding[0] * -26 + embedding[1] * 32 + embedding[2] * 6 + embedding[3] * -15 + embedding[4] * -7 + embedding[5] * 7 + embedding[6] * 9 + embedding[7] * 30 + embedding[8] * 7 + embedding[9] * 11;
    let score_8 = embedding[0] * 14 + embedding[1] * -11 + embedding[2] * -1 + embedding[3] * 2 + embedding[4] * 4 + embedding[5] * -12 + embedding[6] * -7 + embedding[7] * 1 + embedding[8] * 10 + embedding[9] * 23;
    let score_9 = embedding[0] * 11 + embedding[1] * 2 + embedding[2] * -6 + embedding[3] * 3 + embedding[4] * -10 + embedding[5] * -4 + embedding[6] * -21 + embedding[7] * -2 + embedding[8] * -5 + embedding[9] * -4;
    let score_10 = embedding[0] * 14 + embedding[1] * -10 + embedding[2] * -5 + embedding[3] * 3 + embedding[4] * -12 + embedding[5] * -15 + embedding[6] * -8 + embedding[7] * -15 + embedding[8] * 5 + embedding[9] * -2;
    let score_11 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let score_12 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let score_13 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let score_14 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let score_15 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let score_16 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let score_17 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 64;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 29;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 23;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 80;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 93;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_276(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 6 + embedding[1] * -4 + embedding[2] * -5 + embedding[3] * -10 + embedding[4] * 7 + embedding[5] * 4 + embedding[6] * -10 + embedding[7] * -13 + embedding[8] * 4 + embedding[9] * -5;
    let score_1 = embedding[0] * 3 + embedding[1] * -4 + embedding[2] * -3 + embedding[3] * -11 + embedding[4] * 12 + embedding[5] * 7 + embedding[6] * 9 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * 1;
    let score_2 = embedding[0] * 2 + embedding[1] * 4 + embedding[2] * -7 + embedding[3] * -10 + embedding[4] * 5 + embedding[5] * 9 + embedding[6] * -6 + embedding[7] * -4 + embedding[8] * -4 + embedding[9] * -13;
    let score_3 = embedding[0] * -2 + embedding[2] * -4 + embedding[3] * -1 + embedding[4] * 13 + embedding[5] * 12 + embedding[6] * -7 + embedding[7] * -8 + embedding[8] * -2 + embedding[9] * 2;
    let score_4 = embedding[0] * -2 + embedding[1] * -2 + embedding[2] * 15 + embedding[3] * -2 + embedding[4] * 7 + embedding[5] * -5 + embedding[6] * -16 + embedding[7] * -12 + embedding[8] * 6 + embedding[9] * -10;
    let score_5 = embedding[0] * 14 + embedding[1] * -17 + embedding[2] * -20 + embedding[3] * -2 + embedding[4] * -26 + embedding[5] * 4 + embedding[6] * 2 + embedding[7] * 8 + embedding[8] * -8 + embedding[9] * 12;
    let score_6 = embedding[0] * -23 + embedding[1] * 4 + embedding[2] * 2 + embedding[3] * 6 + embedding[4] * 9 + embedding[5] * -12 + embedding[6] * 9 + embedding[7] * -8 + embedding[8] * 4 + embedding[9] * -9;
    let score_7 = embedding[1] * 6 + embedding[2] * -6 + embedding[3] * 22 + embedding[4] * 10 + embedding[5] * -9 + embedding[6] * 4 + embedding[7] * 2 + embedding[8] * 9 + embedding[9] * -11;
    let score_8 = embedding[0] * -13 + embedding[1] * 17 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -9 + embedding[6] * -3 + embedding[7] * 20 + embedding[8] * 6 + embedding[9] * 9;
    let score_9 = embedding[0] * 10 + embedding[1] * -7 + embedding[2] * -1 + embedding[3] * 3 + embedding[4] * 5 + embedding[5] * -6 + embedding[6] * -2 + embedding[7] * 8 + embedding[8] * 5 + embedding[9] * 21;
    let score_10 = embedding[0] * -3 + embedding[1] * 1 + embedding[2] * -5 + embedding[3] * -5 + embedding[4] * 2 + embedding[5] * 10 + embedding[6] * 1 + embedding[7] * -5 + embedding[8] * 1 + embedding[9] * -3;
    let score_11 = embedding[0] * 7 + embedding[1] * 6 + embedding[2] * -3 + embedding[3] * 6 + embedding[4] * -9 + embedding[6] * -14 + embedding[7] * 1 + embedding[8] * -5 + embedding[9] * -2;
    let score_12 = embedding[0] * 7 + embedding[1] * -3 + embedding[2] * -6 + embedding[3] * 4 + embedding[4] * -14 + embedding[5] * -6 + embedding[6] * 1 + embedding[7] * -10 + embedding[8] * 1;
    let score_13 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let score_14 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let score_15 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let score_16 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let score_17 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 22;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 11;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 29;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 23;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 108;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 80;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 93;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_277(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 9 + embedding[1] * -10 + embedding[2] * -9 + embedding[3] * -9 + embedding[4] * 2 + embedding[6] * -12 + embedding[7] * -11 + embedding[8] * 5 + embedding[9] * -2;
    let score_1 = embedding[1] * -2 + embedding[2] * -3 + embedding[3] * -10 + embedding[4] * 13 + embedding[5] * 5 + embedding[6] * 29 + embedding[8] * 16 + embedding[9] * 9;
    let score_2 = embedding[0] * -5 + embedding[1] * 15 + embedding[2] * -7 + embedding[3] * -6 + embedding[5] * 13 + embedding[6] * 2 + embedding[7] * 2 + embedding[8] * -11 + embedding[9] * -18;
    let score_3 = embedding[0] * -13 + embedding[1] * 11 + embedding[2] * -4 + embedding[3] * 9 + embedding[4] * 12 + embedding[5] * 21 + embedding[6] * 1 + embedding[7] * -2 + embedding[8] * -7 + embedding[9] * 8;
    let score_4 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 3 + embedding[4] * 3 + embedding[5] * -11 + embedding[6] * -20 + embedding[7] * -11 + embedding[8] * 7 + embedding[9] * -12;
    let score_5 = embedding[0] * 20 + embedding[1] * -21 + embedding[2] * -21 + embedding[3] * 2 + embedding[4] * -32 + embedding[5] * -1 + embedding[6] * 4 + embedding[7] * 8 + embedding[8] * -10 + embedding[9] * 17;
    let score_6 = embedding[0] * 2 + embedding[1] * 7 + embedding[2] * 17 + embedding[3] * -3 + embedding[4] * -7 + embedding[5] * -12 + embedding[6] * -10 + embedding[7] * -3 + embedding[8] * -5 + embedding[9] * -22;
    let score_7 = embedding[0] * -5 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 14 + embedding[4] * 7 + embedding[5] * -8 + embedding[6] * -17 + embedding[7] * -11 + embedding[8] * -2 + embedding[9] * 2;
    let score_8 = embedding[0] * 6 + embedding[1] * -4 + embedding[2] * 18 + embedding[3] * -7 + embedding[4] * 5 + embedding[5] * -17 + embedding[6] * 2 + embedding[7] * -8 + embedding[8] * 13;
    let score_9 = embedding[0] * -17 + embedding[1] * -8 + embedding[2] * 8 + embedding[3] * 12 + embedding[4] * 23 + embedding[5] * -1 + embedding[6] * 11 + embedding[7] * 33 + embedding[8] * -7 + embedding[9] * -13;
    let score_10 = embedding[0] * 8 + embedding[1] * -19 + embedding[2] * -24 + embedding[3] * -2 + embedding[4] * -30 + embedding[5] * 3 + embedding[6] * 4 + embedding[7] * 16 + embedding[8] * -4 + embedding[9] * 12;
    let score_11 = embedding[0] * -23 + embedding[1] * 30 + embedding[2] * 7 + embedding[3] * -13 + embedding[4] * -9 + embedding[5] * 5 + embedding[6] * 9 + embedding[7] * 29 + embedding[8] * 5 + embedding[9] * 14;
    let score_12 = embedding[0] * 10 + embedding[1] * -10 + embedding[2] * -2 + embedding[3] * 6 + embedding[4] * -1 + embedding[5] * -10 + embedding[6] * -1 + embedding[7] * 8 + embedding[8] * 8 + embedding[9] * 28;
    let score_13 = embedding[0] * 4 + embedding[1] * -3 + embedding[2] * 21 + embedding[3] * -1 + embedding[4] * -10 + embedding[5] * -26 + embedding[6] * -14 + embedding[7] * -5 + embedding[8] * 4 + embedding[9] * -16;
    let score_14 = embedding[0] * 8 + embedding[1] * 3 + embedding[2] * -7 + embedding[3] * 10 + embedding[4] * -17 + embedding[5] * -3 + embedding[6] * -15 + embedding[7] * 2 + embedding[8] * -6;
    let score_15 = embedding[0] * 11 + embedding[1] * -7 + embedding[2] * -8 + embedding[3] * 6 + embedding[4] * -19 + embedding[5] * -13 + embedding[6] * -1 + embedding[7] * -10 + embedding[8] * 2 + embedding[9] * 2;
    let score_16 = embedding[0] * 8 + embedding[1] * -8 + embedding[2] * 11 + embedding[3] * 4 + embedding[4] * -6 + embedding[5] * -18 + embedding[6] * -9 + embedding[7] * 7 + embedding[8] * 7 + embedding[9] * 14;
    let score_17 = embedding[0] * 5 + embedding[1] * -1 + embedding[2] * 9 + embedding[3] * 14 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * -14 + embedding[7] * -5 + embedding[8] * -5 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 65;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 63;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 66;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 72;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 6;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 22;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 29;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 23;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 115;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 80;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 183;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 195;
    }
    best_palette
}

fn predict_pixel_278(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 6 + embedding[1] * -12 + embedding[2] * -12 + embedding[3] * -3 + embedding[4] * -6 + embedding[5] * -2 + embedding[6] * -13 + embedding[7] * -4 + embedding[8] * 6;
    let score_1 = embedding[1] * 13 + embedding[2] * -1 + embedding[3] * -4 + embedding[4] * -2 + embedding[5] * 2 + embedding[6] * 1 + embedding[8] * -15 + embedding[9] * -12;
    let score_2 = embedding[0] * 2 + embedding[1] * 3 + embedding[2] * 4 + embedding[3] * -6 + embedding[4] * 13 + embedding[5] * -2 + embedding[6] * 24 + embedding[7] * -1 + embedding[8] * 9 + embedding[9] * 10;
    let score_3 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 3 + embedding[3] * 9 + embedding[4] * 8 + embedding[5] * 9 + embedding[6] * 1 + embedding[7] * -2 + embedding[8] * -13 + embedding[9] * 9;
    let score_4 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * -1 + embedding[3] * -10 + embedding[4] * 3 + embedding[5] * 4 + embedding[6] * 23 + embedding[7] * 13 + embedding[8] * 18 + embedding[9] * 4;
    let score_5 = embedding[0] * -14 + embedding[1] * 12 + embedding[2] * -1 + embedding[3] * -1 + embedding[4] * -13 + embedding[5] * 9 + embedding[6] * -3 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * -20;
    let score_6 = embedding[0] * -20 + embedding[1] * 7 + embedding[2] * -2 + embedding[3] * 14 + embedding[4] * 2 + embedding[5] * 16 + embedding[6] * -9 + embedding[7] * 7 + embedding[8] * -2 + embedding[9] * 6;
    let score_7 = embedding[0] * 7 + embedding[1] * -4 + embedding[3] * -4 + embedding[4] * 2 + embedding[5] * -7 + embedding[6] * -5 + embedding[7] * -9 + embedding[8] * 1 + embedding[9] * -1;
    let score_8 = embedding[0] * 14 + embedding[1] * -4 + embedding[2] * 9 + embedding[3] * -2 + embedding[4] * -1 + embedding[5] * -15 + embedding[6] * -10 + embedding[7] * -6 + embedding[8] * -4 + embedding[9] * 3;
    let score_9 = embedding[0] * 6 + embedding[1] * 1 + embedding[2] * 7 + embedding[3] * -4 + embedding[4] * 8 + embedding[5] * -7 + embedding[6] * -1 + embedding[7] * -8 + embedding[8] * -1 + embedding[9] * -2;
    let score_10 = embedding[0] * 4 + embedding[1] * 2 + embedding[2] * 28 + embedding[4] * 6 + embedding[5] * -20 + embedding[6] * -6 + embedding[7] * -9 + embedding[8] * 4 + embedding[9] * -3;
    let score_11 = embedding[0] * -6 + embedding[1] * 5 + embedding[2] * -8 + embedding[3] * -4 + embedding[4] * 6 + embedding[5] * 11 + embedding[6] * 7 + embedding[7] * 2 + embedding[8] * 5 + embedding[9] * -2;
    let score_12 = embedding[0] * -2 + embedding[1] * -7 + embedding[2] * -6 + embedding[3] * -1 + embedding[4] * -6 + embedding[5] * -1 + embedding[6] * -11 + embedding[8] * 7 + embedding[9] * -4;
    let score_13 = embedding[0] * -13 + embedding[1] * 1 + embedding[2] * -8 + embedding[3] * -4 + embedding[5] * 12 + embedding[6] * 4 + embedding[7] * 8 + embedding[8] * 7 + embedding[9] * -1;
    let score_14 = embedding[0] * 19 + embedding[1] * -18 + embedding[2] * -17 + embedding[3] * 4 + embedding[4] * -34 + embedding[5] * -5 + embedding[6] * 1 + embedding[7] * 10 + embedding[8] * -11 + embedding[9] * 16;
    let score_15 = embedding[0] * 1 + embedding[1] * 3 + embedding[2] * -4 + embedding[3] * -4 + embedding[4] * 8 + embedding[5] * 4 + embedding[6] * 9 + embedding[7] * -1 + embedding[8] * 1;
    let score_16 = embedding[0] * 4 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * -2 + embedding[4] * -7 + embedding[5] * -21 + embedding[6] * -8 + embedding[7] * -1 + embedding[8] * -5 + embedding[9] * -19;
    let score_17 = embedding[0] * 2 + embedding[1] * -1 + embedding[2] * 14 + embedding[3] * 9 + embedding[4] * 6 + embedding[5] * -11 + embedding[6] * -12 + embedding[7] * -6 + embedding[8] * 2 + embedding[9] * 9;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 7;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 27;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 34;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 2;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 3;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 4;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 30;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 42;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 58;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 57;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 69;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 5;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 12;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 65;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 71;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 99;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 102;
    }
    best_palette
}

fn predict_pixel_279(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 6 + embedding[1] * -12 + embedding[2] * -13 + embedding[3] * -4 + embedding[4] * -6 + embedding[6] * -14 + embedding[7] * -5 + embedding[8] * 5 + embedding[9] * -1;
    let score_1 = embedding[0] * 1 + embedding[1] * 13 + embedding[2] * -2 + embedding[3] * -4 + embedding[4] * -1 + embedding[5] * 3 + embedding[6] * 1 + embedding[7] * -1 + embedding[8] * -16 + embedding[9] * -12;
    let score_2 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * 4 + embedding[3] * -7 + embedding[4] * 14 + embedding[5] * -2 + embedding[6] * 25 + embedding[7] * -2 + embedding[8] * 8 + embedding[9] * 10;
    let score_3 = embedding[0] * -4 + embedding[1] * 10 + embedding[2] * 2 + embedding[3] * 8 + embedding[4] * 9 + embedding[5] * 9 + embedding[6] * 1 + embedding[7] * -3 + embedding[8] * -14 + embedding[9] * 8;
    let score_4 = embedding[0] * -9 + embedding[1] * -1 + embedding[2] * -3 + embedding[3] * -8 + embedding[4] * 4 + embedding[5] * 8 + embedding[6] * 22 + embedding[7] * 14 + embedding[8] * 17 + embedding[9] * 5;
    let score_5 = embedding[0] * -16 + embedding[1] * 11 + embedding[2] * -7 + embedding[3] * -3 + embedding[4] * -8 + embedding[5] * 15 + embedding[7] * 17 + embedding[8] * -4 + embedding[9] * -15;
    let score_6 = embedding[0] * -19 + embedding[1] * 6 + embedding[2] * -5 + embedding[3] * 9 + embedding[4] * 2 + embedding[5] * 18 + embedding[6] * -5 + embedding[7] * 9 + embedding[9] * 5;
    let score_7 = embedding[0] * 7 + embedding[1] * -4 + embedding[3] * -5 + embedding[4] * 3 + embedding[5] * -6 + embedding[6] * -5 + embedding[7] * -9 + embedding[8] * -1 + embedding[9] * -1;
    let score_8 = embedding[0] * 14 + embedding[1] * -4 + embedding[2] * 9 + embedding[3] * -3 + embedding[4] * -1 + embedding[5] * -14 + embedding[6] * -11 + embedding[7] * -7 + embedding[8] * -6 + embedding[9] * 2;
    let score_9 = embedding[0] * 6 + embedding[1] * 1 + embedding[2] * 7 + embedding[3] * -4 + embedding[4] * 9 + embedding[5] * -6 + embedding[6] * -1 + embedding[7] * -8 + embedding[8] * -2 + embedding[9] * -2;
    let score_10 = embedding[0] * 1 + embedding[1] * 4 + embedding[2] * 32 + embedding[3] * 3 + embedding[4] * 5 + embedding[5] * -20 + embedding[6] * -12 + embedding[7] * -9 + embedding[8] * -1 + embedding[9] * -5;
    let score_11 = embedding[0] * -3 + embedding[1] * -7 + embedding[2] * -7 + embedding[3] * -2 + embedding[4] * -5 + embedding[5] * 1 + embedding[6] * -12 + embedding[8] * 6 + embedding[9] * -5;
    let score_12 = embedding[0] * 19 + embedding[1] * -18 + embedding[2] * -18 + embedding[3] * 3 + embedding[4] * -33 + embedding[5] * -4 + embedding[6] * 1 + embedding[7] * 10 + embedding[8] * -11 + embedding[9] * 16;
    let score_13 = embedding[0] * 1 + embedding[1] * 4 + embedding[2] * -4 + embedding[3] * -4 + embedding[4] * 8 + embedding[5] * 4 + embedding[6] * 9 + embedding[7] * -2;
    let score_14 = embedding[0] * 4 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * -2 + embedding[4] * -6 + embedding[5] * -20 + embedding[6] * -9 + embedding[7] * -2 + embedding[8] * -6 + embedding[9] * -19;
    let score_15 = embedding[0] * 2 + embedding[1] * -1 + embedding[2] * 13 + embedding[3] * 8 + embedding[4] * 7 + embedding[5] * -10 + embedding[6] * -12 + embedding[7] * -6 + embedding[8] * 1 + embedding[9] * 8;
    let score_16 = embedding[0] * 5 + embedding[1] * 2 + embedding[2] * 27 + embedding[3] * -4 + embedding[4] * 7 + embedding[5] * -21 + embedding[6] * 7 + embedding[7] * -5 + embedding[8] * 7 + embedding[9] * 1;
    let score_17 = embedding[1] * 4 + embedding[2] * -4 + embedding[3] * -4 + embedding[4] * 8 + embedding[5] * 6 + embedding[6] * 9 + embedding[7] * -2 + embedding[8] * -1;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 7;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 27;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 34;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 2;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 3;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 4;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 30;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 42;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 58;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 57;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 5;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 65;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 71;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 99;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 102;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 103;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 70;
    }
    best_palette
}

fn predict_pixel_280(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[1] * -3 + embedding[2] * -9 + embedding[3] * -11 + embedding[4] * 11 + embedding[5] * 9 + embedding[6] * -9 + embedding[7] * -15 + embedding[9] * -3;
    let score_1 = embedding[0] * 15 + embedding[1] * -8 + embedding[2] * -1 + embedding[3] * -7 + embedding[5] * -6 + embedding[6] * -13 + embedding[7] * -11 + embedding[8] * -7 + embedding[9] * 4;
    let score_2 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * -6 + embedding[3] * 29 + embedding[4] * 10 + embedding[5] * 3 + embedding[6] * 16 + embedding[7] * 6 + embedding[8] * 6 + embedding[9] * -11;
    let score_3 = embedding[0] * 20 + embedding[1] * -23 + embedding[2] * -23 + embedding[4] * -37 + embedding[5] * -1 + embedding[6] * -2 + embedding[7] * 9 + embedding[8] * -15 + embedding[9] * 16;
    let score_4 = embedding[0] * 23 + embedding[1] * -20 + embedding[2] * -5 + embedding[3] * -1 + embedding[4] * -26 + embedding[5] * -14 + embedding[6] * -15 + embedding[7] * -3 + embedding[8] * -13 + embedding[9] * 12;
    let score_5 = embedding[0] * -7 + embedding[1] * 13 + embedding[2] * 18 + embedding[3] * -7 + embedding[4] * -18 + embedding[5] * 19 + embedding[6] * 1 + embedding[7] * 11 + embedding[8] * 2 + embedding[9] * 9;
    let score_6 = embedding[0] * -20 + embedding[1] * 12 + embedding[2] * -11 + embedding[3] * 5 + embedding[4] * 10 + embedding[5] * -14 + embedding[6] * 1 + embedding[7] * 26 + embedding[8] * 5 + embedding[9] * 2;
    let score_7 = embedding[0] * 10 + embedding[1] * -6 + embedding[2] * 5 + embedding[4] * 13 + embedding[5] * -5 + embedding[6] * -13 + embedding[7] * 33 + embedding[8] * -12 + embedding[9] * -1;
    let score_8 = embedding[0] * -16 + embedding[1] * 22 + embedding[2] * 19 + embedding[3] * 11 + embedding[4] * 2 + embedding[5] * 2 + embedding[6] * 9 + embedding[7] * 18 + embedding[8] * 17 + embedding[9] * 1;
    let score_9 = embedding[0] * -32 + embedding[1] * 12 + embedding[2] * 4 + embedding[3] * 21 + embedding[4] * 11 + embedding[5] * -18 + embedding[6] * 21 + embedding[7] * -4 + embedding[8] * 5 + embedding[9] * -11;
    let score_10 = embedding[0] * -31 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 2 + embedding[4] * 16 + embedding[5] * -11 + embedding[6] * 5 + embedding[7] * 11 + embedding[8] * 14 + embedding[9] * -4;
    let score_11 = embedding[0] * -24 + embedding[1] * 24 + embedding[2] * 15 + embedding[3] * -9 + embedding[4] * -12 + embedding[5] * 7 + embedding[6] * 6 + embedding[7] * 25 + embedding[8] * 9 + embedding[9] * 10;
    let score_12 = embedding[0] * -19 + embedding[1] * 22 + embedding[2] * -5 + embedding[3] * 21 + embedding[4] * -9 + embedding[5] * -4 + embedding[6] * 17 + embedding[7] * 6 + embedding[8] * 6;
    let score_13 = embedding[0] * -29 + embedding[1] * 22 + embedding[2] * 41 + embedding[3] * -6 + embedding[4] * -8 + embedding[5] * -1 + embedding[6] * 15 + embedding[7] * 2 + embedding[8] * 12 + embedding[9] * -5;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 9;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 9;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 9;
    let score_17 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 9;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 42;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 22;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 28;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 20;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 8;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 12;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 15;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 11;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 76;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 29;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 35;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 52;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_281(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_1 = embedding[0] * 11 + embedding[1] * -4 + embedding[2] * -3 + embedding[3] * -15 + embedding[4] * 7 + embedding[5] * 7 + embedding[6] * -15 + embedding[7] * -14 + embedding[8] * 5 + embedding[9] * -1;
    let score_2 = embedding[0] * -10 + embedding[1] * -14 + embedding[2] * -7 + embedding[3] * 15 + embedding[4] * 11 + embedding[5] * 2 + embedding[6] * 10 + embedding[7] * 3 + embedding[8] * -6 + embedding[9] * -13;
    let score_3 = embedding[1] * -11 + embedding[2] * 28 + embedding[3] * -3 + embedding[4] * 6 + embedding[5] * 13 + embedding[6] * 12 + embedding[7] * 11 + embedding[8] * -14 + embedding[9] * -3;
    let score_4 = embedding[0] * -16 + embedding[1] * 9 + embedding[2] * -16 + embedding[3] * 4 + embedding[4] * 8 + embedding[5] * -21 + embedding[6] * -4 + embedding[7] * 25 + embedding[8] * 7 + embedding[9] * -1;
    let score_5 = embedding[0] * -27 + embedding[1] * 5 + embedding[2] * -11 + embedding[3] * 14 + embedding[4] * -1 + embedding[5] * -22 + embedding[6] * 16 + embedding[7] * -10 + embedding[8] * 6 + embedding[9] * -11;
    let score_6 = embedding[0] * 12 + embedding[1] * -7 + embedding[2] * 8 + embedding[3] * -2 + embedding[4] * 14 + embedding[6] * -14 + embedding[7] * 33 + embedding[8] * -6 + embedding[9] * -3;
    let score_7 = embedding[0] * -10 + embedding[1] * 21 + embedding[3] * -18 + embedding[4] * -17 + embedding[5] * -4 + embedding[6] * -9 + embedding[7] * 15 + embedding[8] * 10 + embedding[9] * 14;
    let score_8 = embedding[0] * -16 + embedding[1] * 18 + embedding[2] * -15 + embedding[3] * 15 + embedding[4] * -14 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * -7 + embedding[8] * 9;
    let score_9 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 7;
    let score_17 = embedding[0] * 7 + embedding[1] * 2 + embedding[2] * -10 + embedding[3] * -12 + embedding[4] * 10 + embedding[5] * 11 + embedding[6] * -7 + embedding[7] * -19 + embedding[8] * 7 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 9;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 8;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 11;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 29;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 35;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_282(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_1 = embedding[0] * 7 + embedding[1] * -3 + embedding[2] * 12 + embedding[3] * -20 + embedding[4] * 11 + embedding[5] * 13 + embedding[6] * -8 + embedding[7] * 1 + embedding[8] * -3;
    let score_2 = embedding[0] * 12 + embedding[1] * -13 + embedding[2] * 76 + embedding[3] * -15 + embedding[4] * -2 + embedding[5] * 13 + embedding[6] * 6 + embedding[7] * 41 + embedding[8] * -31 + embedding[9] * 5;
    let score_3 = embedding[0] * -15 + embedding[1] * 7 + embedding[2] * -28 + embedding[3] * 11 + embedding[4] * 5 + embedding[5] * -26 + embedding[6] * -9 + embedding[7] * 27 + embedding[8] * 11 + embedding[9] * -2;
    let score_4 = embedding[0] * 13 + embedding[1] * -10 + embedding[2] * 2 + embedding[3] * -4 + embedding[4] * 16 + embedding[5] * -2 + embedding[6] * -17 + embedding[7] * 47 + embedding[8] * -3 + embedding[9] * -5;
    let score_5 = embedding[0] * -7 + embedding[1] * -21 + embedding[2] * -5 + embedding[3] * 17 + embedding[4] * 17 + embedding[5] * -20 + embedding[6] * -6 + embedding[7] * 58 + embedding[8] * -6 + embedding[9] * -14;
    let score_6 = embedding[0] * -29 + embedding[1] * 10 + embedding[2] * -23 + embedding[3] * 13 + embedding[4] * -7 + embedding[5] * -26 + embedding[6] * 8 + embedding[7] * -24 + embedding[8] * 15 + embedding[9] * -7;
    let score_7 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_8 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_9 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_10 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_11 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_12 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_13 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_14 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_15 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_16 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_17 = embedding[0] * 9 + embedding[1] * 9 + embedding[2] * -23 + embedding[3] * -13 + embedding[4] * 4 + embedding[5] * 15 + embedding[6] * -9 + embedding[7] * -35 + embedding[8] * 15 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 11;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_283(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -13 + embedding[9] * 7;
    let score_1 = embedding[0] * 3 + embedding[1] * 12 + embedding[2] * 6 + embedding[3] * -7 + embedding[4] * 12 + embedding[5] * 12 + embedding[6] * 4 + embedding[7] * -21 + embedding[8] * 9 + embedding[9] * -6;
    let score_2 = embedding[0] * -4 + embedding[1] * -9 + embedding[2] * 7 + embedding[3] * -15 + embedding[4] * -5 + embedding[5] * 11 + embedding[6] * -1 + embedding[7] * 7 + embedding[8] * -5 + embedding[9] * 3;
    let score_3 = embedding[0] * -8 + embedding[1] * -4 + embedding[2] * -20 + embedding[3] * 9 + embedding[4] * 2 + embedding[5] * -25 + embedding[6] * -9 + embedding[7] * 39 + embedding[8] * 4 + embedding[9] * 5;
    let score_4 = embedding[0] * 13 + embedding[1] * -12 + embedding[2] * -1 + embedding[3] * 5 + embedding[4] * 7 + embedding[5] * -6 + embedding[6] * -15 + embedding[7] * 47 + embedding[8] * -3 + embedding[9] * -3;
    let score_5 = embedding[0] * -11 + embedding[1] * -27 + embedding[2] * -5 + embedding[3] * 20 + embedding[4] * 9 + embedding[5] * -26 + embedding[6] * -6 + embedding[7] * 72 + embedding[8] * -10 + embedding[9] * -8;
    let score_6 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 23 + embedding[3] * -10 + embedding[4] * 11 + embedding[5] * 9 + embedding[6] * 10 + embedding[7] * 21 + embedding[8] * -3 + embedding[9] * -2;
    let score_7 = embedding[0] * -14 + embedding[1] * 2 + embedding[2] * 1 + embedding[3] * 8 + embedding[4] * 17 + embedding[5] * -20 + embedding[6] * -2 + embedding[7] * 60 + embedding[8] * -1 + embedding[9] * -3;
    let score_8 = embedding[0] * -15 + embedding[1] * -14 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * 22 + embedding[5] * -13 + embedding[6] * 10 + embedding[7] * 71 + embedding[8] * -14 + embedding[9] * -15;
    let score_9 = embedding[0] * 1 + embedding[2] * 21 + embedding[3] * 10 + embedding[4] * 20 + embedding[5] * -2 + embedding[6] * 1 + embedding[7] * 56 + embedding[8] * -7 + embedding[9] * -7;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -13 + embedding[9] * 7;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -13 + embedding[9] * 7;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -13 + embedding[9] * 7;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -13 + embedding[9] * 7;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -13 + embedding[9] * 7;
    let score_15 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -13 + embedding[9] * 7;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -13 + embedding[9] * 7;
    let score_17 = embedding[0] * 12 + embedding[1] * 2 + embedding[2] * -16 + embedding[3] * -9 + embedding[4] * 2 + embedding[5] * 11 + embedding[6] * -7 + embedding[7] * -28 + embedding[8] * 10 + embedding[9] * -1;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 31;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 152;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 188;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 167;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 187;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_284(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_1 = embedding[0] * -4 + embedding[1] * -8 + embedding[2] * 11 + embedding[3] * -10 + embedding[4] * -30 + embedding[5] * 13 + embedding[6] * 16 + embedding[7] * -28 + embedding[8] * 4 + embedding[9] * -5;
    let score_2 = embedding[0] * -13 + embedding[1] * -4 + embedding[2] * -14 + embedding[3] * -13 + embedding[4] * 6 + embedding[6] * -5 + embedding[7] * 13 + embedding[8] * -2 + embedding[9] * 1;
    let score_3 = embedding[0] * -21 + embedding[1] * -6 + embedding[2] * 1 + embedding[3] * -6 + embedding[4] * -22 + embedding[5] * 6 + embedding[6] * 16 + embedding[7] * 5 + embedding[8] * -11 + embedding[9] * 5;
    let score_4 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_17 = embedding[0] * 14 + embedding[2] * -15 + embedding[3] * -13 + embedding[4] * 26 + embedding[5] * 5 + embedding[6] * -23 + embedding[7] * -7 + embedding[8] * 11 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 46;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 175;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_285(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_1 = embedding[0] * -14 + embedding[1] * -6 + embedding[2] * -14 + embedding[3] * -14 + embedding[4] * 3 + embedding[5] * 4 + embedding[6] * 2 + embedding[7] * 3 + embedding[8] * -2 + embedding[9] * -1;
    let score_2 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_3 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_4 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_5 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_6 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_7 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_8 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_9 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_10 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_11 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_12 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_13 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_14 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_15 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_16 = embedding[0] * -13 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_17 = embedding[0] * 10 + embedding[1] * -3 + embedding[2] * -12 + embedding[3] * -16 + embedding[4] * 20 + embedding[5] * 10 + embedding[6] * -14 + embedding[7] * -21 + embedding[8] * 12 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_286(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_287(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_288(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_289(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_290(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_291(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_1 = embedding[0] * -17 + embedding[1] * -9 + embedding[2] * 4 + embedding[3] * -33 + embedding[4] * 13 + embedding[5] * -3 + embedding[6] * -9 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 3;
    let score_2 = embedding[0] * 25 + embedding[1] * -13 + embedding[2] * 14 + embedding[3] * 37 + embedding[4] * 9 + embedding[5] * 2 + embedding[6] * 8 + embedding[7] * -2 + embedding[8] * -4 + embedding[9] * -12;
    let score_3 = embedding[0] * 16 + embedding[1] * 1 + embedding[3] * 20 + embedding[4] * -6 + embedding[5] * 20 + embedding[6] * -6 + embedding[8] * 17 + embedding[9] * -9;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_6 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 12;
    let score_17 = embedding[0] * 3 + embedding[1] * 6 + embedding[2] * -20 + embedding[3] * -16 + embedding[4] * 14 + embedding[5] * 10 + embedding[6] * -15 + embedding[7] * -22 + embedding[8] * 16 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 13;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 14;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_292(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_1 = embedding[0] * -11 + embedding[1] * 1 + embedding[2] * -6 + embedding[3] * -25 + embedding[4] * -5 + embedding[5] * 14 + embedding[6] * -8 + embedding[7] * -19 + embedding[8] * 9 + embedding[9] * 1;
    let score_2 = embedding[0] * -2 + embedding[1] * 23 + embedding[2] * -27 + embedding[3] * 1 + embedding[4] * 24 + embedding[5] * -35 + embedding[6] * -12 + embedding[7] * 34 + embedding[8] * 10 + embedding[9] * 1;
    let score_3 = embedding[0] * 10 + embedding[1] * -20 + embedding[2] * 57 + embedding[3] * 2 + embedding[4] * 5 + embedding[5] * 15 + embedding[6] * 9 + embedding[7] * 21 + embedding[8] * -30 + embedding[9] * -4;
    let score_4 = embedding[0] * 22 + embedding[1] * -23 + embedding[2] * 27 + embedding[3] * 24 + embedding[4] * 9 + embedding[5] * 8 + embedding[6] * 14 + embedding[7] * 6 + embedding[8] * -21 + embedding[9] * -10;
    let score_5 = embedding[0] * 15 + embedding[1] * -14 + embedding[3] * 3 + embedding[4] * 18 + embedding[5] * -5 + embedding[6] * -11 + embedding[7] * 48 + embedding[8] * -7 + embedding[9] * -9;
    let score_6 = embedding[0] * -11 + embedding[1] * -22 + embedding[2] * -3 + embedding[3] * 12 + embedding[4] * 19 + embedding[5] * -19 + embedding[6] * -6 + embedding[7] * 67 + embedding[8] * -9 + embedding[9] * -14;
    let score_7 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_8 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_9 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_10 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_11 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_12 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_13 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_14 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_15 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_16 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_17 = embedding[1] * 10 + embedding[2] * -18 + embedding[3] * -21 + embedding[4] * 4 + embedding[5] * 16 + embedding[6] * -8 + embedding[7] * -34 + embedding[8] * 14 + embedding[9] * -2;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 9;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 13;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 12;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 6;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_293(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 20 + embedding[1] * -4 + embedding[2] * 7 + embedding[3] * -26 + embedding[4] * 21 + embedding[5] * 8 + embedding[6] * -9 + embedding[7] * -21 + embedding[8] * -13;
    let score_1 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 6;
    let score_2 = embedding[0] * -16 + embedding[1] * -19 + embedding[2] * -17 + embedding[3] * 32 + embedding[4] * 2 + embedding[5] * 7 + embedding[6] * 12 + embedding[7] * 8 + embedding[8] * 2 + embedding[9] * -17;
    let score_3 = embedding[0] * -16 + embedding[1] * 11 + embedding[2] * -27 + embedding[3] * 13 + embedding[4] * 1 + embedding[5] * -23 + embedding[6] * -11 + embedding[7] * 39 + embedding[8] * 16 + embedding[9] * 1;
    let score_4 = embedding[0] * 20 + embedding[1] * -17 + embedding[2] * 13 + embedding[3] * -8 + embedding[4] * 23 + embedding[5] * -5 + embedding[6] * -16 + embedding[7] * 45 + embedding[8] * -20 + embedding[9] * -4;
    let score_5 = embedding[0] * -36 + embedding[1] * 1 + embedding[2] * -23 + embedding[3] * 27 + embedding[4] * -13 + embedding[5] * -20 + embedding[6] * 12 + embedding[7] * -8 + embedding[8] * 15 + embedding[9] * -15;
    let score_6 = embedding[0] * 11 + embedding[1] * -4 + embedding[2] * 1 + embedding[3] * -6 + embedding[4] * -16 + embedding[5] * 3 + embedding[6] * -2 + embedding[7] * -16 + embedding[8] * 6 + embedding[9] * -2;
    let score_7 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 6;
    let score_8 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 6;
    let score_9 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 6;
    let score_10 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 6;
    let score_11 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 6;
    let score_12 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 6;
    let score_13 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 6;
    let score_14 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 6;
    let score_15 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 6;
    let score_16 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 6;
    let score_17 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * -20 + embedding[3] * 1 + embedding[4] * -15 + embedding[5] * 18 + embedding[6] * -6 + embedding[7] * -6 + embedding[8] * 26 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 11;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 93;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_294(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 3 + embedding[1] * -9 + embedding[2] * -10 + embedding[3] * -14 + embedding[4] * 8 + embedding[5] * 11 + embedding[6] * -13 + embedding[7] * -9 + embedding[8] * 1 + embedding[9] * -4;
    let score_1 = embedding[0] * 11 + embedding[1] * 3 + embedding[2] * -3 + embedding[3] * -9 + embedding[4] * 4 + embedding[5] * 7 + embedding[6] * -3 + embedding[7] * -6 + embedding[8] * -15 + embedding[9] * -12;
    let score_2 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * 1 + embedding[3] * -10 + embedding[4] * 13 + embedding[5] * 3 + embedding[6] * 12 + embedding[7] * -9 + embedding[8] * 1 + embedding[9] * 2;
    let score_3 = embedding[0] * 7 + embedding[1] * -1 + embedding[2] * -2 + embedding[3] * 3 + embedding[4] * 12 + embedding[5] * 10 + embedding[6] * -7 + embedding[7] * -12 + embedding[8] * -14 + embedding[9] * 6;
    let score_4 = embedding[0] * 6 + embedding[1] * -4 + embedding[2] * 15 + embedding[3] * -3 + embedding[4] * 7 + embedding[5] * -6 + embedding[6] * -12 + embedding[7] * -13 + embedding[8] * -2 + embedding[9] * -8;
    let score_5 = embedding[0] * -11 + embedding[1] * -11 + embedding[2] * -9 + embedding[3] * 22 + embedding[5] * 4 + embedding[6] * 5 + embedding[7] * 8 + embedding[8] * 10 + embedding[9] * -10;
    let score_6 = embedding[0] * -9 + embedding[1] * 17 + embedding[2] * 9 + embedding[3] * 15 + embedding[4] * -16 + embedding[5] * -6 + embedding[6] * -9 + embedding[7] * 36 + embedding[8] * 36 + embedding[9] * 5;
    let score_7 = embedding[0] * -32 + embedding[1] * -6 + embedding[2] * -12 + embedding[3] * 18 + embedding[4] * -7 + embedding[5] * -24 + embedding[6] * 12 + embedding[7] * -7 + embedding[8] * 6 + embedding[9] * -10;
    let score_8 = embedding[0] * 9 + embedding[1] * 2 + embedding[2] * -14 + embedding[3] * -7 + embedding[4] * 7 + embedding[5] * 12 + embedding[6] * 5 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 5;
    let score_9 = embedding[0] * -22 + embedding[1] * 21 + embedding[2] * 2 + embedding[3] * -16 + embedding[4] * -27 + embedding[5] * -10 + embedding[6] * -14 + embedding[7] * 41 + embedding[8] * 19 + embedding[9] * 15;
    let score_10 = embedding[0] * 10 + embedding[1] * -9 + embedding[3] * 6 + embedding[4] * 8 + embedding[5] * -4 + embedding[6] * -2 + embedding[7] * 6 + embedding[8] * 3 + embedding[9] * 20;
    let score_11 = embedding[0] * -26 + embedding[1] * 8 + embedding[2] * -12 + embedding[3] * 17 + embedding[4] * -26 + embedding[5] * -8 + embedding[6] * 9 + embedding[7] * 11 + embedding[8] * 14 + embedding[9] * 1;
    let score_12 = embedding[0] * -25 + embedding[1] * 17 + embedding[2] * 35 + embedding[3] * -10 + embedding[4] * -11 + embedding[5] * -1 + embedding[6] * 12 + embedding[8] * 10 + embedding[9] * -5;
    let score_13 = embedding[0] * 7 + embedding[1] * 4 + embedding[2] * -5 + embedding[3] * 6 + embedding[4] * -11 + embedding[5] * 1 + embedding[6] * -17 + embedding[7] * 1 + embedding[8] * -10 + embedding[9] * -4;
    let score_14 = embedding[0] * 8 + embedding[1] * -5 + embedding[3] * 4 + embedding[4] * -12 + embedding[5] * -4 + embedding[6] * -1 + embedding[7] * -11 + embedding[8] * -1 + embedding[9] * -1;
    let score_15 = embedding[0] * -6 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let score_16 = embedding[0] * -6 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let score_17 = embedding[0] * -6 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 15;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 11;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 26;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 29;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 23;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 35;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 52;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 100;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 179;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_295(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[1] * -9 + embedding[2] * -6 + embedding[3] * -14 + embedding[4] * 8 + embedding[5] * 2 + embedding[6] * -15 + embedding[7] * -9 + embedding[8] * -1 + embedding[9] * -3;
    let score_1 = embedding[0] * 5 + embedding[1] * 5 + embedding[2] * -11 + embedding[3] * -9 + embedding[4] * 2 + embedding[5] * 7 + embedding[6] * -2 + embedding[7] * -2 + embedding[8] * -12 + embedding[9] * -17;
    let score_2 = embedding[0] * 7 + embedding[1] * -6 + embedding[2] * -7 + embedding[3] * -10 + embedding[4] * 15 + embedding[5] * 1 + embedding[6] * 18 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * 4;
    let score_3 = embedding[0] * -2 + embedding[2] * -9 + embedding[3] * 8 + embedding[4] * 13 + embedding[5] * 12 + embedding[6] * -8 + embedding[7] * -8 + embedding[8] * -10 + embedding[9] * 7;
    let score_4 = embedding[0] * -1 + embedding[1] * -5 + embedding[2] * 13 + embedding[3] * -2 + embedding[4] * 6 + embedding[5] * -12 + embedding[6] * -19 + embedding[7] * -12 + embedding[8] * 2 + embedding[9] * -11;
    let score_5 = embedding[0] * 16 + embedding[1] * -19 + embedding[2] * -2 + embedding[3] * -5 + embedding[4] * -17 + embedding[5] * -15 + embedding[6] * -19 + embedding[7] * -2 + embedding[8] * -9 + embedding[9] * 6;
    let score_6 = embedding[0] * -4 + embedding[1] * 11 + embedding[2] * 15 + embedding[3] * -7 + embedding[4] * -16 + embedding[5] * 15 + embedding[6] * -2 + embedding[7] * 13 + embedding[8] * 2 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 21 + embedding[2] * 16 + embedding[3] * 10 + embedding[4] * 3 + embedding[5] * 1 + embedding[6] * 7 + embedding[7] * 20 + embedding[8] * 21 + embedding[9] * 5;
    let score_8 = embedding[0] * -22 + embedding[1] * 4 + embedding[2] * 5 + embedding[3] * 11 + embedding[4] * -4 + embedding[5] * 11 + embedding[6] * 16 + embedding[7] * 19 + embedding[8] * 14 + embedding[9] * -4;
    let score_9 = embedding[0] * -28 + embedding[1] * 8 + embedding[2] * 1 + embedding[3] * 11 + embedding[4] * 8 + embedding[5] * -19 + embedding[6] * 21 + embedding[7] * -8 + embedding[8] * 3 + embedding[9] * -10;
    let score_10 = embedding[0] * -1 + embedding[1] * -16 + embedding[2] * -19 + embedding[3] * -5 + embedding[4] * -22 + embedding[5] * 5 + embedding[6] * -2 + embedding[7] * 19 + embedding[8] * -4 + embedding[9] * 8;
    let score_11 = embedding[0] * -18 + embedding[1] * 22 + embedding[2] * 6 + embedding[3] * -12 + embedding[4] * -11 + embedding[5] * 5 + embedding[6] * -2 + embedding[7] * 23 + embedding[8] * 9 + embedding[9] * 11;
    let score_12 = embedding[0] * 8 + embedding[1] * -8 + embedding[2] * -2 + embedding[3] * 8 + embedding[4] * 7 + embedding[5] * -9 + embedding[6] * -1 + embedding[7] * 7 + embedding[8] * 2 + embedding[9] * 23;
    let score_13 = embedding[0] * -21 + embedding[1] * 20 + embedding[2] * -6 + embedding[3] * 15 + embedding[4] * -14 + embedding[5] * -1 + embedding[6] * 19 + embedding[7] * 10 + embedding[8] * 7;
    let score_14 = embedding[0] * -30 + embedding[1] * 23 + embedding[2] * 43 + embedding[3] * -7 + embedding[4] * -11 + embedding[5] * -3 + embedding[6] * 15 + embedding[7] * 1 + embedding[8] * 10 + embedding[9] * -6;
    let score_15 = embedding[0] * 6 + embedding[1] * 3 + embedding[2] * -8 + embedding[3] * 8 + embedding[4] * -12 + embedding[5] * -1 + embedding[6] * -18 + embedding[7] * 2 + embedding[8] * -13 + embedding[9] * -1;
    let score_16 = embedding[0] * 11 + embedding[1] * -7 + embedding[2] * -1 + embedding[3] * -1 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * -4 + embedding[7] * -11 + embedding[8] * -4 + embedding[9] * 1;
    let score_17 = embedding[0] * -6 + embedding[1] * 6 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 61;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 20;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 15;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 6;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 11;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 22;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 29;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 23;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 35;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 52;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 100;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 93;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_296(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 1 + embedding[1] * -2 + embedding[2] * -5 + embedding[3] * -9 + embedding[4] * 11 + embedding[5] * 4 + embedding[6] * -3 + embedding[7] * -11 + embedding[8] * 1 + embedding[9] * -5;
    let score_1 = embedding[0] * -3 + embedding[1] * 5 + embedding[2] * -3 + embedding[3] * -11 + embedding[4] * 16 + embedding[5] * 7 + embedding[6] * 30 + embedding[7] * 1 + embedding[8] * 13 + embedding[9] * 10;
    let score_2 = embedding[0] * -7 + embedding[1] * 19 + embedding[2] * -9 + embedding[3] * -8 + embedding[4] * 2 + embedding[5] * 13 + embedding[6] * 6 + embedding[7] * 6 + embedding[8] * -14 + embedding[9] * -17;
    let score_3 = embedding[0] * -14 + embedding[1] * 15 + embedding[2] * -5 + embedding[3] * 8 + embedding[4] * 14 + embedding[5] * 20 + embedding[6] * 3 + embedding[8] * -10 + embedding[9] * 8;
    let score_4 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 3 + embedding[4] * 7 + embedding[5] * -11 + embedding[6] * -17 + embedding[7] * -11 + embedding[8] * 4 + embedding[9] * -12;
    let score_5 = embedding[0] * 14 + embedding[1] * -5 + embedding[2] * 6 + embedding[3] * -5 + embedding[4] * -1 + embedding[5] * -12 + embedding[6] * -10 + embedding[7] * -8 + embedding[8] * -5 + embedding[9] * 1;
    let score_6 = embedding[0] * 19 + embedding[1] * -28 + embedding[2] * -26 + embedding[3] * 2 + embedding[4] * -43 + embedding[5] * -4 + embedding[6] * 1 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 21;
    let score_7 = embedding[0] * 22 + embedding[1] * -22 + embedding[2] * -5 + embedding[3] * 1 + embedding[4] * -29 + embedding[5] * -19 + embedding[6] * -15 + embedding[7] * 2 + embedding[8] * -9 + embedding[9] * 13;
    let score_8 = embedding[0] * -1 + embedding[1] * 7 + embedding[2] * 14 + embedding[3] * -5 + embedding[4] * -8 + embedding[5] * -11 + embedding[6] * -9 + embedding[7] * -2 + embedding[8] * -5 + embedding[9] * -23;
    let score_9 = embedding[0] * 7 + embedding[1] * -29 + embedding[2] * -6 + embedding[3] * 8 + embedding[4] * -32 + embedding[5] * -16 + embedding[6] * -20 + embedding[7] * 13 + embedding[8] * 2 + embedding[9] * 15;
    let score_10 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 13 + embedding[3] * 11 + embedding[4] * 7 + embedding[5] * -4 + embedding[6] * -17 + embedding[7] * -10 + embedding[9] * -1;
    let score_11 = embedding[0] * 3 + embedding[1] * -3 + embedding[2] * 12 + embedding[3] * -7 + embedding[4] * 6 + embedding[5] * -13 + embedding[6] * 1 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -2;
    let score_12 = embedding[0] * -14 + embedding[1] * 26 + embedding[2] * 27 + embedding[3] * 25 + embedding[4] * 6 + embedding[5] * 3 + embedding[6] * 23 + embedding[7] * 30 + embedding[8] * 23;
    let score_13 = embedding[0] * -29 + embedding[1] * 11 + embedding[2] * 13 + embedding[3] * 18 + embedding[4] * -1 + embedding[5] * 20 + embedding[6] * 29 + embedding[7] * 29 + embedding[8] * 14 + embedding[9] * -3;
    let score_14 = embedding[0] * 7 + embedding[1] * -24 + embedding[2] * -6 + embedding[3] * 4 + embedding[4] * -26 + embedding[5] * -6 + embedding[6] * -8 + embedding[7] * 6 + embedding[8] * -2 + embedding[9] * 9;
    let score_15 = embedding[0] * 11 + embedding[1] * -9 + embedding[2] * -1 + embedding[3] * 6 + embedding[4] * -5 + embedding[5] * -13 + embedding[6] * 2 + embedding[7] * 10 + embedding[8] * 2 + embedding[9] * 33;
    let score_16 = embedding[0] * 5 + embedding[1] * -3 + embedding[2] * 15 + embedding[3] * -4 + embedding[4] * -9 + embedding[5] * -25 + embedding[6] * -12 + embedding[7] * -5 + embedding[8] * 2 + embedding[9] * -14;
    let score_17 = embedding[0] * 5 + embedding[1] * -3 + embedding[3] * 1 + embedding[4] * -8 + embedding[5] * -3 + embedding[6] * -12 + embedding[7] * -5 + embedding[8] * -3;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 43;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 28;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 63;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 41;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 66;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 72;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 15;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 6;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 61;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 23;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 115;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 80;
    }
    best_palette
}

fn predict_pixel_297(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 10 + embedding[1] * 2 + embedding[2] * -3 + embedding[3] * -4 + embedding[4] * 7 + embedding[7] * -9 + embedding[8] * -4 + embedding[9] * -1;
    let score_1 = embedding[0] * 6 + embedding[1] * -2 + embedding[2] * -2 + embedding[3] * -2 + embedding[4] * 4 + embedding[5] * -4 + embedding[7] * -8 + embedding[9] * -2;
    let score_2 = embedding[0] * -30 + embedding[1] * 15 + embedding[2] * 9 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * 13 + embedding[6] * -9 + embedding[7] * 20 + embedding[8] * -11 + embedding[9] * -15;
    let score_3 = embedding[0] * -35 + embedding[1] * 11 + embedding[2] * 11 + embedding[3] * 26 + embedding[4] * -9 + embedding[5] * 16 + embedding[6] * -13 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * -1;
    let score_4 = embedding[0] * -3 + embedding[1] * 2 + embedding[2] * 7 + embedding[3] * -14 + embedding[4] * -8 + embedding[5] * -2 + embedding[6] * 35 + embedding[7] * 23 + embedding[8] * 17 + embedding[9] * -6;
    let score_5 = embedding[0] * 6 + embedding[1] * 2 + embedding[2] * 4 + embedding[3] * -3 + embedding[4] * 6 + embedding[5] * -5 + embedding[6] * -2 + embedding[7] * -7 + embedding[9] * -4;
    let score_6 = embedding[0] * 14 + embedding[1] * 1 + embedding[2] * 6 + embedding[3] * -1 + embedding[4] * 3 + embedding[5] * -11 + embedding[6] * -3 + embedding[7] * -6 + embedding[8] * -6 + embedding[9] * 4;
    let score_7 = embedding[0] * 8 + embedding[1] * 2 + embedding[2] * 4 + embedding[3] * -2 + embedding[4] * 7 + embedding[5] * -5 + embedding[6] * 2 + embedding[7] * -8 + embedding[8] * -3 + embedding[9] * -2;
    let score_8 = embedding[0] * 17 + embedding[1] * -22 + embedding[2] * -23 + embedding[3] * 7 + embedding[4] * -42 + embedding[5] * -3 + embedding[6] * 6 + embedding[7] * 20 + embedding[8] * -11 + embedding[9] * 20;
    let score_9 = embedding[0] * -14 + embedding[1] * 8 + embedding[3] * 1 + embedding[4] * 2 + embedding[5] * 12 + embedding[6] * 10 + embedding[7] * 10 + embedding[8] * 6;
    let score_10 = embedding[0] * -15 + embedding[1] * 6 + embedding[4] * 1 + embedding[5] * 12 + embedding[6] * 9 + embedding[7] * 10 + embedding[8] * 7;
    let score_11 = embedding[0] * 7 + embedding[1] * 1 + embedding[2] * 2 + embedding[3] * -2 + embedding[4] * 4 + embedding[5] * -5 + embedding[7] * -8 + embedding[8] * -3;
    let score_12 = embedding[0] * -4 + embedding[1] * -3 + embedding[2] * 10 + embedding[3] * 6 + embedding[4] * -8 + embedding[5] * -8 + embedding[6] * -11 + embedding[7] * -1 + embedding[8] * 9 + embedding[9] * -2;
    let score_13 = embedding[0] * -1 + embedding[1] * -26 + embedding[2] * -1 + embedding[3] * 14 + embedding[4] * -35 + embedding[5] * -15 + embedding[6] * -18 + embedding[7] * 21 + embedding[8] * 7 + embedding[9] * 14;
    let score_14 = embedding[0] * -6 + embedding[1] * -6 + embedding[2] * -3 + embedding[3] * 3 + embedding[4] * -11 + embedding[5] * 1 + embedding[6] * -2 + embedding[7] * 10 + embedding[8] * 8 + embedding[9] * 3;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 3 + embedding[3] * 1 + embedding[4] * 4 + embedding[5] * 8 + embedding[6] * 11 + embedding[7] * 8 + embedding[8] * 9 + embedding[9] * 3;
    let score_16 = embedding[0] * -8 + embedding[1] * 3 + embedding[3] * 1 + embedding[5] * 5 + embedding[6] * 4 + embedding[7] * 6 + embedding[8] * 7 + embedding[9] * 2;
    let score_17 = embedding[0] * -1 + embedding[1] * -21 + embedding[3] * 9 + embedding[4] * -29 + embedding[5] * -7 + embedding[6] * -8 + embedding[7] * 12 + embedding[8] * 5 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 30;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 7;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 34;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 27;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 85;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 43;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 59;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 64;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 70;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 71;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 105;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 57;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 41;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 58;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 116;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 117;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 28;
    }
    best_palette
}

fn predict_pixel_298(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 8 + embedding[1] * -5 + embedding[2] * -6 + embedding[3] * -4 + embedding[4] * 4 + embedding[5] * -1 + embedding[6] * -6 + embedding[7] * -8 + embedding[8] * 2 + embedding[9] * -1;
    let score_1 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * 3 + embedding[3] * -5 + embedding[4] * 12 + embedding[5] * 1 + embedding[6] * 26 + embedding[7] * -1 + embedding[8] * 7 + embedding[9] * 12;
    let score_2 = embedding[0] * 1 + embedding[1] * 14 + embedding[2] * -4 + embedding[3] * -2 + embedding[4] * -2 + embedding[5] * 5 + embedding[6] * 3 + embedding[7] * 2 + embedding[8] * -17 + embedding[9] * -8;
    let score_3 = embedding[0] * -5 + embedding[1] * 12 + embedding[2] * 1 + embedding[3] * 9 + embedding[4] * 6 + embedding[5] * 10 + embedding[6] * 2 + embedding[7] * -1 + embedding[8] * -15 + embedding[9] * 10;
    let score_4 = embedding[0] * 5 + embedding[1] * -6 + embedding[2] * -4 + embedding[3] * -4 + embedding[4] * 3 + embedding[5] * -3 + embedding[6] * -4 + embedding[7] * -8 + embedding[8] * 3 + embedding[9] * -2;
    let score_5 = embedding[0] * -24 + embedding[1] * 13 + embedding[2] * 7 + embedding[3] * 10 + embedding[4] * -14 + embedding[5] * 11 + embedding[6] * -13 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * -16;
    let score_6 = embedding[0] * -28 + embedding[1] * 8 + embedding[2] * 9 + embedding[3] * 22 + embedding[4] * -5 + embedding[5] * 13 + embedding[6] * -18 + embedding[7] * 10 + embedding[8] * -9 + embedding[9] * -3;
    let score_7 = embedding[0] * 1 + embedding[1] * -1 + embedding[2] * 4 + embedding[3] * -16 + embedding[4] * -4 + embedding[5] * -3 + embedding[6] * 29 + embedding[7] * 17 + embedding[8] * 15 + embedding[9] * -6;
    let score_8 = embedding[0] * 6 + embedding[1] * -4 + embedding[2] * -2 + embedding[3] * -3 + embedding[4] * 4 + embedding[5] * -3 + embedding[6] * -7 + embedding[7] * -6 + embedding[8] * 3 + embedding[9] * -1;
    let score_9 = embedding[0] * 5 + embedding[1] * -1 + embedding[2] * 2 + embedding[3] * -4 + embedding[4] * 8 + embedding[5] * -4 + embedding[6] * -1 + embedding[7] * -8 + embedding[8] * 1 + embedding[9] * -2;
    let score_10 = embedding[0] * 4 + embedding[1] * -4 + embedding[2] * 6 + embedding[3] * -3 + embedding[4] * 2 + embedding[5] * -7 + embedding[6] * -8 + embedding[7] * -8 + embedding[8] * 5 + embedding[9] * -5;
    let score_11 = embedding[0] * 15 + embedding[1] * -20 + embedding[2] * -23 + embedding[3] * 3 + embedding[4] * -35 + embedding[5] * -1 + embedding[6] * 4 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 17;
    let score_12 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * -5 + embedding[3] * -4 + embedding[4] * 7 + embedding[5] * 8 + embedding[6] * 4 + embedding[7] * -1 + embedding[8] * 3 + embedding[9] * -4;
    let score_13 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * -2 + embedding[3] * -3 + embedding[4] * 3 + embedding[5] * 8 + embedding[6] * 3 + embedding[7] * 4 + embedding[8] * 5 + embedding[9] * -4;
    let score_14 = embedding[0] * -1 + embedding[1] * -3 + embedding[2] * 4 + embedding[3] * 3 + embedding[4] * 1 + embedding[5] * -5 + embedding[6] * -14 + embedding[7] * -7 + embedding[8] * 6 + embedding[9] * -2;
    let score_15 = embedding[0] * 3 + embedding[1] * -20 + embedding[2] * -3 + embedding[3] * 8 + embedding[4] * -21 + embedding[5] * -12 + embedding[6] * -18 + embedding[7] * 13 + embedding[8] * 7 + embedding[9] * 10;
    let score_16 = embedding[1] * -7 + embedding[2] * -5 + embedding[3] * -1 + embedding[4] * -5 + embedding[6] * -6 + embedding[7] * 3 + embedding[8] * 6 + embedding[9] * 1;
    let score_17 = embedding[0] * -1 + embedding[1] * 2 + embedding[2] * -2 + embedding[3] * -4 + embedding[4] * 10 + embedding[5] * 8 + embedding[6] * 9 + embedding[8] * 5 + embedding[9] * 4;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 48;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 47;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 49;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 30;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 34;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 27;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 43;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 59;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 86;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 64;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 70;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 71;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 57;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 41;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 58;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 96;
    }
    best_palette
}

fn predict_pixel_299(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 7 + embedding[1] * -7 + embedding[2] * -8 + embedding[3] * -11 + embedding[4] * 11 + embedding[5] * 3 + embedding[6] * -12 + embedding[7] * -13 + embedding[8] * 5 + embedding[9] * -7;
    let score_1 = embedding[0] * 3 + embedding[1] * -2 + embedding[2] * -3 + embedding[3] * -14 + embedding[4] * 15 + embedding[5] * 5 + embedding[6] * 16 + embedding[7] * -4 + embedding[8] * 16 + embedding[9] * 4;
    let score_2 = embedding[0] * -2 + embedding[1] * 11 + embedding[2] * -7 + embedding[3] * -11 + embedding[4] * 3 + embedding[5] * 11 + embedding[6] * -5 + embedding[8] * -8 + embedding[9] * -21;
    let score_3 = embedding[0] * -8 + embedding[1] * 7 + embedding[2] * -4 + embedding[3] * 5 + embedding[4] * 15 + embedding[5] * 17 + embedding[6] * -9 + embedding[7] * -6 + embedding[8] * -5 + embedding[9] * 4;
    let score_4 = embedding[0] * -4 + embedding[2] * 19 + embedding[3] * -1 + embedding[4] * 8 + embedding[5] * -10 + embedding[6] * -22 + embedding[7] * -12 + embedding[8] * 7 + embedding[9] * -15;
    let score_5 = embedding[0] * 15 + embedding[1] * -19 + embedding[2] * -23 + embedding[3] * -4 + embedding[4] * -24 + embedding[5] * 4 + embedding[6] * 1 + embedding[7] * 7 + embedding[8] * -5 + embedding[9] * 10;
    let score_6 = embedding[0] * 5 + embedding[1] * -14 + embedding[2] * -7 + embedding[3] * -5 + embedding[4] * -8 + embedding[5] * 2 + embedding[6] * -6 + embedding[7] * -6 + embedding[8] * 2;
    let score_7 = embedding[0] * 9 + embedding[1] * -7 + embedding[2] * -1 + embedding[3] * 1 + embedding[4] * 5 + embedding[5] * -6 + embedding[6] * -3 + embedding[7] * 5 + embedding[8] * 9 + embedding[9] * 23;
    let score_8 = embedding[0] * 7 + embedding[1] * 6 + embedding[2] * -7 + embedding[3] * 3 + embedding[4] * -10 + embedding[5] * 2 + embedding[6] * -16 + embedding[8] * -6 + embedding[9] * -4;
    let score_9 = embedding[0] * 10 + embedding[1] * -6 + embedding[2] * -4 + embedding[3] * -1 + embedding[4] * -15 + embedding[5] * -5 + embedding[6] * -2 + embedding[7] * -13 + embedding[8] * 3 + embedding[9] * -1;
    let score_10 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let score_11 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let score_12 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let score_13 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let score_14 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let score_15 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let score_16 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let score_17 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 64;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 28;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 80;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 93;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_300(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 6 + embedding[1] * -6 + embedding[2] * -6 + embedding[3] * -13 + embedding[4] * 16 + embedding[5] * 3 + embedding[6] * 18 + embedding[7] * -7 + embedding[8] * 14 + embedding[9] * 3;
    let score_1 = embedding[0] * 4 + embedding[1] * 9 + embedding[2] * -8 + embedding[3] * -12 + embedding[4] * 4 + embedding[5] * 10 + embedding[6] * -6 + embedding[7] * -1 + embedding[8] * -9 + embedding[9] * -21;
    let score_2 = embedding[0] * -4 + embedding[1] * 3 + embedding[2] * -6 + embedding[3] * 6 + embedding[4] * 15 + embedding[5] * 16 + embedding[6] * -11 + embedding[7] * -9 + embedding[8] * -6 + embedding[9] * 6;
    let score_3 = embedding[0] * 3 + embedding[1] * -7 + embedding[2] * -6 + embedding[3] * -10 + embedding[4] * 5 + embedding[5] * 1 + embedding[6] * -16 + embedding[7] * -11 + embedding[8] * 6 + embedding[9] * -6;
    let score_4 = embedding[0] * -3 + embedding[1] * -3 + embedding[2] * 19 + embedding[3] * -1 + embedding[4] * 6 + embedding[5] * -12 + embedding[6] * -23 + embedding[7] * -14 + embedding[8] * 8 + embedding[9] * -16;
    let score_5 = embedding[0] * 16 + embedding[1] * -21 + embedding[2] * -28 + embedding[3] * -1 + embedding[4] * -31 + embedding[5] * 6 + embedding[6] * 2 + embedding[7] * 11 + embedding[8] * -10 + embedding[9] * 15;
    let score_6 = embedding[0] * -28 + embedding[1] * 6 + embedding[2] * 2 + embedding[3] * 7 + embedding[4] * 9 + embedding[5] * -17 + embedding[6] * 14 + embedding[7] * -10 + embedding[8] * 5 + embedding[9] * -12;
    let score_7 = embedding[0] * 2 + embedding[1] * -14 + embedding[2] * -6 + embedding[3] * -4 + embedding[4] * -10 + embedding[5] * 2 + embedding[6] * -9 + embedding[7] * -4 + embedding[8] * 3 + embedding[9] * -1;
    let score_8 = embedding[0] * 8 + embedding[1] * -6 + embedding[2] * -4 + embedding[3] * 5 + embedding[4] * 10 + embedding[5] * -5 + embedding[6] * -1 + embedding[7] * 9 + embedding[8] * 7 + embedding[9] * 25;
    let score_9 = embedding[0] * -15 + embedding[1] * 23 + embedding[2] * 4 + embedding[3] * -16 + embedding[4] * -9 + embedding[6] * -7 + embedding[7] * 18 + embedding[8] * 6 + embedding[9] * 10;
    let score_10 = embedding[0] * 6 + embedding[1] * 9 + embedding[2] * -8 + embedding[3] * 8 + embedding[4] * -10 + embedding[5] * 4 + embedding[6] * -18 + embedding[7] * 2 + embedding[8] * -9 + embedding[9] * -5;
    let score_11 = embedding[0] * 8 + embedding[1] * -4 + embedding[2] * -3 + embedding[4] * -16 + embedding[5] * -3 + embedding[6] * -3 + embedding[7] * -12 + embedding[8] * 3 + embedding[9] * -1;
    let score_12 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 8;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 22;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 11;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 28;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 23;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 29;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 80;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 93;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_301(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 7 + embedding[1] * -8 + embedding[2] * -6 + embedding[3] * -10 + embedding[4] * 7 + embedding[6] * -10 + embedding[7] * -12 + embedding[8] * 5 + embedding[9] * -4;
    let score_1 = embedding[0] * 2 + embedding[1] * -3 + embedding[2] * -5 + embedding[3] * -15 + embedding[4] * 16 + embedding[5] * 5 + embedding[6] * 34 + embedding[7] * 1 + embedding[8] * 17 + embedding[9] * 14;
    let score_2 = embedding[0] * -2 + embedding[1] * 16 + embedding[2] * -13 + embedding[3] * -9 + embedding[4] * 1 + embedding[5] * 16 + embedding[6] * 1 + embedding[7] * 4 + embedding[8] * -17 + embedding[9] * -20;
    let score_3 = embedding[0] * -12 + embedding[1] * 12 + embedding[2] * -7 + embedding[3] * 12 + embedding[4] * 14 + embedding[5] * 25 + embedding[6] * -3 + embedding[7] * -1 + embedding[8] * -12 + embedding[9] * 11;
    let score_4 = embedding[0] * -5 + embedding[2] * 26 + embedding[3] * 1 + embedding[4] * 5 + embedding[5] * -15 + embedding[6] * -22 + embedding[7] * -12 + embedding[8] * 8 + embedding[9] * -14;
    let score_5 = embedding[0] * 16 + embedding[1] * -21 + embedding[2] * -28 + embedding[3] * -2 + embedding[4] * -30 + embedding[5] * 6 + embedding[6] * 9 + embedding[7] * 12 + embedding[8] * -10 + embedding[9] * 17;
    let score_6 = embedding[1] * 5 + embedding[2] * 13 + embedding[3] * -5 + embedding[4] * -9 + embedding[5] * -10 + embedding[6] * -14 + embedding[7] * -2 + embedding[8] * -4 + embedding[9] * -25;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 14 + embedding[3] * 13 + embedding[4] * 7 + embedding[5] * -4 + embedding[6] * -23 + embedding[7] * -11 + embedding[9] * 1;
    let score_8 = embedding[0] * 5 + embedding[1] * -7 + embedding[2] * 11 + embedding[3] * -9 + embedding[4] * 7 + embedding[5] * -13 + embedding[6] * 1 + embedding[7] * -8 + embedding[8] * 15;
    let score_9 = embedding[0] * 4 + embedding[1] * -14 + embedding[2] * -6 + embedding[3] * -5 + embedding[4] * -10 + embedding[6] * -3 + embedding[7] * -5 + embedding[8] * 2 + embedding[9] * 2;
    let score_10 = embedding[0] * 8 + embedding[1] * -8 + embedding[2] * -4 + embedding[3] * 6 + embedding[4] * 5 + embedding[5] * -10 + embedding[6] * 5 + embedding[7] * 9 + embedding[8] * 8 + embedding[9] * 31;
    let score_11 = embedding[0] * 1 + embedding[1] * -2 + embedding[2] * 26 + embedding[3] * -3 + embedding[4] * -7 + embedding[5] * -29 + embedding[6] * -17 + embedding[7] * -5 + embedding[8] * 5 + embedding[9] * -20;
    let score_12 = embedding[0] * 5 + embedding[1] * 9 + embedding[2] * -10 + embedding[3] * 9 + embedding[4] * -16 + embedding[5] * 3 + embedding[6] * -14 + embedding[7] * 4 + embedding[8] * -13 + embedding[9] * -1;
    let score_13 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -3 + embedding[3] * 1 + embedding[4] * -17 + embedding[5] * -7 + embedding[7] * -11 + embedding[8] * 2 + embedding[9] * 2;
    let score_14 = embedding[0] * 6 + embedding[1] * -7 + embedding[2] * 12 + embedding[3] * 3 + embedding[5] * -16 + embedding[6] * -5 + embedding[7] * 8 + embedding[8] * 7 + embedding[9] * 15;
    let score_15 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * 11 + embedding[3] * 13 + embedding[4] * -17 + embedding[5] * -4 + embedding[6] * -15 + embedding[7] * -5 + embedding[8] * -8 + embedding[9] * -6;
    let score_16 = embedding[0] * 1 + embedding[1] * 1 + embedding[2] * 11 + embedding[3] * 19 + embedding[4] * -12 + embedding[5] * -14 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -16 + embedding[9] * 4;
    let score_17 = embedding[0] * 1 + embedding[1] * 1 + embedding[2] * 11 + embedding[3] * 19 + embedding[4] * -12 + embedding[5] * -14 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -16 + embedding[9] * 4;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 65;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 63;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 66;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 72;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 28;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 23;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 115;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 80;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 93;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 183;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 195;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_302(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 10 + embedding[1] * 2 + embedding[2] * -3 + embedding[3] * -4 + embedding[4] * 7 + embedding[6] * 1 + embedding[7] * -9 + embedding[8] * -5 + embedding[9] * -1;
    let score_1 = embedding[0] * 6 + embedding[1] * -2 + embedding[2] * -2 + embedding[3] * -3 + embedding[4] * 4 + embedding[5] * -3 + embedding[7] * -8 + embedding[8] * -1 + embedding[9] * -2;
    let score_2 = embedding[0] * -27 + embedding[1] * 13 + embedding[2] * 6 + embedding[3] * 13 + embedding[4] * -17 + embedding[5] * 13 + embedding[6] * -10 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * -14;
    let score_3 = embedding[0] * -31 + embedding[1] * 8 + embedding[2] * 8 + embedding[3] * 24 + embedding[4] * -8 + embedding[5] * 14 + embedding[6] * -13 + embedding[7] * 14 + embedding[8] * -6;
    let score_4 = embedding[0] * -3 + embedding[1] * 1 + embedding[2] * 5 + embedding[3] * -14 + embedding[4] * -8 + embedding[5] * -3 + embedding[6] * 33 + embedding[7] * 21 + embedding[8] * 16 + embedding[9] * -6;
    let score_5 = embedding[0] * 6 + embedding[1] * 2 + embedding[2] * 3 + embedding[3] * -3 + embedding[4] * 6 + embedding[5] * -4 + embedding[6] * -1 + embedding[7] * -7 + embedding[9] * -4;
    let score_6 = embedding[0] * 14 + embedding[1] * 1 + embedding[2] * 6 + embedding[3] * -2 + embedding[4] * 2 + embedding[5] * -11 + embedding[6] * -2 + embedding[7] * -6 + embedding[8] * -8 + embedding[9] * 3;
    let score_7 = embedding[0] * 8 + embedding[1] * 2 + embedding[2] * 4 + embedding[3] * -2 + embedding[4] * 8 + embedding[5] * -5 + embedding[6] * 2 + embedding[7] * -8 + embedding[8] * -4 + embedding[9] * -1;
    let score_8 = embedding[0] * 16 + embedding[1] * -15 + embedding[2] * -17 + embedding[3] * 3 + embedding[4] * -23 + embedding[5] * -3 + embedding[6] * 2 + embedding[7] * 12 + embedding[8] * -5 + embedding[9] * 17;
    let score_9 = embedding[0] * -14 + embedding[1] * 8 + embedding[2] * -1 + embedding[3] * 1 + embedding[4] * 2 + embedding[5] * 12 + embedding[6] * 10 + embedding[7] * 9 + embedding[8] * 6;
    let score_10 = embedding[0] * -15 + embedding[1] * 5 + embedding[4] * 1 + embedding[5] * 12 + embedding[6] * 9 + embedding[7] * 10 + embedding[8] * 7;
    let score_11 = embedding[0] * -6 + embedding[1] * -4 + embedding[2] * 10 + embedding[3] * 6 + embedding[4] * -10 + embedding[5] * -8 + embedding[6] * -12 + embedding[8] * 9 + embedding[9] * -2;
    let score_12 = embedding[0] * 7 + embedding[1] * 1 + embedding[2] * 2 + embedding[3] * -2 + embedding[4] * 4 + embedding[5] * -4 + embedding[6] * 1 + embedding[7] * -7 + embedding[8] * -4;
    let score_13 = embedding[0] * -2 + embedding[1] * -25 + embedding[2] * 1 + embedding[3] * 15 + embedding[4] * -33 + embedding[5] * -15 + embedding[6] * -19 + embedding[7] * 20 + embedding[8] * 8 + embedding[9] * 13;
    let score_14 = embedding[0] * -7 + embedding[1] * -6 + embedding[2] * -2 + embedding[3] * 3 + embedding[4] * -10 + embedding[5] * 2 + embedding[6] * -2 + embedding[7] * 9 + embedding[8] * 8 + embedding[9] * 2;
    let score_15 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 2 + embedding[3] * 1 + embedding[4] * 4 + embedding[5] * 8 + embedding[6] * 11 + embedding[7] * 8 + embedding[8] * 9 + embedding[9] * 2;
    let score_16 = embedding[0] * -9 + embedding[1] * 3 + embedding[3] * 2 + embedding[5] * 5 + embedding[6] * 4 + embedding[7] * 6 + embedding[8] * 7 + embedding[9] * 1;
    let score_17 = embedding[0] * -3 + embedding[1] * -20 + embedding[2] * 1 + embedding[3] * 9 + embedding[4] * -26 + embedding[5] * -7 + embedding[6] * -8 + embedding[7] * 10 + embedding[8] * 5 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 30;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 7;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 34;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 27;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 85;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 43;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 59;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 65;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 70;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 71;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 57;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 105;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 41;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 58;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 116;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 117;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 28;
    }
    best_palette
}

fn predict_pixel_303(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 10 + embedding[1] * -6 + embedding[2] * -7 + embedding[3] * -3 + embedding[5] * -1 + embedding[6] * -6 + embedding[7] * -6 + embedding[8] * 1 + embedding[9] * 2;
    let score_1 = embedding[0] * 1 + embedding[1] * 13 + embedding[2] * -2 + embedding[3] * -4 + embedding[4] * 1 + embedding[5] * 4 + embedding[6] * 4 + embedding[7] * -1 + embedding[8] * -15 + embedding[9] * -9;
    let score_2 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * 4 + embedding[3] * -6 + embedding[4] * 14 + embedding[5] * -1 + embedding[6] * 25 + embedding[7] * -2 + embedding[8] * 7 + embedding[9] * 10;
    let score_3 = embedding[0] * -4 + embedding[1] * 11 + embedding[2] * 3 + embedding[3] * 8 + embedding[4] * 8 + embedding[5] * 9 + embedding[6] * 3 + embedding[7] * -2 + embedding[8] * -14 + embedding[9] * 8;
    let score_4 = embedding[0] * 4 + embedding[1] * -5 + embedding[2] * -4 + embedding[3] * -4 + embedding[4] * 3 + embedding[5] * -3 + embedding[6] * -4 + embedding[7] * -8 + embedding[8] * 3 + embedding[9] * -2;
    let score_5 = embedding[0] * -21 + embedding[1] * 12 + embedding[2] * 5 + embedding[3] * 8 + embedding[4] * -13 + embedding[5] * 11 + embedding[6] * -12 + embedding[7] * 12 + embedding[8] * -11 + embedding[9] * -15;
    let score_6 = embedding[0] * -24 + embedding[1] * 6 + embedding[2] * 7 + embedding[3] * 19 + embedding[4] * -4 + embedding[5] * 12 + embedding[6] * -17 + embedding[7] * 7 + embedding[8] * -7 + embedding[9] * -2;
    let score_7 = embedding[1] * -1 + embedding[2] * 3 + embedding[3] * -16 + embedding[4] * -4 + embedding[5] * -4 + embedding[6] * 28 + embedding[7] * 16 + embedding[8] * 15 + embedding[9] * -7;
    let score_8 = embedding[0] * 5 + embedding[1] * -3 + embedding[2] * -3 + embedding[3] * -3 + embedding[4] * 3 + embedding[5] * -3 + embedding[6] * -7 + embedding[7] * -5 + embedding[8] * 3 + embedding[9] * -1;
    let score_9 = embedding[0] * 4 + embedding[1] * 1 + embedding[2] * 3 + embedding[3] * -4 + embedding[4] * 9 + embedding[5] * -3 + embedding[6] * 1 + embedding[7] * -7 + embedding[9] * -2;
    let score_10 = embedding[0] * 2 + embedding[1] * -3 + embedding[2] * 7 + embedding[3] * -3 + embedding[4] * 2 + embedding[5] * -7 + embedding[6] * -8 + embedding[7] * -8 + embedding[8] * 5 + embedding[9] * -5;
    let score_11 = embedding[0] * 16 + embedding[1] * -21 + embedding[2] * -23 + embedding[3] * 4 + embedding[4] * -36 + embedding[5] * -2 + embedding[6] * 4 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 18;
    let score_12 = embedding[0] * -3 + embedding[1] * 6 + embedding[2] * -4 + embedding[3] * -4 + embedding[4] * 8 + embedding[5] * 8 + embedding[6] * 5 + embedding[7] * -1 + embedding[8] * 3 + embedding[9] * -4;
    let score_13 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * -2 + embedding[3] * -3 + embedding[4] * 3 + embedding[5] * 8 + embedding[6] * 3 + embedding[7] * 4 + embedding[8] * 5 + embedding[9] * -4;
    let score_14 = embedding[0] * -2 + embedding[1] * -3 + embedding[2] * 5 + embedding[3] * 3 + embedding[5] * -5 + embedding[6] * -14 + embedding[7] * -7 + embedding[8] * 6 + embedding[9] * -3;
    let score_15 = embedding[0] * 2 + embedding[1] * -20 + embedding[2] * -4 + embedding[3] * 8 + embedding[4] * -22 + embedding[5] * -12 + embedding[6] * -17 + embedding[7] * 13 + embedding[8] * 7 + embedding[9] * 11;
    let score_16 = embedding[1] * -7 + embedding[2] * -5 + embedding[3] * -1 + embedding[4] * -6 + embedding[6] * -6 + embedding[7] * 4 + embedding[8] * 6 + embedding[9] * 1;
    let score_17 = embedding[0] * -2 + embedding[1] * 3 + embedding[2] * -2 + embedding[3] * -4 + embedding[4] * 11 + embedding[5] * 8 + embedding[6] * 10 + embedding[8] * 5 + embedding[9] * 4;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 47;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 48;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 49;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 30;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 34;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 27;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 43;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 59;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 86;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 65;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 70;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 71;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 57;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 41;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 58;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 96;
    }
    best_palette
}

fn predict_pixel_304(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * -5 + embedding[3] * -10 + embedding[4] * 18 + embedding[5] * 8 + embedding[6] * -4 + embedding[7] * -16 + embedding[8] * 2 + embedding[9] * -7;
    let score_1 = embedding[0] * 10 + embedding[1] * -1 + embedding[2] * 3 + embedding[3] * -6 + embedding[4] * 6 + embedding[5] * -7 + embedding[6] * -8 + embedding[7] * -11 + embedding[8] * -5;
    let score_2 = embedding[0] * -11 + embedding[1] * 9 + embedding[2] * -1 + embedding[3] * 26 + embedding[4] * 16 + embedding[5] * 6 + embedding[6] * 16 + embedding[7] * 6 + embedding[8] * 8 + embedding[9] * -13;
    let score_3 = embedding[0] * 16 + embedding[1] * -25 + embedding[2] * -24 + embedding[3] * 2 + embedding[4] * -40 + embedding[5] * -3 + embedding[6] * -1 + embedding[7] * 14 + embedding[8] * -12 + embedding[9] * 17;
    let score_4 = embedding[0] * 18 + embedding[1] * -18 + embedding[2] * -5 + embedding[4] * -24 + embedding[5] * -14 + embedding[6] * -13 + embedding[7] * -1 + embedding[8] * -9 + embedding[9] * 10;
    let score_5 = embedding[0] * 6 + embedding[1] * -27 + embedding[2] * -7 + embedding[3] * 8 + embedding[4] * -27 + embedding[5] * -12 + embedding[6] * -21 + embedding[7] * 14 + embedding[8] * 2 + embedding[9] * 14;
    let score_6 = embedding[0] * -12 + embedding[1] * 19 + embedding[2] * 22 + embedding[3] * -5 + embedding[4] * -12 + embedding[5] * 18 + embedding[6] * 4 + embedding[7] * 11 + embedding[8] * 4 + embedding[9] * 7;
    let score_7 = embedding[0] * 6 + embedding[1] * 1 + embedding[2] * 8 + embedding[3] * 1 + embedding[4] * 18 + embedding[5] * -6 + embedding[6] * -9 + embedding[7] * 33 + embedding[8] * -10 + embedding[9] * -3;
    let score_8 = embedding[0] * -16 + embedding[1] * 29 + embedding[2] * 22 + embedding[3] * 10 + embedding[4] * 6 + embedding[5] * 3 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * 19;
    let score_9 = embedding[0] * 4 + embedding[1] * -21 + embedding[2] * -8 + embedding[3] * 4 + embedding[4] * -22 + embedding[5] * -2 + embedding[6] * -8 + embedding[7] * 4 + embedding[8] * -2 + embedding[9] * 8;
    let score_10 = embedding[0] * -27 + embedding[1] * 32 + embedding[2] * 18 + embedding[3] * -9 + embedding[4] * -6 + embedding[5] * 7 + embedding[6] * 10 + embedding[7] * 24 + embedding[8] * 11 + embedding[9] * 8;
    let score_11 = embedding[0] * -21 + embedding[1] * 30 + embedding[2] * 2 + embedding[3] * 13 + embedding[4] * -8 + embedding[5] * 1 + embedding[6] * 17 + embedding[7] * 6 + embedding[8] * 8 + embedding[9] * 1;
    let score_12 = embedding[0] * -30 + embedding[1] * 30 + embedding[2] * 42 + embedding[3] * -7 + embedding[4] * -5 + embedding[6] * 17 + embedding[8] * 12 + embedding[9] * -6;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_17 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 43;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 22;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 28;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 41;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 20;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 12;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 15;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 61;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 29;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 35;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 52;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_305(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_1 = embedding[0] * 12 + embedding[1] * -8 + embedding[2] * -3 + embedding[3] * -16 + embedding[4] * 7 + embedding[5] * 10 + embedding[6] * -16 + embedding[7] * -12 + embedding[8] * 4 + embedding[9] * -1;
    let score_2 = embedding[0] * -9 + embedding[1] * -13 + embedding[2] * -6 + embedding[3] * 13 + embedding[4] * 14 + embedding[5] * 3 + embedding[6] * 11 + embedding[7] * 4 + embedding[8] * -6 + embedding[9] * -13;
    let score_3 = embedding[0] * -14 + embedding[1] * 7 + embedding[2] * -14 + embedding[3] * 2 + embedding[4] * 10 + embedding[5] * -18 + embedding[6] * -5 + embedding[7] * 26 + embedding[8] * 6 + embedding[9] * -2;
    let score_4 = embedding[0] * 13 + embedding[1] * -9 + embedding[2] * 7 + embedding[3] * -4 + embedding[4] * 15 + embedding[5] * -2 + embedding[6] * -15 + embedding[7] * 37 + embedding[8] * -5 + embedding[9] * -4;
    let score_5 = embedding[0] * -26 + embedding[1] * 6 + embedding[2] * -8 + embedding[3] * 12 + embedding[4] * 3 + embedding[5] * -20 + embedding[6] * 18 + embedding[7] * -11 + embedding[8] * 6 + embedding[9] * -12;
    let score_6 = embedding[0] * -10 + embedding[1] * 19 + embedding[2] * 1 + embedding[3] * -19 + embedding[4] * -15 + embedding[5] * -3 + embedding[6] * -9 + embedding[7] * 15 + embedding[8] * 10 + embedding[9] * 14;
    let score_7 = embedding[0] * -15 + embedding[1] * 17 + embedding[2] * -14 + embedding[3] * 14 + embedding[4] * -13 + embedding[5] * -6 + embedding[6] * 12 + embedding[7] * -8 + embedding[8] * 9 + embedding[9] * -2;
    let score_8 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_17 = embedding[0] * 7 + embedding[2] * -8 + embedding[3] * -13 + embedding[4] * 12 + embedding[5] * 12 + embedding[6] * -7 + embedding[7] * -20 + embedding[8] * 6 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 11;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 29;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 35;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_306(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_1 = embedding[0] * -1 + embedding[1] * -1 + embedding[2] * 15 + embedding[3] * -19 + embedding[5] * 14 + embedding[6] * -11 + embedding[7] * 4 + embedding[8] * 9 + embedding[9] * 1;
    let score_2 = embedding[0] * 8 + embedding[1] * -7 + embedding[2] * 62 + embedding[3] * -11 + embedding[4] * -6 + embedding[5] * 14 + embedding[6] * 7 + embedding[7] * 28 + embedding[8] * -19 + embedding[9] * 2;
    let score_3 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * -26 + embedding[3] * 8 + embedding[4] * 9 + embedding[5] * -24 + embedding[6] * -11 + embedding[7] * 30 + embedding[8] * 7 + embedding[9] * -1;
    let score_4 = embedding[0] * 14 + embedding[1] * -8 + embedding[2] * -3 + embedding[3] * -1 + embedding[4] * 15 + embedding[6] * -17 + embedding[7] * 40 + embedding[9] * -7;
    let score_5 = embedding[0] * -8 + embedding[1] * -22 + embedding[2] * -7 + embedding[3] * 18 + embedding[4] * 18 + embedding[5] * -20 + embedding[6] * -5 + embedding[7] * 56 + embedding[8] * -9 + embedding[9] * -15;
    let score_6 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_7 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_8 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_9 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_10 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_11 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_12 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_14 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_15 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_16 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 6;
    let score_17 = embedding[0] * 8 + embedding[1] * 9 + embedding[2] * -25 + embedding[3] * -12 + embedding[4] * 7 + embedding[5] * 12 + embedding[6] * -6 + embedding[7] * -43 + embedding[8] * 11 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_307(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * 6 + embedding[1] * 10 + embedding[2] * -4 + embedding[3] * -8 + embedding[4] * 21 + embedding[5] * 7 + embedding[6] * -7 + embedding[7] * -4 + embedding[8] * 8 + embedding[9] * -5;
    let score_2 = embedding[0] * -6 + embedding[1] * -5 + embedding[2] * 2 + embedding[3] * -7 + embedding[4] * -42 + embedding[5] * 14 + embedding[6] * 21 + embedding[7] * -29 + embedding[8] * 2 + embedding[9] * 1;
    let score_3 = embedding[0] * 14 + embedding[1] * -11 + embedding[2] * 53 + embedding[3] * -5 + embedding[4] * -6 + embedding[5] * 9 + embedding[6] * 3 + embedding[7] * 30 + embedding[8] * -21 + embedding[9] * 9;
    let score_4 = embedding[0] * -8 + embedding[1] * -24 + embedding[2] * -7 + embedding[3] * 16 + embedding[4] * 14 + embedding[5] * -17 + embedding[6] * -5 + embedding[7] * 48 + embedding[8] * -6 + embedding[9] * -6;
    let score_5 = embedding[0] * -26 + embedding[1] * -3 + embedding[2] * -20 + embedding[3] * 14 + embedding[4] * -8 + embedding[5] * -29 + embedding[6] * 2 + embedding[7] * -4 + embedding[8] * 7 + embedding[9] * -3;
    let score_6 = embedding[0] * 5 + embedding[1] * 3 + embedding[2] * 63 + embedding[3] * -1 + embedding[4] * 18 + embedding[5] * 5 + embedding[6] * 11 + embedding[7] * 48 + embedding[8] * -18 + embedding[9] * 2;
    let score_7 = embedding[0] * -24 + embedding[1] * 8 + embedding[2] * -1 + embedding[3] * 14 + embedding[4] * 4 + embedding[5] * -24 + embedding[6] * 14 + embedding[7] * -4 + embedding[8] * 6 + embedding[9] * -8;
    let score_8 = embedding[0] * -14 + embedding[1] * -12 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * 26 + embedding[5] * -9 + embedding[6] * 8 + embedding[7] * 50 + embedding[8] * -10 + embedding[9] * -13;
    let score_9 = embedding[0] * -15 + embedding[1] * -10 + embedding[2] * 60 + embedding[4] * -45 + embedding[5] * 20 + embedding[6] * 46 + embedding[7] * 5 + embedding[8] * -30 + embedding[9] * 3;
    let score_10 = embedding[0] * -22 + embedding[1] * -28 + embedding[2] * 9 + embedding[3] * 9 + embedding[4] * -22 + embedding[5] * -21 + embedding[6] * 20 + embedding[7] * 22 + embedding[8] * -12 + embedding[9] * -10;
    let score_11 = embedding[0] * -32 + embedding[1] * -3 + embedding[2] * 15 + embedding[3] * 22 + embedding[4] * -34 + embedding[5] * -26 + embedding[6] * 31 + embedding[7] * -19 + embedding[8] * 3 + embedding[9] * 2;
    let score_12 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 14 + embedding[1] * 1 + embedding[2] * -25 + embedding[3] * -11 + embedding[4] * 11 + embedding[5] * 7 + embedding[6] * -19 + embedding[7] * -10 + embedding[8] * 9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 31;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 46;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 11;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 152;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 180;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 167;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 175;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 189;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 208;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_308(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_1 = embedding[0] * -3 + embedding[1] * -2 + embedding[2] * 11 + embedding[3] * -10 + embedding[4] * -35 + embedding[5] * 15 + embedding[6] * 18 + embedding[7] * -31 + embedding[8] * 6 + embedding[9] * -2;
    let score_2 = embedding[0] * -6 + embedding[1] * -19 + embedding[2] * -1 + embedding[3] * 9 + embedding[4] * 22 + embedding[5] * -13 + embedding[6] * -7 + embedding[7] * 48 + embedding[8] * -3 + embedding[9] * -8;
    let score_3 = embedding[0] * -13 + embedding[1] * -2 + embedding[2] * -14 + embedding[3] * -10 + embedding[4] * 4 + embedding[6] * -3 + embedding[7] * 16 + embedding[8] * -2 + embedding[9] * 1;
    let score_4 = embedding[0] * -17 + embedding[1] * -23 + embedding[2] * 10 + embedding[3] * 4 + embedding[4] * -15 + embedding[5] * -18 + embedding[6] * 11 + embedding[7] * 33 + embedding[8] * -13 + embedding[9] * -8;
    let score_5 = embedding[0] * -21 + embedding[1] * -3 + embedding[2] * 2 + embedding[3] * -6 + embedding[4] * -25 + embedding[5] * 6 + embedding[6] * 16 + embedding[7] * 4 + embedding[8] * -9 + embedding[9] * 6;
    let score_6 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_7 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_8 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_9 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_10 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_11 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_12 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_13 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_14 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_15 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_16 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_17 = embedding[0] * 16 + embedding[1] * 6 + embedding[2] * -14 + embedding[3] * -13 + embedding[4] * 19 + embedding[5] * 7 + embedding[6] * -20 + embedding[7] * -12 + embedding[8] * 12 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 46;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 189;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 175;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_309(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 14 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 6 + embedding[6] * -28 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 46;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_310(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_311(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_312(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_313(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_314(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_1 = embedding[0] * 24 + embedding[1] * -17 + embedding[2] * 6 + embedding[3] * 32 + embedding[4] * 9 + embedding[5] * -3 + embedding[6] * 7 + embedding[7] * -1 + embedding[8] * -7 + embedding[9] * -8;
    let score_2 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_3 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_4 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_5 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_6 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_7 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_8 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_9 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_10 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_11 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_12 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_13 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_14 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_15 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_16 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 11;
    let score_17 = embedding[0] * 1 + embedding[1] * 2 + embedding[2] * -25 + embedding[3] * -25 + embedding[4] * 15 + embedding[5] * 13 + embedding[6] * -18 + embedding[7] * -16 + embedding[8] * 13 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 13;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_315(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_1 = embedding[0] * -8 + embedding[1] * -5 + embedding[2] * 13 + embedding[3] * -25 + embedding[4] * 3 + embedding[5] * 9 + embedding[6] * -11 + embedding[7] * 19 + embedding[8] * -2 + embedding[9] * 2;
    let score_2 = embedding[0] * 25 + embedding[1] * -16 + embedding[2] * 11 + embedding[3] * 42 + embedding[4] * 10 + embedding[5] * 1 + embedding[6] * 12 + embedding[7] * -3 + embedding[8] * -9 + embedding[9] * -12;
    let score_3 = embedding[0] * -14 + embedding[1] * -22 + embedding[2] * -3 + embedding[3] * 9 + embedding[4] * 18 + embedding[5] * -9 + embedding[6] * -1 + embedding[7] * 35 + embedding[8] * -6 + embedding[9] * -11;
    let score_4 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_5 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_6 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_7 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_8 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_9 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_10 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_11 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_12 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_13 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_14 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_15 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_16 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 9;
    let score_17 = embedding[0] * 5 + embedding[1] * 7 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 13 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -25 + embedding[8] * 16 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 13;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_316(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_1 = embedding[0] * -3 + embedding[1] * -2 + embedding[2] * -7 + embedding[3] * -24 + embedding[4] * 5 + embedding[5] * 10 + embedding[6] * -11 + embedding[7] * -11 + embedding[8] * 3;
    let score_2 = embedding[0] * 8 + embedding[1] * -17 + embedding[2] * 67 + embedding[3] * -5 + embedding[4] * -2 + embedding[5] * 11 + embedding[6] * 9 + embedding[7] * 28 + embedding[8] * -32 + embedding[9] * 2;
    let score_3 = embedding[0] * 15 + embedding[1] * 29 + embedding[2] * -16 + embedding[3] * 10 + embedding[4] * 18 + embedding[5] * -41 + embedding[6] * -1 + embedding[7] * 18 + embedding[8] * 1 + embedding[9] * 3;
    let score_4 = embedding[0] * 22 + embedding[1] * -19 + embedding[2] * 21 + embedding[3] * 33 + embedding[4] * 4 + embedding[5] * 4 + embedding[6] * 15 + embedding[7] * 1 + embedding[8] * -15 + embedding[9] * -11;
    let score_5 = embedding[0] * -15 + embedding[1] * -21 + embedding[2] * -2 + embedding[3] * 4 + embedding[4] * 12 + embedding[5] * -7 + embedding[6] * -3 + embedding[7] * 42 + embedding[8] * -6 + embedding[9] * -10;
    let score_6 = embedding[0] * -35 + embedding[1] * 5 + embedding[2] * -18 + embedding[3] * 5 + embedding[4] * -6 + embedding[5] * -24 + embedding[6] * 7 + embedding[7] * -16 + embedding[8] * 12 + embedding[9] * -6;
    let score_7 = embedding[0] * 3 + embedding[1] * -6 + embedding[2] * 8 + embedding[4] * 1 + embedding[5] * 3 + embedding[6] * -18 + embedding[7] * 61 + embedding[8] * 6 + embedding[9] * -2;
    let score_8 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_9 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_10 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_11 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_12 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_13 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_14 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_15 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_16 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_17 = embedding[0] * 1 + embedding[1] * 8 + embedding[2] * -20 + embedding[3] * -20 + embedding[4] * 6 + embedding[5] * 13 + embedding[6] * -11 + embedding[7] * -23 + embedding[8] * 13 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 13;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 11;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 12;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_317(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 26 + embedding[1] * -11 + embedding[2] * 8 + embedding[3] * -25 + embedding[4] * 27 + embedding[5] * 9 + embedding[6] * -5 + embedding[7] * -37 + embedding[8] * -24 + embedding[9] * -4;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 7;
    let score_2 = embedding[0] * -25 + embedding[1] * -24 + embedding[2] * -12 + embedding[3] * 38 + embedding[4] * -11 + embedding[5] * 18 + embedding[6] * 15 + embedding[7] * 26 + embedding[8] * 9 + embedding[9] * -16;
    let score_3 = embedding[0] * -17 + embedding[1] * 13 + embedding[2] * -31 + embedding[3] * 13 + embedding[4] * 1 + embedding[5] * -28 + embedding[6] * -13 + embedding[7] * 45 + embedding[8] * 13 + embedding[9] * 3;
    let score_4 = embedding[0] * -17 + embedding[1] * 21 + embedding[2] * -6 + embedding[3] * 25 + embedding[4] * -25 + embedding[5] * -5 + embedding[6] * -9 + embedding[7] * 45 + embedding[8] * 46 + embedding[9] * 5;
    let score_5 = embedding[0] * 29 + embedding[1] * -22 + embedding[2] * 17 + embedding[3] * -10 + embedding[4] * 29 + embedding[5] * -5 + embedding[6] * -13 + embedding[7] * 30 + embedding[8] * -34 + embedding[9] * -5;
    let score_6 = embedding[0] * -27 + embedding[1] * 29 + embedding[2] * -12 + embedding[3] * -4 + embedding[4] * -29 + embedding[5] * -8 + embedding[6] * -15 + embedding[7] * 46 + embedding[8] * 28 + embedding[9] * 12;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 7;
    let score_17 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * -25 + embedding[3] * 1 + embedding[4] * -10 + embedding[5] * 12 + embedding[6] * -6 + embedding[7] * -10 + embedding[8] * 17 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 15;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 12;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 29;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_318(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -3 + embedding[1] * -10 + embedding[2] * -11 + embedding[3] * -17 + embedding[4] * 8 + embedding[5] * 13 + embedding[6] * -15 + embedding[7] * -6 + embedding[8] * 3 + embedding[9] * -4;
    let score_1 = embedding[0] * 15 + embedding[1] * 2 + embedding[2] * -7 + embedding[3] * -8 + embedding[4] * 2 + embedding[5] * 9 + embedding[6] * -5 + embedding[7] * -5 + embedding[8] * -15 + embedding[9] * -13;
    let score_2 = embedding[0] * 15 + embedding[1] * -8 + embedding[2] * -3 + embedding[3] * -9 + embedding[4] * 12 + embedding[5] * 3 + embedding[6] * 13 + embedding[7] * -10 + embedding[8] * 4 + embedding[9] * 5;
    let score_3 = embedding[0] * 10 + embedding[1] * -2 + embedding[2] * -6 + embedding[3] * 7 + embedding[4] * 10 + embedding[5] * 11 + embedding[6] * -10 + embedding[7] * -12 + embedding[8] * -15 + embedding[9] * 7;
    let score_4 = embedding[0] * 9 + embedding[1] * -6 + embedding[2] * 14 + embedding[3] * -1 + embedding[4] * 5 + embedding[5] * -7 + embedding[6] * -16 + embedding[7] * -13 + embedding[8] * -1 + embedding[9] * -8;
    let score_5 = embedding[0] * -13 + embedding[1] * -8 + embedding[2] * -10 + embedding[3] * 17 + embedding[4] * 5 + embedding[5] * 6 + embedding[6] * 6 + embedding[7] * 8 + embedding[8] * 10 + embedding[9] * -11;
    let score_6 = embedding[0] * -8 + embedding[1] * 18 + embedding[2] * 6 + embedding[3] * 8 + embedding[4] * -1 + embedding[5] * 1 + embedding[6] * -3 + embedding[7] * 23 + embedding[8] * 25 + embedding[9] * 4;
    let score_7 = embedding[0] * -30 + embedding[2] * -6 + embedding[3] * 11 + embedding[4] * 2 + embedding[5] * -17 + embedding[6] * 15 + embedding[7] * -5 + embedding[8] * 6 + embedding[9] * -12;
    let score_8 = embedding[0] * -22 + embedding[1] * 20 + embedding[2] * 2 + embedding[3] * -20 + embedding[4] * -17 + embedding[5] * -3 + embedding[6] * -11 + embedding[7] * 32 + embedding[8] * 14 + embedding[9] * 13;
    let score_9 = embedding[0] * 13 + embedding[1] * -10 + embedding[2] * -1 + embedding[3] * 9 + embedding[4] * 8 + embedding[5] * -3 + embedding[6] * -1 + embedding[7] * 5 + embedding[8] * 3 + embedding[9] * 23;
    let score_10 = embedding[0] * -25 + embedding[1] * 11 + embedding[2] * -12 + embedding[3] * 12 + embedding[4] * -17 + embedding[5] * -1 + embedding[6] * 8 + embedding[7] * 9 + embedding[8] * 12 + embedding[9] * -1;
    let score_11 = embedding[0] * -28 + embedding[1] * 16 + embedding[2] * 33 + embedding[3] * -12 + embedding[4] * -11 + embedding[5] * 2 + embedding[6] * 10 + embedding[7] * 2 + embedding[8] * 12 + embedding[9] * -7;
    let score_12 = embedding[0] * 10 + embedding[1] * 5 + embedding[2] * -7 + embedding[3] * 10 + embedding[4] * -12 + embedding[5] * 3 + embedding[6] * -19 + embedding[7] * 2 + embedding[8] * -12 + embedding[9] * -3;
    let score_13 = embedding[0] * 13 + embedding[1] * -4 + embedding[2] * -2 + embedding[3] * 1 + embedding[4] * -13 + embedding[5] * -1 + embedding[7] * -12 + embedding[8] * -1 + embedding[9] * -1;
    let score_14 = embedding[0] * -5 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 7;
    let score_15 = embedding[0] * -5 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 7;
    let score_16 = embedding[0] * -5 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 7;
    let score_17 = embedding[0] * -5 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 15;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 11;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 29;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 23;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 35;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 52;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 100;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 93;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_319(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[1] * 5 + embedding[2] * -9 + embedding[3] * -10 + embedding[4] * -1 + embedding[5] * 3 + embedding[6] * -4 + embedding[7] * -1 + embedding[8] * -10 + embedding[9] * -17;
    let score_1 = embedding[0] * 5 + embedding[1] * -6 + embedding[2] * -5 + embedding[3] * -10 + embedding[4] * 11 + embedding[5] * -1 + embedding[6] * 14 + embedding[7] * -6 + embedding[8] * 11 + embedding[9] * 5;
    let score_2 = embedding[0] * -2 + embedding[1] * -1 + embedding[2] * -8 + embedding[3] * 9 + embedding[4] * 9 + embedding[5] * 10 + embedding[6] * -11 + embedding[7] * -9 + embedding[8] * -7 + embedding[9] * 9;
    let score_3 = embedding[0] * 1 + embedding[1] * -9 + embedding[2] * -3 + embedding[3] * -15 + embedding[4] * 5 + embedding[5] * 2 + embedding[6] * -14 + embedding[7] * -8 + embedding[8] * -2;
    let score_4 = embedding[0] * -2 + embedding[1] * -3 + embedding[2] * 13 + embedding[3] * -3 + embedding[4] * 2 + embedding[5] * -12 + embedding[6] * -17 + embedding[7] * -11 + embedding[8] * 4 + embedding[9] * -9;
    let score_5 = embedding[0] * 4 + embedding[1] * 6 + embedding[2] * -19 + embedding[3] * -4 + embedding[4] * 2 + embedding[5] * 11 + embedding[6] * 7 + embedding[7] * 23 + embedding[8] * -7 + embedding[9] * 8;
    let score_6 = embedding[0] * 3 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * -4 + embedding[4] * -21 + embedding[5] * 14 + embedding[6] * -5 + embedding[7] * 9 + embedding[9] * 7;
    let score_7 = embedding[0] * -17 + embedding[1] * -2 + embedding[2] * 10 + embedding[3] * 15 + embedding[4] * -7 + embedding[5] * 9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * 15 + embedding[9] * -5;
    let score_8 = embedding[0] * -2 + embedding[1] * 14 + embedding[2] * 18 + embedding[3] * 19 + embedding[4] * -1 + embedding[5] * -8 + embedding[6] * 2 + embedding[7] * 18 + embedding[8] * 24 + embedding[9] * 1;
    let score_9 = embedding[0] * -1 + embedding[1] * -13 + embedding[2] * -2 + embedding[3] * -7 + embedding[4] * -7 + embedding[6] * -10 + embedding[7] * -5 + embedding[8] * -1 + embedding[9] * 1;
    let score_10 = embedding[0] * 6 + embedding[1] * -7 + embedding[2] * -1 + embedding[3] * 9 + embedding[4] * 7 + embedding[5] * -9 + embedding[7] * 6 + embedding[8] * 4 + embedding[9] * 24;
    let score_11 = embedding[0] * -22 + embedding[1] * 15 + embedding[2] * 42 + embedding[3] * -6 + embedding[4] * -12 + embedding[5] * -6 + embedding[6] * 9 + embedding[7] * -5 + embedding[8] * 8 + embedding[9] * -7;
    let score_12 = embedding[0] * 10 + embedding[1] * -8 + embedding[2] * 5 + embedding[3] * -4 + embedding[5] * -11 + embedding[6] * 27 + embedding[7] * 5 + embedding[8] * 14 + embedding[9] * 11;
    let score_13 = embedding[0] * 4 + embedding[1] * 7 + embedding[2] * -6 + embedding[3] * 10 + embedding[4] * -12 + embedding[6] * -17 + embedding[7] * 2 + embedding[8] * -12 + embedding[9] * -1;
    let score_14 = embedding[0] * 5 + embedding[1] * 8 + embedding[2] * -7 + embedding[3] * -3 + embedding[4] * -8 + embedding[6] * 2 + embedding[7] * 6 + embedding[8] * -16 + embedding[9] * -13;
    let score_15 = embedding[0] * -2 + embedding[1] * 3 + embedding[3] * 15 + embedding[4] * 6 + embedding[5] * 5 + embedding[6] * -3 + embedding[7] * -4 + embedding[8] * -11 + embedding[9] * 8;
    let score_16 = embedding[0] * 1 + embedding[1] * -3 + embedding[2] * -4 + embedding[3] * -5 + embedding[5] * -5 + embedding[6] * 13 + embedding[7] * 8 + embedding[8] * 5 + embedding[9] * -6;
    let score_17 = embedding[0] * -12 + embedding[1] * 10 + embedding[3] * 11 + embedding[4] * -14 + embedding[5] * -1 + embedding[6] * -13 + embedding[7] * 12 + embedding[8] * -12 + embedding[9] * -12;
    let mut best_score = score_0;
    let mut best_palette = 3;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 26;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 20;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 15;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 61;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 23;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 52;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 144;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 100;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 150;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 159;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 164;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 162;
    }
    best_palette
}

fn predict_pixel_320(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[1] * 2 + embedding[2] * -1 + embedding[3] * -13 + embedding[4] * 21 + embedding[5] * 7 + embedding[6] * 39 + embedding[8] * 19 + embedding[9] * 13;
    let score_1 = embedding[0] * -5 + embedding[1] * 23 + embedding[2] * -8 + embedding[3] * -10 + embedding[4] * 4 + embedding[5] * 17 + embedding[6] * 6 + embedding[7] * 6 + embedding[8] * -17 + embedding[9] * -24;
    let score_2 = embedding[0] * -15 + embedding[1] * 18 + embedding[2] * -4 + embedding[3] * 12 + embedding[4] * 19 + embedding[5] * 26 + embedding[7] * -3 + embedding[8] * -11 + embedding[9] * 10;
    let score_3 = embedding[0] * 4 + embedding[1] * -3 + embedding[2] * -5 + embedding[3] * -8 + embedding[4] * 8 + embedding[5] * 1 + embedding[6] * -6 + embedding[7] * -12 + embedding[8] * 4 + embedding[9] * -4;
    let score_4 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 29 + embedding[3] * 2 + embedding[4] * 8 + embedding[5] * -12 + embedding[6] * -19 + embedding[7] * -13 + embedding[8] * 7 + embedding[9] * -15;
    let score_5 = embedding[0] * 20 + embedding[1] * -28 + embedding[2] * -29 + embedding[4] * -42 + embedding[5] * -1 + embedding[6] * 3 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * 23;
    let score_6 = embedding[0] * 21 + embedding[1] * -22 + embedding[2] * -12 + embedding[4] * -30 + embedding[5] * -13 + embedding[6] * -12 + embedding[7] * 1 + embedding[8] * -7 + embedding[9] * 14;
    let score_7 = embedding[0] * -3 + embedding[1] * 12 + embedding[2] * 15 + embedding[3] * -5 + embedding[4] * -8 + embedding[5] * -9 + embedding[6] * -10 + embedding[8] * -6 + embedding[9] * -29;
    let score_8 = embedding[0] * -12 + embedding[1] * 8 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * 10 + embedding[6] * -20 + embedding[7] * -12;
    let score_9 = embedding[0] * 7 + embedding[1] * -29 + embedding[2] * -6 + embedding[3] * 7 + embedding[4] * -31 + embedding[5] * -14 + embedding[6] * -21 + embedding[7] * 11 + embedding[8] * 4 + embedding[9] * 15;
    let score_10 = embedding[0] * 1 + embedding[1] * -2 + embedding[2] * 14 + embedding[3] * -8 + embedding[4] * 9 + embedding[5] * -11 + embedding[6] * 6 + embedding[7] * -8 + embedding[8] * 16 + embedding[9] * 1;
    let score_11 = embedding[0] * 7 + embedding[1] * -24 + embedding[2] * -7 + embedding[3] * 2 + embedding[4] * -26 + embedding[5] * -6 + embedding[6] * -9 + embedding[7] * 4 + embedding[8] * 1 + embedding[9] * 9;
    let score_12 = embedding[0] * 7 + embedding[1] * -8 + embedding[2] * -6 + embedding[3] * 7 + embedding[4] * 3 + embedding[5] * -10 + embedding[6] * 7 + embedding[7] * 13 + embedding[8] * 7 + embedding[9] * 36;
    let score_13 = embedding[0] * -1 + embedding[1] * 3 + embedding[2] * 28 + embedding[3] * -3 + embedding[4] * -8 + embedding[5] * -29 + embedding[6] * -15 + embedding[7] * -5 + embedding[8] * 4 + embedding[9] * -22;
    let score_14 = embedding[0] * 5 + embedding[1] * 7 + embedding[2] * -11 + embedding[3] * 10 + embedding[4] * -23 + embedding[5] * -1 + embedding[6] * -15 + embedding[7] * 6 + embedding[8] * -13 + embedding[9] * 1;
    let score_15 = embedding[0] * 13 + embedding[1] * -10 + embedding[2] * -5 + embedding[3] * 5 + embedding[4] * -29 + embedding[5] * -9 + embedding[6] * -2 + embedding[7] * -10 + embedding[9] * 7;
    let score_16 = embedding[0] * 8 + embedding[1] * -10 + embedding[2] * 9 + embedding[3] * 6 + embedding[4] * -10 + embedding[5] * -18 + embedding[6] * -8 + embedding[7] * 12 + embedding[8] * 7 + embedding[9] * 21;
    let score_17 = embedding[0] * 5 + embedding[2] * 11 + embedding[3] * 16 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * -15 + embedding[7] * -5 + embedding[8] * -9 + embedding[9] * -2;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 22;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 61;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 63;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 66;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 42;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 72;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 28;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 23;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 115;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 80;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 183;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 195;
    }
    best_palette
}

fn predict_pixel_321(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[1] * -2 + embedding[2] * -4 + embedding[3] * -7 + embedding[4] * 9 + embedding[5] * 7 + embedding[6] * -4 + embedding[7] * -7 + embedding[8] * 7 + embedding[9] * -6;
    let score_1 = embedding[0] * 1 + embedding[1] * 8 + embedding[2] * 5 + embedding[3] * -5 + embedding[4] * 13 + embedding[5] * 4 + embedding[6] * 31 + embedding[8] * 5 + embedding[9] * 13;
    let score_2 = embedding[0] * -8 + embedding[1] * 21 + embedding[2] * 5 + embedding[3] * 14 + embedding[4] * 5 + embedding[5] * 15 + embedding[6] * 12 + embedding[7] * 4 + embedding[8] * -26 + embedding[9] * 14;
    let score_3 = embedding[0] * -3 + embedding[1] * 29 + embedding[2] * 1 + embedding[3] * 1 + embedding[4] * -8 + embedding[5] * 10 + embedding[6] * 12 + embedding[7] * 11 + embedding[8] * -32 + embedding[9] * -10;
    let score_4 = embedding[0] * -8 + embedding[1] * 11 + embedding[2] * 41 + embedding[3] * 10 + embedding[4] * 7 + embedding[5] * -17 + embedding[6] * -11 + embedding[7] * -8 + embedding[8] * -1 + embedding[9] * -8;
    let score_5 = embedding[0] * 16 + embedding[1] * -23 + embedding[2] * -7 + embedding[3] * 3 + embedding[4] * -35 + embedding[5] * -15 + embedding[6] * -14 + embedding[7] * 6 + embedding[8] * -4 + embedding[9] * 10;
    let score_6 = embedding[0] * 20 + embedding[1] * -25 + embedding[2] * -20 + embedding[3] * 5 + embedding[4] * -45 + embedding[5] * -3 + embedding[6] * 3 + embedding[7] * 12 + embedding[8] * -13 + embedding[9] * 19;
    let score_7 = embedding[0] * 1 + embedding[1] * -12 + embedding[2] * -5 + embedding[3] * -2 + embedding[4] * -7 + embedding[6] * -9 + embedding[7] * 1 + embedding[8] * 9 + embedding[9] * -2;
    let score_8 = embedding[0] * 3 + embedding[1] * -27 + embedding[2] * -5 + embedding[3] * 8 + embedding[4] * -31 + embedding[5] * -14 + embedding[6] * -22 + embedding[7] * 18 + embedding[8] * 10 + embedding[9] * 11;
    let score_9 = embedding[0] * 1 + embedding[1] * -12 + embedding[2] * -6 + embedding[3] * -2 + embedding[4] * -9 + embedding[6] * -9 + embedding[7] * 4 + embedding[8] * 9;
    let score_10 = embedding[0] * 2 + embedding[1] * -12 + embedding[2] * -1 + embedding[3] * 1 + embedding[4] * -10 + embedding[5] * -6 + embedding[6] * -15 + embedding[7] * 1 + embedding[8] * 11 + embedding[9] * 2;
    let score_11 = embedding[0] * 1 + embedding[1] * 7 + embedding[2] * -1 + embedding[3] * -4 + embedding[4] * 9 + embedding[5] * 7 + embedding[6] * 13 + embedding[7] * -2 + embedding[8] * -1 + embedding[9] * 1;
    let score_12 = embedding[0] * -1 + embedding[1] * 12 + embedding[2] * 21 + embedding[3] * -1 + embedding[4] * -5 + embedding[5] * -9 + embedding[6] * -3 + embedding[8] * -6 + embedding[9] * -20;
    let score_13 = embedding[0] * -8 + embedding[1] * 9 + embedding[2] * 24 + embedding[3] * 14 + embedding[4] * 9 + embedding[5] * -6 + embedding[6] * -10 + embedding[7] * -7 + embedding[8] * -3 + embedding[9] * 1;
    let score_14 = embedding[0] * 1 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * -4 + embedding[4] * 8 + embedding[5] * -12 + embedding[6] * 7 + embedding[7] * -5 + embedding[8] * 10 + embedding[9] * -2;
    let score_15 = embedding[1] * 9 + embedding[2] * -2 + embedding[3] * -3 + embedding[4] * 9 + embedding[5] * 9 + embedding[6] * 13 + embedding[7] * -2 + embedding[8] * -4 + embedding[9] * 1;
    let score_16 = embedding[0] * 1 + embedding[1] * -10 + embedding[2] * -2 + embedding[3] * -2 + embedding[4] * -9 + embedding[5] * -5 + embedding[6] * -11 + embedding[8] * 6 + embedding[9] * -4;
    let score_17 = embedding[0] * 9 + embedding[1] * -26 + embedding[2] * -22 + embedding[3] * 4 + embedding[4] * -42 + embedding[5] * -3 + embedding[6] * -4 + embedding[7] * 26 + embedding[8] * -2 + embedding[9] * 14;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 3;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 28;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 30;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 42;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 59;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 85;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 12;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 63;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 66;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 72;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 69;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 105;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 64;
    }
    best_palette
}

fn predict_pixel_322(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 11 + embedding[1] * -9 + embedding[2] * -6 + embedding[3] * -6 + embedding[4] * 4 + embedding[5] * -4 + embedding[6] * -6 + embedding[7] * -12 + embedding[8] * 3 + embedding[9] * -1;
    let score_1 = embedding[0] * 6 + embedding[1] * 2 + embedding[2] * 1 + embedding[3] * -6 + embedding[4] * 11 + embedding[5] * -1 + embedding[6] * 42 + embedding[7] * 2 + embedding[8] * 7 + embedding[9] * 20;
    let score_2 = embedding[1] * 25 + embedding[2] * -6 + embedding[4] * -8 + embedding[5] * 9 + embedding[6] * 13 + embedding[7] * 11 + embedding[8] * -29 + embedding[9] * -11;
    let score_3 = embedding[0] * -10 + embedding[1] * 18 + embedding[2] * -3 + embedding[3] * 15 + embedding[4] * 6 + embedding[5] * 17 + embedding[6] * 11 + embedding[7] * 4 + embedding[8] * -24 + embedding[9] * 19;
    let score_4 = embedding[0] * -27 + embedding[1] * 12 + embedding[2] * 7 + embedding[3] * 13 + embedding[4] * -16 + embedding[5] * 12 + embedding[6] * -17 + embedding[7] * 15 + embedding[8] * -12 + embedding[9] * -16;
    let score_5 = embedding[0] * -32 + embedding[1] * 7 + embedding[2] * 7 + embedding[3] * 24 + embedding[4] * -5 + embedding[5] * 16 + embedding[6] * -18 + embedding[7] * 12 + embedding[8] * -7 + embedding[9] * -1;
    let score_6 = embedding[0] * 1 + embedding[1] * -2 + embedding[3] * -16 + embedding[4] * -4 + embedding[5] * -1 + embedding[6] * 21 + embedding[7] * 14 + embedding[8] * 14 + embedding[9] * -7;
    let score_7 = embedding[1] * 1 + embedding[2] * 32 + embedding[3] * 5 + embedding[4] * 1 + embedding[5] * -24 + embedding[6] * -14 + embedding[7] * -11 + embedding[8] * 1 + embedding[9] * -10;
    let score_8 = embedding[0] * -11 + embedding[1] * 3 + embedding[2] * -4 + embedding[4] * 6 + embedding[5] * 12 + embedding[6] * 4 + embedding[7] * 5 + embedding[8] * 7 + embedding[9] * 2;
    let score_9 = embedding[0] * -11 + embedding[2] * -4 + embedding[3] * -2 + embedding[4] * 5 + embedding[5] * 11 + embedding[6] * 3 + embedding[7] * 5 + embedding[8] * 8 + embedding[9] * 3;
    let score_10 = embedding[0] * 14 + embedding[1] * -14 + embedding[3] * -5 + embedding[4] * -7 + embedding[5] * -15 + embedding[6] * -8 + embedding[7] * -8 + embedding[8] * 6 + embedding[9] * 3;
    let score_11 = embedding[0] * 23 + embedding[1] * -19 + embedding[2] * -16 + embedding[3] * 2 + embedding[4] * -30 + embedding[5] * -6 + embedding[6] * 8 + embedding[7] * 4 + embedding[8] * -11 + embedding[9] * 18;
    let score_12 = embedding[0] * -5 + embedding[1] * -2 + embedding[2] * 12 + embedding[3] * 3 + embedding[4] * -9 + embedding[5] * -9 + embedding[6] * -18 + embedding[7] * -2 + embedding[8] * 5 + embedding[9] * -12;
    let score_13 = embedding[0] * 2 + embedding[1] * -12 + embedding[2] * -9 + embedding[3] * -2 + embedding[4] * -5 + embedding[6] * -6 + embedding[7] * 1 + embedding[8] * 8 + embedding[9] * 3;
    let score_14 = embedding[0] * -1 + embedding[1] * -13 + embedding[2] * -6 + embedding[3] * 2 + embedding[4] * -3 + embedding[5] * -2 + embedding[6] * -8 + embedding[7] * 4 + embedding[8] * 13 + embedding[9] * 9;
    let score_15 = embedding[0] * -1 + embedding[1] * -9 + embedding[2] * -7 + embedding[4] * -5 + embedding[6] * -7 + embedding[7] * 2 + embedding[8] * 10 + embedding[9] * 3;
    let score_16 = embedding[0] * 4 + embedding[1] * 2 + embedding[2] * 16 + embedding[3] * -2 + embedding[4] * -8 + embedding[5] * -16 + embedding[6] * -8 + embedding[7] * -3 + embedding[8] * -4 + embedding[9] * -18;
    let score_17 = embedding[1] * -4 + embedding[2] * -3 + embedding[3] * -2 + embedding[4] * 5 + embedding[5] * 1 + embedding[6] * -7 + embedding[7] * -6 + embedding[8] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 47;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 49;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 48;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 5;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 95;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 96;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 28;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 22;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 86;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 30;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 42;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 59;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 63;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 66;
    }
    best_palette
}

fn predict_pixel_323(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 2 + embedding[1] * -3 + embedding[2] * -1 + embedding[3] * -14 + embedding[4] * 18 + embedding[5] * 4 + embedding[6] * 20 + embedding[7] * -5 + embedding[8] * 20 + embedding[9] * 6;
    let score_1 = embedding[0] * -2 + embedding[1] * 15 + embedding[2] * -5 + embedding[3] * -12 + embedding[4] * 5 + embedding[5] * 11 + embedding[6] * -7 + embedding[7] * 1 + embedding[8] * -9 + embedding[9] * -27;
    let score_2 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * -2 + embedding[3] * 8 + embedding[4] * 18 + embedding[5] * 17 + embedding[6] * -13 + embedding[7] * -8 + embedding[8] * -4 + embedding[9] * 4;
    let score_3 = embedding[0] * 1 + embedding[1] * -4 + embedding[2] * -8 + embedding[3] * -10 + embedding[4] * 10 + embedding[5] * 5 + embedding[6] * -9 + embedding[7] * -13 + embedding[8] * 7 + embedding[9] * -7;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * -2 + embedding[4] * 10 + embedding[5] * -10 + embedding[6] * -22 + embedding[7] * -13 + embedding[8] * 9 + embedding[9] * -18;
    let score_5 = embedding[0] * 20 + embedding[1] * -18 + embedding[2] * -9 + embedding[3] * -3 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * -11 + embedding[7] * -2 + embedding[8] * -7 + embedding[9] * 10;
    let score_6 = embedding[0] * 23 + embedding[1] * -22 + embedding[2] * -20 + embedding[4] * -34 + embedding[5] * -3 + embedding[6] * 3 + embedding[7] * 7 + embedding[8] * -13 + embedding[9] * 19;
    let score_7 = embedding[0] * 7 + embedding[1] * -7 + embedding[2] * -3 + embedding[3] * 4 + embedding[4] * 7 + embedding[5] * -9 + embedding[6] * 1 + embedding[7] * 10 + embedding[8] * 10 + embedding[9] * 28;
    let score_8 = embedding[0] * 6 + embedding[1] * -16 + embedding[2] * -23 + embedding[3] * -5 + embedding[4] * -27 + embedding[5] * 5 + embedding[6] * 4 + embedding[7] * 15 + embedding[8] * -3 + embedding[9] * 9;
    let score_9 = embedding[0] * 5 + embedding[1] * 8 + embedding[2] * -6 + embedding[3] * 7 + embedding[4] * -14 + embedding[5] * 3 + embedding[6] * -19 + embedding[7] * 1 + embedding[8] * -9 + embedding[9] * -6;
    let score_10 = embedding[0] * 11 + embedding[1] * -7 + embedding[2] * -5 + embedding[3] * 1 + embedding[4] * -23 + embedding[5] * -6 + embedding[6] * -2 + embedding[7] * -12 + embedding[8] * 1 + embedding[9] * 2;
    let score_11 = embedding[0] * -1 + embedding[1] * 1 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_12 = embedding[0] * -1 + embedding[1] * 1 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_13 = embedding[0] * -1 + embedding[1] * 1 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_14 = embedding[0] * -1 + embedding[1] * 1 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_15 = embedding[0] * -1 + embedding[1] * 1 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_16 = embedding[0] * -1 + embedding[1] * 1 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_17 = embedding[0] * -1 + embedding[1] * 1 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 28;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 64;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 80;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 93;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_324(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 3 + embedding[1] * -5 + embedding[2] * -3 + embedding[3] * -15 + embedding[4] * 19 + embedding[5] * 3 + embedding[6] * 17 + embedding[7] * -7 + embedding[8] * 20 + embedding[9] * 3;
    let score_1 = embedding[0] * 2 + embedding[1] * 11 + embedding[2] * -7 + embedding[3] * -14 + embedding[4] * 4 + embedding[5] * 9 + embedding[6] * -7 + embedding[8] * -7 + embedding[9] * -28;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * -4 + embedding[3] * 8 + embedding[4] * 18 + embedding[5] * 16 + embedding[6] * -13 + embedding[7] * -10 + embedding[8] * -2 + embedding[9] * 7;
    let score_3 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * -3 + embedding[4] * 10 + embedding[5] * -10 + embedding[6] * -22 + embedding[7] * -14 + embedding[8] * 9 + embedding[9] * -18;
    let score_4 = embedding[0] * 1 + embedding[1] * -1 + embedding[2] * -5 + embedding[3] * -10 + embedding[4] * 10 + embedding[5] * 5 + embedding[6] * -8 + embedding[7] * -13 + embedding[8] * 4 + embedding[9] * -8;
    let score_5 = embedding[0] * 18 + embedding[1] * -20 + embedding[2] * -26 + embedding[3] * -2 + embedding[4] * -32 + embedding[5] * 2 + embedding[6] * 3 + embedding[7] * 11 + embedding[8] * -11 + embedding[9] * 15;
    let score_6 = embedding[0] * 18 + embedding[1] * -15 + embedding[2] * -9 + embedding[3] * -4 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * -11 + embedding[7] * -3 + embedding[8] * -7 + embedding[9] * 8;
    let score_7 = embedding[0] * 6 + embedding[1] * -6 + embedding[2] * -2 + embedding[3] * 4 + embedding[4] * 12 + embedding[5] * -6 + embedding[7] * 8 + embedding[8] * 9 + embedding[9] * 26;
    let score_8 = embedding[0] * 5 + embedding[1] * 10 + embedding[2] * -5 + embedding[3] * 6 + embedding[4] * -9 + embedding[5] * 3 + embedding[6] * -18 + embedding[8] * -8 + embedding[9] * -8;
    let score_9 = embedding[0] * 11 + embedding[1] * -4 + embedding[2] * -5 + embedding[3] * -1 + embedding[4] * -19 + embedding[5] * -4 + embedding[6] * -2 + embedding[7] * -12;
    let score_10 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -11 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_11 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -11 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_12 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_13 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -11 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_14 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -11 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_15 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -11 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_16 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -11 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let score_17 = embedding[0] * -2 + embedding[1] * 2 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -16 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 22;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 28;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 80;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 93;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_325(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 3 + embedding[1] * -5 + embedding[2] * -4 + embedding[3] * -13 + embedding[4] * 15 + embedding[5] * 6 + embedding[6] * 25 + embedding[8] * 16 + embedding[9] * 11;
    let score_1 = embedding[1] * 13 + embedding[2] * -8 + embedding[3] * -11 + embedding[4] * 1 + embedding[5] * 13 + embedding[6] * -2 + embedding[7] * 5 + embedding[8] * -14 + embedding[9] * -20;
    let score_2 = embedding[0] * -8 + embedding[1] * 7 + embedding[2] * -4 + embedding[3] * 10 + embedding[4] * 14 + embedding[5] * 20 + embedding[6] * -8 + embedding[7] * -3 + embedding[8] * -9 + embedding[9] * 10;
    let score_3 = embedding[0] * 2 + embedding[1] * -6 + embedding[2] * -9 + embedding[3] * -8 + embedding[4] * 6 + embedding[5] * 4 + embedding[6] * -5 + embedding[7] * -10 + embedding[8] * 4 + embedding[9] * -2;
    let score_4 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 25 + embedding[3] * 2 + embedding[4] * 6 + embedding[5] * -11 + embedding[6] * -20 + embedding[7] * -10 + embedding[8] * 7 + embedding[9] * -13;
    let score_5 = embedding[0] * 21 + embedding[1] * -20 + embedding[2] * -11 + embedding[3] * -1 + embedding[4] * -27 + embedding[5] * -12 + embedding[6] * -7 + embedding[7] * 1 + embedding[8] * -10 + embedding[9] * 15;
    let score_6 = embedding[0] * 24 + embedding[1] * -24 + embedding[2] * -22 + embedding[3] * 2 + embedding[4] * -38 + embedding[5] * -2 + embedding[6] * 8 + embedding[7] * 11 + embedding[8] * -17 + embedding[9] * 24;
    let score_7 = embedding[0] * -4 + embedding[1] * 9 + embedding[2] * 14 + embedding[3] * -5 + embedding[4] * -10 + embedding[5] * -8 + embedding[6] * -12 + embedding[7] * 3 + embedding[8] * -7 + embedding[9] * -28;
    let score_8 = embedding[0] * -13 + embedding[1] * 5 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * 8 + embedding[5] * 1 + embedding[6] * -22 + embedding[7] * -9 + embedding[8] * -3 + embedding[9] * 3;
    let score_9 = embedding[0] * 2 + embedding[1] * -5 + embedding[2] * 11 + embedding[3] * -8 + embedding[4] * 7 + embedding[5] * -10 + embedding[6] * 6 + embedding[7] * -5 + embedding[8] * 16 + embedding[9] * 3;
    let score_10 = embedding[0] * -35 + embedding[1] * 9 + embedding[2] * 8 + embedding[3] * 9 + embedding[4] * 9 + embedding[5] * -18 + embedding[6] * 20 + embedding[7] * -9 + embedding[8] * 5 + embedding[9] * -10;
    let score_11 = embedding[0] * 7 + embedding[1] * -17 + embedding[2] * -26 + embedding[3] * -4 + embedding[4] * -30 + embedding[5] * 6 + embedding[6] * 9 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 15;
    let score_12 = embedding[0] * 9 + embedding[1] * -9 + embedding[2] * -6 + embedding[3] * 6 + embedding[4] * 3 + embedding[5] * -8 + embedding[6] * 6 + embedding[7] * 13 + embedding[8] * 6 + embedding[9] * 35;
    let score_13 = embedding[0] * -3 + embedding[1] * 1 + embedding[2] * 27 + embedding[3] * -2 + embedding[4] * -10 + embedding[5] * -28 + embedding[6] * -15 + embedding[7] * -2 + embedding[8] * 3 + embedding[9] * -22;
    let score_14 = embedding[0] * 5 + embedding[1] * 8 + embedding[2] * -8 + embedding[3] * 10 + embedding[4] * -17 + embedding[5] * 3 + embedding[6] * -15 + embedding[7] * 6 + embedding[8] * -14 + embedding[9] * 1;
    let score_15 = embedding[0] * 13 + embedding[1] * -8 + embedding[2] * -7 + embedding[3] * 2 + embedding[4] * -27 + embedding[5] * -7 + embedding[6] * 3 + embedding[7] * -9 + embedding[8] * -2 + embedding[9] * 7;
    let score_16 = embedding[0] * -18 + embedding[1] * -1 + embedding[2] * 10 + embedding[3] * 17 + embedding[4] * -9 + embedding[5] * -30 + embedding[6] * 4 + embedding[7] * -6 + embedding[8] * 5 + embedding[9] * -8;
    let score_17 = embedding[0] * 7 + embedding[1] * -9 + embedding[2] * 10 + embedding[3] * 5 + embedding[4] * -5 + embedding[5] * -17 + embedding[6] * -4 + embedding[7] * 13 + embedding[8] * 6 + embedding[9] * 22;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 28;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 63;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 66;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 72;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 11;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 65;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 23;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 115;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 80;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 203;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 183;
    }
    best_palette
}

fn predict_pixel_326(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -1 + embedding[2] * -4 + embedding[3] * -6 + embedding[4] * 8 + embedding[5] * 7 + embedding[6] * -4 + embedding[7] * -6 + embedding[8] * 6 + embedding[9] * -6;
    let score_1 = embedding[0] * 1 + embedding[1] * 17 + embedding[3] * -2 + embedding[5] * 8 + embedding[6] * 6 + embedding[7] * 2 + embedding[8] * -18 + embedding[9] * -9;
    let score_2 = embedding[0] * 2 + embedding[1] * 5 + embedding[2] * 3 + embedding[3] * -6 + embedding[4] * 12 + embedding[5] * 2 + embedding[6] * 23 + embedding[7] * -1 + embedding[8] * 4 + embedding[9] * 9;
    let score_3 = embedding[0] * -4 + embedding[1] * 15 + embedding[2] * 3 + embedding[3] * 9 + embedding[4] * 8 + embedding[5] * 11 + embedding[6] * 5 + embedding[7] * -1 + embedding[8] * -18 + embedding[9] * 9;
    let score_4 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 30 + embedding[3] * 5 + embedding[4] * 7 + embedding[5] * -12 + embedding[6] * -8 + embedding[7] * -7 + embedding[8] * 1 + embedding[9] * -8;
    let score_5 = embedding[0] * 15 + embedding[1] * -22 + embedding[2] * -8 + embedding[3] * 4 + embedding[4] * -35 + embedding[5] * -14 + embedding[6] * -13 + embedding[7] * 7 + embedding[8] * -5 + embedding[9] * 11;
    let score_6 = embedding[0] * 19 + embedding[1] * -23 + embedding[2] * -20 + embedding[3] * 6 + embedding[4] * -44 + embedding[5] * -2 + embedding[6] * 4 + embedding[7] * 13 + embedding[8] * -14 + embedding[9] * 19;
    let score_7 = embedding[1] * -10 + embedding[2] * -5 + embedding[3] * -1 + embedding[4] * -7 + embedding[5] * 1 + embedding[6] * -8 + embedding[7] * 2 + embedding[8] * 7 + embedding[9] * -2;
    let score_8 = embedding[0] * 2 + embedding[1] * -26 + embedding[2] * -5 + embedding[3] * 10 + embedding[4] * -31 + embedding[5] * -13 + embedding[6] * -21 + embedding[7] * 19 + embedding[8] * 8 + embedding[9] * 12;
    let score_9 = embedding[1] * -10 + embedding[2] * -7 + embedding[4] * -9 + embedding[5] * 1 + embedding[6] * -8 + embedding[7] * 5 + embedding[8] * 8;
    let score_10 = embedding[0] * 1 + embedding[1] * -11 + embedding[2] * -2 + embedding[3] * 2 + embedding[4] * -10 + embedding[5] * -5 + embedding[6] * -14 + embedding[7] * 2 + embedding[8] * 10 + embedding[9] * 3;
    let score_11 = embedding[0] * 1 + embedding[1] * 8 + embedding[2] * -1 + embedding[3] * -3 + embedding[4] * 9 + embedding[5] * 7 + embedding[6] * 12 + embedding[7] * -2 + embedding[8] * -3;
    let score_12 = embedding[0] * -1 + embedding[1] * 14 + embedding[2] * 20 + embedding[4] * -4 + embedding[5] * -8 + embedding[6] * -1 + embedding[7] * 1 + embedding[8] * -8 + embedding[9] * -19;
    let score_13 = embedding[0] * -9 + embedding[1] * 11 + embedding[2] * 23 + embedding[3] * 15 + embedding[4] * 10 + embedding[5] * -5 + embedding[6] * -9 + embedding[7] * -6 + embedding[8] * -6 + embedding[9] * 2;
    let score_14 = embedding[1] * 5 + embedding[2] * 23 + embedding[3] * -3 + embedding[4] * 8 + embedding[5] * -12 + embedding[6] * 9 + embedding[7] * -4 + embedding[8] * 8;
    let score_15 = embedding[0] * 1 + embedding[1] * 9 + embedding[2] * -2 + embedding[3] * -2 + embedding[4] * 8 + embedding[5] * 8 + embedding[6] * 12 + embedding[7] * -2 + embedding[8] * -4;
    let score_16 = embedding[1] * -9 + embedding[2] * -2 + embedding[4] * -9 + embedding[5] * -4 + embedding[6] * -10 + embedding[7] * 1 + embedding[8] * 5 + embedding[9] * -3;
    let score_17 = embedding[0] * 7 + embedding[1] * -24 + embedding[2] * -22 + embedding[3] * 5 + embedding[4] * -42 + embedding[5] * -2 + embedding[6] * -2 + embedding[7] * 27 + embedding[8] * -3 + embedding[9] * 15;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 28;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 30;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 42;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 59;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 85;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 12;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 63;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 66;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 72;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 69;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 105;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 65;
    }
    best_palette
}

fn predict_pixel_327(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 10 + embedding[1] * -8 + embedding[2] * -6 + embedding[3] * -6 + embedding[4] * 3 + embedding[5] * -2 + embedding[6] * -5 + embedding[7] * -10 + embedding[8] * 2;
    let score_1 = embedding[0] * 3 + embedding[1] * 17 + embedding[2] * -1 + embedding[3] * -3 + embedding[4] * -1 + embedding[5] * 4 + embedding[6] * 9 + embedding[7] * 2 + embedding[8] * -21 + embedding[9] * -11;
    let score_2 = embedding[0] * 7 + embedding[1] * 2 + embedding[2] * 1 + embedding[3] * -7 + embedding[4] * 12 + embedding[5] * -4 + embedding[6] * 33 + embedding[7] * -1 + embedding[8] * 6 + embedding[9] * 14;
    let score_3 = embedding[0] * -7 + embedding[1] * 14 + embedding[3] * 10 + embedding[4] * 10 + embedding[5] * 13 + embedding[6] * 7 + embedding[7] * -1 + embedding[8] * -17 + embedding[9] * 12;
    let score_4 = embedding[0] * -23 + embedding[1] * 11 + embedding[2] * 5 + embedding[3] * 11 + embedding[4] * -14 + embedding[5] * 12 + embedding[6] * -16 + embedding[7] * 12 + embedding[8] * -10 + embedding[9] * -15;
    let score_5 = embedding[0] * -27 + embedding[1] * 6 + embedding[2] * 4 + embedding[3] * 21 + embedding[4] * -3 + embedding[5] * 15 + embedding[6] * -17 + embedding[7] * 10 + embedding[8] * -5 + embedding[9] * 1;
    let score_6 = embedding[1] * -1 + embedding[2] * -1 + embedding[3] * -15 + embedding[4] * -4 + embedding[5] * -2 + embedding[6] * 19 + embedding[7] * 13 + embedding[8] * 13 + embedding[9] * -7;
    let score_7 = embedding[1] * 3 + embedding[2] * 29 + embedding[3] * 3 + embedding[4] * 2 + embedding[5] * -21 + embedding[6] * -12 + embedding[7] * -9 + embedding[8] * 1 + embedding[9] * -9;
    let score_8 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * -4 + embedding[3] * 1 + embedding[4] * 5 + embedding[5] * 12 + embedding[6] * 3 + embedding[7] * 5 + embedding[8] * 6 + embedding[9] * 2;
    let score_9 = embedding[0] * -12 + embedding[1] * 1 + embedding[2] * -4 + embedding[3] * -1 + embedding[4] * 4 + embedding[5] * 11 + embedding[6] * 2 + embedding[7] * 6 + embedding[8] * 8 + embedding[9] * 3;
    let score_10 = embedding[0] * 14 + embedding[1] * -13 + embedding[2] * -1 + embedding[3] * -5 + embedding[4] * -8 + embedding[5] * -14 + embedding[6] * -7 + embedding[7] * -6 + embedding[8] * 5 + embedding[9] * 3;
    let score_11 = embedding[0] * -6 + embedding[2] * 12 + embedding[3] * 4 + embedding[4] * -10 + embedding[5] * -9 + embedding[6] * -18 + embedding[7] * -1 + embedding[8] * 5 + embedding[9] * -10;
    let score_12 = embedding[0] * 21 + embedding[1] * -17 + embedding[2] * -16 + embedding[3] * 2 + embedding[4] * -28 + embedding[5] * -6 + embedding[6] * 8 + embedding[7] * 5 + embedding[8] * -11 + embedding[9] * 16;
    let score_13 = embedding[1] * -8 + embedding[2] * -6 + embedding[4] * -3 + embedding[6] * -7 + embedding[7] * -1 + embedding[8] * 8 + embedding[9] * 1;
    let score_14 = embedding[0] * -3 + embedding[1] * -11 + embedding[2] * -7 + embedding[3] * 3 + embedding[4] * -3 + embedding[6] * -8 + embedding[7] * 6 + embedding[8] * 12 + embedding[9] * 9;
    let score_15 = embedding[0] * -2 + embedding[1] * -10 + embedding[2] * -10 + embedding[3] * 1 + embedding[4] * -9 + embedding[5] * 2 + embedding[6] * -5 + embedding[7] * 8 + embedding[8] * 9 + embedding[9] * 6;
    let score_16 = embedding[0] * 2 + embedding[1] * 5 + embedding[2] * 16 + embedding[3] * -2 + embedding[4] * -8 + embedding[5] * -15 + embedding[6] * -6 + embedding[7] * -2 + embedding[8] * -6 + embedding[9] * -19;
    let score_17 = embedding[0] * -1 + embedding[1] * -2 + embedding[2] * -3 + embedding[3] * -1 + embedding[4] * 5 + embedding[5] * 2 + embedding[6] * -7 + embedding[7] * -5 + embedding[8] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 47;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 49;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 48;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 5;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 95;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 96;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 28;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 86;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 22;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 30;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 42;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 59;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 63;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 66;
    }
    best_palette
}

fn predict_pixel_328(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 1 + embedding[1] * 5 + embedding[2] * -5 + embedding[3] * -12 + embedding[4] * 22 + embedding[5] * 8 + embedding[6] * -4 + embedding[7] * -17 + embedding[8] * 1 + embedding[9] * -8;
    let score_1 = embedding[0] * -7 + embedding[1] * 6 + embedding[3] * 25 + embedding[4] * 20 + embedding[5] * 5 + embedding[6] * 14 + embedding[7] * 2 + embedding[8] * 8 + embedding[9] * -14;
    let score_2 = embedding[0] * 16 + embedding[1] * -24 + embedding[2] * -23 + embedding[3] * 1 + embedding[4] * -35 + embedding[7] * 12 + embedding[8] * -12 + embedding[9] * 15;
    let score_3 = embedding[0] * 16 + embedding[1] * -16 + embedding[2] * -7 + embedding[3] * -1 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * -11 + embedding[7] * -3 + embedding[8] * -8 + embedding[9] * 6;
    let score_4 = embedding[0] * 4 + embedding[1] * -25 + embedding[2] * -6 + embedding[3] * 7 + embedding[4] * -25 + embedding[5] * -9 + embedding[6] * -20 + embedding[7] * 11 + embedding[8] * 2 + embedding[9] * 12;
    let score_5 = embedding[0] * -4 + embedding[1] * 16 + embedding[2] * 20 + embedding[3] * -5 + embedding[4] * -8 + embedding[5] * 16 + embedding[7] * 8 + embedding[8] * 5 + embedding[9] * 5;
    let score_6 = embedding[0] * -11 + embedding[1] * 25 + embedding[2] * 18 + embedding[3] * 10 + embedding[4] * 11 + embedding[6] * 6 + embedding[7] * 12 + embedding[8] * 20 + embedding[9] * -1;
    let score_7 = embedding[0] * 4 + embedding[1] * -21 + embedding[2] * -7 + embedding[3] * 3 + embedding[4] * -19 + embedding[6] * -8 + embedding[7] * 3 + embedding[8] * -2 + embedding[9] * 6;
    let score_8 = embedding[0] * -19 + embedding[1] * 28 + embedding[2] * 15 + embedding[3] * -10 + embedding[4] * -1 + embedding[5] * 2 + embedding[6] * 2 + embedding[7] * 18 + embedding[8] * 12 + embedding[9] * 6;
    let score_9 = embedding[0] * -17 + embedding[1] * 28 + embedding[2] * 2 + embedding[3] * 11 + embedding[4] * -3 + embedding[5] * 1 + embedding[6] * 13 + embedding[7] * 3 + embedding[8] * 8 + embedding[9] * -1;
    let score_10 = embedding[0] * -25 + embedding[1] * 28 + embedding[2] * 40 + embedding[3] * -8 + embedding[6] * 15 + embedding[7] * -2 + embedding[8] * 11 + embedding[9] * -7;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_17 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 6;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 22;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 61;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 42;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 20;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 15;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 28;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 29;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 35;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 52;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_329(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_1 = embedding[0] * 11 + embedding[1] * -11 + embedding[2] * -1 + embedding[3] * -16 + embedding[4] * 2 + embedding[5] * 9 + embedding[6] * -18 + embedding[7] * -5 + embedding[8] * 5 + embedding[9] * 1;
    let score_2 = embedding[0] * -8 + embedding[1] * -12 + embedding[2] * -7 + embedding[3] * 15 + embedding[4] * 12 + embedding[5] * 3 + embedding[6] * 11 + embedding[7] * 4 + embedding[8] * -6 + embedding[9] * -13;
    let score_3 = embedding[0] * -13 + embedding[1] * 7 + embedding[2] * -14 + embedding[3] * 2 + embedding[4] * 9 + embedding[5] * -17 + embedding[6] * -4 + embedding[7] * 25 + embedding[8] * 6 + embedding[9] * -2;
    let score_4 = embedding[0] * 14 + embedding[1] * -10 + embedding[2] * 7 + embedding[3] * -4 + embedding[4] * 13 + embedding[5] * -1 + embedding[6] * -16 + embedding[7] * 40 + embedding[8] * -5 + embedding[9] * -3;
    let score_5 = embedding[0] * -25 + embedding[1] * 8 + embedding[2] * -9 + embedding[3] * 13 + embedding[4] * 2 + embedding[5] * -20 + embedding[6] * 19 + embedding[7] * -12 + embedding[8] * 6 + embedding[9] * -12;
    let score_6 = embedding[0] * -10 + embedding[1] * 18 + embedding[2] * 1 + embedding[3] * -19 + embedding[4] * -18 + embedding[5] * -2 + embedding[6] * -10 + embedding[7] * 17 + embedding[8] * 11 + embedding[9] * 14;
    let score_7 = embedding[0] * -14 + embedding[1] * 18 + embedding[2] * -14 + embedding[3] * 16 + embedding[4] * -14 + embedding[5] * -6 + embedding[6] * 13 + embedding[7] * -9 + embedding[8] * 9 + embedding[9] * -2;
    let score_8 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_17 = embedding[0] * 8 + embedding[1] * 1 + embedding[2] * -9 + embedding[3] * -12 + embedding[4] * 10 + embedding[5] * 12 + embedding[6] * -6 + embedding[7] * -21 + embedding[8] * 6 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 12;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 11;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 29;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 35;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_330(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_1 = embedding[0] * -2 + embedding[1] * -3 + embedding[2] * 14 + embedding[3] * -18 + embedding[4] * 2 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * 18 + embedding[8] * 8 + embedding[9] * 1;
    let score_2 = embedding[0] * 10 + embedding[1] * -9 + embedding[2] * 60 + embedding[3] * -9 + embedding[4] * -4 + embedding[5] * 14 + embedding[6] * 7 + embedding[7] * 33 + embedding[8] * -21 + embedding[9] * 1;
    let score_3 = embedding[0] * -7 + embedding[1] * -23 + embedding[2] * -6 + embedding[3] * 13 + embedding[4] * 14 + embedding[5] * -6 + embedding[6] * 2 + embedding[7] * 34 + embedding[8] * -8 + embedding[9] * -14;
    let score_4 = embedding[0] * -26 + embedding[1] * 3 + embedding[2] * -24 + embedding[3] * 15 + embedding[4] * -4 + embedding[5] * -25 + embedding[6] * 13 + embedding[7] * -28 + embedding[8] * 7 + embedding[9] * -10;
    let score_5 = embedding[0] * -11 + embedding[1] * 22 + embedding[2] * -11 + embedding[3] * -19 + embedding[4] * -20 + embedding[5] * -5 + embedding[6] * -11 + embedding[7] * 12 + embedding[8] * 11 + embedding[9] * 15;
    let score_6 = embedding[0] * 7 + embedding[1] * -7 + embedding[2] * 13 + embedding[4] * 4 + embedding[5] * 6 + embedding[6] * -18 + embedding[7] * 59 + embedding[8] * 4 + embedding[9] * -2;
    let score_7 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * 12 + embedding[1] * 5 + embedding[2] * -27 + embedding[3] * -9 + embedding[4] * 10 + embedding[5] * 11 + embedding[6] * -6 + embedding[7] * -35 + embedding[8] * 6 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 11;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 29;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_331(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_1 = embedding[0] * 3 + embedding[1] * 11 + embedding[2] * -6 + embedding[3] * -8 + embedding[4] * 22 + embedding[5] * 6 + embedding[6] * -7 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -6;
    let score_2 = embedding[0] * -8 + embedding[1] * -5 + embedding[3] * -7 + embedding[4] * -41 + embedding[5] * 12 + embedding[6] * 20 + embedding[7] * -31 + embedding[8] * 4 + embedding[9] * 1;
    let score_3 = embedding[0] * 11 + embedding[1] * -11 + embedding[2] * 49 + embedding[3] * -5 + embedding[4] * -6 + embedding[5] * 7 + embedding[6] * 3 + embedding[7] * 27 + embedding[8] * -19 + embedding[9] * 7;
    let score_4 = embedding[0] * -9 + embedding[1] * -22 + embedding[2] * -9 + embedding[3] * 15 + embedding[4] * 14 + embedding[5] * -17 + embedding[6] * -6 + embedding[7] * 46 + embedding[8] * -4 + embedding[9] * -6;
    let score_5 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * 60 + embedding[4] * 19 + embedding[5] * 4 + embedding[6] * 11 + embedding[7] * 46 + embedding[8] * -15 + embedding[9] * 1;
    let score_6 = embedding[0] * -16 + embedding[1] * -11 + embedding[2] * 17 + embedding[3] * 16 + embedding[4] * 26 + embedding[5] * -10 + embedding[6] * 7 + embedding[7] * 48 + embedding[8] * -9 + embedding[9] * -14;
    let score_7 = embedding[0] * -17 + embedding[1] * -10 + embedding[2] * 57 + embedding[4] * -45 + embedding[5] * 18 + embedding[6] * 46 + embedding[7] * 3 + embedding[8] * -28 + embedding[9] * 2;
    let score_8 = embedding[0] * -19 + embedding[1] * -29 + embedding[2] * 11 + embedding[3] * 7 + embedding[4] * -19 + embedding[5] * -16 + embedding[6] * 15 + embedding[7] * 22 + embedding[8] * -15 + embedding[9] * -9;
    let score_9 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_10 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_11 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_12 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_13 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_14 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_15 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_16 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_17 = embedding[0] * 11 + embedding[1] * 2 + embedding[2] * -28 + embedding[3] * -10 + embedding[4] * 12 + embedding[5] * 5 + embedding[6] * -18 + embedding[7] * -13 + embedding[8] * 11 + embedding[9] * -1;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 31;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 46;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 152;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 167;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 175;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 189;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_332(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_1 = embedding[0] * -4 + embedding[1] * -2 + embedding[2] * 5 + embedding[3] * -13 + embedding[4] * -33 + embedding[5] * 15 + embedding[6] * 14 + embedding[7] * -30 + embedding[8] * 5 + embedding[9] * -1;
    let score_2 = embedding[0] * -5 + embedding[1] * -19 + embedding[2] * -5 + embedding[3] * 9 + embedding[4] * 25 + embedding[5] * -14 + embedding[6] * -11 + embedding[7] * 48 + embedding[8] * -3 + embedding[9] * -9;
    let score_3 = embedding[0] * -13 + embedding[1] * -22 + embedding[2] * 7 + embedding[3] * 7 + embedding[4] * -14 + embedding[5] * -17 + embedding[6] * 7 + embedding[7] * 30 + embedding[8] * -11 + embedding[9] * -10;
    let score_4 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_5 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_6 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_7 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_8 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_9 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_10 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_11 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_12 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_13 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_14 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_15 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_16 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * 4 + embedding[2] * -21 + embedding[3] * -15 + embedding[4] * 20 + embedding[5] * 7 + embedding[6] * -23 + embedding[7] * -10 + embedding[8] * 12 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 46;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 189;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_333(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 14 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -3;
    let score_2 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 6 + embedding[6] * -28 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 46;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_334(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_335(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -19 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_336(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_337(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_338(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 51 + embedding[3] * -9 + embedding[4] * 5 + embedding[5] * 11 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -19 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 6 + embedding[1] * -1 + embedding[2] * -36 + embedding[3] * -15 + embedding[4] * 23 + embedding[5] * 10 + embedding[6] * -18 + embedding[7] * -23 + embedding[8] * 18 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_339(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_1 = embedding[0] * 6 + embedding[1] * -16 + embedding[2] * 56 + embedding[3] * -11 + embedding[4] * -1 + embedding[5] * 17 + embedding[6] * 3 + embedding[7] * 20 + embedding[8] * -22;
    let score_2 = embedding[0] * 20 + embedding[1] * 18 + embedding[2] * -19 + embedding[3] * 13 + embedding[4] * 23 + embedding[5] * -38 + embedding[6] * -4 + embedding[7] * 17 + embedding[8] * -1 + embedding[9] * -2;
    let score_3 = embedding[0] * -28 + embedding[1] * -3 + embedding[2] * -20 + embedding[3] * 5 + embedding[5] * -19 + embedding[6] * 2 + embedding[7] * -21 + embedding[8] * 11 + embedding[9] * -10;
    let score_4 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_5 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_6 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_7 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_8 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_9 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_10 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_11 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_12 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_13 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_14 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_15 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_16 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -18 + embedding[5] * -13 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 5;
    let score_17 = embedding[0] * 6 + embedding[1] * -5 + embedding[2] * -23 + embedding[3] * -15 + embedding[4] * 13 + embedding[5] * 17 + embedding[6] * -16 + embedding[7] * -23 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 9;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 11;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_340(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_1 = embedding[0] * 6 + embedding[1] * 2 + embedding[2] * -6 + embedding[3] * -20 + embedding[4] * 13 + embedding[5] * 13 + embedding[6] * -8 + embedding[7] * -16 + embedding[8] * 4 + embedding[9] * -2;
    let score_2 = embedding[0] * -18 + embedding[1] * 4 + embedding[2] * -18 + embedding[3] * 5 + embedding[4] * 10 + embedding[5] * -26 + embedding[6] * -7 + embedding[7] * 36 + embedding[8] * 6;
    let score_3 = embedding[0] * 24 + embedding[1] * -16 + embedding[2] * 19 + embedding[3] * 36 + embedding[4] * 10 + embedding[5] * 1 + embedding[6] * 14 + embedding[7] * 2 + embedding[8] * -13 + embedding[9] * -10;
    let score_4 = embedding[0] * -16 + embedding[1] * -19 + embedding[2] * -2 + embedding[3] * 13 + embedding[4] * 18 + embedding[5] * -20 + embedding[7] * 61 + embedding[8] * -6 + embedding[9] * -12;
    let score_5 = embedding[0] * -30 + embedding[1] * 3 + embedding[2] * -10 + embedding[3] * 7 + embedding[4] * 2 + embedding[5] * -23 + embedding[6] * 7 + embedding[7] * -7 + embedding[8] * 8 + embedding[9] * -6;
    let score_6 = embedding[0] * 7 + embedding[1] * -7 + embedding[2] * 12 + embedding[3] * 2 + embedding[4] * 8 + embedding[5] * 2 + embedding[6] * -15 + embedding[7] * 59 + embedding[8] * 3 + embedding[9] * -2;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_8 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_10 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_11 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_13 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_14 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_16 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -12 + embedding[9] * 7;
    let score_17 = embedding[0] * 6 + embedding[1] * 5 + embedding[2] * -9 + embedding[3] * -19 + embedding[4] * 12 + embedding[5] * 15 + embedding[6] * -8 + embedding[7] * -19 + embedding[8] * 9 + embedding[9] * -3;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 11;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_341(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 11 + embedding[2] * -5 + embedding[3] * -16 + embedding[4] * 11 + embedding[5] * 9 + embedding[6] * -5 + embedding[7] * -20 + embedding[8] * -2 + embedding[9] * -3;
    let score_1 = embedding[0] * 11 + embedding[1] * 1 + embedding[2] * -3 + embedding[3] * -15 + embedding[4] * 10 + embedding[5] * 6 + embedding[6] * -4 + embedding[7] * -19 + embedding[8] * -5 + embedding[9] * -2;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_3 = embedding[0] * -18 + embedding[1] * -22 + embedding[2] * 1 + embedding[3] * 26 + embedding[4] * -3 + embedding[5] * 12 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * 1 + embedding[9] * -11;
    let score_4 = embedding[0] * -15 + embedding[1] * 10 + embedding[2] * -15 + embedding[3] * 4 + embedding[4] * 1 + embedding[5] * -26 + embedding[6] * -10 + embedding[7] * 46 + embedding[8] * 8 + embedding[9] * 6;
    let score_5 = embedding[0] * 23 + embedding[1] * -23 + embedding[2] * 16 + embedding[3] * 43 + embedding[4] * 9 + embedding[5] * -1 + embedding[6] * 15 + embedding[7] * 1 + embedding[8] * -19 + embedding[9] * -15;
    let score_6 = embedding[0] * -6 + embedding[1] * 18 + embedding[2] * 11 + embedding[3] * 14 + embedding[4] * -13 + embedding[5] * -8 + embedding[6] * -7 + embedding[7] * 44 + embedding[8] * 35 + embedding[9] * 8;
    let score_7 = embedding[0] * -29 + embedding[1] * -4 + embedding[2] * -10 + embedding[3] * 17 + embedding[4] * -4 + embedding[5] * -21 + embedding[6] * 13 + embedding[7] * -8 + embedding[8] * 2 + embedding[9] * -11;
    let score_8 = embedding[0] * -13 + embedding[1] * 24 + embedding[2] * 3 + embedding[3] * -12 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * -12 + embedding[7] * 39 + embedding[8] * 19 + embedding[9] * 14;
    let score_9 = embedding[0] * 3 + embedding[2] * 10 + embedding[3] * 10 + embedding[4] * 2 + embedding[5] * -9 + embedding[6] * -14 + embedding[7] * 80 + embedding[8] * 9;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_17 = embedding[0] * 1 + embedding[1] * 2 + embedding[2] * -9 + embedding[3] * -12 + embedding[4] * 4 + embedding[5] * 8 + embedding[6] * -5 + embedding[7] * -12 + embedding[8] * 5 + embedding[9] * -2;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 54;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 8;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 13;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 15;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 11;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 29;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 12;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_342(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 14 + embedding[1] * -8 + embedding[2] * -10 + embedding[3] * -13 + embedding[4] * 15 + embedding[5] * 9 + embedding[6] * -12 + embedding[7] * -14 + embedding[8] * -3 + embedding[9] * -4;
    let score_1 = embedding[0] * 8 + embedding[1] * -6 + embedding[2] * -10 + embedding[3] * -9 + embedding[4] * 11 + embedding[5] * 9 + embedding[6] * -9 + embedding[7] * -7 + embedding[8] * 1 + embedding[9] * -5;
    let score_2 = embedding[0] * -5 + embedding[1] * -15 + embedding[2] * -3 + embedding[3] * 19 + embedding[4] * -1 + embedding[5] * 12 + embedding[6] * 9 + embedding[7] * 3 + embedding[8] * 3 + embedding[9] * -8;
    let score_3 = embedding[0] * -1 + embedding[1] * 14 + embedding[2] * 6 + embedding[3] * 12 + embedding[4] * -3 + embedding[5] * -3 + embedding[6] * -6 + embedding[7] * 22 + embedding[8] * 25 + embedding[9] * 7;
    let score_4 = embedding[0] * -20 + embedding[1] * -3 + embedding[2] * -7 + embedding[3] * 13 + embedding[4] * 1 + embedding[5] * -19 + embedding[6] * 12 + embedding[7] * -8 + embedding[8] * 5 + embedding[9] * -9;
    let score_5 = embedding[0] * -12 + embedding[1] * 19 + embedding[2] * 1 + embedding[3] * -13 + embedding[4] * -16 + embedding[5] * -6 + embedding[6] * -11 + embedding[7] * 28 + embedding[8] * 14 + embedding[9] * 13;
    let score_6 = embedding[0] * -16 + embedding[1] * 10 + embedding[2] * -10 + embedding[3] * 14 + embedding[4] * -20 + embedding[5] * -5 + embedding[6] * 7 + embedding[7] * 6 + embedding[8] * 11 + embedding[9] * 1;
    let score_7 = embedding[0] * -17 + embedding[1] * 17 + embedding[2] * 33 + embedding[3] * -8 + embedding[4] * -8 + embedding[5] * -1 + embedding[6] * 10 + embedding[8] * 9 + embedding[9] * -5;
    let score_8 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 9;
    let score_9 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 9;
    let score_10 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 9;
    let score_11 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 9;
    let score_12 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 9;
    let score_13 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 9;
    let score_14 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 9;
    let score_15 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 9;
    let score_16 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 9;
    let score_17 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 9;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 54;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 11;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 29;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 35;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 52;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_343(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -37 + embedding[1] * -23 + embedding[2] * -27 + embedding[3] * -3 + embedding[4] * 1 + embedding[5] * 21 + embedding[6] * -16 + embedding[7] * 13 + embedding[8] * 22 + embedding[9] * -15;
    let score_1 = embedding[0] * 40 + embedding[1] * 24 + embedding[2] * 7 + embedding[3] * -23 + embedding[4] * -4 + embedding[5] * -8 + embedding[7] * -12 + embedding[8] * -60 + embedding[9] * -34;
    let score_2 = embedding[0] * 41 + embedding[1] * -7 + embedding[2] * 17 + embedding[3] * -29 + embedding[4] * 29 + embedding[5] * -25 + embedding[6] * 48 + embedding[7] * -22 + embedding[8] * 3 + embedding[9] * 27;
    let score_3 = embedding[0] * 23 + embedding[1] * 12 + embedding[2] * 14 + embedding[3] * 31 + embedding[4] * 25 + embedding[5] * 6 + embedding[6] * -16 + embedding[7] * -32 + embedding[8] * -61 + embedding[9] * 44;
    let score_4 = embedding[0] * 30 + embedding[1] * 5 + embedding[2] * 46 + embedding[3] * -9 + embedding[4] * 13 + embedding[5] * -41 + embedding[6] * -22 + embedding[7] * -31 + embedding[8] * -30 + embedding[9] * -6;
    let score_5 = embedding[0] * -40 + embedding[1] * -24 + embedding[2] * -23 + embedding[3] * 34 + embedding[4] * -3 + embedding[5] * 17 + embedding[6] * 1 + embedding[7] * 30 + embedding[8] * 27 + embedding[9] * -22;
    let score_6 = embedding[0] * -15 + embedding[1] * 14 + embedding[2] * 9 + embedding[3] * -13 + embedding[4] * -17 + embedding[5] * 29 + embedding[6] * -3 + embedding[7] * 14 + embedding[8] * 11 + embedding[9] * 5;
    let score_7 = embedding[0] * -30 + embedding[1] * 6 + embedding[2] * -4 + embedding[3] * 13 + embedding[4] * -7 + embedding[5] * 9 + embedding[6] * -11 + embedding[7] * 38 + embedding[8] * 42 + embedding[9] * -3;
    let score_8 = embedding[0] * -41 + embedding[1] * 14 + embedding[2] * -7 + embedding[3] * -13 + embedding[4] * -26 + embedding[5] * 11 + embedding[6] * -14 + embedding[7] * 47 + embedding[8] * 30 + embedding[9] * 8;
    let score_9 = embedding[0] * 30 + embedding[1] * -7 + embedding[2] * 10 + embedding[3] * 9 + embedding[4] * 27 + embedding[5] * -18 + embedding[6] * 4 + embedding[7] * -3 + embedding[8] * -14 + embedding[9] * 57;
    let score_10 = embedding[0] * -31 + embedding[1] * -32 + embedding[2] * -24 + embedding[3] * 3 + embedding[4] * -14 + embedding[5] * 16 + embedding[6] * -13 + embedding[7] * 13 + embedding[8] * 16 + embedding[9] * -11;
    let score_11 = embedding[0] * -45 + embedding[1] * 16 + embedding[2] * 48 + embedding[3] * -11 + embedding[4] * -13 + embedding[5] * 9 + embedding[6] * 15 + embedding[7] * 1 + embedding[8] * 17 + embedding[9] * -10;
    let score_12 = embedding[0] * 15 + embedding[1] * 16 + embedding[2] * -4 + embedding[3] * 19 + embedding[4] * -16 + embedding[5] * 5 + embedding[6] * -35 + embedding[7] * -7 + embedding[8] * -40 + embedding[9] * 1;
    let score_13 = embedding[0] * 28 + embedding[2] * 3 + embedding[3] * -8 + embedding[4] * -10 + embedding[5] * -14 + embedding[6] * 12 + embedding[7] * -23 + embedding[8] * -4 + embedding[9] * 2;
    let score_14 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 13 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 7;
    let score_15 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 13 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 7;
    let score_16 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 13 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 7;
    let score_17 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * 13 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 20;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 15;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 29;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 23;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 61;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 52;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 100;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 93;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_344(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[2] * -1 + embedding[3] * -20 + embedding[4] * 28 + embedding[5] * 5 + embedding[6] * 44 + embedding[7] * -9 + embedding[8] * 23 + embedding[9] * 15;
    let score_1 = embedding[0] * 6 + embedding[1] * 24 + embedding[2] * -8 + embedding[3] * -22 + embedding[4] * -2 + embedding[5] * 11 + embedding[6] * 7 + embedding[7] * 1 + embedding[8] * -19 + embedding[9] * -37;
    let score_2 = embedding[0] * -10 + embedding[1] * 12 + embedding[2] * -5 + embedding[3] * 20 + embedding[4] * 25 + embedding[5] * 28 + embedding[6] * -2 + embedding[7] * -14 + embedding[8] * -11 + embedding[9] * 27;
    let score_3 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 32 + embedding[3] * -2 + embedding[5] * -21 + embedding[6] * -26 + embedding[7] * -18 + embedding[8] * 7 + embedding[9] * -18;
    let score_4 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * -3 + embedding[3] * -3 + embedding[4] * -13 + embedding[5] * -7 + embedding[6] * -20 + embedding[7] * -9 + embedding[8] * -1 + embedding[9] * -2;
    let score_5 = embedding[0] * 3 + embedding[1] * 22 + embedding[2] * -28 + embedding[3] * -7 + embedding[4] * 17 + embedding[5] * 25 + embedding[6] * 22 + embedding[7] * 27 + embedding[8] * -9 + embedding[9] * 14;
    let score_6 = embedding[1] * -31 + embedding[2] * -4 + embedding[3] * 10 + embedding[4] * -38 + embedding[5] * -14 + embedding[6] * -28 + embedding[7] * 13 + embedding[8] * 5 + embedding[9] * 14;
    let score_7 = embedding[0] * 1 + embedding[1] * -26 + embedding[2] * -5 + embedding[3] * 4 + embedding[4] * -32 + embedding[5] * -4 + embedding[6] * -16 + embedding[7] * 4 + embedding[8] * 2 + embedding[9] * 7;
    let score_8 = embedding[0] * 4 + embedding[3] * 10 + embedding[4] * 24 + embedding[5] * 1 + embedding[6] * 12 + embedding[7] * 4 + embedding[8] * 8 + embedding[9] * 40;
    let score_9 = embedding[0] * -2 + embedding[1] * -20 + embedding[2] * -22 + embedding[3] * 3 + embedding[4] * -33 + embedding[5] * 8 + embedding[6] * -2 + embedding[7] * 24 + embedding[8] * -2 + embedding[9] * 13;
    let score_10 = embedding[1] * 8 + embedding[2] * 17 + embedding[3] * -9 + embedding[4] * -29 + embedding[5] * -16 + embedding[6] * -16 + embedding[7] * 7 + embedding[8] * -11 + embedding[9] * -40;
    let score_11 = embedding[0] * -17 + embedding[1] * -3 + embedding[2] * 20 + embedding[3] * 35 + embedding[4] * -9 + embedding[6] * -34 + embedding[7] * -7 + embedding[8] * -3 + embedding[9] * 19;
    let score_12 = embedding[0] * 4 + embedding[1] * -17 + embedding[2] * 15 + embedding[3] * -7 + embedding[4] * -2 + embedding[5] * -20 + embedding[6] * 15 + embedding[7] * 3 + embedding[8] * 27 + embedding[9] * 11;
    let score_13 = embedding[0] * 3 + embedding[1] * 12 + embedding[2] * -6 + embedding[3] * 13 + embedding[4] * -13 + embedding[5] * 6 + embedding[6] * -20 + embedding[8] * -16 + embedding[9] * -5;
    let score_14 = embedding[0] * -5 + embedding[1] * -7 + embedding[2] * 31 + embedding[3] * -9 + embedding[4] * -21 + embedding[5] * -37 + embedding[6] * -16 + embedding[7] * 4 + embedding[8] * 10 + embedding[9] * -19;
    let score_15 = embedding[0] * 6 + embedding[1] * -2 + embedding[2] * 8 + embedding[3] * 3 + embedding[4] * -24 + embedding[5] * -9 + embedding[6] * -4 + embedding[7] * -20 + embedding[9] * -5;
    let score_16 = embedding[0] * 6 + embedding[1] * 16 + embedding[2] * -16 + embedding[3] * 12 + embedding[4] * 3 + embedding[5] * 7 + embedding[6] * 21 + embedding[7] * 22 + embedding[8] * 2 + embedding[9] * 12;
    let score_17 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 26;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 43;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 28;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 23;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 22;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 63;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 66;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 72;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 80;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 115;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 202;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_345(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 10 + embedding[1] * -2 + embedding[2] * 2 + embedding[3] * -18 + embedding[4] * 19 + embedding[5] * -3 + embedding[6] * 42 + embedding[7] * -2 + embedding[8] * 20 + embedding[9] * 14;
    let score_1 = embedding[0] * 4 + embedding[1] * 22 + embedding[2] * -3 + embedding[3] * -13 + embedding[4] * -6 + embedding[5] * 7 + embedding[6] * -1 + embedding[7] * 2 + embedding[8] * -21 + embedding[9] * -34;
    let score_2 = embedding[0] * -12 + embedding[1] * 13 + embedding[2] * 1 + embedding[3] * 25 + embedding[4] * 20 + embedding[5] * 21 + embedding[6] * -10 + embedding[7] * -12 + embedding[8] * -18 + embedding[9] * 24;
    let score_3 = embedding[0] * -1 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * -1 + embedding[5] * -14 + embedding[6] * -22 + embedding[7] * -15 + embedding[8] * 4 + embedding[9] * -12;
    let score_4 = embedding[0] * 4 + embedding[1] * -15 + embedding[2] * -12 + embedding[3] * -4 + embedding[4] * -9 + embedding[5] * -1 + embedding[6] * -14 + embedding[7] * -6 + embedding[8] * 6;
    let score_5 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * -8 + embedding[3] * -6 + embedding[4] * 13 + embedding[5] * 16 + embedding[6] * 7 + embedding[7] * -4 + embedding[8] * 2 + embedding[9] * -1;
    let score_6 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * -9 + embedding[3] * -7 + embedding[4] * 9 + embedding[5] * 14 + embedding[6] * 4 + embedding[7] * -3 + embedding[8] * 3 + embedding[9] * -1;
    let score_7 = embedding[0] * 2 + embedding[1] * -28 + embedding[2] * -10 + embedding[3] * 7 + embedding[4] * -31 + embedding[5] * -11 + embedding[6] * -24 + embedding[7] * 14 + embedding[8] * 9 + embedding[9] * 12;
    let score_8 = embedding[0] * 6 + embedding[1] * -6 + embedding[3] * 8 + embedding[4] * 15 + embedding[5] * -5 + embedding[6] * 8 + embedding[7] * 10 + embedding[8] * 8 + embedding[9] * 32;
    let score_9 = embedding[0] * -1 + embedding[1] * -24 + embedding[2] * -20 + embedding[3] * 3 + embedding[4] * -32 + embedding[5] * 2 + embedding[6] * -6 + embedding[7] * 24 + embedding[8] * 4 + embedding[9] * 11;
    let score_10 = embedding[0] * 2 + embedding[1] * 4 + embedding[2] * 8 + embedding[3] * -4 + embedding[4] * -30 + embedding[5] * -13 + embedding[6] * -18 + embedding[7] * 10 + embedding[8] * -8 + embedding[9] * -32;
    let score_11 = embedding[0] * -17 + embedding[1] * -7 + embedding[2] * 16 + embedding[3] * 33 + embedding[4] * -15 + embedding[5] * -2 + embedding[6] * -35 + embedding[7] * -3 + embedding[8] * -2 + embedding[9] * 13;
    let score_12 = embedding[0] * 6 + embedding[1] * -20 + embedding[2] * 5 + embedding[3] * -5 + embedding[4] * -11 + embedding[5] * -17 + embedding[6] * 4 + embedding[7] * 5 + embedding[8] * 23 + embedding[9] * 8;
    let score_13 = embedding[0] * -1 + embedding[1] * 13 + embedding[2] * -5 + embedding[3] * 15 + embedding[4] * -14 + embedding[5] * 4 + embedding[6] * -22 + embedding[7] * 1 + embedding[8] * -15 + embedding[9] * -5;
    let score_14 = embedding[0] * 8 + embedding[1] * -2 + embedding[2] * -2 + embedding[3] * 2 + embedding[4] * -19 + embedding[5] * -6 + embedding[6] * 1 + embedding[7] * -14 + embedding[8] * 4 + embedding[9] * -1;
    let score_15 = embedding[0] * -2 + embedding[1] * -11 + embedding[2] * 16 + embedding[3] * -8 + embedding[4] * -24 + embedding[5] * -29 + embedding[6] * -11 + embedding[7] * 10 + embedding[8] * 11 + embedding[9] * -11;
    let score_16 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 69;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 43;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 23;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 22;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 63;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 66;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 72;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 80;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 93;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 115;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_346(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 8 + embedding[1] * 1 + embedding[2] * 2 + embedding[3] * -19 + embedding[4] * 28 + embedding[5] * 4 + embedding[6] * 50 + embedding[7] * -8 + embedding[8] * 23 + embedding[9] * 19;
    let score_1 = embedding[0] * 2 + embedding[1] * 29 + embedding[2] * -8 + embedding[3] * -20 + embedding[4] * 1 + embedding[5] * 14 + embedding[6] * 4 + embedding[8] * -26 + embedding[9] * -38;
    let score_2 = embedding[0] * -8 + embedding[1] * 16 + embedding[2] * -3 + embedding[3] * 20 + embedding[4] * 27 + embedding[5] * 29 + embedding[6] * -2 + embedding[7] * -13 + embedding[8] * -16 + embedding[9] * 27;
    let score_3 = embedding[0] * -3 + embedding[1] * 2 + embedding[2] * 25 + embedding[3] * -2 + embedding[4] * 4 + embedding[5] * -14 + embedding[6] * -26 + embedding[7] * -18 + embedding[8] * 5 + embedding[9] * -17;
    let score_4 = embedding[0] * 3 + embedding[1] * -14 + embedding[2] * -13 + embedding[3] * -6 + embedding[4] * -7 + embedding[5] * 2 + embedding[6] * -16 + embedding[7] * -9 + embedding[8] * 5 + embedding[9] * -3;
    let score_5 = embedding[0] * 1 + embedding[1] * -27 + embedding[2] * -10 + embedding[3] * 5 + embedding[4] * -30 + embedding[5] * -9 + embedding[6] * -26 + embedding[7] * 11 + embedding[8] * 9 + embedding[9] * 9;
    let score_6 = embedding[0] * 5 + embedding[1] * -1 + embedding[2] * -2 + embedding[3] * 7 + embedding[4] * 25 + embedding[5] * 1 + embedding[6] * 15 + embedding[7] * 7 + embedding[8] * 9 + embedding[9] * 39;
    let score_7 = embedding[1] * -23 + embedding[2] * -22 + embedding[3] * 1 + embedding[4] * -30 + embedding[5] * 5 + embedding[6] * -8 + embedding[7] * 20 + embedding[8] * 2 + embedding[9] * 9;
    let score_8 = embedding[0] * 2 + embedding[1] * 4 + embedding[2] * 8 + embedding[3] * -5 + embedding[4] * -26 + embedding[5] * -11 + embedding[6] * -21 + embedding[7] * 7 + embedding[8] * -8 + embedding[9] * -34;
    let score_9 = embedding[0] * -16 + embedding[1] * -7 + embedding[2] * 16 + embedding[3] * 28 + embedding[4] * -15 + embedding[5] * -1 + embedding[6] * -37 + embedding[7] * -5 + embedding[9] * 10;
    let score_10 = embedding[0] * 4 + embedding[1] * -19 + embedding[2] * 5 + embedding[3] * -7 + embedding[4] * -7 + embedding[5] * -14 + embedding[6] * 4 + embedding[7] * 1 + embedding[8] * 24 + embedding[9] * 7;
    let score_11 = embedding[0] * 1 + embedding[1] * 12 + embedding[2] * -10 + embedding[3] * 12 + embedding[4] * -12 + embedding[5] * 8 + embedding[6] * -22 + embedding[7] * 1 + embedding[8] * -16 + embedding[9] * -8;
    let score_12 = embedding[0] * 7 + embedding[2] * -4 + embedding[4] * -17 + embedding[5] * -2 + embedding[6] * -1 + embedding[7] * -18 + embedding[8] * 3 + embedding[9] * -3;
    let score_13 = embedding[0] * -4 + embedding[1] * -9 + embedding[2] * 19 + embedding[3] * -9 + embedding[4] * -20 + embedding[5] * -29 + embedding[6] * -15 + embedding[7] * 5 + embedding[8] * 11 + embedding[9] * -15;
    let score_14 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 43;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 23;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 22;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 63;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 66;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 72;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 80;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 93;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 115;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_347(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 6 + embedding[1] * -6 + embedding[2] * -2 + embedding[3] * -21 + embedding[4] * 27 + embedding[5] * 3 + embedding[6] * 34 + embedding[7] * -9 + embedding[8] * 29 + embedding[9] * 13;
    let score_1 = embedding[0] * 8 + embedding[1] * 18 + embedding[2] * -12 + embedding[3] * -24 + embedding[4] * -4 + embedding[5] * 9 + embedding[6] * -2 + embedding[7] * 3 + embedding[8] * -15 + embedding[9] * -41;
    let score_2 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * -6 + embedding[3] * 19 + embedding[4] * 22 + embedding[5] * 25 + embedding[6] * -14 + embedding[7] * -15 + embedding[8] * -8 + embedding[9] * 23;
    let score_3 = embedding[0] * -2 + embedding[1] * -2 + embedding[2] * 20 + embedding[3] * -7 + embedding[4] * 3 + embedding[5] * -16 + embedding[6] * -28 + embedding[7] * -17 + embedding[8] * 9 + embedding[9] * -18;
    let score_4 = embedding[0] * 8 + embedding[1] * -20 + embedding[2] * -10 + embedding[3] * -5 + embedding[4] * -14 + embedding[5] * -5 + embedding[6] * -26 + embedding[7] * -8 + embedding[8] * 3 + embedding[9] * -2;
    let score_5 = embedding[0] * 5 + embedding[1] * -6 + embedding[2] * -4 + embedding[3] * 5 + embedding[4] * 23 + embedding[5] * -1 + embedding[6] * 4 + embedding[7] * 4 + embedding[8] * 13 + embedding[9] * 35;
    let score_6 = embedding[0] * 1 + embedding[1] * -18 + embedding[2] * -23 + embedding[3] * -4 + embedding[4] * -25 + embedding[5] * 9 + embedding[6] * -9 + embedding[7] * 16 + embedding[9] * 5;
    let score_7 = embedding[0] * 4 + embedding[1] * 6 + embedding[2] * -9 + embedding[3] * 9 + embedding[4] * -13 + embedding[5] * 6 + embedding[6] * -27 + embedding[7] * -1 + embedding[8] * -11 + embedding[9] * -8;
    let score_8 = embedding[0] * 10 + embedding[1] * -3 + embedding[2] * -3 + embedding[3] * -1 + embedding[4] * -18 + embedding[5] * -5 + embedding[6] * -7 + embedding[7] * -18 + embedding[8] * 4 + embedding[9] * -3;
    let score_9 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_10 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_11 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_12 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_13 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_14 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_15 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_16 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_17 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 23;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 22;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 80;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 93;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_348(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 6 + embedding[1] * -6 + embedding[2] * -3 + embedding[3] * -14 + embedding[4] * 17 + embedding[5] * 4 + embedding[6] * 15 + embedding[7] * -5 + embedding[8] * 18 + embedding[9] * 6;
    let score_1 = embedding[0] * 4 + embedding[1] * 6 + embedding[2] * -10 + embedding[3] * -15 + embedding[4] * -1 + embedding[5] * 7 + embedding[6] * -9 + embedding[7] * -1 + embedding[8] * -7 + embedding[9] * -22;
    let score_2 = embedding[0] * -4 + embedding[1] * 3 + embedding[2] * -6 + embedding[3] * 6 + embedding[4] * 15 + embedding[5] * 15 + embedding[6] * -11 + embedding[7] * -10 + embedding[8] * -3 + embedding[9] * 9;
    let score_3 = embedding[1] * -1 + embedding[2] * 17 + embedding[3] * -3 + embedding[4] * 4 + embedding[5] * -12 + embedding[6] * -22 + embedding[7] * -12 + embedding[8] * 7 + embedding[9] * -12;
    let score_4 = embedding[0] * 6 + embedding[1] * -9 + embedding[2] * -7 + embedding[3] * -7 + embedding[4] * -5 + embedding[6] * -13 + embedding[7] * -8 + embedding[9] * -4;
    let score_5 = embedding[0] * 6 + embedding[2] * -9 + embedding[3] * -7 + embedding[4] * 9 + embedding[5] * 7 + embedding[6] * -2 + embedding[7] * -8 + embedding[8] * -1 + embedding[9] * -2;
    let score_6 = embedding[0] * 6 + embedding[1] * -5 + embedding[2] * -1 + embedding[3] * 5 + embedding[4] * 13 + embedding[5] * -5 + embedding[7] * 9 + embedding[8] * 11 + embedding[9] * 24;
    let score_7 = embedding[1] * -11 + embedding[2] * -19 + embedding[3] * -3 + embedding[4] * -19 + embedding[5] * 8 + embedding[6] * -2 + embedding[7] * 17 + embedding[8] * -1 + embedding[9] * 3;
    let score_8 = embedding[0] * 5 + embedding[1] * 8 + embedding[2] * -6 + embedding[3] * 7 + embedding[4] * -12 + embedding[5] * 2 + embedding[6] * -18 + embedding[7] * 1 + embedding[8] * -9 + embedding[9] * -7;
    let score_9 = embedding[0] * 8 + embedding[1] * -2 + embedding[2] * -2 + embedding[4] * -16 + embedding[5] * -3 + embedding[6] * -4 + embedding[7] * -11 + embedding[8] * 1 + embedding[9] * -2;
    let score_10 = embedding[0] * -2 + embedding[1] * 3 + embedding[2] * 15 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 8;
    let score_11 = embedding[0] * -2 + embedding[1] * 3 + embedding[2] * 15 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 8;
    let score_12 = embedding[0] * -2 + embedding[1] * 3 + embedding[2] * 15 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 8;
    let score_13 = embedding[0] * -2 + embedding[1] * 3 + embedding[2] * 15 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 8;
    let score_14 = embedding[0] * -2 + embedding[1] * 3 + embedding[2] * 15 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 8;
    let score_15 = embedding[0] * -2 + embedding[1] * 3 + embedding[2] * 15 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 8;
    let score_16 = embedding[0] * -2 + embedding[1] * 3 + embedding[2] * 15 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 8;
    let score_17 = embedding[0] * -2 + embedding[1] * 3 + embedding[2] * 15 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 8;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 91;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 23;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 22;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 80;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 179;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_349(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[2] * -4 + embedding[3] * -15 + embedding[4] * 19 + embedding[5] * 7 + embedding[6] * 23 + embedding[7] * -6 + embedding[8] * 13 + embedding[9] * 6;
    let score_1 = embedding[0] * 3 + embedding[1] * 11 + embedding[2] * -10 + embedding[3] * -15 + embedding[4] * 3 + embedding[5] * 11 + embedding[6] * 1 + embedding[7] * -2 + embedding[8] * -9 + embedding[9] * -20;
    let score_2 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * -8 + embedding[3] * 5 + embedding[4] * 16 + embedding[5] * 19 + embedding[6] * -3 + embedding[7] * -10 + embedding[8] * -6 + embedding[9] * 10;
    let score_3 = embedding[0] * -3 + embedding[1] * 1 + embedding[2] * 19 + embedding[3] * -2 + embedding[4] * 4 + embedding[5] * -12 + embedding[6] * -19 + embedding[7] * -12 + embedding[8] * 5 + embedding[9] * -12;
    let score_4 = embedding[0] * 6 + embedding[1] * -17 + embedding[2] * -7 + embedding[4] * -16 + embedding[5] * -5 + embedding[6] * -19 + embedding[7] * -1 + embedding[8] * 1 + embedding[9] * 1;
    let score_5 = embedding[0] * 4 + embedding[1] * 5 + embedding[2] * -9 + embedding[3] * -7 + embedding[4] * 12 + embedding[5] * 11 + embedding[6] * 6 + embedding[7] * -8 + embedding[8] * -4;
    let score_6 = embedding[0] * 5 + embedding[1] * -2 + embedding[2] * -3 + embedding[3] * 5 + embedding[4] * 13 + embedding[5] * -3 + embedding[6] * 4 + embedding[7] * 9 + embedding[8] * 7 + embedding[9] * 26;
    let score_7 = embedding[1] * -14 + embedding[2] * -18 + embedding[4] * -23 + embedding[5] * 7 + embedding[6] * -3 + embedding[7] * 19 + embedding[8] * -2 + embedding[9] * 6;
    let score_8 = embedding[0] * 1 + embedding[1] * 1 + embedding[2] * 9 + embedding[3] * -4 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * -16 + embedding[7] * 8 + embedding[8] * -5 + embedding[9] * -24;
    let score_9 = embedding[0] * -11 + embedding[1] * -4 + embedding[2] * 13 + embedding[3] * 23 + embedding[4] * -11 + embedding[5] * -2 + embedding[6] * -30 + embedding[7] * -3 + embedding[8] * -2 + embedding[9] * 9;
    let score_10 = embedding[0] * 4 + embedding[1] * -13 + embedding[2] * 7 + embedding[3] * -5 + embedding[4] * -3 + embedding[5] * -12 + embedding[6] * 7 + embedding[7] * 3 + embedding[8] * 18 + embedding[9] * 6;
    let score_11 = embedding[0] * 3 + embedding[1] * 8 + embedding[2] * -7 + embedding[3] * 10 + embedding[4] * -14 + embedding[5] * 2 + embedding[6] * -17 + embedding[7] * 3 + embedding[8] * -8 + embedding[9] * -2;
    let score_12 = embedding[0] * 7 + embedding[1] * -1 + embedding[2] * -1 + embedding[3] * 3 + embedding[4] * -19 + embedding[5] * -3 + embedding[6] * -2 + embedding[7] * -11 + embedding[8] * 1;
    let score_13 = embedding[0] * -4 + embedding[1] * -7 + embedding[2] * 21 + embedding[3] * -6 + embedding[4] * -20 + embedding[5] * -28 + embedding[6] * -14 + embedding[7] * 7 + embedding[8] * 6 + embedding[9] * -12;
    let score_14 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -12 + embedding[9] * 7;
    let score_15 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -12 + embedding[9] * 7;
    let score_16 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -12 + embedding[9] * 7;
    let score_17 = embedding[0] * -4 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -12 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 91;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 23;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 22;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 63;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 66;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 72;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 80;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 93;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 115;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_350(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 10 + embedding[1] * -4 + embedding[2] * 2 + embedding[3] * -17 + embedding[4] * 15 + embedding[5] * -5 + embedding[6] * 38 + embedding[7] * -2 + embedding[8] * 19 + embedding[9] * 13;
    let score_1 = embedding[0] * 4 + embedding[1] * 20 + embedding[2] * -3 + embedding[3] * -11 + embedding[4] * -9 + embedding[5] * 5 + embedding[6] * -1 + embedding[7] * 3 + embedding[8] * -19 + embedding[9] * -31;
    let score_2 = embedding[0] * -12 + embedding[1] * 10 + embedding[2] * 2 + embedding[3] * 25 + embedding[4] * 16 + embedding[5] * 19 + embedding[6] * -11 + embedding[7] * -10 + embedding[8] * -16 + embedding[9] * 23;
    let score_3 = embedding[0] * -1 + embedding[1] * -1 + embedding[2] * 19 + embedding[3] * -1 + embedding[4] * -3 + embedding[5] * -16 + embedding[6] * -20 + embedding[7] * -12 + embedding[8] * 6 + embedding[9] * -10;
    let score_4 = embedding[0] * 5 + embedding[1] * -16 + embedding[2] * -9 + embedding[3] * -2 + embedding[4] * -10 + embedding[5] * -3 + embedding[6] * -13 + embedding[7] * -4 + embedding[8] * 6 + embedding[9] * 3;
    let score_5 = embedding[0] * -8 + embedding[1] * 7 + embedding[2] * -7 + embedding[3] * -4 + embedding[4] * 10 + embedding[5] * 14 + embedding[6] * 6 + embedding[7] * -3 + embedding[8] * 2;
    let score_6 = embedding[0] * -9 + embedding[1] * 4 + embedding[2] * -8 + embedding[3] * -5 + embedding[4] * 7 + embedding[5] * 13 + embedding[6] * 4 + embedding[7] * -2 + embedding[8] * 3 + embedding[9] * 1;
    let score_7 = embedding[0] * 2 + embedding[1] * -28 + embedding[2] * -6 + embedding[3] * 8 + embedding[4] * -31 + embedding[5] * -13 + embedding[6] * -23 + embedding[7] * 13 + embedding[8] * 9 + embedding[9] * 13;
    let score_8 = embedding[0] * 6 + embedding[1] * -7 + embedding[2] * 1 + embedding[3] * 9 + embedding[4] * 12 + embedding[5] * -7 + embedding[6] * 6 + embedding[7] * 11 + embedding[8] * 9 + embedding[9] * 32;
    let score_9 = embedding[0] * 3 + embedding[1] * -22 + embedding[2] * -17 + embedding[3] * 2 + embedding[4] * -28 + embedding[5] * -1 + embedding[6] * -6 + embedding[7] * 15 + embedding[8] * 4 + embedding[9] * 10;
    let score_10 = embedding[0] * 2 + embedding[1] * 3 + embedding[2] * 10 + embedding[3] * -3 + embedding[4] * -31 + embedding[5] * -14 + embedding[6] * -16 + embedding[7] * 11 + embedding[8] * -9 + embedding[9] * -30;
    let score_11 = embedding[0] * -13 + embedding[1] * -4 + embedding[2] * 16 + embedding[3] * 25 + embedding[4] * -7 + embedding[5] * -3 + embedding[6] * -29 + embedding[7] * -6 + embedding[9] * 10;
    let score_12 = embedding[0] * 4 + embedding[1] * -13 + embedding[2] * 8 + embedding[3] * -4 + embedding[4] * -5 + embedding[5] * -16 + embedding[6] * -4 + embedding[7] * -3 + embedding[8] * 17 + embedding[9] * 3;
    let score_13 = embedding[0] * 14 + embedding[1] * -5 + embedding[2] * 11 + embedding[3] * -10 + embedding[4] * 5 + embedding[5] * -15 + embedding[6] * 49 + embedding[7] * 7 + embedding[8] * 23 + embedding[9] * 19;
    let score_14 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * -8 + embedding[3] * 1 + embedding[4] * -4 + embedding[5] * 2 + embedding[6] * -7 + embedding[7] * -3 + embedding[8] * -4 + embedding[9] * -3;
    let score_15 = embedding[0] * 3 + embedding[1] * 23 + embedding[3] * -6 + embedding[4] * -13 + embedding[6] * 6 + embedding[7] * 11 + embedding[8] * -26 + embedding[9] * -27;
    let score_16 = embedding[0] * -13 + embedding[1] * 13 + embedding[2] * 8 + embedding[3] * 30 + embedding[4] * 11 + embedding[5] * 16 + embedding[6] * -5 + embedding[7] * -5 + embedding[8] * -21 + embedding[9] * 22;
    let score_17 = embedding[0] * -17 + embedding[1] * 20 + embedding[2] * 5 + embedding[3] * 14 + embedding[4] * -16 + embedding[5] * 5 + embedding[6] * -12 + embedding[7] * 14 + embedding[8] * -22 + embedding[9] * -21;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 69;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 43;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 23;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 22;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 63;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 66;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 72;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 144;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 80;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 150;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 159;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 162;
    }
    best_palette
}

fn predict_pixel_351(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 6 + embedding[1] * -2 + embedding[3] * -21 + embedding[4] * 21 + embedding[5] * 2 + embedding[6] * 42 + embedding[7] * -8 + embedding[8] * 20 + embedding[9] * 15;
    let score_1 = embedding[0] * 2 + embedding[1] * 24 + embedding[2] * -7 + embedding[3] * -19 + embedding[4] * -3 + embedding[5] * 13 + embedding[6] * 3 + embedding[7] * -2 + embedding[8] * -23 + embedding[9] * -32;
    let score_2 = embedding[0] * -11 + embedding[1] * 13 + embedding[2] * -1 + embedding[3] * 21 + embedding[4] * 20 + embedding[5] * 27 + embedding[6] * -7 + embedding[7] * -13 + embedding[8] * -18 + embedding[9] * 25;
    let score_3 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 32 + embedding[5] * -17 + embedding[6] * -27 + embedding[7] * -18 + embedding[8] * 4 + embedding[9] * -15;
    let score_4 = embedding[0] * 5 + embedding[1] * -16 + embedding[2] * -10 + embedding[3] * -6 + embedding[4] * -8 + embedding[5] * 1 + embedding[6] * -14 + embedding[7] * -8 + embedding[8] * 4 + embedding[9] * 2;
    let score_5 = embedding[0] * 3 + embedding[1] * 18 + embedding[2] * -24 + embedding[3] * -8 + embedding[4] * 13 + embedding[5] * 24 + embedding[6] * 21 + embedding[7] * 27 + embedding[8] * -11 + embedding[9] * 14;
    let score_6 = embedding[0] * 1 + embedding[1] * -29 + embedding[2] * -8 + embedding[3] * 5 + embedding[4] * -31 + embedding[5] * -10 + embedding[6] * -25 + embedding[7] * 12 + embedding[8] * 8 + embedding[9] * 14;
    let score_7 = embedding[0] * 2 + embedding[1] * 16 + embedding[2] * -5 + embedding[3] * 33 + embedding[4] * 19 + embedding[5] * -7 + embedding[6] * 15 + embedding[7] * 5 + embedding[8] * 7 + embedding[9] * -14;
    let score_8 = embedding[0] * 5 + embedding[1] * -5 + embedding[3] * 7 + embedding[4] * 18 + embedding[5] * -1 + embedding[6] * 9 + embedding[7] * 6 + embedding[8] * 7 + embedding[9] * 36;
    let score_9 = embedding[1] * -24 + embedding[2] * -20 + embedding[4] * -31 + embedding[5] * 5 + embedding[6] * -6 + embedding[7] * 22 + embedding[8] * 2 + embedding[9] * 13;
    let score_10 = embedding[1] * 3 + embedding[2] * 13 + embedding[3] * -5 + embedding[4] * -28 + embedding[5] * -13 + embedding[6] * -20 + embedding[7] * 5 + embedding[8] * -8 + embedding[9] * -32;
    let score_11 = embedding[0] * -18 + embedding[1] * -6 + embedding[2] * 19 + embedding[3] * 30 + embedding[4] * -13 + embedding[6] * -36 + embedding[7] * -5 + embedding[8] * -4 + embedding[9] * 12;
    let score_12 = embedding[0] * 4 + embedding[1] * -21 + embedding[2] * 10 + embedding[3] * -8 + embedding[4] * -10 + embedding[5] * -19 + embedding[6] * 5 + embedding[7] * 3 + embedding[8] * 24 + embedding[9] * 9;
    let score_13 = embedding[1] * 13 + embedding[2] * -8 + embedding[3] * 12 + embedding[4] * -14 + embedding[5] * 9 + embedding[6] * -21 + embedding[7] * -1 + embedding[8] * -17 + embedding[9] * -3;
    let score_14 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 25 + embedding[3] * -9 + embedding[4] * -22 + embedding[5] * -33 + embedding[6] * -14 + embedding[7] * 6 + embedding[8] * 12 + embedding[9] * -13;
    let score_15 = embedding[0] * 6 + embedding[1] * -3 + embedding[2] * 6 + embedding[3] * 3 + embedding[4] * -20 + embedding[5] * -9 + embedding[7] * -19 + embedding[8] * 3 + embedding[9] * -2;
    let score_16 = embedding[0] * 8 + embedding[1] * 9 + embedding[2] * -14 + embedding[3] * 18 + embedding[4] * -2 + embedding[5] * -2 + embedding[6] * 15 + embedding[7] * 20 + embedding[8] * 4 + embedding[9] * 7;
    let score_17 = embedding[0] * 3 + embedding[1] * -4 + embedding[2] * 7 + embedding[3] * 22 + embedding[4] * -18 + embedding[5] * -28 + embedding[6] * 2 + embedding[7] * 15 + embedding[8] * 12 + embedding[9] * -14;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 26;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 43;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 23;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 22;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 63;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 66;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 72;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 80;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 115;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 202;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 196;
    }
    best_palette
}

fn predict_pixel_352(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 10 + embedding[1] * 2 + embedding[2] * -9 + embedding[3] * -12 + embedding[4] * 22 + embedding[5] * 5 + embedding[6] * -5 + embedding[7] * -21 + embedding[8] * -4 + embedding[9] * -8;
    let score_1 = embedding[0] * -6 + embedding[1] * -9 + embedding[2] * 2 + embedding[3] * 25 + embedding[4] * 6 + embedding[5] * 10 + embedding[6] * 16 + embedding[7] * -2 + embedding[8] * 1 + embedding[9] * -12;
    let score_2 = embedding[0] * 2 + embedding[1] * -24 + embedding[2] * -7 + embedding[3] * 9 + embedding[4] * -25 + embedding[5] * -8 + embedding[6] * -19 + embedding[7] * 14 + embedding[8] * 5 + embedding[9] * 11;
    let score_3 = embedding[0] * 3 + embedding[1] * 14 + embedding[2] * 16 + embedding[3] * -6 + embedding[4] * -12 + embedding[5] * 13 + embedding[6] * -2 + embedding[7] * 6 + embedding[8] * 5 + embedding[9] * 7;
    let score_4 = embedding[0] * -2 + embedding[1] * 24 + embedding[2] * 12 + embedding[3] * 10 + embedding[4] * 3 + embedding[5] * -4 + embedding[7] * 13 + embedding[8] * 22 + embedding[9] * 4;
    let score_5 = embedding[0] * 4 + embedding[1] * -21 + embedding[2] * -8 + embedding[3] * 5 + embedding[4] * -19 + embedding[6] * -8 + embedding[7] * 3 + embedding[9] * 4;
    let score_6 = embedding[0] * -14 + embedding[1] * 28 + embedding[2] * 7 + embedding[3] * -11 + embedding[4] * -12 + embedding[5] * -3 + embedding[6] * -2 + embedding[7] * 21 + embedding[8] * 12 + embedding[9] * 11;
    let score_7 = embedding[0] * 1 + embedding[1] * -21 + embedding[2] * -17 + embedding[3] * 6 + embedding[4] * -31 + embedding[5] * 2 + embedding[6] * -4 + embedding[7] * 23 + embedding[9] * 10;
    let score_8 = embedding[0] * -13 + embedding[1] * 23 + embedding[2] * -3 + embedding[3] * 8 + embedding[4] * -12 + embedding[5] * -1 + embedding[6] * 9 + embedding[7] * 2 + embedding[8] * 9 + embedding[9] * 2;
    let score_9 = embedding[0] * -20 + embedding[1] * 27 + embedding[2] * 38 + embedding[3] * -9 + embedding[4] * -4 + embedding[5] * -3 + embedding[6] * 14 + embedding[7] * -4 + embedding[8] * 9 + embedding[9] * -6;
    let score_10 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_17 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 6;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 43;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 20;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 15;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 28;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 29;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 22;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 35;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 52;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_353(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_1 = embedding[0] * 12 + embedding[1] * -11 + embedding[3] * -17 + embedding[4] * 7 + embedding[5] * 12 + embedding[6] * -19 + embedding[7] * 3 + embedding[8] * 4;
    let score_2 = embedding[0] * -6 + embedding[1] * -14 + embedding[2] * -8 + embedding[3] * 13 + embedding[4] * 13 + embedding[5] * 3 + embedding[6] * 9 + embedding[7] * 4 + embedding[8] * -7 + embedding[9] * -14;
    let score_3 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * -14 + embedding[4] * 10 + embedding[5] * -17 + embedding[6] * -6 + embedding[7] * 26 + embedding[8] * 6 + embedding[9] * -2;
    let score_4 = embedding[0] * -22 + embedding[1] * 5 + embedding[2] * -9 + embedding[3] * 13 + embedding[4] * 3 + embedding[5] * -20 + embedding[6] * 17 + embedding[7] * -11 + embedding[8] * 6 + embedding[9] * -13;
    let score_5 = embedding[0] * -7 + embedding[1] * 15 + embedding[2] * 2 + embedding[3] * -20 + embedding[4] * -15 + embedding[5] * -2 + embedding[6] * -13 + embedding[7] * 20 + embedding[8] * 10 + embedding[9] * 14;
    let score_6 = embedding[0] * -11 + embedding[1] * 15 + embedding[2] * -14 + embedding[3] * 16 + embedding[4] * -14 + embedding[5] * -6 + embedding[6] * 11 + embedding[7] * -9 + embedding[8] * 8 + embedding[9] * -3;
    let score_7 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_8 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_9 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_10 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_11 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_12 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_13 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_14 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_15 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_16 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_17 = embedding[0] * 11 + embedding[1] * -1 + embedding[2] * -9 + embedding[3] * -12 + embedding[4] * 11 + embedding[5] * 13 + embedding[6] * -8 + embedding[7] * -20 + embedding[8] * 5 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 11;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 29;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 35;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_354(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_1 = embedding[0] * 3 + embedding[1] * -8 + embedding[2] * 27 + embedding[3] * -20 + embedding[4] * 3 + embedding[5] * 20 + embedding[6] * -11 + embedding[7] * 18 + embedding[8] * 4;
    let score_2 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * -25 + embedding[3] * 11 + embedding[4] * 7 + embedding[5] * -26 + embedding[6] * -7 + embedding[7] * 27 + embedding[8] * 5 + embedding[9] * -1;
    let score_3 = embedding[0] * -10 + embedding[1] * -21 + embedding[2] * -10 + embedding[3] * 19 + embedding[4] * 16 + embedding[5] * -20 + embedding[6] * 2 + embedding[7] * 45 + embedding[8] * -10 + embedding[9] * -15;
    let score_4 = embedding[0] * -22 + embedding[1] * 1 + embedding[2] * -18 + embedding[3] * 15 + embedding[5] * -23 + embedding[6] * 12 + embedding[7] * -23 + embedding[8] * 5 + embedding[9] * -10;
    let score_5 = embedding[0] * -7 + embedding[1] * 19 + embedding[2] * -2 + embedding[3] * -21 + embedding[4] * -20 + embedding[6] * -9 + embedding[7] * 11 + embedding[8] * 8 + embedding[9] * 14;
    let score_6 = embedding[0] * 11 + embedding[1] * -9 + embedding[2] * 21 + embedding[3] * -2 + embedding[4] * 5 + embedding[5] * 10 + embedding[6] * -17 + embedding[7] * 57 + embedding[8] * 2 + embedding[9] * -1;
    let score_7 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_8 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_9 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_10 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_11 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_12 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_13 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_14 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_15 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_16 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 6;
    let score_17 = embedding[0] * 16 + embedding[1] * 1 + embedding[2] * -17 + embedding[3] * -11 + embedding[4] * 11 + embedding[5] * 17 + embedding[6] * -4 + embedding[7] * -36 + embedding[8] * 4 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 11;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 29;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 12;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_355(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_1 = embedding[0] * 2 + embedding[1] * 9 + embedding[2] * -5 + embedding[3] * -6 + embedding[4] * 17 + embedding[5] * 9 + embedding[7] * -14 + embedding[8] * 9 + embedding[9] * -7;
    let score_2 = embedding[0] * 8 + embedding[1] * -13 + embedding[2] * 51 + embedding[3] * -3 + embedding[4] * -10 + embedding[5] * 10 + embedding[6] * 10 + embedding[7] * 19 + embedding[8] * -20 + embedding[9] * 5;
    let score_3 = embedding[0] * -11 + embedding[1] * -26 + embedding[2] * -8 + embedding[3] * 13 + embedding[4] * 9 + embedding[5] * -13 + embedding[6] * -1 + embedding[7] * 38 + embedding[8] * -7 + embedding[9] * -7;
    let score_4 = embedding[0] * -10 + embedding[1] * -6 + embedding[2] * -6 + embedding[3] * -14 + embedding[4] * -6 + embedding[5] * -4 + embedding[6] * -14 + embedding[7] * 18 + embedding[8] * 4 + embedding[9] * 4;
    let score_5 = embedding[1] * 1 + embedding[2] * 61 + embedding[4] * 16 + embedding[5] * 6 + embedding[6] * 16 + embedding[7] * 40 + embedding[8] * -18 + embedding[9] * -1;
    let score_6 = embedding[0] * -13 + embedding[1] * 6 + embedding[2] * 16 + embedding[3] * -11 + embedding[4] * 7 + embedding[5] * -3 + embedding[6] * -4 + embedding[7] * 26 + embedding[8] * -1;
    let score_7 = embedding[0] * -18 + embedding[1] * -14 + embedding[2] * 19 + embedding[3] * 15 + embedding[4] * 22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 40 + embedding[8] * -12 + embedding[9] * -15;
    let score_8 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_9 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_10 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_11 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_12 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_13 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_14 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_15 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_16 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[2] * -27 + embedding[3] * -8 + embedding[4] * 7 + embedding[5] * 8 + embedding[6] * -11 + embedding[7] * -21 + embedding[8] * 11 + embedding[9] * -2;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 31;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 176;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 152;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 167;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_356(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 51 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 10 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -19 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 5 + embedding[1] * -1 + embedding[2] * -36 + embedding[3] * -15 + embedding[4] * 23 + embedding[5] * 10 + embedding[6] * -17 + embedding[7] * -24 + embedding[8] * 18 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_357(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -19 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_358(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_359(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_360(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_361(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_362(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 52 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 11 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -19 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 5 + embedding[1] * -2 + embedding[2] * -35 + embedding[3] * -15 + embedding[4] * 23 + embedding[5] * 10 + embedding[6] * -17 + embedding[7] * -23 + embedding[8] * 18 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_363(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_1 = embedding[0] * 9 + embedding[1] * -9 + embedding[2] * 55 + embedding[3] * -3 + embedding[4] * 2 + embedding[5] * 11 + embedding[6] * 7 + embedding[7] * 22 + embedding[8] * -23 + embedding[9] * 2;
    let score_2 = embedding[0] * 24 + embedding[1] * -17 + embedding[2] * 13 + embedding[3] * 34 + embedding[4] * 7 + embedding[5] * 3 + embedding[6] * 11 + embedding[7] * -1 + embedding[8] * -9 + embedding[9] * -11;
    let score_3 = embedding[0] * -18 + embedding[1] * -16 + embedding[2] * -12 + embedding[3] * 4 + embedding[4] * 16 + embedding[5] * -12 + embedding[6] * -3 + embedding[7] * 36 + embedding[8] * 2 + embedding[9] * -10;
    let score_4 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_5 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_6 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_7 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_8 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_9 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_10 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_11 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_12 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_13 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_14 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_15 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_16 = embedding[0] * -8 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 8;
    let score_17 = embedding[0] * 2 + embedding[1] * 12 + embedding[2] * -32 + embedding[3] * -24 + embedding[4] * 10 + embedding[5] * 11 + embedding[6] * -19 + embedding[7] * -28 + embedding[8] * 22 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 9;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 13;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_364(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_1 = embedding[0] * -7 + embedding[1] * -9 + embedding[2] * 15 + embedding[3] * -23 + embedding[4] * 5 + embedding[5] * 17 + embedding[6] * -12 + embedding[7] * 9 + embedding[8] * 6 + embedding[9] * -2;
    let score_2 = embedding[0] * 27 + embedding[1] * -14 + embedding[2] * 12 + embedding[3] * 38 + embedding[4] * 8 + embedding[5] * 2 + embedding[6] * 13 + embedding[7] * -4 + embedding[8] * -13 + embedding[9] * -13;
    let score_3 = embedding[0] * -13 + embedding[1] * -18 + embedding[2] * -4 + embedding[3] * 6 + embedding[4] * 16 + embedding[5] * -8 + embedding[6] * -2 + embedding[7] * 36 + embedding[8] * -6 + embedding[9] * -11;
    let score_4 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_5 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_6 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_7 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_8 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_9 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_10 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_11 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_12 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_13 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_14 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_15 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_16 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 7;
    let score_17 = embedding[0] * 7 + embedding[1] * 12 + embedding[2] * -28 + embedding[3] * -16 + embedding[4] * 12 + embedding[5] * 9 + embedding[6] * -13 + embedding[7] * -26 + embedding[8] * 13 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 13;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_365(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 8;
    let score_1 = embedding[0] * 8 + embedding[1] * -1 + embedding[2] * -5 + embedding[3] * -18 + embedding[4] * 11 + embedding[5] * 10 + embedding[6] * -6 + embedding[7] * -21 + embedding[8] * -2 + embedding[9] * -2;
    let score_2 = embedding[0] * -15 + embedding[1] * -22 + embedding[3] * 21 + embedding[4] * 1 + embedding[5] * 11 + embedding[6] * 14 + embedding[7] * 12 + embedding[8] * -1 + embedding[9] * -11;
    let score_3 = embedding[0] * -15 + embedding[1] * 9 + embedding[2] * -16 + embedding[3] * 2 + embedding[4] * 3 + embedding[5] * -26 + embedding[6] * -10 + embedding[7] * 44 + embedding[8] * 8 + embedding[9] * 7;
    let score_4 = embedding[0] * 20 + embedding[1] * -22 + embedding[2] * 13 + embedding[3] * 39 + embedding[4] * 9 + embedding[6] * 15 + embedding[7] * -3 + embedding[8] * -17 + embedding[9] * -14;
    let score_5 = embedding[0] * -5 + embedding[1] * 17 + embedding[2] * 10 + embedding[3] * 9 + embedding[4] * -8 + embedding[5] * -8 + embedding[6] * -8 + embedding[7] * 39 + embedding[8] * 33 + embedding[9] * 8;
    let score_6 = embedding[0] * -30 + embedding[1] * -7 + embedding[2] * -9 + embedding[3] * 15 + embedding[4] * -2 + embedding[5] * -20 + embedding[6] * 12 + embedding[7] * -12 + embedding[8] * 2 + embedding[9] * -11;
    let score_7 = embedding[0] * -11 + embedding[1] * 22 + embedding[2] * 2 + embedding[3] * -15 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * -13 + embedding[7] * 33 + embedding[8] * 18 + embedding[9] * 14;
    let score_8 = embedding[0] * 2 + embedding[1] * 1 + embedding[2] * 10 + embedding[3] * 7 + embedding[4] * 3 + embedding[5] * -9 + embedding[6] * -15 + embedding[7] * 77 + embedding[8] * 9 + embedding[9] * 1;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 8;
    let score_17 = embedding[0] * 8 + embedding[1] * 2 + embedding[2] * -10 + embedding[3] * -18 + embedding[4] * 10 + embedding[5] * 9 + embedding[6] * -6 + embedding[7] * -23 + embedding[8] * 1 + embedding[9] * -2;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 13;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 15;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 11;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 29;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 12;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_366(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 15 + embedding[1] * -3 + embedding[2] * -5 + embedding[3] * -17 + embedding[4] * 12 + embedding[5] * 12 + embedding[6] * -5 + embedding[7] * -25 + embedding[8] * -4 + embedding[9] * -4;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 7;
    let score_2 = embedding[0] * -14 + embedding[1] * -24 + embedding[2] * -1 + embedding[3] * 24 + embedding[5] * 13 + embedding[6] * 14 + embedding[7] * 13 + embedding[9] * -13;
    let score_3 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * -16 + embedding[3] * 6 + embedding[4] * 3 + embedding[5] * -23 + embedding[6] * -9 + embedding[7] * 42 + embedding[8] * 7 + embedding[9] * 4;
    let score_4 = embedding[0] * -3 + embedding[1] * 14 + embedding[2] * 10 + embedding[3] * 14 + embedding[4] * -10 + embedding[5] * -6 + embedding[6] * -6 + embedding[7] * 39 + embedding[8] * 33 + embedding[9] * 6;
    let score_5 = embedding[0] * -27 + embedding[1] * -8 + embedding[2] * -10 + embedding[3] * 17 + embedding[4] * -2 + embedding[5] * -19 + embedding[6] * 13 + embedding[7] * -11 + embedding[8] * 2 + embedding[9] * -13;
    let score_6 = embedding[0] * -9 + embedding[1] * 20 + embedding[2] * 3 + embedding[3] * -12 + embedding[4] * -16 + embedding[5] * -6 + embedding[6] * -12 + embedding[7] * 33 + embedding[8] * 17 + embedding[9] * 12;
    let score_7 = embedding[0] * 5 + embedding[1] * -1 + embedding[2] * 8 + embedding[3] * 10 + embedding[4] * 3 + embedding[5] * -6 + embedding[6] * -14 + embedding[7] * 74 + embedding[8] * 9 + embedding[9] * -1;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 7;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 7;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 7;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 7;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 7;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 7;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 7;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 7;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 7;
    let score_17 = embedding[0] * 5 + embedding[1] * -1 + embedding[2] * -10 + embedding[3] * -11 + embedding[4] * 7 + embedding[5] * 10 + embedding[6] * -5 + embedding[7] * -17 + embedding[8] * 3 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 15;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 11;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 29;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 12;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_367(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -16 + embedding[1] * -8 + embedding[2] * -20 + embedding[3] * -5 + embedding[4] * 20 + embedding[5] * 19 + embedding[6] * -7 + embedding[7] * -8 + embedding[8] * 8 + embedding[9] * -12;
    let score_1 = embedding[0] * 28 + embedding[2] * 2 + embedding[3] * -37 + embedding[4] * -4 + embedding[5] * -22 + embedding[6] * 46 + embedding[7] * 1 + embedding[8] * 5 + embedding[9] * -15;
    let score_2 = embedding[0] * 26 + embedding[1] * 7 + embedding[2] * -5 + embedding[3] * -38 + embedding[4] * -14 + embedding[5] * -17 + embedding[6] * 34 + embedding[7] * 7 + embedding[8] * -8 + embedding[9] * -31;
    let score_3 = embedding[0] * 1 + embedding[1] * -3 + embedding[2] * 5 + embedding[3] * 25 + embedding[4] * 19 + embedding[5] * 4 + embedding[6] * -2 + embedding[7] * -13 + embedding[8] * -12 + embedding[9] * 42;
    let score_4 = embedding[0] * 15 + embedding[1] * -3 + embedding[2] * -11 + embedding[3] * -5 + embedding[4] * 11 + embedding[5] * 1 + embedding[7] * -17 + embedding[8] * -16 + embedding[9] * 7;
    let score_5 = embedding[0] * 12 + embedding[1] * -2 + embedding[2] * 15 + embedding[3] * -20 + embedding[4] * -4 + embedding[5] * -26 + embedding[6] * 5 + embedding[7] * -8 + embedding[8] * 4 + embedding[9] * -21;
    let score_6 = embedding[0] * -29 + embedding[1] * -17 + embedding[2] * -10 + embedding[3] * 29 + embedding[4] * 5 + embedding[5] * 25 + embedding[6] * 13 + embedding[7] * 9 + embedding[8] * 10 + embedding[9] * -15;
    let score_7 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 13 + embedding[3] * -5 + embedding[4] * -24 + embedding[5] * 28 + embedding[6] * -6 + embedding[7] * 20 + embedding[8] * 12 + embedding[9] * 8;
    let score_8 = embedding[0] * -15 + embedding[1] * 13 + embedding[2] * 13 + embedding[3] * 51 + embedding[4] * -22 + embedding[5] * 2 + embedding[6] * -41 + embedding[7] * -7 + embedding[8] * -52 + embedding[9] * 5;
    let score_9 = embedding[0] * -7 + embedding[1] * 16 + embedding[2] * 9 + embedding[3] * 32 + embedding[4] * -32 + embedding[5] * -6 + embedding[6] * -36 + embedding[8] * -59 + embedding[9] * -16;
    let score_10 = embedding[0] * -20 + embedding[1] * 16 + embedding[2] * 9 + embedding[3] * 14 + embedding[4] * -2 + embedding[5] * 5 + embedding[6] * -5 + embedding[7] * 35 + embedding[8] * 40 + embedding[9] * 3;
    let score_11 = embedding[0] * -2 + embedding[1] * -17 + embedding[2] * 24 + embedding[3] * 20 + embedding[4] * 15 + embedding[5] * -17 + embedding[6] * 22 + embedding[7] * -7 + embedding[8] * 22 + embedding[9] * 52;
    let score_12 = embedding[0] * -28 + embedding[1] * 31 + embedding[2] * 2 + embedding[3] * -10 + embedding[4] * -20 + embedding[5] * 7 + embedding[6] * -10 + embedding[7] * 46 + embedding[8] * 25 + embedding[9] * 15;
    let score_13 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 9 + embedding[3] * 27 + embedding[4] * 14 + embedding[5] * -17 + embedding[6] * -1 + embedding[7] * 10 + embedding[8] * 9 + embedding[9] * 61;
    let score_14 = embedding[0] * -39 + embedding[1] * 22 + embedding[2] * 39 + embedding[3] * -6 + embedding[4] * -4 + embedding[5] * 8 + embedding[6] * 13 + embedding[7] * 1 + embedding[8] * 18 + embedding[9] * -9;
    let score_15 = embedding[0] * -16 + embedding[1] * 6 + embedding[2] * 26 + embedding[3] * 34 + embedding[4] * -36 + embedding[5] * -18 + embedding[6] * -44 + embedding[7] * -6 + embedding[8] * -41 + embedding[9] * -17;
    let score_16 = embedding[0] * -3 + embedding[1] * 12 + embedding[2] * -6 + embedding[3] * 37 + embedding[4] * -30 + embedding[6] * -37 + embedding[7] * 2 + embedding[8] * -31 + embedding[9] * -2;
    let score_17 = embedding[0] * -5 + embedding[1] * -22 + embedding[2] * 39 + embedding[3] * 46 + embedding[5] * -46 + embedding[6] * -12 + embedding[7] * 7 + embedding[8] * 23 + embedding[9] * 58;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 6;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 20;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 15;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 38;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 29;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 23;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 52;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 79;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 100;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 123;
    }
    best_palette
}

fn predict_pixel_368(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 15 + embedding[1] * -7 + embedding[2] * -4 + embedding[3] * -31 + embedding[4] * 23 + embedding[5] * -4 + embedding[6] * 48 + embedding[7] * -9 + embedding[8] * 32 + embedding[9] * 6;
    let score_1 = embedding[0] * 12 + embedding[1] * 25 + embedding[2] * -12 + embedding[3] * -31 + embedding[4] * -9 + embedding[5] * 9 + embedding[6] * 3 + embedding[7] * 4 + embedding[8] * -22 + embedding[9] * -55;
    let score_2 = embedding[0] * -13 + embedding[1] * 2 + embedding[2] * -9 + embedding[3] * 32 + embedding[4] * 21 + embedding[5] * 31 + embedding[6] * -24 + embedding[7] * -17 + embedding[8] * -14 + embedding[9] * 30;
    let score_3 = embedding[1] * -1 + embedding[2] * 24 + embedding[3] * -7 + embedding[4] * 5 + embedding[5] * -19 + embedding[6] * -26 + embedding[7] * -19 + embedding[8] * 10 + embedding[9] * -24;
    let score_4 = embedding[0] * -4 + embedding[1] * -27 + embedding[2] * -7 + embedding[3] * 11 + embedding[4] * -19 + embedding[5] * -5 + embedding[6] * -32 + embedding[7] * 5 + embedding[8] * 10 + embedding[9] * 15;
    let score_5 = embedding[0] * 5 + embedding[1] * -6 + embedding[2] * -7 + embedding[3] * 12 + embedding[4] * 22 + embedding[5] * 5 + embedding[6] * -2 + embedding[7] * 5 + embedding[8] * 10 + embedding[9] * 41;
    let score_6 = embedding[0] * -6 + embedding[1] * -21 + embedding[2] * -10 + embedding[3] * 3 + embedding[4] * -13 + embedding[5] * 6 + embedding[6] * -17 + embedding[7] * -4 + embedding[8] * 7 + embedding[9] * 7;
    let score_7 = embedding[0] * 2 + embedding[1] * 16 + embedding[2] * -11 + embedding[3] * 17 + embedding[4] * -8 + embedding[5] * 14 + embedding[6] * -28 + embedding[7] * -4 + embedding[8] * -23 + embedding[9] * -3;
    let score_8 = embedding[0] * 12 + embedding[1] * 6 + embedding[2] * -2 + embedding[3] * -13 + embedding[4] * -11 + embedding[5] * -4 + embedding[6] * 13 + embedding[7] * -17 + embedding[8] * 9 + embedding[9] * -16;
    let score_9 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_10 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_11 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 18 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_12 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_13 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 18 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_14 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_15 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_16 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_17 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 23;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 61;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 100;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 93;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_369(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 11 + embedding[1] * -9 + embedding[3] * -24 + embedding[4] * 16 + embedding[5] * -8 + embedding[6] * 43 + embedding[7] * -6 + embedding[8] * 29 + embedding[9] * 10;
    let score_1 = embedding[0] * 9 + embedding[1] * 20 + embedding[2] * -4 + embedding[3] * -23 + embedding[4] * -14 + embedding[5] * 1 + embedding[6] * 4 + embedding[7] * 2 + embedding[8] * -20 + embedding[9] * -47;
    let score_2 = embedding[0] * -13 + embedding[1] * 1 + embedding[2] * -4 + embedding[3] * 32 + embedding[4] * 16 + embedding[5] * 23 + embedding[6] * -18 + embedding[7] * -13 + embedding[8] * -14 + embedding[9] * 34;
    let score_3 = embedding[0] * -3 + embedding[1] * -4 + embedding[2] * 34 + embedding[3] * -1 + embedding[4] * -5 + embedding[5] * -27 + embedding[6] * -24 + embedding[7] * -18 + embedding[8] * 9 + embedding[9] * -21;
    let score_4 = embedding[0] * 5 + embedding[1] * 13 + embedding[2] * -29 + embedding[3] * -4 + embedding[4] * 9 + embedding[5] * 19 + embedding[6] * 17 + embedding[7] * 31 + embedding[8] * -6 + embedding[9] * 15;
    let score_5 = embedding[0] * -4 + embedding[1] * -27 + embedding[2] * -2 + embedding[3] * 13 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * -26 + embedding[7] * 8 + embedding[8] * 8 + embedding[9] * 18;
    let score_6 = embedding[0] * -6 + embedding[1] * -22 + embedding[2] * -4 + embedding[3] * 5 + embedding[4] * -17 + embedding[6] * -12 + embedding[7] * -1 + embedding[8] * 6 + embedding[9] * 10;
    let score_7 = embedding[0] * 4 + embedding[1] * -8 + embedding[2] * -1 + embedding[3] * 18 + embedding[4] * 17 + embedding[5] * -3 + embedding[6] * 1 + embedding[7] * 6 + embedding[8] * 9 + embedding[9] * 43;
    let score_8 = embedding[0] * -1 + embedding[1] * 14 + embedding[2] * -7 + embedding[3] * 22 + embedding[4] * -14 + embedding[5] * 9 + embedding[6] * -24 + embedding[7] * -1 + embedding[8] * -22 + embedding[9] * 3;
    let score_9 = embedding[0] * 12 + embedding[1] * -8 + embedding[2] * 2 + embedding[3] * -17 + embedding[4] * 11 + embedding[5] * -10 + embedding[6] * 47 + embedding[7] * -5 + embedding[8] * 23 + embedding[9] * 14;
    let score_10 = embedding[0] * 9 + embedding[1] * 21 + embedding[2] * -2 + embedding[3] * -16 + embedding[4] * -29 + embedding[5] * -6 + embedding[6] * 9 + embedding[7] * 8 + embedding[8] * -26 + embedding[9] * -45;
    let score_11 = embedding[0] * -20 + embedding[1] * 12 + embedding[2] * -2 + embedding[3] * 10 + embedding[4] * -13 + embedding[5] * 18 + embedding[6] * -17 + embedding[7] * 8 + embedding[8] * -21 + embedding[9] * -14;
    let score_12 = embedding[0] * -21 + embedding[1] * 12 + embedding[2] * -2 + embedding[3] * 25 + embedding[4] * -4 + embedding[5] * 23 + embedding[6] * -25 + embedding[7] * 2 + embedding[8] * -20 + embedding[9] * 6;
    let score_13 = embedding[0] * -13 + embedding[1] * 5 + embedding[2] * 2 + embedding[3] * 50 + embedding[4] * 6 + embedding[5] * 19 + embedding[6] * -14 + embedding[7] * -7 + embedding[8] * -18 + embedding[9] * 46;
    let score_14 = embedding[0] * 7 + embedding[1] * -5 + embedding[2] * 3 + embedding[3] * -22 + embedding[4] * 5 + embedding[5] * -8 + embedding[6] * 38 + embedding[7] * 5 + embedding[8] * 21 + embedding[9] * -2;
    let score_15 = embedding[0] * 8 + embedding[1] * -1 + embedding[2] * 13 + embedding[3] * -3 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 10 + embedding[7] * -18 + embedding[8] * 9 + embedding[9] * -11;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 30 + embedding[3] * -1 + embedding[4] * -9 + embedding[5] * -26 + embedding[6] * -14 + embedding[7] * -15 + embedding[9] * -16;
    let score_17 = embedding[1] * -10 + embedding[2] * 3 + embedding[3] * 19 + embedding[4] * 14 + embedding[5] * -6 + embedding[6] * 14 + embedding[7] * 8 + embedding[8] * 9 + embedding[9] * 36;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 26;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 28;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 80;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 155;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 157;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 163;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 166;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 160;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 168;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 190;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 204;
    }
    best_palette
}

fn predict_pixel_370(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 11 + embedding[1] * -9 + embedding[3] * -24 + embedding[4] * 16 + embedding[5] * -7 + embedding[6] * 43 + embedding[7] * -6 + embedding[8] * 28 + embedding[9] * 10;
    let score_1 = embedding[0] * 9 + embedding[1] * 20 + embedding[2] * -4 + embedding[3] * -24 + embedding[4] * -14 + embedding[5] * 1 + embedding[6] * 3 + embedding[7] * 3 + embedding[8] * -20 + embedding[9] * -47;
    let score_2 = embedding[0] * -13 + embedding[1] * 1 + embedding[2] * -4 + embedding[3] * 31 + embedding[4] * 16 + embedding[5] * 23 + embedding[6] * -19 + embedding[7] * -13 + embedding[8] * -14 + embedding[9] * 34;
    let score_3 = embedding[0] * -3 + embedding[1] * -4 + embedding[2] * 35 + embedding[3] * -1 + embedding[4] * -5 + embedding[5] * -27 + embedding[6] * -24 + embedding[7] * -17 + embedding[8] * 8 + embedding[9] * -21;
    let score_4 = embedding[0] * 5 + embedding[1] * 13 + embedding[2] * -28 + embedding[3] * -5 + embedding[4] * 9 + embedding[5] * 19 + embedding[6] * 17 + embedding[7] * 32 + embedding[8] * -7 + embedding[9] * 15;
    let score_5 = embedding[0] * -4 + embedding[1] * -27 + embedding[2] * -1 + embedding[3] * 13 + embedding[4] * -23 + embedding[5] * -11 + embedding[6] * -26 + embedding[7] * 8 + embedding[8] * 8 + embedding[9] * 18;
    let score_6 = embedding[0] * -7 + embedding[1] * -21 + embedding[2] * -4 + embedding[3] * 5 + embedding[4] * -17 + embedding[6] * -12 + embedding[7] * -1 + embedding[8] * 6 + embedding[9] * 10;
    let score_7 = embedding[0] * 4 + embedding[1] * -8 + embedding[3] * 17 + embedding[4] * 17 + embedding[5] * -2 + embedding[6] * 1 + embedding[7] * 6 + embedding[8] * 9 + embedding[9] * 43;
    let score_8 = embedding[0] * -1 + embedding[1] * 14 + embedding[2] * -6 + embedding[3] * 22 + embedding[4] * -14 + embedding[5] * 9 + embedding[6] * -24 + embedding[7] * -1 + embedding[8] * -22 + embedding[9] * 3;
    let score_9 = embedding[0] * 12 + embedding[1] * -8 + embedding[2] * 3 + embedding[3] * -17 + embedding[4] * 11 + embedding[5] * -10 + embedding[6] * 46 + embedding[7] * -4 + embedding[8] * 23 + embedding[9] * 14;
    let score_10 = embedding[0] * 9 + embedding[1] * 21 + embedding[2] * -1 + embedding[3] * -16 + embedding[4] * -29 + embedding[5] * -6 + embedding[6] * 8 + embedding[7] * 8 + embedding[8] * -26 + embedding[9] * -45;
    let score_11 = embedding[0] * -20 + embedding[1] * 13 + embedding[2] * -2 + embedding[3] * 10 + embedding[4] * -14 + embedding[5] * 18 + embedding[6] * -18 + embedding[7] * 8 + embedding[8] * -21 + embedding[9] * -14;
    let score_12 = embedding[0] * -21 + embedding[1] * 13 + embedding[2] * -1 + embedding[3] * 25 + embedding[4] * -4 + embedding[5] * 23 + embedding[6] * -26 + embedding[7] * 3 + embedding[8] * -20 + embedding[9] * 6;
    let score_13 = embedding[0] * -13 + embedding[1] * 5 + embedding[2] * 3 + embedding[3] * 50 + embedding[4] * 6 + embedding[5] * 19 + embedding[6] * -14 + embedding[7] * -7 + embedding[8] * -18 + embedding[9] * 46;
    let score_14 = embedding[0] * 7 + embedding[1] * -4 + embedding[2] * 3 + embedding[3] * -22 + embedding[4] * 5 + embedding[5] * -8 + embedding[6] * 38 + embedding[7] * 5 + embedding[8] * 21 + embedding[9] * -2;
    let score_15 = embedding[0] * 7 + embedding[1] * -1 + embedding[2] * 13 + embedding[3] * -4 + embedding[4] * -19 + embedding[5] * -15 + embedding[6] * 10 + embedding[7] * -18 + embedding[8] * 9 + embedding[9] * -11;
    let score_16 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 30 + embedding[3] * -1 + embedding[4] * -10 + embedding[5] * -25 + embedding[6] * -15 + embedding[7] * -14 + embedding[9] * -16;
    let score_17 = embedding[1] * -10 + embedding[2] * 3 + embedding[3] * 19 + embedding[4] * 14 + embedding[5] * -5 + embedding[6] * 14 + embedding[7] * 8 + embedding[8] * 9 + embedding[9] * 36;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 26;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 28;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 80;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 155;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 157;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 163;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 166;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 160;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 168;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 190;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 204;
    }
    best_palette
}

fn predict_pixel_371(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 12 + embedding[1] * -17 + embedding[2] * -2 + embedding[3] * -28 + embedding[4] * 21 + embedding[5] * -8 + embedding[6] * 37 + embedding[7] * -8 + embedding[8] * 37 + embedding[9] * 11;
    let score_1 = embedding[0] * 8 + embedding[1] * 16 + embedding[2] * -8 + embedding[3] * -26 + embedding[4] * -16 + embedding[5] * 3 + embedding[6] * -10 + embedding[7] * 2 + embedding[8] * -21 + embedding[9] * -54;
    let score_2 = embedding[0] * -13 + embedding[1] * -2 + embedding[2] * -5 + embedding[3] * 34 + embedding[4] * 19 + embedding[5] * 25 + embedding[6] * -30 + embedding[7] * -19 + embedding[8] * -12 + embedding[9] * 33;
    let score_3 = embedding[0] * -2 + embedding[1] * -6 + embedding[2] * 32 + embedding[3] * -4 + embedding[4] * -2 + embedding[5] * -25 + embedding[6] * -35 + embedding[7] * -19 + embedding[8] * 10 + embedding[9] * -26;
    let score_4 = embedding[0] * 7 + embedding[1] * 8 + embedding[2] * -33 + embedding[3] * -9 + embedding[4] * 12 + embedding[5] * 20 + embedding[6] * 9 + embedding[7] * 33 + embedding[8] * -3 + embedding[9] * 12;
    let score_5 = embedding[0] * -4 + embedding[1] * -17 + embedding[2] * -10 + embedding[3] * -6 + embedding[4] * -6 + embedding[5] * 6 + embedding[6] * -7 + embedding[7] * -11 + embedding[8] * 8 + embedding[9] * 1;
    let score_6 = embedding[0] * 6 + embedding[1] * -14 + embedding[2] * -1 + embedding[3] * 13 + embedding[4] * 21 + embedding[5] * -3 + embedding[6] * -3 + embedding[7] * 3 + embedding[8] * 14 + embedding[9] * 41;
    let score_7 = embedding[0] * 1 + embedding[1] * 12 + embedding[2] * -8 + embedding[3] * 21 + embedding[4] * -13 + embedding[5] * 11 + embedding[6] * -33 + embedding[7] * -5 + embedding[8] * -22 + embedding[9] * -4;
    let score_8 = embedding[0] * 9 + embedding[1] * -6 + embedding[2] * 10 + embedding[3] * -6 + embedding[4] * -14 + embedding[5] * -12 + embedding[6] * 4 + embedding[7] * -21 + embedding[8] * 12 + embedding[9] * -13;
    let score_9 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_10 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_11 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_12 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_13 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_14 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_15 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_16 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let score_17 = embedding[0] * -2 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 26;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 28;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 23;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 1;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 93;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_372(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 17 + embedding[1] * 3 + embedding[2] * -1 + embedding[3] * -9 + embedding[4] * 16 + embedding[5] * 2 + embedding[6] * -4 + embedding[7] * -19 + embedding[8] * -8 + embedding[9] * -1;
    let score_1 = embedding[0] * -3 + embedding[1] * -9 + embedding[2] * -5 + embedding[3] * -10 + embedding[5] * 5 + embedding[6] * 11 + embedding[7] * 8 + embedding[8] * 22;
    let score_2 = embedding[0] * -6 + embedding[2] * -8 + embedding[3] * -9 + embedding[4] * -15 + embedding[5] * 8 + embedding[6] * -5 + embedding[7] * 11 + embedding[8] * 3 + embedding[9] * -21;
    let score_3 = embedding[0] * -15 + embedding[1] * -5 + embedding[2] * -3 + embedding[3] * 14 + embedding[4] * 2 + embedding[5] * 14 + embedding[6] * -14 + embedding[8] * 7 + embedding[9] * 11;
    let score_4 = embedding[0] * -10 + embedding[1] * -5 + embedding[2] * 13 + embedding[3] * 3 + embedding[4] * -7 + embedding[5] * -5 + embedding[6] * -19 + embedding[7] * -2 + embedding[8] * 15 + embedding[9] * -11;
    let score_5 = embedding[0] * 7 + embedding[1] * 1 + embedding[2] * -8 + embedding[3] * -7 + embedding[4] * 10 + embedding[5] * 7 + embedding[6] * -3 + embedding[7] * -9 + embedding[8] * -2 + embedding[9] * -4;
    let score_6 = embedding[0] * 6 + embedding[1] * 7 + embedding[2] * -20 + embedding[3] * -6 + embedding[4] * 5 + embedding[5] * 15 + embedding[6] * 8 + embedding[7] * 20 + embedding[8] * -2 + embedding[9] * 7;
    let score_7 = embedding[0] * -6 + embedding[1] * -14 + embedding[2] * -5 + embedding[3] * 1 + embedding[4] * -14 + embedding[5] * 6 + embedding[6] * -7 + embedding[7] * 2 + embedding[8] * 8 + embedding[9] * -1;
    let score_8 = embedding[0] * 10 + embedding[1] * 6 + embedding[2] * -2 + embedding[3] * 9 + embedding[4] * -10 + embedding[5] * -1 + embedding[6] * -16 + embedding[7] * -1 + embedding[8] * -3 + embedding[9] * -4;
    let score_9 = embedding[0] * 12 + embedding[1] * -3 + embedding[2] * 2 + embedding[3] * 4 + embedding[4] * -14 + embedding[5] * -4 + embedding[7] * -13 + embedding[8] * 2 + embedding[9] * -1;
    let score_10 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_11 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_12 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_13 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_14 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_15 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_16 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_17 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 91;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 106;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 28;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 80;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 179;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_373(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 17 + embedding[1] * 3 + embedding[2] * -2 + embedding[3] * -9 + embedding[4] * 15 + embedding[5] * 2 + embedding[6] * -6 + embedding[7] * -19 + embedding[8] * -8 + embedding[9] * -1;
    let score_1 = embedding[0] * -2 + embedding[1] * -9 + embedding[2] * -6 + embedding[3] * -10 + embedding[4] * 1 + embedding[5] * 5 + embedding[6] * 10 + embedding[7] * 7 + embedding[8] * 22;
    let score_2 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * -8 + embedding[3] * -10 + embedding[4] * -14 + embedding[5] * 8 + embedding[6] * -6 + embedding[7] * 11 + embedding[8] * 3 + embedding[9] * -22;
    let score_3 = embedding[0] * -14 + embedding[1] * -5 + embedding[2] * -4 + embedding[3] * 13 + embedding[4] * 3 + embedding[5] * 14 + embedding[6] * -15 + embedding[7] * -1 + embedding[8] * 7 + embedding[9] * 10;
    let score_4 = embedding[0] * -10 + embedding[1] * -5 + embedding[2] * 12 + embedding[3] * 3 + embedding[4] * -7 + embedding[5] * -4 + embedding[6] * -20 + embedding[7] * -3 + embedding[8] * 15 + embedding[9] * -11;
    let score_5 = embedding[0] * 8 + embedding[1] * 1 + embedding[2] * -9 + embedding[3] * -7 + embedding[4] * 11 + embedding[5] * 7 + embedding[6] * -5 + embedding[7] * -9 + embedding[8] * -2 + embedding[9] * -4;
    let score_6 = embedding[0] * 6 + embedding[1] * 7 + embedding[2] * -21 + embedding[3] * -6 + embedding[4] * 5 + embedding[5] * 16 + embedding[6] * 7 + embedding[7] * 19 + embedding[8] * -2 + embedding[9] * 7;
    let score_7 = embedding[0] * -5 + embedding[1] * -13 + embedding[2] * -6 + embedding[3] * 1 + embedding[4] * -13 + embedding[5] * 6 + embedding[6] * -8 + embedding[7] * 1 + embedding[8] * 8 + embedding[9] * -1;
    let score_8 = embedding[0] * 12 + embedding[1] * -3 + embedding[2] * 1 + embedding[3] * 3 + embedding[4] * -13 + embedding[5] * -4 + embedding[6] * -1 + embedding[7] * -14 + embedding[8] * 2 + embedding[9] * -1;
    let score_9 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_10 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_11 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_12 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_13 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_14 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_15 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_16 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let score_17 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 91;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 26;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 28;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 93;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_374(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 13 + embedding[1] * -9 + embedding[2] * -1 + embedding[3] * -27 + embedding[4] * 18 + embedding[5] * -7 + embedding[6] * 43 + embedding[7] * -9 + embedding[8] * 29 + embedding[9] * 8;
    let score_1 = embedding[0] * 10 + embedding[1] * 21 + embedding[2] * -5 + embedding[3] * -26 + embedding[4] * -13 + embedding[5] * 1 + embedding[6] * 4 + embedding[7] * 1 + embedding[8] * -20 + embedding[9] * -50;
    let score_2 = embedding[0] * -12 + embedding[1] * 1 + embedding[2] * -5 + embedding[3] * 31 + embedding[4] * 19 + embedding[5] * 24 + embedding[6] * -19 + embedding[7] * -16 + embedding[8] * -14 + embedding[9] * 32;
    let score_3 = embedding[0] * -2 + embedding[1] * -3 + embedding[2] * 35 + embedding[3] * -4 + embedding[4] * -3 + embedding[5] * -27 + embedding[6] * -25 + embedding[7] * -20 + embedding[8] * 9 + embedding[9] * -23;
    let score_4 = embedding[0] * 7 + embedding[1] * 14 + embedding[2] * -31 + embedding[3] * -7 + embedding[4] * 12 + embedding[5] * 21 + embedding[6] * 17 + embedding[7] * 30 + embedding[8] * -6 + embedding[9] * 14;
    let score_5 = embedding[0] * -3 + embedding[1] * -28 + embedding[2] * -3 + embedding[3] * 11 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * -28 + embedding[7] * 6 + embedding[8] * 9 + embedding[9] * 16;
    let score_6 = embedding[0] * -5 + embedding[1] * -22 + embedding[2] * -5 + embedding[3] * 3 + embedding[4] * -16 + embedding[5] * 1 + embedding[6] * -13 + embedding[7] * -3 + embedding[8] * 7 + embedding[9] * 9;
    let score_7 = embedding[0] * 5 + embedding[1] * -8 + embedding[2] * -2 + embedding[3] * 15 + embedding[4] * 20 + embedding[5] * -1 + embedding[6] * 1 + embedding[7] * 4 + embedding[8] * 10 + embedding[9] * 42;
    let score_8 = embedding[1] * 14 + embedding[2] * -8 + embedding[3] * 20 + embedding[4] * -12 + embedding[5] * 11 + embedding[6] * -26 + embedding[7] * -3 + embedding[8] * -22 + embedding[9] * 1;
    let score_9 = embedding[0] * -18 + embedding[1] * 13 + embedding[2] * -3 + embedding[3] * 7 + embedding[4] * -11 + embedding[5] * 19 + embedding[6] * -19 + embedding[7] * 6 + embedding[8] * -21 + embedding[9] * -17;
    let score_10 = embedding[0] * -20 + embedding[1] * 13 + embedding[2] * -3 + embedding[3] * 23 + embedding[4] * -2 + embedding[5] * 24 + embedding[6] * -26 + embedding[8] * -20 + embedding[9] * 5;
    let score_11 = embedding[0] * 9 + embedding[1] * -4 + embedding[2] * 2 + embedding[3] * -25 + embedding[4] * 8 + embedding[5] * -7 + embedding[6] * 38 + embedding[7] * 3 + embedding[8] * 22 + embedding[9] * -4;
    let score_12 = embedding[0] * 9 + embedding[1] * -1 + embedding[2] * 13 + embedding[3] * -6 + embedding[4] * -17 + embedding[5] * -15 + embedding[6] * 9 + embedding[7] * -20 + embedding[8] * 10 + embedding[9] * -13;
    let score_13 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 13 + embedding[3] * 23 + embedding[4] * -15 + embedding[5] * -15 + embedding[6] * -11 + embedding[7] * -9 + embedding[8] * -3 + embedding[9] * 2;
    let score_14 = embedding[0] * -1 + embedding[1] * 3 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 6;
    let score_15 = embedding[0] * -1 + embedding[1] * 3 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 6;
    let score_16 = embedding[0] * -1 + embedding[1] * 3 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 6;
    let score_17 = embedding[0] * -1 + embedding[1] * 3 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 26;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 28;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 80;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 163;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 166;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 168;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 93;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 207;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_375(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 13 + embedding[1] * -9 + embedding[3] * -26 + embedding[4] * 18 + embedding[5] * -8 + embedding[6] * 50 + embedding[7] * -5 + embedding[8] * 30 + embedding[9] * 10;
    let score_1 = embedding[0] * 11 + embedding[1] * 23 + embedding[2] * -9 + embedding[3] * -26 + embedding[4] * -13 + embedding[5] * 4 + embedding[6] * 7 + embedding[7] * 6 + embedding[8] * -22 + embedding[9] * -48;
    let score_2 = embedding[0] * -14 + embedding[1] * 1 + embedding[2] * -6 + embedding[3] * 35 + embedding[4] * 17 + embedding[5] * 25 + embedding[6] * -19 + embedding[7] * -13 + embedding[8] * -15 + embedding[9] * 36;
    let score_3 = embedding[0] * -1 + embedding[1] * -2 + embedding[2] * 26 + embedding[3] * -2 + embedding[5] * -23 + embedding[6] * -22 + embedding[7] * -15 + embedding[8] * 9 + embedding[9] * -18;
    let score_4 = embedding[0] * -6 + embedding[1] * -28 + embedding[2] * -3 + embedding[3] * 16 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * -27 + embedding[7] * 8 + embedding[8] * 8 + embedding[9] * 19;
    let score_5 = embedding[0] * -8 + embedding[1] * -22 + embedding[2] * -6 + embedding[3] * 8 + embedding[4] * -17 + embedding[5] * 2 + embedding[6] * -13 + embedding[7] * -1 + embedding[8] * 6 + embedding[9] * 12;
    let score_6 = embedding[0] * 3 + embedding[1] * -8 + embedding[2] * -3 + embedding[3] * 17 + embedding[4] * 19 + embedding[6] * 2 + embedding[7] * 8 + embedding[8] * 8 + embedding[9] * 45;
    let score_7 = embedding[1] * 15 + embedding[2] * -7 + embedding[3] * 23 + embedding[4] * -14 + embedding[5] * 10 + embedding[6] * -24 + embedding[7] * -1 + embedding[8] * -25 + embedding[9] * 2;
    let score_8 = embedding[0] * 14 + embedding[1] * -8 + embedding[2] * 2 + embedding[3] * -19 + embedding[4] * 13 + embedding[5] * -11 + embedding[6] * 53 + embedding[7] * -4 + embedding[8] * 26 + embedding[9] * 14;
    let score_9 = embedding[0] * 12 + embedding[1] * 24 + embedding[2] * -6 + embedding[3] * -18 + embedding[4] * -28 + embedding[5] * -3 + embedding[6] * 12 + embedding[7] * 11 + embedding[8] * -27 + embedding[9] * -47;
    let score_10 = embedding[0] * -19 + embedding[1] * 15 + embedding[2] * -5 + embedding[3] * 10 + embedding[4] * -12 + embedding[5] * 20 + embedding[6] * -16 + embedding[7] * 10 + embedding[8] * -23 + embedding[9] * -17;
    let score_11 = embedding[0] * -15 + embedding[1] * 4 + embedding[3] * 52 + embedding[4] * 7 + embedding[5] * 22 + embedding[6] * -15 + embedding[7] * -8 + embedding[8] * -19 + embedding[9] * 48;
    let score_12 = embedding[0] * -23 + embedding[1] * 12 + embedding[2] * -4 + embedding[3] * 28 + embedding[4] * -4 + embedding[5] * 25 + embedding[6] * -26 + embedding[7] * 4 + embedding[8] * -22 + embedding[9] * 8;
    let score_13 = embedding[0] * 10 + embedding[1] * -3 + embedding[2] * 2 + embedding[3] * -24 + embedding[4] * 6 + embedding[5] * -7 + embedding[6] * 44 + embedding[7] * 7 + embedding[8] * 22 + embedding[9] * -2;
    let score_14 = embedding[0] * 11 + embedding[1] * 4 + embedding[2] * 1 + embedding[3] * -8 + embedding[4] * -16 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * -14 + embedding[8] * 8 + embedding[9] * -10;
    let score_15 = embedding[0] * -5 + embedding[1] * 5 + embedding[2] * 26 + embedding[3] * -4 + embedding[4] * -6 + embedding[5] * -22 + embedding[6] * -11 + embedding[7] * -12 + embedding[8] * 1 + embedding[9] * -15;
    let score_16 = embedding[1] * -10 + embedding[2] * 3 + embedding[3] * 20 + embedding[4] * 15 + embedding[5] * -5 + embedding[6] * 15 + embedding[7] * 8 + embedding[8] * 10 + embedding[9] * 37;
    let score_17 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 11 + embedding[3] * 24 + embedding[4] * -16 + embedding[5] * -12 + embedding[6] * -11 + embedding[7] * -7 + embedding[8] * -2 + embedding[9] * 3;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 28;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 23;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 80;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 155;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 157;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 163;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 160;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 166;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 168;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 93;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 190;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 204;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 207;
    }
    best_palette
}

fn predict_pixel_376(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 11 + embedding[1] * -8 + embedding[2] * -13 + embedding[3] * -10 + embedding[4] * 19 + embedding[5] * 4 + embedding[6] * -12 + embedding[7] * -15 + embedding[8] * -1 + embedding[9] * -5;
    let score_1 = embedding[0] * -2 + embedding[1] * -17 + embedding[2] * -4 + embedding[3] * 23 + embedding[4] * 4 + embedding[5] * 10 + embedding[6] * 11 + embedding[9] * -9;
    let score_2 = embedding[0] * 2 + embedding[1] * 4 + embedding[2] * 12 + embedding[3] * -5 + embedding[4] * -19 + embedding[5] * 14 + embedding[6] * -7 + embedding[7] * 12 + embedding[8] * 5 + embedding[9] * 8;
    let score_3 = embedding[0] * -1 + embedding[1] * 15 + embedding[2] * 9 + embedding[3] * 10 + embedding[4] * -2 + embedding[5] * -4 + embedding[6] * -2 + embedding[7] * 20 + embedding[8] * 22 + embedding[9] * 6;
    let score_4 = embedding[0] * -11 + embedding[1] * 20 + embedding[2] * 6 + embedding[3] * -11 + embedding[4] * -17 + embedding[5] * -4 + embedding[6] * -6 + embedding[7] * 26 + embedding[8] * 14 + embedding[9] * 14;
    let score_5 = embedding[0] * 7 + embedding[1] * -18 + embedding[2] * -9 + embedding[4] * -3 + embedding[5] * 1 + embedding[6] * -8 + embedding[7] * -8 + embedding[8] * -2 + embedding[9] * -1;
    let score_6 = embedding[0] * -9 + embedding[1] * 14 + embedding[2] * -7 + embedding[3] * 10 + embedding[4] * -15 + embedding[5] * -1 + embedding[6] * 4 + embedding[7] * 5 + embedding[8] * 9 + embedding[9] * 5;
    let score_7 = embedding[0] * -18 + embedding[1] * 17 + embedding[2] * 33 + embedding[3] * -7 + embedding[4] * -9 + embedding[5] * -3 + embedding[6] * 7 + embedding[7] * 1 + embedding[8] * 11 + embedding[9] * -3;
    let score_8 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 11;
    let score_9 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 11;
    let score_10 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 11;
    let score_11 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 11;
    let score_12 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 11;
    let score_13 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 11;
    let score_14 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 11;
    let score_15 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 11;
    let score_16 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 11;
    let score_17 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 11;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 6;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 20;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 29;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 61;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 35;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 52;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_377(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_1 = embedding[0] * 8 + embedding[1] * -8 + embedding[2] * 7 + embedding[3] * -17 + embedding[5] * 15 + embedding[6] * -12 + embedding[7] * 1 + embedding[8] * 5;
    let score_2 = embedding[0] * -4 + embedding[1] * -15 + embedding[2] * -10 + embedding[3] * 16 + embedding[4] * 12 + embedding[5] * 1 + embedding[6] * 10 + embedding[7] * 1 + embedding[8] * -9 + embedding[9] * -13;
    let score_3 = embedding[0] * -12 + embedding[1] * 5 + embedding[2] * -18 + embedding[3] * 6 + embedding[4] * 7 + embedding[5] * -25 + embedding[6] * -7 + embedding[7] * 31 + embedding[8] * 4;
    let score_4 = embedding[0] * -23 + embedding[1] * 2 + embedding[2] * -15 + embedding[3] * 17 + embedding[4] * 2 + embedding[5] * -25 + embedding[6] * 15 + embedding[7] * -11 + embedding[8] * 6 + embedding[9] * -11;
    let score_5 = embedding[0] * -8 + embedding[1] * 15 + embedding[2] * 2 + embedding[3] * -19 + embedding[4] * -20 + embedding[5] * -1 + embedding[6] * -10 + embedding[7] * 20 + embedding[8] * 9 + embedding[9] * 15;
    let score_6 = embedding[0] * -12 + embedding[1] * 14 + embedding[2] * -17 + embedding[3] * 19 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * -9 + embedding[8] * 8 + embedding[9] * -2;
    let score_7 = embedding[0] * 9 + embedding[1] * -10 + embedding[2] * 16 + embedding[3] * 1 + embedding[4] * 4 + embedding[5] * 3 + embedding[6] * -18 + embedding[7] * 62 + embedding[8] * 2;
    let score_8 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_9 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_10 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_11 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_12 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_13 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_14 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_15 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_16 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 18 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * 12 + embedding[1] * -2 + embedding[2] * -11 + embedding[3] * -11 + embedding[4] * 12 + embedding[5] * 11 + embedding[6] * -7 + embedding[7] * -22 + embedding[8] * 3 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 11;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 29;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 35;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 12;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_378(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * 9 + embedding[1] * -10 + embedding[2] * 24 + embedding[3] * -17 + embedding[4] * 11 + embedding[5] * 16 + embedding[6] * -11 + embedding[7] * 13 + embedding[8] * 3 + embedding[9] * -2;
    let score_2 = embedding[1] * -9 + embedding[2] * -16 + embedding[3] * 22 + embedding[4] * 4 + embedding[5] * 4 + embedding[6] * 10 + embedding[7] * -12 + embedding[8] * -4 + embedding[9] * -11;
    let score_3 = embedding[0] * -24 + embedding[1] * -2 + embedding[2] * -16 + embedding[3] * 17 + embedding[4] * -2 + embedding[5] * -22 + embedding[6] * 11 + embedding[7] * -13 + embedding[8] * 6 + embedding[9] * -11;
    let score_4 = embedding[0] * -7 + embedding[1] * 17 + embedding[2] * -5 + embedding[3] * -19 + embedding[4] * -15 + embedding[5] * -4 + embedding[6] * -12 + embedding[7] * 22 + embedding[8] * 9 + embedding[9] * 14;
    let score_5 = embedding[0] * -12 + embedding[1] * 12 + embedding[2] * -20 + embedding[3] * 17 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 9 + embedding[7] * -6 + embedding[8] * 8 + embedding[9] * -2;
    let score_6 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 12 + embedding[1] * -3 + embedding[2] * -14 + embedding[3] * -14 + embedding[4] * 16 + embedding[5] * 12 + embedding[6] * -11 + embedding[7] * -16 + embedding[8] * 4 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 11;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 29;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 35;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_379(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -17 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 6;
    let score_1 = embedding[0] * 4 + embedding[1] * 10 + embedding[2] * -6 + embedding[3] * -9 + embedding[4] * 17 + embedding[5] * 8 + embedding[6] * -4 + embedding[7] * -11 + embedding[8] * 8 + embedding[9] * -5;
    let score_2 = embedding[0] * 7 + embedding[1] * -17 + embedding[2] * 67 + embedding[3] * -5 + embedding[4] * -11 + embedding[5] * 15 + embedding[6] * 13 + embedding[7] * 29 + embedding[8] * -23 + embedding[9] * 6;
    let score_3 = embedding[0] * -10 + embedding[1] * -25 + embedding[2] * -10 + embedding[3] * 16 + embedding[4] * 9 + embedding[5] * -17 + embedding[6] * -2 + embedding[7] * 42 + embedding[8] * -6 + embedding[9] * -6;
    let score_4 = embedding[0] * -27 + embedding[1] * -2 + embedding[2] * -23 + embedding[3] * 15 + embedding[4] * -11 + embedding[5] * -30 + embedding[6] * 3 + embedding[7] * -10 + embedding[8] * 8 + embedding[9] * -3;
    let score_5 = embedding[0] * 5 + embedding[1] * -11 + embedding[2] * 25 + embedding[3] * -3 + embedding[4] * -6 + embedding[5] * 16 + embedding[6] * 1 + embedding[7] * 10 + embedding[8] * 2 + embedding[9] * -1;
    let score_6 = embedding[1] * -1 + embedding[2] * 77 + embedding[3] * -2 + embedding[4] * 14 + embedding[5] * 11 + embedding[6] * 20 + embedding[7] * 47 + embedding[8] * -20;
    let score_7 = embedding[0] * -25 + embedding[1] * 9 + embedding[2] * -5 + embedding[3] * 16 + embedding[4] * 3 + embedding[5] * -26 + embedding[6] * 14 + embedding[7] * -7 + embedding[8] * 7 + embedding[9] * -7;
    let score_8 = embedding[0] * -16 + embedding[1] * -13 + embedding[2] * 19 + embedding[3] * 16 + embedding[4] * 21 + embedding[5] * -8 + embedding[6] * 11 + embedding[7] * 43 + embedding[8] * -10 + embedding[9] * -14;
    let score_9 = embedding[0] * -1 + embedding[1] * 1 + embedding[2] * 44 + embedding[3] * -4 + embedding[4] * 12 + embedding[5] * 12 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * 1 + embedding[9] * -5;
    let score_10 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -17 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 6;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -17 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 6;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -17 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 6;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -17 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 6;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -17 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 6;
    let score_15 = embedding[0] * -6 + embedding[1] * 1 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -17 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 6;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -17 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -13 + embedding[9] * 6;
    let score_17 = embedding[0] * 12 + embedding[1] * 1 + embedding[2] * -28 + embedding[3] * -11 + embedding[4] * 6 + embedding[5] * 7 + embedding[6] * -15 + embedding[7] * -18 + embedding[8] * 9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 31;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 11;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 176;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 180;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 167;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 152;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_380(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_1 = embedding[0] * -7 + embedding[1] * -7 + embedding[2] * -5 + embedding[3] * -10 + embedding[4] * -27 + embedding[5] * 11 + embedding[6] * 11 + embedding[7] * -31 + embedding[8] * 7 + embedding[9] * -4;
    let score_2 = embedding[0] * 13 + embedding[1] * -10 + embedding[2] * 49 + embedding[3] * -8 + embedding[4] * 11 + embedding[5] * 4 + embedding[6] * -8 + embedding[7] * 35 + embedding[8] * -17 + embedding[9] * 4;
    let score_3 = embedding[0] * -15 + embedding[1] * -10 + embedding[2] * 56 + embedding[4] * -30 + embedding[5] * 17 + embedding[6] * 35 + embedding[7] * 5 + embedding[8] * -26;
    let score_4 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_5 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_6 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_7 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_8 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_9 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_10 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_11 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_12 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_13 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_14 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_15 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_16 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_17 = embedding[0] * 12 + embedding[1] * 1 + embedding[2] * -33 + embedding[3] * -13 + embedding[4] * 27 + embedding[5] * 3 + embedding[6] * -26 + embedding[7] * -10 + embedding[8] * 14 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 46;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 175;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_381(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_382(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_383(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -24 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_384(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_385(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_386(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 52 + embedding[3] * -9 + embedding[4] * 5 + embedding[5] * 11 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -20 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 5 + embedding[1] * -1 + embedding[2] * -35 + embedding[3] * -15 + embedding[4] * 22 + embedding[5] * 10 + embedding[6] * -17 + embedding[7] * -23 + embedding[8] * 18 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_387(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 51 + embedding[3] * -9 + embedding[4] * 5 + embedding[5] * 11 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -19 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 5 + embedding[1] * -2 + embedding[2] * -35 + embedding[3] * -15 + embedding[4] * 22 + embedding[5] * 10 + embedding[6] * -18 + embedding[7] * -24 + embedding[8] * 18 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 9;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_388(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_1 = embedding[0] * 3 + embedding[1] * -14 + embedding[2] * 20 + embedding[3] * -12 + embedding[4] * 11 + embedding[5] * 21 + embedding[6] * -5 + embedding[7] * 5 + embedding[9] * -7;
    let score_2 = embedding[0] * -8 + embedding[1] * -19 + embedding[2] * -4 + embedding[3] * 9 + embedding[4] * 17 + embedding[5] * -11 + embedding[6] * -1 + embedding[7] * 35 + embedding[8] * -4 + embedding[9] * -11;
    let score_3 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * 8 + embedding[1] * 9 + embedding[2] * -25 + embedding[3] * -16 + embedding[4] * 11 + embedding[5] * 8 + embedding[6] * -14 + embedding[7] * -28 + embedding[8] * 14 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_389(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * 2 + embedding[1] * -13 + embedding[2] * 21 + embedding[3] * -17 + embedding[4] * 10 + embedding[5] * 19 + embedding[6] * -6 + embedding[8] * -3 + embedding[9] * -5;
    let score_2 = embedding[0] * -10 + embedding[1] * -8 + embedding[2] * -8 + embedding[3] * 19 + embedding[4] * -3 + embedding[5] * 12 + embedding[6] * 9 + embedding[7] * -4 + embedding[8] * 5 + embedding[9] * -8;
    let score_3 = embedding[0] * 24 + embedding[1] * -20 + embedding[2] * 15 + embedding[3] * 32 + embedding[4] * 9 + embedding[5] * 3 + embedding[6] * 13 + embedding[7] * -2 + embedding[8] * -21 + embedding[9] * -12;
    let score_4 = embedding[0] * -2 + embedding[1] * 20 + embedding[2] * 5 + embedding[3] * 10 + embedding[4] * -10 + embedding[5] * -7 + embedding[6] * -11 + embedding[7] * 31 + embedding[8] * 32 + embedding[9] * 7;
    let score_5 = embedding[0] * -27 + embedding[2] * -15 + embedding[3] * 10 + embedding[5] * -20 + embedding[6] * 8 + embedding[7] * -14 + embedding[8] * 3 + embedding[9] * -10;
    let score_6 = embedding[0] * -11 + embedding[1] * 25 + embedding[2] * -4 + embedding[3] * -18 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * -17 + embedding[7] * 32 + embedding[8] * 17 + embedding[9] * 14;
    let score_7 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 8 + embedding[1] * 4 + embedding[2] * -19 + embedding[3] * -18 + embedding[4] * 15 + embedding[5] * 7 + embedding[6] * -12 + embedding[7] * -19 + embedding[8] * 1 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 15;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 11;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 29;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_390(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 39 + embedding[1] * -11 + embedding[2] * 19 + embedding[3] * -36 + embedding[4] * 27 + embedding[5] * 16 + embedding[6] * -7 + embedding[7] * -37 + embedding[8] * -30 + embedding[9] * 2;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_2 = embedding[0] * -29 + embedding[1] * -23 + embedding[2] * -11 + embedding[3] * 36 + embedding[4] * -8 + embedding[5] * 14 + embedding[6] * 16 + embedding[7] * 22 + embedding[8] * 9 + embedding[9] * -17;
    let score_3 = embedding[0] * -18 + embedding[1] * 12 + embedding[2] * -29 + embedding[3] * 15 + embedding[4] * 4 + embedding[5] * -27 + embedding[6] * -9 + embedding[7] * 51 + embedding[8] * 14 + embedding[9] * 2;
    let score_4 = embedding[0] * -17 + embedding[1] * 16 + embedding[2] * -3 + embedding[3] * 25 + embedding[4] * -17 + embedding[5] * -6 + embedding[6] * -6 + embedding[7] * 45 + embedding[8] * 42 + embedding[9] * 3;
    let score_5 = embedding[0] * -40 + embedding[1] * -4 + embedding[2] * -23 + embedding[3] * 32 + embedding[4] * -7 + embedding[5] * -23 + embedding[6] * 16 + embedding[7] * -8 + embedding[8] * 8 + embedding[9] * -17;
    let score_6 = embedding[0] * -23 + embedding[1] * 22 + embedding[2] * -8 + embedding[3] * -6 + embedding[4] * -20 + embedding[5] * -6 + embedding[6] * -13 + embedding[7] * 44 + embedding[8] * 24 + embedding[9] * 10;
    let score_7 = embedding[0] * 14 + embedding[1] * -10 + embedding[2] * 5 + embedding[3] * 11 + embedding[4] * 10 + embedding[5] * -3 + embedding[6] * -17 + embedding[7] * 67 + embedding[8] * -2 + embedding[9] * -5;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 17 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 7;
    let score_17 = embedding[0] * -11 + embedding[1] * 3 + embedding[2] * -24 + embedding[3] * 2 + embedding[4] * -1 + embedding[5] * 10 + embedding[6] * -5 + embedding[7] * -9 + embedding[8] * 14 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 8;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 15;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 11;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 29;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 12;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_391(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -13 + embedding[1] * -7 + embedding[2] * -19 + embedding[3] * -5 + embedding[4] * 16 + embedding[5] * 19 + embedding[6] * -8 + embedding[7] * -11 + embedding[8] * 7 + embedding[9] * -10;
    let score_1 = embedding[0] * 27 + embedding[1] * 1 + embedding[2] * 1 + embedding[3] * -36 + embedding[4] * -4 + embedding[5] * -24 + embedding[6] * 46 + embedding[7] * 4 + embedding[8] * 4 + embedding[9] * -15;
    let score_2 = embedding[0] * 26 + embedding[1] * 7 + embedding[2] * -6 + embedding[3] * -36 + embedding[4] * -14 + embedding[5] * -19 + embedding[6] * 34 + embedding[7] * 9 + embedding[8] * -9 + embedding[9] * -31;
    let score_3 = embedding[1] * -2 + embedding[2] * 5 + embedding[3] * 24 + embedding[4] * 18 + embedding[5] * 3 + embedding[6] * -4 + embedding[7] * -14 + embedding[8] * -11 + embedding[9] * 39;
    let score_4 = embedding[0] * 12 + embedding[1] * -2 + embedding[2] * -12 + embedding[3] * -4 + embedding[4] * 11 + embedding[5] * 3 + embedding[6] * -2 + embedding[7] * -17 + embedding[8] * -14 + embedding[9] * 5;
    let score_5 = embedding[0] * -30 + embedding[1] * -28 + embedding[2] * -10 + embedding[3] * 34 + embedding[4] * 8 + embedding[5] * 24 + embedding[6] * 12 + embedding[7] * 24 + embedding[8] * 4 + embedding[9] * -18;
    let score_6 = embedding[0] * 10 + embedding[1] * -1 + embedding[2] * 14 + embedding[3] * -17 + embedding[4] * -4 + embedding[5] * -25 + embedding[6] * 3 + embedding[7] * -9 + embedding[8] * 4 + embedding[9] * -20;
    let score_7 = embedding[0] * -5 + embedding[1] * 11 + embedding[2] * 20 + embedding[3] * -3 + embedding[4] * -46 + embedding[5] * 31 + embedding[6] * -10 + embedding[7] * 24 + embedding[8] * 11 + embedding[9] * 17;
    let score_8 = embedding[0] * -10 + embedding[1] * 11 + embedding[2] * 12 + embedding[3] * 43 + embedding[4] * -14 + embedding[5] * -1 + embedding[6] * -34 + embedding[7] * -10 + embedding[8] * -46 + embedding[9] * 7;
    let score_9 = embedding[1] * 12 + embedding[2] * 8 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * -29 + embedding[7] * -4 + embedding[8] * -53 + embedding[9] * -11;
    let score_10 = embedding[0] * -20 + embedding[1] * 17 + embedding[2] * 11 + embedding[3] * 15 + embedding[4] * -12 + embedding[5] * 6 + embedding[6] * -7 + embedding[7] * 41 + embedding[8] * 42 + embedding[9] * 5;
    let score_11 = embedding[0] * -1 + embedding[1] * -15 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * 15 + embedding[5] * -18 + embedding[6] * 19 + embedding[7] * -8 + embedding[8] * 18 + embedding[9] * 48;
    let score_12 = embedding[0] * -25 + embedding[1] * 32 + embedding[2] * 6 + embedding[3] * -9 + embedding[4] * -33 + embedding[5] * 9 + embedding[6] * -13 + embedding[7] * 49 + embedding[8] * 27 + embedding[9] * 17;
    let score_13 = embedding[0] * 6 + embedding[1] * -12 + embedding[2] * 10 + embedding[3] * 28 + embedding[4] * 13 + embedding[5] * -18 + embedding[6] * -1 + embedding[7] * 10 + embedding[8] * 8 + embedding[9] * 59;
    let score_14 = embedding[0] * -37 + embedding[1] * 22 + embedding[2] * 39 + embedding[3] * -5 + embedding[4] * -7 + embedding[5] * 9 + embedding[6] * 11 + embedding[7] * -1 + embedding[8] * 17 + embedding[9] * -8;
    let score_15 = embedding[0] * -14 + embedding[1] * 3 + embedding[2] * 25 + embedding[3] * 30 + embedding[4] * -30 + embedding[5] * -19 + embedding[6] * -39 + embedding[7] * -8 + embedding[8] * -33 + embedding[9] * -14;
    let score_16 = embedding[0] * -2 + embedding[1] * 9 + embedding[2] * -7 + embedding[3] * 31 + embedding[4] * -25 + embedding[5] * -1 + embedding[6] * -33 + embedding[8] * -24 + embedding[9] * -2;
    let score_17 = embedding[0] * -4 + embedding[1] * -21 + embedding[2] * 39 + embedding[3] * 45 + embedding[4] * -1 + embedding[5] * -47 + embedding[6] * -13 + embedding[7] * 8 + embedding[8] * 21 + embedding[9] * 56;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 5;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 20;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 15;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 38;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 29;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 23;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 52;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 79;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 100;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 123;
    }
    best_palette
}

fn predict_pixel_392(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -6 + embedding[3] * -35 + embedding[4] * -3 + embedding[5] * -7 + embedding[6] * 41 + embedding[7] * 11 + embedding[8] * 24 + embedding[9] * -20;
    let score_1 = embedding[0] * 9 + embedding[1] * 5 + embedding[2] * -11 + embedding[3] * -33 + embedding[4] * -13 + embedding[5] * -2 + embedding[6] * 22 + embedding[7] * 14 + embedding[8] * 6 + embedding[9] * -37;
    let score_2 = embedding[0] * -12 + embedding[1] * -5 + embedding[2] * -1 + embedding[3] * 33 + embedding[4] * 20 + embedding[5] * 19 + embedding[6] * -12 + embedding[7] * -11 + embedding[8] * 1 + embedding[9] * 40;
    let score_3 = embedding[0] * 10 + embedding[1] * -2 + embedding[2] * -11 + embedding[3] * 3 + embedding[4] * 11 + embedding[5] * 8 + embedding[6] * -5 + embedding[7] * -16 + embedding[8] * -11 + embedding[9] * 9;
    let score_4 = embedding[0] * 1 + embedding[1] * -2 + embedding[2] * 14 + embedding[3] * -12 + embedding[4] * -1 + embedding[5] * -14 + embedding[6] * -2 + embedding[7] * -5 + embedding[8] * 13 + embedding[9] * -23;
    let score_5 = embedding[1] * -1 + embedding[2] * -9 + embedding[3] * 3 + embedding[4] * 16 + embedding[5] * 13 + embedding[6] * -4 + embedding[7] * -13 + embedding[8] * -5 + embedding[9] * 2;
    let score_6 = embedding[0] * -10 + embedding[1] * 12 + embedding[2] * 11 + embedding[3] * 52 + embedding[4] * -14 + embedding[5] * 4 + embedding[6] * -40 + embedding[7] * -8 + embedding[8] * -42 + embedding[9] * 12;
    let score_7 = embedding[0] * -5 + embedding[1] * 17 + embedding[2] * 10 + embedding[3] * 34 + embedding[4] * -28 + embedding[5] * -3 + embedding[6] * -35 + embedding[7] * -1 + embedding[8] * -55 + embedding[9] * -12;
    let score_8 = embedding[0] * -4 + embedding[1] * -12 + embedding[2] * 19 + embedding[3] * 23 + embedding[4] * 16 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * -6 + embedding[8] * 19 + embedding[9] * 47;
    let score_9 = embedding[0] * 11 + embedding[1] * -3 + embedding[2] * -4 + embedding[3] * -26 + embedding[4] * -2 + embedding[5] * -7 + embedding[6] * 39 + embedding[7] * 9 + embedding[8] * 18 + embedding[9] * -12;
    let score_10 = embedding[0] * 10 + embedding[1] * 8 + embedding[2] * -6 + embedding[3] * -30 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 25 + embedding[7] * 16 + embedding[8] * 3 + embedding[9] * -38;
    let score_11 = embedding[0] * -10 + embedding[1] * -7 + embedding[2] * 2 + embedding[3] * 35 + embedding[4] * 18 + embedding[5] * 14 + embedding[6] * -6 + embedding[7] * -8 + embedding[9] * 45;
    let score_12 = embedding[0] * 3 + embedding[1] * -12 + embedding[2] * 8 + embedding[3] * 32 + embedding[4] * 14 + embedding[5] * -9 + embedding[6] * -4 + embedding[7] * 10 + embedding[8] * 9 + embedding[9] * 56;
    let score_13 = embedding[0] * -11 + embedding[1] * 8 + embedding[2] * 28 + embedding[3] * 37 + embedding[4] * -30 + embedding[5] * -15 + embedding[6] * -41 + embedding[7] * -6 + embedding[8] * -37 + embedding[9] * -10;
    let score_14 = embedding[0] * 2 + embedding[2] * 17 + embedding[3] * -6 + embedding[4] * -2 + embedding[5] * -20 + embedding[6] * -3 + embedding[7] * -5 + embedding[8] * 9 + embedding[9] * -18;
    let score_15 = embedding[0] * -5 + embedding[1] * 13 + embedding[2] * 1 + embedding[3] * 41 + embedding[4] * -23 + embedding[5] * 4 + embedding[6] * -34 + embedding[7] * -4 + embedding[8] * -38 + embedding[9] * 7;
    let score_16 = embedding[0] * -3 + embedding[1] * 17 + embedding[2] * 12 + embedding[3] * 34 + embedding[4] * -36 + embedding[5] * -4 + embedding[6] * -31 + embedding[7] * 5 + embedding[8] * -46 + embedding[9] * -16;
    let score_17 = embedding[0] * -4 + embedding[1] * -14 + embedding[2] * 31 + embedding[3] * 43 + embedding[4] * 6 + embedding[5] * -25 + embedding[6] * -13 + embedding[7] * 5 + embedding[8] * 15 + embedding[9] * 57;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 32;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 33;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 38;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 138;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 140;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 139;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 23;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 79;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 169;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 100;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 193;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 123;
    }
    best_palette
}

fn predict_pixel_393(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 8 + embedding[1] * 6 + embedding[2] * -7 + embedding[3] * -16 + embedding[4] * -12 + embedding[5] * -1 + embedding[6] * -1 + embedding[7] * 6 + embedding[8] * -8 + embedding[9] * -29;
    let score_1 = embedding[0] * 6 + embedding[1] * -8 + embedding[3] * -14 + embedding[4] * 8 + embedding[5] * -3 + embedding[6] * 19 + embedding[7] * -3 + embedding[8] * 19 + embedding[9] * 7;
    let score_2 = embedding[0] * -13 + embedding[1] * -4 + embedding[2] * -2 + embedding[3] * 29 + embedding[4] * 10 + embedding[5] * 14 + embedding[6] * -19 + embedding[7] * -12 + embedding[8] * -7 + embedding[9] * 28;
    let score_3 = embedding[0] * -1 + embedding[1] * -3 + embedding[2] * 20 + embedding[3] * -1 + embedding[4] * -3 + embedding[5] * -18 + embedding[6] * -18 + embedding[7] * -10 + embedding[8] * 8 + embedding[9] * -12;
    let score_4 = embedding[0] * -2 + embedding[1] * -2 + embedding[2] * -6 + embedding[3] * 2 + embedding[4] * 10 + embedding[5] * 11 + embedding[6] * -4 + embedding[7] * -10 + embedding[8] * -2 + embedding[9] * 3;
    let score_5 = embedding[0] * 2 + embedding[1] * 8 + embedding[2] * -20 + embedding[3] * -2 + embedding[4] * 3 + embedding[5] * 15 + embedding[6] * 10 + embedding[7] * 25 + embedding[8] * -6 + embedding[9] * 12;
    let score_6 = embedding[0] * 8 + embedding[1] * 16 + embedding[2] * 13 + embedding[3] * -2 + embedding[4] * 8 + embedding[5] * 7 + embedding[6] * 30 + embedding[7] * -1 + embedding[8] * -20 + embedding[9] * 9;
    let score_7 = embedding[0] * -16 + embedding[1] * -3 + embedding[2] * 2 + embedding[3] * 30 + embedding[4] * 5 + embedding[5] * 12 + embedding[6] * -19 + embedding[7] * -6 + embedding[8] * -7 + embedding[9] * 25;
    let score_8 = embedding[0] * 4 + embedding[1] * -9 + embedding[2] * 7 + embedding[3] * 22 + embedding[4] * 6 + embedding[5] * -7 + embedding[6] * -10 + embedding[7] * 9 + embedding[8] * 6 + embedding[9] * 40;
    let score_9 = embedding[0] * 1 + embedding[1] * 6 + embedding[2] * -3 + embedding[3] * -12 + embedding[4] * -12 + embedding[5] * -1 + embedding[7] * 6 + embedding[8] * -6 + embedding[9] * -24;
    let score_10 = embedding[0] * 1 + embedding[1] * -6 + embedding[2] * -2 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 1 + embedding[6] * 8 + embedding[7] * 2 + embedding[8] * 13 + embedding[9] * 3;
    let score_11 = embedding[0] * -7 + embedding[1] * -2 + embedding[2] * 22 + embedding[3] * 4 + embedding[4] * -7 + embedding[5] * -17 + embedding[6] * -18 + embedding[7] * -5 + embedding[8] * 5 + embedding[9] * -14;
    let score_12 = embedding[0] * 11 + embedding[1] * -10 + embedding[2] * 9 + embedding[3] * -7 + embedding[4] * -2 + embedding[5] * -14 + embedding[6] * 30 + embedding[7] * 6 + embedding[8] * 22 + embedding[9] * 13;
    let score_13 = embedding[1] * 8 + embedding[2] * -3 + embedding[3] * 24 + embedding[4] * -14 + embedding[5] * 3 + embedding[6] * -21 + embedding[8] * -12 + embedding[9] * 5;
    let score_14 = embedding[0] * 4 + embedding[1] * 10 + embedding[2] * -6 + embedding[3] * -9 + embedding[4] * -19 + embedding[5] * -5 + embedding[6] * 1 + embedding[7] * 12 + embedding[8] * -13 + embedding[9] * -28;
    let score_15 = embedding[0] * 8 + embedding[1] * -7 + embedding[2] * 2 + embedding[3] * -8 + embedding[4] * 3 + embedding[5] * -5 + embedding[6] * 22 + embedding[7] * -3 + embedding[8] * 13 + embedding[9] * 12;
    let score_16 = embedding[0] * 8 + embedding[1] * 8 + embedding[2] * -4 + embedding[3] * -8 + embedding[4] * -27 + embedding[5] * -7 + embedding[6] * 4 + embedding[7] * 9 + embedding[8] * -15 + embedding[9] * -29;
    let score_17 = embedding[0] * -14 + embedding[2] * 6 + embedding[3] * 46 + embedding[5] * 9 + embedding[6] * -14 + embedding[7] * -6 + embedding[8] * -10 + embedding[9] * 39;
    let mut best_score = score_0;
    let mut best_palette = 3;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 26;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 7;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 139;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 23;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 140;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 138;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 169;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 144;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 100;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 150;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 155;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 157;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 160;
    }
    best_palette
}

fn predict_pixel_394(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 12 + embedding[1] * -19 + embedding[2] * -3 + embedding[3] * -27 + embedding[4] * 21 + embedding[5] * -8 + embedding[6] * 39 + embedding[7] * -8 + embedding[8] * 39 + embedding[9] * 13;
    let score_1 = embedding[0] * 8 + embedding[1] * 15 + embedding[2] * -11 + embedding[3] * -24 + embedding[4] * -21 + embedding[5] * 2 + embedding[6] * -14 + embedding[7] * 4 + embedding[8] * -22 + embedding[9] * -55;
    let score_2 = embedding[0] * -15 + embedding[1] * -4 + embedding[2] * -5 + embedding[3] * 41 + embedding[4] * 16 + embedding[5] * 24 + embedding[6] * -37 + embedding[7] * -18 + embedding[8] * -13 + embedding[9] * 34;
    let score_3 = embedding[0] * -2 + embedding[1] * -8 + embedding[2] * 29 + embedding[3] * -2 + embedding[4] * -5 + embedding[5] * -26 + embedding[6] * -39 + embedding[7] * -17 + embedding[8] * 10 + embedding[9] * -25;
    let score_4 = embedding[0] * 8 + embedding[1] * 9 + embedding[2] * -32 + embedding[3] * -12 + embedding[4] * 12 + embedding[5] * 23 + embedding[6] * 16 + embedding[7] * 31 + embedding[8] * -2 + embedding[9] * 13;
    let score_5 = embedding[0] * 15 + embedding[1] * 17 + embedding[2] * 12 + embedding[3] * -13 + embedding[4] * 21 + embedding[5] * 12 + embedding[6] * 46 + embedding[7] * -3 + embedding[8] * -20 + embedding[9] * 11;
    let score_6 = embedding[0] * -5 + embedding[1] * -20 + embedding[2] * -12 + embedding[3] * -4 + embedding[4] * -9 + embedding[5] * 6 + embedding[6] * -11 + embedding[7] * -10 + embedding[8] * 8 + embedding[9] * 1;
    let score_7 = embedding[0] * 5 + embedding[1] * -17 + embedding[2] * -1 + embedding[3] * 14 + embedding[4] * 21 + embedding[5] * -4 + embedding[6] * -2 + embedding[7] * 4 + embedding[8] * 17 + embedding[9] * 41;
    let score_8 = embedding[0] * -1 + embedding[1] * 11 + embedding[2] * -9 + embedding[3] * 25 + embedding[4] * -17 + embedding[5] * 10 + embedding[6] * -39 + embedding[7] * -5 + embedding[8] * -21 + embedding[9] * -3;
    let score_9 = embedding[0] * 9 + embedding[1] * -7 + embedding[2] * 6 + embedding[3] * -5 + embedding[4] * -17 + embedding[5] * -11 + embedding[6] * 3 + embedding[7] * -19 + embedding[8] * 13 + embedding[9] * -13;
    let score_10 = embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_11 = embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_12 = embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_13 = embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_14 = embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_15 = embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_16 = embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_17 = embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 26;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 1;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 80;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 93;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_395(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 12 + embedding[1] * -19 + embedding[2] * -2 + embedding[3] * -27 + embedding[4] * 21 + embedding[5] * -8 + embedding[6] * 39 + embedding[7] * -8 + embedding[8] * 39 + embedding[9] * 13;
    let score_1 = embedding[0] * 8 + embedding[1] * 15 + embedding[2] * -10 + embedding[3] * -24 + embedding[4] * -21 + embedding[5] * 2 + embedding[6] * -14 + embedding[7] * 4 + embedding[8] * -23 + embedding[9] * -55;
    let score_2 = embedding[0] * -15 + embedding[1] * -4 + embedding[2] * -4 + embedding[3] * 41 + embedding[4] * 16 + embedding[5] * 24 + embedding[6] * -37 + embedding[7] * -18 + embedding[8] * -13 + embedding[9] * 34;
    let score_3 = embedding[0] * -2 + embedding[1] * -8 + embedding[2] * 29 + embedding[3] * -1 + embedding[4] * -5 + embedding[5] * -25 + embedding[6] * -39 + embedding[7] * -18 + embedding[8] * 10 + embedding[9] * -25;
    let score_4 = embedding[0] * 7 + embedding[1] * 9 + embedding[2] * -32 + embedding[3] * -11 + embedding[4] * 12 + embedding[5] * 23 + embedding[6] * 16 + embedding[7] * 31 + embedding[8] * -2 + embedding[9] * 13;
    let score_5 = embedding[0] * 15 + embedding[1] * 17 + embedding[2] * 12 + embedding[3] * -12 + embedding[4] * 21 + embedding[5] * 12 + embedding[6] * 45 + embedding[7] * -3 + embedding[8] * -20 + embedding[9] * 11;
    let score_6 = embedding[0] * -5 + embedding[1] * -20 + embedding[2] * -11 + embedding[3] * -4 + embedding[4] * -9 + embedding[5] * 6 + embedding[6] * -11 + embedding[7] * -10 + embedding[8] * 8 + embedding[9] * 1;
    let score_7 = embedding[0] * 5 + embedding[1] * -17 + embedding[3] * 14 + embedding[4] * 21 + embedding[5] * -3 + embedding[6] * -2 + embedding[7] * 4 + embedding[8] * 17 + embedding[9] * 41;
    let score_8 = embedding[0] * -1 + embedding[1] * 11 + embedding[2] * -9 + embedding[3] * 25 + embedding[4] * -17 + embedding[5] * 10 + embedding[6] * -39 + embedding[7] * -5 + embedding[8] * -22 + embedding[9] * -3;
    let score_9 = embedding[0] * 8 + embedding[1] * -7 + embedding[2] * 6 + embedding[3] * -5 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 3 + embedding[7] * -19 + embedding[8] * 12 + embedding[9] * -13;
    let score_10 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_11 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_12 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_13 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_14 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_15 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_16 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_17 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -14 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 26;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 1;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 80;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 93;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_396(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -25 + embedding[1] * -14 + embedding[2] * -27 + embedding[3] * -11 + embedding[4] * -1 + embedding[5] * 24 + embedding[6] * -15 + embedding[7] * 16 + embedding[8] * 30 + embedding[9] * -12;
    let score_1 = embedding[0] * 18 + embedding[1] * 13 + embedding[2] * 2 + embedding[3] * -15 + embedding[4] * -10 + embedding[5] * -5 + embedding[6] * -9 + embedding[7] * -9 + embedding[8] * -32 + embedding[9] * -35;
    let score_2 = embedding[0] * 19 + embedding[1] * -12 + embedding[2] * 11 + embedding[3] * -19 + embedding[4] * 16 + embedding[5] * -15 + embedding[6] * 31 + embedding[7] * -17 + embedding[8] * 20 + embedding[9] * 14;
    let score_3 = embedding[0] * 6 + embedding[1] * 4 + embedding[2] * 12 + embedding[3] * 29 + embedding[4] * 12 + embedding[5] * 8 + embedding[6] * -25 + embedding[7] * -25 + embedding[8] * -33 + embedding[9] * 26;
    let score_4 = embedding[0] * 9 + embedding[1] * -5 + embedding[2] * 36 + embedding[4] * 1 + embedding[5] * -29 + embedding[6] * -30 + embedding[7] * -25 + embedding[8] * -4 + embedding[9] * -15;
    let score_5 = embedding[0] * -8 + embedding[1] * 7 + embedding[2] * -40 + embedding[3] * -12 + embedding[4] * 2 + embedding[5] * 30 + embedding[6] * 8 + embedding[7] * 39 + embedding[8] * 8 + embedding[9] * 6;
    let score_6 = embedding[0] * 23 + embedding[1] * 18 + embedding[2] * 25 + embedding[3] * -7 + embedding[4] * 16 + embedding[5] * 1 + embedding[6] * 38 + embedding[7] * -16 + embedding[8] * -33 + embedding[9] * 13;
    let score_7 = embedding[0] * 12 + embedding[1] * -15 + embedding[2] * 6 + embedding[3] * 13 + embedding[4] * 15 + embedding[5] * -11 + embedding[6] * -3 + embedding[7] * 1 + embedding[8] * 9 + embedding[9] * 39;
    let score_8 = embedding[0] * -23 + embedding[1] * -10 + embedding[2] * -25 + embedding[3] * -10 + embedding[4] * -1 + embedding[5] * 22 + embedding[6] * -12 + embedding[7] * 18 + embedding[8] * 24 + embedding[9] * -11;
    let score_9 = embedding[0] * 2 + embedding[1] * 9 + embedding[2] * -3 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * 5 + embedding[6] * -36 + embedding[7] * -5 + embedding[8] * -18 + embedding[9] * -5;
    let score_10 = embedding[0] * 13 + embedding[1] * -8 + embedding[2] * 9 + embedding[3] * -1 + embedding[4] * -14 + embedding[5] * -13 + embedding[6] * 1 + embedding[7] * -20 + embedding[8] * 11 + embedding[9] * -3;
    let score_11 = embedding[0] * -4 + embedding[1] * 9 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_12 = embedding[0] * -4 + embedding[1] * 9 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -4 + embedding[1] * 9 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -4 + embedding[1] * 9 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -4 + embedding[1] * 9 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -4 + embedding[1] * 9 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * -4 + embedding[1] * 9 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 26;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 7;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 91;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 80;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 179;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_397(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 9 + embedding[1] * -14 + embedding[2] * -3 + embedding[3] * -19 + embedding[4] * 15 + embedding[5] * -5 + embedding[6] * 25 + embedding[7] * -7 + embedding[8] * 27 + embedding[9] * 8;
    let score_1 = embedding[0] * 7 + embedding[1] * 7 + embedding[2] * -11 + embedding[3] * -18 + embedding[4] * -11 + embedding[5] * 4 + embedding[6] * -9 + embedding[7] * 2 + embedding[8] * -11 + embedding[9] * -35;
    let score_2 = embedding[0] * -10 + embedding[1] * -4 + embedding[2] * -5 + embedding[3] * 24 + embedding[4] * 12 + embedding[5] * 17 + embedding[6] * -25 + embedding[7] * -14 + embedding[8] * -8 + embedding[9] * 22;
    let score_3 = embedding[0] * -1 + embedding[1] * -6 + embedding[2] * 21 + embedding[3] * -1 + embedding[4] * -1 + embedding[5] * -17 + embedding[6] * -30 + embedding[7] * -15 + embedding[8] * 10 + embedding[9] * -16;
    let score_4 = embedding[0] * 6 + embedding[1] * 7 + embedding[2] * -24 + embedding[3] * -8 + embedding[4] * 7 + embedding[5] * 18 + embedding[6] * 9 + embedding[7] * 24 + embedding[8] * -4 + embedding[9] * 9;
    let score_5 = embedding[0] * 12 + embedding[1] * 14 + embedding[2] * 10 + embedding[3] * -9 + embedding[4] * 14 + embedding[5] * 9 + embedding[6] * 33 + embedding[7] * -2 + embedding[8] * -16 + embedding[9] * 8;
    let score_6 = embedding[0] * -4 + embedding[1] * -14 + embedding[2] * -8 + embedding[3] * -2 + embedding[4] * -8 + embedding[5] * 6 + embedding[6] * -11 + embedding[7] * -8 + embedding[8] * 4 + embedding[9] * -1;
    let score_7 = embedding[0] * 5 + embedding[1] * -12 + embedding[3] * 13 + embedding[4] * 14 + embedding[5] * -5 + embedding[6] * -5 + embedding[7] * 5 + embedding[8] * 11 + embedding[9] * 34;
    let score_8 = embedding[0] * -5 + embedding[2] * -11 + embedding[3] * -3 + embedding[4] * 6 + embedding[5] * 13 + embedding[6] * -11 + embedding[7] * -1 + embedding[8] * -1 + embedding[9] * -7;
    let score_9 = embedding[0] * 2 + embedding[1] * 8 + embedding[2] * -7 + embedding[3] * 18 + embedding[4] * -13 + embedding[5] * 6 + embedding[6] * -28 + embedding[7] * -2 + embedding[8] * -13 + embedding[9] * -1;
    let score_10 = embedding[0] * 8 + embedding[1] * -6 + embedding[2] * 5 + embedding[3] * -1 + embedding[4] * -16 + embedding[5] * -8 + embedding[6] * 1 + embedding[7] * -15 + embedding[8] * 9 + embedding[9] * -7;
    let score_11 = embedding[1] * 3 + embedding[2] * 14 + embedding[3] * 18 + embedding[4] * -14 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 7;
    let score_12 = embedding[1] * 3 + embedding[2] * 14 + embedding[3] * 18 + embedding[4] * -14 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 7;
    let score_13 = embedding[1] * 3 + embedding[2] * 14 + embedding[3] * 18 + embedding[4] * -14 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 7;
    let score_14 = embedding[1] * 3 + embedding[2] * 14 + embedding[3] * 18 + embedding[4] * -14 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 7;
    let score_15 = embedding[1] * 3 + embedding[2] * 14 + embedding[3] * 18 + embedding[4] * -14 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 7;
    let score_16 = embedding[1] * 3 + embedding[2] * 14 + embedding[3] * 18 + embedding[4] * -14 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 7;
    let score_17 = embedding[1] * 3 + embedding[2] * 14 + embedding[3] * 18 + embedding[4] * -14 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 26;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 1;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 91;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 80;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 93;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_398(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 11 + embedding[1] * -18 + embedding[2] * -2 + embedding[3] * -27 + embedding[4] * 20 + embedding[5] * -8 + embedding[6] * 39 + embedding[7] * -8 + embedding[8] * 39 + embedding[9] * 13;
    let score_1 = embedding[0] * 8 + embedding[1] * 15 + embedding[2] * -10 + embedding[3] * -24 + embedding[4] * -21 + embedding[5] * 2 + embedding[6] * -14 + embedding[7] * 4 + embedding[8] * -22 + embedding[9] * -55;
    let score_2 = embedding[0] * -16 + embedding[1] * -4 + embedding[2] * -4 + embedding[3] * 41 + embedding[4] * 15 + embedding[5] * 24 + embedding[6] * -38 + embedding[7] * -18 + embedding[8] * -13 + embedding[9] * 34;
    let score_3 = embedding[0] * -2 + embedding[1] * -8 + embedding[2] * 29 + embedding[3] * -2 + embedding[4] * -5 + embedding[5] * -25 + embedding[6] * -39 + embedding[7] * -17 + embedding[8] * 10 + embedding[9] * -25;
    let score_4 = embedding[0] * 7 + embedding[1] * 9 + embedding[2] * -32 + embedding[3] * -11 + embedding[4] * 11 + embedding[5] * 23 + embedding[6] * 16 + embedding[7] * 31 + embedding[8] * -2 + embedding[9] * 13;
    let score_5 = embedding[0] * 15 + embedding[1] * 17 + embedding[2] * 12 + embedding[3] * -12 + embedding[4] * 20 + embedding[5] * 12 + embedding[6] * 45 + embedding[7] * -3 + embedding[8] * -20 + embedding[9] * 11;
    let score_6 = embedding[0] * -5 + embedding[1] * -20 + embedding[2] * -11 + embedding[3] * -4 + embedding[4] * -10 + embedding[5] * 6 + embedding[6] * -11 + embedding[7] * -10 + embedding[8] * 8 + embedding[9] * 1;
    let score_7 = embedding[0] * 5 + embedding[1] * -16 + embedding[3] * 14 + embedding[4] * 21 + embedding[5] * -3 + embedding[6] * -3 + embedding[7] * 4 + embedding[8] * 17 + embedding[9] * 42;
    let score_8 = embedding[0] * -1 + embedding[1] * 11 + embedding[2] * -9 + embedding[3] * 25 + embedding[4] * -17 + embedding[5] * 10 + embedding[6] * -39 + embedding[7] * -5 + embedding[8] * -21 + embedding[9] * -3;
    let score_9 = embedding[0] * 8 + embedding[1] * -7 + embedding[2] * 6 + embedding[3] * -5 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 3 + embedding[7] * -19 + embedding[8] * 13 + embedding[9] * -13;
    let score_10 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 7;
    let score_11 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 7;
    let score_12 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_13 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_14 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 7;
    let score_15 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 7;
    let score_16 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 7;
    let score_17 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 26;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 1;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 80;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 93;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_399(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 14 + embedding[1] * -7 + embedding[2] * -6 + embedding[3] * -42 + embedding[4] * -5 + embedding[5] * -10 + embedding[6] * 44 + embedding[7] * 15 + embedding[8] * 29 + embedding[9] * -25;
    let score_1 = embedding[0] * 10 + embedding[1] * 5 + embedding[2] * -13 + embedding[3] * -40 + embedding[4] * -15 + embedding[5] * -3 + embedding[6] * 21 + embedding[7] * 16 + embedding[8] * 6 + embedding[9] * -46;
    let score_2 = embedding[0] * -15 + embedding[1] * -5 + embedding[2] * -2 + embedding[3] * 36 + embedding[4] * 22 + embedding[5] * 20 + embedding[6] * -16 + embedding[7] * -11 + embedding[8] * 2 + embedding[9] * 43;
    let score_3 = embedding[0] * 13 + embedding[2] * -11 + embedding[3] * -1 + embedding[4] * 13 + embedding[5] * 11 + embedding[6] * -2 + embedding[7] * -18 + embedding[8] * -15 + embedding[9] * 8;
    let score_4 = embedding[0] * 2 + embedding[1] * -1 + embedding[2] * 14 + embedding[3] * -18 + embedding[4] * -2 + embedding[5] * -16 + embedding[6] * -3 + embedding[7] * -4 + embedding[8] * 15 + embedding[9] * -30;
    let score_5 = embedding[0] * 1 + embedding[2] * -12 + embedding[3] * -1 + embedding[4] * 17 + embedding[5] * 17 + embedding[6] * -4 + embedding[7] * -15 + embedding[8] * -7 + embedding[9] * -1;
    let score_6 = embedding[0] * -15 + embedding[1] * 15 + embedding[2] * 14 + embedding[3] * 55 + embedding[4] * -17 + embedding[5] * 8 + embedding[6] * -42 + embedding[7] * -9 + embedding[8] * -51 + embedding[9] * 10;
    let score_7 = embedding[0] * -3 + embedding[1] * 17 + embedding[2] * 7 + embedding[3] * 31 + embedding[4] * -26 + embedding[5] * 1 + embedding[6] * -34 + embedding[7] * -3 + embedding[8] * -56 + embedding[9] * -13;
    let score_8 = embedding[0] * -6 + embedding[1] * -16 + embedding[2] * 24 + embedding[3] * 24 + embedding[4] * 17 + embedding[5] * -9 + embedding[6] * 18 + embedding[7] * -7 + embedding[8] * 24 + embedding[9] * 53;
    let score_9 = embedding[0] * 3 + embedding[1] * -13 + embedding[2] * 9 + embedding[3] * 31 + embedding[4] * 17 + embedding[5] * -8 + embedding[6] * -5 + embedding[7] * 9 + embedding[8] * 12 + embedding[9] * 60;
    let score_10 = embedding[0] * -11 + embedding[1] * 8 + embedding[2] * 27 + embedding[3] * 35 + embedding[4] * -32 + embedding[5] * -14 + embedding[6] * -41 + embedding[7] * -8 + embedding[8] * -38 + embedding[9] * -15;
    let score_11 = embedding[0] * 3 + embedding[1] * 15 + embedding[2] * -4 + embedding[3] * 39 + embedding[4] * -24 + embedding[5] * 4 + embedding[6] * -35 + embedding[7] * -1 + embedding[8] * -35 + embedding[9] * 2;
    let score_12 = embedding[0] * 11 + embedding[1] * 8 + embedding[2] * -10 + embedding[3] * -24 + embedding[4] * -31 + embedding[5] * -8 + embedding[6] * 18 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * -43;
    let score_13 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -5 + embedding[3] * -26 + embedding[4] * -2 + embedding[5] * -6 + embedding[6] * 33 + embedding[7] * 8 + embedding[8] * 16 + embedding[9] * -13;
    let score_14 = embedding[0] * -17 + embedding[1] * -7 + embedding[2] * 8 + embedding[3] * 53 + embedding[4] * 14 + embedding[5] * 14 + embedding[6] * -3 + embedding[7] * -7 + embedding[8] * 1 + embedding[9] * 58;
    let score_15 = embedding[0] * -9 + embedding[1] * -16 + embedding[2] * 40 + embedding[3] * 50 + embedding[4] * 7 + embedding[5] * -34 + embedding[6] * -16 + embedding[7] * 2 + embedding[8] * 18 + embedding[9] * 59;
    let score_16 = embedding[0] * 13 + embedding[1] * 1 + embedding[2] * -8 + embedding[3] * -3 + embedding[4] * -13 + embedding[5] * -1 + embedding[6] * 11 + embedding[7] * -13 + embedding[9] * -7;
    let score_17 = embedding[0] * -4 + embedding[1] * -15 + embedding[2] * 22 + embedding[3] * 35 + embedding[4] * 12 + embedding[5] * -7 + embedding[6] * 8 + embedding[7] * -14 + embedding[8] * 12 + embedding[9] * 43;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 32;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 33;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 38;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 23;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 79;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 100;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 157;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 155;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 160;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 123;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 93;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 210;
    }
    best_palette
}

fn predict_pixel_400(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 12 + embedding[1] * -9 + embedding[2] * -17 + embedding[3] * -12 + embedding[4] * 20 + embedding[5] * 6 + embedding[6] * -11 + embedding[7] * -23 + embedding[8] * -4 + embedding[9] * -7;
    let score_1 = embedding[0] * -4 + embedding[1] * -26 + embedding[2] * -7 + embedding[3] * 22 + embedding[4] * 13 + embedding[5] * 9 + embedding[6] * 10 + embedding[7] * 12 + embedding[8] * -5 + embedding[9] * -14;
    let score_2 = embedding[0] * 6 + embedding[1] * 5 + embedding[2] * 11 + embedding[3] * -6 + embedding[4] * -28 + embedding[5] * 14 + embedding[6] * -9 + embedding[7] * 11 + embedding[8] * 5 + embedding[9] * 12;
    let score_3 = embedding[0] * -1 + embedding[1] * 16 + embedding[2] * 8 + embedding[3] * 11 + embedding[4] * -7 + embedding[5] * -6 + embedding[6] * -7 + embedding[7] * 24 + embedding[8] * 30 + embedding[9] * 7;
    let score_4 = embedding[0] * -12 + embedding[1] * 20 + embedding[2] * 2 + embedding[3] * -11 + embedding[4] * -23 + embedding[5] * -4 + embedding[6] * -9 + embedding[7] * 30 + embedding[8] * 18 + embedding[9] * 15;
    let score_5 = embedding[0] * -10 + embedding[1] * 13 + embedding[2] * -10 + embedding[3] * 8 + embedding[4] * -18 + embedding[5] * -1 + embedding[6] * 3 + embedding[7] * 1 + embedding[8] * 12 + embedding[9] * 4;
    let score_6 = embedding[0] * -18 + embedding[1] * 18 + embedding[2] * 33 + embedding[3] * -8 + embedding[4] * -8 + embedding[5] * -2 + embedding[6] * 9 + embedding[7] * -4 + embedding[8] * 10 + embedding[9] * -5;
    let score_7 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 10;
    let score_8 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 10;
    let score_9 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 10;
    let score_10 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 10;
    let score_11 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 10;
    let score_12 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 10;
    let score_13 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 10;
    let score_14 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 10;
    let score_15 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 10;
    let score_16 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 10;
    let score_17 = embedding[0] * -11 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 10;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 6;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 20;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 29;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 35;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 52;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_401(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_1 = embedding[0] * 2 + embedding[1] * -7 + embedding[2] * 34 + embedding[3] * -24 + embedding[4] * -5 + embedding[5] * 20 + embedding[6] * -16 + embedding[7] * 26 + embedding[8] * 4 + embedding[9] * 8;
    let score_2 = embedding[0] * -13 + embedding[1] * 10 + embedding[2] * -27 + embedding[3] * 8 + embedding[4] * 10 + embedding[5] * -33 + embedding[6] * -6 + embedding[7] * 24 + embedding[8] * 5 + embedding[9] * 2;
    let score_3 = embedding[0] * 3 + embedding[1] * -8 + embedding[2] * -18 + embedding[3] * 29 + embedding[4] * 5 + embedding[5] * 1 + embedding[6] * 17 + embedding[7] * -23 + embedding[8] * -5 + embedding[9] * -13;
    let score_4 = embedding[0] * -21 + embedding[1] * 2 + embedding[2] * -23 + embedding[3] * 25 + embedding[4] * 1 + embedding[5] * -27 + embedding[6] * 16 + embedding[7] * -20 + embedding[8] * 6 + embedding[9] * -12;
    let score_5 = embedding[0] * -8 + embedding[1] * 11 + embedding[2] * 13 + embedding[3] * -22 + embedding[4] * -21 + embedding[5] * 2 + embedding[6] * -12 + embedding[7] * 27 + embedding[8] * 8 + embedding[9] * 16;
    let score_6 = embedding[0] * -10 + embedding[1] * 13 + embedding[2] * -24 + embedding[3] * 26 + embedding[4] * -13 + embedding[5] * -14 + embedding[6] * 15 + embedding[7] * -17 + embedding[8] * 7 + embedding[9] * -5;
    let score_7 = embedding[0] * 11 + embedding[1] * -11 + embedding[2] * 27 + embedding[3] * -4 + embedding[4] * 4 + embedding[5] * 9 + embedding[6] * -20 + embedding[7] * 62 + embedding[8] * 2 + embedding[9] * 4;
    let score_8 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_9 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_10 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_11 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_12 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_13 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_14 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_15 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_16 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * 17 + embedding[1] * -3 + embedding[2] * -16 + embedding[3] * -7 + embedding[4] * 18 + embedding[5] * 10 + embedding[6] * -5 + embedding[7] * -29 + embedding[8] * 3 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 8;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 11;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 29;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 35;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 12;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_402(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_1 = embedding[0] * 6 + embedding[1] * -8 + embedding[2] * 22 + embedding[3] * -15 + embedding[4] * 9 + embedding[5] * 14 + embedding[6] * -10 + embedding[7] * 10 + embedding[8] * 6 + embedding[9] * -2;
    let score_2 = embedding[0] * -5 + embedding[1] * -20 + embedding[2] * -14 + embedding[3] * 17 + embedding[4] * 13 + embedding[5] * 3 + embedding[6] * 5 + embedding[7] * 6 + embedding[8] * -7 + embedding[9] * -15;
    let score_3 = embedding[0] * -10 + embedding[1] * 19 + embedding[2] * -7 + embedding[3] * -18 + embedding[4] * -16 + embedding[5] * -5 + embedding[6] * -12 + embedding[7] * 18 + embedding[8] * 11 + embedding[9] * 13;
    let score_4 = embedding[0] * -6 + embedding[1] * 17 + embedding[2] * -19 + embedding[3] * 10 + embedding[4] * -19 + embedding[5] * -3 + embedding[6] * 4 + embedding[7] * -7 + embedding[8] * 9 + embedding[9] * 2;
    let score_5 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_6 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_7 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_8 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_9 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_10 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_11 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_12 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_13 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_14 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_15 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_16 = embedding[0] * -12 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 7;
    let score_17 = embedding[0] * 8 + embedding[1] * -1 + embedding[2] * -17 + embedding[3] * -13 + embedding[4] * 15 + embedding[5] * 10 + embedding[6] * -10 + embedding[7] * -21 + embedding[8] * 6 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 29;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 35;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_403(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_1 = embedding[0] * 3 + embedding[1] * -14 + embedding[2] * 66 + embedding[3] * -8 + embedding[4] * -1 + embedding[5] * 12 + embedding[6] * 11 + embedding[7] * 31 + embedding[8] * -22 + embedding[9] * 1;
    let score_2 = embedding[0] * 1 + embedding[1] * -9 + embedding[2] * 18 + embedding[3] * -8 + embedding[4] * 5 + embedding[5] * 15 + embedding[6] * -2 + embedding[7] * 2 + embedding[8] * 7 + embedding[9] * -6;
    let score_3 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 9;
    let score_17 = embedding[0] * 7 + embedding[1] * 1 + embedding[2] * -37 + embedding[3] * -15 + embedding[4] * 20 + embedding[5] * 7 + embedding[6] * -17 + embedding[7] * -26 + embedding[8] * 15 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 9;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_404(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 51 + embedding[3] * -10 + embedding[4] * 6 + embedding[5] * 11 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -19 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 6 + embedding[1] * -2 + embedding[2] * -36 + embedding[3] * -16 + embedding[4] * 23 + embedding[5] * 10 + embedding[6] * -17 + embedding[7] * -23 + embedding[8] * 18 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_405(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -26 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_406(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -22 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 7 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_407(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_408(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_409(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_410(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 52 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 11 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -20 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 6 + embedding[1] * -1 + embedding[2] * -35 + embedding[3] * -15 + embedding[4] * 23 + embedding[5] * 10 + embedding[6] * -17 + embedding[7] * -23 + embedding[8] * 18 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_411(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 51 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 11 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -20 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 6 + embedding[1] * -2 + embedding[2] * -36 + embedding[3] * -15 + embedding[4] * 23 + embedding[5] * 10 + embedding[6] * -18 + embedding[7] * -24 + embedding[8] * 18 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 9;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_412(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_1 = embedding[0] * 2 + embedding[1] * -13 + embedding[2] * 24 + embedding[3] * -16 + embedding[4] * 10 + embedding[5] * 20 + embedding[6] * -5 + embedding[7] * 12 + embedding[8] * -1 + embedding[9] * -5;
    let score_2 = embedding[0] * -6 + embedding[1] * -14 + embedding[2] * -15 + embedding[3] * 12 + embedding[4] * 11 + embedding[5] * 3 + embedding[6] * 1 + embedding[7] * 4 + embedding[8] * -4 + embedding[9] * -12;
    let score_3 = embedding[0] * 20 + embedding[1] * -19 + embedding[2] * 13 + embedding[3] * 34 + embedding[4] * 7 + embedding[5] * 4 + embedding[6] * 11 + embedding[7] * 1 + embedding[8] * -11 + embedding[9] * -14;
    let score_4 = embedding[0] * -28 + embedding[1] * 5 + embedding[2] * -21 + embedding[3] * 9 + embedding[4] * -3 + embedding[5] * -25 + embedding[6] * 6 + embedding[7] * -16 + embedding[8] * 12 + embedding[9] * -9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -10 + embedding[9] * 5;
    let score_17 = embedding[0] * 8 + embedding[1] * 11 + embedding[2] * -22 + embedding[3] * -21 + embedding[4] * 10 + embedding[5] * 7 + embedding[6] * -16 + embedding[7] * -20 + embedding[8] * 14 + embedding[9] * -1;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 11;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_413(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_1 = embedding[0] * -6 + embedding[1] * -16 + embedding[2] * 23 + embedding[3] * -19 + embedding[4] * 8 + embedding[5] * 19 + embedding[6] * -6 + embedding[7] * 4 + embedding[8] * -2 + embedding[9] * -5;
    let score_2 = embedding[0] * -11 + embedding[1] * -21 + embedding[2] * -5 + embedding[3] * 18 + embedding[4] * 9 + embedding[5] * 15 + embedding[6] * 9 + embedding[7] * 11 + embedding[8] * -1 + embedding[9] * -13;
    let score_3 = embedding[0] * 21 + embedding[1] * -21 + embedding[2] * 13 + embedding[3] * 39 + embedding[4] * 9 + embedding[5] * 4 + embedding[6] * 15 + embedding[7] * -3 + embedding[8] * -19 + embedding[9] * -15;
    let score_4 = embedding[0] * -1 + embedding[1] * 24 + embedding[2] * 1 + embedding[3] * 11 + embedding[4] * -11 + embedding[5] * -10 + embedding[6] * -11 + embedding[7] * 29 + embedding[8] * 34 + embedding[9] * 8;
    let score_5 = embedding[0] * -13 + embedding[1] * 27 + embedding[2] * -6 + embedding[3] * -20 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * -18 + embedding[7] * 30 + embedding[8] * 17 + embedding[9] * 16;
    let score_6 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[0] * 9 + embedding[1] * 10 + embedding[2] * -23 + embedding[3] * -17 + embedding[4] * 14 + embedding[5] * 5 + embedding[6] * -12 + embedding[7] * -24 + embedding[8] * 4 + embedding[9] * -3;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 15;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 29;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_414(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 33 + embedding[1] * -9 + embedding[2] * 13 + embedding[3] * -24 + embedding[4] * 23 + embedding[5] * 16 + embedding[6] * -7 + embedding[7] * -36 + embedding[8] * -20 + embedding[9] * -3;
    let score_1 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_2 = embedding[0] * -28 + embedding[1] * -24 + embedding[2] * -17 + embedding[3] * 31 + embedding[4] * 2 + embedding[5] * 11 + embedding[6] * 9 + embedding[7] * 30 + embedding[8] * 8 + embedding[9] * -18;
    let score_3 = embedding[0] * -17 + embedding[1] * 16 + embedding[2] * -4 + embedding[3] * 22 + embedding[4] * -14 + embedding[5] * -10 + embedding[6] * -9 + embedding[7] * 48 + embedding[8] * 42 + embedding[9] * 2;
    let score_4 = embedding[0] * -20 + embedding[1] * 24 + embedding[2] * -11 + embedding[3] * -7 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * -16 + embedding[7] * 43 + embedding[8] * 23 + embedding[9] * 9;
    let score_5 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_6 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_7 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_8 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_9 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_10 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_11 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_12 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_13 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_14 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_15 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_16 = embedding[0] * -11 + embedding[2] * 19 + embedding[3] * 19 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 6;
    let score_17 = embedding[0] * -13 + embedding[2] * -28 + embedding[3] * -5 + embedding[4] * 8 + embedding[5] * 5 + embedding[6] * -10 + embedding[8] * 12 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 29;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_415(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -14 + embedding[1] * -7 + embedding[2] * -20 + embedding[3] * -5 + embedding[4] * 17 + embedding[5] * 20 + embedding[6] * -7 + embedding[7] * -9 + embedding[8] * 7 + embedding[9] * -11;
    let score_1 = embedding[0] * 23 + embedding[1] * 5 + embedding[2] * -8 + embedding[3] * -35 + embedding[4] * -15 + embedding[5] * -18 + embedding[6] * 28 + embedding[7] * 10 + embedding[8] * -8 + embedding[9] * -33;
    let score_2 = embedding[0] * 24 + embedding[1] * -2 + embedding[2] * -1 + embedding[3] * -35 + embedding[4] * -3 + embedding[5] * -23 + embedding[6] * 42 + embedding[7] * 4 + embedding[8] * 7 + embedding[9] * -15;
    let score_3 = embedding[0] * -1 + embedding[1] * -4 + embedding[2] * 6 + embedding[3] * 28 + embedding[4] * 18 + embedding[5] * 1 + embedding[6] * -5 + embedding[7] * -12 + embedding[8] * -9 + embedding[9] * 42;
    let score_4 = embedding[0] * 16 + embedding[2] * -8 + embedding[3] * -5 + embedding[4] * 12 + embedding[5] * 1 + embedding[6] * 3 + embedding[7] * -17 + embedding[8] * -20 + embedding[9] * 7;
    let score_5 = embedding[0] * 10 + embedding[1] * -2 + embedding[2] * 15 + embedding[3] * -17 + embedding[4] * -5 + embedding[5] * -27 + embedding[6] * 3 + embedding[7] * -7 + embedding[8] * 4 + embedding[9] * -23;
    let score_6 = embedding[0] * -27 + embedding[1] * -18 + embedding[2] * -10 + embedding[3] * 31 + embedding[5] * 27 + embedding[6] * 13 + embedding[7] * 7 + embedding[8] * 9 + embedding[9] * -15;
    let score_7 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * -2 + embedding[4] * -47 + embedding[5] * 33 + embedding[6] * -9 + embedding[7] * 25 + embedding[8] * 12 + embedding[9] * 15;
    let score_8 = embedding[0] * -8 + embedding[1] * 11 + embedding[2] * 13 + embedding[3] * 41 + embedding[4] * -13 + embedding[5] * -2 + embedding[6] * -31 + embedding[7] * -9 + embedding[8] * -48 + embedding[9] * 8;
    let score_9 = embedding[0] * 3 + embedding[1] * 12 + embedding[2] * 8 + embedding[3] * 23 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * -27 + embedding[7] * -4 + embedding[8] * -55 + embedding[9] * -11;
    let score_10 = embedding[0] * -20 + embedding[1] * 17 + embedding[2] * 11 + embedding[3] * 14 + embedding[4] * -11 + embedding[5] * 7 + embedding[6] * -7 + embedding[7] * 40 + embedding[8] * 42 + embedding[9] * 4;
    let score_11 = embedding[1] * -15 + embedding[2] * 24 + embedding[3] * 20 + embedding[4] * 17 + embedding[5] * -19 + embedding[6] * 20 + embedding[7] * -7 + embedding[8] * 17 + embedding[9] * 50;
    let score_12 = embedding[0] * -26 + embedding[1] * 31 + embedding[2] * 5 + embedding[3] * -10 + embedding[4] * -32 + embedding[5] * 10 + embedding[6] * -13 + embedding[7] * 50 + embedding[8] * 27 + embedding[9] * 16;
    let score_13 = embedding[0] * 7 + embedding[1] * -13 + embedding[2] * 11 + embedding[3] * 29 + embedding[4] * 14 + embedding[5] * -19 + embedding[6] * -1 + embedding[7] * 10 + embedding[8] * 7 + embedding[9] * 60;
    let score_14 = embedding[0] * -38 + embedding[1] * 21 + embedding[2] * 37 + embedding[3] * -5 + embedding[4] * -7 + embedding[5] * 10 + embedding[6] * 11 + embedding[7] * 1 + embedding[8] * 18 + embedding[9] * -9;
    let score_15 = embedding[0] * -13 + embedding[1] * 3 + embedding[2] * 25 + embedding[3] * 29 + embedding[4] * -30 + embedding[5] * -19 + embedding[6] * -37 + embedding[7] * -7 + embedding[8] * -34 + embedding[9] * -16;
    let score_16 = embedding[0] * -2 + embedding[1] * 10 + embedding[2] * -7 + embedding[3] * 30 + embedding[4] * -24 + embedding[5] * -1 + embedding[6] * -32 + embedding[7] * 2 + embedding[8] * -24 + embedding[9] * -3;
    let score_17 = embedding[0] * -4 + embedding[1] * -21 + embedding[2] * 40 + embedding[3] * 46 + embedding[5] * -47 + embedding[6] * -13 + embedding[7] * 9 + embedding[8] * 21 + embedding[9] * 57;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 6;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 20;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 15;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 38;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 29;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 23;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 52;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 79;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 100;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 123;
    }
    best_palette
}

fn predict_pixel_416(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 14 + embedding[1] * -8 + embedding[2] * -11 + embedding[3] * -48 + embedding[4] * -9 + embedding[5] * -11 + embedding[6] * 43 + embedding[7] * 16 + embedding[8] * 34 + embedding[9] * -29;
    let score_1 = embedding[0] * 13 + embedding[1] * 3 + embedding[2] * -18 + embedding[3] * -47 + embedding[4] * -19 + embedding[5] * -5 + embedding[6] * 24 + embedding[7] * 18 + embedding[8] * 11 + embedding[9] * -49;
    let score_2 = embedding[0] * -12 + embedding[1] * -5 + embedding[2] * -1 + embedding[3] * 33 + embedding[4] * 25 + embedding[5] * 21 + embedding[6] * -17 + embedding[7] * -12 + embedding[8] * 3 + embedding[9] * 47;
    let score_3 = embedding[0] * 15 + embedding[1] * 3 + embedding[2] * -11 + embedding[3] * -2 + embedding[4] * 15 + embedding[5] * 11 + embedding[6] * -3 + embedding[7] * -19 + embedding[8] * -16 + embedding[9] * 10;
    let score_4 = embedding[0] * 4 + embedding[1] * -3 + embedding[2] * 15 + embedding[3] * -23 + embedding[4] * -4 + embedding[5] * -21 + embedding[6] * -2 + embedding[7] * -2 + embedding[8] * 21 + embedding[9] * -32;
    let score_5 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * -10 + embedding[3] * -2 + embedding[4] * 22 + embedding[5] * 17 + embedding[6] * -4 + embedding[7] * -16 + embedding[8] * -10 + embedding[9] * 2;
    let score_6 = embedding[0] * -12 + embedding[1] * 18 + embedding[2] * 14 + embedding[3] * 53 + embedding[4] * -15 + embedding[5] * 9 + embedding[6] * -43 + embedding[7] * -10 + embedding[8] * -53 + embedding[9] * 11;
    let score_7 = embedding[0] * -4 + embedding[1] * 20 + embedding[2] * 8 + embedding[3] * 33 + embedding[4] * -28 + embedding[5] * 1 + embedding[6] * -40 + embedding[7] * -2 + embedding[8] * -60 + embedding[9] * -14;
    let score_8 = embedding[0] * -13 + embedding[1] * -13 + embedding[2] * -15 + embedding[3] * 29 + embedding[4] * 3 + embedding[5] * 17 + embedding[6] * 34 + embedding[7] * -3 + embedding[8] * -6 + embedding[9] * -18;
    let score_9 = embedding[0] * -5 + embedding[1] * -16 + embedding[2] * 24 + embedding[3] * 25 + embedding[4] * 21 + embedding[5] * -7 + embedding[6] * 15 + embedding[7] * -9 + embedding[8] * 25 + embedding[9] * 57;
    let score_10 = embedding[0] * 4 + embedding[1] * -13 + embedding[2] * 9 + embedding[3] * 30 + embedding[4] * 20 + embedding[5] * -7 + embedding[6] * -8 + embedding[7] * 8 + embedding[8] * 13 + embedding[9] * 64;
    let score_11 = embedding[0] * -10 + embedding[1] * 10 + embedding[2] * 29 + embedding[3] * 34 + embedding[4] * -33 + embedding[5] * -14 + embedding[6] * -44 + embedding[7] * -8 + embedding[8] * -40 + embedding[9] * -15;
    let score_12 = embedding[0] * 4 + embedding[1] * 17 + embedding[2] * -5 + embedding[3] * 37 + embedding[4] * -26 + embedding[5] * 4 + embedding[6] * -37 + embedding[7] * -1 + embedding[8] * -35 + embedding[9] * 4;
    let score_13 = embedding[0] * -6 + embedding[1] * -17 + embedding[2] * 41 + embedding[3] * 47 + embedding[4] * 9 + embedding[5] * -35 + embedding[6] * -17 + embedding[7] * 2 + embedding[8] * 20 + embedding[9] * 62;
    let score_14 = embedding[0] * 14 + embedding[1] * 2 + embedding[2] * -10 + embedding[3] * -8 + embedding[4] * -14 + embedding[5] * -3 + embedding[6] * 12 + embedding[7] * -12 + embedding[8] * 5 + embedding[9] * -8;
    let score_15 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 13 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let score_16 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 13 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 13 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 32;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 33;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 6;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 38;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 23;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 79;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 100;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 123;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 93;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_417(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 12 + embedding[1] * -9 + embedding[2] * -9 + embedding[3] * -28 + embedding[4] * 4 + embedding[5] * -3 + embedding[6] * 26 + embedding[7] * 2 + embedding[8] * 22 + embedding[9] * -9;
    let score_1 = embedding[0] * 10 + embedding[1] * 3 + embedding[2] * -15 + embedding[3] * -27 + embedding[4] * -9 + embedding[5] * 2 + embedding[6] * 10 + embedding[7] * 6 + embedding[8] * 2 + embedding[9] * -31;
    let score_2 = embedding[0] * -8 + embedding[1] * -4 + embedding[2] * -3 + embedding[3] * 20 + embedding[4] * 18 + embedding[5] * 17 + embedding[6] * -11 + embedding[7] * -10 + embedding[8] * 2 + embedding[9] * 27;
    let score_3 = embedding[0] * 3 + embedding[1] * -5 + embedding[2] * 11 + embedding[3] * -13 + embedding[5] * -12 + embedding[6] * -7 + embedding[7] * -9 + embedding[8] * 14 + embedding[9] * -18;
    let score_4 = embedding[0] * 11 + embedding[1] * 1 + embedding[2] * -8 + embedding[3] * -8 + embedding[4] * 13 + embedding[5] * 9 + embedding[6] * 4 + embedding[7] * -14 + embedding[8] * -8 + embedding[9] * 2;
    let score_5 = embedding[0] * 2 + embedding[1] * 1 + embedding[2] * -8 + embedding[3] * -7 + embedding[4] * 19 + embedding[5] * 13 + embedding[6] * 2 + embedding[7] * -12 + embedding[8] * -4 + embedding[9] * -3;
    let score_6 = embedding[0] * 5 + embedding[1] * 6 + embedding[2] * -26 + embedding[3] * -10 + embedding[4] * 6 + embedding[5] * 17 + embedding[6] * 13 + embedding[7] * 24 + embedding[8] * -2 + embedding[9] * 7;
    let score_7 = embedding[0] * -16 + embedding[1] * 17 + embedding[2] * 18 + embedding[3] * 51 + embedding[4] * -20 + embedding[5] * 4 + embedding[6] * -40 + embedding[7] * -5 + embedding[8] * -46 + embedding[9] * 3;
    let score_8 = embedding[0] * -8 + embedding[1] * 17 + embedding[2] * 13 + embedding[3] * 29 + embedding[4] * -34 + embedding[5] * -4 + embedding[6] * -36 + embedding[7] * 3 + embedding[8] * -55 + embedding[9] * -20;
    let score_9 = embedding[0] * -2 + embedding[1] * -16 + embedding[2] * 23 + embedding[3] * 13 + embedding[4] * 14 + embedding[5] * -12 + embedding[6] * 22 + embedding[7] * -5 + embedding[8] * 26 + embedding[9] * 41;
    let score_10 = embedding[0] * 7 + embedding[1] * -13 + embedding[2] * 9 + embedding[3] * 21 + embedding[4] * 13 + embedding[5] * -13 + embedding[7] * 10 + embedding[8] * 14 + embedding[9] * 50;
    let score_11 = embedding[0] * -14 + embedding[1] * 9 + embedding[2] * 34 + embedding[3] * 33 + embedding[4] * -36 + embedding[5] * -19 + embedding[6] * -42 + embedding[7] * -5 + embedding[8] * -37 + embedding[9] * -17;
    let score_12 = embedding[0] * 1 + embedding[1] * 16 + embedding[2] * -2 + embedding[3] * 32 + embedding[4] * -28 + embedding[5] * 1 + embedding[6] * -32 + embedding[7] * 3 + embedding[8] * -29 + embedding[9] * -3;
    let score_13 = embedding[0] * -6 + embedding[1] * -14 + embedding[2] * 40 + embedding[3] * 36 + embedding[4] * 2 + embedding[5] * -41 + embedding[6] * -10 + embedding[7] * 4 + embedding[8] * 20 + embedding[9] * 50;
    let score_14 = embedding[0] * 11 + embedding[1] * -2 + embedding[2] * -2 + embedding[3] * -5 + embedding[4] * -15 + embedding[5] * -5 + embedding[6] * 11 + embedding[7] * -12 + embedding[8] * 7 + embedding[9] * -7;
    let score_15 = embedding[0] * 3 + embedding[2] * 13 + embedding[3] * 18 + embedding[4] * -12 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -17 + embedding[9] * 4;
    let score_16 = embedding[0] * 3 + embedding[2] * 13 + embedding[3] * 18 + embedding[4] * -12 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -17 + embedding[9] * 4;
    let score_17 = embedding[0] * 3 + embedding[2] * 13 + embedding[3] * 18 + embedding[4] * -12 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -17 + embedding[9] * 4;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 106;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 32;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 33;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 38;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 23;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 79;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 80;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 123;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 93;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_418(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[1] * 2 + embedding[2] * -17 + embedding[3] * -15 + embedding[4] * -8 + embedding[5] * 8 + embedding[6] * -2 + embedding[7] * 5 + embedding[8] * 1 + embedding[9] * -22;
    let score_1 = embedding[0] * 2 + embedding[1] * -12 + embedding[2] * -12 + embedding[3] * -15 + embedding[4] * 5 + embedding[5] * 2 + embedding[6] * 17 + embedding[7] * 1 + embedding[8] * 23 + embedding[9] * 1;
    let score_2 = embedding[0] * -11 + embedding[1] * -5 + embedding[2] * -10 + embedding[3] * 10 + embedding[4] * 10 + embedding[5] * 15 + embedding[6] * -12 + embedding[7] * -5 + embedding[8] * 4 + embedding[9] * 13;
    let score_3 = embedding[0] * -1 + embedding[2] * -9 + embedding[3] * -7 + embedding[4] * 13 + embedding[5] * 10 + embedding[6] * 1 + embedding[7] * -8 + embedding[9] * -4;
    let score_4 = embedding[0] * -5 + embedding[1] * -7 + embedding[2] * 4 + embedding[3] * -3 + embedding[5] * -7 + embedding[6] * -12 + embedding[7] * -6 + embedding[8] * 15 + embedding[9] * -10;
    let score_5 = embedding[0] * 18 + embedding[1] * 11 + embedding[2] * 9 + embedding[3] * -7 + embedding[4] * 11 + embedding[5] * -4 + embedding[6] * 18 + embedding[7] * -11 + embedding[8] * -21 + embedding[9] * 9;
    let score_6 = embedding[0] * 14 + embedding[1] * 9 + embedding[2] * 5 + embedding[3] * -8 + embedding[4] * 12 + embedding[5] * -1 + embedding[6] * 14 + embedding[7] * -10 + embedding[8] * -15 + embedding[9] * 3;
    let score_7 = embedding[0] * 26 + embedding[1] * 14 + embedding[2] * 23 + embedding[3] * -4 + embedding[4] * 8 + embedding[5] * -16 + embedding[6] * 24 + embedding[7] * -11 + embedding[8] * -29 + embedding[9] * 10;
    let score_8 = embedding[1] * 5 + embedding[2] * -25 + embedding[3] * -7 + embedding[4] * 4 + embedding[5] * 17 + embedding[6] * 12 + embedding[7] * 24 + embedding[9] * 6;
    let score_9 = embedding[0] * -10 + embedding[1] * 13 + embedding[2] * 5 + embedding[3] * 27 + embedding[4] * -35 + embedding[5] * -2 + embedding[6] * -37 + embedding[7] * 6 + embedding[8] * -41 + embedding[9] * -20;
    let score_10 = embedding[0] * -17 + embedding[1] * 14 + embedding[2] * 16 + embedding[3] * 43 + embedding[4] * -23 + embedding[5] * 3 + embedding[6] * -37 + embedding[7] * -2 + embedding[8] * -37;
    let score_11 = embedding[0] * -6 + embedding[1] * -15 + embedding[2] * 24 + embedding[3] * 7 + embedding[4] * 8 + embedding[5] * -16 + embedding[6] * 21 + embedding[7] * -3 + embedding[8] * 27 + embedding[9] * 30;
    let score_12 = embedding[1] * 12 + embedding[4] * -27 + embedding[5] * -8 + embedding[6] * -13 + embedding[7] * 11 + embedding[8] * -20 + embedding[9] * -30;
    let score_13 = embedding[0] * 2 + embedding[1] * -14 + embedding[2] * 7 + embedding[3] * -6 + embedding[4] * 5 + embedding[5] * -15 + embedding[6] * 24 + embedding[7] * 1 + embedding[8] * 26 + embedding[9] * 16;
    let score_14 = embedding[0] * -14 + embedding[1] * 4 + embedding[2] * 5 + embedding[3] * 30 + embedding[4] * -2 + embedding[5] * 8 + embedding[6] * -20 + embedding[7] * -1 + embedding[8] * -14 + embedding[9] * 17;
    let score_15 = embedding[0] * -2 + embedding[1] * -17 + embedding[2] * 4 + embedding[3] * 15 + embedding[4] * 9 + embedding[5] * -9 + embedding[6] * 2 + embedding[7] * 12 + embedding[8] * 16 + embedding[9] * 32;
    let score_16 = embedding[0] * -16 + embedding[1] * 6 + embedding[2] * 27 + embedding[3] * 32 + embedding[4] * -36 + embedding[5] * -16 + embedding[6] * -41 + embedding[8] * -32 + embedding[9] * -15;
    let score_17 = embedding[0] * -7 + embedding[1] * -16 + embedding[2] * 39 + embedding[3] * 14 + embedding[4] * -1 + embedding[5] * -37 + embedding[6] * -8 + embedding[7] * -3 + embedding[8] * 26 + embedding[9] * 17;
    let mut best_score = score_0;
    let mut best_palette = 3;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 107;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 39;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 26;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 32;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 38;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 110;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 114;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 119;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 23;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 79;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 151;
    }
    best_palette
}

fn predict_pixel_419(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 5 + embedding[1] * -16 + embedding[2] * -13 + embedding[3] * -20 + embedding[4] * 6 + embedding[5] * -1 + embedding[6] * 26 + embedding[7] * 1 + embedding[8] * 31 + embedding[9] * 4;
    let score_1 = embedding[0] * 1 + embedding[1] * 2 + embedding[2] * -20 + embedding[3] * -19 + embedding[4] * -11 + embedding[5] * 8 + embedding[6] * -1 + embedding[7] * 6 + embedding[8] * -1 + embedding[9] * -27;
    let score_2 = embedding[0] * -14 + embedding[1] * -5 + embedding[2] * -11 + embedding[3] * 13 + embedding[4] * 12 + embedding[5] * 19 + embedding[6] * -14 + embedding[7] * -7 + embedding[8] * 3 + embedding[9] * 16;
    let score_3 = embedding[0] * 21 + embedding[1] * 12 + embedding[2] * 8 + embedding[3] * -11 + embedding[4] * 16 + embedding[5] * -3 + embedding[6] * 20 + embedding[7] * -14 + embedding[8] * -23 + embedding[9] * 7;
    let score_4 = embedding[0] * -5 + embedding[1] * -8 + embedding[2] * 5 + embedding[3] * -4 + embedding[5] * -9 + embedding[6] * -13 + embedding[7] * -8 + embedding[8] * 17 + embedding[9] * -10;
    let score_5 = embedding[0] * -2 + embedding[1] * 1 + embedding[2] * -10 + embedding[3] * -7 + embedding[4] * 17 + embedding[5] * 14 + embedding[6] * 4 + embedding[7] * -9 + embedding[8] * -2 + embedding[9] * -4;
    let score_6 = embedding[0] * 29 + embedding[1] * 16 + embedding[2] * 23 + embedding[3] * -7 + embedding[4] * 12 + embedding[5] * -16 + embedding[6] * 29 + embedding[7] * -15 + embedding[8] * -31 + embedding[9] * 11;
    let score_7 = embedding[0] * 1 + embedding[1] * 5 + embedding[2] * -29 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 20 + embedding[6] * 13 + embedding[7] * 25 + embedding[8] * 1 + embedding[9] * 7;
    let score_8 = embedding[0] * -23 + embedding[1] * 18 + embedding[2] * 17 + embedding[3] * 55 + embedding[4] * -26 + embedding[5] * 4 + embedding[6] * -45 + embedding[7] * -2 + embedding[8] * -50;
    let score_9 = embedding[0] * -11 + embedding[1] * 20 + embedding[2] * 12 + embedding[3] * 33 + embedding[4] * -42 + embedding[5] * -6 + embedding[6] * -41 + embedding[7] * 8 + embedding[8] * -58 + embedding[9] * -26;
    let score_10 = embedding[0] * -4 + embedding[1] * -20 + embedding[2] * 26 + embedding[3] * 8 + embedding[4] * 10 + embedding[5] * -20 + embedding[6] * 31 + embedding[7] * -4 + embedding[8] * 36 + embedding[9] * 39;
    let score_11 = embedding[0] * 1 + embedding[1] * 13 + embedding[2] * -1 + embedding[3] * -2 + embedding[4] * -32 + embedding[5] * -8 + embedding[6] * -14 + embedding[7] * 12 + embedding[8] * -25 + embedding[9] * -36;
    let score_12 = embedding[0] * 5 + embedding[1] * -19 + embedding[2] * 8 + embedding[3] * -8 + embedding[4] * 7 + embedding[5] * -19 + embedding[6] * 34 + embedding[7] * 1 + embedding[8] * 35 + embedding[9] * 23;
    let score_13 = embedding[0] * -17 + embedding[1] * 5 + embedding[2] * 4 + embedding[3] * 34 + embedding[5] * 11 + embedding[6] * -23 + embedding[7] * -3 + embedding[8] * -17 + embedding[9] * 19;
    let score_14 = embedding[1] * -20 + embedding[2] * 5 + embedding[3] * 16 + embedding[4] * 12 + embedding[5] * -12 + embedding[6] * 7 + embedding[7] * 11 + embedding[8] * 24 + embedding[9] * 41;
    let score_15 = embedding[0] * -18 + embedding[1] * 9 + embedding[2] * 33 + embedding[3] * 38 + embedding[4] * -42 + embedding[5] * -19 + embedding[6] * -47 + embedding[7] * -1 + embedding[8] * -42 + embedding[9] * -20;
    let score_16 = embedding[0] * -6 + embedding[1] * -20 + embedding[2] * 43 + embedding[3] * 15 + embedding[4] * 2 + embedding[5] * -43 + embedding[6] * -7 + embedding[7] * -4 + embedding[8] * 31 + embedding[9] * 22;
    let score_17 = embedding[0] * -6 + embedding[1] * 18 + embedding[2] * -4 + embedding[3] * 37 + embedding[4] * -36 + embedding[5] * 1 + embedding[6] * -39 + embedding[7] * 8 + embedding[8] * -30 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 39;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 26;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 38;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 110;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 114;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 119;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 23;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 79;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 151;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 80;
    }
    best_palette
}

fn predict_pixel_420(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 5 + embedding[1] * -16 + embedding[2] * -13 + embedding[3] * -20 + embedding[4] * 6 + embedding[5] * -1 + embedding[6] * 26 + embedding[7] * 1 + embedding[8] * 31 + embedding[9] * 4;
    let score_1 = embedding[0] * 1 + embedding[1] * 3 + embedding[2] * -20 + embedding[3] * -19 + embedding[4] * -11 + embedding[5] * 8 + embedding[6] * -1 + embedding[7] * 6 + embedding[8] * -1 + embedding[9] * -27;
    let score_2 = embedding[0] * -14 + embedding[1] * -5 + embedding[2] * -11 + embedding[3] * 14 + embedding[4] * 12 + embedding[5] * 19 + embedding[6] * -14 + embedding[7] * -7 + embedding[8] * 2 + embedding[9] * 16;
    let score_3 = embedding[0] * 20 + embedding[1] * 12 + embedding[2] * 8 + embedding[3] * -10 + embedding[4] * 16 + embedding[5] * -3 + embedding[6] * 20 + embedding[7] * -14 + embedding[8] * -23 + embedding[9] * 7;
    let score_4 = embedding[0] * -5 + embedding[1] * -8 + embedding[2] * 5 + embedding[3] * -4 + embedding[5] * -9 + embedding[6] * -13 + embedding[7] * -8 + embedding[8] * 17 + embedding[9] * -10;
    let score_5 = embedding[0] * -3 + embedding[1] * 1 + embedding[2] * -10 + embedding[3] * -7 + embedding[4] * 17 + embedding[5] * 14 + embedding[6] * 4 + embedding[7] * -9 + embedding[8] * -3 + embedding[9] * -4;
    let score_6 = embedding[0] * 29 + embedding[1] * 16 + embedding[2] * 23 + embedding[3] * -7 + embedding[4] * 11 + embedding[5] * -16 + embedding[6] * 29 + embedding[7] * -15 + embedding[8] * -31 + embedding[9] * 11;
    let score_7 = embedding[1] * 5 + embedding[2] * -29 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 20 + embedding[6] * 13 + embedding[7] * 25 + embedding[9] * 7;
    let score_8 = embedding[0] * -23 + embedding[1] * 19 + embedding[2] * 17 + embedding[3] * 55 + embedding[4] * -26 + embedding[5] * 4 + embedding[6] * -45 + embedding[7] * -2 + embedding[8] * -50;
    let score_9 = embedding[0] * -12 + embedding[1] * 20 + embedding[2] * 12 + embedding[3] * 33 + embedding[4] * -42 + embedding[5] * -6 + embedding[6] * -42 + embedding[7] * 8 + embedding[8] * -58 + embedding[9] * -26;
    let score_10 = embedding[0] * -4 + embedding[1] * -20 + embedding[2] * 26 + embedding[3] * 8 + embedding[4] * 10 + embedding[5] * -20 + embedding[6] * 31 + embedding[7] * -4 + embedding[8] * 36 + embedding[9] * 39;
    let score_11 = embedding[0] * 1 + embedding[1] * 14 + embedding[2] * -1 + embedding[3] * -2 + embedding[4] * -32 + embedding[5] * -8 + embedding[6] * -14 + embedding[7] * 12 + embedding[8] * -25 + embedding[9] * -36;
    let score_12 = embedding[0] * 5 + embedding[1] * -18 + embedding[2] * 8 + embedding[3] * -8 + embedding[4] * 7 + embedding[5] * -19 + embedding[6] * 34 + embedding[7] * 1 + embedding[8] * 34 + embedding[9] * 23;
    let score_13 = embedding[0] * -17 + embedding[1] * 5 + embedding[2] * 4 + embedding[3] * 34 + embedding[5] * 11 + embedding[6] * -23 + embedding[7] * -3 + embedding[8] * -17 + embedding[9] * 19;
    let score_14 = embedding[0] * -1 + embedding[1] * -20 + embedding[2] * 5 + embedding[3] * 17 + embedding[4] * 11 + embedding[5] * -12 + embedding[6] * 7 + embedding[7] * 11 + embedding[8] * 24 + embedding[9] * 41;
    let score_15 = embedding[0] * -18 + embedding[1] * 10 + embedding[2] * 33 + embedding[3] * 38 + embedding[4] * -43 + embedding[5] * -19 + embedding[6] * -47 + embedding[7] * -1 + embedding[8] * -42 + embedding[9] * -20;
    let score_16 = embedding[0] * -6 + embedding[1] * -20 + embedding[2] * 43 + embedding[3] * 15 + embedding[4] * 2 + embedding[5] * -43 + embedding[6] * -7 + embedding[7] * -5 + embedding[8] * 31 + embedding[9] * 22;
    let score_17 = embedding[0] * -7 + embedding[1] * 19 + embedding[2] * -4 + embedding[3] * 37 + embedding[4] * -37 + embedding[5] * 1 + embedding[6] * -39 + embedding[7] * 8 + embedding[8] * -30 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 39;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 26;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 38;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 110;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 114;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 119;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 23;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 79;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 151;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 80;
    }
    best_palette
}

fn predict_pixel_421(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 5 + embedding[1] * -16 + embedding[2] * -13 + embedding[3] * -20 + embedding[4] * 6 + embedding[5] * -1 + embedding[6] * 26 + embedding[7] * 1 + embedding[8] * 31 + embedding[9] * 4;
    let score_1 = embedding[0] * 1 + embedding[1] * 2 + embedding[2] * -20 + embedding[3] * -19 + embedding[4] * -11 + embedding[5] * 8 + embedding[6] * -1 + embedding[7] * 7 + embedding[8] * -1 + embedding[9] * -27;
    let score_2 = embedding[0] * -14 + embedding[1] * -5 + embedding[2] * -11 + embedding[3] * 13 + embedding[4] * 12 + embedding[5] * 19 + embedding[6] * -14 + embedding[7] * -7 + embedding[8] * 2 + embedding[9] * 16;
    let score_3 = embedding[0] * 21 + embedding[1] * 12 + embedding[2] * 8 + embedding[3] * -11 + embedding[4] * 16 + embedding[5] * -3 + embedding[6] * 20 + embedding[7] * -14 + embedding[8] * -23 + embedding[9] * 7;
    let score_4 = embedding[0] * -5 + embedding[1] * -8 + embedding[2] * 5 + embedding[3] * -4 + embedding[5] * -9 + embedding[6] * -13 + embedding[7] * -8 + embedding[8] * 17 + embedding[9] * -10;
    let score_5 = embedding[0] * -2 + embedding[1] * 1 + embedding[2] * -10 + embedding[3] * -8 + embedding[4] * 17 + embedding[5] * 14 + embedding[6] * 4 + embedding[7] * -9 + embedding[8] * -2 + embedding[9] * -4;
    let score_6 = embedding[0] * 29 + embedding[1] * 16 + embedding[2] * 23 + embedding[3] * -8 + embedding[4] * 12 + embedding[5] * -16 + embedding[6] * 29 + embedding[7] * -15 + embedding[8] * -31 + embedding[9] * 11;
    let score_7 = embedding[0] * 1 + embedding[1] * 5 + embedding[2] * -29 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 20 + embedding[6] * 13 + embedding[7] * 25 + embedding[8] * 1 + embedding[9] * 7;
    let score_8 = embedding[0] * -23 + embedding[1] * 18 + embedding[2] * 17 + embedding[3] * 55 + embedding[4] * -26 + embedding[5] * 4 + embedding[6] * -46 + embedding[7] * -2 + embedding[8] * -50;
    let score_9 = embedding[0] * -11 + embedding[1] * 20 + embedding[2] * 12 + embedding[3] * 33 + embedding[4] * -42 + embedding[5] * -6 + embedding[6] * -42 + embedding[7] * 8 + embedding[8] * -58 + embedding[9] * -26;
    let score_10 = embedding[0] * -4 + embedding[1] * -20 + embedding[2] * 26 + embedding[3] * 8 + embedding[4] * 10 + embedding[5] * -20 + embedding[6] * 30 + embedding[7] * -4 + embedding[8] * 36 + embedding[9] * 39;
    let score_11 = embedding[0] * 1 + embedding[1] * 13 + embedding[2] * -1 + embedding[3] * -2 + embedding[4] * -32 + embedding[5] * -8 + embedding[6] * -14 + embedding[7] * 12 + embedding[8] * -25 + embedding[9] * -36;
    let score_12 = embedding[0] * 5 + embedding[1] * -19 + embedding[2] * 8 + embedding[3] * -8 + embedding[4] * 7 + embedding[5] * -19 + embedding[6] * 33 + embedding[7] * 2 + embedding[8] * 34 + embedding[9] * 23;
    let score_13 = embedding[0] * -17 + embedding[1] * 5 + embedding[2] * 4 + embedding[3] * 34 + embedding[5] * 12 + embedding[6] * -23 + embedding[7] * -3 + embedding[8] * -17 + embedding[9] * 19;
    let score_14 = embedding[1] * -20 + embedding[2] * 5 + embedding[3] * 16 + embedding[4] * 12 + embedding[5] * -12 + embedding[6] * 7 + embedding[7] * 11 + embedding[8] * 24 + embedding[9] * 41;
    let score_15 = embedding[0] * -18 + embedding[1] * 9 + embedding[2] * 33 + embedding[3] * 37 + embedding[4] * -42 + embedding[5] * -19 + embedding[6] * -48 + embedding[7] * -1 + embedding[8] * -42 + embedding[9] * -20;
    let score_16 = embedding[0] * -6 + embedding[1] * -20 + embedding[2] * 43 + embedding[3] * 15 + embedding[4] * 2 + embedding[5] * -43 + embedding[6] * -7 + embedding[7] * -4 + embedding[8] * 31 + embedding[9] * 22;
    let score_17 = embedding[0] * -6 + embedding[1] * 18 + embedding[2] * -4 + embedding[3] * 36 + embedding[4] * -37 + embedding[5] * 1 + embedding[6] * -40 + embedding[7] * 8 + embedding[8] * -30 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 39;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 26;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 38;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 110;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 114;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 119;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 23;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 79;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 151;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 80;
    }
    best_palette
}

fn predict_pixel_422(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 2 + embedding[1] * 1 + embedding[2] * -17 + embedding[3] * -14 + embedding[4] * -13 + embedding[5] * 5 + embedding[6] * -4 + embedding[7] * 6 + embedding[8] * -1 + embedding[9] * -22;
    let score_1 = embedding[0] * 4 + embedding[1] * -14 + embedding[2] * -12 + embedding[3] * -15 + embedding[4] * 1 + embedding[5] * -1 + embedding[6] * 18 + embedding[7] * 1 + embedding[8] * 23 + embedding[9] * 5;
    let score_2 = embedding[0] * -11 + embedding[1] * -6 + embedding[2] * -9 + embedding[3] * 11 + embedding[4] * 7 + embedding[5] * 14 + embedding[6] * -12 + embedding[7] * -6 + embedding[8] * 2 + embedding[9] * 14;
    let score_3 = embedding[0] * -8 + embedding[1] * 7 + embedding[2] * 4 + embedding[3] * -8 + embedding[4] * 26 + embedding[5] * 14 + embedding[6] * 9 + embedding[7] * -6 + embedding[8] * 1 + embedding[9] * -9;
    let score_4 = embedding[0] * -4 + embedding[1] * -8 + embedding[2] * 5 + embedding[3] * -1 + embedding[4] * -2 + embedding[5] * -9 + embedding[6] * -15 + embedding[7] * -6 + embedding[8] * 12 + embedding[9] * -10;
    let score_5 = embedding[0] * 16 + embedding[1] * 9 + embedding[2] * 7 + embedding[3] * -8 + embedding[4] * 11 + embedding[5] * -2 + embedding[6] * 17 + embedding[7] * -10 + embedding[8] * -17 + embedding[9] * 8;
    let score_6 = embedding[0] * 11 + embedding[1] * 7 + embedding[2] * 4 + embedding[3] * -8 + embedding[4] * 13 + embedding[6] * 13 + embedding[7] * -10 + embedding[8] * -12 + embedding[9] * 2;
    let score_7 = embedding[0] * 24 + embedding[1] * 12 + embedding[2] * 21 + embedding[3] * -4 + embedding[4] * 7 + embedding[5] * -15 + embedding[6] * 21 + embedding[7] * -10 + embedding[8] * -26 + embedding[9] * 11;
    let score_8 = embedding[1] * 5 + embedding[2] * -23 + embedding[3] * -6 + embedding[4] * 4 + embedding[5] * 16 + embedding[6] * 12 + embedding[7] * 23 + embedding[9] * 8;
    let score_9 = embedding[0] * -15 + embedding[1] * 10 + embedding[2] * 7 + embedding[3] * 41 + embedding[4] * -26 + embedding[5] * 1 + embedding[6] * -36 + embedding[7] * -1 + embedding[8] * -34 + embedding[9] * 4;
    let score_10 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 2 + embedding[3] * 24 + embedding[4] * -39 + embedding[5] * -3 + embedding[6] * -37 + embedding[7] * 8 + embedding[8] * -39 + embedding[9] * -16;
    let score_11 = embedding[0] * -3 + embedding[1] * -18 + embedding[2] * 20 + embedding[3] * 7 + embedding[4] * 3 + embedding[5] * -17 + embedding[6] * 23 + embedding[7] * -3 + embedding[8] * 28 + embedding[9] * 33;
    let score_12 = embedding[0] * 2 + embedding[1] * 9 + embedding[2] * -2 + embedding[3] * -1 + embedding[4] * -33 + embedding[5] * -10 + embedding[6] * -13 + embedding[7] * 11 + embedding[8] * -21 + embedding[9] * -29;
    let score_13 = embedding[0] * 5 + embedding[1] * -16 + embedding[2] * 6 + embedding[3] * -5 + embedding[4] * 1 + embedding[5] * -17 + embedding[6] * 27 + embedding[7] * 2 + embedding[8] * 26 + embedding[9] * 20;
    let score_14 = embedding[0] * -14 + embedding[1] * 3 + embedding[2] * 4 + embedding[3] * 30 + embedding[4] * -3 + embedding[5] * 8 + embedding[6] * -20 + embedding[7] * -1 + embedding[8] * -14 + embedding[9] * 19;
    let score_15 = embedding[0] * 3 + embedding[1] * -19 + embedding[2] * 1 + embedding[3] * 14 + embedding[4] * 1 + embedding[5] * -13 + embedding[6] * 5 + embedding[7] * 12 + embedding[8] * 18 + embedding[9] * 34;
    let score_16 = embedding[0] * -13 + embedding[1] * 2 + embedding[2] * 27 + embedding[3] * 29 + embedding[4] * -35 + embedding[5] * -17 + embedding[6] * -42 + embedding[7] * -1 + embedding[8] * -25 + embedding[9] * -12;
    let score_17 = embedding[0] * -6 + embedding[1] * -17 + embedding[2] * 40 + embedding[3] * 16 + embedding[4] * -3 + embedding[5] * -40 + embedding[6] * -9 + embedding[7] * -4 + embedding[8] * 25 + embedding[9] * 19;
    let mut best_score = score_0;
    let mut best_palette = 3;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 107;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 39;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 106;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 32;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 33;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 38;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 110;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 114;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 119;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 23;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 79;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 151;
    }
    best_palette
}

fn predict_pixel_423(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 8 + embedding[1] * 1 + embedding[2] * -13 + embedding[3] * -21 + embedding[4] * -10 + embedding[5] * 1 + embedding[6] * 6 + embedding[7] * 6 + embedding[9] * -24;
    let score_1 = embedding[0] * 9 + embedding[1] * -7 + embedding[2] * -9 + embedding[3] * -21 + embedding[4] * -1 + embedding[5] * -2 + embedding[6] * 18 + embedding[7] * 2 + embedding[8] * 17 + embedding[9] * -6;
    let score_2 = embedding[0] * -5 + embedding[1] * -5 + embedding[2] * -4 + embedding[3] * 17 + embedding[4] * 13 + embedding[5] * 13 + embedding[6] * -10 + embedding[7] * -8 + embedding[8] * 1 + embedding[9] * 26;
    let score_3 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 6 + embedding[3] * -7 + embedding[4] * 28 + embedding[5] * 14 + embedding[6] * 7 + embedding[7] * -8 + embedding[8] * -2 + embedding[9] * -9;
    let score_4 = embedding[0] * 10 + embedding[2] * -9 + embedding[3] * -4 + embedding[4] * 9 + embedding[5] * 8 + embedding[7] * -12 + embedding[8] * -8 + embedding[9] * 6;
    let score_5 = embedding[0] * 1 + embedding[1] * -4 + embedding[2] * 8 + embedding[3] * -7 + embedding[4] * 1 + embedding[5] * -9 + embedding[6] * -9 + embedding[7] * -7 + embedding[8] * 9 + embedding[9] * -14;
    let score_6 = embedding[0] * -1 + embedding[1] * 9 + embedding[2] * 3 + embedding[3] * 17 + embedding[4] * -22 + embedding[6] * -24 + embedding[8] * -34 + embedding[9] * -10;
    let score_7 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 4 + embedding[3] * 31 + embedding[4] * -12 + embedding[5] * 5 + embedding[6] * -25 + embedding[7] * -6 + embedding[8] * -27 + embedding[9] * 10;
    let score_8 = embedding[0] * -1 + embedding[1] * -11 + embedding[2] * 12 + embedding[3] * 10 + embedding[4] * 9 + embedding[5] * -5 + embedding[6] * 11 + embedding[7] * -7 + embedding[8] * 16 + embedding[9] * 30;
    let score_9 = embedding[0] * 8 + embedding[1] * -11 + embedding[2] * 1 + embedding[3] * 17 + embedding[4] * 4 + embedding[5] * -10 + embedding[6] * -3 + embedding[7] * 11 + embedding[8] * 9 + embedding[9] * 39;
    let score_10 = embedding[0] * -8 + embedding[1] * 3 + embedding[2] * 25 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -14 + embedding[6] * -32 + embedding[7] * -4 + embedding[8] * -20 + embedding[9] * -9;
    let score_11 = embedding[0] * 4 + embedding[1] * 10 + embedding[2] * -5 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -1 + embedding[6] * -25 + embedding[7] * 3 + embedding[8] * -20 + embedding[9] * 2;
    let score_12 = embedding[0] * 13 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -11 + embedding[5] * -16 + embedding[6] * 30 + embedding[7] * 13 + embedding[8] * 20 + embedding[9] * 2;
    let score_13 = embedding[0] * 9 + embedding[1] * 4 + embedding[2] * -9 + embedding[3] * -13 + embedding[4] * -11 + embedding[5] * -2 + embedding[6] * 8 + embedding[7] * 9 + embedding[8] * -8 + embedding[9] * -15;
    let score_14 = embedding[0] * 5 + embedding[1] * -3 + embedding[2] * -6 + embedding[3] * -16 + embedding[4] * -5 + embedding[5] * -6 + embedding[6] * 21 + embedding[7] * 14 + embedding[8] * 10 + embedding[9] * -14;
    let score_15 = embedding[0] * -9 + embedding[1] * -3 + embedding[2] * -1 + embedding[3] * 14 + embedding[4] * 4 + embedding[5] * 12 + embedding[6] * -11 + embedding[7] * 1 + embedding[9] * 16;
    let score_16 = embedding[0] * -10 + embedding[1] * 12 + embedding[2] * -1 + embedding[3] * 7 + embedding[4] * -22 + embedding[5] * -1 + embedding[6] * -10 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * -21;
    let score_17 = embedding[0] * -4 + embedding[1] * -11 + embedding[2] * 32 + embedding[3] * 34 + embedding[4] * -5 + embedding[5] * -34 + embedding[6] * -13 + embedding[7] * 4 + embedding[8] * 14 + embedding[9] * 42;
    let mut best_score = score_0;
    let mut best_palette = 3;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 33;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 32;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 38;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 23;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 79;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 80;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 144;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 150;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 164;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 165;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 162;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 123;
    }
    best_palette
}

fn predict_pixel_424(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 8 + embedding[1] * -5 + embedding[2] * -10 + embedding[3] * -11 + embedding[4] * 22 + embedding[5] * 7 + embedding[6] * -8 + embedding[7] * -14 + embedding[8] * -1 + embedding[9] * -7;
    let score_1 = embedding[0] * -3 + embedding[1] * -17 + embedding[2] * -6 + embedding[3] * 20 + embedding[5] * 9 + embedding[6] * 9 + embedding[7] * 1 + embedding[9] * -7;
    let score_2 = embedding[0] * 3 + embedding[1] * -1 + embedding[2] * 6 + embedding[3] * -1 + embedding[4] * -32 + embedding[5] * 9 + embedding[6] * -8 + embedding[7] * 11 + embedding[8] * 4 + embedding[9] * 9;
    let score_3 = embedding[1] * 8 + embedding[2] * 2 + embedding[3] * 12 + embedding[4] * -15 + embedding[5] * -5 + embedding[6] * -9 + embedding[7] * 24 + embedding[8] * 23 + embedding[9] * 6;
    let score_4 = embedding[0] * 11 + embedding[1] * 6 + embedding[2] * 3 + embedding[3] * -3 + embedding[4] * 15 + embedding[5] * 5 + embedding[6] * 19 + embedding[7] * -8 + embedding[8] * -15 + embedding[9] * 2;
    let score_5 = embedding[0] * -11 + embedding[1] * 12 + embedding[2] * 2 + embedding[3] * -8 + embedding[4] * -27 + embedding[5] * -6 + embedding[6] * -10 + embedding[7] * 30 + embedding[8] * 11 + embedding[9] * 16;
    let score_6 = embedding[0] * -12 + embedding[1] * 10 + embedding[2] * -9 + embedding[3] * 11 + embedding[4] * -27 + embedding[5] * -2 + embedding[6] * 1 + embedding[7] * 6 + embedding[8] * 9 + embedding[9] * 6;
    let score_7 = embedding[0] * -12 + embedding[1] * 11 + embedding[2] * 21 + embedding[3] * -7 + embedding[4] * -7 + embedding[5] * -2 + embedding[6] * 3 + embedding[7] * -5 + embedding[8] * 8 + embedding[9] * -4;
    let score_8 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 10;
    let score_9 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 10;
    let score_10 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 10;
    let score_11 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 10;
    let score_12 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 10;
    let score_13 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 10;
    let score_14 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 10;
    let score_15 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 10;
    let score_16 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 10;
    let score_17 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 10;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 6;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 20;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 29;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 35;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 52;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_425(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_1 = embedding[0] * 4 + embedding[2] * 3 + embedding[3] * -14 + embedding[4] * 20 + embedding[5] * 14 + embedding[6] * -7 + embedding[7] * -7 + embedding[8] * 5 + embedding[9] * -8;
    let score_2 = embedding[0] * 3 + embedding[1] * -14 + embedding[2] * -15 + embedding[3] * 22 + embedding[4] * -4 + embedding[5] * 3 + embedding[6] * 4 + embedding[7] * -7 + embedding[8] * -2 + embedding[9] * -8;
    let score_3 = embedding[0] * -8 + embedding[1] * 14 + embedding[2] * -5 + embedding[3] * -15 + embedding[4] * -15 + embedding[5] * -6 + embedding[6] * -14 + embedding[7] * 20 + embedding[8] * 10 + embedding[9] * 11;
    let score_4 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * -16 + embedding[3] * 10 + embedding[4] * -25 + embedding[5] * -4 + embedding[6] * -3 + embedding[7] * -1 + embedding[8] * 9 + embedding[9] * 5;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 11;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 11;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 11;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 11;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 11;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 11;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 11;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 11;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 11;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 11;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 11;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 11;
    let score_17 = embedding[0] * 11 + embedding[1] * -4 + embedding[2] * -15 + embedding[3] * -11 + embedding[4] * 13 + embedding[5] * 8 + embedding[6] * -14 + embedding[7] * -14 + embedding[8] * 6 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 29;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 35;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_426(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_1 = embedding[0] * 5 + embedding[1] * 3 + embedding[2] * 10 + embedding[3] * -13 + embedding[4] * 24 + embedding[5] * 16 + embedding[6] * -2 + embedding[7] * -5 + embedding[8] * 5 + embedding[9] * -10;
    let score_2 = embedding[0] * -3 + embedding[1] * -21 + embedding[2] * -13 + embedding[3] * 14 + embedding[4] * 7 + embedding[6] * 1 + embedding[7] * 7 + embedding[8] * -4 + embedding[9] * -10;
    let score_3 = embedding[0] * -14 + embedding[1] * -9 + embedding[2] * -14 + embedding[3] * 11 + embedding[4] * -8 + embedding[5] * -18 + embedding[6] * 2 + embedding[7] * -5 + embedding[8] * 4 + embedding[9] * -5;
    let score_4 = embedding[0] * -5 + embedding[1] * 16 + embedding[2] * -5 + embedding[3] * -16 + embedding[4] * -17 + embedding[5] * -5 + embedding[6] * -14 + embedding[7] * 20 + embedding[8] * 10 + embedding[9] * 14;
    let score_5 = embedding[0] * -8 + embedding[1] * 9 + embedding[2] * -19 + embedding[3] * 13 + embedding[4] * -24 + embedding[5] * -7 + embedding[7] * -2 + embedding[8] * 9 + embedding[9] * 4;
    let score_6 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_7 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_8 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_9 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_10 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_11 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_12 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_13 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_14 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_15 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_16 = embedding[0] * -12 + embedding[1] * 4 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -5 + embedding[9] * 8;
    let score_17 = embedding[0] * 13 + embedding[1] * -4 + embedding[2] * -14 + embedding[3] * -13 + embedding[4] * 12 + embedding[5] * 11 + embedding[6] * -14 + embedding[7] * -14 + embedding[8] * 6 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 11;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 29;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 35;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_427(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_1 = embedding[0] * 1 + embedding[1] * 5 + embedding[2] * 2 + embedding[3] * -12 + embedding[4] * 22 + embedding[5] * 12 + embedding[6] * -5 + embedding[7] * -8 + embedding[8] * 9 + embedding[9] * -10;
    let score_2 = embedding[0] * -2 + embedding[1] * -20 + embedding[2] * -19 + embedding[3] * 13 + embedding[4] * 6 + embedding[6] * -3 + embedding[7] * 7 + embedding[8] * -1 + embedding[9] * -9;
    let score_3 = embedding[0] * 9 + embedding[1] * -11 + embedding[2] * 17 + embedding[3] * -8 + embedding[5] * 10 + embedding[6] * -3 + embedding[7] * 5 + embedding[8] * -7 + embedding[9] * -1;
    let score_4 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_5 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_6 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_7 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_8 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_9 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_10 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_11 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_12 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_13 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_14 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_15 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_16 = embedding[0] * -6 + embedding[1] * -2 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -2 + embedding[2] * -22 + embedding[3] * -13 + embedding[4] * 10 + embedding[5] * 7 + embedding[6] * -16 + embedding[7] * -16 + embedding[8] * 10 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 9;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_428(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_1 = embedding[0] * 8 + embedding[1] * -1 + embedding[2] * -6 + embedding[3] * -12 + embedding[4] * 23 + embedding[5] * 7 + embedding[6] * -14 + embedding[8] * 4 + embedding[9] * -7;
    let score_2 = embedding[0] * -7 + embedding[1] * -9 + embedding[2] * 8 + embedding[3] * -10 + embedding[4] * -29 + embedding[5] * 12 + embedding[6] * 16 + embedding[7] * -24 + embedding[8] * 1 + embedding[9] * -4;
    let score_3 = embedding[0] * -15 + embedding[1] * -9 + embedding[2] * 26 + embedding[3] * -2 + embedding[4] * -35 + embedding[5] * 17 + embedding[6] * 27 + embedding[7] * -8 + embedding[8] * -13 + embedding[9] * -4;
    let score_4 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_5 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_6 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_17 = embedding[0] * 11 + embedding[1] * -2 + embedding[2] * -17 + embedding[3] * -13 + embedding[4] * 23 + embedding[5] * 6 + embedding[6] * -22 + embedding[7] * -6 + embedding[8] * 8 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 46;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 175;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_429(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_430(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_431(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_432(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -22 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_433(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -21 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_434(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 51 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 10 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -20 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 5 + embedding[1] * -1 + embedding[2] * -36 + embedding[3] * -15 + embedding[4] * 23 + embedding[5] * 10 + embedding[6] * -17 + embedding[7] * -24 + embedding[8] * 18 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_435(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_1 = embedding[0] * 5 + embedding[1] * -12 + embedding[2] * 57 + embedding[3] * -8 + embedding[5] * 11 + embedding[6] * 6 + embedding[7] * 29 + embedding[8] * -22 + embedding[9] * 1;
    let score_2 = embedding[0] * 4 + embedding[1] * -2 + embedding[3] * -12 + embedding[4] * 13 + embedding[5] * 10 + embedding[6] * -14 + embedding[7] * 4 + embedding[8] * 9 + embedding[9] * -5;
    let score_3 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_6 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -11 + embedding[9] * 11;
    let score_17 = embedding[0] * 4 + embedding[2] * -30 + embedding[3] * -13 + embedding[4] * 14 + embedding[5] * 9 + embedding[6] * -12 + embedding[7] * -24 + embedding[8] * 11 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 9;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_436(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_1 = embedding[0] * 2 + embedding[1] * -16 + embedding[2] * 18 + embedding[3] * -13 + embedding[4] * 14 + embedding[5] * 20 + embedding[6] * -6 + embedding[7] * 5 + embedding[8] * -1 + embedding[9] * -8;
    let score_2 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_3 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_4 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_5 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_6 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_7 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_8 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_9 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_10 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_11 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_12 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_13 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_14 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_15 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_16 = embedding[0] * -8 + embedding[1] * 6 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 8;
    let score_17 = embedding[0] * 6 + embedding[1] * 4 + embedding[2] * -25 + embedding[3] * -15 + embedding[4] * 16 + embedding[5] * 6 + embedding[6] * -14 + embedding[7] * -24 + embedding[8] * 12 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_437(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_1 = embedding[0] * 3 + embedding[1] * -6 + embedding[2] * 7 + embedding[3] * -17 + embedding[4] * 11 + embedding[5] * 21 + embedding[6] * -1 + embedding[7] * -7 + embedding[8] * -6 + embedding[9] * -3;
    let score_2 = embedding[0] * -18 + embedding[1] * -13 + embedding[2] * 2 + embedding[3] * 5 + embedding[4] * 1 + embedding[5] * 6 + embedding[6] * -5 + embedding[7] * 35 + embedding[8] * 13 + embedding[9] * -8;
    let score_3 = embedding[0] * 22 + embedding[1] * -20 + embedding[2] * 19 + embedding[3] * 31 + embedding[4] * 8 + embedding[5] * 2 + embedding[6] * 13 + embedding[8] * -21 + embedding[9] * -10;
    let score_4 = embedding[1] * 14 + embedding[2] * 2 + embedding[3] * 20 + embedding[4] * -5 + embedding[5] * -11 + embedding[6] * -6 + embedding[7] * 23 + embedding[8] * 31;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 9;
    let score_17 = embedding[0] * 4 + embedding[1] * 5 + embedding[2] * -19 + embedding[3] * -20 + embedding[4] * 12 + embedding[5] * 7 + embedding[6] * -13 + embedding[7] * -20 + embedding[8] * 4 + embedding[9] * -3;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 13;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 15;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_438(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 27 + embedding[1] * -10 + embedding[2] * 8 + embedding[3] * -18 + embedding[4] * 23 + embedding[5] * 16 + embedding[6] * -5 + embedding[7] * -29 + embedding[8] * -20 + embedding[9] * -5;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_2 = embedding[0] * -27 + embedding[1] * -15 + embedding[2] * -17 + embedding[3] * 28 + embedding[4] * -8 + embedding[5] * 11 + embedding[6] * 7 + embedding[7] * 19 + embedding[8] * 15 + embedding[9] * -14;
    let score_3 = embedding[0] * -19 + embedding[1] * 15 + embedding[2] * -3 + embedding[3] * 22 + embedding[4] * -15 + embedding[5] * -11 + embedding[6] * -12 + embedding[7] * 50 + embedding[8] * 45 + embedding[9] * 2;
    let score_4 = embedding[0] * 28 + embedding[1] * 4 + embedding[2] * 24 + embedding[3] * -11 + embedding[4] * 17 + embedding[5] * 14 + embedding[6] * 28 + embedding[7] * -20 + embedding[8] * -35 + embedding[9] * 4;
    let score_5 = embedding[0] * -25 + embedding[1] * 23 + embedding[2] * -12 + embedding[3] * -8 + embedding[4] * -20 + embedding[5] * -11 + embedding[6] * -19 + embedding[7] * 48 + embedding[8] * 27 + embedding[9] * 10;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[0] * -14 + embedding[1] * -3 + embedding[2] * -28 + embedding[3] * -6 + embedding[4] * 7 + embedding[5] * 4 + embedding[6] * -13 + embedding[7] * 5 + embedding[8] * 14 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 6;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 29;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_439(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -13 + embedding[1] * -8 + embedding[2] * -20 + embedding[3] * -8 + embedding[4] * 16 + embedding[5] * 19 + embedding[6] * -4 + embedding[7] * -7 + embedding[8] * 9 + embedding[9] * -11;
    let score_1 = embedding[0] * 19 + embedding[1] * 6 + embedding[2] * -11 + embedding[3] * -24 + embedding[4] * -11 + embedding[5] * -11 + embedding[6] * 10 + embedding[7] * 3 + embedding[8] * -14 + embedding[9] * -27;
    let score_2 = embedding[0] * 21 + embedding[1] * -8 + embedding[2] * -3 + embedding[3] * -25 + embedding[4] * 5 + embedding[5] * -17 + embedding[6] * 33 + embedding[7] * -1 + embedding[8] * 8 + embedding[9] * -2;
    let score_3 = embedding[0] * -1 + embedding[1] * -2 + embedding[2] * 4 + embedding[3] * 26 + embedding[4] * 15 + embedding[5] * 2 + embedding[6] * -8 + embedding[7] * -13 + embedding[8] * -17 + embedding[9] * 37;
    let score_4 = embedding[0] * 10 + embedding[1] * -6 + embedding[2] * 15 + embedding[3] * -12 + embedding[4] * -3 + embedding[5] * -27 + embedding[6] * -3 + embedding[7] * -10 + embedding[8] * 4 + embedding[9] * -15;
    let score_5 = embedding[0] * 21 + embedding[1] * 8 + embedding[2] * 4 + embedding[3] * -6 + embedding[4] * 13 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * -13 + embedding[8] * -26 + embedding[9] * 10;
    let score_6 = embedding[0] * -26 + embedding[1] * -19 + embedding[2] * -10 + embedding[3] * 28 + embedding[4] * -1 + embedding[5] * 26 + embedding[6] * 17 + embedding[7] * 10 + embedding[8] * 11 + embedding[9] * -15;
    let score_7 = embedding[0] * -6 + embedding[1] * 10 + embedding[2] * 19 + embedding[3] * -4 + embedding[4] * -48 + embedding[5] * 32 + embedding[6] * -6 + embedding[7] * 29 + embedding[8] * 14 + embedding[9] * 16;
    let score_8 = embedding[0] * -11 + embedding[1] * 11 + embedding[2] * 17 + embedding[3] * 50 + embedding[4] * -14 + embedding[5] * -5 + embedding[6] * -35 + embedding[7] * -7 + embedding[8] * -48 + embedding[9] * 15;
    let score_9 = embedding[0] * 3 + embedding[1] * 13 + embedding[2] * 11 + embedding[3] * 18 + embedding[4] * -28 + embedding[5] * -16 + embedding[6] * -26 + embedding[7] * 2 + embedding[8] * -55 + embedding[9] * -18;
    let score_10 = embedding[0] * -19 + embedding[1] * 16 + embedding[2] * 10 + embedding[3] * 11 + embedding[4] * -12 + embedding[5] * 6 + embedding[6] * -4 + embedding[7] * 43 + embedding[8] * 44 + embedding[9] * 4;
    let score_11 = embedding[0] * -1 + embedding[1] * -18 + embedding[2] * 30 + embedding[3] * 15 + embedding[4] * 13 + embedding[5] * -29 + embedding[6] * 25 + embedding[7] * -6 + embedding[8] * 23 + embedding[9] * 46;
    let score_12 = embedding[0] * 8 + embedding[1] * 5 + embedding[2] * -24 + embedding[3] * -10 + embedding[4] * 6 + embedding[5] * 10 + embedding[6] * 15 + embedding[7] * 24 + embedding[8] * -9 + embedding[9] * 8;
    let score_13 = embedding[0] * -25 + embedding[1] * 30 + embedding[2] * 5 + embedding[3] * -12 + embedding[4] * -33 + embedding[5] * 10 + embedding[6] * -10 + embedding[7] * 52 + embedding[8] * 29 + embedding[9] * 16;
    let score_14 = embedding[0] * 11 + embedding[1] * -14 + embedding[2] * 10 + embedding[3] * 24 + embedding[4] * 13 + embedding[5] * -23 + embedding[6] * 3 + embedding[7] * 10 + embedding[8] * 4 + embedding[9] * 54;
    let score_15 = embedding[0] * -37 + embedding[1] * 20 + embedding[2] * 38 + embedding[3] * -7 + embedding[4] * -7 + embedding[5] * 9 + embedding[6] * 14 + embedding[7] * 2 + embedding[8] * 19 + embedding[9] * -10;
    let score_16 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 31 + embedding[3] * 27 + embedding[4] * -33 + embedding[5] * -26 + embedding[6] * -36 + embedding[7] * -5 + embedding[8] * -33 + embedding[9] * -18;
    let score_17 = embedding[0] * -1 + embedding[1] * 10 + embedding[2] * -6 + embedding[3] * 31 + embedding[4] * -27 + embedding[5] * -1 + embedding[6] * -32 + embedding[7] * 4 + embedding[8] * -24 + embedding[9] * -3;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 6;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 20;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 15;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 38;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 26;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 29;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 23;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 52;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 79;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 100;
    }
    best_palette
}

fn predict_pixel_440(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -10 + embedding[3] * -29 + embedding[4] * -4 + embedding[5] * -8 + embedding[6] * 27 + embedding[7] * 4 + embedding[8] * 23 + embedding[9] * -13;
    let score_1 = embedding[0] * 9 + embedding[1] * 4 + embedding[2] * -13 + embedding[3] * -29 + embedding[4] * -14 + embedding[5] * -3 + embedding[6] * 10 + embedding[7] * 8 + embedding[8] * 4 + embedding[9] * -34;
    let score_2 = embedding[0] * -9 + embedding[1] * -3 + embedding[2] * -2 + embedding[3] * 23 + embedding[4] * 18 + embedding[5] * 14 + embedding[6] * -14 + embedding[7] * -10 + embedding[8] * 3 + embedding[9] * 30;
    let score_3 = embedding[0] * 1 + embedding[1] * -1 + embedding[2] * -7 + embedding[3] * -7 + embedding[4] * 18 + embedding[5] * 16 + embedding[6] * -5 + embedding[7] * -12 + embedding[8] * -3 + embedding[9] * -2;
    let score_4 = embedding[0] * 12 + embedding[2] * -9 + embedding[3] * -6 + embedding[4] * 12 + embedding[5] * 10 + embedding[6] * -3 + embedding[7] * -15 + embedding[8] * -9 + embedding[9] * 5;
    let score_5 = embedding[0] * 3 + embedding[1] * -2 + embedding[2] * 11 + embedding[3] * -14 + embedding[4] * -3 + embedding[5] * -14 + embedding[6] * -7 + embedding[7] * -7 + embedding[8] * 13 + embedding[9] * -22;
    let score_6 = embedding[0] * 4 + embedding[1] * 6 + embedding[2] * -25 + embedding[3] * -6 + embedding[4] * 6 + embedding[5] * 17 + embedding[6] * 8 + embedding[7] * 22 + embedding[8] * -4 + embedding[9] * 8;
    let score_7 = embedding[0] * -12 + embedding[1] * 12 + embedding[2] * 16 + embedding[3] * 40 + embedding[4] * -9 + embedding[5] * 3 + embedding[6] * -35 + embedding[7] * -8 + embedding[8] * -38 + embedding[9] * 8;
    let score_8 = embedding[0] * -1 + embedding[1] * 13 + embedding[2] * 12 + embedding[3] * 20 + embedding[4] * -20 + embedding[5] * -6 + embedding[6] * -29 + embedding[7] * -2 + embedding[8] * -49 + embedding[9] * -13;
    let score_9 = embedding[0] * -8 + embedding[1] * -10 + embedding[2] * -12 + embedding[3] * 25 + embedding[4] * 3 + embedding[5] * 12 + embedding[6] * 18 + embedding[7] * -7 + embedding[8] * -6 + embedding[9] * -11;
    let score_10 = embedding[0] * -3 + embedding[1] * -13 + embedding[2] * 25 + embedding[3] * 16 + embedding[4] * 15 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * -7 + embedding[8] * 22 + embedding[9] * 43;
    let score_11 = embedding[0] * 4 + embedding[1] * -11 + embedding[2] * 11 + embedding[3] * 25 + embedding[4] * 13 + embedding[5] * -13 + embedding[6] * -5 + embedding[7] * 8 + embedding[8] * 13 + embedding[9] * 51;
    let score_12 = embedding[0] * -13 + embedding[1] * 5 + embedding[2] * 33 + embedding[3] * 28 + embedding[4] * -29 + embedding[5] * -16 + embedding[6] * -39 + embedding[7] * -6 + embedding[8] * -31 + embedding[9] * -13;
    let score_13 = embedding[0] * -7 + embedding[1] * -13 + embedding[2] * 39 + embedding[3] * 40 + embedding[4] * 1 + embedding[5] * -39 + embedding[6] * -15 + embedding[7] * 3 + embedding[8] * 19 + embedding[9] * 52;
    let score_14 = embedding[0] * 10 + embedding[2] * -3 + embedding[3] * -4 + embedding[4] * -17 + embedding[5] * -6 + embedding[6] * 9 + embedding[7] * -13 + embedding[8] * 5 + embedding[9] * -7;
    let score_15 = embedding[0] * -1 + embedding[1] * 3 + embedding[2] * 13 + embedding[3] * 16 + embedding[4] * -15 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 5;
    let score_16 = embedding[0] * -1 + embedding[1] * 3 + embedding[2] * 13 + embedding[3] * 16 + embedding[4] * -15 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 5;
    let score_17 = embedding[0] * -1 + embedding[1] * 3 + embedding[2] * 13 + embedding[3] * 16 + embedding[4] * -15 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 5;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 26;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 32;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 33;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 6;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 38;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 79;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 123;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 93;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_441(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 14 + embedding[1] * -9 + embedding[2] * -8 + embedding[3] * -46 + embedding[4] * -8 + embedding[5] * -13 + embedding[6] * 41 + embedding[7] * 13 + embedding[8] * 34 + embedding[9] * -27;
    let score_1 = embedding[0] * 12 + embedding[1] * 3 + embedding[2] * -14 + embedding[3] * -45 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 21 + embedding[7] * 17 + embedding[8] * 9 + embedding[9] * -50;
    let score_2 = embedding[0] * -13 + embedding[1] * -6 + embedding[2] * 3 + embedding[3] * 35 + embedding[4] * 25 + embedding[5] * 19 + embedding[6] * -18 + embedding[7] * -12 + embedding[8] * 4 + embedding[9] * 48;
    let score_3 = embedding[0] * 16 + embedding[1] * 2 + embedding[2] * -10 + embedding[3] * -3 + embedding[4] * 15 + embedding[5] * 10 + embedding[6] * -2 + embedding[7] * -19 + embedding[8] * -16 + embedding[9] * 9;
    let score_4 = embedding[0] * 4 + embedding[1] * -3 + embedding[2] * 18 + embedding[3] * -23 + embedding[4] * -8 + embedding[5] * -24 + embedding[6] * -2 + embedding[7] * -2 + embedding[8] * 21 + embedding[9] * -34;
    let score_5 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * -9 + embedding[3] * -3 + embedding[4] * 22 + embedding[5] * 16 + embedding[6] * -3 + embedding[7] * -15 + embedding[8] * -10 + embedding[9] * 1;
    let score_6 = embedding[0] * 4 + embedding[1] * 10 + embedding[2] * -32 + embedding[3] * -8 + embedding[4] * 7 + embedding[5] * 22 + embedding[6] * 13 + embedding[7] * 32 + embedding[8] * -5 + embedding[9] * 10;
    let score_7 = embedding[0] * -14 + embedding[1] * 17 + embedding[2] * 17 + embedding[3] * 56 + embedding[4] * -16 + embedding[5] * 6 + embedding[6] * -45 + embedding[7] * -10 + embedding[8] * -53 + embedding[9] * 12;
    let score_8 = embedding[0] * -5 + embedding[1] * 20 + embedding[2] * 11 + embedding[3] * 34 + embedding[4] * -33 + embedding[5] * -2 + embedding[6] * -41 + embedding[7] * -1 + embedding[8] * -62 + embedding[9] * -18;
    let score_9 = embedding[0] * -6 + embedding[1] * -17 + embedding[2] * 29 + embedding[3] * 26 + embedding[4] * 19 + embedding[5] * -12 + embedding[6] * 17 + embedding[7] * -9 + embedding[8] * 28 + embedding[9] * 59;
    let score_10 = embedding[0] * -13 + embedding[1] * -11 + embedding[2] * -13 + embedding[3] * 30 + embedding[4] * 4 + embedding[5] * 15 + embedding[6] * 29 + embedding[7] * -5 + embedding[8] * -7 + embedding[9] * -17;
    let score_11 = embedding[0] * 3 + embedding[1] * -15 + embedding[2] * 14 + embedding[3] * 33 + embedding[4] * 19 + embedding[5] * -10 + embedding[6] * -8 + embedding[7] * 6 + embedding[8] * 15 + embedding[9] * 67;
    let score_12 = embedding[0] * -11 + embedding[1] * 10 + embedding[2] * 33 + embedding[3] * 34 + embedding[4] * -37 + embedding[5] * -18 + embedding[6] * -45 + embedding[7] * -7 + embedding[8] * -42 + embedding[9] * -17;
    let score_13 = embedding[0] * 4 + embedding[1] * 17 + embedding[2] * -4 + embedding[3] * 36 + embedding[4] * -27 + embedding[5] * 3 + embedding[6] * -36 + embedding[7] * -1 + embedding[8] * -35 + embedding[9] * 3;
    let score_14 = embedding[0] * -7 + embedding[1] * -18 + embedding[2] * 46 + embedding[3] * 50 + embedding[4] * 8 + embedding[5] * -39 + embedding[6] * -17 + embedding[7] * 1 + embedding[8] * 22 + embedding[9] * 65;
    let score_15 = embedding[0] * 14 + embedding[2] * -3 + embedding[3] * -8 + embedding[4] * -17 + embedding[5] * -6 + embedding[6] * 13 + embedding[7] * -14 + embedding[8] * 6 + embedding[9] * -12;
    let score_16 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -6 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -6 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 26;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 32;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 33;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 38;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 6;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 79;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 80;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 123;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_442(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 2 + embedding[1] * 3 + embedding[2] * -20 + embedding[3] * -19 + embedding[4] * -10 + embedding[5] * 8 + embedding[6] * -5 + embedding[7] * 5 + embedding[9] * -29;
    let score_1 = embedding[0] * 6 + embedding[1] * -17 + embedding[2] * -13 + embedding[3] * -20 + embedding[4] * 6 + embedding[5] * -1 + embedding[6] * 22 + embedding[7] * 1 + embedding[8] * 34 + embedding[9] * 3;
    let score_2 = embedding[0] * -13 + embedding[1] * -5 + embedding[2] * -12 + embedding[3] * 13 + embedding[4] * 13 + embedding[5] * 20 + embedding[6] * -18 + embedding[7] * -7 + embedding[8] * 4 + embedding[9] * 15;
    let score_3 = embedding[0] * 21 + embedding[1] * 11 + embedding[2] * 7 + embedding[3] * -11 + embedding[4] * 16 + embedding[5] * -2 + embedding[6] * 16 + embedding[7] * -15 + embedding[8] * -21 + embedding[9] * 5;
    let score_4 = embedding[0] * -5 + embedding[1] * -9 + embedding[2] * 6 + embedding[3] * -4 + embedding[4] * 2 + embedding[5] * -9 + embedding[6] * -17 + embedding[7] * -9 + embedding[8] * 20 + embedding[9] * -11;
    let score_5 = embedding[0] * -2 + embedding[2] * -11 + embedding[3] * -8 + embedding[4] * 17 + embedding[5] * 14 + embedding[6] * -2 + embedding[7] * -10 + embedding[9] * -6;
    let score_6 = embedding[0] * 29 + embedding[1] * 16 + embedding[2] * 21 + embedding[3] * -8 + embedding[4] * 12 + embedding[5] * -15 + embedding[6] * 24 + embedding[7] * -16 + embedding[8] * -29 + embedding[9] * 9;
    let score_7 = embedding[0] * 1 + embedding[1] * 5 + embedding[2] * -30 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 20 + embedding[6] * 9 + embedding[7] * 24 + embedding[8] * 2 + embedding[9] * 5;
    let score_8 = embedding[0] * -21 + embedding[1] * 14 + embedding[2] * 14 + embedding[3] * 44 + embedding[4] * -16 + embedding[5] * 7 + embedding[6] * -42 + embedding[7] * -5 + embedding[8] * -42;
    let score_9 = embedding[0] * -12 + embedding[1] * 15 + embedding[2] * 9 + embedding[3] * 25 + embedding[4] * -32 + embedding[5] * -3 + embedding[6] * -37 + embedding[7] * 3 + embedding[8] * -49 + embedding[9] * -24;
    let score_10 = embedding[0] * -4 + embedding[1] * -19 + embedding[2] * 19 + embedding[3] * 5 + embedding[4] * 11 + embedding[5] * -14 + embedding[6] * 23 + embedding[7] * -5 + embedding[8] * 34 + embedding[9] * 33;
    let score_11 = embedding[0] * -1 + embedding[1] * -19 + embedding[2] * 2 + embedding[3] * 15 + embedding[4] * 11 + embedding[5] * -9 + embedding[6] * -1 + embedding[7] * 10 + embedding[8] * 23 + embedding[9] * 37;
    let score_12 = embedding[0] * -19 + embedding[1] * 7 + embedding[2] * 33 + embedding[3] * 31 + embedding[4] * -32 + embedding[5] * -18 + embedding[6] * -45 + embedding[7] * -4 + embedding[8] * -30 + embedding[9] * -19;
    let score_13 = embedding[0] * -10 + embedding[1] * -19 + embedding[2] * 37 + embedding[3] * 31 + embedding[4] * -2 + embedding[5] * -40 + embedding[6] * -10 + embedding[7] * 3 + embedding[8] * 27 + embedding[9] * 39;
    let score_14 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -8 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 3;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 39;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 26;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 38;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 79;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 123;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_443(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 7 + embedding[1] * -2 + embedding[2] * -5 + embedding[3] * -6 + embedding[4] * 7 + embedding[5] * 4 + embedding[6] * -4 + embedding[7] * -8 + embedding[8] * 2 + embedding[9] * -3;
    let score_1 = embedding[0] * -2 + embedding[1] * -2 + embedding[2] * -1 + embedding[3] * -4 + embedding[4] * -5 + embedding[5] * 3 + embedding[6] * -6 + embedding[7] * 3 + embedding[8] * 5 + embedding[9] * -11;
    let score_2 = embedding[0] * -2 + embedding[1] * -3 + embedding[2] * -4 + embedding[3] * -1 + embedding[4] * 1 + embedding[5] * 6 + embedding[6] * -5 + embedding[7] * 1 + embedding[8] * 7 + embedding[9] * -4;
    let score_3 = embedding[0] * -3 + embedding[1] * -3 + embedding[2] * -4 + embedding[3] * -3 + embedding[4] * 1 + embedding[5] * 7 + embedding[6] * -6 + embedding[7] * 2 + embedding[8] * 7 + embedding[9] * -4;
    let score_4 = embedding[1] * -6 + embedding[2] * -3 + embedding[3] * -4 + embedding[5] * 2 + embedding[6] * 3 + embedding[7] * 4 + embedding[8] * 13 + embedding[9] * -2;
    let score_5 = embedding[0] * -5 + embedding[1] * -3 + embedding[2] * -2 + embedding[3] * 5 + embedding[4] * 1 + embedding[5] * 8 + embedding[6] * -7 + embedding[7] * 1 + embedding[8] * 5 + embedding[9] * 3;
    let score_6 = embedding[0] * 6 + embedding[4] * 3 + embedding[5] * 1 + embedding[6] * -8 + embedding[7] * -7 + embedding[8] * -3 + embedding[9] * -3;
    let score_7 = embedding[0] * 3 + embedding[1] * 1 + embedding[3] * -2 + embedding[4] * 8 + embedding[5] * 2 + embedding[6] * -2 + embedding[7] * -6 + embedding[8] * -1 + embedding[9] * -4;
    let score_8 = embedding[0] * 2 + embedding[1] * 1 + embedding[3] * -2 + embedding[4] * 9 + embedding[5] * 5 + embedding[6] * -1 + embedding[7] * -5 + embedding[8] * -1 + embedding[9] * -4;
    let score_9 = embedding[0] * 4 + embedding[1] * 6 + embedding[2] * -13 + embedding[3] * -2 + embedding[5] * 10 + embedding[6] * 8 + embedding[7] * 21 + embedding[8] * -1 + embedding[9] * 6;
    let score_10 = embedding[0] * 6 + embedding[1] * 1 + embedding[2] * -1 + embedding[4] * 3 + embedding[6] * 1 + embedding[7] * -2 + embedding[8] * 1 + embedding[9] * -2;
    let score_11 = embedding[0] * 3 + embedding[2] * -1 + embedding[3] * 3 + embedding[4] * 1 + embedding[5] * -2 + embedding[6] * -2 + embedding[7] * -3 + embedding[8] * -3 + embedding[9] * -3;
    let score_12 = embedding[0] * -6 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 22 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 9 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 6;
    let score_13 = embedding[0] * -6 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 22 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 9 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 6;
    let score_14 = embedding[0] * -6 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 22 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 9 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 6;
    let score_15 = embedding[0] * -6 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 22 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 9 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 6;
    let score_16 = embedding[0] * -6 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 22 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 9 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * -6 + embedding[1] * 6 + embedding[2] * 19 + embedding[3] * 22 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 9 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 56;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 60;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 62;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 67;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 73;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 74;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 27;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 92;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 26;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 11;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 191;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_444(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 8 + embedding[1] * -2 + embedding[2] * -6 + embedding[3] * -7 + embedding[4] * 8 + embedding[5] * 4 + embedding[6] * -5 + embedding[7] * -9 + embedding[8] * 3 + embedding[9] * -4;
    let score_1 = embedding[0] * -2 + embedding[1] * -2 + embedding[2] * -2 + embedding[3] * -5 + embedding[4] * -4 + embedding[5] * 4 + embedding[6] * -7 + embedding[7] * 2 + embedding[8] * 5 + embedding[9] * -12;
    let score_2 = embedding[0] * -2 + embedding[1] * -3 + embedding[2] * -5 + embedding[3] * -2 + embedding[4] * 2 + embedding[5] * 7 + embedding[6] * -5 + embedding[8] * 8 + embedding[9] * -5;
    let score_3 = embedding[0] * -2 + embedding[1] * -3 + embedding[2] * -5 + embedding[3] * -4 + embedding[4] * 2 + embedding[5] * 7 + embedding[6] * -6 + embedding[8] * 8 + embedding[9] * -4;
    let score_4 = embedding[1] * -6 + embedding[2] * -4 + embedding[3] * -6 + embedding[4] * 1 + embedding[5] * 3 + embedding[6] * 3 + embedding[7] * 3 + embedding[8] * 14 + embedding[9] * -2;
    let score_5 = embedding[0] * -5 + embedding[1] * -3 + embedding[2] * -3 + embedding[3] * 4 + embedding[4] * 2 + embedding[5] * 8 + embedding[6] * -7 + embedding[8] * 5 + embedding[9] * 3;
    let score_6 = embedding[0] * 6 + embedding[2] * -1 + embedding[3] * -1 + embedding[4] * 4 + embedding[5] * 2 + embedding[6] * -9 + embedding[7] * -8 + embedding[8] * -2 + embedding[9] * -4;
    let score_7 = embedding[0] * 3 + embedding[1] * 1 + embedding[2] * -1 + embedding[3] * -3 + embedding[4] * 9 + embedding[5] * 3 + embedding[6] * -2 + embedding[7] * -7 + embedding[9] * -5;
    let score_8 = embedding[0] * 2 + embedding[1] * 1 + embedding[3] * -3 + embedding[4] * 10 + embedding[5] * 5 + embedding[6] * -2 + embedding[7] * -7 + embedding[9] * -5;
    let score_9 = embedding[0] * 4 + embedding[1] * 6 + embedding[2] * -14 + embedding[3] * -3 + embedding[4] * 1 + embedding[5] * 11 + embedding[6] * 7 + embedding[7] * 20 + embedding[8] * -1 + embedding[9] * 6;
    let score_10 = embedding[0] * -6 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 10 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 6;
    let score_11 = embedding[0] * -6 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 10 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 6;
    let score_12 = embedding[0] * -6 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 21 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 10 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 6;
    let score_13 = embedding[0] * -6 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 21 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 10 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 6;
    let score_14 = embedding[0] * -6 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 21 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 10 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 6;
    let score_15 = embedding[0] * -6 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 10 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 6;
    let score_16 = embedding[0] * -6 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 21 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 10 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * -6 + embedding[1] * 5 + embedding[2] * 18 + embedding[3] * 21 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 10 + embedding[7] * 18 + embedding[8] * -7 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 56;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 60;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 62;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 67;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 73;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 74;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 27;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 92;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 26;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_445(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 5 + embedding[2] * -3 + embedding[3] * -6 + embedding[4] * 9 + embedding[5] * 5 + embedding[6] * -3 + embedding[7] * -8 + embedding[8] * 2 + embedding[9] * -5;
    let score_1 = embedding[0] * -2 + embedding[1] * -3 + embedding[2] * -2 + embedding[3] * -4 + embedding[4] * -8 + embedding[5] * 1 + embedding[6] * -7 + embedding[7] * 3 + embedding[8] * 5 + embedding[9] * -10;
    let score_2 = embedding[0] * -1 + embedding[1] * -4 + embedding[2] * -4 + embedding[3] * -1 + embedding[4] * -2 + embedding[5] * 5 + embedding[6] * -6 + embedding[7] * 1 + embedding[8] * 6 + embedding[9] * -2;
    let score_3 = embedding[0] * -2 + embedding[1] * -4 + embedding[2] * -5 + embedding[3] * -3 + embedding[4] * -2 + embedding[5] * 5 + embedding[6] * -6 + embedding[7] * 2 + embedding[8] * 7 + embedding[9] * -3;
    let score_4 = embedding[0] * 1 + embedding[1] * -8 + embedding[2] * -3 + embedding[3] * -4 + embedding[4] * -3 + embedding[5] * 1 + embedding[6] * 2 + embedding[7] * 5 + embedding[8] * 12;
    let score_5 = embedding[0] * 5 + embedding[1] * 1 + embedding[2] * 2 + embedding[3] * 1 + embedding[4] * 4 + embedding[5] * 2 + embedding[6] * -8 + embedding[7] * -7 + embedding[8] * -3 + embedding[9] * -3;
    let score_6 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * -3 + embedding[3] * 7 + embedding[4] * -2 + embedding[5] * 6 + embedding[6] * -8 + embedding[7] * 2 + embedding[8] * 4 + embedding[9] * 5;
    let score_7 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * 2 + embedding[3] * -1 + embedding[4] * 8 + embedding[5] * 2 + embedding[6] * -2 + embedding[7] * -6 + embedding[8] * -2 + embedding[9] * -4;
    let score_8 = embedding[0] * 2 + embedding[1] * 2 + embedding[2] * 2 + embedding[3] * -2 + embedding[4] * 9 + embedding[5] * 4 + embedding[7] * -6 + embedding[8] * -2 + embedding[9] * -4;
    let score_9 = embedding[0] * 4 + embedding[1] * 7 + embedding[2] * -12 + embedding[3] * -2 + embedding[5] * 10 + embedding[6] * 9 + embedding[7] * 22 + embedding[8] * -2 + embedding[9] * 6;
    let score_10 = embedding[0] * 5 + embedding[1] * 2 + embedding[2] * 1 + embedding[4] * 3 + embedding[6] * 2 + embedding[7] * -1 + embedding[8] * 1 + embedding[9] * -3;
    let score_11 = embedding[0] * 2 + embedding[1] * 1 + embedding[2] * 1 + embedding[3] * 3 + embedding[4] * 2 + embedding[5] * -2 + embedding[6] * -1 + embedding[7] * -3 + embedding[8] * -3 + embedding[9] * -3;
    let score_12 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 9 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 9 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 6;
    let score_14 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 9 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 6;
    let score_15 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 9 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 6;
    let score_16 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 9 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * -7 + embedding[1] * 8 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * 9 + embedding[7] * 19 + embedding[8] * -7 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 56;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 60;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 62;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 67;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 74;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 73;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 27;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 92;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 26;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 11;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 191;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_446(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[2] * -17 + embedding[3] * -12 + embedding[4] * -11 + embedding[5] * 3 + embedding[6] * -7 + embedding[7] * 4 + embedding[8] * -1 + embedding[9] * -18;
    let score_1 = embedding[0] * 6 + embedding[1] * -12 + embedding[2] * -13 + embedding[3] * -12 + embedding[4] * 2 + embedding[5] * -3 + embedding[6] * 10 + embedding[7] * 1 + embedding[8] * 20 + embedding[9] * 4;
    let score_2 = embedding[0] * -6 + embedding[1] * -7 + embedding[2] * -10 + embedding[3] * 9 + embedding[4] * 4 + embedding[5] * 11 + embedding[6] * -16 + embedding[7] * -6 + embedding[8] * 2 + embedding[9] * 14;
    let score_3 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 13 + embedding[3] * -8 + embedding[4] * 25 + embedding[5] * 11 + embedding[6] * 11 + embedding[7] * -4 + embedding[8] * 3 + embedding[9] * -11;
    let score_4 = embedding[0] * 15 + embedding[1] * 6 + embedding[2] * 3 + embedding[3] * -7 + embedding[4] * 9 + embedding[5] * -3 + embedding[6] * 9 + embedding[7] * -11 + embedding[8] * -14 + embedding[9] * 5;
    let score_5 = embedding[0] * -3 + embedding[1] * -1 + embedding[2] * -6 + embedding[3] * -8 + embedding[4] * 11 + embedding[5] * 11 + embedding[6] * 1 + embedding[7] * -12 + embedding[9] * -5;
    let score_6 = embedding[0] * -2 + embedding[1] * -9 + embedding[2] * 4 + embedding[3] * -1 + embedding[4] * -1 + embedding[5] * -10 + embedding[6] * -17 + embedding[7] * -6 + embedding[8] * 13 + embedding[9] * -7;
    let score_7 = embedding[0] * -6 + embedding[1] * 4 + embedding[2] * 1 + embedding[3] * -8 + embedding[4] * 17 + embedding[5] * 11 + embedding[6] * 7 + embedding[7] * -5 + embedding[8] * 2 + embedding[9] * -9;
    let score_8 = embedding[0] * 25 + embedding[1] * 11 + embedding[2] * 18 + embedding[3] * -3 + embedding[4] * 5 + embedding[5] * -16 + embedding[6] * 16 + embedding[7] * -11 + embedding[8] * -23 + embedding[9] * 9;
    let score_9 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * -23 + embedding[3] * -6 + embedding[4] * 1 + embedding[5] * 13 + embedding[6] * 8 + embedding[7] * 21 + embedding[8] * -1 + embedding[9] * 8;
    let score_10 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 3 + embedding[3] * 31 + embedding[4] * -19 + embedding[5] * -1 + embedding[6] * -31 + embedding[7] * -3 + embedding[8] * -26 + embedding[9] * 5;
    let score_11 = embedding[0] * -4 + embedding[1] * 6 + embedding[3] * 15 + embedding[4] * -26 + embedding[5] * -4 + embedding[6] * -27 + embedding[7] * 5 + embedding[8] * -30 + embedding[9] * -15;
    let score_12 = embedding[0] * -1 + embedding[1] * -15 + embedding[2] * 12 + embedding[3] * 5 + embedding[4] * 3 + embedding[5] * -12 + embedding[6] * 13 + embedding[7] * -2 + embedding[8] * 21 + embedding[9] * 26;
    let score_13 = embedding[0] * 5 + embedding[1] * -17 + embedding[2] * -2 + embedding[3] * 14 + embedding[5] * -13 + embedding[6] * -2 + embedding[7] * 12 + embedding[8] * 15 + embedding[9] * 31;
    let score_14 = embedding[0] * -10 + embedding[1] * -1 + embedding[2] * 25 + embedding[3] * 24 + embedding[4] * -26 + embedding[5] * -17 + embedding[6] * -38 + embedding[7] * -3 + embedding[8] * -18 + embedding[9] * -9;
    let score_15 = embedding[0] * -5 + embedding[1] * -16 + embedding[2] * 32 + embedding[3] * 31 + embedding[4] * -10 + embedding[5] * -41 + embedding[6] * -13 + embedding[7] * 6 + embedding[8] * 18 + embedding[9] * 36;
    let score_16 = embedding[0] * 3 + embedding[2] * 12 + embedding[3] * 19 + embedding[4] * -11 + embedding[5] * -12 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -16 + embedding[9] * 4;
    let score_17 = embedding[0] * 3 + embedding[2] * 12 + embedding[3] * 19 + embedding[4] * -11 + embedding[5] * -12 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -16 + embedding[9] * 4;
    let mut best_score = score_0;
    let mut best_palette = 3;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 37;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 5;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 78;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 39;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 26;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 32;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 33;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 38;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 23;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 79;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 123;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_447(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 9 + embedding[1] * -9 + embedding[2] * -10 + embedding[3] * -22 + embedding[4] * -4 + embedding[5] * -7 + embedding[6] * 20 + embedding[7] * 3 + embedding[8] * 18 + embedding[9] * -5;
    let score_1 = embedding[0] * 8 + embedding[1] * -1 + embedding[2] * -14 + embedding[3] * -22 + embedding[4] * -13 + embedding[5] * -3 + embedding[6] * 5 + embedding[7] * 7 + embedding[8] * -1 + embedding[9] * -24;
    let score_2 = embedding[0] * -4 + embedding[1] * -6 + embedding[2] * -7 + embedding[3] * 16 + embedding[4] * 8 + embedding[5] * 9 + embedding[6] * -11 + embedding[7] * -7 + embedding[8] * -1 + embedding[9] * 26;
    let score_3 = embedding[0] * -8 + embedding[1] * 12 + embedding[2] * 18 + embedding[3] * -8 + embedding[4] * 30 + embedding[5] * 11 + embedding[6] * 13 + embedding[7] * -3 + embedding[9] * -11;
    let score_4 = embedding[0] * 11 + embedding[1] * -3 + embedding[2] * -11 + embedding[3] * -5 + embedding[4] * 6 + embedding[5] * 6 + embedding[6] * -2 + embedding[7] * -11 + embedding[8] * -9 + embedding[9] * 7;
    let score_5 = embedding[1] * 1 + embedding[3] * -6 + embedding[4] * 17 + embedding[5] * 12 + embedding[6] * -1 + embedding[7] * -9 + embedding[8] * -1 + embedding[9] * -4;
    let score_6 = embedding[0] * 2 + embedding[1] * -5 + embedding[2] * 10 + embedding[3] * -8 + embedding[4] * -3 + embedding[5] * -15 + embedding[6] * -11 + embedding[7] * -5 + embedding[8] * 9 + embedding[9] * -14;
    let score_7 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 7 + embedding[3] * -8 + embedding[4] * 22 + embedding[5] * 11 + embedding[6] * 9 + embedding[7] * -4 + embedding[8] * -1 + embedding[9] * -9;
    let score_8 = embedding[0] * 4 + embedding[1] * 4 + embedding[2] * -23 + embedding[3] * -5 + embedding[5] * 12 + embedding[6] * 9 + embedding[7] * 23 + embedding[8] * -6 + embedding[9] * 11;
    let score_9 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 5 + embedding[3] * 36 + embedding[4] * -23 + embedding[5] * -1 + embedding[6] * -32 + embedding[7] * -5 + embedding[8] * -34 + embedding[9] * 9;
    let score_10 = embedding[0] * -1 + embedding[1] * 7 + embedding[2] * 4 + embedding[3] * 20 + embedding[4] * -31 + embedding[5] * -5 + embedding[6] * -30 + embedding[7] * 2 + embedding[8] * -41 + embedding[9] * -10;
    let score_11 = embedding[0] * -1 + embedding[1] * -14 + embedding[2] * 17 + embedding[3] * 10 + embedding[4] * 4 + embedding[5] * -12 + embedding[6] * 15 + embedding[7] * -6 + embedding[8] * 18 + embedding[9] * 35;
    let score_12 = embedding[0] * -4 + embedding[1] * -14 + embedding[2] * -12 + embedding[3] * 26 + embedding[4] * -6 + embedding[5] * 4 + embedding[6] * 16 + embedding[7] * -4 + embedding[8] * -7 + embedding[9] * -6;
    let score_13 = embedding[0] * 4 + embedding[1] * 12 + embedding[2] * 4 + embedding[3] * 24 + embedding[4] * -8 + embedding[5] * -13 + embedding[6] * -5 + embedding[7] * 22 + embedding[8] * 30 + embedding[9] * 5;
    let score_14 = embedding[0] * 9 + embedding[1] * -14 + embedding[2] * 1 + embedding[3] * 18 + embedding[5] * -15 + embedding[6] * -3 + embedding[7] * 10 + embedding[8] * 9 + embedding[9] * 42;
    let score_15 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 26 + embedding[3] * 28 + embedding[4] * -29 + embedding[5] * -18 + embedding[6] * -39 + embedding[7] * -4 + embedding[8] * -25 + embedding[9] * -7;
    let score_16 = embedding[0] * 5 + embedding[1] * 9 + embedding[2] * -5 + embedding[3] * 27 + embedding[4] * -30 + embedding[5] * -4 + embedding[6] * -28 + embedding[7] * 4 + embedding[8] * -25 + embedding[9] * 3;
    let score_17 = embedding[0] * -3 + embedding[1] * -16 + embedding[2] * 38 + embedding[3] * 36 + embedding[4] * -9 + embedding[5] * -41 + embedding[6] * -14 + embedding[7] * 7 + embedding[8] * 17 + embedding[9] * 44;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 37;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 5;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 78;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 26;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 32;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 33;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 38;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 6;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 15;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 23;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 79;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 80;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 123;
    }
    best_palette
}

fn predict_pixel_448(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 10 + embedding[1] * -8 + embedding[2] * -13 + embedding[3] * -8 + embedding[4] * 12 + embedding[5] * 4 + embedding[6] * -11 + embedding[7] * -11 + embedding[9] * -4;
    let score_1 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 10 + embedding[3] * -6 + embedding[4] * 25 + embedding[5] * 6 + embedding[6] * 4 + embedding[7] * -3 + embedding[8] * -1 + embedding[9] * -10;
    let score_2 = embedding[0] * 2 + embedding[3] * -6 + embedding[4] * 19 + embedding[5] * 6 + embedding[6] * 1 + embedding[7] * -4 + embedding[8] * -2 + embedding[9] * -7;
    let score_3 = embedding[0] * -3 + embedding[1] * -19 + embedding[2] * -8 + embedding[3] * 23 + embedding[4] * -7 + embedding[5] * 7 + embedding[6] * 6 + embedding[7] * 5 + embedding[8] * 2 + embedding[9] * -5;
    let score_4 = embedding[0] * 4 + embedding[1] * -3 + embedding[2] * 3 + embedding[3] * 1 + embedding[4] * -38 + embedding[5] * 7 + embedding[6] * -11 + embedding[7] * 14 + embedding[8] * 4 + embedding[9] * 12;
    let score_5 = embedding[0] * 1 + embedding[1] * 6 + embedding[2] * 2 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -6 + embedding[6] * -11 + embedding[7] * 26 + embedding[8] * 24 + embedding[9] * 7;
    let score_6 = embedding[0] * 9 + embedding[1] * 6 + embedding[2] * 5 + embedding[3] * -1 + embedding[4] * 11 + embedding[5] * 4 + embedding[6] * 18 + embedding[7] * -3 + embedding[8] * -13 + embedding[9] * 3;
    let score_7 = embedding[0] * -10 + embedding[1] * 10 + embedding[2] * -1 + embedding[3] * -7 + embedding[4] * -30 + embedding[5] * -7 + embedding[6] * -13 + embedding[7] * 32 + embedding[8] * 12 + embedding[9] * 17;
    let score_8 = embedding[0] * -11 + embedding[1] * 7 + embedding[2] * -12 + embedding[3] * 12 + embedding[4] * -33 + embedding[5] * -4 + embedding[6] * -1 + embedding[7] * 8 + embedding[8] * 10 + embedding[9] * 8;
    let score_9 = embedding[0] * -14 + embedding[1] * 13 + embedding[2] * 26 + embedding[3] * -6 + embedding[4] * -5 + embedding[5] * -3 + embedding[6] * 6 + embedding[8] * 8 + embedding[9] * -5;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let score_17 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 9;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 37;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 78;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 20;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 15;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 7;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 29;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 35;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 52;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_449(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 10;
    let score_1 = embedding[0] * -1 + embedding[1] * 3 + embedding[2] * 9 + embedding[3] * -7 + embedding[4] * 22 + embedding[5] * 8 + embedding[6] * 2 + embedding[7] * -4 + embedding[8] * 4 + embedding[9] * -11;
    let score_2 = embedding[0] * 9 + embedding[1] * -8 + embedding[2] * 2 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 15 + embedding[6] * -5 + embedding[7] * -1 + embedding[8] * 1 + embedding[9] * -1;
    let score_3 = embedding[0] * 2 + embedding[1] * -2 + embedding[3] * -7 + embedding[4] * 16 + embedding[5] * 9 + embedding[7] * -5 + embedding[8] * 3 + embedding[9] * -9;
    let score_4 = embedding[0] * -8 + embedding[1] * 10 + embedding[2] * -8 + embedding[3] * -11 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * -12 + embedding[7] * 21 + embedding[8] * 10 + embedding[9] * 13;
    let score_5 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * -19 + embedding[3] * 12 + embedding[4] * -30 + embedding[5] * -6 + embedding[6] * -1 + embedding[7] * 1 + embedding[8] * 9 + embedding[9] * 7;
    let score_6 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 10;
    let score_7 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 10;
    let score_8 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 10;
    let score_9 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 10;
    let score_10 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 10;
    let score_11 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 10;
    let score_12 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 10;
    let score_13 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 10;
    let score_14 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 10;
    let score_15 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 10;
    let score_16 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 10;
    let score_17 = embedding[0] * 9 + embedding[1] * -8 + embedding[2] * -18 + embedding[3] * -8 + embedding[4] * 9 + embedding[5] * 5 + embedding[6] * -12 + embedding[7] * -13 + embedding[8] * 6 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 37;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 78;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 29;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 35;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_450(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_1 = embedding[0] * 1 + embedding[1] * 5 + embedding[2] * 6 + embedding[3] * -9 + embedding[4] * 23 + embedding[5] * 9 + embedding[7] * -5 + embedding[8] * 5 + embedding[9] * -10;
    let score_2 = embedding[0] * 2 + embedding[1] * -13 + embedding[2] * 8 + embedding[3] * -13 + embedding[4] * 6 + embedding[5] * 10 + embedding[6] * -11 + embedding[7] * 14 + embedding[8] * 1 + embedding[9] * -2;
    let score_3 = embedding[0] * 3 + embedding[2] * -3 + embedding[3] * -9 + embedding[4] * 16 + embedding[5] * 9 + embedding[6] * -2 + embedding[7] * -6 + embedding[8] * 4 + embedding[9] * -8;
    let score_4 = embedding[0] * -2 + embedding[1] * 7 + embedding[2] * -23 + embedding[3] * 13 + embedding[4] * -27 + embedding[5] * -5 + embedding[7] * -3 + embedding[8] * 8 + embedding[9] * 6;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_17 = embedding[0] * 11 + embedding[1] * -6 + embedding[2] * -21 + embedding[3] * -10 + embedding[4] * 9 + embedding[5] * 7 + embedding[6] * -14 + embedding[7] * -14 + embedding[8] * 7 + embedding[9] * -4;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 37;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 78;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 35;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_451(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_1 = embedding[1] * 6 + embedding[2] * 3 + embedding[3] * -8 + embedding[4] * 22 + embedding[5] * 9 + embedding[6] * 1 + embedding[7] * -6 + embedding[8] * 6 + embedding[9] * -10;
    let score_2 = embedding[0] * 2 + embedding[1] * 1 + embedding[2] * -7 + embedding[3] * -9 + embedding[4] * 15 + embedding[5] * 9 + embedding[6] * -2 + embedding[7] * -7 + embedding[8] * 5 + embedding[9] * -7;
    let score_3 = embedding[0] * 4 + embedding[1] * -9 + embedding[2] * 7 + embedding[3] * -7 + embedding[4] * 5 + embedding[5] * 11 + embedding[6] * -9 + embedding[7] * 10 + embedding[8] * 8 + embedding[9] * -3;
    let score_4 = embedding[0] * 9 + embedding[1] * -16 + embedding[2] * 20 + embedding[3] * -6 + embedding[4] * -10 + embedding[5] * 9 + embedding[6] * -2 + embedding[7] * 13 + embedding[8] * -8 + embedding[9] * 4;
    let score_5 = embedding[0] * -4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_6 = embedding[0] * -4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_7 = embedding[0] * -4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_8 = embedding[0] * -4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_9 = embedding[0] * -4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_10 = embedding[0] * -4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_11 = embedding[0] * -4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_12 = embedding[0] * -4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_13 = embedding[0] * -4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_14 = embedding[0] * -4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_15 = embedding[0] * -4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_16 = embedding[0] * -3 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -12 + embedding[9] * 9;
    let score_17 = embedding[0] * 9 + embedding[1] * -4 + embedding[2] * -25 + embedding[3] * -10 + embedding[4] * 8 + embedding[5] * 5 + embedding[6] * -13 + embedding[7] * -15 + embedding[8] * 7 + embedding[9] * -3;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 87;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 130;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 9;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_452(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_1 = embedding[0] * 3 + embedding[2] * -2 + embedding[3] * -14 + embedding[4] * 25 + embedding[5] * 13 + embedding[6] * -6 + embedding[7] * -7 + embedding[8] * 7 + embedding[9] * -11;
    let score_2 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_3 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_4 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_5 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_6 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_7 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_8 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_9 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_10 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_11 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_12 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -5 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -7 + embedding[2] * -22 + embedding[3] * -15 + embedding[4] * 15 + embedding[5] * 10 + embedding[6] * -18 + embedding[7] * -14 + embedding[8] * 9 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_453(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -21 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_454(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_455(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -24 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_456(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 6 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -21 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_457(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_458(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_459(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_1 = embedding[0] * -1 + embedding[1] * -19 + embedding[2] * 5 + embedding[3] * -11 + embedding[4] * 11 + embedding[5] * 13 + embedding[6] * -3 + embedding[7] * -3 + embedding[8] * -7 + embedding[9] * -7;
    let score_2 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_3 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_4 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_5 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_6 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_7 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_8 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_9 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_10 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_11 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_12 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_13 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_14 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_15 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_16 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_17 = embedding[0] * 9 + embedding[1] * 4 + embedding[2] * -18 + embedding[3] * -16 + embedding[4] * 21 + embedding[5] * 10 + embedding[6] * -15 + embedding[7] * -17 + embedding[8] * 15 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_460(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_1 = embedding[0] * 1 + embedding[1] * -17 + embedding[2] * 68 + embedding[3] * -7 + embedding[4] * -3 + embedding[5] * 15 + embedding[6] * 11 + embedding[7] * 31 + embedding[8] * -24 + embedding[9] * 1;
    let score_2 = embedding[0] * -2 + embedding[1] * -13 + embedding[3] * -11 + embedding[4] * 9 + embedding[5] * 15 + embedding[6] * -4 + embedding[7] * -7 + embedding[8] * 1 + embedding[9] * -8;
    let score_3 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_5 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_7 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_8 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_12 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 8;
    let score_14 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -9 + embedding[1] * 7 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 7 + embedding[1] * 7 + embedding[2] * -31 + embedding[3] * -15 + embedding[4] * 17 + embedding[5] * 6 + embedding[6] * -17 + embedding[7] * -25 + embedding[8] * 19 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 9;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_461(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_1 = embedding[0] * 6 + embedding[1] * -9 + embedding[2] * 1 + embedding[3] * -15 + embedding[4] * 7 + embedding[5] * 19 + embedding[6] * -3 + embedding[7] * -10 + embedding[8] * -6 + embedding[9] * -3;
    let score_2 = embedding[0] * 3 + embedding[1] * 8 + embedding[2] * 7 + embedding[3] * 26 + embedding[4] * -4 + embedding[5] * -11 + embedding[6] * -5 + embedding[7] * 26 + embedding[8] * 31 + embedding[9] * -1;
    let score_3 = embedding[0] * -10 + embedding[1] * -7 + embedding[2] * 6 + embedding[3] * 13 + embedding[4] * -10 + embedding[5] * 10 + embedding[6] * 1 + embedding[7] * 20 + embedding[8] * 19 + embedding[9] * -3;
    let score_4 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_5 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_6 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_7 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_8 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_9 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_10 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_11 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_12 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_13 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_14 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_15 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_16 = embedding[0] * -8 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -5 + embedding[9] * 8;
    let score_17 = embedding[0] * 7 + embedding[1] * -2 + embedding[2] * -16 + embedding[3] * -16 + embedding[4] * 17 + embedding[5] * 6 + embedding[6] * -14 + embedding[7] * -17 + embedding[8] * 1 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 15;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_462(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 26 + embedding[1] * -13 + embedding[2] * 3 + embedding[3] * -15 + embedding[4] * 28 + embedding[5] * 12 + embedding[6] * -3 + embedding[7] * -33 + embedding[8] * -24 + embedding[9] * -9;
    let score_1 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_2 = embedding[0] * 11 + embedding[1] * 2 + embedding[2] * 20 + embedding[3] * -9 + embedding[4] * -38 + embedding[5] * 22 + embedding[6] * -11 + embedding[7] * 14 + embedding[8] * -3 + embedding[9] * 16;
    let score_3 = embedding[0] * -26 + embedding[1] * 13 + embedding[3] * 26 + embedding[4] * -25 + embedding[5] * -9 + embedding[6] * -10 + embedding[7] * 59 + embedding[8] * 48 + embedding[9] * 4;
    let score_4 = embedding[0] * -35 + embedding[1] * -7 + embedding[2] * -10 + embedding[3] * 26 + embedding[4] * -22 + embedding[5] * 6 + embedding[7] * 43 + embedding[8] * 32 + embedding[9] * -7;
    let score_5 = embedding[0] * 27 + embedding[1] * 1 + embedding[2] * 20 + embedding[3] * -9 + embedding[4] * 22 + embedding[5] * 11 + embedding[6] * 30 + embedding[7] * -25 + embedding[8] * -40 + embedding[9] * 1;
    let score_6 = embedding[0] * -26 + embedding[1] * 27 + embedding[2] * -9 + embedding[3] * -4 + embedding[4] * -35 + embedding[5] * -6 + embedding[6] * -18 + embedding[7] * 57 + embedding[8] * 27 + embedding[9] * 13;
    let score_7 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[0] * -15 + embedding[1] * -5 + embedding[2] * -33 + embedding[3] * -4 + embedding[4] * 8 + embedding[5] * 3 + embedding[6] * -11 + embedding[8] * 10 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 20;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 29;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_463(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 13 + embedding[3] * 13 + embedding[4] * -17 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 7;
    let score_1 = embedding[0] * 29 + embedding[1] * 2 + embedding[3] * -22 + embedding[4] * -3 + embedding[5] * -2 + embedding[6] * 4 + embedding[7] * -9 + embedding[8] * -38 + embedding[9] * -28;
    let score_2 = embedding[0] * 31 + embedding[1] * -14 + embedding[2] * 13 + embedding[3] * -23 + embedding[4] * 17 + embedding[5] * -13 + embedding[6] * 32 + embedding[7] * -18 + embedding[8] * -5 + embedding[9] * 10;
    let score_3 = embedding[0] * 10 + embedding[1] * -8 + embedding[2] * 9 + embedding[3] * 22 + embedding[4] * 15 + embedding[5] * 9 + embedding[6] * -14 + embedding[7] * -24 + embedding[8] * -37 + embedding[9] * 28;
    let score_4 = embedding[0] * -3 + embedding[1] * -12 + embedding[2] * -4 + embedding[3] * -7 + embedding[4] * 22 + embedding[5] * 23 + embedding[6] * -3 + embedding[7] * -15 + embedding[8] * -13 + embedding[9] * -11;
    let score_5 = embedding[0] * 20 + embedding[1] * -9 + embedding[2] * 31 + embedding[3] * -11 + embedding[4] * 8 + embedding[5] * -24 + embedding[6] * -13 + embedding[7] * -24 + embedding[8] * -17 + embedding[9] * -9;
    let score_6 = embedding[0] * 1 + embedding[1] * 8 + embedding[2] * 25 + embedding[3] * -6 + embedding[4] * -47 + embedding[5] * 30 + embedding[6] * -14 + embedding[7] * 23 + embedding[9] * 22;
    let score_7 = embedding[0] * -49 + embedding[1] * 19 + embedding[2] * -7 + embedding[3] * 26 + embedding[4] * -30 + embedding[5] * -3 + embedding[6] * -13 + embedding[7] * 68 + embedding[8] * 60 + embedding[9] * 2;
    let score_8 = embedding[0] * 31 + embedding[1] * 10 + embedding[2] * 26 + embedding[3] * -12 + embedding[4] * 17 + embedding[5] * 2 + embedding[6] * 40 + embedding[7] * -16 + embedding[8] * -44 + embedding[9] * 8;
    let score_9 = embedding[0] * -59 + embedding[1] * -5 + embedding[2] * -20 + embedding[3] * 30 + embedding[4] * -28 + embedding[5] * 13 + embedding[6] * -3 + embedding[7] * 56 + embedding[8] * 42 + embedding[9] * -8;
    let score_10 = embedding[0] * -45 + embedding[1] * 37 + embedding[2] * -16 + embedding[3] * -1 + embedding[4] * -41 + embedding[5] * -2 + embedding[6] * -22 + embedding[7] * 66 + embedding[8] * 40 + embedding[9] * 15;
    let score_11 = embedding[0] * 23 + embedding[1] * -16 + embedding[2] * 10 + embedding[3] * 9 + embedding[4] * 16 + embedding[5] * -9 + embedding[6] * -1 + embedding[7] * -1 + embedding[8] * -15 + embedding[9] * 38;
    let score_12 = embedding[0] * -52 + embedding[1] * 31 + embedding[2] * 28 + embedding[3] * -1 + embedding[4] * -17 + embedding[5] * 4 + embedding[6] * 7 + embedding[7] * 12 + embedding[8] * 24 + embedding[9] * -8;
    let score_13 = embedding[0] * 6 + embedding[2] * -5 + embedding[3] * 15 + embedding[4] * -14 + embedding[5] * 7 + embedding[6] * -25 + embedding[7] * -9 + embedding[8] * -22 + embedding[9] * -3;
    let score_14 = embedding[0] * 22 + embedding[1] * -10 + embedding[2] * 2 + embedding[3] * -5 + embedding[4] * -11 + embedding[5] * -6 + embedding[6] * 8 + embedding[7] * -20 + embedding[8] * -7 + embedding[9] * -4;
    let score_15 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 13 + embedding[3] * 13 + embedding[4] * -17 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 7;
    let score_16 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 13 + embedding[3] * 13 + embedding[4] * -17 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 7;
    let score_17 = embedding[0] * -37 + embedding[1] * -2 + embedding[2] * -43 + embedding[3] * 3 + embedding[4] * 2 + embedding[5] * 8 + embedding[6] * -15 + embedding[7] * 12 + embedding[8] * 21 + embedding[9] * -14;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 20;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 15;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 7;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 6;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 29;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 52;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 100;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 93;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_464(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -17 + embedding[1] * -7 + embedding[2] * -22 + embedding[3] * -8 + embedding[4] * 18 + embedding[5] * 23 + embedding[6] * -8 + embedding[7] * -5 + embedding[8] * 15 + embedding[9] * -15;
    let score_1 = embedding[0] * 20 + embedding[1] * 6 + embedding[2] * -11 + embedding[3] * -31 + embedding[4] * -9 + embedding[5] * -7 + embedding[6] * 17 + embedding[7] * 5 + embedding[8] * -5 + embedding[9] * -31;
    let score_2 = embedding[0] * 21 + embedding[1] * -4 + embedding[2] * -5 + embedding[3] * -29 + embedding[4] * 2 + embedding[5] * -12 + embedding[6] * 32 + embedding[7] * -1 + embedding[8] * 12 + embedding[9] * -8;
    let score_3 = embedding[0] * -3 + embedding[1] * -4 + embedding[2] * 2 + embedding[3] * 27 + embedding[4] * 16 + embedding[5] * 8 + embedding[6] * -8 + embedding[7] * -13 + embedding[8] * -6 + embedding[9] * 39;
    let score_4 = embedding[0] * 22 + embedding[1] * 5 + embedding[2] * -5 + embedding[3] * -9 + embedding[4] * 11 + embedding[6] * 10 + embedding[7] * -14 + embedding[8] * -19 + embedding[9] * 7;
    let score_5 = embedding[0] * 10 + embedding[1] * -3 + embedding[2] * 14 + embedding[3] * -18 + embedding[4] * -1 + embedding[5] * -22 + embedding[6] * -1 + embedding[7] * -6 + embedding[8] * 8 + embedding[9] * -22;
    let score_6 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 14 + embedding[3] * -6 + embedding[4] * -42 + embedding[5] * 32 + embedding[6] * -9 + embedding[7] * 23 + embedding[8] * 13 + embedding[9] * 13;
    let score_7 = embedding[0] * -18 + embedding[1] * 13 + embedding[2] * 15 + embedding[3] * 49 + embedding[4] * -14 + embedding[5] * 6 + embedding[6] * -37 + embedding[7] * -7 + embedding[8] * -43 + embedding[9] * 12;
    let score_8 = embedding[0] * -6 + embedding[1] * 17 + embedding[2] * 8 + embedding[3] * 20 + embedding[4] * -28 + embedding[5] * -5 + embedding[6] * -27 + embedding[7] * 4 + embedding[8] * -50 + embedding[9] * -24;
    let score_9 = embedding[0] * -2 + embedding[1] * -17 + embedding[2] * 21 + embedding[3] * 15 + embedding[4] * 14 + embedding[5] * -16 + embedding[6] * 24 + embedding[7] * -5 + embedding[8] * 27 + embedding[9] * 47;
    let score_10 = embedding[0] * -26 + embedding[1] * 21 + embedding[2] * -7 + embedding[3] * -17 + embedding[4] * -21 + embedding[5] * 13 + embedding[6] * -10 + embedding[7] * 29 + embedding[8] * 17 + embedding[9] * 12;
    let score_11 = embedding[0] * 6 + embedding[1] * -13 + embedding[2] * 7 + embedding[3] * 24 + embedding[4] * 14 + embedding[5] * -14 + embedding[7] * 11 + embedding[8] * 13 + embedding[9] * 56;
    let score_12 = embedding[0] * -16 + embedding[1] * 6 + embedding[2] * 27 + embedding[3] * 23 + embedding[4] * -32 + embedding[5] * -19 + embedding[6] * -37 + embedding[7] * -3 + embedding[8] * -31 + embedding[9] * -24;
    let score_13 = embedding[0] * -5 + embedding[1] * -20 + embedding[2] * 36 + embedding[3] * 45 + embedding[5] * -41 + embedding[6] * -13 + embedding[7] * 6 + embedding[8] * 22 + embedding[9] * 55;
    let score_14 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -11 + embedding[3] * -8 + embedding[4] * -16 + embedding[5] * -6 + embedding[6] * 12 + embedding[7] * -9 + embedding[8] * 9 + embedding[9] * -9;
    let score_15 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 20;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 32;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 33;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 38;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 29;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 79;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 123;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 93;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_465(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 13 + embedding[1] * -11 + embedding[2] * -10 + embedding[3] * -35 + embedding[4] * 2 + embedding[5] * -7 + embedding[6] * 34 + embedding[7] * 3 + embedding[8] * 30 + embedding[9] * -13;
    let score_1 = embedding[0] * 12 + embedding[1] * 5 + embedding[2] * -16 + embedding[3] * -36 + embedding[4] * -15 + embedding[5] * -2 + embedding[6] * 11 + embedding[7] * 11 + embedding[8] * 1 + embedding[9] * -46;
    let score_2 = embedding[0] * -15 + embedding[1] * -7 + embedding[3] * 36 + embedding[4] * 21 + embedding[5] * 21 + embedding[6] * -19 + embedding[7] * -14 + embedding[8] * 2 + embedding[9] * 44;
    let score_3 = embedding[0] * 5 + embedding[1] * -5 + embedding[2] * 13 + embedding[3] * -20 + embedding[4] * -3 + embedding[5] * -18 + embedding[6] * -6 + embedding[7] * -6 + embedding[8] * 18 + embedding[9] * -28;
    let score_4 = embedding[0] * 18 + embedding[1] * 5 + embedding[2] * -8 + embedding[3] * -9 + embedding[4] * 15 + embedding[5] * 9 + embedding[6] * 6 + embedding[7] * -16 + embedding[8] * -17 + embedding[9] * 7;
    let score_5 = embedding[0] * 2 + embedding[1] * 3 + embedding[2] * -10 + embedding[3] * -8 + embedding[4] * 21 + embedding[5] * 16 + embedding[6] * 1 + embedding[7] * -12 + embedding[8] * -7 + embedding[9] * -2;
    let score_6 = embedding[0] * 4 + embedding[1] * 8 + embedding[2] * -31 + embedding[3] * -11 + embedding[4] * 9 + embedding[5] * 22 + embedding[6] * 14 + embedding[7] * 28 + embedding[8] * -3 + embedding[9] * 8;
    let score_7 = embedding[0] * -21 + embedding[1] * 16 + embedding[2] * 18 + embedding[3] * 61 + embedding[4] * -17 + embedding[5] * 8 + embedding[6] * -47 + embedding[7] * -8 + embedding[8] * -49 + embedding[9] * 15;
    let score_8 = embedding[0] * -7 + embedding[1] * 20 + embedding[2] * 10 + embedding[3] * 26 + embedding[4] * -37 + embedding[5] * -3 + embedding[6] * -38 + embedding[7] * 4 + embedding[8] * -58 + embedding[9] * -27;
    let score_9 = embedding[0] * -5 + embedding[1] * -19 + embedding[2] * 26 + embedding[3] * 18 + embedding[4] * 16 + embedding[5] * -14 + embedding[6] * 24 + embedding[7] * -7 + embedding[8] * 31 + embedding[9] * 54;
    let score_10 = embedding[0] * 3 + embedding[1] * -16 + embedding[2] * 11 + embedding[3] * 30 + embedding[4] * 18 + embedding[5] * -12 + embedding[6] * -5 + embedding[7] * 7 + embedding[8] * 16 + embedding[9] * 61;
    let score_11 = embedding[0] * -11 + embedding[1] * 10 + embedding[2] * 32 + embedding[3] * 29 + embedding[4] * -39 + embedding[5] * -19 + embedding[6] * -43 + embedding[7] * -4 + embedding[8] * -39 + embedding[9] * -24;
    let score_12 = embedding[1] * 18 + embedding[2] * -5 + embedding[3] * 35 + embedding[4] * -29 + embedding[5] * 4 + embedding[6] * -37 + embedding[7] * 2 + embedding[8] * -33 + embedding[9] * -1;
    let score_13 = embedding[0] * -8 + embedding[1] * -18 + embedding[2] * 41 + embedding[3] * 45 + embedding[4] * 5 + embedding[5] * -39 + embedding[6] * -15 + embedding[7] * 1 + embedding[8] * 22 + embedding[9] * 60;
    let score_14 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -4 + embedding[3] * -12 + embedding[4] * -17 + embedding[5] * -6 + embedding[6] * 16 + embedding[7] * -12 + embedding[8] * 10 + embedding[9] * -13;
    let score_15 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 7;
    let score_16 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 7;
    let score_17 = embedding[0] * -3 + embedding[1] * 7 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 26;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 32;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 33;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 38;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 23;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 79;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 80;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 123;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 93;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_466(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[1] * -13 + embedding[2] * -10 + embedding[3] * -18 + embedding[4] * 6 + embedding[6] * 16 + embedding[8] * 26 + embedding[9] * 1;
    let score_1 = embedding[0] * 2 + embedding[1] * 2 + embedding[2] * -16 + embedding[3] * -16 + embedding[4] * -7 + embedding[5] * 7 + embedding[6] * -3 + embedding[7] * 3 + embedding[8] * 1 + embedding[9] * -23;
    let score_2 = embedding[0] * -10 + embedding[1] * -5 + embedding[2] * -10 + embedding[3] * 9 + embedding[4] * 12 + embedding[5] * 16 + embedding[6] * -14 + embedding[7] * -7 + embedding[8] * 4 + embedding[9] * 11;
    let score_3 = embedding[0] * 18 + embedding[1] * 10 + embedding[2] * 7 + embedding[3] * -9 + embedding[4] * 14 + embedding[5] * -2 + embedding[6] * 14 + embedding[7] * -13 + embedding[8] * -18 + embedding[9] * 5;
    let score_4 = embedding[0] * -4 + embedding[1] * -9 + embedding[2] * 6 + embedding[3] * -3 + embedding[4] * 3 + embedding[5] * -8 + embedding[6] * -14 + embedding[7] * -7 + embedding[8] * 15 + embedding[9] * -10;
    let score_5 = embedding[0] * 1 + embedding[1] * 1 + embedding[2] * -10 + embedding[3] * -8 + embedding[4] * 15 + embedding[5] * 11 + embedding[6] * -1 + embedding[7] * -10 + embedding[9] * -5;
    let score_6 = embedding[0] * 26 + embedding[1] * 15 + embedding[2] * 22 + embedding[3] * -6 + embedding[4] * 10 + embedding[5] * -15 + embedding[6] * 22 + embedding[7] * -14 + embedding[8] * -27 + embedding[9] * 8;
    let score_7 = embedding[0] * 1 + embedding[1] * 5 + embedding[2] * -25 + embedding[3] * -8 + embedding[4] * 6 + embedding[5] * 17 + embedding[6] * 10 + embedding[7] * 21 + embedding[8] * 1 + embedding[9] * 6;
    let score_8 = embedding[0] * -17 + embedding[1] * 11 + embedding[2] * 10 + embedding[3] * 39 + embedding[4] * -14 + embedding[5] * 6 + embedding[6] * -34 + embedding[7] * -3 + embedding[8] * -33 + embedding[9] * -2;
    let score_9 = embedding[0] * -9 + embedding[1] * 12 + embedding[2] * 5 + embedding[3] * 20 + embedding[4] * -23 + embedding[6] * -30 + embedding[7] * 1 + embedding[8] * -36 + embedding[9] * -20;
    let score_10 = embedding[0] * -5 + embedding[1] * -15 + embedding[2] * 17 + embedding[3] * 4 + embedding[4] * 10 + embedding[5] * -11 + embedding[6] * 18 + embedding[7] * -4 + embedding[8] * 26 + embedding[9] * 26;
    let score_11 = embedding[0] * -1 + embedding[1] * -14 + embedding[2] * 2 + embedding[3] * 12 + embedding[4] * 10 + embedding[5] * -8 + embedding[6] * -1 + embedding[7] * 9 + embedding[8] * 17 + embedding[9] * 30;
    let score_12 = embedding[0] * -14 + embedding[1] * 2 + embedding[2] * 26 + embedding[3] * 27 + embedding[4] * -28 + embedding[5] * -15 + embedding[6] * -37 + embedding[7] * -4 + embedding[8] * -23 + embedding[9] * -16;
    let score_13 = embedding[0] * -3 + embedding[1] * 11 + embedding[2] * -7 + embedding[3] * 22 + embedding[4] * -24 + embedding[5] * 2 + embedding[6] * -28 + embedding[7] * 5 + embedding[8] * -14 + embedding[9] * -7;
    let score_14 = embedding[0] * -8 + embedding[1] * -16 + embedding[2] * 35 + embedding[3] * 30 + embedding[4] * -4 + embedding[5] * -37 + embedding[6] * -11 + embedding[7] * 4 + embedding[8] * 21 + embedding[9] * 34;
    let score_15 = embedding[0] * 4 + embedding[1] * -5 + embedding[2] * -4 + embedding[4] * -17 + embedding[5] * -2 + embedding[6] * 4 + embedding[7] * -10 + embedding[8] * 10 + embedding[9] * -3;
    let score_16 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 16 + embedding[4] * -15 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 6;
    let score_17 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 14 + embedding[3] * 16 + embedding[4] * -15 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -13 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 39;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 26;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 38;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 79;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 80;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 123;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_467(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 7 + embedding[1] * -18 + embedding[2] * -10 + embedding[3] * -23 + embedding[4] * 6 + embedding[5] * -6 + embedding[6] * 28 + embedding[7] * 2 + embedding[8] * 36 + embedding[9] * 1;
    let score_1 = embedding[0] * 4 + embedding[1] * 5 + embedding[2] * -20 + embedding[3] * -22 + embedding[4] * -15 + embedding[5] * 5 + embedding[6] * -2 + embedding[7] * 8 + embedding[8] * -2 + embedding[9] * -36;
    let score_2 = embedding[0] * -16 + embedding[1] * -5 + embedding[2] * -9 + embedding[3] * 22 + embedding[4] * 14 + embedding[5] * 21 + embedding[6] * -17 + embedding[7] * -9 + embedding[8] * 2 + embedding[9] * 22;
    let score_3 = embedding[0] * -4 + embedding[1] * -9 + embedding[2] * 9 + embedding[3] * -7 + embedding[5] * -13 + embedding[6] * -15 + embedding[7] * -9 + embedding[8] * 23 + embedding[9] * -13;
    let score_4 = embedding[0] * 20 + embedding[1] * 11 + embedding[2] * 7 + embedding[3] * -10 + embedding[4] * 16 + embedding[5] * -2 + embedding[6] * 19 + embedding[7] * -14 + embedding[8] * -21 + embedding[9] * 9;
    let score_5 = embedding[0] * -2 + embedding[1] * 1 + embedding[2] * -10 + embedding[3] * -7 + embedding[4] * 20 + embedding[5] * 15 + embedding[6] * 3 + embedding[7] * -10 + embedding[8] * -2 + embedding[9] * -4;
    let score_6 = embedding[0] * 28 + embedding[1] * 15 + embedding[2] * 21 + embedding[3] * -7 + embedding[4] * 12 + embedding[5] * -14 + embedding[6] * 25 + embedding[7] * -14 + embedding[8] * -29 + embedding[9] * 11;
    let score_7 = embedding[0] * 1 + embedding[1] * 6 + embedding[2] * -30 + embedding[3] * -10 + embedding[4] * 7 + embedding[5] * 20 + embedding[6] * 12 + embedding[7] * 26 + embedding[8] * 2 + embedding[9] * 6;
    let score_8 = embedding[0] * -23 + embedding[1] * 16 + embedding[2] * 15 + embedding[3] * 54 + embedding[4] * -21 + embedding[5] * 6 + embedding[6] * -46 + embedding[7] * -4 + embedding[8] * -45 + embedding[9] * 2;
    let score_9 = embedding[0] * -13 + embedding[1] * 17 + embedding[2] * 9 + embedding[3] * 29 + embedding[4] * -39 + embedding[5] * -3 + embedding[6] * -41 + embedding[7] * 5 + embedding[8] * -52 + embedding[9] * -27;
    let score_10 = embedding[0] * -4 + embedding[1] * -21 + embedding[2] * 21 + embedding[3] * 5 + embedding[4] * 11 + embedding[5] * -16 + embedding[6] * 29 + embedding[7] * -3 + embedding[8] * 37 + embedding[9] * 35;
    let score_11 = embedding[0] * -2 + embedding[1] * -19 + embedding[2] * 6 + embedding[3] * 18 + embedding[4] * 15 + embedding[5] * -8 + embedding[6] * 1 + embedding[7] * 10 + embedding[8] * 23 + embedding[9] * 42;
    let score_12 = embedding[0] * -18 + embedding[1] * 8 + embedding[2] * 32 + embedding[3] * 34 + embedding[4] * -38 + embedding[5] * -19 + embedding[6] * -47 + embedding[7] * -3 + embedding[8] * -35 + embedding[9] * -20;
    let score_13 = embedding[0] * -7 + embedding[1] * 17 + embedding[2] * -6 + embedding[3] * 34 + embedding[4] * -34 + embedding[5] * 3 + embedding[6] * -40 + embedding[7] * 7 + embedding[8] * -27 + embedding[9] * -7;
    let score_14 = embedding[0] * -11 + embedding[1] * -19 + embedding[2] * 38 + embedding[3] * 33 + embedding[4] * -1 + embedding[5] * -42 + embedding[6] * -11 + embedding[7] * 3 + embedding[8] * 28 + embedding[9] * 42;
    let score_15 = embedding[0] * 5 + embedding[1] * -6 + embedding[2] * -4 + embedding[3] * -4 + embedding[4] * -18 + embedding[5] * -5 + embedding[6] * 9 + embedding[7] * -12 + embedding[8] * 16 + embedding[9] * -5;
    let score_16 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 39;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 26;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 38;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 113;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 79;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 80;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 194;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_468(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 8 + embedding[1] * -17 + embedding[2] * -11 + embedding[3] * -23 + embedding[4] * 6 + embedding[5] * -5 + embedding[6] * 28 + embedding[7] * 1 + embedding[8] * 36;
    let score_1 = embedding[0] * 4 + embedding[1] * 5 + embedding[2] * -20 + embedding[3] * -22 + embedding[4] * -15 + embedding[5] * 5 + embedding[6] * -2 + embedding[7] * 8 + embedding[8] * -2 + embedding[9] * -36;
    let score_2 = embedding[0] * -15 + embedding[1] * -5 + embedding[2] * -9 + embedding[3] * 21 + embedding[4] * 14 + embedding[5] * 21 + embedding[6] * -17 + embedding[7] * -9 + embedding[8] * 2 + embedding[9] * 22;
    let score_3 = embedding[0] * -4 + embedding[1] * -9 + embedding[2] * 9 + embedding[3] * -7 + embedding[5] * -13 + embedding[6] * -15 + embedding[7] * -9 + embedding[8] * 22 + embedding[9] * -14;
    let score_4 = embedding[0] * 21 + embedding[1] * 11 + embedding[2] * 7 + embedding[3] * -10 + embedding[4] * 16 + embedding[5] * -2 + embedding[6] * 19 + embedding[7] * -14 + embedding[8] * -21 + embedding[9] * 9;
    let score_5 = embedding[0] * -2 + embedding[1] * 1 + embedding[2] * -10 + embedding[3] * -7 + embedding[4] * 20 + embedding[5] * 15 + embedding[6] * 3 + embedding[7] * -11 + embedding[8] * -2 + embedding[9] * -5;
    let score_6 = embedding[0] * 29 + embedding[1] * 15 + embedding[2] * 21 + embedding[3] * -7 + embedding[4] * 12 + embedding[5] * -14 + embedding[6] * 25 + embedding[7] * -14 + embedding[8] * -29 + embedding[9] * 11;
    let score_7 = embedding[0] * 2 + embedding[1] * 6 + embedding[2] * -30 + embedding[3] * -10 + embedding[4] * 7 + embedding[5] * 21 + embedding[6] * 12 + embedding[7] * 26 + embedding[8] * 2 + embedding[9] * 6;
    let score_8 = embedding[0] * -23 + embedding[1] * 16 + embedding[2] * 15 + embedding[3] * 53 + embedding[4] * -21 + embedding[5] * 7 + embedding[6] * -46 + embedding[7] * -4 + embedding[8] * -45 + embedding[9] * 1;
    let score_9 = embedding[0] * -12 + embedding[1] * 17 + embedding[2] * 9 + embedding[3] * 29 + embedding[4] * -39 + embedding[5] * -3 + embedding[6] * -41 + embedding[7] * 5 + embedding[8] * -53 + embedding[9] * -27;
    let score_10 = embedding[0] * -4 + embedding[1] * -20 + embedding[2] * 21 + embedding[3] * 5 + embedding[4] * 11 + embedding[5] * -16 + embedding[6] * 29 + embedding[7] * -3 + embedding[8] * 37 + embedding[9] * 35;
    let score_11 = embedding[0] * -2 + embedding[1] * -19 + embedding[2] * 6 + embedding[3] * 18 + embedding[4] * 15 + embedding[5] * -8 + embedding[6] * 1 + embedding[7] * 10 + embedding[8] * 23 + embedding[9] * 41;
    let score_12 = embedding[0] * -17 + embedding[1] * 8 + embedding[2] * 32 + embedding[3] * 34 + embedding[4] * -38 + embedding[5] * -19 + embedding[6] * -47 + embedding[7] * -3 + embedding[8] * -35 + embedding[9] * -21;
    let score_13 = embedding[0] * -7 + embedding[1] * 17 + embedding[2] * -6 + embedding[3] * 34 + embedding[4] * -34 + embedding[5] * 3 + embedding[6] * -40 + embedding[7] * 6 + embedding[8] * -28 + embedding[9] * -8;
    let score_14 = embedding[0] * -11 + embedding[1] * -19 + embedding[2] * 38 + embedding[3] * 33 + embedding[4] * -1 + embedding[5] * -42 + embedding[6] * -11 + embedding[7] * 3 + embedding[8] * 27 + embedding[9] * 42;
    let score_15 = embedding[0] * 6 + embedding[1] * -5 + embedding[2] * -5 + embedding[3] * -4 + embedding[4] * -18 + embedding[5] * -5 + embedding[6] * 9 + embedding[7] * -12 + embedding[8] * 15 + embedding[9] * -6;
    let score_16 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -8 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 39;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 26;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 38;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 79;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 80;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 123;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_469(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 6 + embedding[1] * -11 + embedding[2] * -11 + embedding[3] * -18 + embedding[4] * 7 + embedding[5] * -3 + embedding[6] * 18 + embedding[8] * 25 + embedding[9] * -2;
    let score_1 = embedding[0] * 3 + embedding[1] * 2 + embedding[2] * -16 + embedding[3] * -18 + embedding[4] * -6 + embedding[5] * 5 + embedding[6] * -1 + embedding[7] * 4 + embedding[8] * 1 + embedding[9] * -26;
    let score_2 = embedding[0] * -10 + embedding[1] * -5 + embedding[2] * -8 + embedding[3] * 13 + embedding[4] * 11 + embedding[5] * 16 + embedding[6] * -15 + embedding[7] * -8 + embedding[8] * 4 + embedding[9] * 17;
    let score_3 = embedding[0] * -3 + embedding[1] * -2 + embedding[2] * -8 + embedding[3] * -10 + embedding[4] * 10 + embedding[5] * 15 + embedding[6] * 4 + embedding[7] * -15 + embedding[8] * 1 + embedding[9] * -6;
    let score_4 = embedding[0] * -3 + embedding[1] * -8 + embedding[2] * 6 + embedding[3] * -7 + embedding[4] * 2 + embedding[5] * -10 + embedding[6] * -14 + embedding[7] * -8 + embedding[8] * 17 + embedding[9] * -12;
    let score_5 = embedding[0] * 18 + embedding[1] * 9 + embedding[2] * 6 + embedding[3] * -9 + embedding[4] * 12 + embedding[5] * -2 + embedding[6] * 15 + embedding[7] * -12 + embedding[8] * -17 + embedding[9] * 7;
    let score_6 = embedding[0] * 25 + embedding[1] * 13 + embedding[2] * 19 + embedding[3] * -6 + embedding[4] * 9 + embedding[5] * -14 + embedding[6] * 21 + embedding[7] * -12 + embedding[8] * -25 + embedding[9] * 9;
    let score_7 = embedding[0] * 2 + embedding[1] * 5 + embedding[2] * -26 + embedding[3] * -9 + embedding[4] * 5 + embedding[5] * 16 + embedding[6] * 11 + embedding[7] * 23 + embedding[8] * 1 + embedding[9] * 6;
    let score_8 = embedding[0] * -15 + embedding[1] * 14 + embedding[2] * 14 + embedding[3] * 45 + embedding[4] * -20 + embedding[5] * 2 + embedding[6] * -40 + embedding[7] * -3 + embedding[8] * -41 + embedding[9] * 1;
    let score_9 = embedding[0] * -10 + embedding[1] * 15 + embedding[2] * 10 + embedding[3] * 27 + embedding[4] * -32 + embedding[5] * -5 + embedding[6] * -35 + embedding[7] * 5 + embedding[8] * -45 + embedding[9] * -21;
    let score_10 = embedding[0] * -4 + embedding[1] * -16 + embedding[2] * 18 + embedding[3] * 6 + embedding[4] * 11 + embedding[5] * -14 + embedding[6] * 20 + embedding[7] * -3 + embedding[8] * 26 + embedding[9] * 30;
    let score_11 = embedding[1] * -16 + embedding[2] * 5 + embedding[3] * 16 + embedding[4] * 11 + embedding[5] * -10 + embedding[6] * -1 + embedding[7] * 11 + embedding[8] * 18 + embedding[9] * 38;
    let score_12 = embedding[0] * -17 + embedding[1] * 6 + embedding[2] * 30 + embedding[3] * 32 + embedding[4] * -32 + embedding[5] * -17 + embedding[6] * -42 + embedding[7] * -3 + embedding[8] * -32 + embedding[9] * -17;
    let score_13 = embedding[0] * -4 + embedding[1] * 15 + embedding[2] * -4 + embedding[3] * 30 + embedding[4] * -30 + embedding[6] * -35 + embedding[7] * 6 + embedding[8] * -22 + embedding[9] * -7;
    let score_14 = embedding[0] * -9 + embedding[1] * -16 + embedding[2] * 36 + embedding[3] * 31 + embedding[4] * -3 + embedding[5] * -41 + embedding[6] * -11 + embedding[7] * 5 + embedding[8] * 22 + embedding[9] * 40;
    let score_15 = embedding[0] * 5 + embedding[1] * -3 + embedding[2] * -4 + embedding[3] * 1 + embedding[4] * -16 + embedding[5] * -4 + embedding[6] * 6 + embedding[7] * -11 + embedding[8] * 8 + embedding[9] * -4;
    let score_16 = embedding[0] * 1 + embedding[1] * 2 + embedding[2] * 13 + embedding[3] * 17 + embedding[4] * -13 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 5;
    let score_17 = embedding[0] * 1 + embedding[1] * 2 + embedding[2] * 13 + embedding[3] * 17 + embedding[4] * -13 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -15 + embedding[9] * 5;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 39;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 26;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 38;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 79;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 80;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 123;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_470(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[1] * 2 + embedding[2] * -17 + embedding[3] * -14 + embedding[4] * -8 + embedding[5] * 2 + embedding[6] * -8 + embedding[7] * 7 + embedding[9] * -22;
    let score_1 = embedding[0] * 6 + embedding[1] * -12 + embedding[2] * -13 + embedding[3] * -14 + embedding[4] * 6 + embedding[5] * -3 + embedding[6] * 13 + embedding[7] * 2 + embedding[8] * 22 + embedding[9] * 5;
    let score_2 = embedding[0] * -8 + embedding[1] * -4 + embedding[2] * -10 + embedding[3] * 11 + embedding[4] * 10 + embedding[5] * 11 + embedding[6] * -17 + embedding[7] * -2 + embedding[8] * 3 + embedding[9] * 14;
    let score_3 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 2 + embedding[3] * -9 + embedding[4] * 28 + embedding[5] * 12 + embedding[6] * 5 + embedding[7] * -5 + embedding[8] * 1 + embedding[9] * -9;
    let score_4 = embedding[0] * 16 + embedding[1] * 9 + embedding[2] * 4 + embedding[3] * -8 + embedding[4] * 16 + embedding[5] * -3 + embedding[6] * 10 + embedding[7] * -9 + embedding[8] * -13 + embedding[9] * 5;
    let score_5 = embedding[0] * -2 + embedding[1] * -8 + embedding[2] * 4 + embedding[3] * -2 + embedding[4] * 2 + embedding[5] * -11 + embedding[6] * -18 + embedding[7] * -4 + embedding[8] * 14 + embedding[9] * -9;
    let score_6 = embedding[0] * -19 + embedding[1] * -11 + embedding[2] * 15 + embedding[3] * -8 + embedding[4] * -50 + embedding[5] * 18 + embedding[6] * 42 + embedding[7] * -32 + embedding[8] * -2 + embedding[9] * -5;
    let score_7 = embedding[0] * 25 + embedding[1] * 13 + embedding[2] * 18 + embedding[3] * -5 + embedding[4] * 10 + embedding[5] * -16 + embedding[6] * 17 + embedding[7] * -9 + embedding[8] * -23 + embedding[9] * 9;
    let score_8 = embedding[0] * 2 + embedding[1] * 5 + embedding[2] * -24 + embedding[3] * -6 + embedding[4] * 8 + embedding[5] * 14 + embedding[6] * 8 + embedding[7] * 24 + embedding[8] * 1 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 8 + embedding[2] * 3 + embedding[3] * 36 + embedding[4] * -18 + embedding[5] * -1 + embedding[6] * -36 + embedding[7] * 2 + embedding[8] * -27 + embedding[9] * 4;
    let score_10 = embedding[0] * -5 + embedding[1] * 9 + embedding[3] * 18 + embedding[4] * -27 + embedding[5] * -5 + embedding[6] * -33 + embedding[7] * 8 + embedding[8] * -32 + embedding[9] * -16;
    let score_11 = embedding[0] * -1 + embedding[1] * -14 + embedding[2] * 12 + embedding[3] * 4 + embedding[4] * 7 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * -2 + embedding[8] * 24 + embedding[9] * 27;
    let score_12 = embedding[0] * 5 + embedding[1] * -16 + embedding[2] * -3 + embedding[3] * 13 + embedding[4] * 5 + embedding[5] * -12 + embedding[6] * -2 + embedding[7] * 14 + embedding[8] * 17 + embedding[9] * 30;
    let score_13 = embedding[0] * -11 + embedding[1] * 1 + embedding[2] * 25 + embedding[3] * 26 + embedding[4] * -25 + embedding[5] * -17 + embedding[6] * -41 + embedding[7] * -1 + embedding[8] * -20 + embedding[9] * -10;
    let score_14 = embedding[0] * -1 + embedding[1] * 11 + embedding[2] * -8 + embedding[3] * 24 + embedding[4] * -28 + embedding[5] * -3 + embedding[6] * -34 + embedding[7] * 10 + embedding[8] * -17 + embedding[9] * -3;
    let score_15 = embedding[0] * -6 + embedding[1] * -15 + embedding[2] * 33 + embedding[3] * 31 + embedding[4] * -5 + embedding[5] * -41 + embedding[6] * -13 + embedding[7] * 7 + embedding[8] * 20 + embedding[9] * 35;
    let score_16 = embedding[0] * 3 + embedding[1] * -4 + embedding[2] * -3 + embedding[3] * 3 + embedding[4] * -23 + embedding[5] * -4 + embedding[6] * 6 + embedding[7] * -11 + embedding[8] * 8 + embedding[9] * -1;
    let score_17 = embedding[0] * -6 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 15 + embedding[8] * -6 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 3;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 45;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 39;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 26;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 32;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 33;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 38;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 23;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 79;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 80;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 123;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 93;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_471(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -15 + embedding[1] * -1 + embedding[2] * -8 + embedding[3] * -11 + embedding[4] * 21 + embedding[5] * 20 + embedding[6] * 3 + embedding[7] * -8 + embedding[8] * 10 + embedding[9] * -17;
    let score_1 = embedding[0] * 13 + embedding[1] * 2 + embedding[2] * -11 + embedding[3] * -17 + embedding[4] * -6 + embedding[5] * -4 + embedding[6] * 9 + embedding[7] * 1 + embedding[8] * -8 + embedding[9] * -16;
    let score_2 = embedding[0] * 13 + embedding[1] * -5 + embedding[2] * -6 + embedding[3] * -18 + embedding[4] * 2 + embedding[5] * -7 + embedding[6] * 20 + embedding[7] * -2 + embedding[8] * 6 + embedding[9] * -1;
    let score_3 = embedding[0] * 5 + embedding[1] * -4 + embedding[2] * -2 + embedding[3] * 15 + embedding[4] * 6 + embedding[5] * 3 + embedding[6] * -5 + embedding[7] * -8 + embedding[8] * -11 + embedding[9] * 26;
    let score_4 = embedding[0] * 15 + embedding[1] * 1 + embedding[2] * -7 + embedding[3] * -5 + embedding[4] * 6 + embedding[5] * 1 + embedding[6] * 6 + embedding[7] * -10 + embedding[8] * -14 + embedding[9] * 6;
    let score_5 = embedding[0] * 5 + embedding[1] * -5 + embedding[2] * 12 + embedding[3] * -8 + embedding[5] * -17 + embedding[6] * -6 + embedding[7] * -6 + embedding[8] * 5 + embedding[9] * -14;
    let score_6 = embedding[1] * 8 + embedding[2] * 2 + embedding[3] * 17 + embedding[4] * -25 + embedding[5] * -4 + embedding[6] * -23 + embedding[7] * 4 + embedding[8] * -36 + embedding[9] * -13;
    let score_7 = embedding[0] * -3 + embedding[1] * 6 + embedding[2] * 5 + embedding[3] * 33 + embedding[4] * -20 + embedding[5] * -2 + embedding[6] * -25 + embedding[7] * -3 + embedding[8] * -35 + embedding[9] * 10;
    let score_8 = embedding[0] * 3 + embedding[1] * 4 + embedding[2] * 4 + embedding[3] * -7 + embedding[4] * -21 + embedding[5] * 14 + embedding[6] * -6 + embedding[7] * 14 + embedding[8] * 7 + embedding[9] * 7;
    let score_9 = embedding[0] * -1 + embedding[1] * -15 + embedding[2] * 12 + embedding[3] * 7 + embedding[4] * 4 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * -5 + embedding[8] * 15 + embedding[9] * 30;
    let score_10 = embedding[0] * -9 + embedding[1] * 18 + embedding[2] * 3 + embedding[3] * 9 + embedding[4] * -3 + embedding[5] * 1 + embedding[6] * -4 + embedding[7] * 32 + embedding[8] * 29 + embedding[9] * -3;
    let score_11 = embedding[0] * -18 + embedding[1] * 22 + embedding[2] * 2 + embedding[3] * -12 + embedding[4] * -14 + embedding[6] * -6 + embedding[7] * 40 + embedding[8] * 17 + embedding[9] * 7;
    let score_12 = embedding[0] * 12 + embedding[1] * -11 + embedding[2] * 2 + embedding[3] * 15 + embedding[5] * -16 + embedding[6] * 2 + embedding[7] * 11 + embedding[8] * 2 + embedding[9] * 40;
    let score_13 = embedding[0] * -9 + embedding[2] * 24 + embedding[3] * 23 + embedding[4] * -23 + embedding[5] * -16 + embedding[6] * -32 + embedding[7] * -4 + embedding[8] * -22 + embedding[9] * -10;
    let score_14 = embedding[0] * 5 + embedding[1] * 11 + embedding[2] * -3 + embedding[3] * 24 + embedding[4] * -29 + embedding[5] * -4 + embedding[6] * -23 + embedding[7] * 4 + embedding[8] * -24 + embedding[9] * 1;
    let score_15 = embedding[0] * -2 + embedding[1] * -14 + embedding[2] * 37 + embedding[3] * 36 + embedding[4] * -9 + embedding[5] * -38 + embedding[6] * -12 + embedding[7] * 7 + embedding[8] * 13 + embedding[9] * 43;
    let score_16 = embedding[0] * 10 + embedding[2] * -5 + embedding[4] * -15 + embedding[5] * -7 + embedding[6] * 10 + embedding[7] * -8 + embedding[8] * -2 + embedding[9] * -1;
    let score_17 = embedding[0] * -6 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 14 + embedding[4] * -19 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 33;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 32;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 20;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 38;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 15;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 29;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 23;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 79;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 80;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 123;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 93;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_472(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 13 + embedding[1] * -1 + embedding[2] * -1 + embedding[3] * -11 + embedding[4] * 28 + embedding[5] * 9 + embedding[6] * -2 + embedding[7] * -21 + embedding[8] * -8 + embedding[9] * -9;
    let score_1 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_2 = embedding[1] * -2 + embedding[2] * 2 + embedding[3] * -1 + embedding[4] * -31 + embedding[5] * 9 + embedding[6] * -10 + embedding[7] * 11 + embedding[8] * 5 + embedding[9] * 6;
    let score_3 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * -3 + embedding[3] * 18 + embedding[4] * -29 + embedding[5] * -6 + embedding[6] * -11 + embedding[7] * 35 + embedding[8] * 28 + embedding[9] * 7;
    let score_4 = embedding[0] * 17 + embedding[1] * 10 + embedding[2] * 13 + embedding[3] * -4 + embedding[4] * 22 + embedding[5] * 7 + embedding[6] * 25 + embedding[7] * -13 + embedding[8] * -22 + embedding[9] * -1;
    let score_5 = embedding[0] * -16 + embedding[1] * -13 + embedding[2] * -7 + embedding[3] * 15 + embedding[4] * -24 + embedding[5] * 4 + embedding[6] * -1 + embedding[7] * 26 + embedding[8] * 18;
    let score_6 = embedding[0] * -16 + embedding[1] * 9 + embedding[2] * -5 + embedding[3] * -4 + embedding[4] * -35 + embedding[5] * -7 + embedding[6] * -13 + embedding[7] * 38 + embedding[8] * 13 + embedding[9] * 15;
    let score_7 = embedding[0] * -19 + embedding[1] * 6 + embedding[2] * -15 + embedding[3] * 15 + embedding[4] * -41 + embedding[5] * -4 + embedding[6] * -1 + embedding[7] * 16 + embedding[8] * 14 + embedding[9] * 9;
    let score_8 = embedding[0] * -11 + embedding[1] * 11 + embedding[2] * 20 + embedding[3] * -7 + embedding[4] * -6 + embedding[5] * -1 + embedding[6] * 5 + embedding[7] * -8 + embedding[8] * 5 + embedding[9] * -5;
    let score_9 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[1] * -10 + embedding[2] * -17 + embedding[3] * -7 + embedding[4] * 9 + embedding[5] * 6 + embedding[6] * -10 + embedding[7] * -4 + embedding[8] * 2 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 20;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 29;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 35;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 52;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_473(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_1 = embedding[0] * 3 + embedding[2] * 6 + embedding[3] * -11 + embedding[4] * 27 + embedding[5] * 13 + embedding[7] * -7 + embedding[8] * 4 + embedding[9] * -11;
    let score_2 = embedding[0] * 5 + embedding[1] * -2 + embedding[2] * 3 + embedding[3] * -4 + embedding[4] * -30 + embedding[5] * 10 + embedding[6] * -12 + embedding[7] * 9 + embedding[8] * 7 + embedding[9] * 8;
    let score_3 = embedding[0] * -7 + embedding[1] * 10 + embedding[2] * -7 + embedding[3] * -13 + embedding[4] * -20 + embedding[5] * -4 + embedding[6] * -13 + embedding[7] * 19 + embedding[8] * 10 + embedding[9] * 12;
    let score_4 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * -18 + embedding[3] * 10 + embedding[4] * -26 + embedding[5] * -5 + embedding[6] * -2 + embedding[7] * -1 + embedding[8] * 9 + embedding[9] * 6;
    let score_5 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_6 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_7 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_8 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_9 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_10 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_11 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_12 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_13 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_14 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_15 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_16 = embedding[0] * -8 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 11;
    let score_17 = embedding[0] * 10 + embedding[1] * -8 + embedding[2] * -18 + embedding[3] * -11 + embedding[4] * 15 + embedding[5] * 6 + embedding[6] * -13 + embedding[7] * -15 + embedding[8] * 5 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 20;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 29;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 35;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_474(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_1 = embedding[0] * 2 + embedding[2] * -4 + embedding[3] * -12 + embedding[4] * 16 + embedding[5] * 12 + embedding[6] * -6 + embedding[7] * -12 + embedding[8] * 8 + embedding[9] * -8;
    let score_2 = embedding[0] * 11 + embedding[1] * -12 + embedding[2] * 12 + embedding[3] * -9 + embedding[4] * 4 + embedding[5] * 5 + embedding[6] * -11 + embedding[7] * 13 + embedding[8] * -5 + embedding[9] * 1;
    let score_3 = embedding[0] * -3 + embedding[1] * 8 + embedding[2] * -21 + embedding[3] * 10 + embedding[4] * -19 + embedding[5] * -4 + embedding[6] * -2 + embedding[7] * -3 + embedding[8] * 9 + embedding[9] * 3;
    let score_4 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_5 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_6 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_7 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_8 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_9 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_10 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_11 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_12 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_13 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_14 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_15 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_16 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 11;
    let score_17 = embedding[0] * 8 + embedding[1] * -4 + embedding[2] * -19 + embedding[3] * -12 + embedding[4] * 13 + embedding[5] * 7 + embedding[6] * -16 + embedding[7] * -13 + embedding[8] * 8 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 35;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_475(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_1 = embedding[0] * 4 + embedding[1] * -2 + embedding[2] * -5 + embedding[3] * -15 + embedding[4] * 21 + embedding[5] * 13 + embedding[6] * -7 + embedding[7] * -11 + embedding[8] * 8 + embedding[9] * -10;
    let score_2 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_3 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_5 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_6 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_7 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_8 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_9 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_10 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_11 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_12 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_14 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_15 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_16 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 21 + embedding[3] * 23 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 6;
    let score_17 = embedding[0] * 11 + embedding[1] * -6 + embedding[2] * -21 + embedding[3] * -15 + embedding[4] * 15 + embedding[5] * 10 + embedding[6] * -18 + embedding[7] * -13 + embedding[8] * 9 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_476(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * 4 + embedding[1] * -5 + embedding[2] * -9 + embedding[3] * -14 + embedding[4] * 10 + embedding[5] * 11 + embedding[6] * -7 + embedding[7] * -15 + embedding[8] * 7 + embedding[9] * -7;
    let score_2 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -9 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 22 + embedding[4] * -23 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -16 + embedding[3] * -15 + embedding[4] * 15 + embedding[5] * 10 + embedding[6] * -17 + embedding[7] * -13 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_477(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -9 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 14 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_17 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 6 + embedding[6] * -27 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_478(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 13 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -3;
    let score_2 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 5 + embedding[6] * -28 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_479(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_480(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_481(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_482(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_483(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -1 + embedding[1] * -19 + embedding[2] * 5 + embedding[3] * -11 + embedding[4] * 11 + embedding[5] * 13 + embedding[6] * -3 + embedding[7] * -2 + embedding[8] * -7 + embedding[9] * -7;
    let score_2 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 9 + embedding[1] * 4 + embedding[2] * -18 + embedding[3] * -16 + embedding[4] * 20 + embedding[5] * 10 + embedding[6] * -15 + embedding[7] * -17 + embedding[8] * 15 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_484(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_1 = embedding[0] * 4 + embedding[1] * -12 + embedding[2] * 61 + embedding[3] * -7 + embedding[4] * -2 + embedding[5] * 12 + embedding[6] * 8 + embedding[7] * 25 + embedding[8] * -23;
    let score_2 = embedding[0] * -5 + embedding[1] * -14 + embedding[2] * -7 + embedding[3] * -10 + embedding[4] * 9 + embedding[5] * 10 + embedding[6] * -4 + embedding[7] * -9 + embedding[8] * -2 + embedding[9] * -7;
    let score_3 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_4 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_5 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_6 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_7 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_8 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_9 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_10 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_11 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_12 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_13 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_14 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_15 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_16 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -10 + embedding[9] * 9;
    let score_17 = embedding[0] * 7 + embedding[1] * 8 + embedding[2] * -27 + embedding[3] * -15 + embedding[4] * 19 + embedding[5] * 7 + embedding[6] * -16 + embedding[7] * -22 + embedding[8] * 19 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 9;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 1;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_485(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_1 = embedding[0] * 5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -14 + embedding[4] * 12 + embedding[5] * 19 + embedding[7] * -9 + embedding[8] * -8 + embedding[9] * -5;
    let score_2 = embedding[0] * 3 + embedding[1] * 7 + embedding[2] * 8 + embedding[3] * 25 + embedding[4] * -4 + embedding[5] * -10 + embedding[6] * -5 + embedding[7] * 26 + embedding[8] * 30 + embedding[9] * -1;
    let score_3 = embedding[0] * -10 + embedding[1] * -7 + embedding[2] * 6 + embedding[3] * 12 + embedding[4] * -9 + embedding[5] * 10 + embedding[6] * 1 + embedding[7] * 21 + embedding[8] * 19 + embedding[9] * -3;
    let score_4 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_5 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_6 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_7 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_8 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_9 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_10 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_11 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_12 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_13 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_14 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_15 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_16 = embedding[0] * -9 + embedding[1] * 1 + embedding[2] * 23 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -6 + embedding[9] * 9;
    let score_17 = embedding[0] * 7 + embedding[1] * -2 + embedding[2] * -16 + embedding[3] * -17 + embedding[4] * 18 + embedding[5] * 7 + embedding[6] * -13 + embedding[7] * -17 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 15;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_486(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 25 + embedding[1] * -10 + embedding[3] * -19 + embedding[4] * 18 + embedding[5] * 14 + embedding[6] * -4 + embedding[7] * -32 + embedding[8] * -23 + embedding[9] * -4;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_2 = embedding[0] * -17 + embedding[1] * 10 + embedding[2] * -3 + embedding[3] * 36 + embedding[4] * -11 + embedding[5] * -13 + embedding[6] * -9 + embedding[7] * 53 + embedding[8] * 45 + embedding[9] * -5;
    let score_3 = embedding[0] * -32 + embedding[1] * -4 + embedding[2] * -7 + embedding[3] * 24 + embedding[4] * -16 + embedding[5] * 7 + embedding[6] * -4 + embedding[7] * 48 + embedding[8] * 33 + embedding[9] * -7;
    let score_4 = embedding[0] * 26 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * -13 + embedding[4] * 13 + embedding[5] * 13 + embedding[6] * 28 + embedding[7] * -25 + embedding[8] * -38 + embedding[9] * 5;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -6 + embedding[9] * 8;
    let score_17 = embedding[0] * -16 + embedding[1] * -2 + embedding[2] * -28 + embedding[3] * -9 + embedding[4] * 11 + embedding[5] * 7 + embedding[6] * -19 + embedding[7] * 10 + embedding[8] * 14 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 15;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_487(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 13 + embedding[3] * 13 + embedding[4] * -16 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -6 + embedding[9] * 7;
    let score_1 = embedding[0] * 30 + embedding[1] * 8 + embedding[2] * 2 + embedding[3] * -17 + embedding[4] * 8 + embedding[6] * 3 + embedding[7] * -17 + embedding[8] * -43 + embedding[9] * -13;
    let score_2 = embedding[0] * 29 + embedding[1] * -5 + embedding[2] * 8 + embedding[3] * -17 + embedding[4] * 22 + embedding[5] * -5 + embedding[6] * 23 + embedding[7] * -21 + embedding[8] * -19 + embedding[9] * 12;
    let score_3 = embedding[0] * 20 + embedding[1] * 2 + embedding[2] * 7 + embedding[3] * 3 + embedding[4] * 19 + embedding[5] * 5 + embedding[6] * -5 + embedding[7] * -25 + embedding[8] * -42 + embedding[9] * 18;
    let score_4 = embedding[0] * 20 + embedding[1] * -3 + embedding[2] * 25 + embedding[3] * -7 + embedding[4] * 14 + embedding[5] * -15 + embedding[6] * -12 + embedding[7] * -25 + embedding[8] * -24 + embedding[9] * -6;
    let score_5 = embedding[0] * -20 + embedding[1] * -29 + embedding[2] * -2 + embedding[3] * 5 + embedding[4] * 3 + embedding[5] * 20 + embedding[6] * 2 + embedding[7] * -2 + embedding[8] * -2 + embedding[9] * -18;
    let score_6 = embedding[0] * -2 + embedding[1] * 10 + embedding[2] * 26 + embedding[3] * -7 + embedding[4] * -63 + embedding[5] * 27 + embedding[6] * -16 + embedding[7] * 32 + embedding[8] * 9 + embedding[9] * 25;
    let score_7 = embedding[0] * -54 + embedding[1] * 9 + embedding[2] * -6 + embedding[3] * 29 + embedding[4] * -32 + embedding[5] * -3 + embedding[6] * -15 + embedding[7] * 76 + embedding[8] * 67;
    let score_8 = embedding[0] * 31 + embedding[1] * 17 + embedding[2] * 21 + embedding[3] * -12 + embedding[4] * 19 + embedding[5] * 1 + embedding[6] * 35 + embedding[7] * -19 + embedding[8] * -48 + embedding[9] * 11;
    let score_9 = embedding[0] * -65 + embedding[1] * -15 + embedding[2] * -18 + embedding[3] * 32 + embedding[4] * -30 + embedding[5] * 13 + embedding[6] * -4 + embedding[7] * 64 + embedding[8] * 49 + embedding[9] * -11;
    let score_10 = embedding[0] * 27 + embedding[1] * 5 + embedding[2] * 3 + embedding[3] * -14 + embedding[4] * 18 + embedding[5] * 1 + embedding[6] * 6 + embedding[7] * -21 + embedding[8] * -31 + embedding[9] * 2;
    let score_11 = embedding[0] * -45 + embedding[1] * 32 + embedding[2] * -12 + embedding[3] * -2 + embedding[4] * -47 + embedding[5] * -1 + embedding[6] * -24 + embedding[7] * 71 + embedding[8] * 46 + embedding[9] * 16;
    let score_12 = embedding[0] * 24 + embedding[1] * -6 + embedding[2] * 7 + embedding[3] * 1 + embedding[4] * 18 + embedding[5] * -9 + embedding[6] * 3 + embedding[7] * -2 + embedding[8] * -18 + embedding[9] * 32;
    let score_13 = embedding[0] * -47 + embedding[1] * 28 + embedding[2] * 33 + embedding[3] * -4 + embedding[4] * -21 + embedding[5] * 2 + embedding[6] * 8 + embedding[7] * 12 + embedding[8] * 26 + embedding[9] * -7;
    let score_14 = embedding[0] * 9 + embedding[1] * 4 + embedding[2] * -7 + embedding[3] * 8 + embedding[4] * -13 + embedding[5] * 7 + embedding[6] * -23 + embedding[7] * -7 + embedding[8] * -22 + embedding[9] * -3;
    let score_15 = embedding[0] * 18 + embedding[1] * -5 + embedding[2] * -3 + embedding[3] * -1 + embedding[4] * -11 + embedding[5] * -2 + embedding[6] * 4 + embedding[7] * -18 + embedding[8] * -9 + embedding[9] * -1;
    let score_16 = embedding[0] * -2 + embedding[1] * 8 + embedding[2] * 13 + embedding[3] * 13 + embedding[4] * -16 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -6 + embedding[9] * 7;
    let score_17 = embedding[0] * -45 + embedding[1] * -13 + embedding[2] * -41 + embedding[3] * 4 + embedding[5] * 9 + embedding[6] * -18 + embedding[7] * 23 + embedding[8] * 29 + embedding[9] * -16;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 20;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 15;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 7;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 6;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 109;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 29;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 23;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 52;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 100;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_488(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -21 + embedding[1] * -8 + embedding[2] * -20 + embedding[3] * -3 + embedding[4] * 14 + embedding[5] * 28 + embedding[6] * -10 + embedding[7] * -3 + embedding[8] * 14 + embedding[9] * -14;
    let score_1 = embedding[0] * 16 + embedding[1] * 8 + embedding[2] * -9 + embedding[3] * -16 + embedding[4] * -10 + embedding[8] * -15 + embedding[9] * -27;
    let score_2 = embedding[0] * 18 + embedding[1] * -8 + embedding[2] * 2 + embedding[3] * -18 + embedding[4] * 10 + embedding[5] * -9 + embedding[6] * 26 + embedding[7] * -8 + embedding[8] * 14 + embedding[9] * 9;
    let score_3 = embedding[0] * -3 + embedding[1] * -2 + embedding[2] * -2 + embedding[3] * 24 + embedding[4] * 8 + embedding[5] * 10 + embedding[6] * -18 + embedding[7] * -13 + embedding[8] * -16 + embedding[9] * 26;
    let score_4 = embedding[0] * 8 + embedding[1] * -3 + embedding[2] * 19 + embedding[3] * -7 + embedding[4] * 1 + embedding[5] * -21 + embedding[6] * -16 + embedding[7] * -13 + embedding[8] * 3 + embedding[9] * -10;
    let score_5 = embedding[0] * 18 + embedding[1] * 16 + embedding[2] * 14 + embedding[3] * -7 + embedding[4] * 10 + embedding[5] * 3 + embedding[6] * 35 + embedding[7] * -6 + embedding[8] * -23 + embedding[9] * 8;
    let score_6 = embedding[0] * -31 + embedding[1] * 22 + embedding[2] * -8 + embedding[3] * -13 + embedding[4] * -16 + embedding[5] * 5 + embedding[6] * -15 + embedding[7] * 32 + embedding[8] * 16 + embedding[9] * 10;
    let score_7 = embedding[0] * 11 + embedding[1] * -10 + embedding[3] * 13 + embedding[4] * 9 + embedding[5] * -7 + embedding[6] * -4 + embedding[7] * 9 + embedding[8] * 5 + embedding[9] * 36;
    let score_8 = embedding[0] * 21 + embedding[1] * -11 + embedding[2] * 8 + embedding[3] * -13 + embedding[5] * -19 + embedding[6] * 37 + embedding[7] * 3 + embedding[8] * 18 + embedding[9] * 12;
    let score_9 = embedding[1] * 7 + embedding[2] * -12 + embedding[3] * 18 + embedding[4] * -17 + embedding[5] * 8 + embedding[6] * -24 + embedding[8] * -6 + embedding[9] * -4;
    let score_10 = embedding[0] * 14 + embedding[1] * 10 + embedding[2] * -8 + embedding[3] * -12 + embedding[4] * -13 + embedding[5] * -2 + embedding[6] * 5 + embedding[7] * 7 + embedding[8] * -19 + embedding[9] * -23;
    let score_11 = embedding[0] * -8 + embedding[1] * 1 + embedding[3] * 30 + embedding[4] * 4 + embedding[5] * 9 + embedding[6] * -15 + embedding[7] * -7 + embedding[8] * -15 + embedding[9] * 25;
    let score_12 = embedding[0] * 9 + embedding[1] * -2 + embedding[2] * -6 + embedding[4] * -13 + embedding[5] * -8 + embedding[6] * 6 + embedding[7] * -10 + embedding[8] * 6 + embedding[9] * -6;
    let score_13 = embedding[0] * -7 + embedding[2] * 19 + embedding[3] * 3 + embedding[5] * -18 + embedding[6] * -16 + embedding[7] * -5 + embedding[8] * 3 + embedding[9] * -19;
    let score_14 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * -5 + embedding[1] * 9 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -8 + embedding[9] * 8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 29;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 144;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 100;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 150;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 159;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 93;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 185;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_489(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 14 + embedding[1] * -8 + embedding[2] * -12 + embedding[3] * -33 + embedding[4] * 2 + embedding[5] * -4 + embedding[6] * 27 + embedding[7] * 3 + embedding[8] * 24 + embedding[9] * -13;
    let score_1 = embedding[0] * 12 + embedding[1] * 4 + embedding[2] * -17 + embedding[3] * -34 + embedding[4] * -11 + embedding[5] * 2 + embedding[6] * 9 + embedding[7] * 9 + embedding[8] * 2 + embedding[9] * -39;
    let score_2 = embedding[0] * -13 + embedding[1] * -6 + embedding[2] * -4 + embedding[3] * 28 + embedding[4] * 20 + embedding[5] * 21 + embedding[6] * -17 + embedding[7] * -12 + embedding[8] * 2 + embedding[9] * 37;
    let score_3 = embedding[0] * 5 + embedding[1] * -4 + embedding[2] * 7 + embedding[3] * -18 + embedding[4] * 2 + embedding[5] * -12 + embedding[6] * -8 + embedding[7] * -7 + embedding[8] * 15 + embedding[9] * -25;
    let score_4 = embedding[0] * 17 + embedding[1] * 5 + embedding[2] * -9 + embedding[3] * -8 + embedding[4] * 14 + embedding[5] * 9 + embedding[6] * 3 + embedding[7] * -15 + embedding[8] * -15 + embedding[9] * 5;
    let score_5 = embedding[0] * 1 + embedding[1] * 2 + embedding[2] * -12 + embedding[3] * -8 + embedding[4] * 19 + embedding[5] * 18 + embedding[6] * -2 + embedding[7] * -12 + embedding[8] * -4 + embedding[9] * -5;
    let score_6 = embedding[0] * -19 + embedding[1] * 14 + embedding[2] * 15 + embedding[3] * 50 + embedding[4] * -10 + embedding[5] * 11 + embedding[6] * -39 + embedding[7] * -8 + embedding[8] * -43 + embedding[9] * 14;
    let score_7 = embedding[0] * -5 + embedding[1] * 18 + embedding[2] * 8 + embedding[3] * 19 + embedding[4] * -25 + embedding[6] * -30 + embedding[7] * 2 + embedding[8] * -52 + embedding[9] * -24;
    let score_8 = embedding[0] * -4 + embedding[1] * -16 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * 16 + embedding[5] * -9 + embedding[6] * 19 + embedding[7] * -6 + embedding[8] * 27 + embedding[9] * 47;
    let score_9 = embedding[0] * 3 + embedding[1] * -13 + embedding[2] * 7 + embedding[3] * 24 + embedding[4] * 17 + embedding[5] * -7 + embedding[6] * -5 + embedding[7] * 9 + embedding[8] * 13 + embedding[9] * 55;
    let score_10 = embedding[0] * -12 + embedding[1] * 8 + embedding[2] * 30 + embedding[3] * 25 + embedding[4] * -31 + embedding[5] * -16 + embedding[6] * -39 + embedding[7] * -4 + embedding[8] * -31 + embedding[9] * -22;
    let score_11 = embedding[0] * -8 + embedding[1] * -15 + embedding[2] * 36 + embedding[3] * 40 + embedding[4] * 4 + embedding[5] * -35 + embedding[6] * -16 + embedding[7] * 3 + embedding[8] * 20 + embedding[9] * 55;
    let score_12 = embedding[0] * 14 + embedding[2] * -10 + embedding[3] * -12 + embedding[4] * -14 + embedding[5] * -2 + embedding[6] * 12 + embedding[7] * -10 + embedding[8] * 9 + embedding[9] * -11;
    let score_13 = embedding[0] * 1 + embedding[1] * 3 + embedding[2] * 13 + embedding[3] * 16 + embedding[4] * -14 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 6;
    let score_14 = embedding[0] * 1 + embedding[1] * 3 + embedding[2] * 13 + embedding[3] * 16 + embedding[4] * -14 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 6;
    let score_15 = embedding[0] * 1 + embedding[1] * 3 + embedding[2] * 13 + embedding[3] * 16 + embedding[4] * -14 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 6;
    let score_16 = embedding[0] * 1 + embedding[1] * 3 + embedding[2] * 13 + embedding[3] * 16 + embedding[4] * -14 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 6;
    let score_17 = embedding[0] * 1 + embedding[1] * 3 + embedding[2] * 13 + embedding[3] * 16 + embedding[4] * -14 + embedding[5] * -8 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -15 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 32;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 33;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 38;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 23;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 79;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 123;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 93;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_490(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -15 + embedding[1] * -6 + embedding[2] * -21 + embedding[3] * -11 + embedding[4] * 18 + embedding[5] * 24 + embedding[6] * -5 + embedding[7] * -4 + embedding[8] * 16 + embedding[9] * -13;
    let score_1 = embedding[0] * 18 + embedding[1] * 7 + embedding[2] * -8 + embedding[3] * -30 + embedding[4] * -11 + embedding[5] * -7 + embedding[6] * 15 + embedding[7] * 4 + embedding[8] * -5 + embedding[9] * -32;
    let score_2 = embedding[0] * 19 + embedding[1] * -5 + embedding[2] * -2 + embedding[3] * -29 + embedding[4] * 2 + embedding[5] * -11 + embedding[6] * 32 + embedding[7] * -2 + embedding[8] * 16 + embedding[9] * -6;
    let score_3 = embedding[0] * -3 + embedding[1] * -3 + embedding[2] * 5 + embedding[3] * 27 + embedding[4] * 15 + embedding[5] * 8 + embedding[6] * -8 + embedding[7] * -13 + embedding[8] * -6 + embedding[9] * 41;
    let score_4 = embedding[0] * 20 + embedding[1] * 4 + embedding[2] * -4 + embedding[3] * -10 + embedding[4] * 11 + embedding[5] * 2 + embedding[6] * 10 + embedding[7] * -14 + embedding[8] * -16 + embedding[9] * 8;
    let score_5 = embedding[0] * 10 + embedding[1] * -3 + embedding[2] * 17 + embedding[3] * -19 + embedding[4] * -3 + embedding[5] * -23 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -22;
    let score_6 = embedding[0] * -7 + embedding[1] * 6 + embedding[2] * -32 + embedding[3] * -11 + embedding[4] * 10 + embedding[5] * 27 + embedding[6] * 14 + embedding[7] * 27 + embedding[8] * 4 + embedding[9] * 3;
    let score_7 = embedding[0] * -18 + embedding[1] * 15 + embedding[2] * 16 + embedding[3] * 52 + embedding[4] * -22 + embedding[5] * 4 + embedding[6] * -41 + embedding[7] * -6 + embedding[8] * -46 + embedding[9] * 9;
    let score_8 = embedding[0] * -8 + embedding[1] * 18 + embedding[2] * 11 + embedding[3] * 26 + embedding[4] * -39 + embedding[5] * -5 + embedding[6] * -36 + embedding[7] * 5 + embedding[8] * -53 + embedding[9] * -24;
    let score_9 = embedding[0] * -3 + embedding[1] * -18 + embedding[2] * 26 + embedding[3] * 13 + embedding[4] * 12 + embedding[5] * -18 + embedding[6] * 26 + embedding[7] * -6 + embedding[8] * 30 + embedding[9] * 48;
    let score_10 = embedding[0] * 7 + embedding[1] * -13 + embedding[2] * 12 + embedding[3] * 25 + embedding[4] * 12 + embedding[5] * -17 + embedding[6] * 1 + embedding[7] * 7 + embedding[8] * 13 + embedding[9] * 59;
    let score_11 = embedding[0] * -15 + embedding[1] * 8 + embedding[2] * 30 + embedding[3] * 27 + embedding[4] * -39 + embedding[5] * -19 + embedding[6] * -42 + embedding[7] * -2 + embedding[8] * -36 + embedding[9] * -23;
    let score_12 = embedding[0] * -4 + embedding[1] * 15 + embedding[2] * -7 + embedding[3] * 33 + embedding[4] * -32 + embedding[5] * 3 + embedding[6] * -36 + embedding[7] * 5 + embedding[8] * -22 + embedding[9] * -5;
    let score_13 = embedding[0] * -4 + embedding[1] * -20 + embedding[2] * 40 + embedding[3] * 44 + embedding[4] * -1 + embedding[5] * -43 + embedding[6] * -12 + embedding[7] * 3 + embedding[8] * 21 + embedding[9] * 57;
    let score_14 = embedding[0] * 10 + embedding[1] * -2 + embedding[2] * -6 + embedding[3] * -8 + embedding[4] * -17 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * -10 + embedding[8] * 9 + embedding[9] * -9;
    let score_15 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 15 + embedding[8] * -9 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 26;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 32;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 33;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 38;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 23;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 79;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 80;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 123;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 93;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_491(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * -15 + embedding[3] * -20 + embedding[4] * -10 + embedding[5] * 4 + embedding[6] * 1 + embedding[7] * 6 + embedding[9] * -30;
    let score_1 = embedding[0] * 5 + embedding[1] * -12 + embedding[2] * -9 + embedding[3] * -21 + embedding[4] * 7 + embedding[5] * -3 + embedding[6] * 20 + embedding[8] * 28 + embedding[9] * -3;
    let score_2 = embedding[0] * -12 + embedding[1] * -4 + embedding[2] * -7 + embedding[3] * 16 + embedding[4] * 13 + embedding[5] * 16 + embedding[6] * -15 + embedding[7] * -9 + embedding[8] * 4 + embedding[9] * 18;
    let score_3 = embedding[0] * 17 + embedding[1] * 5 + embedding[2] * -3 + embedding[3] * -10 + embedding[4] * 13 + embedding[5] * 4 + embedding[6] * 11 + embedding[7] * -13 + embedding[8] * -13 + embedding[9] * 7;
    let score_4 = embedding[0] * -2 + embedding[1] * -7 + embedding[2] * 5 + embedding[3] * -10 + embedding[4] * 1 + embedding[5] * -10 + embedding[6] * -9 + embedding[7] * -5 + embedding[8] * 18 + embedding[9] * -16;
    let score_5 = embedding[1] * 1 + embedding[2] * -9 + embedding[3] * -8 + embedding[4] * 17 + embedding[5] * 13 + embedding[6] * 3 + embedding[7] * -10 + embedding[8] * -2 + embedding[9] * -3;
    let score_6 = embedding[0] * 26 + embedding[1] * 10 + embedding[2] * 13 + embedding[3] * -7 + embedding[4] * 10 + embedding[5] * -9 + embedding[6] * 19 + embedding[7] * -13 + embedding[8] * -22 + embedding[9] * 10;
    let score_7 = embedding[0] * 2 + embedding[1] * 6 + embedding[2] * -26 + embedding[3] * -10 + embedding[4] * 7 + embedding[5] * 18 + embedding[6] * 12 + embedding[7] * 22 + embedding[9] * 7;
    let score_8 = embedding[0] * -20 + embedding[1] * 16 + embedding[2] * 16 + embedding[3] * 49 + embedding[4] * -21 + embedding[5] * 3 + embedding[6] * -41 + embedding[7] * -3 + embedding[8] * -45 + embedding[9] * 3;
    let score_9 = embedding[0] * -11 + embedding[1] * 17 + embedding[2] * 11 + embedding[3] * 29 + embedding[4] * -36 + embedding[5] * -4 + embedding[6] * -37 + embedding[7] * 4 + embedding[8] * -52 + embedding[9] * -21;
    let score_10 = embedding[0] * -5 + embedding[1] * -15 + embedding[2] * 21 + embedding[3] * 6 + embedding[4] * 12 + embedding[5] * -12 + embedding[6] * 22 + embedding[7] * -5 + embedding[8] * 28 + embedding[9] * 31;
    let score_11 = embedding[0] * -3 + embedding[1] * -14 + embedding[2] * 8 + embedding[3] * 16 + embedding[4] * 14 + embedding[5] * -8 + embedding[6] * -1 + embedding[7] * 9 + embedding[8] * 18 + embedding[9] * 37;
    let score_12 = embedding[0] * -18 + embedding[1] * 9 + embedding[2] * 32 + embedding[3] * 34 + embedding[4] * -37 + embedding[5] * -16 + embedding[6] * -44 + embedding[7] * -1 + embedding[8] * -38 + embedding[9] * -18;
    let score_13 = embedding[0] * -4 + embedding[1] * 16 + embedding[2] * -4 + embedding[3] * 31 + embedding[4] * -32 + embedding[5] * 2 + embedding[6] * -34 + embedding[7] * 6 + embedding[8] * -25 + embedding[9] * -6;
    let score_14 = embedding[0] * -11 + embedding[1] * -16 + embedding[2] * 37 + embedding[3] * 32 + embedding[4] * -2 + embedding[5] * -38 + embedding[6] * -10 + embedding[7] * 3 + embedding[8] * 23 + embedding[9] * 41;
    let score_15 = embedding[0] * 7 + embedding[1] * -4 + embedding[2] * -6 + embedding[3] * -2 + embedding[4] * -17 + embedding[5] * -3 + embedding[6] * 10 + embedding[7] * -11 + embedding[8] * 10 + embedding[9] * -2;
    let score_16 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -10 + embedding[9] * 6;
    let score_17 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -10 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 3;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 39;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 26;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 38;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 79;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 80;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 123;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_492(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 2 + embedding[1] * 3 + embedding[2] * -12 + embedding[3] * -17 + embedding[4] * -11 + embedding[5] * 2 + embedding[6] * 1 + embedding[7] * 6 + embedding[8] * -1 + embedding[9] * -27;
    let score_1 = embedding[0] * 4 + embedding[1] * -11 + embedding[2] * -7 + embedding[3] * -18 + embedding[4] * 6 + embedding[5] * -3 + embedding[6] * 17 + embedding[8] * 25 + embedding[9] * -2;
    let score_2 = embedding[0] * -11 + embedding[1] * -5 + embedding[2] * -4 + embedding[3] * 18 + embedding[4] * 13 + embedding[5] * 14 + embedding[6] * -15 + embedding[7] * -9 + embedding[8] * 2 + embedding[9] * 21;
    let score_3 = embedding[0] * 17 + embedding[1] * 5 + embedding[3] * -7 + embedding[4] * 12 + embedding[5] * 2 + embedding[6] * 10 + embedding[7] * -12 + embedding[8] * -14 + embedding[9] * 9;
    let score_4 = embedding[0] * -3 + embedding[1] * -6 + embedding[2] * 6 + embedding[3] * -7 + embedding[4] * -1 + embedding[5] * -11 + embedding[6] * -9 + embedding[7] * -5 + embedding[8] * 16 + embedding[9] * -13;
    let score_5 = embedding[1] * 1 + embedding[2] * -6 + embedding[3] * -5 + embedding[4] * 16 + embedding[5] * 12 + embedding[6] * 3 + embedding[7] * -9 + embedding[8] * -3 + embedding[9] * -1;
    let score_6 = embedding[0] * 25 + embedding[1] * 10 + embedding[2] * 16 + embedding[3] * -4 + embedding[4] * 9 + embedding[5] * -11 + embedding[6] * 17 + embedding[7] * -13 + embedding[8] * -24 + embedding[9] * 12;
    let score_7 = embedding[0] * 2 + embedding[1] * 5 + embedding[2] * -23 + embedding[3] * -7 + embedding[4] * 6 + embedding[5] * 16 + embedding[6] * 12 + embedding[7] * 22 + embedding[8] * -1 + embedding[9] * 10;
    let score_8 = embedding[0] * -15 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 40 + embedding[4] * -13 + embedding[5] * 5 + embedding[6] * -33 + embedding[7] * -5 + embedding[8] * -33 + embedding[9] * 9;
    let score_9 = embedding[0] * -10 + embedding[1] * 14 + embedding[2] * 10 + embedding[3] * 25 + embedding[4] * -31 + embedding[5] * -4 + embedding[6] * -32 + embedding[7] * 4 + embedding[8] * -46 + embedding[9] * -18;
    let score_10 = embedding[0] * -7 + embedding[1] * -11 + embedding[2] * 4 + embedding[3] * 7 + embedding[4] * 13 + embedding[5] * 2 + embedding[6] * 4 + embedding[7] * -6 + embedding[8] * 19 + embedding[9] * 23;
    let score_11 = embedding[1] * -12 + embedding[2] * 2 + embedding[3] * 15 + embedding[4] * 10 + embedding[5] * -3 + embedding[6] * -4 + embedding[7] * 9 + embedding[8] * 13 + embedding[9] * 33;
    let score_12 = embedding[0] * -14 + embedding[1] * 7 + embedding[2] * 30 + embedding[3] * 26 + embedding[4] * -29 + embedding[5] * -17 + embedding[6] * -37 + embedding[7] * 1 + embedding[8] * -29 + embedding[9] * -17;
    let score_13 = embedding[0] * -3 + embedding[1] * 13 + embedding[2] * -3 + embedding[3] * 27 + embedding[4] * -28 + embedding[6] * -31 + embedding[7] * 5 + embedding[8] * -20 + embedding[9] * -4;
    let score_14 = embedding[0] * 10 + embedding[1] * -13 + embedding[2] * 4 + embedding[3] * -11 + embedding[4] * -4 + embedding[5] * -16 + embedding[6] * 31 + embedding[7] * 10 + embedding[8] * 26 + embedding[9] * 7;
    let score_15 = embedding[0] * 1 + embedding[1] * -8 + embedding[2] * -6 + embedding[3] * -15 + embedding[4] * -3 + embedding[5] * -7 + embedding[6] * 18 + embedding[7] * 12 + embedding[8] * 16 + embedding[9] * -12;
    let score_16 = embedding[0] * 4 + embedding[1] * 7 + embedding[2] * -6 + embedding[3] * -8 + embedding[4] * -17 + embedding[5] * -1 + embedding[6] * 4 + embedding[7] * 8 + embedding[8] * -9 + embedding[9] * -17;
    let score_17 = embedding[0] * -15 + embedding[1] * 12 + embedding[2] * -2 + embedding[3] * 8 + embedding[4] * -24 + embedding[6] * -14 + embedding[7] * 16 + embedding[8] * -13 + embedding[9] * -25;
    let mut best_score = score_0;
    let mut best_palette = 3;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 39;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 26;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 38;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 79;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 80;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 144;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 164;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 150;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 162;
    }
    best_palette
}

fn predict_pixel_493(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 3 + embedding[1] * 2 + embedding[2] * -15 + embedding[3] * -20 + embedding[4] * -10 + embedding[5] * 4 + embedding[6] * 1 + embedding[7] * 6 + embedding[8] * 1 + embedding[9] * -30;
    let score_1 = embedding[0] * 5 + embedding[1] * -12 + embedding[2] * -9 + embedding[3] * -21 + embedding[4] * 7 + embedding[5] * -3 + embedding[6] * 20 + embedding[7] * 1 + embedding[8] * 28 + embedding[9] * -3;
    let score_2 = embedding[0] * -12 + embedding[1] * -5 + embedding[2] * -7 + embedding[3] * 16 + embedding[4] * 13 + embedding[5] * 16 + embedding[6] * -15 + embedding[7] * -9 + embedding[8] * 4 + embedding[9] * 18;
    let score_3 = embedding[0] * 18 + embedding[1] * 4 + embedding[2] * -3 + embedding[3] * -11 + embedding[4] * 13 + embedding[5] * 4 + embedding[6] * 11 + embedding[7] * -13 + embedding[8] * -12 + embedding[9] * 7;
    let score_4 = embedding[0] * -2 + embedding[1] * -7 + embedding[2] * 4 + embedding[3] * -10 + embedding[4] * 1 + embedding[5] * -9 + embedding[6] * -9 + embedding[7] * -5 + embedding[8] * 18 + embedding[9] * -16;
    let score_5 = embedding[0] * 1 + embedding[2] * -9 + embedding[3] * -8 + embedding[4] * 18 + embedding[5] * 13 + embedding[6] * 4 + embedding[7] * -10 + embedding[8] * -2 + embedding[9] * -3;
    let score_6 = embedding[0] * 26 + embedding[1] * 9 + embedding[2] * 13 + embedding[3] * -7 + embedding[4] * 10 + embedding[5] * -9 + embedding[6] * 19 + embedding[7] * -13 + embedding[8] * -22 + embedding[9] * 10;
    let score_7 = embedding[0] * 3 + embedding[1] * 5 + embedding[2] * -26 + embedding[3] * -10 + embedding[4] * 7 + embedding[5] * 18 + embedding[6] * 12 + embedding[7] * 22 + embedding[9] * 7;
    let score_8 = embedding[0] * -19 + embedding[1] * 16 + embedding[2] * 16 + embedding[3] * 49 + embedding[4] * -21 + embedding[5] * 3 + embedding[6] * -41 + embedding[7] * -3 + embedding[8] * -45 + embedding[9] * 3;
    let score_9 = embedding[0] * -11 + embedding[1] * 16 + embedding[2] * 11 + embedding[3] * 29 + embedding[4] * -36 + embedding[5] * -4 + embedding[6] * -37 + embedding[7] * 4 + embedding[8] * -52 + embedding[9] * -21;
    let score_10 = embedding[0] * -5 + embedding[1] * -16 + embedding[2] * 21 + embedding[3] * 6 + embedding[4] * 12 + embedding[5] * -12 + embedding[6] * 22 + embedding[7] * -5 + embedding[8] * 28 + embedding[9] * 31;
    let score_11 = embedding[0] * -3 + embedding[1] * -14 + embedding[2] * 8 + embedding[3] * 16 + embedding[4] * 14 + embedding[5] * -8 + embedding[6] * -1 + embedding[7] * 9 + embedding[8] * 18 + embedding[9] * 37;
    let score_12 = embedding[0] * -17 + embedding[1] * 9 + embedding[2] * 32 + embedding[3] * 34 + embedding[4] * -37 + embedding[5] * -16 + embedding[6] * -44 + embedding[7] * -1 + embedding[8] * -38 + embedding[9] * -18;
    let score_13 = embedding[0] * -4 + embedding[1] * 16 + embedding[2] * -4 + embedding[3] * 31 + embedding[4] * -32 + embedding[5] * 2 + embedding[6] * -34 + embedding[7] * 6 + embedding[8] * -24 + embedding[9] * -6;
    let score_14 = embedding[0] * -11 + embedding[1] * -16 + embedding[2] * 37 + embedding[3] * 32 + embedding[4] * -2 + embedding[5] * -38 + embedding[6] * -10 + embedding[7] * 3 + embedding[8] * 23 + embedding[9] * 41;
    let score_15 = embedding[0] * 7 + embedding[1] * -4 + embedding[2] * -6 + embedding[3] * -2 + embedding[4] * -17 + embedding[5] * -3 + embedding[6] * 10 + embedding[7] * -11 + embedding[8] * 11 + embedding[9] * -2;
    let score_16 = embedding[0] * -3 + embedding[1] * 6 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -10 + embedding[9] * 6;
    let score_17 = embedding[0] * -3 + embedding[1] * 6 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -10 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 3;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 39;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 26;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 32;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 33;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 38;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 23;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 79;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 80;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 123;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 93;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_494(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -14 + embedding[1] * -7 + embedding[2] * -17 + embedding[3] * -12 + embedding[4] * 12 + embedding[5] * 24 + embedding[7] * -9 + embedding[8] * 15 + embedding[9] * -15;
    let score_1 = embedding[0] * 14 + embedding[1] * 4 + embedding[2] * -11 + embedding[3] * -26 + embedding[4] * -3 + embedding[5] * -2 + embedding[6] * 11 + embedding[7] * 4 + embedding[8] * -3 + embedding[9] * -24;
    let score_2 = embedding[0] * 15 + embedding[1] * -3 + embedding[2] * -5 + embedding[3] * -25 + embedding[4] * 6 + embedding[5] * -7 + embedding[6] * 25 + embedding[8] * 13 + embedding[9] * -6;
    let score_3 = embedding[0] * 2 + embedding[1] * -2 + embedding[2] * 1 + embedding[3] * 19 + embedding[4] * 13 + embedding[5] * 6 + embedding[6] * -7 + embedding[7] * -8 + embedding[8] * -5 + embedding[9] * 32;
    let score_4 = embedding[0] * 18 + embedding[1] * 4 + embedding[2] * -6 + embedding[3] * -9 + embedding[4] * 10 + embedding[5] * 2 + embedding[6] * 8 + embedding[7] * -11 + embedding[8] * -13 + embedding[9] * 6;
    let score_5 = embedding[0] * 7 + embedding[1] * -2 + embedding[2] * 11 + embedding[3] * -16 + embedding[4] * 1 + embedding[5] * -17 + embedding[6] * -3 + embedding[7] * -3 + embedding[8] * 11 + embedding[9] * -18;
    let score_6 = embedding[0] * -10 + embedding[1] * 14 + embedding[2] * 12 + embedding[3] * 41 + embedding[4] * -17 + embedding[5] * 3 + embedding[6] * -33 + embedding[7] * -2 + embedding[8] * -42 + embedding[9] * 6;
    let score_7 = embedding[0] * -7 + embedding[1] * 16 + embedding[2] * 7 + embedding[3] * 23 + embedding[4] * -28 + embedding[5] * -2 + embedding[6] * -30 + embedding[7] * 6 + embedding[8] * -43 + embedding[9] * -20;
    let score_8 = embedding[1] * -14 + embedding[2] * 17 + embedding[3] * 9 + embedding[4] * 13 + embedding[5] * -12 + embedding[6] * 21 + embedding[7] * -2 + embedding[8] * 23 + embedding[9] * 39;
    let score_9 = embedding[0] * 9 + embedding[1] * -10 + embedding[2] * 7 + embedding[3] * 19 + embedding[4] * 11 + embedding[5] * -14 + embedding[6] * 1 + embedding[7] * 13 + embedding[8] * 11 + embedding[9] * 50;
    let score_10 = embedding[0] * -14 + embedding[1] * 7 + embedding[2] * 25 + embedding[3] * 26 + embedding[4] * -30 + embedding[5] * -15 + embedding[6] * -37 + embedding[7] * -2 + embedding[8] * -28 + embedding[9] * -19;
    let score_11 = embedding[1] * 15 + embedding[2] * -5 + embedding[3] * 28 + embedding[4] * -29 + embedding[5] * 3 + embedding[6] * -30 + embedding[7] * 5 + embedding[8] * -22 + embedding[9] * -6;
    let score_12 = embedding[0] * -2 + embedding[1] * -16 + embedding[2] * 35 + embedding[3] * 38 + embedding[4] * -2 + embedding[5] * -40 + embedding[6] * -11 + embedding[7] * 7 + embedding[8] * 18 + embedding[9] * 50;
    let score_13 = embedding[0] * 8 + embedding[2] * -11 + embedding[3] * -2 + embedding[4] * -14 + embedding[5] * -3 + embedding[6] * 9 + embedding[7] * -8 + embedding[8] * 10 + embedding[9] * -6;
    let score_14 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 15 + embedding[8] * -9 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 32;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 33;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 38;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 23;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 79;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 80;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 123;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 93;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_495(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 24 + embedding[1] * -5 + embedding[2] * -3 + embedding[3] * -14 + embedding[4] * 22 + embedding[5] * 9 + embedding[6] * -11 + embedding[7] * -19 + embedding[8] * -14 + embedding[9] * -3;
    let score_1 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * -19 + embedding[2] * 21 + embedding[3] * -7 + embedding[4] * -58 + embedding[5] * 21 + embedding[6] * 47 + embedding[7] * -47 + embedding[8] * -18 + embedding[9] * -5;
    let score_3 = embedding[0] * -12 + embedding[1] * 15 + embedding[2] * -6 + embedding[3] * 23 + embedding[4] * -5 + embedding[5] * -14 + embedding[6] * -16 + embedding[7] * 58 + embedding[8] * 39 + embedding[9] * 2;
    let score_4 = embedding[0] * 25 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * -8 + embedding[4] * 15 + embedding[5] * 8 + embedding[6] * 21 + embedding[7] * -13 + embedding[8] * -27 + embedding[9] * 5;
    let score_5 = embedding[0] * -24 + embedding[1] * -6 + embedding[2] * -9 + embedding[3] * 18 + embedding[4] * -5 + embedding[5] * 3 + embedding[6] * -8 + embedding[7] * 44 + embedding[8] * 26 + embedding[9] * -6;
    let score_6 = embedding[0] * -14 + embedding[1] * 21 + embedding[2] * -9 + embedding[3] * -8 + embedding[4] * -6 + embedding[5] * -14 + embedding[6] * -26 + embedding[7] * 54 + embedding[8] * 21 + embedding[9] * 11;
    let score_7 = embedding[0] * -19 + embedding[1] * 12 + embedding[2] * -22 + embedding[3] * 14 + embedding[4] * -15 + embedding[5] * -6 + embedding[6] * -5 + embedding[7] * 25 + embedding[8] * 17 + embedding[9] * 1;
    let score_8 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_9 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_10 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_11 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_12 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 18 + embedding[3] * 16 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[0] * -7 + embedding[1] * -7 + embedding[2] * -23 + embedding[3] * -8 + embedding[4] * 19 + embedding[5] * 4 + embedding[6] * -19 + embedding[7] * 8 + embedding[8] * 7 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 45;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 29;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 35;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_496(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_1 = embedding[0] * -3 + embedding[1] * -16 + embedding[2] * 22 + embedding[3] * -9 + embedding[4] * -20 + embedding[5] * 21 + embedding[6] * 15 + embedding[7] * -23 + embedding[8] * -10 + embedding[9] * -2;
    let score_2 = embedding[0] * 7 + embedding[1] * -2 + embedding[2] * 18 + embedding[3] * -6 + embedding[4] * -29 + embedding[5] * 18 + embedding[6] * -7 + embedding[7] * 13 + embedding[8] * 1 + embedding[9] * 11;
    let score_3 = embedding[0] * -2 + embedding[1] * 14 + embedding[2] * 2 + embedding[3] * 19 + embedding[4] * -4 + embedding[5] * -14 + embedding[6] * -13 + embedding[7] * 42 + embedding[8] * 31 + embedding[9] * 5;
    let score_4 = embedding[0] * 13 + embedding[1] * 6 + embedding[2] * 2 + embedding[3] * -4 + embedding[4] * 18 + embedding[5] * 2 + embedding[6] * 18 + embedding[7] * -5 + embedding[8] * -21 + embedding[9] * 2;
    let score_5 = embedding[0] * -11 + embedding[1] * -8 + embedding[2] * -1 + embedding[3] * 14 + embedding[4] * -6 + embedding[5] * 4 + embedding[6] * -2 + embedding[7] * 26 + embedding[8] * 16 + embedding[9] * -2;
    let score_6 = embedding[0] * -7 + embedding[1] * 16 + embedding[2] * -3 + embedding[3] * -5 + embedding[4] * -11 + embedding[5] * -10 + embedding[6] * -17 + embedding[7] * 36 + embedding[8] * 16 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 12 + embedding[2] * -13 + embedding[3] * 9 + embedding[4] * -10 + embedding[5] * -3 + embedding[6] * -5 + embedding[7] * 10 + embedding[8] * 10 + embedding[9] * 5;
    let score_8 = embedding[0] * -17 + embedding[1] * 9 + embedding[2] * 28 + embedding[3] * -8 + embedding[4] * -8 + embedding[5] * 1 + embedding[6] * 5 + embedding[7] * -1 + embedding[8] * 6 + embedding[9] * -4;
    let score_9 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_10 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_11 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_12 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_13 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_14 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_15 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_16 = embedding[0] * -9 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_17 = embedding[0] * 12 + embedding[1] * -8 + embedding[2] * -20 + embedding[3] * -8 + embedding[4] * 24 + embedding[6] * -16 + embedding[7] * -13 + embedding[8] * -5 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 20;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 6;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 29;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 35;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 52;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_497(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 10;
    let score_1 = embedding[0] * 8 + embedding[1] * -4 + embedding[2] * -3 + embedding[3] * -11 + embedding[4] * 4 + embedding[5] * 18 + embedding[6] * -3 + embedding[7] * -8 + embedding[8] * 4;
    let score_2 = embedding[0] * 9 + embedding[1] * -14 + embedding[2] * 65 + embedding[3] * -6 + embedding[4] * -7 + embedding[5] * 16 + embedding[6] * 10 + embedding[7] * 31 + embedding[8] * -24 + embedding[9] * 5;
    let score_3 = embedding[0] * -6 + embedding[1] * 12 + embedding[2] * -25 + embedding[3] * 13 + embedding[4] * -18 + embedding[5] * -6 + embedding[7] * -7 + embedding[8] * 13;
    let score_4 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_5 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 10;
    let score_6 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_7 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_8 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_9 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_10 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 10;
    let score_11 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_12 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_13 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_14 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_15 = embedding[0] * -7 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_16 = embedding[0] * -8 + embedding[1] * 5 + embedding[2] * 23 + embedding[3] * 18 + embedding[4] * -23 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -6 + embedding[9] * 10;
    let score_17 = embedding[0] * 6 + embedding[1] * -3 + embedding[2] * -23 + embedding[3] * -11 + embedding[4] * 14 + embedding[5] * 6 + embedding[6] * -16 + embedding[7] * -17 + embedding[8] * 13 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 35;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_498(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_1 = embedding[0] * -3 + embedding[1] * -14 + embedding[2] * 14 + embedding[3] * -14 + embedding[4] * -8 + embedding[5] * 18 + embedding[6] * 7 + embedding[7] * -18 + embedding[8] * -1 + embedding[9] * -6;
    let score_2 = embedding[0] * -1 + embedding[1] * 12 + embedding[2] * -22 + embedding[3] * 8 + embedding[4] * -7 + embedding[5] * -4 + embedding[6] * -7 + embedding[8] * 10;
    let score_3 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_4 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_5 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_6 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_7 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_8 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_9 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_10 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_11 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_12 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_13 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_14 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_15 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_16 = embedding[0] * -6 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_17 = embedding[0] * 12 + embedding[1] * -3 + embedding[2] * -21 + embedding[3] * -15 + embedding[4] * 25 + embedding[5] * 6 + embedding[6] * -23 + embedding[7] * -9 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 35;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_499(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -24 + embedding[5] * 14 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 30 + embedding[5] * 6 + embedding[6] * -28 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 45;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_500(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 14 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 6 + embedding[6] * -28 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 45;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_501(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 14 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 6 + embedding[6] * -28 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 45;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_502(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 13 + embedding[6] * 10 + embedding[7] * -26 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 5 + embedding[6] * -28 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_503(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_504(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_505(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_506(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_507(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -6 + embedding[1] * -14 + embedding[2] * -5 + embedding[3] * -11 + embedding[4] * 7 + embedding[5] * 9 + embedding[6] * -4 + embedding[7] * -10 + embedding[8] * 1 + embedding[9] * -7;
    let score_2 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -9 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 19 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 10 + embedding[1] * 1 + embedding[2] * -16 + embedding[3] * -15 + embedding[4] * 19 + embedding[5] * 12 + embedding[6] * -12 + embedding[7] * -17 + embedding[8] * 12 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_508(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_1 = embedding[0] * -1 + embedding[1] * -18 + embedding[2] * 4 + embedding[3] * -11 + embedding[4] * 11 + embedding[5] * 14 + embedding[6] * -3 + embedding[7] * -3 + embedding[8] * -7 + embedding[9] * -7;
    let score_2 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_3 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_6 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_7 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_11 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 19 + embedding[8] * -9 + embedding[9] * 10;
    let score_17 = embedding[0] * 9 + embedding[1] * 4 + embedding[2] * -19 + embedding[3] * -16 + embedding[4] * 20 + embedding[5] * 10 + embedding[6] * -15 + embedding[7] * -18 + embedding[8] * 16 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_509(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_1 = embedding[0] * 1 + embedding[1] * -12 + embedding[2] * 7 + embedding[3] * -10 + embedding[4] * 6 + embedding[5] * 17 + embedding[6] * -1 + embedding[7] * -12 + embedding[8] * -5 + embedding[9] * -6;
    let score_2 = embedding[0] * 3 + embedding[1] * -17 + embedding[2] * 67 + embedding[3] * -10 + embedding[5] * 15 + embedding[6] * 11 + embedding[7] * 24 + embedding[8] * -33 + embedding[9] * 2;
    let score_3 = embedding[0] * 2 + embedding[1] * 13 + embedding[3] * 25 + embedding[4] * -3 + embedding[5] * -13 + embedding[6] * -6 + embedding[7] * 21 + embedding[8] * 33;
    let score_4 = embedding[0] * -13 + embedding[1] * -3 + embedding[3] * 12 + embedding[4] * -9 + embedding[5] * 8 + embedding[6] * 1 + embedding[7] * 18 + embedding[8] * 21 + embedding[9] * -3;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 22 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 10;
    let score_17 = embedding[0] * 6 + embedding[1] * 3 + embedding[2] * -25 + embedding[3] * -18 + embedding[4] * 19 + embedding[5] * 4 + embedding[6] * -15 + embedding[7] * -22 + embedding[8] * 3 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_510(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 28 + embedding[1] * -12 + embedding[2] * 2 + embedding[3] * -20 + embedding[4] * 28 + embedding[5] * 12 + embedding[6] * -1 + embedding[7] * -39 + embedding[8] * -28 + embedding[9] * -9;
    let score_1 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_2 = embedding[0] * -13 + embedding[1] * 12 + embedding[2] * -1 + embedding[3] * 33 + embedding[4] * -14 + embedding[5] * -10 + embedding[6] * -10 + embedding[7] * 50 + embedding[8] * 43 + embedding[9] * -2;
    let score_3 = embedding[0] * -28 + embedding[1] * -2 + embedding[2] * -6 + embedding[3] * 21 + embedding[4] * -18 + embedding[5] * 10 + embedding[6] * -5 + embedding[7] * 44 + embedding[8] * 30 + embedding[9] * -5;
    let score_4 = embedding[0] * -8 + embedding[1] * -1 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_5 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_6 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_7 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_8 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_9 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_10 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_11 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_12 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_13 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_14 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_15 = embedding[0] * -8 + embedding[1] * -1 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_16 = embedding[0] * -8 + embedding[1] * -1 + embedding[2] * 20 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -5 + embedding[9] * 5;
    let score_17 = embedding[0] * -12 + embedding[2] * -26 + embedding[3] * -13 + embedding[4] * 9 + embedding[5] * 10 + embedding[6] * -19 + embedding[7] * 5 + embedding[8] * 12 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 15;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_511(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_1 = embedding[0] * 22 + embedding[1] * 4 + embedding[3] * -19 + embedding[4] * -2 + embedding[5] * 6 + embedding[6] * -8 + embedding[7] * -13 + embedding[8] * -35 + embedding[9] * -22;
    let score_2 = embedding[0] * 20 + embedding[1] * -15 + embedding[2] * 6 + embedding[3] * -19 + embedding[4] * 18 + embedding[5] * 1 + embedding[6] * 20 + embedding[7] * -21 + embedding[8] * -1 + embedding[9] * 11;
    let score_3 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * 8 + embedding[3] * 11 + embedding[4] * 12 + embedding[5] * 14 + embedding[6] * -18 + embedding[7] * -24 + embedding[8] * -34 + embedding[9] * 15;
    let score_4 = embedding[0] * 8 + embedding[1] * -9 + embedding[2] * 1 + embedding[3] * -16 + embedding[4] * 7 + embedding[5] * 27 + embedding[6] * 8 + embedding[7] * -17 + embedding[8] * -13 + embedding[9] * -3;
    let score_5 = embedding[0] * 17 + embedding[1] * -5 + embedding[2] * 29 + embedding[3] * -7 + embedding[4] * 8 + embedding[5] * -15 + embedding[6] * -19 + embedding[7] * -27 + embedding[8] * -19 + embedding[9] * -7;
    let score_6 = embedding[0] * -32 + embedding[1] * 14 + embedding[2] * -10 + embedding[3] * 38 + embedding[4] * -16 + embedding[5] * -7 + embedding[6] * -3 + embedding[7] * 65 + embedding[8] * 57 + embedding[9] * -5;
    let score_7 = embedding[0] * -47 + embedding[1] * -1 + embedding[2] * -14 + embedding[3] * 26 + embedding[4] * -21 + embedding[5] * 13 + embedding[6] * 2 + embedding[7] * 60 + embedding[8] * 45 + embedding[9] * -8;
    let score_8 = embedding[0] * 17 + embedding[1] * -16 + embedding[2] * 6 + embedding[3] * 3 + embedding[4] * 14 + embedding[5] * -3 + embedding[6] * -2 + embedding[7] * -2 + embedding[8] * -5 + embedding[9] * 31;
    let score_9 = embedding[0] * 11 + embedding[1] * 5 + embedding[2] * -2 + embedding[3] * 10 + embedding[4] * -15 + embedding[5] * 7 + embedding[6] * -25 + embedding[7] * -11 + embedding[8] * -28 + embedding[9] * -7;
    let score_10 = embedding[0] * 18 + embedding[1] * -10 + embedding[2] * 2 + embedding[3] * -3 + embedding[4] * -11 + embedding[5] * 1 + embedding[6] * 2 + embedding[7] * -22 + embedding[8] * -6;
    let score_11 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_12 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 12 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * -31 + embedding[1] * 2 + embedding[2] * -34 + embedding[3] * -8 + embedding[4] * 6 + embedding[5] * 12 + embedding[6] * -13 + embedding[7] * 21 + embedding[8] * 26 + embedding[9] * -9;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 15;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 23;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 100;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 93;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_512(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -14 + embedding[1] * -6 + embedding[2] * -17 + embedding[3] * -7 + embedding[4] * 12 + embedding[5] * 21 + embedding[6] * -11 + embedding[7] * -4 + embedding[8] * 13 + embedding[9] * -13;
    let score_1 = embedding[0] * 15 + embedding[1] * 5 + embedding[2] * -9 + embedding[3] * -16 + embedding[4] * -3 + embedding[5] * 1 + embedding[6] * -4 + embedding[7] * -3 + embedding[8] * -12 + embedding[9] * -19;
    let score_2 = embedding[0] * 13 + embedding[1] * -5 + embedding[2] * -1 + embedding[3] * -15 + embedding[4] * 10 + embedding[5] * -6 + embedding[6] * 14 + embedding[7] * -8 + embedding[8] * 9 + embedding[9] * 5;
    let score_3 = embedding[0] * 1 + embedding[1] * -1 + embedding[2] * -4 + embedding[3] * 10 + embedding[4] * 8 + embedding[5] * 7 + embedding[6] * -14 + embedding[7] * -13 + embedding[8] * -11 + embedding[9] * 17;
    let score_4 = embedding[0] * 6 + embedding[1] * -4 + embedding[2] * 16 + embedding[3] * -6 + embedding[4] * 3 + embedding[5] * -16 + embedding[6] * -17 + embedding[7] * -11 + embedding[8] * 3 + embedding[9] * -8;
    let score_5 = embedding[0] * 15 + embedding[1] * 14 + embedding[2] * 11 + embedding[3] * -9 + embedding[4] * 9 + embedding[5] * 3 + embedding[6] * 27 + embedding[7] * -5 + embedding[8] * -17 + embedding[9] * 5;
    let score_6 = embedding[0] * 8 + embedding[1] * 1 + embedding[2] * -8 + embedding[3] * -11 + embedding[4] * 8 + embedding[5] * 4 + embedding[6] * -3 + embedding[7] * -8 + embedding[8] * -1 + embedding[9] * -4;
    let score_7 = embedding[0] * 10 + embedding[1] * -8 + embedding[2] * -1 + embedding[3] * 6 + embedding[4] * 7 + embedding[5] * -8 + embedding[6] * -6 + embedding[7] * 10 + embedding[8] * 8 + embedding[9] * 29;
    let score_8 = embedding[0] * 3 + embedding[1] * 6 + embedding[2] * -10 + embedding[3] * 10 + embedding[4] * -14 + embedding[5] * 4 + embedding[6] * -21 + embedding[8] * -4 + embedding[9] * -6;
    let score_9 = embedding[0] * 8 + embedding[1] * -1 + embedding[2] * -6 + embedding[3] * -1 + embedding[4] * -14 + embedding[5] * -6 + embedding[7] * -10 + embedding[8] * 5 + embedding[9] * -3;
    let score_10 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_11 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_12 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * -5 + embedding[1] * 8 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 109;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 100;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 93;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_513(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 11 + embedding[1] * -11 + embedding[2] * -6 + embedding[3] * -21 + embedding[4] * 11 + embedding[5] * -6 + embedding[6] * 21 + embedding[7] * -6 + embedding[8] * 26 + embedding[9] * 3;
    let score_1 = embedding[0] * 10 + embedding[1] * 6 + embedding[2] * -14 + embedding[3] * -21 + embedding[4] * -9 + embedding[5] * 5 + embedding[6] * -7 + embedding[7] * 3 + embedding[8] * -8 + embedding[9] * -37;
    let score_2 = embedding[0] * -11 + embedding[1] * -4 + embedding[2] * -7 + embedding[3] * 24 + embedding[4] * 16 + embedding[5] * 21 + embedding[6] * -24 + embedding[7] * -14 + embedding[8] * -6 + embedding[9] * 23;
    let score_3 = embedding[0] * 1 + embedding[1] * -4 + embedding[2] * 15 + embedding[3] * -6 + embedding[4] * 4 + embedding[5] * -15 + embedding[6] * -25 + embedding[7] * -14 + embedding[8] * 12 + embedding[9] * -16;
    let score_4 = embedding[0] * -1 + embedding[1] * 1 + embedding[2] * -8 + embedding[3] * -3 + embedding[4] * 18 + embedding[5] * 19 + embedding[6] * -7 + embedding[7] * -12 + embedding[8] * -4 + embedding[9] * -4;
    let score_5 = embedding[0] * 12 + embedding[1] * 15 + embedding[2] * 11 + embedding[3] * -7 + embedding[4] * 14 + embedding[5] * 11 + embedding[6] * 25 + embedding[7] * -6 + embedding[8] * -19 + embedding[9] * 3;
    let score_6 = embedding[0] * 7 + embedding[1] * -10 + embedding[3] * 13 + embedding[4] * 13 + embedding[5] * -2 + embedding[6] * -11 + embedding[7] * 7 + embedding[8] * 9 + embedding[9] * 35;
    let score_7 = embedding[0] * 4 + embedding[1] * 9 + embedding[2] * -8 + embedding[3] * 18 + embedding[4] * -10 + embedding[5] * 8 + embedding[6] * -26 + embedding[7] * -2 + embedding[8] * -11 + embedding[9] * -3;
    let score_8 = embedding[0] * 12 + embedding[1] * -2 + embedding[2] * -5 + embedding[3] * -6 + embedding[4] * -12 + embedding[5] * -4 + embedding[6] * 4 + embedding[7] * -13 + embedding[8] * 9 + embedding[9] * -8;
    let score_9 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -14 + embedding[9] * 7;
    let score_10 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -14 + embedding[9] * 7;
    let score_11 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -14 + embedding[9] * 7;
    let score_12 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -14 + embedding[9] * 7;
    let score_13 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -14 + embedding[9] * 7;
    let score_14 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -14 + embedding[9] * 7;
    let score_15 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -14 + embedding[9] * 7;
    let score_16 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -14 + embedding[9] * 7;
    let score_17 = embedding[0] * -1 + embedding[1] * 4 + embedding[2] * 14 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -14 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 23;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 100;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 93;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_514(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 19 + embedding[1] * -1 + embedding[2] * -10 + embedding[3] * -12 + embedding[4] * 20 + embedding[5] * 9 + embedding[6] * -7 + embedding[7] * -20 + embedding[8] * -6 + embedding[9] * -4;
    let score_1 = embedding[1] * -10 + embedding[2] * -11 + embedding[3] * -12 + embedding[4] * 1 + embedding[5] * 7 + embedding[6] * 6 + embedding[7] * 8 + embedding[8] * 24 + embedding[9] * -1;
    let score_2 = embedding[0] * -4 + embedding[2] * -13 + embedding[3] * -11 + embedding[4] * -14 + embedding[5] * 13 + embedding[6] * -9 + embedding[7] * 16 + embedding[8] * 6 + embedding[9] * -24;
    let score_3 = embedding[0] * -12 + embedding[1] * -7 + embedding[2] * -9 + embedding[3] * 11 + embedding[4] * 4 + embedding[5] * 19 + embedding[6] * -17 + embedding[8] * 12 + embedding[9] * 10;
    let score_4 = embedding[0] * -8 + embedding[1] * -6 + embedding[2] * 7 + embedding[3] * 1 + embedding[4] * -4 + embedding[6] * -23 + embedding[7] * 1 + embedding[8] * 17 + embedding[9] * -13;
    let score_5 = embedding[0] * 20 + embedding[1] * 15 + embedding[2] * 9 + embedding[3] * -6 + embedding[4] * 15 + embedding[5] * 7 + embedding[6] * 25 + embedding[7] * -12 + embedding[8] * -22 + embedding[9] * 6;
    let score_6 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_7 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_8 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_9 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_10 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_11 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_12 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_13 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_14 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_15 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_16 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_515(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 1 + embedding[1] * -5 + embedding[2] * -16 + embedding[3] * -13 + embedding[4] * 14 + embedding[5] * 12 + embedding[6] * -15 + embedding[7] * -11 + embedding[8] * 11 + embedding[9] * -9;
    let score_1 = embedding[0] * 9 + embedding[1] * -1 + embedding[2] * -12 + embedding[3] * -10 + embedding[4] * 12 + embedding[5] * 9 + embedding[6] * -6 + embedding[7] * -10 + embedding[8] * 2 + embedding[9] * -4;
    let score_2 = embedding[0] * 15 + embedding[1] * 5 + embedding[2] * 2 + embedding[3] * -7 + embedding[4] * 9 + embedding[5] * -4 + embedding[7] * -12 + embedding[8] * -5 + embedding[9] * -1;
    let score_3 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_4 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_5 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_6 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_7 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_8 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_9 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_10 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_11 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_12 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_13 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_14 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_15 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_16 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let score_17 = embedding[0] * -5 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -10 + embedding[9] * 9;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 7;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 39;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_516(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 2 + embedding[1] * -5 + embedding[2] * -16 + embedding[3] * -13 + embedding[4] * 15 + embedding[5] * 12 + embedding[6] * -14 + embedding[7] * -11 + embedding[8] * 11 + embedding[9] * -9;
    let score_1 = embedding[0] * 9 + embedding[1] * -1 + embedding[2] * -12 + embedding[3] * -10 + embedding[4] * 12 + embedding[5] * 8 + embedding[6] * -6 + embedding[7] * -10 + embedding[8] * 3 + embedding[9] * -3;
    let score_2 = embedding[0] * 15 + embedding[1] * 5 + embedding[2] * 2 + embedding[3] * -7 + embedding[4] * 10 + embedding[5] * -4 + embedding[6] * 1 + embedding[7] * -12 + embedding[8] * -5 + embedding[9] * -1;
    let score_3 = embedding[0] * -4 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_4 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_5 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_6 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_7 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_8 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_9 = embedding[0] * -4 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_10 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_11 = embedding[0] * -4 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_12 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_13 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_14 = embedding[0] * -4 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_15 = embedding[0] * -4 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_16 = embedding[0] * -4 + embedding[1] * 6 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_17 = embedding[0] * -4 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 7;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 39;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_517(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 1 + embedding[1] * -5 + embedding[2] * -16 + embedding[3] * -13 + embedding[4] * 15 + embedding[5] * 12 + embedding[6] * -15 + embedding[7] * -11 + embedding[8] * 11 + embedding[9] * -9;
    let score_1 = embedding[0] * 9 + embedding[1] * -1 + embedding[2] * -11 + embedding[3] * -11 + embedding[4] * 12 + embedding[5] * 9 + embedding[6] * -6 + embedding[7] * -10 + embedding[8] * 3 + embedding[9] * -3;
    let score_2 = embedding[0] * 15 + embedding[1] * 5 + embedding[2] * 2 + embedding[3] * -7 + embedding[4] * 10 + embedding[5] * -4 + embedding[7] * -12 + embedding[8] * -5 + embedding[9] * -1;
    let score_3 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_4 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_5 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_6 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_7 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_8 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_9 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_10 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_11 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_12 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_13 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_14 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_15 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_16 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_17 = embedding[0] * -5 + embedding[1] * 7 + embedding[2] * 20 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -12 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 7;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 39;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_518(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 24 + embedding[1] * -8 + embedding[2] * 2 + embedding[3] * -16 + embedding[4] * 22 + embedding[5] * 11 + embedding[6] * -5 + embedding[7] * -28 + embedding[8] * -17 + embedding[9] * -6;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_2 = embedding[0] * -15 + embedding[1] * 8 + embedding[2] * -2 + embedding[3] * 36 + embedding[4] * -17 + embedding[5] * -10 + embedding[6] * -7 + embedding[7] * 47 + embedding[8] * 41 + embedding[9] * -2;
    let score_3 = embedding[0] * -30 + embedding[1] * -6 + embedding[2] * -6 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * 9 + embedding[6] * -2 + embedding[7] * 40 + embedding[8] * 28 + embedding[9] * -5;
    let score_4 = embedding[0] * 25 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * -9 + embedding[4] * 17 + embedding[5] * 9 + embedding[6] * 27 + embedding[7] * -20 + embedding[8] * -32 + embedding[9] * 2;
    let score_5 = embedding[0] * -26 + embedding[1] * 12 + embedding[2] * -24 + embedding[3] * 14 + embedding[4] * -27 + embedding[5] * -2 + embedding[6] * -2 + embedding[7] * 17 + embedding[8] * 16 + embedding[9] * 2;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 19 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[0] * -11 + embedding[1] * -7 + embedding[2] * -25 + embedding[3] * -14 + embedding[4] * 9 + embedding[5] * 10 + embedding[6] * -18 + embedding[7] * 2 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 15;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 7;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 35;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_519(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_1 = embedding[0] * 5 + embedding[1] * -7 + embedding[2] * -2 + embedding[3] * -13 + embedding[4] * 10 + embedding[5] * 12 + embedding[6] * -6 + embedding[7] * -14 + embedding[8] * -3 + embedding[9] * -5;
    let score_2 = embedding[0] * 4 + embedding[1] * 7 + embedding[2] * 5 + embedding[3] * 28 + embedding[4] * -5 + embedding[5] * -14 + embedding[6] * -6 + embedding[7] * 31 + embedding[8] * 28;
    let score_3 = embedding[0] * 9 + embedding[1] * 6 + embedding[2] * 7 + embedding[3] * -8 + embedding[4] * 10 + embedding[5] * 8 + embedding[6] * 18 + embedding[7] * -6 + embedding[8] * -15 + embedding[9] * 3;
    let score_4 = embedding[0] * -8 + embedding[1] * -8 + embedding[2] * 3 + embedding[3] * 12 + embedding[4] * -7 + embedding[5] * 6 + embedding[6] * -3 + embedding[7] * 20 + embedding[8] * 16 + embedding[9] * -4;
    let score_5 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * -12 + embedding[3] * 11 + embedding[4] * -15 + embedding[5] * -3 + embedding[6] * -1 + embedding[7] * 4 + embedding[8] * 7 + embedding[9] * 3;
    let score_6 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_7 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_8 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 19 + embedding[4] * -22 + embedding[5] * -8 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -7 + embedding[9] * 10;
    let score_17 = embedding[0] * 7 + embedding[1] * -7 + embedding[2] * -13 + embedding[3] * -14 + embedding[4] * 14 + embedding[5] * 7 + embedding[6] * -14 + embedding[7] * -13 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 15;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 7;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 35;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_520(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_1 = embedding[0] * 3 + embedding[1] * -7 + embedding[2] * 11 + embedding[3] * -8 + embedding[4] * -12 + embedding[5] * 18 + embedding[6] * -4 + embedding[7] * -5 + embedding[8] * 3;
    let score_2 = embedding[0] * -13 + embedding[1] * -15 + embedding[2] * 19 + embedding[3] * -8 + embedding[4] * -53 + embedding[5] * 18 + embedding[6] * 31 + embedding[7] * -32 + embedding[8] * -8 + embedding[9] * -1;
    let score_3 = embedding[0] * 11 + embedding[1] * -9 + embedding[2] * 37 + embedding[3] * -11 + embedding[4] * 10 + embedding[5] * 4 + embedding[6] * -6 + embedding[7] * 25 + embedding[8] * -17 + embedding[9] * 5;
    let score_4 = embedding[0] * 5 + embedding[1] * 12 + embedding[2] * -4 + embedding[3] * 30 + embedding[4] * 2 + embedding[5] * -20 + embedding[6] * -12 + embedding[7] * 34 + embedding[8] * 31 + embedding[9] * 1;
    let score_5 = embedding[0] * 12 + embedding[1] * 9 + embedding[2] * 4 + embedding[3] * -8 + embedding[4] * 18 + embedding[5] * 4 + embedding[6] * 12 + embedding[7] * 1 + embedding[8] * -15 + embedding[9] * 6;
    let score_6 = embedding[0] * -10 + embedding[1] * -6 + embedding[2] * 1 + embedding[3] * 13 + embedding[4] * -9 + embedding[5] * 6 + embedding[6] * -4 + embedding[7] * 21 + embedding[8] * 18 + embedding[9] * -2;
    let score_7 = embedding[0] * -6 + embedding[1] * 15 + embedding[2] * -19 + embedding[3] * 12 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * -3 + embedding[7] * 5 + embedding[8] * 9 + embedding[9] * 6;
    let score_8 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_9 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_10 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_11 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_12 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_13 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_14 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_15 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_16 = embedding[0] * -9 + embedding[1] * 5 + embedding[2] * 20 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_17 = embedding[0] * 9 + embedding[1] * -1 + embedding[2] * -22 + embedding[3] * -11 + embedding[4] * 22 + embedding[6] * -21 + embedding[7] * -9 + embedding[8] * 2 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 45;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 9;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 15;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 6;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 35;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_521(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_1 = embedding[0] * 3 + embedding[1] * -9 + embedding[2] * 5 + embedding[3] * -14 + embedding[4] * 3 + embedding[5] * 17 + embedding[6] * 3 + embedding[7] * -14 + embedding[8] * -1 + embedding[9] * -5;
    let score_2 = embedding[0] * -2 + embedding[1] * 9 + embedding[2] * -18 + embedding[3] * 8 + embedding[4] * -11 + embedding[5] * -3 + embedding[6] * -4 + embedding[7] * -2 + embedding[8] * 8;
    let score_3 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_4 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_5 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_6 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_7 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_8 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_9 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_15 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 10;
    let score_17 = embedding[0] * 9 + embedding[1] * -6 + embedding[2] * -16 + embedding[3] * -14 + embedding[4] * 19 + embedding[5] * 7 + embedding[6] * -19 + embedding[7] * -11 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 35;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_522(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_1 = embedding[0] * -6 + embedding[1] * -12 + embedding[2] * 7 + embedding[3] * -12 + embedding[4] * -28 + embedding[5] * 13 + embedding[6] * 14 + embedding[7] * -26 + embedding[8] * 1 + embedding[9] * -3;
    let score_2 = embedding[0] * -2 + embedding[1] * 11 + embedding[2] * -19 + embedding[3] * 5 + embedding[4] * -11 + embedding[5] * -4 + embedding[6] * -6 + embedding[8] * 9 + embedding[9] * 3;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 21 + embedding[3] * 20 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 11;
    let score_17 = embedding[0] * 12 + embedding[1] * -4 + embedding[2] * -20 + embedding[3] * -15 + embedding[4] * 28 + embedding[5] * 6 + embedding[6] * -24 + embedding[7] * -5 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 35;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_523(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 13 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_17 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 5 + embedding[6] * -27 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 88;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_524(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 13 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_3 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_6 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_7 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_9 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 5 + embedding[6] * -28 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 45;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_525(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 14 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -3;
    let score_2 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 6 + embedding[6] * -27 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 88;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_526(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 13 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 5 + embedding[6] * -28 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_527(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_528(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_529(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_530(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_531(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_532(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_1 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 51 + embedding[3] * -9 + embedding[4] * 5 + embedding[5] * 10 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -19 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 9;
    let score_17 = embedding[0] * 6 + embedding[1] * -2 + embedding[2] * -36 + embedding[3] * -15 + embedding[4] * 23 + embedding[5] * 10 + embedding[6] * -18 + embedding[7] * -24 + embedding[8] * 19 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_533(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_1 = embedding[0] * 7 + embedding[1] * -12 + embedding[2] * 56 + embedding[3] * -11 + embedding[4] * 2 + embedding[5] * 9 + embedding[6] * 6 + embedding[7] * 23 + embedding[8] * -31 + embedding[9] * 4;
    let score_2 = embedding[0] * 1 + embedding[1] * 9 + embedding[2] * 4 + embedding[3] * 25 + embedding[4] * -4 + embedding[5] * -11 + embedding[6] * -4 + embedding[7] * 24 + embedding[8] * 31;
    let score_3 = embedding[0] * 4 + embedding[1] * -4 + embedding[2] * -8 + embedding[3] * -10 + embedding[4] * 7 + embedding[5] * 12 + embedding[6] * -6 + embedding[7] * -16 + embedding[8] * -1 + embedding[9] * -4;
    let score_4 = embedding[0] * -12 + embedding[1] * -3 + embedding[2] * 1 + embedding[3] * 15 + embedding[4] * -10 + embedding[5] * 9 + embedding[6] * 2 + embedding[7] * 20 + embedding[8] * 20 + embedding[9] * -2;
    let score_5 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_6 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_7 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_8 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_9 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_10 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_11 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_12 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_13 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_14 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_15 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_16 = embedding[0] * -10 + embedding[1] * 3 + embedding[2] * 22 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 10;
    let score_17 = embedding[0] * 6 + embedding[1] * -1 + embedding[2] * -21 + embedding[3] * -17 + embedding[4] * 19 + embedding[5] * 8 + embedding[6] * -13 + embedding[7] * -20 + embedding[8] * 1 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 9;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 15;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_534(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 25 + embedding[1] * -6 + embedding[2] * -3 + embedding[3] * -16 + embedding[4] * 22 + embedding[5] * 12 + embedding[6] * -4 + embedding[7] * -33 + embedding[8] * -19 + embedding[9] * -7;
    let score_1 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_2 = embedding[0] * -12 + embedding[1] * 7 + embedding[2] * 2 + embedding[3] * 29 + embedding[4] * -10 + embedding[5] * -8 + embedding[6] * -7 + embedding[7] * 44 + embedding[8] * 39 + embedding[9] * -3;
    let score_3 = embedding[0] * -27 + embedding[1] * -7 + embedding[2] * -3 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * 12 + embedding[6] * -3 + embedding[7] * 39 + embedding[8] * 26 + embedding[9] * -6;
    let score_4 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_5 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_6 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_7 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_8 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_9 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_10 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_11 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_12 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_13 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_14 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_15 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_16 = embedding[0] * -8 + embedding[2] * 20 + embedding[3] * 18 + embedding[4] * -21 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -6 + embedding[9] * 6;
    let score_17 = embedding[0] * -11 + embedding[1] * -6 + embedding[2] * -23 + embedding[3] * -17 + embedding[4] * 13 + embedding[5] * 12 + embedding[6] * -16 + embedding[8] * 7 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 15;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 6;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_535(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_1 = embedding[0] * 26 + embedding[1] * 9 + embedding[3] * -18 + embedding[4] * 10 + embedding[6] * 2 + embedding[7] * -15 + embedding[8] * -35 + embedding[9] * -12;
    let score_2 = embedding[0] * 26 + embedding[1] * -2 + embedding[2] * 7 + embedding[3] * -19 + embedding[4] * 23 + embedding[5] * -7 + embedding[6] * 18 + embedding[7] * -19 + embedding[8] * -12 + embedding[9] * 9;
    let score_3 = embedding[0] * 19 + embedding[1] * 4 + embedding[2] * 4 + embedding[4] * 20 + embedding[5] * 3 + embedding[6] * -5 + embedding[7] * -23 + embedding[8] * -38 + embedding[9] * 14;
    let score_4 = embedding[0] * 19 + embedding[2] * 24 + embedding[3] * -7 + embedding[4] * 14 + embedding[5] * -17 + embedding[6] * -9 + embedding[7] * -24 + embedding[8] * -20 + embedding[9] * -7;
    let score_5 = embedding[0] * -42 + embedding[1] * 3 + embedding[2] * -3 + embedding[3] * 38 + embedding[4] * -28 + embedding[5] * -1 + embedding[6] * -10 + embedding[7] * 64 + embedding[8] * 57 + embedding[9] * -6;
    let score_6 = embedding[0] * -12 + embedding[1] * -13 + embedding[2] * -5 + embedding[3] * -1 + embedding[4] * -5 + embedding[5] * 24 + embedding[6] * -3 + embedding[7] * -6 + embedding[8] * 4 + embedding[9] * -12;
    let score_7 = embedding[0] * -57 + embedding[1] * -11 + embedding[2] * -7 + embedding[3] * 26 + embedding[4] * -33 + embedding[5] * 19 + embedding[6] * -5 + embedding[7] * 59 + embedding[8] * 45 + embedding[9] * -9;
    let score_8 = embedding[0] * 22 + embedding[1] * 2 + embedding[2] * 1 + embedding[3] * -14 + embedding[4] * 17 + embedding[5] * -1 + embedding[6] * 6 + embedding[7] * -20 + embedding[8] * -26 + embedding[9] * 3;
    let score_9 = embedding[0] * 21 + embedding[1] * -5 + embedding[2] * 4 + embedding[3] * 1 + embedding[4] * 18 + embedding[5] * -11 + embedding[6] * 2 + embedding[7] * 2 + embedding[8] * -12 + embedding[9] * 30;
    let score_10 = embedding[0] * 6 + embedding[1] * 5 + embedding[2] * -9 + embedding[3] * 6 + embedding[4] * -12 + embedding[5] * 4 + embedding[6] * -23 + embedding[7] * -5 + embedding[8] * -15 + embedding[9] * -5;
    let score_11 = embedding[0] * 15 + embedding[1] * -3 + embedding[2] * -4 + embedding[3] * -2 + embedding[4] * -9 + embedding[5] * -4 + embedding[6] * 3 + embedding[7] * -17 + embedding[8] * -5 + embedding[9] * -2;
    let score_12 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -4 + embedding[1] * 8 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * -41 + embedding[1] * -10 + embedding[2] * -27 + embedding[3] * -7 + embedding[4] * -6 + embedding[5] * 19 + embedding[6] * -19 + embedding[7] * 21 + embedding[8] * 26 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 15;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 1;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 6;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 111;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 23;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 100;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 93;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_536(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * -2 + embedding[2] * -15 + embedding[3] * -12 + embedding[4] * 8 + embedding[5] * 18 + embedding[6] * -3 + embedding[7] * -3 + embedding[8] * 10 + embedding[9] * -8;
    let score_1 = embedding[0] * 11 + embedding[1] * 6 + embedding[2] * -8 + embedding[3] * -13 + embedding[4] * 2 + embedding[5] * 4 + embedding[6] * -8 + embedding[7] * -6 + embedding[8] * -10 + embedding[9] * -16;
    let score_2 = embedding[0] * 9 + embedding[1] * -4 + embedding[2] * -2 + embedding[3] * -14 + embedding[4] * 13 + embedding[6] * 8 + embedding[7] * -10 + embedding[8] * 11 + embedding[9] * 3;
    let score_3 = embedding[0] * 4 + embedding[2] * -3 + embedding[3] * 3 + embedding[4] * 10 + embedding[5] * 7 + embedding[6] * -14 + embedding[7] * -13 + embedding[8] * -11 + embedding[9] * 6;
    let score_4 = embedding[0] * 7 + embedding[1] * -2 + embedding[2] * 16 + embedding[3] * -3 + embedding[4] * 7 + embedding[5] * -11 + embedding[6] * -15 + embedding[7] * -14 + embedding[8] * -1 + embedding[9] * -9;
    let score_5 = embedding[0] * 6 + embedding[1] * -1 + embedding[2] * -8 + embedding[3] * -10 + embedding[4] * 8 + embedding[5] * 6 + embedding[6] * -2 + embedding[7] * -9 + embedding[9] * -3;
    let score_6 = embedding[0] * 10 + embedding[1] * -6 + embedding[3] * 4 + embedding[4] * 9 + embedding[5] * -8 + embedding[6] * -4 + embedding[7] * 8 + embedding[8] * 8 + embedding[9] * 23;
    let score_7 = embedding[0] * 7 + embedding[1] * 8 + embedding[2] * -5 + embedding[3] * 9 + embedding[4] * -12 + embedding[5] * 2 + embedding[6] * -18 + embedding[7] * -1 + embedding[8] * -10 + embedding[9] * -5;
    let score_8 = embedding[0] * 9 + embedding[1] * -1 + embedding[2] * -3 + embedding[3] * -1 + embedding[4] * -13 + embedding[5] * -3 + embedding[6] * 1 + embedding[7] * -11 + embedding[8] * 3 + embedding[9] * -1;
    let score_9 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let score_10 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let score_11 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let score_12 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -13 + embedding[9] * 6;
    let score_13 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let score_14 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -13 + embedding[9] * 6;
    let score_15 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -13 + embedding[9] * 6;
    let score_16 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -12 + embedding[9] * 6;
    let score_17 = embedding[0] * -3 + embedding[1] * 4 + embedding[2] * 15 + embedding[3] * 16 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 12 + embedding[7] * 13 + embedding[8] * -13 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 111;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 23;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 100;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 93;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_537(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 6 + embedding[1] * -6 + embedding[2] * -5 + embedding[3] * -13 + embedding[4] * 10 + embedding[5] * 1 + embedding[6] * 10 + embedding[7] * -5 + embedding[8] * 15 + embedding[9] * 3;
    let score_1 = embedding[0] * 8 + embedding[1] * 3 + embedding[2] * -11 + embedding[3] * -14 + embedding[4] * -2 + embedding[5] * 6 + embedding[6] * -6 + embedding[7] * -1 + embedding[8] * -6 + embedding[9] * -20;
    let score_2 = embedding[0] * -5 + embedding[1] * -2 + embedding[2] * -7 + embedding[3] * 9 + embedding[4] * 10 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -10 + embedding[8] * -3 + embedding[9] * 11;
    let score_3 = embedding[0] * 2 + embedding[1] * -5 + embedding[2] * 12 + embedding[3] * -3 + embedding[4] * 3 + embedding[5] * -8 + embedding[6] * -17 + embedding[7] * -10 + embedding[8] * 7 + embedding[9] * -10;
    let score_4 = embedding[0] * 3 + embedding[1] * 3 + embedding[2] * -4 + embedding[3] * -7 + embedding[4] * 14 + embedding[5] * 12 + embedding[6] * 1 + embedding[7] * -9 + embedding[8] * -4 + embedding[9] * -3;
    let score_5 = embedding[0] * 4 + embedding[1] * 2 + embedding[2] * -7 + embedding[3] * -9 + embedding[4] * 8 + embedding[5] * 7 + embedding[6] * -4 + embedding[7] * -7 + embedding[8] * 1 + embedding[9] * -4;
    let score_6 = embedding[0] * 3 + embedding[2] * -8 + embedding[3] * -8 + embedding[4] * 9 + embedding[5] * 7 + embedding[6] * -3 + embedding[7] * -8 + embedding[9] * -3;
    let score_7 = embedding[0] * 8 + embedding[1] * -7 + embedding[2] * 2 + embedding[3] * 8 + embedding[4] * 7 + embedding[5] * -7 + embedding[6] * -5 + embedding[7] * 10 + embedding[8] * 8 + embedding[9] * 26;
    let score_8 = embedding[0] * 6 + embedding[1] * 7 + embedding[2] * -6 + embedding[3] * 11 + embedding[4] * -11 + embedding[5] * 2 + embedding[6] * -17 + embedding[8] * -8 + embedding[9] * -2;
    let score_9 = embedding[0] * 8 + embedding[1] * -1 + embedding[2] * -3 + embedding[3] * 1 + embedding[4] * -13 + embedding[5] * -4 + embedding[6] * 1 + embedding[7] * -10 + embedding[8] * 3 + embedding[9] * -2;
    let score_10 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 8;
    let score_11 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 8;
    let score_12 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 8;
    let score_13 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 8;
    let score_14 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 8;
    let score_15 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 8;
    let score_16 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 8;
    let score_17 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -16 + embedding[5] * -10 + embedding[6] * 11 + embedding[7] * 14 + embedding[8] * -11 + embedding[9] * 8;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 1;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 109;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 111;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 23;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 100;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 93;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_538(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 15 + embedding[2] * -8 + embedding[3] * -10 + embedding[4] * 18 + embedding[5] * 8 + embedding[6] * -4 + embedding[7] * -20 + embedding[8] * -6 + embedding[9] * -4;
    let score_1 = embedding[0] * -4 + embedding[1] * -9 + embedding[2] * -9 + embedding[3] * -10 + embedding[5] * 6 + embedding[6] * 8 + embedding[7] * 7 + embedding[8] * 22;
    let score_2 = embedding[0] * -6 + embedding[2] * -11 + embedding[3] * -10 + embedding[4] * -14 + embedding[5] * 12 + embedding[6] * -5 + embedding[7] * 14 + embedding[8] * 6 + embedding[9] * -22;
    let score_3 = embedding[0] * -14 + embedding[1] * -6 + embedding[2] * -7 + embedding[3] * 11 + embedding[4] * 4 + embedding[5] * 17 + embedding[6] * -12 + embedding[8] * 12 + embedding[9] * 9;
    let score_4 = embedding[0] * -11 + embedding[1] * -6 + embedding[2] * 8 + embedding[3] * 1 + embedding[4] * -5 + embedding[6] * -18 + embedding[8] * 16 + embedding[9] * -12;
    let score_5 = embedding[0] * -9 + embedding[1] * -1 + embedding[2] * -8 + embedding[3] * -4 + embedding[4] * -1 + embedding[5] * 14 + embedding[6] * -5 + embedding[7] * 4 + embedding[8] * 11 + embedding[9] * -7;
    let score_6 = embedding[0] * -11 + embedding[1] * -1 + embedding[2] * -8 + embedding[3] * -1 + embedding[4] * -1 + embedding[5] * 12 + embedding[6] * -6 + embedding[7] * 5 + embedding[8] * 10 + embedding[9] * -8;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 4;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 4;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 4;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 4;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 4;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 4;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 4;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 4;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 4;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 4;
    let score_17 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 16 + embedding[3] * 18 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 13 + embedding[7] * 16 + embedding[8] * -5 + embedding[9] * 4;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 109;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 111;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_539(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let score_1 = embedding[0] * 21 + embedding[1] * 3 + embedding[2] * -2 + embedding[3] * -9 + embedding[4] * 19 + embedding[5] * 3 + embedding[6] * -2 + embedding[7] * -19 + embedding[8] * -11;
    let score_2 = embedding[0] * 1 + embedding[1] * -9 + embedding[2] * -8 + embedding[3] * -10 + embedding[4] * 2 + embedding[5] * 1 + embedding[6] * 6 + embedding[7] * 4 + embedding[8] * 21 + embedding[9] * 1;
    let score_3 = embedding[0] * -1 + embedding[2] * -10 + embedding[3] * -10 + embedding[4] * -12 + embedding[5] * 7 + embedding[6] * -7 + embedding[7] * 11 + embedding[8] * 5 + embedding[9] * -21;
    let score_4 = embedding[0] * -10 + embedding[1] * -5 + embedding[2] * -6 + embedding[3] * 11 + embedding[4] * 6 + embedding[5] * 11 + embedding[6] * -14 + embedding[7] * -3 + embedding[8] * 10 + embedding[9] * 10;
    let score_5 = embedding[0] * -6 + embedding[1] * -6 + embedding[2] * 9 + embedding[3] * 1 + embedding[4] * -4 + embedding[5] * -6 + embedding[6] * -20 + embedding[7] * -3 + embedding[8] * 15 + embedding[9] * -11;
    let score_6 = embedding[0] * 21 + embedding[1] * 17 + embedding[2] * 13 + embedding[3] * -4 + embedding[4] * 15 + embedding[5] * 2 + embedding[6] * 27 + embedding[7] * -14 + embedding[8] * -24 + embedding[9] * 8;
    let score_7 = embedding[0] * -5 + embedding[1] * -1 + embedding[2] * -7 + embedding[3] * -5 + embedding[5] * 8 + embedding[6] * -7 + embedding[7] * 1 + embedding[8] * 10 + embedding[9] * -6;
    let score_8 = embedding[0] * -7 + embedding[1] * -1 + embedding[2] * -7 + embedding[3] * -1 + embedding[4] * 1 + embedding[5] * 7 + embedding[6] * -9 + embedding[7] * 2 + embedding[8] * 9 + embedding[9] * -6;
    let score_9 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let score_10 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let score_11 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let score_12 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let score_13 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let score_14 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let score_15 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let score_16 = embedding[0] * -7 + embedding[1] * 7 + embedding[2] * 15 + embedding[3] * 15 + embedding[4] * -18 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 14 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * 19 + embedding[1] * 2 + embedding[2] * -6 + embedding[3] * -10 + embedding[4] * 19 + embedding[5] * 2 + embedding[6] * -7 + embedding[7] * -21 + embedding[8] * -7 + embedding[9] * -1;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 3;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 4;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 5;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 7;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 109;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 111;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_540(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 4 + embedding[1] * -4 + embedding[2] * -14 + embedding[3] * -12 + embedding[4] * 15 + embedding[5] * 12 + embedding[6] * -11 + embedding[7] * -12 + embedding[8] * 8 + embedding[9] * -8;
    let score_1 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_2 = embedding[0] * 10 + embedding[2] * -9 + embedding[3] * -10 + embedding[4] * 12 + embedding[5] * 5 + embedding[6] * -3 + embedding[7] * -12 + embedding[8] * 2 + embedding[9] * -4;
    let score_3 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_4 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_5 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_6 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_7 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_8 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_9 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_10 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 6;
    let score_11 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_12 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_13 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_14 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_15 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_16 = embedding[0] * -5 + embedding[1] * 4 + embedding[2] * 18 + embedding[3] * 24 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 13 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 6;
    let score_17 = embedding[0] * 8 + embedding[1] * -2 + embedding[2] * -12 + embedding[3] * -12 + embedding[4] * 14 + embedding[5] * 5 + embedding[6] * -11 + embedding[7] * -15 + embedding[8] * 4 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 7;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_541(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_1 = embedding[0] * 8 + embedding[2] * -15 + embedding[3] * -13 + embedding[4] * 12 + embedding[5] * 6 + embedding[6] * -10 + embedding[7] * -15 + embedding[8] * -1 + embedding[9] * -4;
    let score_2 = embedding[0] * 9 + embedding[1] * -8 + embedding[2] * 59 + embedding[3] * -10 + embedding[4] * -1 + embedding[5] * 9 + embedding[6] * 11 + embedding[7] * 25 + embedding[8] * -32 + embedding[9] * 5;
    let score_3 = embedding[1] * 9 + embedding[2] * 5 + embedding[3] * 29 + embedding[4] * -9 + embedding[5] * -13 + embedding[6] * -7 + embedding[7] * 27 + embedding[8] * 33 + embedding[9] * -1;
    let score_4 = embedding[0] * -13 + embedding[1] * -4 + embedding[2] * 1 + embedding[3] * 18 + embedding[4] * -14 + embedding[5] * 5 + embedding[6] * -1 + embedding[7] * 23 + embedding[8] * 22 + embedding[9] * -4;
    let score_5 = embedding[0] * 9 + embedding[1] * 10 + embedding[2] * 7 + embedding[3] * -9 + embedding[4] * 10 + embedding[5] * 9 + embedding[6] * 18 + embedding[7] * -6 + embedding[8] * -17 + embedding[9] * 4;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 16 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 9;
    let score_17 = embedding[0] * 5 + embedding[1] * -2 + embedding[2] * -18 + embedding[3] * -13 + embedding[4] * 12 + embedding[5] * 4 + embedding[6] * -16 + embedding[7] * -17 + embedding[8] * 3 + embedding[9] * -6;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_542(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_1 = embedding[0] * 5 + embedding[1] * -3 + embedding[2] * -9 + embedding[3] * -11 + embedding[4] * 13 + embedding[5] * 8 + embedding[6] * -10 + embedding[7] * -15 + embedding[8] * -3 + embedding[9] * -5;
    let score_2 = embedding[0] * 9 + embedding[1] * -11 + embedding[2] * 62 + embedding[3] * -10 + embedding[4] * 1 + embedding[5] * 10 + embedding[6] * 11 + embedding[7] * 26 + embedding[8] * -35 + embedding[9] * 5;
    let score_3 = embedding[1] * 10 + embedding[2] * 5 + embedding[3] * 32 + embedding[4] * -11 + embedding[5] * -13 + embedding[6] * -3 + embedding[7] * 28 + embedding[8] * 32;
    let score_4 = embedding[0] * -14 + embedding[1] * -5 + embedding[2] * 2 + embedding[3] * 19 + embedding[4] * -14 + embedding[5] * 5 + embedding[6] * 1 + embedding[7] * 23 + embedding[8] * 21 + embedding[9] * -3;
    let score_5 = embedding[0] * 8 + embedding[1] * 7 + embedding[2] * 9 + embedding[3] * -9 + embedding[4] * 12 + embedding[5] * 10 + embedding[6] * 17 + embedding[7] * -6 + embedding[8] * -17 + embedding[9] * 4;
    let score_6 = embedding[0] * -9 + embedding[1] * 14 + embedding[2] * -15 + embedding[3] * 12 + embedding[4] * -20 + embedding[5] * -5 + embedding[6] * 1 + embedding[7] * 1 + embedding[8] * 9 + embedding[9] * 4;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_17 = embedding[0] * 6 + embedding[1] * -4 + embedding[2] * -17 + embedding[3] * -14 + embedding[4] * 15 + embedding[5] * 5 + embedding[6] * -14 + embedding[7] * -16 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 15;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 35;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_543(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 11;
    let score_1 = embedding[0] * 9 + embedding[1] * -12 + embedding[2] * 61 + embedding[3] * -11 + embedding[4] * 1 + embedding[5] * 11 + embedding[6] * 12 + embedding[7] * 24 + embedding[8] * -34 + embedding[9] * 5;
    let score_2 = embedding[0] * -1 + embedding[1] * 9 + embedding[2] * 4 + embedding[3] * 31 + embedding[4] * -11 + embedding[5] * -12 + embedding[6] * -3 + embedding[7] * 27 + embedding[8] * 33 + embedding[9] * 1;
    let score_3 = embedding[0] * 4 + embedding[1] * -7 + embedding[2] * -5 + embedding[3] * -8 + embedding[4] * 5 + embedding[5] * 11 + embedding[6] * -6 + embedding[7] * -13 + embedding[8] * -1 + embedding[9] * -4;
    let score_4 = embedding[0] * -14 + embedding[1] * -5 + embedding[2] * 2 + embedding[3] * 18 + embedding[4] * -14 + embedding[5] * 7 + embedding[6] * 2 + embedding[7] * 22 + embedding[8] * 21 + embedding[9] * -2;
    let score_5 = embedding[0] * 8 + embedding[1] * 7 + embedding[2] * 7 + embedding[3] * -10 + embedding[4] * 13 + embedding[5] * 10 + embedding[6] * 19 + embedding[7] * -6 + embedding[8] * -17 + embedding[9] * 5;
    let score_6 = embedding[0] * -10 + embedding[1] * 14 + embedding[2] * -16 + embedding[3] * 12 + embedding[4] * -20 + embedding[5] * -4 + embedding[6] * 2 + embedding[7] * 1 + embedding[8] * 9 + embedding[9] * 5;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 11;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 11;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 11;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 11;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 11;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 11;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 11;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 11;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 11;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 17 + embedding[4] * -22 + embedding[5] * -9 + embedding[6] * 16 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 11;
    let score_17 = embedding[0] * 6 + embedding[1] * -4 + embedding[2] * -17 + embedding[3] * -15 + embedding[4] * 16 + embedding[5] * 6 + embedding[6] * -14 + embedding[7] * -17 + embedding[8] * 1 + embedding[9] * -5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 9;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 15;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 1;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 6;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 7;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 35;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_544(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_1 = embedding[0] * 4 + embedding[1] * -9 + embedding[2] * 4 + embedding[3] * -14 + embedding[4] * 3 + embedding[5] * 16 + embedding[6] * 4 + embedding[7] * -14 + embedding[8] * -1 + embedding[9] * -5;
    let score_2 = embedding[0] * -2 + embedding[1] * 9 + embedding[2] * -18 + embedding[3] * 8 + embedding[4] * -9 + embedding[5] * -3 + embedding[6] * -4 + embedding[7] * -1 + embedding[8] * 9 + embedding[9] * -1;
    let score_3 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_4 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_5 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_6 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_7 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_8 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_9 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_15 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 23 + embedding[3] * 20 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 10;
    let score_17 = embedding[0] * 9 + embedding[1] * -6 + embedding[2] * -16 + embedding[3] * -14 + embedding[4] * 21 + embedding[5] * 7 + embedding[6] * -19 + embedding[7] * -10 + embedding[8] * 9 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 35;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_545(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 13 + embedding[6] * 11 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 5 + embedding[6] * -27 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 45;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_546(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 14 + embedding[6] * 10 + embedding[7] * -26 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 6 + embedding[6] * -28 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 45;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_547(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -14 + embedding[4] * -25 + embedding[5] * 14 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 6 + embedding[6] * -28 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 45;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_548(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 14 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 1 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -8 + embedding[9] * 9;
    let score_17 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 6 + embedding[6] * -28 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 88;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_549(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -9 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 13 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 13 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 6 + embedding[6] * -28 + embedding[7] * -7 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_550(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_551(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -7 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_552(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_553(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_554(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_555(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -24 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_556(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 51 + embedding[3] * -9 + embedding[4] * 6 + embedding[5] * 11 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -20 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 6 + embedding[1] * -2 + embedding[2] * -36 + embedding[3] * -15 + embedding[4] * 23 + embedding[5] * 10 + embedding[6] * -18 + embedding[7] * -24 + embedding[8] * 18 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_557(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * 7 + embedding[1] * -14 + embedding[2] * 51 + embedding[3] * -9 + embedding[4] * 5 + embedding[5] * 11 + embedding[6] * 4 + embedding[7] * 23 + embedding[8] * -20 + embedding[9] * -2;
    let score_2 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -7 + embedding[1] * 3 + embedding[2] * 24 + embedding[3] * 19 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 18 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 5 + embedding[1] * -1 + embedding[2] * -35 + embedding[3] * -15 + embedding[4] * 22 + embedding[5] * 10 + embedding[6] * -18 + embedding[7] * -23 + embedding[8] * 18 + embedding[9] * -11;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 9;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_558(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 41 + embedding[1] * 4 + embedding[2] * -1 + embedding[3] * -17 + embedding[4] * 30 + embedding[5] * 2 + embedding[6] * -6 + embedding[7] * -32 + embedding[8] * -17;
    let score_1 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_2 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_3 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_4 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_5 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_6 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_7 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -20 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_8 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_9 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_10 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_11 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_12 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_13 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_14 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_15 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_16 = embedding[0] * -8 + embedding[1] * 8 + embedding[2] * 18 + embedding[3] * 17 + embedding[4] * -21 + embedding[5] * -10 + embedding[6] * 16 + embedding[7] * 16 + embedding[8] * -7 + embedding[9] * 5;
    let score_17 = embedding[0] * -34 + embedding[1] * -12 + embedding[2] * -29 + embedding[3] * -9 + embedding[4] * -5 + embedding[5] * 23 + embedding[6] * -20 + embedding[7] * 14 + embedding[8] * 33 + embedding[9] * -16;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_559(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -10 + embedding[9] * 8;
    let score_1 = embedding[0] * 30 + embedding[1] * 21 + embedding[2] * -5 + embedding[3] * -26 + embedding[4] * -6 + embedding[5] * -2 + embedding[6] * -3 + embedding[7] * -5 + embedding[8] * -44 + embedding[9] * -47;
    let score_2 = embedding[0] * 31 + embedding[1] * -11 + embedding[2] * 6 + embedding[3] * -32 + embedding[4] * 29 + embedding[5] * -20 + embedding[6] * 47 + embedding[7] * -18 + embedding[8] * 22 + embedding[9] * 17;
    let score_3 = embedding[0] * 13 + embedding[1] * 10 + embedding[2] * 3 + embedding[3] * 32 + embedding[4] * 26 + embedding[5] * 12 + embedding[6] * -20 + embedding[7] * -27 + embedding[8] * -46 + embedding[9] * 37;
    let score_4 = embedding[0] * 20 + embedding[1] * 2 + embedding[2] * 37 + embedding[3] * -11 + embedding[4] * 12 + embedding[5] * -37 + embedding[6] * -25 + embedding[7] * -27 + embedding[8] * -13 + embedding[9] * -17;
    let score_5 = embedding[0] * 20 + embedding[1] * -11 + embedding[3] * 9 + embedding[4] * 27 + embedding[5] * -12 + embedding[6] * 1 + embedding[7] * 1 + embedding[8] * 3 + embedding[9] * 50;
    let score_6 = embedding[0] * 5 + embedding[1] * 15 + embedding[2] * -14 + embedding[3] * 19 + embedding[4] * -16 + embedding[5] * 10 + embedding[6] * -39 + embedding[7] * -2 + embedding[8] * -26 + embedding[9] * -10;
    let score_7 = embedding[0] * 19 + embedding[1] * -3 + embedding[2] * -7 + embedding[3] * -12 + embedding[4] * -9 + embedding[5] * -9 + embedding[6] * 11 + embedding[7] * -19 + embedding[8] * 13 + embedding[9] * -8;
    let score_8 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -10 + embedding[9] * 8;
    let score_9 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -10 + embedding[9] * 8;
    let score_10 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -10 + embedding[9] * 8;
    let score_11 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -10 + embedding[9] * 8;
    let score_12 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -10 + embedding[9] * 8;
    let score_13 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -10 + embedding[9] * 8;
    let score_14 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -10 + embedding[9] * 8;
    let score_15 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -10 + embedding[9] * 8;
    let score_16 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 14 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 14 + embedding[7] * 13 + embedding[8] * -10 + embedding[9] * 8;
    let score_17 = embedding[0] * -64 + embedding[1] * -19 + embedding[2] * -42 + embedding[3] * -8 + embedding[4] * -19 + embedding[5] * 43 + embedding[6] * -25 + embedding[7] * 29 + embedding[8] * 56 + embedding[9] * -29;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 23;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 100;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 93;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_560(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -64 + embedding[1] * -19 + embedding[2] * -42 + embedding[3] * -8 + embedding[4] * -20 + embedding[5] * 43 + embedding[6] * -25 + embedding[7] * 29 + embedding[8] * 57 + embedding[9] * -29;
    let score_1 = embedding[0] * 30 + embedding[1] * 21 + embedding[2] * -5 + embedding[3] * -26 + embedding[4] * -6 + embedding[5] * -2 + embedding[6] * -3 + embedding[7] * -5 + embedding[8] * -44 + embedding[9] * -47;
    let score_2 = embedding[0] * 31 + embedding[1] * -11 + embedding[2] * 6 + embedding[3] * -32 + embedding[4] * 29 + embedding[5] * -19 + embedding[6] * 47 + embedding[7] * -17 + embedding[8] * 23 + embedding[9] * 17;
    let score_3 = embedding[0] * 13 + embedding[1] * 10 + embedding[2] * 3 + embedding[3] * 32 + embedding[4] * 26 + embedding[5] * 12 + embedding[6] * -20 + embedding[7] * -27 + embedding[8] * -46 + embedding[9] * 37;
    let score_4 = embedding[0] * 20 + embedding[1] * 2 + embedding[2] * 37 + embedding[3] * -11 + embedding[4] * 12 + embedding[5] * -36 + embedding[6] * -25 + embedding[7] * -27 + embedding[8] * -13 + embedding[9] * -17;
    let score_5 = embedding[0] * 20 + embedding[1] * -11 + embedding[3] * 9 + embedding[4] * 27 + embedding[5] * -12 + embedding[6] * 1 + embedding[7] * 1 + embedding[8] * 4 + embedding[9] * 50;
    let score_6 = embedding[0] * 5 + embedding[1] * 15 + embedding[2] * -14 + embedding[3] * 19 + embedding[4] * -16 + embedding[5] * 11 + embedding[6] * -39 + embedding[7] * -2 + embedding[8] * -25 + embedding[9] * -10;
    let score_7 = embedding[0] * 19 + embedding[1] * -3 + embedding[2] * -7 + embedding[3] * -12 + embedding[4] * -10 + embedding[5] * -9 + embedding[6] * 11 + embedding[7] * -18 + embedding[8] * 14 + embedding[9] * -9;
    let score_8 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -9 + embedding[9] * 7;
    let score_9 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -9 + embedding[9] * 7;
    let score_10 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -9 + embedding[9] * 7;
    let score_11 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -9 + embedding[9] * 7;
    let score_12 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -9 + embedding[9] * 7;
    let score_13 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -9 + embedding[9] * 7;
    let score_14 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -9 + embedding[9] * 7;
    let score_15 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -9 + embedding[9] * 7;
    let score_16 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -9 + embedding[9] * 7;
    let score_17 = embedding[0] * -5 + embedding[1] * 10 + embedding[2] * 14 + embedding[3] * 15 + embedding[4] * -17 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 13 + embedding[8] * -9 + embedding[9] * 7;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 2;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 23;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 100;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 93;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_561(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 16 + embedding[1] * -19 + embedding[2] * -5 + embedding[3] * -36 + embedding[4] * 24 + embedding[5] * -9 + embedding[6] * 47 + embedding[7] * -8 + embedding[8] * 47 + embedding[9] * 14;
    let score_1 = embedding[0] * 12 + embedding[1] * 17 + embedding[2] * -21 + embedding[3] * -32 + embedding[4] * -17 + embedding[5] * 10 + embedding[6] * -10 + embedding[7] * 8 + embedding[8] * -26 + embedding[9] * -63;
    let score_2 = embedding[0] * -14 + embedding[1] * -1 + embedding[2] * -9 + embedding[3] * 40 + embedding[4] * 22 + embedding[5] * 33 + embedding[6] * -37 + embedding[7] * -19 + embedding[8] * -18 + embedding[9] * 41;
    let score_3 = embedding[0] * 1 + embedding[1] * -2 + embedding[2] * 23 + embedding[3] * -10 + embedding[4] * 5 + embedding[5] * -22 + embedding[6] * -35 + embedding[7] * -17 + embedding[8] * 11 + embedding[9] * -26;
    let score_4 = embedding[0] * 6 + embedding[1] * -15 + embedding[2] * -5 + embedding[3] * 10 + embedding[4] * 25 + embedding[5] * 1 + embedding[6] * -3 + embedding[7] * 5 + embedding[8] * 15 + embedding[9] * 49;
    let score_5 = embedding[0] * 1 + embedding[1] * 17 + embedding[2] * -13 + embedding[3] * 19 + embedding[4] * -15 + embedding[5] * 16 + embedding[6] * -38 + embedding[7] * -4 + embedding[8] * -28 + embedding[9] * -7;
    let score_6 = embedding[0] * 16 + embedding[1] * -1 + embedding[2] * -4 + embedding[3] * -15 + embedding[4] * -11 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * -17 + embedding[8] * 17 + embedding[9] * -13;
    let score_7 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 8;
    let score_8 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 8;
    let score_9 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 8;
    let score_10 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 8;
    let score_11 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 8;
    let score_12 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 8;
    let score_13 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 8;
    let score_14 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 8;
    let score_15 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 8;
    let score_16 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 8;
    let score_17 = embedding[0] * -2 + embedding[1] * 5 + embedding[2] * 15 + embedding[3] * 17 + embedding[4] * -15 + embedding[5] * -9 + embedding[6] * 13 + embedding[7] * 14 + embedding[8] * -14 + embedding[9] * 8;
    let mut best_score = score_0;
    let mut best_palette = 2;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 3;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 4;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 5;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 23;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 100;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 93;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_562(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * 78 + embedding[1] * 15 + embedding[2] * 10 + embedding[3] * -25 + embedding[4] * 40 + embedding[5] * -12 + embedding[6] * 10 + embedding[7] * -53 + embedding[8] * -60 + embedding[9] * 14;
    let score_1 = embedding[0] * -26 + embedding[1] * -23 + embedding[2] * -18 + embedding[3] * -13 + embedding[4] * -8 + embedding[5] * 12 + embedding[6] * 22 + embedding[7] * 22 + embedding[8] * 52 + embedding[9] * -3;
    let score_2 = embedding[0] * -36 + embedding[1] * -1 + embedding[2] * -25 + embedding[3] * -10 + embedding[4] * -33 + embedding[5] * 23 + embedding[6] * -13 + embedding[7] * 34 + embedding[8] * 13 + embedding[9] * -44;
    let score_3 = embedding[0] * -48 + embedding[1] * -10 + embedding[2] * -15 + embedding[3] * 32 + embedding[5] * 37 + embedding[6] * -30 + embedding[7] * 14 + embedding[8] * 22 + embedding[9] * 18;
    let score_4 = embedding[0] * -37 + embedding[1] * -9 + embedding[2] * 7 + embedding[3] * 10 + embedding[4] * -9 + embedding[5] * 2 + embedding[6] * -36 + embedding[7] * 11 + embedding[8] * 34 + embedding[9] * -22;
    let score_5 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_6 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_7 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_8 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_9 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_10 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_11 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_12 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_13 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_14 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_15 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_16 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let score_17 = embedding[0] * -9 + embedding[1] * 9 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -19 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -7 + embedding[9] * 6;
    let mut best_score = score_0;
    let mut best_palette = 1;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_563(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 6;
    let score_1 = embedding[0] * -33 + embedding[1] * -26 + embedding[2] * -21 + embedding[3] * -10 + embedding[4] * -9 + embedding[5] * 13 + embedding[6] * 17 + embedding[7] * 26 + embedding[8] * 55 + embedding[9] * -4;
    let score_2 = embedding[0] * -44 + embedding[1] * -6 + embedding[2] * -27 + embedding[3] * -6 + embedding[4] * -33 + embedding[5] * 25 + embedding[6] * -16 + embedding[7] * 37 + embedding[8] * 19 + embedding[9] * -43;
    let score_3 = embedding[0] * -55 + embedding[1] * -14 + embedding[2] * -17 + embedding[3] * 33 + embedding[4] * -3 + embedding[5] * 37 + embedding[6] * -32 + embedding[7] * 18 + embedding[8] * 26 + embedding[9] * 14;
    let score_4 = embedding[0] * -45 + embedding[1] * -13 + embedding[2] * 3 + embedding[3] * 12 + embedding[4] * -12 + embedding[5] * 5 + embedding[6] * -37 + embedding[7] * 15 + embedding[8] * 38 + embedding[9] * -23;
    let score_5 = embedding[0] * 49 + embedding[1] * 12 + embedding[2] * 30 + embedding[3] * -19 + embedding[4] * 25 + embedding[5] * 3 + embedding[6] * 30 + embedding[7] * -22 + embedding[8] * -54 + embedding[9] * 13;
    let score_6 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 6;
    let score_7 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 6;
    let score_8 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 6;
    let score_9 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 6;
    let score_10 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 6;
    let score_11 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 6;
    let score_12 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 6;
    let score_13 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 6;
    let score_14 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 6;
    let score_15 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 6;
    let score_16 = embedding[0] * -9 + embedding[1] * 8 + embedding[2] * 16 + embedding[3] * 16 + embedding[4] * -18 + embedding[5] * -10 + embedding[6] * 15 + embedding[7] * 15 + embedding[8] * -8 + embedding[9] * 6;
    let score_17 = embedding[0] * 47 + embedding[1] * 10 + embedding[3] * -20 + embedding[4] * 27 + embedding[5] * -7 + embedding[6] * 3 + embedding[7] * -36 + embedding[8] * -36 + embedding[9] * 5;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 2;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 3;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 4;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 5;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 1;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_564(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * 1 + embedding[1] * -2 + embedding[2] * -22 + embedding[3] * -13 + embedding[4] * 11 + embedding[5] * 13 + embedding[6] * -12 + embedding[7] * -11 + embedding[8] * 13 + embedding[9] * -8;
    let score_2 = embedding[0] * 5 + embedding[1] * -11 + embedding[2] * 48 + embedding[3] * -9 + embedding[4] * 1 + embedding[5] * 12 + embedding[6] * 4 + embedding[7] * 24 + embedding[8] * -18 + embedding[9] * -2;
    let score_3 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 18 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -7 + embedding[1] * 4 + embedding[2] * 22 + embedding[3] * 19 + embedding[4] * -19 + embedding[5] * -7 + embedding[6] * 12 + embedding[7] * 17 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 8 + embedding[1] * -1 + embedding[2] * -18 + embedding[3] * -12 + embedding[4] * 14 + embedding[5] * 6 + embedding[6] * -13 + embedding[7] * -17 + embedding[8] * 7 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 9;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_565(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_1 = embedding[0] * 10 + embedding[1] * -4 + embedding[2] * 10 + embedding[3] * -13 + embedding[4] * 15 + embedding[5] * 17 + embedding[6] * 11 + embedding[8] * -8 + embedding[9] * -3;
    let score_2 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_3 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_4 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_5 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_6 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_7 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_8 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_9 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_10 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_11 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_12 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_13 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_14 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_15 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 7;
    let score_16 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -21 + embedding[5] * -8 + embedding[6] * 14 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_17 = embedding[0] * 7 + embedding[1] * -6 + embedding[2] * -21 + embedding[3] * -15 + embedding[4] * 17 + embedding[5] * 9 + embedding[6] * -16 + embedding[7] * -14 + embedding[8] * 11 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_566(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_1 = embedding[0] * 9 + embedding[1] * -4 + embedding[2] * 11 + embedding[3] * -12 + embedding[4] * 15 + embedding[5] * 18 + embedding[6] * 11 + embedding[8] * -8 + embedding[9] * -2;
    let score_2 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_3 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_4 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_5 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_6 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_7 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_8 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_9 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_10 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_11 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_12 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_13 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_14 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_15 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_16 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 22 + embedding[4] * -20 + embedding[5] * -7 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -10 + embedding[9] * 8;
    let score_17 = embedding[0] * 6 + embedding[1] * -7 + embedding[2] * -20 + embedding[3] * -14 + embedding[4] * 17 + embedding[5] * 9 + embedding[6] * -16 + embedding[7] * -15 + embedding[8] * 11 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_567(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_1 = embedding[0] * 9 + embedding[1] * -4 + embedding[2] * 11 + embedding[3] * -13 + embedding[4] * 15 + embedding[5] * 17 + embedding[6] * 11 + embedding[8] * -8 + embedding[9] * -3;
    let score_2 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_3 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_4 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_5 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_6 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_7 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_8 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_9 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_10 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_11 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_12 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_13 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_14 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_15 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_16 = embedding[0] * -5 + embedding[1] * -4 + embedding[2] * 24 + embedding[3] * 21 + embedding[4] * -20 + embedding[5] * -8 + embedding[6] * 13 + embedding[7] * 21 + embedding[8] * -10 + embedding[9] * 8;
    let score_17 = embedding[0] * 6 + embedding[1] * -7 + embedding[2] * -20 + embedding[3] * -14 + embedding[4] * 17 + embedding[5] * 9 + embedding[6] * -17 + embedding[7] * -14 + embedding[8] * 11 + embedding[9] * -10;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_568(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_569(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -14 + embedding[4] * -25 + embedding[5] * 13 + embedding[6] * 10 + embedding[7] * -26 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 22 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -17 + embedding[4] * 29 + embedding[5] * 5 + embedding[6] * -27 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_570(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 14 + embedding[6] * 10 + embedding[7] * -26 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -7 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 22 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 14 + embedding[7] * 17 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 6 + embedding[6] * -28 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_571(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -10 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 13 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -4;
    let score_2 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 5 + embedding[6] * -27 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_572(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_1 = embedding[0] * -5 + embedding[1] * -9 + embedding[2] * 3 + embedding[3] * -13 + embedding[4] * -25 + embedding[5] * 13 + embedding[6] * 10 + embedding[7] * -27 + embedding[8] * 3 + embedding[9] * -3;
    let score_2 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_3 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_4 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_5 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_6 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_7 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_8 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_9 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_10 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_11 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_12 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_13 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_14 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_15 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_16 = embedding[0] * -6 + embedding[1] * 2 + embedding[2] * 21 + embedding[3] * 21 + embedding[4] * -22 + embedding[5] * -11 + embedding[6] * 15 + embedding[7] * 16 + embedding[8] * -9 + embedding[9] * 9;
    let score_17 = embedding[0] * 14 + embedding[1] * -2 + embedding[2] * -24 + embedding[3] * -16 + embedding[4] * 29 + embedding[5] * 6 + embedding[6] * -27 + embedding[7] * -6 + embedding[8] * 10 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 1;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_573(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 22 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -7;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_574(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_1 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_2 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_3 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_4 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_5 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_6 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_7 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_8 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_9 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_10 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_11 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_12 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_13 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_14 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_15 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_16 = embedding[0] * -11 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -12 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 8;
    let score_17 = embedding[0] * 10 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn predict_pixel_575(embedding: [i32; DIMS]) -> u8 {
    let score_0 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_1 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_2 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_3 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_4 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_5 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_6 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_7 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_8 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_9 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_10 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_11 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_12 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_13 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_14 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_15 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 25 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_16 = embedding[0] * -10 + embedding[1] * 5 + embedding[2] * 21 + embedding[3] * 24 + embedding[4] * -23 + embedding[5] * -13 + embedding[6] * 14 + embedding[7] * 20 + embedding[8] * -8 + embedding[9] * 7;
    let score_17 = embedding[0] * 11 + embedding[1] * -5 + embedding[2] * -21 + embedding[3] * -25 + embedding[4] * 23 + embedding[5] * 13 + embedding[6] * -14 + embedding[7] * -20 + embedding[8] * 8 + embedding[9] * -8;
    let mut best_score = score_0;
    let mut best_palette = 0;
    if score_1 > best_score {
        best_score = score_1;
        best_palette = 0;
    }
    if score_2 > best_score {
        best_score = score_2;
        best_palette = 0;
    }
    if score_3 > best_score {
        best_score = score_3;
        best_palette = 0;
    }
    if score_4 > best_score {
        best_score = score_4;
        best_palette = 0;
    }
    if score_5 > best_score {
        best_score = score_5;
        best_palette = 0;
    }
    if score_6 > best_score {
        best_score = score_6;
        best_palette = 0;
    }
    if score_7 > best_score {
        best_score = score_7;
        best_palette = 0;
    }
    if score_8 > best_score {
        best_score = score_8;
        best_palette = 0;
    }
    if score_9 > best_score {
        best_score = score_9;
        best_palette = 0;
    }
    if score_10 > best_score {
        best_score = score_10;
        best_palette = 0;
    }
    if score_11 > best_score {
        best_score = score_11;
        best_palette = 0;
    }
    if score_12 > best_score {
        best_score = score_12;
        best_palette = 0;
    }
    if score_13 > best_score {
        best_score = score_13;
        best_palette = 0;
    }
    if score_14 > best_score {
        best_score = score_14;
        best_palette = 0;
    }
    if score_15 > best_score {
        best_score = score_15;
        best_palette = 0;
    }
    if score_16 > best_score {
        best_score = score_16;
        best_palette = 0;
    }
    if score_17 > best_score {
        best_score = score_17;
        best_palette = 0;
    }
    best_palette
}

fn main(embedding: [u8; DIMS], packed_embedding_input: pub Field, packed_pixels: pub [Field; PACKED_CHUNKS]) {
    let mut packed_embedding = 0;
    let mut embedding_factor = 1;
    packed_embedding = packed_embedding + (embedding[0] as Field) * embedding_factor;
    embedding_factor = embedding_factor * 256;
    packed_embedding = packed_embedding + (embedding[1] as Field) * embedding_factor;
    embedding_factor = embedding_factor * 256;
    packed_embedding = packed_embedding + (embedding[2] as Field) * embedding_factor;
    embedding_factor = embedding_factor * 256;
    packed_embedding = packed_embedding + (embedding[3] as Field) * embedding_factor;
    embedding_factor = embedding_factor * 256;
    packed_embedding = packed_embedding + (embedding[4] as Field) * embedding_factor;
    embedding_factor = embedding_factor * 256;
    packed_embedding = packed_embedding + (embedding[5] as Field) * embedding_factor;
    embedding_factor = embedding_factor * 256;
    packed_embedding = packed_embedding + (embedding[6] as Field) * embedding_factor;
    embedding_factor = embedding_factor * 256;
    packed_embedding = packed_embedding + (embedding[7] as Field) * embedding_factor;
    embedding_factor = embedding_factor * 256;
    packed_embedding = packed_embedding + (embedding[8] as Field) * embedding_factor;
    embedding_factor = embedding_factor * 256;
    packed_embedding = packed_embedding + (embedding[9] as Field) * embedding_factor;
    embedding_factor = embedding_factor * 256;
    assert(packed_embedding == packed_embedding_input);
    let signed_embedding = [signed_byte(embedding[0]), signed_byte(embedding[1]), signed_byte(embedding[2]), signed_byte(embedding[3]), signed_byte(embedding[4]), signed_byte(embedding[5]), signed_byte(embedding[6]), signed_byte(embedding[7]), signed_byte(embedding[8]), signed_byte(embedding[9])];

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_0 = predict_pixel_0(signed_embedding);
    chunk = chunk + (pixel_0 as Field) * factor;
    factor = factor * 256;
    let pixel_1 = predict_pixel_1(signed_embedding);
    chunk = chunk + (pixel_1 as Field) * factor;
    factor = factor * 256;
    let pixel_2 = predict_pixel_2(signed_embedding);
    chunk = chunk + (pixel_2 as Field) * factor;
    factor = factor * 256;
    let pixel_3 = predict_pixel_3(signed_embedding);
    chunk = chunk + (pixel_3 as Field) * factor;
    factor = factor * 256;
    let pixel_4 = predict_pixel_4(signed_embedding);
    chunk = chunk + (pixel_4 as Field) * factor;
    factor = factor * 256;
    let pixel_5 = predict_pixel_5(signed_embedding);
    chunk = chunk + (pixel_5 as Field) * factor;
    factor = factor * 256;
    let pixel_6 = predict_pixel_6(signed_embedding);
    chunk = chunk + (pixel_6 as Field) * factor;
    factor = factor * 256;
    let pixel_7 = predict_pixel_7(signed_embedding);
    chunk = chunk + (pixel_7 as Field) * factor;
    factor = factor * 256;
    let pixel_8 = predict_pixel_8(signed_embedding);
    chunk = chunk + (pixel_8 as Field) * factor;
    factor = factor * 256;
    let pixel_9 = predict_pixel_9(signed_embedding);
    chunk = chunk + (pixel_9 as Field) * factor;
    factor = factor * 256;
    let pixel_10 = predict_pixel_10(signed_embedding);
    chunk = chunk + (pixel_10 as Field) * factor;
    factor = factor * 256;
    let pixel_11 = predict_pixel_11(signed_embedding);
    chunk = chunk + (pixel_11 as Field) * factor;
    factor = factor * 256;
    let pixel_12 = predict_pixel_12(signed_embedding);
    chunk = chunk + (pixel_12 as Field) * factor;
    factor = factor * 256;
    let pixel_13 = predict_pixel_13(signed_embedding);
    chunk = chunk + (pixel_13 as Field) * factor;
    factor = factor * 256;
    let pixel_14 = predict_pixel_14(signed_embedding);
    chunk = chunk + (pixel_14 as Field) * factor;
    factor = factor * 256;
    let pixel_15 = predict_pixel_15(signed_embedding);
    chunk = chunk + (pixel_15 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[0]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_16 = predict_pixel_16(signed_embedding);
    chunk = chunk + (pixel_16 as Field) * factor;
    factor = factor * 256;
    let pixel_17 = predict_pixel_17(signed_embedding);
    chunk = chunk + (pixel_17 as Field) * factor;
    factor = factor * 256;
    let pixel_18 = predict_pixel_18(signed_embedding);
    chunk = chunk + (pixel_18 as Field) * factor;
    factor = factor * 256;
    let pixel_19 = predict_pixel_19(signed_embedding);
    chunk = chunk + (pixel_19 as Field) * factor;
    factor = factor * 256;
    let pixel_20 = predict_pixel_20(signed_embedding);
    chunk = chunk + (pixel_20 as Field) * factor;
    factor = factor * 256;
    let pixel_21 = predict_pixel_21(signed_embedding);
    chunk = chunk + (pixel_21 as Field) * factor;
    factor = factor * 256;
    let pixel_22 = predict_pixel_22(signed_embedding);
    chunk = chunk + (pixel_22 as Field) * factor;
    factor = factor * 256;
    let pixel_23 = predict_pixel_23(signed_embedding);
    chunk = chunk + (pixel_23 as Field) * factor;
    factor = factor * 256;
    let pixel_24 = predict_pixel_24(signed_embedding);
    chunk = chunk + (pixel_24 as Field) * factor;
    factor = factor * 256;
    let pixel_25 = predict_pixel_25(signed_embedding);
    chunk = chunk + (pixel_25 as Field) * factor;
    factor = factor * 256;
    let pixel_26 = predict_pixel_26(signed_embedding);
    chunk = chunk + (pixel_26 as Field) * factor;
    factor = factor * 256;
    let pixel_27 = predict_pixel_27(signed_embedding);
    chunk = chunk + (pixel_27 as Field) * factor;
    factor = factor * 256;
    let pixel_28 = predict_pixel_28(signed_embedding);
    chunk = chunk + (pixel_28 as Field) * factor;
    factor = factor * 256;
    let pixel_29 = predict_pixel_29(signed_embedding);
    chunk = chunk + (pixel_29 as Field) * factor;
    factor = factor * 256;
    let pixel_30 = predict_pixel_30(signed_embedding);
    chunk = chunk + (pixel_30 as Field) * factor;
    factor = factor * 256;
    let pixel_31 = predict_pixel_31(signed_embedding);
    chunk = chunk + (pixel_31 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[1]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_32 = predict_pixel_32(signed_embedding);
    chunk = chunk + (pixel_32 as Field) * factor;
    factor = factor * 256;
    let pixel_33 = predict_pixel_33(signed_embedding);
    chunk = chunk + (pixel_33 as Field) * factor;
    factor = factor * 256;
    let pixel_34 = predict_pixel_34(signed_embedding);
    chunk = chunk + (pixel_34 as Field) * factor;
    factor = factor * 256;
    let pixel_35 = predict_pixel_35(signed_embedding);
    chunk = chunk + (pixel_35 as Field) * factor;
    factor = factor * 256;
    let pixel_36 = predict_pixel_36(signed_embedding);
    chunk = chunk + (pixel_36 as Field) * factor;
    factor = factor * 256;
    let pixel_37 = predict_pixel_37(signed_embedding);
    chunk = chunk + (pixel_37 as Field) * factor;
    factor = factor * 256;
    let pixel_38 = predict_pixel_38(signed_embedding);
    chunk = chunk + (pixel_38 as Field) * factor;
    factor = factor * 256;
    let pixel_39 = predict_pixel_39(signed_embedding);
    chunk = chunk + (pixel_39 as Field) * factor;
    factor = factor * 256;
    let pixel_40 = predict_pixel_40(signed_embedding);
    chunk = chunk + (pixel_40 as Field) * factor;
    factor = factor * 256;
    let pixel_41 = predict_pixel_41(signed_embedding);
    chunk = chunk + (pixel_41 as Field) * factor;
    factor = factor * 256;
    let pixel_42 = predict_pixel_42(signed_embedding);
    chunk = chunk + (pixel_42 as Field) * factor;
    factor = factor * 256;
    let pixel_43 = predict_pixel_43(signed_embedding);
    chunk = chunk + (pixel_43 as Field) * factor;
    factor = factor * 256;
    let pixel_44 = predict_pixel_44(signed_embedding);
    chunk = chunk + (pixel_44 as Field) * factor;
    factor = factor * 256;
    let pixel_45 = predict_pixel_45(signed_embedding);
    chunk = chunk + (pixel_45 as Field) * factor;
    factor = factor * 256;
    let pixel_46 = predict_pixel_46(signed_embedding);
    chunk = chunk + (pixel_46 as Field) * factor;
    factor = factor * 256;
    let pixel_47 = predict_pixel_47(signed_embedding);
    chunk = chunk + (pixel_47 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[2]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_48 = predict_pixel_48(signed_embedding);
    chunk = chunk + (pixel_48 as Field) * factor;
    factor = factor * 256;
    let pixel_49 = predict_pixel_49(signed_embedding);
    chunk = chunk + (pixel_49 as Field) * factor;
    factor = factor * 256;
    let pixel_50 = predict_pixel_50(signed_embedding);
    chunk = chunk + (pixel_50 as Field) * factor;
    factor = factor * 256;
    let pixel_51 = predict_pixel_51(signed_embedding);
    chunk = chunk + (pixel_51 as Field) * factor;
    factor = factor * 256;
    let pixel_52 = predict_pixel_52(signed_embedding);
    chunk = chunk + (pixel_52 as Field) * factor;
    factor = factor * 256;
    let pixel_53 = predict_pixel_53(signed_embedding);
    chunk = chunk + (pixel_53 as Field) * factor;
    factor = factor * 256;
    let pixel_54 = predict_pixel_54(signed_embedding);
    chunk = chunk + (pixel_54 as Field) * factor;
    factor = factor * 256;
    let pixel_55 = predict_pixel_55(signed_embedding);
    chunk = chunk + (pixel_55 as Field) * factor;
    factor = factor * 256;
    let pixel_56 = predict_pixel_56(signed_embedding);
    chunk = chunk + (pixel_56 as Field) * factor;
    factor = factor * 256;
    let pixel_57 = predict_pixel_57(signed_embedding);
    chunk = chunk + (pixel_57 as Field) * factor;
    factor = factor * 256;
    let pixel_58 = predict_pixel_58(signed_embedding);
    chunk = chunk + (pixel_58 as Field) * factor;
    factor = factor * 256;
    let pixel_59 = predict_pixel_59(signed_embedding);
    chunk = chunk + (pixel_59 as Field) * factor;
    factor = factor * 256;
    let pixel_60 = predict_pixel_60(signed_embedding);
    chunk = chunk + (pixel_60 as Field) * factor;
    factor = factor * 256;
    let pixel_61 = predict_pixel_61(signed_embedding);
    chunk = chunk + (pixel_61 as Field) * factor;
    factor = factor * 256;
    let pixel_62 = predict_pixel_62(signed_embedding);
    chunk = chunk + (pixel_62 as Field) * factor;
    factor = factor * 256;
    let pixel_63 = predict_pixel_63(signed_embedding);
    chunk = chunk + (pixel_63 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[3]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_64 = predict_pixel_64(signed_embedding);
    chunk = chunk + (pixel_64 as Field) * factor;
    factor = factor * 256;
    let pixel_65 = predict_pixel_65(signed_embedding);
    chunk = chunk + (pixel_65 as Field) * factor;
    factor = factor * 256;
    let pixel_66 = predict_pixel_66(signed_embedding);
    chunk = chunk + (pixel_66 as Field) * factor;
    factor = factor * 256;
    let pixel_67 = predict_pixel_67(signed_embedding);
    chunk = chunk + (pixel_67 as Field) * factor;
    factor = factor * 256;
    let pixel_68 = predict_pixel_68(signed_embedding);
    chunk = chunk + (pixel_68 as Field) * factor;
    factor = factor * 256;
    let pixel_69 = predict_pixel_69(signed_embedding);
    chunk = chunk + (pixel_69 as Field) * factor;
    factor = factor * 256;
    let pixel_70 = predict_pixel_70(signed_embedding);
    chunk = chunk + (pixel_70 as Field) * factor;
    factor = factor * 256;
    let pixel_71 = predict_pixel_71(signed_embedding);
    chunk = chunk + (pixel_71 as Field) * factor;
    factor = factor * 256;
    let pixel_72 = predict_pixel_72(signed_embedding);
    chunk = chunk + (pixel_72 as Field) * factor;
    factor = factor * 256;
    let pixel_73 = predict_pixel_73(signed_embedding);
    chunk = chunk + (pixel_73 as Field) * factor;
    factor = factor * 256;
    let pixel_74 = predict_pixel_74(signed_embedding);
    chunk = chunk + (pixel_74 as Field) * factor;
    factor = factor * 256;
    let pixel_75 = predict_pixel_75(signed_embedding);
    chunk = chunk + (pixel_75 as Field) * factor;
    factor = factor * 256;
    let pixel_76 = predict_pixel_76(signed_embedding);
    chunk = chunk + (pixel_76 as Field) * factor;
    factor = factor * 256;
    let pixel_77 = predict_pixel_77(signed_embedding);
    chunk = chunk + (pixel_77 as Field) * factor;
    factor = factor * 256;
    let pixel_78 = predict_pixel_78(signed_embedding);
    chunk = chunk + (pixel_78 as Field) * factor;
    factor = factor * 256;
    let pixel_79 = predict_pixel_79(signed_embedding);
    chunk = chunk + (pixel_79 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[4]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_80 = predict_pixel_80(signed_embedding);
    chunk = chunk + (pixel_80 as Field) * factor;
    factor = factor * 256;
    let pixel_81 = predict_pixel_81(signed_embedding);
    chunk = chunk + (pixel_81 as Field) * factor;
    factor = factor * 256;
    let pixel_82 = predict_pixel_82(signed_embedding);
    chunk = chunk + (pixel_82 as Field) * factor;
    factor = factor * 256;
    let pixel_83 = predict_pixel_83(signed_embedding);
    chunk = chunk + (pixel_83 as Field) * factor;
    factor = factor * 256;
    let pixel_84 = predict_pixel_84(signed_embedding);
    chunk = chunk + (pixel_84 as Field) * factor;
    factor = factor * 256;
    let pixel_85 = predict_pixel_85(signed_embedding);
    chunk = chunk + (pixel_85 as Field) * factor;
    factor = factor * 256;
    let pixel_86 = predict_pixel_86(signed_embedding);
    chunk = chunk + (pixel_86 as Field) * factor;
    factor = factor * 256;
    let pixel_87 = predict_pixel_87(signed_embedding);
    chunk = chunk + (pixel_87 as Field) * factor;
    factor = factor * 256;
    let pixel_88 = predict_pixel_88(signed_embedding);
    chunk = chunk + (pixel_88 as Field) * factor;
    factor = factor * 256;
    let pixel_89 = predict_pixel_89(signed_embedding);
    chunk = chunk + (pixel_89 as Field) * factor;
    factor = factor * 256;
    let pixel_90 = predict_pixel_90(signed_embedding);
    chunk = chunk + (pixel_90 as Field) * factor;
    factor = factor * 256;
    let pixel_91 = predict_pixel_91(signed_embedding);
    chunk = chunk + (pixel_91 as Field) * factor;
    factor = factor * 256;
    let pixel_92 = predict_pixel_92(signed_embedding);
    chunk = chunk + (pixel_92 as Field) * factor;
    factor = factor * 256;
    let pixel_93 = predict_pixel_93(signed_embedding);
    chunk = chunk + (pixel_93 as Field) * factor;
    factor = factor * 256;
    let pixel_94 = predict_pixel_94(signed_embedding);
    chunk = chunk + (pixel_94 as Field) * factor;
    factor = factor * 256;
    let pixel_95 = predict_pixel_95(signed_embedding);
    chunk = chunk + (pixel_95 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[5]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_96 = predict_pixel_96(signed_embedding);
    chunk = chunk + (pixel_96 as Field) * factor;
    factor = factor * 256;
    let pixel_97 = predict_pixel_97(signed_embedding);
    chunk = chunk + (pixel_97 as Field) * factor;
    factor = factor * 256;
    let pixel_98 = predict_pixel_98(signed_embedding);
    chunk = chunk + (pixel_98 as Field) * factor;
    factor = factor * 256;
    let pixel_99 = predict_pixel_99(signed_embedding);
    chunk = chunk + (pixel_99 as Field) * factor;
    factor = factor * 256;
    let pixel_100 = predict_pixel_100(signed_embedding);
    chunk = chunk + (pixel_100 as Field) * factor;
    factor = factor * 256;
    let pixel_101 = predict_pixel_101(signed_embedding);
    chunk = chunk + (pixel_101 as Field) * factor;
    factor = factor * 256;
    let pixel_102 = predict_pixel_102(signed_embedding);
    chunk = chunk + (pixel_102 as Field) * factor;
    factor = factor * 256;
    let pixel_103 = predict_pixel_103(signed_embedding);
    chunk = chunk + (pixel_103 as Field) * factor;
    factor = factor * 256;
    let pixel_104 = predict_pixel_104(signed_embedding);
    chunk = chunk + (pixel_104 as Field) * factor;
    factor = factor * 256;
    let pixel_105 = predict_pixel_105(signed_embedding);
    chunk = chunk + (pixel_105 as Field) * factor;
    factor = factor * 256;
    let pixel_106 = predict_pixel_106(signed_embedding);
    chunk = chunk + (pixel_106 as Field) * factor;
    factor = factor * 256;
    let pixel_107 = predict_pixel_107(signed_embedding);
    chunk = chunk + (pixel_107 as Field) * factor;
    factor = factor * 256;
    let pixel_108 = predict_pixel_108(signed_embedding);
    chunk = chunk + (pixel_108 as Field) * factor;
    factor = factor * 256;
    let pixel_109 = predict_pixel_109(signed_embedding);
    chunk = chunk + (pixel_109 as Field) * factor;
    factor = factor * 256;
    let pixel_110 = predict_pixel_110(signed_embedding);
    chunk = chunk + (pixel_110 as Field) * factor;
    factor = factor * 256;
    let pixel_111 = predict_pixel_111(signed_embedding);
    chunk = chunk + (pixel_111 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[6]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_112 = predict_pixel_112(signed_embedding);
    chunk = chunk + (pixel_112 as Field) * factor;
    factor = factor * 256;
    let pixel_113 = predict_pixel_113(signed_embedding);
    chunk = chunk + (pixel_113 as Field) * factor;
    factor = factor * 256;
    let pixel_114 = predict_pixel_114(signed_embedding);
    chunk = chunk + (pixel_114 as Field) * factor;
    factor = factor * 256;
    let pixel_115 = predict_pixel_115(signed_embedding);
    chunk = chunk + (pixel_115 as Field) * factor;
    factor = factor * 256;
    let pixel_116 = predict_pixel_116(signed_embedding);
    chunk = chunk + (pixel_116 as Field) * factor;
    factor = factor * 256;
    let pixel_117 = predict_pixel_117(signed_embedding);
    chunk = chunk + (pixel_117 as Field) * factor;
    factor = factor * 256;
    let pixel_118 = predict_pixel_118(signed_embedding);
    chunk = chunk + (pixel_118 as Field) * factor;
    factor = factor * 256;
    let pixel_119 = predict_pixel_119(signed_embedding);
    chunk = chunk + (pixel_119 as Field) * factor;
    factor = factor * 256;
    let pixel_120 = predict_pixel_120(signed_embedding);
    chunk = chunk + (pixel_120 as Field) * factor;
    factor = factor * 256;
    let pixel_121 = predict_pixel_121(signed_embedding);
    chunk = chunk + (pixel_121 as Field) * factor;
    factor = factor * 256;
    let pixel_122 = predict_pixel_122(signed_embedding);
    chunk = chunk + (pixel_122 as Field) * factor;
    factor = factor * 256;
    let pixel_123 = predict_pixel_123(signed_embedding);
    chunk = chunk + (pixel_123 as Field) * factor;
    factor = factor * 256;
    let pixel_124 = predict_pixel_124(signed_embedding);
    chunk = chunk + (pixel_124 as Field) * factor;
    factor = factor * 256;
    let pixel_125 = predict_pixel_125(signed_embedding);
    chunk = chunk + (pixel_125 as Field) * factor;
    factor = factor * 256;
    let pixel_126 = predict_pixel_126(signed_embedding);
    chunk = chunk + (pixel_126 as Field) * factor;
    factor = factor * 256;
    let pixel_127 = predict_pixel_127(signed_embedding);
    chunk = chunk + (pixel_127 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[7]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_128 = predict_pixel_128(signed_embedding);
    chunk = chunk + (pixel_128 as Field) * factor;
    factor = factor * 256;
    let pixel_129 = predict_pixel_129(signed_embedding);
    chunk = chunk + (pixel_129 as Field) * factor;
    factor = factor * 256;
    let pixel_130 = predict_pixel_130(signed_embedding);
    chunk = chunk + (pixel_130 as Field) * factor;
    factor = factor * 256;
    let pixel_131 = predict_pixel_131(signed_embedding);
    chunk = chunk + (pixel_131 as Field) * factor;
    factor = factor * 256;
    let pixel_132 = predict_pixel_132(signed_embedding);
    chunk = chunk + (pixel_132 as Field) * factor;
    factor = factor * 256;
    let pixel_133 = predict_pixel_133(signed_embedding);
    chunk = chunk + (pixel_133 as Field) * factor;
    factor = factor * 256;
    let pixel_134 = predict_pixel_134(signed_embedding);
    chunk = chunk + (pixel_134 as Field) * factor;
    factor = factor * 256;
    let pixel_135 = predict_pixel_135(signed_embedding);
    chunk = chunk + (pixel_135 as Field) * factor;
    factor = factor * 256;
    let pixel_136 = predict_pixel_136(signed_embedding);
    chunk = chunk + (pixel_136 as Field) * factor;
    factor = factor * 256;
    let pixel_137 = predict_pixel_137(signed_embedding);
    chunk = chunk + (pixel_137 as Field) * factor;
    factor = factor * 256;
    let pixel_138 = predict_pixel_138(signed_embedding);
    chunk = chunk + (pixel_138 as Field) * factor;
    factor = factor * 256;
    let pixel_139 = predict_pixel_139(signed_embedding);
    chunk = chunk + (pixel_139 as Field) * factor;
    factor = factor * 256;
    let pixel_140 = predict_pixel_140(signed_embedding);
    chunk = chunk + (pixel_140 as Field) * factor;
    factor = factor * 256;
    let pixel_141 = predict_pixel_141(signed_embedding);
    chunk = chunk + (pixel_141 as Field) * factor;
    factor = factor * 256;
    let pixel_142 = predict_pixel_142(signed_embedding);
    chunk = chunk + (pixel_142 as Field) * factor;
    factor = factor * 256;
    let pixel_143 = predict_pixel_143(signed_embedding);
    chunk = chunk + (pixel_143 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[8]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_144 = predict_pixel_144(signed_embedding);
    chunk = chunk + (pixel_144 as Field) * factor;
    factor = factor * 256;
    let pixel_145 = predict_pixel_145(signed_embedding);
    chunk = chunk + (pixel_145 as Field) * factor;
    factor = factor * 256;
    let pixel_146 = predict_pixel_146(signed_embedding);
    chunk = chunk + (pixel_146 as Field) * factor;
    factor = factor * 256;
    let pixel_147 = predict_pixel_147(signed_embedding);
    chunk = chunk + (pixel_147 as Field) * factor;
    factor = factor * 256;
    let pixel_148 = predict_pixel_148(signed_embedding);
    chunk = chunk + (pixel_148 as Field) * factor;
    factor = factor * 256;
    let pixel_149 = predict_pixel_149(signed_embedding);
    chunk = chunk + (pixel_149 as Field) * factor;
    factor = factor * 256;
    let pixel_150 = predict_pixel_150(signed_embedding);
    chunk = chunk + (pixel_150 as Field) * factor;
    factor = factor * 256;
    let pixel_151 = predict_pixel_151(signed_embedding);
    chunk = chunk + (pixel_151 as Field) * factor;
    factor = factor * 256;
    let pixel_152 = predict_pixel_152(signed_embedding);
    chunk = chunk + (pixel_152 as Field) * factor;
    factor = factor * 256;
    let pixel_153 = predict_pixel_153(signed_embedding);
    chunk = chunk + (pixel_153 as Field) * factor;
    factor = factor * 256;
    let pixel_154 = predict_pixel_154(signed_embedding);
    chunk = chunk + (pixel_154 as Field) * factor;
    factor = factor * 256;
    let pixel_155 = predict_pixel_155(signed_embedding);
    chunk = chunk + (pixel_155 as Field) * factor;
    factor = factor * 256;
    let pixel_156 = predict_pixel_156(signed_embedding);
    chunk = chunk + (pixel_156 as Field) * factor;
    factor = factor * 256;
    let pixel_157 = predict_pixel_157(signed_embedding);
    chunk = chunk + (pixel_157 as Field) * factor;
    factor = factor * 256;
    let pixel_158 = predict_pixel_158(signed_embedding);
    chunk = chunk + (pixel_158 as Field) * factor;
    factor = factor * 256;
    let pixel_159 = predict_pixel_159(signed_embedding);
    chunk = chunk + (pixel_159 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[9]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_160 = predict_pixel_160(signed_embedding);
    chunk = chunk + (pixel_160 as Field) * factor;
    factor = factor * 256;
    let pixel_161 = predict_pixel_161(signed_embedding);
    chunk = chunk + (pixel_161 as Field) * factor;
    factor = factor * 256;
    let pixel_162 = predict_pixel_162(signed_embedding);
    chunk = chunk + (pixel_162 as Field) * factor;
    factor = factor * 256;
    let pixel_163 = predict_pixel_163(signed_embedding);
    chunk = chunk + (pixel_163 as Field) * factor;
    factor = factor * 256;
    let pixel_164 = predict_pixel_164(signed_embedding);
    chunk = chunk + (pixel_164 as Field) * factor;
    factor = factor * 256;
    let pixel_165 = predict_pixel_165(signed_embedding);
    chunk = chunk + (pixel_165 as Field) * factor;
    factor = factor * 256;
    let pixel_166 = predict_pixel_166(signed_embedding);
    chunk = chunk + (pixel_166 as Field) * factor;
    factor = factor * 256;
    let pixel_167 = predict_pixel_167(signed_embedding);
    chunk = chunk + (pixel_167 as Field) * factor;
    factor = factor * 256;
    let pixel_168 = predict_pixel_168(signed_embedding);
    chunk = chunk + (pixel_168 as Field) * factor;
    factor = factor * 256;
    let pixel_169 = predict_pixel_169(signed_embedding);
    chunk = chunk + (pixel_169 as Field) * factor;
    factor = factor * 256;
    let pixel_170 = predict_pixel_170(signed_embedding);
    chunk = chunk + (pixel_170 as Field) * factor;
    factor = factor * 256;
    let pixel_171 = predict_pixel_171(signed_embedding);
    chunk = chunk + (pixel_171 as Field) * factor;
    factor = factor * 256;
    let pixel_172 = predict_pixel_172(signed_embedding);
    chunk = chunk + (pixel_172 as Field) * factor;
    factor = factor * 256;
    let pixel_173 = predict_pixel_173(signed_embedding);
    chunk = chunk + (pixel_173 as Field) * factor;
    factor = factor * 256;
    let pixel_174 = predict_pixel_174(signed_embedding);
    chunk = chunk + (pixel_174 as Field) * factor;
    factor = factor * 256;
    let pixel_175 = predict_pixel_175(signed_embedding);
    chunk = chunk + (pixel_175 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[10]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_176 = predict_pixel_176(signed_embedding);
    chunk = chunk + (pixel_176 as Field) * factor;
    factor = factor * 256;
    let pixel_177 = predict_pixel_177(signed_embedding);
    chunk = chunk + (pixel_177 as Field) * factor;
    factor = factor * 256;
    let pixel_178 = predict_pixel_178(signed_embedding);
    chunk = chunk + (pixel_178 as Field) * factor;
    factor = factor * 256;
    let pixel_179 = predict_pixel_179(signed_embedding);
    chunk = chunk + (pixel_179 as Field) * factor;
    factor = factor * 256;
    let pixel_180 = predict_pixel_180(signed_embedding);
    chunk = chunk + (pixel_180 as Field) * factor;
    factor = factor * 256;
    let pixel_181 = predict_pixel_181(signed_embedding);
    chunk = chunk + (pixel_181 as Field) * factor;
    factor = factor * 256;
    let pixel_182 = predict_pixel_182(signed_embedding);
    chunk = chunk + (pixel_182 as Field) * factor;
    factor = factor * 256;
    let pixel_183 = predict_pixel_183(signed_embedding);
    chunk = chunk + (pixel_183 as Field) * factor;
    factor = factor * 256;
    let pixel_184 = predict_pixel_184(signed_embedding);
    chunk = chunk + (pixel_184 as Field) * factor;
    factor = factor * 256;
    let pixel_185 = predict_pixel_185(signed_embedding);
    chunk = chunk + (pixel_185 as Field) * factor;
    factor = factor * 256;
    let pixel_186 = predict_pixel_186(signed_embedding);
    chunk = chunk + (pixel_186 as Field) * factor;
    factor = factor * 256;
    let pixel_187 = predict_pixel_187(signed_embedding);
    chunk = chunk + (pixel_187 as Field) * factor;
    factor = factor * 256;
    let pixel_188 = predict_pixel_188(signed_embedding);
    chunk = chunk + (pixel_188 as Field) * factor;
    factor = factor * 256;
    let pixel_189 = predict_pixel_189(signed_embedding);
    chunk = chunk + (pixel_189 as Field) * factor;
    factor = factor * 256;
    let pixel_190 = predict_pixel_190(signed_embedding);
    chunk = chunk + (pixel_190 as Field) * factor;
    factor = factor * 256;
    let pixel_191 = predict_pixel_191(signed_embedding);
    chunk = chunk + (pixel_191 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[11]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_192 = predict_pixel_192(signed_embedding);
    chunk = chunk + (pixel_192 as Field) * factor;
    factor = factor * 256;
    let pixel_193 = predict_pixel_193(signed_embedding);
    chunk = chunk + (pixel_193 as Field) * factor;
    factor = factor * 256;
    let pixel_194 = predict_pixel_194(signed_embedding);
    chunk = chunk + (pixel_194 as Field) * factor;
    factor = factor * 256;
    let pixel_195 = predict_pixel_195(signed_embedding);
    chunk = chunk + (pixel_195 as Field) * factor;
    factor = factor * 256;
    let pixel_196 = predict_pixel_196(signed_embedding);
    chunk = chunk + (pixel_196 as Field) * factor;
    factor = factor * 256;
    let pixel_197 = predict_pixel_197(signed_embedding);
    chunk = chunk + (pixel_197 as Field) * factor;
    factor = factor * 256;
    let pixel_198 = predict_pixel_198(signed_embedding);
    chunk = chunk + (pixel_198 as Field) * factor;
    factor = factor * 256;
    let pixel_199 = predict_pixel_199(signed_embedding);
    chunk = chunk + (pixel_199 as Field) * factor;
    factor = factor * 256;
    let pixel_200 = predict_pixel_200(signed_embedding);
    chunk = chunk + (pixel_200 as Field) * factor;
    factor = factor * 256;
    let pixel_201 = predict_pixel_201(signed_embedding);
    chunk = chunk + (pixel_201 as Field) * factor;
    factor = factor * 256;
    let pixel_202 = predict_pixel_202(signed_embedding);
    chunk = chunk + (pixel_202 as Field) * factor;
    factor = factor * 256;
    let pixel_203 = predict_pixel_203(signed_embedding);
    chunk = chunk + (pixel_203 as Field) * factor;
    factor = factor * 256;
    let pixel_204 = predict_pixel_204(signed_embedding);
    chunk = chunk + (pixel_204 as Field) * factor;
    factor = factor * 256;
    let pixel_205 = predict_pixel_205(signed_embedding);
    chunk = chunk + (pixel_205 as Field) * factor;
    factor = factor * 256;
    let pixel_206 = predict_pixel_206(signed_embedding);
    chunk = chunk + (pixel_206 as Field) * factor;
    factor = factor * 256;
    let pixel_207 = predict_pixel_207(signed_embedding);
    chunk = chunk + (pixel_207 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[12]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_208 = predict_pixel_208(signed_embedding);
    chunk = chunk + (pixel_208 as Field) * factor;
    factor = factor * 256;
    let pixel_209 = predict_pixel_209(signed_embedding);
    chunk = chunk + (pixel_209 as Field) * factor;
    factor = factor * 256;
    let pixel_210 = predict_pixel_210(signed_embedding);
    chunk = chunk + (pixel_210 as Field) * factor;
    factor = factor * 256;
    let pixel_211 = predict_pixel_211(signed_embedding);
    chunk = chunk + (pixel_211 as Field) * factor;
    factor = factor * 256;
    let pixel_212 = predict_pixel_212(signed_embedding);
    chunk = chunk + (pixel_212 as Field) * factor;
    factor = factor * 256;
    let pixel_213 = predict_pixel_213(signed_embedding);
    chunk = chunk + (pixel_213 as Field) * factor;
    factor = factor * 256;
    let pixel_214 = predict_pixel_214(signed_embedding);
    chunk = chunk + (pixel_214 as Field) * factor;
    factor = factor * 256;
    let pixel_215 = predict_pixel_215(signed_embedding);
    chunk = chunk + (pixel_215 as Field) * factor;
    factor = factor * 256;
    let pixel_216 = predict_pixel_216(signed_embedding);
    chunk = chunk + (pixel_216 as Field) * factor;
    factor = factor * 256;
    let pixel_217 = predict_pixel_217(signed_embedding);
    chunk = chunk + (pixel_217 as Field) * factor;
    factor = factor * 256;
    let pixel_218 = predict_pixel_218(signed_embedding);
    chunk = chunk + (pixel_218 as Field) * factor;
    factor = factor * 256;
    let pixel_219 = predict_pixel_219(signed_embedding);
    chunk = chunk + (pixel_219 as Field) * factor;
    factor = factor * 256;
    let pixel_220 = predict_pixel_220(signed_embedding);
    chunk = chunk + (pixel_220 as Field) * factor;
    factor = factor * 256;
    let pixel_221 = predict_pixel_221(signed_embedding);
    chunk = chunk + (pixel_221 as Field) * factor;
    factor = factor * 256;
    let pixel_222 = predict_pixel_222(signed_embedding);
    chunk = chunk + (pixel_222 as Field) * factor;
    factor = factor * 256;
    let pixel_223 = predict_pixel_223(signed_embedding);
    chunk = chunk + (pixel_223 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[13]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_224 = predict_pixel_224(signed_embedding);
    chunk = chunk + (pixel_224 as Field) * factor;
    factor = factor * 256;
    let pixel_225 = predict_pixel_225(signed_embedding);
    chunk = chunk + (pixel_225 as Field) * factor;
    factor = factor * 256;
    let pixel_226 = predict_pixel_226(signed_embedding);
    chunk = chunk + (pixel_226 as Field) * factor;
    factor = factor * 256;
    let pixel_227 = predict_pixel_227(signed_embedding);
    chunk = chunk + (pixel_227 as Field) * factor;
    factor = factor * 256;
    let pixel_228 = predict_pixel_228(signed_embedding);
    chunk = chunk + (pixel_228 as Field) * factor;
    factor = factor * 256;
    let pixel_229 = predict_pixel_229(signed_embedding);
    chunk = chunk + (pixel_229 as Field) * factor;
    factor = factor * 256;
    let pixel_230 = predict_pixel_230(signed_embedding);
    chunk = chunk + (pixel_230 as Field) * factor;
    factor = factor * 256;
    let pixel_231 = predict_pixel_231(signed_embedding);
    chunk = chunk + (pixel_231 as Field) * factor;
    factor = factor * 256;
    let pixel_232 = predict_pixel_232(signed_embedding);
    chunk = chunk + (pixel_232 as Field) * factor;
    factor = factor * 256;
    let pixel_233 = predict_pixel_233(signed_embedding);
    chunk = chunk + (pixel_233 as Field) * factor;
    factor = factor * 256;
    let pixel_234 = predict_pixel_234(signed_embedding);
    chunk = chunk + (pixel_234 as Field) * factor;
    factor = factor * 256;
    let pixel_235 = predict_pixel_235(signed_embedding);
    chunk = chunk + (pixel_235 as Field) * factor;
    factor = factor * 256;
    let pixel_236 = predict_pixel_236(signed_embedding);
    chunk = chunk + (pixel_236 as Field) * factor;
    factor = factor * 256;
    let pixel_237 = predict_pixel_237(signed_embedding);
    chunk = chunk + (pixel_237 as Field) * factor;
    factor = factor * 256;
    let pixel_238 = predict_pixel_238(signed_embedding);
    chunk = chunk + (pixel_238 as Field) * factor;
    factor = factor * 256;
    let pixel_239 = predict_pixel_239(signed_embedding);
    chunk = chunk + (pixel_239 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[14]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_240 = predict_pixel_240(signed_embedding);
    chunk = chunk + (pixel_240 as Field) * factor;
    factor = factor * 256;
    let pixel_241 = predict_pixel_241(signed_embedding);
    chunk = chunk + (pixel_241 as Field) * factor;
    factor = factor * 256;
    let pixel_242 = predict_pixel_242(signed_embedding);
    chunk = chunk + (pixel_242 as Field) * factor;
    factor = factor * 256;
    let pixel_243 = predict_pixel_243(signed_embedding);
    chunk = chunk + (pixel_243 as Field) * factor;
    factor = factor * 256;
    let pixel_244 = predict_pixel_244(signed_embedding);
    chunk = chunk + (pixel_244 as Field) * factor;
    factor = factor * 256;
    let pixel_245 = predict_pixel_245(signed_embedding);
    chunk = chunk + (pixel_245 as Field) * factor;
    factor = factor * 256;
    let pixel_246 = predict_pixel_246(signed_embedding);
    chunk = chunk + (pixel_246 as Field) * factor;
    factor = factor * 256;
    let pixel_247 = predict_pixel_247(signed_embedding);
    chunk = chunk + (pixel_247 as Field) * factor;
    factor = factor * 256;
    let pixel_248 = predict_pixel_248(signed_embedding);
    chunk = chunk + (pixel_248 as Field) * factor;
    factor = factor * 256;
    let pixel_249 = predict_pixel_249(signed_embedding);
    chunk = chunk + (pixel_249 as Field) * factor;
    factor = factor * 256;
    let pixel_250 = predict_pixel_250(signed_embedding);
    chunk = chunk + (pixel_250 as Field) * factor;
    factor = factor * 256;
    let pixel_251 = predict_pixel_251(signed_embedding);
    chunk = chunk + (pixel_251 as Field) * factor;
    factor = factor * 256;
    let pixel_252 = predict_pixel_252(signed_embedding);
    chunk = chunk + (pixel_252 as Field) * factor;
    factor = factor * 256;
    let pixel_253 = predict_pixel_253(signed_embedding);
    chunk = chunk + (pixel_253 as Field) * factor;
    factor = factor * 256;
    let pixel_254 = predict_pixel_254(signed_embedding);
    chunk = chunk + (pixel_254 as Field) * factor;
    factor = factor * 256;
    let pixel_255 = predict_pixel_255(signed_embedding);
    chunk = chunk + (pixel_255 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[15]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_256 = predict_pixel_256(signed_embedding);
    chunk = chunk + (pixel_256 as Field) * factor;
    factor = factor * 256;
    let pixel_257 = predict_pixel_257(signed_embedding);
    chunk = chunk + (pixel_257 as Field) * factor;
    factor = factor * 256;
    let pixel_258 = predict_pixel_258(signed_embedding);
    chunk = chunk + (pixel_258 as Field) * factor;
    factor = factor * 256;
    let pixel_259 = predict_pixel_259(signed_embedding);
    chunk = chunk + (pixel_259 as Field) * factor;
    factor = factor * 256;
    let pixel_260 = predict_pixel_260(signed_embedding);
    chunk = chunk + (pixel_260 as Field) * factor;
    factor = factor * 256;
    let pixel_261 = predict_pixel_261(signed_embedding);
    chunk = chunk + (pixel_261 as Field) * factor;
    factor = factor * 256;
    let pixel_262 = predict_pixel_262(signed_embedding);
    chunk = chunk + (pixel_262 as Field) * factor;
    factor = factor * 256;
    let pixel_263 = predict_pixel_263(signed_embedding);
    chunk = chunk + (pixel_263 as Field) * factor;
    factor = factor * 256;
    let pixel_264 = predict_pixel_264(signed_embedding);
    chunk = chunk + (pixel_264 as Field) * factor;
    factor = factor * 256;
    let pixel_265 = predict_pixel_265(signed_embedding);
    chunk = chunk + (pixel_265 as Field) * factor;
    factor = factor * 256;
    let pixel_266 = predict_pixel_266(signed_embedding);
    chunk = chunk + (pixel_266 as Field) * factor;
    factor = factor * 256;
    let pixel_267 = predict_pixel_267(signed_embedding);
    chunk = chunk + (pixel_267 as Field) * factor;
    factor = factor * 256;
    let pixel_268 = predict_pixel_268(signed_embedding);
    chunk = chunk + (pixel_268 as Field) * factor;
    factor = factor * 256;
    let pixel_269 = predict_pixel_269(signed_embedding);
    chunk = chunk + (pixel_269 as Field) * factor;
    factor = factor * 256;
    let pixel_270 = predict_pixel_270(signed_embedding);
    chunk = chunk + (pixel_270 as Field) * factor;
    factor = factor * 256;
    let pixel_271 = predict_pixel_271(signed_embedding);
    chunk = chunk + (pixel_271 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[16]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_272 = predict_pixel_272(signed_embedding);
    chunk = chunk + (pixel_272 as Field) * factor;
    factor = factor * 256;
    let pixel_273 = predict_pixel_273(signed_embedding);
    chunk = chunk + (pixel_273 as Field) * factor;
    factor = factor * 256;
    let pixel_274 = predict_pixel_274(signed_embedding);
    chunk = chunk + (pixel_274 as Field) * factor;
    factor = factor * 256;
    let pixel_275 = predict_pixel_275(signed_embedding);
    chunk = chunk + (pixel_275 as Field) * factor;
    factor = factor * 256;
    let pixel_276 = predict_pixel_276(signed_embedding);
    chunk = chunk + (pixel_276 as Field) * factor;
    factor = factor * 256;
    let pixel_277 = predict_pixel_277(signed_embedding);
    chunk = chunk + (pixel_277 as Field) * factor;
    factor = factor * 256;
    let pixel_278 = predict_pixel_278(signed_embedding);
    chunk = chunk + (pixel_278 as Field) * factor;
    factor = factor * 256;
    let pixel_279 = predict_pixel_279(signed_embedding);
    chunk = chunk + (pixel_279 as Field) * factor;
    factor = factor * 256;
    let pixel_280 = predict_pixel_280(signed_embedding);
    chunk = chunk + (pixel_280 as Field) * factor;
    factor = factor * 256;
    let pixel_281 = predict_pixel_281(signed_embedding);
    chunk = chunk + (pixel_281 as Field) * factor;
    factor = factor * 256;
    let pixel_282 = predict_pixel_282(signed_embedding);
    chunk = chunk + (pixel_282 as Field) * factor;
    factor = factor * 256;
    let pixel_283 = predict_pixel_283(signed_embedding);
    chunk = chunk + (pixel_283 as Field) * factor;
    factor = factor * 256;
    let pixel_284 = predict_pixel_284(signed_embedding);
    chunk = chunk + (pixel_284 as Field) * factor;
    factor = factor * 256;
    let pixel_285 = predict_pixel_285(signed_embedding);
    chunk = chunk + (pixel_285 as Field) * factor;
    factor = factor * 256;
    let pixel_286 = predict_pixel_286(signed_embedding);
    chunk = chunk + (pixel_286 as Field) * factor;
    factor = factor * 256;
    let pixel_287 = predict_pixel_287(signed_embedding);
    chunk = chunk + (pixel_287 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[17]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_288 = predict_pixel_288(signed_embedding);
    chunk = chunk + (pixel_288 as Field) * factor;
    factor = factor * 256;
    let pixel_289 = predict_pixel_289(signed_embedding);
    chunk = chunk + (pixel_289 as Field) * factor;
    factor = factor * 256;
    let pixel_290 = predict_pixel_290(signed_embedding);
    chunk = chunk + (pixel_290 as Field) * factor;
    factor = factor * 256;
    let pixel_291 = predict_pixel_291(signed_embedding);
    chunk = chunk + (pixel_291 as Field) * factor;
    factor = factor * 256;
    let pixel_292 = predict_pixel_292(signed_embedding);
    chunk = chunk + (pixel_292 as Field) * factor;
    factor = factor * 256;
    let pixel_293 = predict_pixel_293(signed_embedding);
    chunk = chunk + (pixel_293 as Field) * factor;
    factor = factor * 256;
    let pixel_294 = predict_pixel_294(signed_embedding);
    chunk = chunk + (pixel_294 as Field) * factor;
    factor = factor * 256;
    let pixel_295 = predict_pixel_295(signed_embedding);
    chunk = chunk + (pixel_295 as Field) * factor;
    factor = factor * 256;
    let pixel_296 = predict_pixel_296(signed_embedding);
    chunk = chunk + (pixel_296 as Field) * factor;
    factor = factor * 256;
    let pixel_297 = predict_pixel_297(signed_embedding);
    chunk = chunk + (pixel_297 as Field) * factor;
    factor = factor * 256;
    let pixel_298 = predict_pixel_298(signed_embedding);
    chunk = chunk + (pixel_298 as Field) * factor;
    factor = factor * 256;
    let pixel_299 = predict_pixel_299(signed_embedding);
    chunk = chunk + (pixel_299 as Field) * factor;
    factor = factor * 256;
    let pixel_300 = predict_pixel_300(signed_embedding);
    chunk = chunk + (pixel_300 as Field) * factor;
    factor = factor * 256;
    let pixel_301 = predict_pixel_301(signed_embedding);
    chunk = chunk + (pixel_301 as Field) * factor;
    factor = factor * 256;
    let pixel_302 = predict_pixel_302(signed_embedding);
    chunk = chunk + (pixel_302 as Field) * factor;
    factor = factor * 256;
    let pixel_303 = predict_pixel_303(signed_embedding);
    chunk = chunk + (pixel_303 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[18]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_304 = predict_pixel_304(signed_embedding);
    chunk = chunk + (pixel_304 as Field) * factor;
    factor = factor * 256;
    let pixel_305 = predict_pixel_305(signed_embedding);
    chunk = chunk + (pixel_305 as Field) * factor;
    factor = factor * 256;
    let pixel_306 = predict_pixel_306(signed_embedding);
    chunk = chunk + (pixel_306 as Field) * factor;
    factor = factor * 256;
    let pixel_307 = predict_pixel_307(signed_embedding);
    chunk = chunk + (pixel_307 as Field) * factor;
    factor = factor * 256;
    let pixel_308 = predict_pixel_308(signed_embedding);
    chunk = chunk + (pixel_308 as Field) * factor;
    factor = factor * 256;
    let pixel_309 = predict_pixel_309(signed_embedding);
    chunk = chunk + (pixel_309 as Field) * factor;
    factor = factor * 256;
    let pixel_310 = predict_pixel_310(signed_embedding);
    chunk = chunk + (pixel_310 as Field) * factor;
    factor = factor * 256;
    let pixel_311 = predict_pixel_311(signed_embedding);
    chunk = chunk + (pixel_311 as Field) * factor;
    factor = factor * 256;
    let pixel_312 = predict_pixel_312(signed_embedding);
    chunk = chunk + (pixel_312 as Field) * factor;
    factor = factor * 256;
    let pixel_313 = predict_pixel_313(signed_embedding);
    chunk = chunk + (pixel_313 as Field) * factor;
    factor = factor * 256;
    let pixel_314 = predict_pixel_314(signed_embedding);
    chunk = chunk + (pixel_314 as Field) * factor;
    factor = factor * 256;
    let pixel_315 = predict_pixel_315(signed_embedding);
    chunk = chunk + (pixel_315 as Field) * factor;
    factor = factor * 256;
    let pixel_316 = predict_pixel_316(signed_embedding);
    chunk = chunk + (pixel_316 as Field) * factor;
    factor = factor * 256;
    let pixel_317 = predict_pixel_317(signed_embedding);
    chunk = chunk + (pixel_317 as Field) * factor;
    factor = factor * 256;
    let pixel_318 = predict_pixel_318(signed_embedding);
    chunk = chunk + (pixel_318 as Field) * factor;
    factor = factor * 256;
    let pixel_319 = predict_pixel_319(signed_embedding);
    chunk = chunk + (pixel_319 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[19]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_320 = predict_pixel_320(signed_embedding);
    chunk = chunk + (pixel_320 as Field) * factor;
    factor = factor * 256;
    let pixel_321 = predict_pixel_321(signed_embedding);
    chunk = chunk + (pixel_321 as Field) * factor;
    factor = factor * 256;
    let pixel_322 = predict_pixel_322(signed_embedding);
    chunk = chunk + (pixel_322 as Field) * factor;
    factor = factor * 256;
    let pixel_323 = predict_pixel_323(signed_embedding);
    chunk = chunk + (pixel_323 as Field) * factor;
    factor = factor * 256;
    let pixel_324 = predict_pixel_324(signed_embedding);
    chunk = chunk + (pixel_324 as Field) * factor;
    factor = factor * 256;
    let pixel_325 = predict_pixel_325(signed_embedding);
    chunk = chunk + (pixel_325 as Field) * factor;
    factor = factor * 256;
    let pixel_326 = predict_pixel_326(signed_embedding);
    chunk = chunk + (pixel_326 as Field) * factor;
    factor = factor * 256;
    let pixel_327 = predict_pixel_327(signed_embedding);
    chunk = chunk + (pixel_327 as Field) * factor;
    factor = factor * 256;
    let pixel_328 = predict_pixel_328(signed_embedding);
    chunk = chunk + (pixel_328 as Field) * factor;
    factor = factor * 256;
    let pixel_329 = predict_pixel_329(signed_embedding);
    chunk = chunk + (pixel_329 as Field) * factor;
    factor = factor * 256;
    let pixel_330 = predict_pixel_330(signed_embedding);
    chunk = chunk + (pixel_330 as Field) * factor;
    factor = factor * 256;
    let pixel_331 = predict_pixel_331(signed_embedding);
    chunk = chunk + (pixel_331 as Field) * factor;
    factor = factor * 256;
    let pixel_332 = predict_pixel_332(signed_embedding);
    chunk = chunk + (pixel_332 as Field) * factor;
    factor = factor * 256;
    let pixel_333 = predict_pixel_333(signed_embedding);
    chunk = chunk + (pixel_333 as Field) * factor;
    factor = factor * 256;
    let pixel_334 = predict_pixel_334(signed_embedding);
    chunk = chunk + (pixel_334 as Field) * factor;
    factor = factor * 256;
    let pixel_335 = predict_pixel_335(signed_embedding);
    chunk = chunk + (pixel_335 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[20]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_336 = predict_pixel_336(signed_embedding);
    chunk = chunk + (pixel_336 as Field) * factor;
    factor = factor * 256;
    let pixel_337 = predict_pixel_337(signed_embedding);
    chunk = chunk + (pixel_337 as Field) * factor;
    factor = factor * 256;
    let pixel_338 = predict_pixel_338(signed_embedding);
    chunk = chunk + (pixel_338 as Field) * factor;
    factor = factor * 256;
    let pixel_339 = predict_pixel_339(signed_embedding);
    chunk = chunk + (pixel_339 as Field) * factor;
    factor = factor * 256;
    let pixel_340 = predict_pixel_340(signed_embedding);
    chunk = chunk + (pixel_340 as Field) * factor;
    factor = factor * 256;
    let pixel_341 = predict_pixel_341(signed_embedding);
    chunk = chunk + (pixel_341 as Field) * factor;
    factor = factor * 256;
    let pixel_342 = predict_pixel_342(signed_embedding);
    chunk = chunk + (pixel_342 as Field) * factor;
    factor = factor * 256;
    let pixel_343 = predict_pixel_343(signed_embedding);
    chunk = chunk + (pixel_343 as Field) * factor;
    factor = factor * 256;
    let pixel_344 = predict_pixel_344(signed_embedding);
    chunk = chunk + (pixel_344 as Field) * factor;
    factor = factor * 256;
    let pixel_345 = predict_pixel_345(signed_embedding);
    chunk = chunk + (pixel_345 as Field) * factor;
    factor = factor * 256;
    let pixel_346 = predict_pixel_346(signed_embedding);
    chunk = chunk + (pixel_346 as Field) * factor;
    factor = factor * 256;
    let pixel_347 = predict_pixel_347(signed_embedding);
    chunk = chunk + (pixel_347 as Field) * factor;
    factor = factor * 256;
    let pixel_348 = predict_pixel_348(signed_embedding);
    chunk = chunk + (pixel_348 as Field) * factor;
    factor = factor * 256;
    let pixel_349 = predict_pixel_349(signed_embedding);
    chunk = chunk + (pixel_349 as Field) * factor;
    factor = factor * 256;
    let pixel_350 = predict_pixel_350(signed_embedding);
    chunk = chunk + (pixel_350 as Field) * factor;
    factor = factor * 256;
    let pixel_351 = predict_pixel_351(signed_embedding);
    chunk = chunk + (pixel_351 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[21]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_352 = predict_pixel_352(signed_embedding);
    chunk = chunk + (pixel_352 as Field) * factor;
    factor = factor * 256;
    let pixel_353 = predict_pixel_353(signed_embedding);
    chunk = chunk + (pixel_353 as Field) * factor;
    factor = factor * 256;
    let pixel_354 = predict_pixel_354(signed_embedding);
    chunk = chunk + (pixel_354 as Field) * factor;
    factor = factor * 256;
    let pixel_355 = predict_pixel_355(signed_embedding);
    chunk = chunk + (pixel_355 as Field) * factor;
    factor = factor * 256;
    let pixel_356 = predict_pixel_356(signed_embedding);
    chunk = chunk + (pixel_356 as Field) * factor;
    factor = factor * 256;
    let pixel_357 = predict_pixel_357(signed_embedding);
    chunk = chunk + (pixel_357 as Field) * factor;
    factor = factor * 256;
    let pixel_358 = predict_pixel_358(signed_embedding);
    chunk = chunk + (pixel_358 as Field) * factor;
    factor = factor * 256;
    let pixel_359 = predict_pixel_359(signed_embedding);
    chunk = chunk + (pixel_359 as Field) * factor;
    factor = factor * 256;
    let pixel_360 = predict_pixel_360(signed_embedding);
    chunk = chunk + (pixel_360 as Field) * factor;
    factor = factor * 256;
    let pixel_361 = predict_pixel_361(signed_embedding);
    chunk = chunk + (pixel_361 as Field) * factor;
    factor = factor * 256;
    let pixel_362 = predict_pixel_362(signed_embedding);
    chunk = chunk + (pixel_362 as Field) * factor;
    factor = factor * 256;
    let pixel_363 = predict_pixel_363(signed_embedding);
    chunk = chunk + (pixel_363 as Field) * factor;
    factor = factor * 256;
    let pixel_364 = predict_pixel_364(signed_embedding);
    chunk = chunk + (pixel_364 as Field) * factor;
    factor = factor * 256;
    let pixel_365 = predict_pixel_365(signed_embedding);
    chunk = chunk + (pixel_365 as Field) * factor;
    factor = factor * 256;
    let pixel_366 = predict_pixel_366(signed_embedding);
    chunk = chunk + (pixel_366 as Field) * factor;
    factor = factor * 256;
    let pixel_367 = predict_pixel_367(signed_embedding);
    chunk = chunk + (pixel_367 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[22]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_368 = predict_pixel_368(signed_embedding);
    chunk = chunk + (pixel_368 as Field) * factor;
    factor = factor * 256;
    let pixel_369 = predict_pixel_369(signed_embedding);
    chunk = chunk + (pixel_369 as Field) * factor;
    factor = factor * 256;
    let pixel_370 = predict_pixel_370(signed_embedding);
    chunk = chunk + (pixel_370 as Field) * factor;
    factor = factor * 256;
    let pixel_371 = predict_pixel_371(signed_embedding);
    chunk = chunk + (pixel_371 as Field) * factor;
    factor = factor * 256;
    let pixel_372 = predict_pixel_372(signed_embedding);
    chunk = chunk + (pixel_372 as Field) * factor;
    factor = factor * 256;
    let pixel_373 = predict_pixel_373(signed_embedding);
    chunk = chunk + (pixel_373 as Field) * factor;
    factor = factor * 256;
    let pixel_374 = predict_pixel_374(signed_embedding);
    chunk = chunk + (pixel_374 as Field) * factor;
    factor = factor * 256;
    let pixel_375 = predict_pixel_375(signed_embedding);
    chunk = chunk + (pixel_375 as Field) * factor;
    factor = factor * 256;
    let pixel_376 = predict_pixel_376(signed_embedding);
    chunk = chunk + (pixel_376 as Field) * factor;
    factor = factor * 256;
    let pixel_377 = predict_pixel_377(signed_embedding);
    chunk = chunk + (pixel_377 as Field) * factor;
    factor = factor * 256;
    let pixel_378 = predict_pixel_378(signed_embedding);
    chunk = chunk + (pixel_378 as Field) * factor;
    factor = factor * 256;
    let pixel_379 = predict_pixel_379(signed_embedding);
    chunk = chunk + (pixel_379 as Field) * factor;
    factor = factor * 256;
    let pixel_380 = predict_pixel_380(signed_embedding);
    chunk = chunk + (pixel_380 as Field) * factor;
    factor = factor * 256;
    let pixel_381 = predict_pixel_381(signed_embedding);
    chunk = chunk + (pixel_381 as Field) * factor;
    factor = factor * 256;
    let pixel_382 = predict_pixel_382(signed_embedding);
    chunk = chunk + (pixel_382 as Field) * factor;
    factor = factor * 256;
    let pixel_383 = predict_pixel_383(signed_embedding);
    chunk = chunk + (pixel_383 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[23]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_384 = predict_pixel_384(signed_embedding);
    chunk = chunk + (pixel_384 as Field) * factor;
    factor = factor * 256;
    let pixel_385 = predict_pixel_385(signed_embedding);
    chunk = chunk + (pixel_385 as Field) * factor;
    factor = factor * 256;
    let pixel_386 = predict_pixel_386(signed_embedding);
    chunk = chunk + (pixel_386 as Field) * factor;
    factor = factor * 256;
    let pixel_387 = predict_pixel_387(signed_embedding);
    chunk = chunk + (pixel_387 as Field) * factor;
    factor = factor * 256;
    let pixel_388 = predict_pixel_388(signed_embedding);
    chunk = chunk + (pixel_388 as Field) * factor;
    factor = factor * 256;
    let pixel_389 = predict_pixel_389(signed_embedding);
    chunk = chunk + (pixel_389 as Field) * factor;
    factor = factor * 256;
    let pixel_390 = predict_pixel_390(signed_embedding);
    chunk = chunk + (pixel_390 as Field) * factor;
    factor = factor * 256;
    let pixel_391 = predict_pixel_391(signed_embedding);
    chunk = chunk + (pixel_391 as Field) * factor;
    factor = factor * 256;
    let pixel_392 = predict_pixel_392(signed_embedding);
    chunk = chunk + (pixel_392 as Field) * factor;
    factor = factor * 256;
    let pixel_393 = predict_pixel_393(signed_embedding);
    chunk = chunk + (pixel_393 as Field) * factor;
    factor = factor * 256;
    let pixel_394 = predict_pixel_394(signed_embedding);
    chunk = chunk + (pixel_394 as Field) * factor;
    factor = factor * 256;
    let pixel_395 = predict_pixel_395(signed_embedding);
    chunk = chunk + (pixel_395 as Field) * factor;
    factor = factor * 256;
    let pixel_396 = predict_pixel_396(signed_embedding);
    chunk = chunk + (pixel_396 as Field) * factor;
    factor = factor * 256;
    let pixel_397 = predict_pixel_397(signed_embedding);
    chunk = chunk + (pixel_397 as Field) * factor;
    factor = factor * 256;
    let pixel_398 = predict_pixel_398(signed_embedding);
    chunk = chunk + (pixel_398 as Field) * factor;
    factor = factor * 256;
    let pixel_399 = predict_pixel_399(signed_embedding);
    chunk = chunk + (pixel_399 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[24]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_400 = predict_pixel_400(signed_embedding);
    chunk = chunk + (pixel_400 as Field) * factor;
    factor = factor * 256;
    let pixel_401 = predict_pixel_401(signed_embedding);
    chunk = chunk + (pixel_401 as Field) * factor;
    factor = factor * 256;
    let pixel_402 = predict_pixel_402(signed_embedding);
    chunk = chunk + (pixel_402 as Field) * factor;
    factor = factor * 256;
    let pixel_403 = predict_pixel_403(signed_embedding);
    chunk = chunk + (pixel_403 as Field) * factor;
    factor = factor * 256;
    let pixel_404 = predict_pixel_404(signed_embedding);
    chunk = chunk + (pixel_404 as Field) * factor;
    factor = factor * 256;
    let pixel_405 = predict_pixel_405(signed_embedding);
    chunk = chunk + (pixel_405 as Field) * factor;
    factor = factor * 256;
    let pixel_406 = predict_pixel_406(signed_embedding);
    chunk = chunk + (pixel_406 as Field) * factor;
    factor = factor * 256;
    let pixel_407 = predict_pixel_407(signed_embedding);
    chunk = chunk + (pixel_407 as Field) * factor;
    factor = factor * 256;
    let pixel_408 = predict_pixel_408(signed_embedding);
    chunk = chunk + (pixel_408 as Field) * factor;
    factor = factor * 256;
    let pixel_409 = predict_pixel_409(signed_embedding);
    chunk = chunk + (pixel_409 as Field) * factor;
    factor = factor * 256;
    let pixel_410 = predict_pixel_410(signed_embedding);
    chunk = chunk + (pixel_410 as Field) * factor;
    factor = factor * 256;
    let pixel_411 = predict_pixel_411(signed_embedding);
    chunk = chunk + (pixel_411 as Field) * factor;
    factor = factor * 256;
    let pixel_412 = predict_pixel_412(signed_embedding);
    chunk = chunk + (pixel_412 as Field) * factor;
    factor = factor * 256;
    let pixel_413 = predict_pixel_413(signed_embedding);
    chunk = chunk + (pixel_413 as Field) * factor;
    factor = factor * 256;
    let pixel_414 = predict_pixel_414(signed_embedding);
    chunk = chunk + (pixel_414 as Field) * factor;
    factor = factor * 256;
    let pixel_415 = predict_pixel_415(signed_embedding);
    chunk = chunk + (pixel_415 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[25]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_416 = predict_pixel_416(signed_embedding);
    chunk = chunk + (pixel_416 as Field) * factor;
    factor = factor * 256;
    let pixel_417 = predict_pixel_417(signed_embedding);
    chunk = chunk + (pixel_417 as Field) * factor;
    factor = factor * 256;
    let pixel_418 = predict_pixel_418(signed_embedding);
    chunk = chunk + (pixel_418 as Field) * factor;
    factor = factor * 256;
    let pixel_419 = predict_pixel_419(signed_embedding);
    chunk = chunk + (pixel_419 as Field) * factor;
    factor = factor * 256;
    let pixel_420 = predict_pixel_420(signed_embedding);
    chunk = chunk + (pixel_420 as Field) * factor;
    factor = factor * 256;
    let pixel_421 = predict_pixel_421(signed_embedding);
    chunk = chunk + (pixel_421 as Field) * factor;
    factor = factor * 256;
    let pixel_422 = predict_pixel_422(signed_embedding);
    chunk = chunk + (pixel_422 as Field) * factor;
    factor = factor * 256;
    let pixel_423 = predict_pixel_423(signed_embedding);
    chunk = chunk + (pixel_423 as Field) * factor;
    factor = factor * 256;
    let pixel_424 = predict_pixel_424(signed_embedding);
    chunk = chunk + (pixel_424 as Field) * factor;
    factor = factor * 256;
    let pixel_425 = predict_pixel_425(signed_embedding);
    chunk = chunk + (pixel_425 as Field) * factor;
    factor = factor * 256;
    let pixel_426 = predict_pixel_426(signed_embedding);
    chunk = chunk + (pixel_426 as Field) * factor;
    factor = factor * 256;
    let pixel_427 = predict_pixel_427(signed_embedding);
    chunk = chunk + (pixel_427 as Field) * factor;
    factor = factor * 256;
    let pixel_428 = predict_pixel_428(signed_embedding);
    chunk = chunk + (pixel_428 as Field) * factor;
    factor = factor * 256;
    let pixel_429 = predict_pixel_429(signed_embedding);
    chunk = chunk + (pixel_429 as Field) * factor;
    factor = factor * 256;
    let pixel_430 = predict_pixel_430(signed_embedding);
    chunk = chunk + (pixel_430 as Field) * factor;
    factor = factor * 256;
    let pixel_431 = predict_pixel_431(signed_embedding);
    chunk = chunk + (pixel_431 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[26]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_432 = predict_pixel_432(signed_embedding);
    chunk = chunk + (pixel_432 as Field) * factor;
    factor = factor * 256;
    let pixel_433 = predict_pixel_433(signed_embedding);
    chunk = chunk + (pixel_433 as Field) * factor;
    factor = factor * 256;
    let pixel_434 = predict_pixel_434(signed_embedding);
    chunk = chunk + (pixel_434 as Field) * factor;
    factor = factor * 256;
    let pixel_435 = predict_pixel_435(signed_embedding);
    chunk = chunk + (pixel_435 as Field) * factor;
    factor = factor * 256;
    let pixel_436 = predict_pixel_436(signed_embedding);
    chunk = chunk + (pixel_436 as Field) * factor;
    factor = factor * 256;
    let pixel_437 = predict_pixel_437(signed_embedding);
    chunk = chunk + (pixel_437 as Field) * factor;
    factor = factor * 256;
    let pixel_438 = predict_pixel_438(signed_embedding);
    chunk = chunk + (pixel_438 as Field) * factor;
    factor = factor * 256;
    let pixel_439 = predict_pixel_439(signed_embedding);
    chunk = chunk + (pixel_439 as Field) * factor;
    factor = factor * 256;
    let pixel_440 = predict_pixel_440(signed_embedding);
    chunk = chunk + (pixel_440 as Field) * factor;
    factor = factor * 256;
    let pixel_441 = predict_pixel_441(signed_embedding);
    chunk = chunk + (pixel_441 as Field) * factor;
    factor = factor * 256;
    let pixel_442 = predict_pixel_442(signed_embedding);
    chunk = chunk + (pixel_442 as Field) * factor;
    factor = factor * 256;
    let pixel_443 = predict_pixel_443(signed_embedding);
    chunk = chunk + (pixel_443 as Field) * factor;
    factor = factor * 256;
    let pixel_444 = predict_pixel_444(signed_embedding);
    chunk = chunk + (pixel_444 as Field) * factor;
    factor = factor * 256;
    let pixel_445 = predict_pixel_445(signed_embedding);
    chunk = chunk + (pixel_445 as Field) * factor;
    factor = factor * 256;
    let pixel_446 = predict_pixel_446(signed_embedding);
    chunk = chunk + (pixel_446 as Field) * factor;
    factor = factor * 256;
    let pixel_447 = predict_pixel_447(signed_embedding);
    chunk = chunk + (pixel_447 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[27]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_448 = predict_pixel_448(signed_embedding);
    chunk = chunk + (pixel_448 as Field) * factor;
    factor = factor * 256;
    let pixel_449 = predict_pixel_449(signed_embedding);
    chunk = chunk + (pixel_449 as Field) * factor;
    factor = factor * 256;
    let pixel_450 = predict_pixel_450(signed_embedding);
    chunk = chunk + (pixel_450 as Field) * factor;
    factor = factor * 256;
    let pixel_451 = predict_pixel_451(signed_embedding);
    chunk = chunk + (pixel_451 as Field) * factor;
    factor = factor * 256;
    let pixel_452 = predict_pixel_452(signed_embedding);
    chunk = chunk + (pixel_452 as Field) * factor;
    factor = factor * 256;
    let pixel_453 = predict_pixel_453(signed_embedding);
    chunk = chunk + (pixel_453 as Field) * factor;
    factor = factor * 256;
    let pixel_454 = predict_pixel_454(signed_embedding);
    chunk = chunk + (pixel_454 as Field) * factor;
    factor = factor * 256;
    let pixel_455 = predict_pixel_455(signed_embedding);
    chunk = chunk + (pixel_455 as Field) * factor;
    factor = factor * 256;
    let pixel_456 = predict_pixel_456(signed_embedding);
    chunk = chunk + (pixel_456 as Field) * factor;
    factor = factor * 256;
    let pixel_457 = predict_pixel_457(signed_embedding);
    chunk = chunk + (pixel_457 as Field) * factor;
    factor = factor * 256;
    let pixel_458 = predict_pixel_458(signed_embedding);
    chunk = chunk + (pixel_458 as Field) * factor;
    factor = factor * 256;
    let pixel_459 = predict_pixel_459(signed_embedding);
    chunk = chunk + (pixel_459 as Field) * factor;
    factor = factor * 256;
    let pixel_460 = predict_pixel_460(signed_embedding);
    chunk = chunk + (pixel_460 as Field) * factor;
    factor = factor * 256;
    let pixel_461 = predict_pixel_461(signed_embedding);
    chunk = chunk + (pixel_461 as Field) * factor;
    factor = factor * 256;
    let pixel_462 = predict_pixel_462(signed_embedding);
    chunk = chunk + (pixel_462 as Field) * factor;
    factor = factor * 256;
    let pixel_463 = predict_pixel_463(signed_embedding);
    chunk = chunk + (pixel_463 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[28]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_464 = predict_pixel_464(signed_embedding);
    chunk = chunk + (pixel_464 as Field) * factor;
    factor = factor * 256;
    let pixel_465 = predict_pixel_465(signed_embedding);
    chunk = chunk + (pixel_465 as Field) * factor;
    factor = factor * 256;
    let pixel_466 = predict_pixel_466(signed_embedding);
    chunk = chunk + (pixel_466 as Field) * factor;
    factor = factor * 256;
    let pixel_467 = predict_pixel_467(signed_embedding);
    chunk = chunk + (pixel_467 as Field) * factor;
    factor = factor * 256;
    let pixel_468 = predict_pixel_468(signed_embedding);
    chunk = chunk + (pixel_468 as Field) * factor;
    factor = factor * 256;
    let pixel_469 = predict_pixel_469(signed_embedding);
    chunk = chunk + (pixel_469 as Field) * factor;
    factor = factor * 256;
    let pixel_470 = predict_pixel_470(signed_embedding);
    chunk = chunk + (pixel_470 as Field) * factor;
    factor = factor * 256;
    let pixel_471 = predict_pixel_471(signed_embedding);
    chunk = chunk + (pixel_471 as Field) * factor;
    factor = factor * 256;
    let pixel_472 = predict_pixel_472(signed_embedding);
    chunk = chunk + (pixel_472 as Field) * factor;
    factor = factor * 256;
    let pixel_473 = predict_pixel_473(signed_embedding);
    chunk = chunk + (pixel_473 as Field) * factor;
    factor = factor * 256;
    let pixel_474 = predict_pixel_474(signed_embedding);
    chunk = chunk + (pixel_474 as Field) * factor;
    factor = factor * 256;
    let pixel_475 = predict_pixel_475(signed_embedding);
    chunk = chunk + (pixel_475 as Field) * factor;
    factor = factor * 256;
    let pixel_476 = predict_pixel_476(signed_embedding);
    chunk = chunk + (pixel_476 as Field) * factor;
    factor = factor * 256;
    let pixel_477 = predict_pixel_477(signed_embedding);
    chunk = chunk + (pixel_477 as Field) * factor;
    factor = factor * 256;
    let pixel_478 = predict_pixel_478(signed_embedding);
    chunk = chunk + (pixel_478 as Field) * factor;
    factor = factor * 256;
    let pixel_479 = predict_pixel_479(signed_embedding);
    chunk = chunk + (pixel_479 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[29]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_480 = predict_pixel_480(signed_embedding);
    chunk = chunk + (pixel_480 as Field) * factor;
    factor = factor * 256;
    let pixel_481 = predict_pixel_481(signed_embedding);
    chunk = chunk + (pixel_481 as Field) * factor;
    factor = factor * 256;
    let pixel_482 = predict_pixel_482(signed_embedding);
    chunk = chunk + (pixel_482 as Field) * factor;
    factor = factor * 256;
    let pixel_483 = predict_pixel_483(signed_embedding);
    chunk = chunk + (pixel_483 as Field) * factor;
    factor = factor * 256;
    let pixel_484 = predict_pixel_484(signed_embedding);
    chunk = chunk + (pixel_484 as Field) * factor;
    factor = factor * 256;
    let pixel_485 = predict_pixel_485(signed_embedding);
    chunk = chunk + (pixel_485 as Field) * factor;
    factor = factor * 256;
    let pixel_486 = predict_pixel_486(signed_embedding);
    chunk = chunk + (pixel_486 as Field) * factor;
    factor = factor * 256;
    let pixel_487 = predict_pixel_487(signed_embedding);
    chunk = chunk + (pixel_487 as Field) * factor;
    factor = factor * 256;
    let pixel_488 = predict_pixel_488(signed_embedding);
    chunk = chunk + (pixel_488 as Field) * factor;
    factor = factor * 256;
    let pixel_489 = predict_pixel_489(signed_embedding);
    chunk = chunk + (pixel_489 as Field) * factor;
    factor = factor * 256;
    let pixel_490 = predict_pixel_490(signed_embedding);
    chunk = chunk + (pixel_490 as Field) * factor;
    factor = factor * 256;
    let pixel_491 = predict_pixel_491(signed_embedding);
    chunk = chunk + (pixel_491 as Field) * factor;
    factor = factor * 256;
    let pixel_492 = predict_pixel_492(signed_embedding);
    chunk = chunk + (pixel_492 as Field) * factor;
    factor = factor * 256;
    let pixel_493 = predict_pixel_493(signed_embedding);
    chunk = chunk + (pixel_493 as Field) * factor;
    factor = factor * 256;
    let pixel_494 = predict_pixel_494(signed_embedding);
    chunk = chunk + (pixel_494 as Field) * factor;
    factor = factor * 256;
    let pixel_495 = predict_pixel_495(signed_embedding);
    chunk = chunk + (pixel_495 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[30]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_496 = predict_pixel_496(signed_embedding);
    chunk = chunk + (pixel_496 as Field) * factor;
    factor = factor * 256;
    let pixel_497 = predict_pixel_497(signed_embedding);
    chunk = chunk + (pixel_497 as Field) * factor;
    factor = factor * 256;
    let pixel_498 = predict_pixel_498(signed_embedding);
    chunk = chunk + (pixel_498 as Field) * factor;
    factor = factor * 256;
    let pixel_499 = predict_pixel_499(signed_embedding);
    chunk = chunk + (pixel_499 as Field) * factor;
    factor = factor * 256;
    let pixel_500 = predict_pixel_500(signed_embedding);
    chunk = chunk + (pixel_500 as Field) * factor;
    factor = factor * 256;
    let pixel_501 = predict_pixel_501(signed_embedding);
    chunk = chunk + (pixel_501 as Field) * factor;
    factor = factor * 256;
    let pixel_502 = predict_pixel_502(signed_embedding);
    chunk = chunk + (pixel_502 as Field) * factor;
    factor = factor * 256;
    let pixel_503 = predict_pixel_503(signed_embedding);
    chunk = chunk + (pixel_503 as Field) * factor;
    factor = factor * 256;
    let pixel_504 = predict_pixel_504(signed_embedding);
    chunk = chunk + (pixel_504 as Field) * factor;
    factor = factor * 256;
    let pixel_505 = predict_pixel_505(signed_embedding);
    chunk = chunk + (pixel_505 as Field) * factor;
    factor = factor * 256;
    let pixel_506 = predict_pixel_506(signed_embedding);
    chunk = chunk + (pixel_506 as Field) * factor;
    factor = factor * 256;
    let pixel_507 = predict_pixel_507(signed_embedding);
    chunk = chunk + (pixel_507 as Field) * factor;
    factor = factor * 256;
    let pixel_508 = predict_pixel_508(signed_embedding);
    chunk = chunk + (pixel_508 as Field) * factor;
    factor = factor * 256;
    let pixel_509 = predict_pixel_509(signed_embedding);
    chunk = chunk + (pixel_509 as Field) * factor;
    factor = factor * 256;
    let pixel_510 = predict_pixel_510(signed_embedding);
    chunk = chunk + (pixel_510 as Field) * factor;
    factor = factor * 256;
    let pixel_511 = predict_pixel_511(signed_embedding);
    chunk = chunk + (pixel_511 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[31]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_512 = predict_pixel_512(signed_embedding);
    chunk = chunk + (pixel_512 as Field) * factor;
    factor = factor * 256;
    let pixel_513 = predict_pixel_513(signed_embedding);
    chunk = chunk + (pixel_513 as Field) * factor;
    factor = factor * 256;
    let pixel_514 = predict_pixel_514(signed_embedding);
    chunk = chunk + (pixel_514 as Field) * factor;
    factor = factor * 256;
    let pixel_515 = predict_pixel_515(signed_embedding);
    chunk = chunk + (pixel_515 as Field) * factor;
    factor = factor * 256;
    let pixel_516 = predict_pixel_516(signed_embedding);
    chunk = chunk + (pixel_516 as Field) * factor;
    factor = factor * 256;
    let pixel_517 = predict_pixel_517(signed_embedding);
    chunk = chunk + (pixel_517 as Field) * factor;
    factor = factor * 256;
    let pixel_518 = predict_pixel_518(signed_embedding);
    chunk = chunk + (pixel_518 as Field) * factor;
    factor = factor * 256;
    let pixel_519 = predict_pixel_519(signed_embedding);
    chunk = chunk + (pixel_519 as Field) * factor;
    factor = factor * 256;
    let pixel_520 = predict_pixel_520(signed_embedding);
    chunk = chunk + (pixel_520 as Field) * factor;
    factor = factor * 256;
    let pixel_521 = predict_pixel_521(signed_embedding);
    chunk = chunk + (pixel_521 as Field) * factor;
    factor = factor * 256;
    let pixel_522 = predict_pixel_522(signed_embedding);
    chunk = chunk + (pixel_522 as Field) * factor;
    factor = factor * 256;
    let pixel_523 = predict_pixel_523(signed_embedding);
    chunk = chunk + (pixel_523 as Field) * factor;
    factor = factor * 256;
    let pixel_524 = predict_pixel_524(signed_embedding);
    chunk = chunk + (pixel_524 as Field) * factor;
    factor = factor * 256;
    let pixel_525 = predict_pixel_525(signed_embedding);
    chunk = chunk + (pixel_525 as Field) * factor;
    factor = factor * 256;
    let pixel_526 = predict_pixel_526(signed_embedding);
    chunk = chunk + (pixel_526 as Field) * factor;
    factor = factor * 256;
    let pixel_527 = predict_pixel_527(signed_embedding);
    chunk = chunk + (pixel_527 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[32]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_528 = predict_pixel_528(signed_embedding);
    chunk = chunk + (pixel_528 as Field) * factor;
    factor = factor * 256;
    let pixel_529 = predict_pixel_529(signed_embedding);
    chunk = chunk + (pixel_529 as Field) * factor;
    factor = factor * 256;
    let pixel_530 = predict_pixel_530(signed_embedding);
    chunk = chunk + (pixel_530 as Field) * factor;
    factor = factor * 256;
    let pixel_531 = predict_pixel_531(signed_embedding);
    chunk = chunk + (pixel_531 as Field) * factor;
    factor = factor * 256;
    let pixel_532 = predict_pixel_532(signed_embedding);
    chunk = chunk + (pixel_532 as Field) * factor;
    factor = factor * 256;
    let pixel_533 = predict_pixel_533(signed_embedding);
    chunk = chunk + (pixel_533 as Field) * factor;
    factor = factor * 256;
    let pixel_534 = predict_pixel_534(signed_embedding);
    chunk = chunk + (pixel_534 as Field) * factor;
    factor = factor * 256;
    let pixel_535 = predict_pixel_535(signed_embedding);
    chunk = chunk + (pixel_535 as Field) * factor;
    factor = factor * 256;
    let pixel_536 = predict_pixel_536(signed_embedding);
    chunk = chunk + (pixel_536 as Field) * factor;
    factor = factor * 256;
    let pixel_537 = predict_pixel_537(signed_embedding);
    chunk = chunk + (pixel_537 as Field) * factor;
    factor = factor * 256;
    let pixel_538 = predict_pixel_538(signed_embedding);
    chunk = chunk + (pixel_538 as Field) * factor;
    factor = factor * 256;
    let pixel_539 = predict_pixel_539(signed_embedding);
    chunk = chunk + (pixel_539 as Field) * factor;
    factor = factor * 256;
    let pixel_540 = predict_pixel_540(signed_embedding);
    chunk = chunk + (pixel_540 as Field) * factor;
    factor = factor * 256;
    let pixel_541 = predict_pixel_541(signed_embedding);
    chunk = chunk + (pixel_541 as Field) * factor;
    factor = factor * 256;
    let pixel_542 = predict_pixel_542(signed_embedding);
    chunk = chunk + (pixel_542 as Field) * factor;
    factor = factor * 256;
    let pixel_543 = predict_pixel_543(signed_embedding);
    chunk = chunk + (pixel_543 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[33]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_544 = predict_pixel_544(signed_embedding);
    chunk = chunk + (pixel_544 as Field) * factor;
    factor = factor * 256;
    let pixel_545 = predict_pixel_545(signed_embedding);
    chunk = chunk + (pixel_545 as Field) * factor;
    factor = factor * 256;
    let pixel_546 = predict_pixel_546(signed_embedding);
    chunk = chunk + (pixel_546 as Field) * factor;
    factor = factor * 256;
    let pixel_547 = predict_pixel_547(signed_embedding);
    chunk = chunk + (pixel_547 as Field) * factor;
    factor = factor * 256;
    let pixel_548 = predict_pixel_548(signed_embedding);
    chunk = chunk + (pixel_548 as Field) * factor;
    factor = factor * 256;
    let pixel_549 = predict_pixel_549(signed_embedding);
    chunk = chunk + (pixel_549 as Field) * factor;
    factor = factor * 256;
    let pixel_550 = predict_pixel_550(signed_embedding);
    chunk = chunk + (pixel_550 as Field) * factor;
    factor = factor * 256;
    let pixel_551 = predict_pixel_551(signed_embedding);
    chunk = chunk + (pixel_551 as Field) * factor;
    factor = factor * 256;
    let pixel_552 = predict_pixel_552(signed_embedding);
    chunk = chunk + (pixel_552 as Field) * factor;
    factor = factor * 256;
    let pixel_553 = predict_pixel_553(signed_embedding);
    chunk = chunk + (pixel_553 as Field) * factor;
    factor = factor * 256;
    let pixel_554 = predict_pixel_554(signed_embedding);
    chunk = chunk + (pixel_554 as Field) * factor;
    factor = factor * 256;
    let pixel_555 = predict_pixel_555(signed_embedding);
    chunk = chunk + (pixel_555 as Field) * factor;
    factor = factor * 256;
    let pixel_556 = predict_pixel_556(signed_embedding);
    chunk = chunk + (pixel_556 as Field) * factor;
    factor = factor * 256;
    let pixel_557 = predict_pixel_557(signed_embedding);
    chunk = chunk + (pixel_557 as Field) * factor;
    factor = factor * 256;
    let pixel_558 = predict_pixel_558(signed_embedding);
    chunk = chunk + (pixel_558 as Field) * factor;
    factor = factor * 256;
    let pixel_559 = predict_pixel_559(signed_embedding);
    chunk = chunk + (pixel_559 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[34]);

    let mut chunk = 0;
    let mut factor = 1;
    let pixel_560 = predict_pixel_560(signed_embedding);
    chunk = chunk + (pixel_560 as Field) * factor;
    factor = factor * 256;
    let pixel_561 = predict_pixel_561(signed_embedding);
    chunk = chunk + (pixel_561 as Field) * factor;
    factor = factor * 256;
    let pixel_562 = predict_pixel_562(signed_embedding);
    chunk = chunk + (pixel_562 as Field) * factor;
    factor = factor * 256;
    let pixel_563 = predict_pixel_563(signed_embedding);
    chunk = chunk + (pixel_563 as Field) * factor;
    factor = factor * 256;
    let pixel_564 = predict_pixel_564(signed_embedding);
    chunk = chunk + (pixel_564 as Field) * factor;
    factor = factor * 256;
    let pixel_565 = predict_pixel_565(signed_embedding);
    chunk = chunk + (pixel_565 as Field) * factor;
    factor = factor * 256;
    let pixel_566 = predict_pixel_566(signed_embedding);
    chunk = chunk + (pixel_566 as Field) * factor;
    factor = factor * 256;
    let pixel_567 = predict_pixel_567(signed_embedding);
    chunk = chunk + (pixel_567 as Field) * factor;
    factor = factor * 256;
    let pixel_568 = predict_pixel_568(signed_embedding);
    chunk = chunk + (pixel_568 as Field) * factor;
    factor = factor * 256;
    let pixel_569 = predict_pixel_569(signed_embedding);
    chunk = chunk + (pixel_569 as Field) * factor;
    factor = factor * 256;
    let pixel_570 = predict_pixel_570(signed_embedding);
    chunk = chunk + (pixel_570 as Field) * factor;
    factor = factor * 256;
    let pixel_571 = predict_pixel_571(signed_embedding);
    chunk = chunk + (pixel_571 as Field) * factor;
    factor = factor * 256;
    let pixel_572 = predict_pixel_572(signed_embedding);
    chunk = chunk + (pixel_572 as Field) * factor;
    factor = factor * 256;
    let pixel_573 = predict_pixel_573(signed_embedding);
    chunk = chunk + (pixel_573 as Field) * factor;
    factor = factor * 256;
    let pixel_574 = predict_pixel_574(signed_embedding);
    chunk = chunk + (pixel_574 as Field) * factor;
    factor = factor * 256;
    let pixel_575 = predict_pixel_575(signed_embedding);
    chunk = chunk + (pixel_575 as Field) * factor;
    factor = factor * 256;
    assert(chunk == packed_pixels[35]);
}

#[test]
fn proves_real_slonks_model_output_for_source_9909() {
    let embedding = [32, 240, 231, 15, 30, 251, 244, 230, 28, 29];
    let packed_embedding_input = 137481779039913153916960;
    let packed_pixels = [
        0, 0,
        0, 0,
        0, 0,
        52126588069996700174786783787089920, 13292279957849158729038070602803445760,
        723498485404011018, 13801328669470220261246931753708290048,
        13344202926434507005323375566095646720, 12136667051817453675481610,
        13344406545919155429936248076736921600, 2663648288428366573436144617058339338,
        18591424419387081218, 30692135055614057488847178127007285248,
        30577436199911599904416092882777145345, 18519108530661818647,
        1334888617431441817047800456557887488, 30691687013208969763914187749271273473,
        18542752428705535255, 30692135055614057488847179226518913024,
        30577436199911599904416092882777145345, 20110543765240223511,
        30692135055614057488847178127007285248, 30577436199911599904416092882777145345,
        20110568048985315095, 30691687092774531622212832717566902272,
        30577436199911599904416092882777145345, 20110568050495264535,
        1449119148345908284963422800839704576, 30577436199911599904416092882777145344,
        282578800154391, 1317966181440464183361536,
        30577436199911599904416092882777145344, 71447
    ];
    main(embedding, packed_embedding_input, packed_pixels);
}
